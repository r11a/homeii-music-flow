name: Publish Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  validate:
    uses: ./.github/workflows/validate.yml
  publish:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v4

      - name: Verify tag matches the package version
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: node --input-type=module -e 'import fs from "node:fs"; const version=JSON.parse(fs.readFileSync("package.json","utf8")).version; if(process.env.TAG_NAME !== `v${version}`) throw new Error("Release tag must match package.json");'

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ github.ref_name }}
        shell: bash
        run: |
          set -euo pipefail
          version="${TAG_NAME#v}"
          title="HOMEii Music Flow ${version}"
          notes_file="RELEASE_NOTES_${version}.md"
          is_prerelease=false
          if [[ "$version" == *-* ]]; then
            is_prerelease=true
          fi
          if [ ! -f "$notes_file" ]; then
            echo "Missing release notes file: $notes_file" >&2
            exit 1
          fi
          if gh release view "$TAG_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            if [ "$is_prerelease" = true ]; then
              gh release edit "$TAG_NAME" \
                --repo "$GITHUB_REPOSITORY" \
                --title "$title" \
                --notes-file "$notes_file" \
                --prerelease
            else
              gh release edit "$TAG_NAME" \
                --repo "$GITHUB_REPOSITORY" \
                --title "$title" \
                --notes-file "$notes_file" \
                --latest
            fi
          else
            if [ "$is_prerelease" = true ]; then
              gh release create "$TAG_NAME" \
                --repo "$GITHUB_REPOSITORY" \
                --title "$title" \
                --notes-file "$notes_file" \
                --prerelease \
                --latest=false
            else
              gh release create "$TAG_NAME" \
                --repo "$GITHUB_REPOSITORY" \
                --title "$title" \
                --notes-file "$notes_file" \
                --latest
            fi
          fi
