const messages$7 = Object.freeze({
  "ui.a_guided_music_wizard": "A guided music wizard",
  "ui.a_radio_station": "A radio station",
  "ui.accent_color": "Accent color",
  "ui.actions": "actions",
  "ui.actions_2": "Actions",
  "ui.active_days": "Active days",
  "ui.active_player_helper": "Active player helper",
  "ui.active_player_helper_must_be_an_input_text_entity": "Active player helper must be an input_text entity",
  "ui.active_players": "Active Players",
  "ui.add": "Add",
  "ui.add_to_queue": "Add to queue",
  "ui.add_to_selection": "Add to selection",
  "ui.added_to_liked": "Added to liked",
  "ui.added_to_music_assistant_liked": "Added to Music Assistant liked",
  "ui.added_to_queue": "Added to queue",
  "ui.added_to_studio_queue": "Added to Studio queue",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Adds motion to the background layers. Subtle keeps it calm, Strong is noticeable, and Extreme is the boldest animated look.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Adds to the current volume only during announcements, then restores the previous volume.",
  "ui.after_run": "After run",
  "ui.album": "Album",
  "ui.albums": "Albums",
  "ui.all_albums": "All Albums",
  "ui.all_artists": "All Artists",
  "ui.all_countries": "All countries",
  "ui.all_player_groups_disconnected": "All player groups disconnected",
  "ui.all_players": "All players",
  "ui.all_players_2": "all players",
  "ui.all_podcasts": "All Podcasts",
  "ui.all_tracks": "All Tracks",
  "ui.allow_local_likes": "Allow local likes",
  "ui.always_visible": "Always visible",
  "ui.announce": "Announce",
  "ui.announce_to_all_players": "Announce to all players",
  "ui.announcement": "Announcement",
  "ui.announcement_failed_with_error": "Announcement failed{error}",
  "ui.announcement_language": "Announcement language",
  "ui.announcement_presets": "Announcement presets",
  "ui.announcement_sent_to_player": "Announcement sent to {player}",
  "ui.announcement_studio": "announcement studio",
  "ui.announcement_studio_2": "Announcement Studio",
  "ui.announcement_target": "Announcement target",
  "ui.announcement_to_player_preview": "Announcement to {player}: {preview}",
  "ui.announcement_volume_boost": "Announcement volume boost",
  "ui.announcements": "Announcements",
  "ui.appearance": "Appearance",
  "ui.alphabetical": "Alphabetical",
  "ui.auxiliary_button": "Auxiliary button",
  "ui.auxiliary_button_action": "Auxiliary button action",
  "ui.auxiliary_button_entity": "Auxiliary button entity",
  "ui.auxiliary_button_icon": "Auxiliary button icon",
  "ui.auxiliary_button_name": "Auxiliary button name",
  "ui.auxiliary_buttons": "Auxiliary buttons",
  "ui.apply_group": "Apply Group",
  "ui.apply_schedule": "Apply schedule",
  "ui.artist": "Artist",
  "ui.artist_radio": "Artist radio",
  "ui.artists": "Artists",
  "ui.artwork": "Artwork",
  "ui.artwork_swipe": "Artwork swipe",
  "ui.ascending": "Ascending",
  "ui.at_least_one_player_must_stay_selected": "At least one player must stay selected",
  "ui.australia": "Australia",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Auto chooses mobile or tablet based on actual width.",
  "ui.auto_cloud_default": "Auto / Cloud default",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Auto extracts colors from the current artwork and keeps the effect subtle. Strong makes the palette richer and brighter.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Auto leaves Home Assistant Cloud voice defaults untouched. Manual choices send an explicit language.",
  "ui.available": "Available",
  "ui.back": "Back",
  "ui.back_2": "Back",
  "ui.back_to_countries": "Back to countries",
  "ui.back_to_queue": "Back to queue",
  "ui.background_motion": "Background motion",
  "ui.behavior": "Behavior",
  "ui.browse_covers": "Browse covers",
  "ui.browse_playlists_artists_albums_and_radio": "Browse playlists, artists, albums and radio",
  "ui.browser_players": "Browser Players",
  "ui.build_custom_mix": "Build custom mix",
  "ui.building_studio_mix": "Building Studio mix...",
  "ui.button": "Button",
  "ui.by_mood": "By mood",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Cache duration in milliseconds for selected data requests.",
  "ui.cache_ttl": "Cache TTL",
  "ui.calm": "Calm",
  "ui.canada": "Canada",
  "ui.cancel": "Cancel",
  "ui.cancel_2": "Cancel",
  "ui.cancel_selection": "Cancel selection",
  "ui.card_configuration": "Card configuration",
  "ui.card_height": "Card height",
  "ui.card_height_in_pixels": "Card height in pixels.",
  "ui.card_settings_are_managed_from_the_visual_editor": "Card settings are managed from the visual editor",
  "ui.change_song": "Change song",
  "ui.change_track": "Change track",
  "ui.chill_mix": "Chill mix",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Choose a country, or All countries to browse every country inside the Radio tab.",
  "ui.choose_a_queue_position": "Choose a queue position",
  "ui.choose_a_source_player": "Choose a source player",
  "ui.choose_a_target_player": "Choose a target player",
  "ui.choose_at_least_one_player": "Choose at least one player",
  "ui.choose_icons_text_or_both_for_the_footer": "Choose icons, text, or both for the footer.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Choose Music Assistant players only. The in-card pinning list shows only Music Assistant players to avoid confusion.",
  "ui.choose_one_clear_option": "Choose one clear option",
  "ui.choose_player": "Choose Player",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Choose something from the quick shelf or tap the wand for a random playlist.",
  "ui.choose_source": "Choose source",
  "ui.choose_target": "Choose target",
  "ui.choose_target_player": "Choose target player",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Choose the accent color for the mobile layout.",
  "ui.choose_the_music": "Choose the music",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Choose and order the small buttons shown under the song title.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Choose the source first. Playback will use the current target.",
  "ui.choose_where_to_play": "Choose where to play",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Choose whether artwork swipe changes track or browses covers.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Choose whether settings are controlled from the in-card UI or from the card configuration in the dashboard.",
  "ui.choose_which_actions_appear_in_the_main_bar": "Choose which actions appear in the main bar.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Choose which days the Auto night-mode window applies to.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Choose which icons appear in the quick action row. Clean all asks for confirmation before disconnecting active players.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Choose which players appear as tiles in the room.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Choose which players stay in the current control selection.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Choose which tabs are available in the library screen.",
  "ui.choose_who_is_controlled": "Choose who is controlled",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "Choosing Visual disables the in-card settings screen.",
  "ui.clean_all": "Clean all",
  "ui.clean_all_confirm_action": "Clean all",
  "ui.clean_all_confirm_copy": "This will disconnect all active players, stop playback and clear their queues. Continue?",
  "ui.clean_all_confirm_title": "Clean all players?",
  "ui.clean_the_studio_wall": "Clean the Studio wall",
  "ui.clear": "Clear",
  "ui.clear_queue": "Clear queue",
  "ui.clear_search": "Clear search",
  "ui.clone_queue": "Clone queue",
  "ui.close": "Close",
  "ui.collapse_compact_player": "Collapse compact player",
  "ui.compact_mode": "Compact mode",
  "ui.configure_ready_made_announcement_phrases": "Configure ready-made announcement phrases.",
  "ui.connect_this_device": "Connect This Device",
  "ui.connect_this_device_2": "Connect this device",
  "ui.connected": "Connected",
  "ui.connected_2": "Connected",
  "ui.connected_players": "connected players",
  "ui.connected_players_2": "Connected players",
  "ui.connecting": "Connecting",
  "ui.connecting_2": "Connecting...",
  "ui.confirm": "Confirm",
  "ui.connection_and_behavior": "Connection and Behavior",
  "ui.connects_directly_from_this_card_using_sendspin": "Connects directly from this card using Sendspin",
  "ui.continue": "Continue",
  "ui.continue_2": "Continue",
  "ui.continue_from_recent_music_assistant_activity": "Continue from recent Music Assistant activity.",
  "ui.continue_listening": "Continue listening",
  "ui.continue_what_was_played_recently": "Continue what was played recently",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Control whether the card background moves gently, and how strong the motion feels.",
  "ui.controlling": "Controlling",
  "ui.could_not_build_quick_mix_from_the_current_song": "Could not build Quick Mix from the current song",
  "ui.could_not_build_studio_mix": "Could not build Studio mix",
  "ui.could_not_choose_media": "Could not choose media",
  "ui.could_not_clone_the_queue": "Could not clone the queue",
  "ui.could_not_find_music": "Could not find music",
  "ui.could_not_play_label": "Could not play: {label}",
  "ui.could_not_start_chill_mix": "Could not start chill mix",
  "ui.could_not_start_playback": "Could not start playback",
  "ui.could_not_start_playback_2": "Could not start playback",
  "ui.could_not_start_playback_in_studio": "Could not start playback in Studio",
  "ui.could_not_start_quick_mix": "Could not start Quick Mix",
  "ui.could_not_start_radio_playback": "Could not start radio playback",
  "ui.could_not_transfer_the_queue": "Could not transfer the queue",
  "ui.could_not_update_active_player_helper": "Could not update active player helper",
  "ui.create_schedule": "Create schedule",
  "ui.current_path": "Current path",
  "ui.custom": "Custom",
  "ui.dark": "Dark",
  "ui.delete_scene": "Delete scene",
  "ui.delete_schedule": "Delete schedule",
  "ui.descending": "Descending",
  "ui.dictate": "Dictate",
  "ui.disabled": "Disabled",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Disables blur, animations, dynamic backgrounds, and heavy shadows. Recommended for Nest Hub and weaker tablets.",
  "ui.disconnect": "Disconnect",
  "ui.disconnect_all": "Disconnect all",
  "ui.disconnect_groups": "Disconnect groups",
  "ui.disconnect_player_groups": "Disconnect player groups",
  "ui.disconnect_this_device": "Disconnect This Device",
  "ui.disconnected": "Disconnected",
  "ui.disconnecting": "Disconnecting",
  "ui.disconnecting_player_groups": "Disconnecting player groups...",
  "ui.discover": "Discover",
  "ui.down": "Down",
  "ui.dynamic_theme": "Dynamic theme",
  "ui.edit_schedule": "Edit schedule",
  "ui.enable": "Enable",
  "ui.enable_or_disable_the_footer_search_button": "Enable or disable the footer search button.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Enable or disable the Studio button in the footer bar.",
  "ui.enable_the_floating_home_shortcut_button": "Enable the Home shortcut in Quick Actions.",
  "ui.enabled": "Enabled",
  "ui.enables_switching_to_local_liked_mode": "Enables switching to local liked mode.",
  "ui.end_time": "End time",
  "ui.excluded_players": "Excluded players",
  "ui.energetic": "Energetic",
  "ui.energy_and_rhythm": "Energy and rhythm",
  "ui.enter_an_announcement_first": "Enter an announcement first",
  "ui.example_quiet_jazz_greek_music_workout": "Example: quiet jazz, Greek music, workout",
  "ui.existing_content": "Existing content",
  "ui.expand_player": "Expand player",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Extract colors from the current artwork and apply them to the interface.",
  "ui.extreme": "Extreme",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Fallback for setups that need an alternate queue service path.",
  "ui.fallback_mode": "Fallback mode",
  "ui.family_friendly": "Family friendly",
  "ui.favorite": "Favorite",
  "ui.favorite_action_failed": "Favorite action failed",
  "ui.favorite_action_sent": "Favorite action sent",
  "ui.favorite_button_entity": "Favorite button entity",
  "ui.favorite_center": "favorite center",
  "ui.favorite_center_2": "Favorite Center",
  "ui.favorite_radio": "Favorite Radio",
  "ui.favorite_updated": "Favorite updated",
  "ui.favorites_and_liked_music": "Favorites and liked music",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Feature detection, player context and this-device playback tools.",
  "ui.find_music": "Find music",
  "ui.finding_music": "Finding music",
  "ui.fixed": "Fixed",
  "ui.flow_mix": "FLOW Mix",
  "ui.font_scale": "Font scale",
  "ui.font_size": "Font size",
  "ui.footer_search": "Footer search",
  "ui.footer_style": "Footer style",
  "ui.footer_style_2": "Footer style",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "For example /lovelace/home or any other Home Assistant path.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "For weaker displays: turns off heavy blur, animations, dynamic theme extraction, and background motion.",
  "ui.france": "France",
  "ui.free_style": "Free style",
  "ui.free_style_quiet_jazz_greek_music_workout": "Free style: quiet jazz, Greek music, workout...",
  "ui.fresh_start": "Fresh start",
  "ui.fresh_start_2": "Fresh start",
  "ui.fri": "Fri",
  "ui.friendly_and_simple": "Friendly and simple",
  "ui.from": "From",
  "ui.general": "General",
  "ui.general_and_display": "General and Display",
  "ui.germany": "Germany",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Global scale for every interface font. 1 is the default size.",
  "ui.greece": "Greece",
  "ui.grid": "Grid",
  "ui.group": "Group",
  "ui.group_cleared": "Group cleared",
  "ui.group_cleared_2": "Group cleared",
  "ui.group_speakers": "Group Speakers",
  "ui.group_speakers_2": "Group Speakers",
  "ui.group_updated": "Group updated",
  "ui.group_volume": "Group volume",
  "ui.group_volume_up_energetic_mix": "Group, volume up, energetic mix",
  "ui.grouped_players": "Grouped players",
  "ui.hebrew": "Hebrew",
  "ui.history": "History",
  "ui.history_tabs": "History tabs",
  "ui.home": "Home",
  "ui.home_party_night_presets": "Home, party, night presets",
  "ui.home_path": "Home path",
  "ui.home_scene_prepared": "Home scene prepared",
  "ui.home_shortcut": "Home shortcut",
  "ui.home_shortcut_path": "Home shortcut path",
  "ui.icon_and_text": "Icon and text",
  "ui.icon_only": "Icon only",
  "ui.icon_plus_text": "Icon + text",
  "ui.idle": "Idle",
  "ui.idle_2": "Idle",
  "ui.in_card_ui": "In-card UI",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Includes the custom theme mode from the in-card settings screen.",
  "ui.info": "Info",
  "ui.israel": "Israel",
  "ui.israeli": "Israeli",
  "ui.italy": "Italy",
  "ui.items": "items",
  "ui.join_selected_players": "Join selected players",
  "ui.kids": "Kids",
  "ui.language": "Language",
  "ui.larger_lyrics": "Larger lyrics",
  "ui.layout_adapted_to_height": "Layout adapted to height",
  "ui.layout_mode": "Layout mode",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Leave empty if the card should use Home Assistant only.",
  "ui.library": "Library",
  "ui.library_2": "Library",
  "ui.library_pages": "Library pages",
  "ui.default_library_layout": "Default library layout",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Choose how library pages open. You can still switch Grid/List inside the library.",
  "ui.library_radio": "Library radio",
  "ui.library_surprise": "Library surprise",
  "ui.library_tabs": "Library Tabs",
  "ui.library_tabs_2": "Library tabs",
  "ui.light": "Light",
  "ui.like": "Like",
  "ui.like_2": "Like",
  "ui.like_3": "Like",
  "ui.liked": "Liked",
  "ui.liked_mode": "Liked mode",
  "ui.liked_sync": "Liked sync",
  "ui.list": "List",
  "ui.listening": "Listening...",
  "ui.loading": "Loading...",
  "ui.loading_favorites": "Loading favorites...",
  "ui.loading_library": "Loading library...",
  "ui.loading_lyrics": "Loading lyrics...",
  "ui.loading_players": "Loading players...",
  "ui.loading_queue": "Loading queue...",
  "ui.loading_recent_items": "Loading recent items...",
  "ui.loading_schedules": "Loading schedules...",
  "ui.local": "Local",
  "ui.local_favorites": "Local favorites",
  "ui.local_favorites_2": "Local favorites",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "Local mode is only meaningful when local likes are enabled.",
  "ui.low_light_listening": "Low light listening",
  "ui.low_volume_and_quiet_mix": "Low volume and quiet mix",
  "ui.lower_volume_mood": "Lower volume mood",
  "ui.lyrics": "Lyrics",
  "ui.lyrics_earlier": "Lyrics earlier",
  "ui.lyrics_font_size": "Lyrics font size",
  "ui.lyrics_later": "Lyrics later",
  "ui.lyrics_timing": "Lyrics timing",
  "ui.lyrics_unavailable_right_now": "Lyrics unavailable right now",
  "ui.ma_favorite_action_failed_with_error": "Music Assistant favorite action failed{error}",
  "ui.ma_interface_path": "MA interface path",
  "ui.main_bar": "Main Bar",
  "ui.main_bar_items": "Main bar items",
  "ui.main_bar_items_2": "Main bar items",
  "ui.main_opacity": "Main opacity",
  "ui.mainly_relevant_on_larger_layouts": "Mainly relevant on larger layouts.",
  "ui.mass_queue_send_command_fallback": "mass_queue/send_command fallback",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Matches the microphone setting from the in-card settings screen.",
  "ui.media": "Media",
  "ui.media_2": "Media",
  "ui.media_actions": "Media actions",
  "ui.media_layout": "Media layout",
  "ui.media_selected_target": "Selected: {media} ? Target: {target}",
  "ui.menu": "Menu",
  "ui.message": "Message",
  "ui.microphone": "Microphone",
  "ui.microphone_2": "Microphone",
  "ui.microphone_is_disabled": "Microphone is disabled",
  "ui.missing_url": "Missing URL",
  "ui.mobile": "Mobile",
  "ui.mon": "Mon",
  "ui.mood_style_or_free_text": "Mood, style or free text",
  "ui.more_info": "More info",
  "ui.more_info_is_unavailable_for_this_player": "More info is unavailable for this player",
  "ui.morning": "Morning",
  "ui.morning_mix": "Morning mix",
  "ui.move_to_next": "Move to next",
  "ui.move_to_position": "Move to position",
  "ui.music": "Music",
  "ui.music_assistant_required": "Music Assistant required",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "Music Assistant config entry id, if you want direct integration lookup through Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "Music Assistant config entry was not found",
  "ui.music_assistant_token": "Music Assistant token",
  "ui.music_assistant_url": "Music Assistant URL",
  "ui.music_hub": "music hub",
  "ui.music_hub_2": "Music Hub",
  "ui.mute": "Mute",
  "ui.mute_command_failed": "Mute command failed",
  "ui.my_studio_scene": "My Studio Scene",
  "ui.name_or_keyword": "Name or keyword",
  "ui.name_this_studio_scene": "Name this Studio scene...",
  "ui.netherlands": "Netherlands",
  "ui.new_schedule": "New schedule",
  "ui.new_wake_schedule": "New wake schedule",
  "ui.newest": "Newest",
  "ui.next": "Next",
  "ui.next_track_failed": "Next track failed",
  "ui.night": "Night",
  "ui.night_end_time": "Night end time",
  "ui.night_mode": "Night mode",
  "ui.night_mode_auto_window": "Night mode auto {start}-{end}",
  "ui.night_mode_days": "Night mode days",
  "ui.night_mode_is_always_on": "Night mode is always on",
  "ui.night_mode_is_off": "Night mode is off",
  "ui.night_mode_is_off_until_you_choose_another_mode": "Night mode is off until you choose another mode.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "Night mode stays on until you choose another mode.",
  "ui.night_schedule_updated": "Night schedule updated",
  "ui.night_start_time": "Night start time",
  "ui.no_active_media": "No active media",
  "ui.no_active_players": "No active players",
  "ui.no_available_players": "No available players",
  "ui.no_content_found": "No content found",
  "ui.no_dynamic_player_groups_to_disconnect": "No dynamic player groups to disconnect",
  "ui.no_extra_ma_players": "No extra MA players",
  "ui.no_extra_ma_players_2": "No extra MA players",
  "ui.no_favorites_found": "No favorites found.",
  "ui.no_liked_media_yet": "No liked media yet",
  "ui.no_liked_tracks_to_play": "No liked tracks to play",
  "ui.no_light_entities_found": "No light entities found.",
  "ui.no_lyrics_found": "No lyrics found",
  "ui.no_matching_content_was_found": "No matching content was found",
  "ui.no_media_found": "No media found.",
  "ui.no_media_found_for_this_search": "No media found for this search.",
  "ui.no_mix_content_found": "No mix content found",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "Music Assistant is required. Enable the Music Assistant integration in Home Assistant and expose at least one Music Assistant media_player. Other Home Assistant media players are intentionally hidden.",
  "ui.no_music_assistant_players_were_found_for_pinning": "No Music Assistant players were found for pinning.",
  "ui.no_next_track_in_queue": "No next track in queue",
  "ui.no_player_groups_to_disconnect": "No player groups to disconnect",
  "ui.no_player_light_pairs_yet": "No player-light pairs yet.",
  "ui.no_player_selected": "No player selected",
  "ui.no_players_found": "No players found",
  "ui.no_players_found_yet": "No players found yet",
  "ui.no_playlists_found": "No playlists found",
  "ui.no_queue": "No queue",
  "ui.no_queue_item_to_rebuild": "No queue item to rebuild",
  "ui.no_queue_to_clone": "No queue to clone",
  "ui.no_radio_browser_countries_found": "No Radio Browser countries found",
  "ui.no_radio_stations_found": "No radio stations found",
  "ui.no_recent_listening_yet": "No recent listening yet.",
  "ui.no_results": "No results",
  "ui.no_results_found": "No results found",
  "ui.no_saved_studio_scenes_yet": "No saved Studio scenes yet.",
  "ui.no_scheduled_start_is_active": "No scheduled start is active",
  "ui.no_selected_tracks_to_play": "No selected tracks to play",
  "ui.no_sleep_timer_is_active": "No sleep timer is active",
  "ui.no_speech_was_captured": "No speech was captured",
  "ui.no_target_players_available": "No target players available",
  "ui.no_target_players_available_2": "No target players available",
  "ui.no_tts_service_or_entity_is_configured": "No TTS service or entity is configured",
  "ui.no_wake_schedules_yet": "No wake schedules yet",
  "ui.none": "None",
  "ui.nothing_is_playing_right_now": "Nothing is playing right now",
  "ui.nothing_playing": "Nothing playing",
  "ui.now_playing": "Now Playing",
  "ui.now_playing_2": "Now playing",
  "ui.off": "Off",
  "ui.off_2": "Off",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "Off disables it, On keeps it active, and Auto follows the configured time window.",
  "ui.oldest": "Oldest",
  "ui.on": "On",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "One tap prepares players, grouping, volume and content. Save your own current Studio target as a local scene.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Only actions for the current target are shown here.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Only Music Assistant players are shown here. Browser and local Sendspin players are hidden to avoid confusion.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Only needed when using a direct Music Assistant URL.",
  "ui.opacity_for_popups_and_overlays": "Opacity for popups and overlays.",
  "ui.opacity_for_the_main_card_background": "Opacity for the main card background.",
  "ui.open_full_interface": "Open full interface",
  "ui.open_full_queue": "Open full queue",
  "ui.open_interface_in": "Open interface in",
  "ui.open_ma": "Open MA",
  "ui.open_music_assistant": "Open Music Assistant",
  "ui.open_music_assistant_2": "Open Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Open Music Assistant on this device to activate the browser player",
  "ui.open_player_picker": "Open Player Picker",
  "ui.open_queue": "Open queue",
  "ui.open_saved_songs": "Open saved songs",
  "ui.open_the_full_music_assistant_interface": "Open the full Music Assistant interface?",
  "ui.open_the_full_music_assistant_interface_2": "Open the full Music Assistant interface",
  "ui.opening_studio_library": "Opening Studio library",
  "ui.opening_timers": "Opening timers",
  "ui.option": "Option",
  "ui.optional": "Optional",
  "ui.optional_2": "Optional",
  "ui.optional_entity_used_for_an_external_favorite_button": "Optional entity used for an external favorite button.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Optional input_text helper updated with the active player entity_id for automations and templates.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Optional input_text helper updated with the active player entity_id for automations and templates.",
  "ui.other": "Other",
  "ui.other_players": "Other players",
  "ui.party": "Party",
  "ui.path_used_when_opening_the_music_assistant_interface": "Path used when opening the Music Assistant interface.",
  "ui.pause": "Pause",
  "ui.paused": "Paused",
  "ui.per_device_in_card": "Per device / in-card",
  "ui.performance_mode": "Performance mode",
  "ui.performance_mode_for_weak_devices": "Performance mode for weak devices",
  "ui.performance_profile": "Performance profile",
  "ui.performance_profile_helper": "Choose how much visual work the card is allowed to do. Ultra Lite reduces transparency, blur, dynamic artwork and motion for weak devices.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Pick a mood or type your own style. Studio will search Music Assistant and build a queue.",
  "ui.pinned": "Pinned",
  "ui.pinned_players": "Pinned players",
  "ui.play": "Play",
  "ui.play_2": "Play",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Play a few tracks and they will appear here.",
  "ui.play_a_saved_list": "Play a saved list",
  "ui.play_all": "Play all",
  "ui.play_an_artist": "Play an artist",
  "ui.play_artwork_action": "Play artwork action",
  "ui.play_everywhere": "Play everywhere",
  "ui.play_next": "Play next",
  "ui.play_next_and_clear_queue": "Play next and clear queue",
  "ui.play_now": "Play now",
  "ui.play_now_and_clear_queue": "Play now and clear queue",
  "ui.play_pause": "Play / Pause",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Play, queue, radio, or remove favorites directly from Studio.",
  "ui.play_selected": "Play selected",
  "ui.playback_command_failed": "Playback command failed",
  "ui.playback_command_failed_2": "Playback command failed",
  "ui.playback_started": "Playback started",
  "ui.player": "player",
  "ui.player_2": "Player",
  "ui.player_count_many": "{count} players",
  "ui.player_count_one": "1 player",
  "ui.player_groups_could_not_be_disconnected": "Player groups could not be disconnected",
  "ui.player_is_pinned_from_settings": "Player is pinned from settings",
  "ui.player_is_ready": "Player is ready",
  "ui.player_is_ready_nothing_is_playing_right_now": "Player is ready - nothing is playing right now",
  "ui.player_order": "Player order",
  "ui.player_on_this_device": "Player on this device",
  "ui.player_selected": "Player selected",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "Player settings and DSP presets stay read-only until Music Assistant exposes a reliable schema for this player.",
  "ui.player_sort": "Player sort",
  "ui.players": "Players",
  "ui.players_queue_and_groups": "Players, queue and groups",
  "ui.playing": "Playing",
  "ui.playing_a_random_playlist": "Playing a random playlist",
  "ui.playing_a_random_radio_station": "Playing a random radio station",
  "ui.playing_on": "Playing on",
  "ui.playing_on_2": "Playing on",
  "ui.playlist": "Playlist",
  "ui.playlists": "Playlists",
  "ui.podcast": "Podcast",
  "ui.podcasts": "Podcasts",
  "ui.popup_opacity": "Popup opacity",
  "ui.preset_message": "Preset message",
  "ui.previous": "Previous",
  "ui.primary": "Primary",
  "ui.primary_target": "Primary target",
  "ui.pro_tools": "Pro tools",
  "ui.queue": "Queue",
  "ui.queue_2": "Queue",
  "ui.queue_action_failed": "Queue action failed",
  "ui.queue_action_is_not_available_for_this_player": "Queue action is not available for this player",
  "ui.queue_actions": "Queue actions",
  "ui.queue_cleared": "Queue cleared",
  "ui.queue_cleared_and_item_moved_next": "Queue cleared and item moved next",
  "ui.queue_cloned": "Queue cloned",
  "ui.queue_cockpit": "queue cockpit",
  "ui.queue_cockpit_2": "Queue Cockpit",
  "ui.queue_is_empty": "Queue is empty",
  "ui.queue_is_unavailable_for_this_player": "Queue is unavailable for this player",
  "ui.queue_item": "Queue item",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "Queue item id is not ready yet. Refresh the queue and try again.",
  "ui.queue_results": "Queue results",
  "ui.queue_transferred": "Queue transferred",
  "ui.queues_refreshed": "Queues refreshed",
  "ui.quick_actions": "Quick actions",
  "ui.quick_actions_2": "Quick Actions",
  "ui.quick_mix": "Quick mix",
  "ui.quick_mix_started": "Quick mix started",
  "ui.quick_play": "Quick play",
  "ui.radio": "Radio",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Radio Browser countries",
  "ui.radio_browser_country": "Radio Browser country",
  "ui.radio_browser_country_2": "Radio Browser country",
  "ui.radio_mode_is_not_available_for_this_media_type": "Radio mode is not available for this media type",
  "ui.radio_mode_started": "Radio mode started",
  "ui.radio_station_changed": "Radio station changed",
  "ui.radio_stations": "Radio Stations",
  "ui.random": "Random",
  "ui.random_gentle_morning_mix": "Random gentle morning mix",
  "ui.random_playlist": "Random playlist",
  "ui.random_radio": "Random radio",
  "ui.ready": "Ready",
  "ui.ready_to_play": "Ready to play",
  "ui.recent": "Recent",
  "ui.recent_continue": "Recent / Continue",
  "ui.recent_listening": "recent listening",
  "ui.recent_track": "Recent track",
  "ui.recently_added": "Recently Added",
  "ui.recently_played": "Recently Played",
  "ui.recently_played_2": "Recently played",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "Recommendations will appear once HOMEii Flow sees your queue or recent listening.",
  "ui.recommended": "Recommended",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Recommended format: HH:MM such as 06:00. Crossing midnight is supported.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Recommended format: HH:MM such as 22:00.",
  "ui.recommended_playlist": "Recommended playlist",
  "ui.recommended_track": "Recommended track",
  "ui.reconnects_when_you_return_to_this_dashboard": "Reconnects when you return to this dashboard",
  "ui.refresh": "Refresh",
  "ui.remember_as_this_device": "Remember as this device",
  "ui.remove": "Remove",
  "ui.remove_from_selection": "Remove from selection",
  "ui.removed_from_liked": "Removed from liked",
  "ui.removed_from_music_assistant_liked": "Removed from Music Assistant liked",
  "ui.reorder_queue": "Reorder queue",
  "ui.repeat": "Repeat",
  "ui.repeat_all": "Repeat all",
  "ui.repeat_off": "Repeat off",
  "ui.repeat_one": "Repeat one",
  "ui.reset": "Reset",
  "ui.reset_lyrics_font_size": "Reset lyrics font size",
  "ui.reset_lyrics_timing": "Reset lyrics timing",
  "ui.results": "Results",
  "ui.rtl": "RTL",
  "ui.sat": "Sat",
  "ui.save_current_target": "Save current target",
  "ui.save_schedule": "Save schedule",
  "ui.scene_players_are_not_available": "Scene players are not available",
  "ui.scene_presets": "scene presets",
  "ui.scene_presets_2": "Scene Presets",
  "ui.scenes": "Scenes",
  "ui.scales_interface_icons_without_changing_button_sizes": "Scales interface icons without changing button sizes or layout.",
  "ui.scheduled_start": "Scheduled start",
  "ui.scheduled_start_activated_label": "Scheduled start activated: {label}",
  "ui.scheduled_start_cleared": "Scheduled start cleared",
  "ui.scheduled_start_failed": "Scheduled start failed",
  "ui.scheduled_start_saved": "Scheduled start saved",
  "ui.scheduled_starts_active_count": "{count} scheduled starts active",
  "ui.schedules": "Schedules",
  "ui.search": "Search",
  "ui.search_and_choose_play_next_add_radio_or_like": "Search and choose Play, Next, Add, Radio, or Like.",
  "ui.search_everything": "Search everything...",
  "ui.search_failed": "Search failed",
  "ui.search_in_queue": "Search in queue",
  "ui.search_queue_and_library": "Search queue and library...",
  "ui.search_the_library": "Search the library...",
  "ui.search_tracks_albums_artists_and_playlists": "Search tracks, albums, artists and playlists",
  "ui.searching": "Searching...",
  "ui.searching_library": "Searching library...",
  "ui.searching_smart_selection": "Searching smart selection...",
  "ui.seek": "Seek",
  "ui.select": "Select",
  "ui.select_lights": "Select light or lights",
  "ui.select_player": "Select player",
  "ui.select_2": "Select",
  "ui.select_a_player_first": "Select a player first",
  "ui.select_at_least_one_studio_player": "Select at least one Studio player",
  "ui.select_at_least_two_players_to_create_a_group": "Select at least two players to create a group",
  "ui.selected": "Selected",
  "ui.selected_music": "Selected music",
  "ui.selected_player": "Selected Player",
  "ui.selected_player_2": "Selected player",
  "ui.selected_player_3": "Selected player",
  "ui.selected_players_at_comfortable_volume": "Selected players at comfortable volume",
  "ui.selected_playlist": "Selected playlist",
  "ui.send_a_short_voice_message_or_announcement_url": "Send a short voice message or announcement URL.",
  "ui.send_a_voice_message": "Send a voice message",
  "ui.send_announcement": "Send announcement",
  "ui.sendspin_and_diagnostics": "Sendspin and diagnostics",
  "ui.set_sleep_timer": "Set sleep timer",
  "ui.set_volume": "Set volume",
  "ui.set_the_visual_order_of_quick_actions": "Set the visual order. Only selected Quick Actions are shown; unselected items are ignored.",
  "ui.settings": "Settings",
  "ui.settings_source": "Settings source",
  "ui.show_ma_button": "Show MA button",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Show or hide the inline next-track row in Now Playing.",
  "ui.show_theme_toggle": "Show theme toggle",
  "ui.show_up_next": "Show Up Next",
  "ui.show_up_next_2": "Show Up Next",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Shows a standalone compact player tile with artwork, basic controls, volume and an expand action.",
  "ui.shuffle": "Shuffle",
  "ui.shuffle_all": "Shuffle all",
  "ui.shuffle_favorites": "Shuffle favorites",
  "ui.shuffle_play": "Shuffle play",
  "ui.skipped_to_up_next": "Skipped to up next",
  "ui.sleep_timer": "Sleep timer",
  "ui.sleep_timer_2": "Sleep Timer",
  "ui.sleep_timer_active_remaining": "Sleep timer active: {remaining}",
  "ui.sleep_timer_added_minutes": "Added {minutes} minutes to the timer",
  "ui.sleep_timer_and_morning_playback": "Sleep timer and morning playback",
  "ui.sleep_timer_and_scheduled_playback": "Sleep timer and scheduled playback",
  "ui.sleep_timer_cleared": "Sleep timer cleared",
  "ui.sleep_timer_finished": "Sleep timer finished",
  "ui.sleep_timer_presets": "Sleep timer presets",
  "ui.sleep_timer_set_for_15_minutes": "Sleep timer set for 15 minutes",
  "ui.sleep_timer_set_minutes": "Sleep timer set for {minutes} minutes",
  "ui.sleep_timer_updated_minutes": "Sleep timer updated to {minutes} minutes",
  "ui.smaller_lyrics": "Smaller lyrics",
  "ui.smart": "Smart",
  "ui.smart_mix": "smart mix",
  "ui.smart_mix_builder": "Smart Mix Builder",
  "ui.smart_voice_selection": "Smart voice selection",
  "ui.soft_and_relaxed": "Soft and relaxed",
  "ui.soft_relaxed_music": "Soft, relaxed music",
  "ui.some_players_could_not_be_stopped_or_cleared": "Some players could not be stopped or cleared",
  "ui.songs_around_an_artist": "Songs around an artist",
  "ui.sort": "Sort",
  "ui.sort_ascending": "Sort ascending",
  "ui.sort_descending": "Sort descending",
  "ui.source_queue": "Source queue",
  "ui.spain": "Spain",
  "ui.speak_to_target_players": "Speak to target players",
  "ui.start_radio_mode": "Start radio mode",
  "ui.start_time": "Start time",
  "ui.start_time_2": "Start time",
  "ui.started_on_selected_player": "Started on selected player",
  "ui.starting_a_chill_mix": "Starting a chill mix",
  "ui.starting_quick_mix": "Starting Quick Mix...",
  "ui.stations": "stations",
  "ui.stay_active": "Stay active",
  "ui.stays_active": "Stays active",
  "ui.stop_all": "Stop all",
  "ui.stop_all_players": "Stop all players",
  "ui.stop_command_failed": "Stop command failed",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Stop playback, clear queues and disconnect groups",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Stopped all players, cleared playlists and disconnected groups",
  "ui.stops_this_browser_player": "Stops this browser player",
  "ui.strong": "Strong",
  "ui.studio": "Studio",
  "ui.studio_closed": "Studio closed",
  "ui.studio_media_action_failed": "Studio media action failed",
  "ui.studio_mix_started": "Studio mix started",
  "ui.studio_opened": "Studio opened",
  "ui.studio_pro": "studio pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Studio scene",
  "ui.studio_scene_deleted": "Studio scene deleted",
  "ui.studio_scene_saved": "Studio scene saved",
  "ui.studio_scene_was_not_found": "Studio scene was not found",
  "ui.studio_search": "studio search",
  "ui.studio_shortcut": "Studio shortcut",
  "ui.subtle": "Subtle",
  "ui.sun": "Sun",
  "ui.surprise_me": "Surprise me",
  "ui.sync": "Sync",
  "ui.sync_lyrics": "Sync lyrics",
  "ui.tablet": "Tablet",
  "ui.tap_to_start_a_sleep_timer": "Tap to start a sleep timer",
  "ui.target_players": "Target players",
  "ui.target_queue": "Target queue",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Text announcements use Home Assistant TTS into the selected Music Assistant player. URL announcements use Music Assistant announcement playback.",
  "ui.text_only": "Text only",
  "ui.text_tone": "Text tone",
  "ui.theme": "Theme",
  "ui.theme_2": "Theme",
  "ui.theme_mode": "Theme mode",
  "ui.theme_toggle": "Theme toggle",
  "ui.theme_toggle_2": "Theme toggle",
  "ui.this_device": "This Device",
  "ui.this_device_2": "This device",
  "ui.this_device_player_connected": "This device player connected",
  "ui.this_device_player_disconnected": "This device player disconnected",
  "ui.thu": "Thu",
  "ui.timer": "Timer",
  "ui.timer_active_remaining": "Timer active: {remaining}",
  "ui.timers": "Timers",
  "ui.to": "To",
  "ui.today": "Today",
  "ui.track": "Track",
  "ui.track_lyrics": "Track Lyrics",
  "ui.tracks": "Tracks",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Transfer, clone, inspect, or clear queues without hiding the Studio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "Transfer is unavailable while a pinned player is configured",
  "ui.transfer_queue": "Transfer Queue",
  "ui.transfer_queue_2": "Transfer queue",
  "ui.transfer_queue_3": "Transfer queue",
  "ui.try_again": "Try again",
  "ui.tts_entity": "TTS entity",
  "ui.tts_entity_used_by_the_announcement_screen": "TTS entity used by the announcement screen.",
  "ui.tts_language": "TTS language",
  "ui.tue": "Tue",
  "ui.turkey": "Turkey",
  "ui.turn_off": "Turn off",
  "ui.turns_off_after_run": "Turns off after run",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Turns off heavy visuals so the card runs smoother on Nest Hub, older tablets, and weaker displays.",
  "ui.type_a_free_style_first": "Type a free style first",
  "ui.type_an_announcement": "Type an announcement...",
  "ui.type_anything": "Type anything",
  "ui.type_what_should_be_announced": "Type what should be announced...",
  "ui.ungroup": "Ungroup",
  "ui.ungroup_2": "Ungroup",
  "ui.united_kingdom": "United Kingdom",
  "ui.united_states": "United States",
  "ui.unknown": "Unknown",
  "ui.up": "Up",
  "ui.move_down": "Move down",
  "ui.move_up": "Move up",
  "ui.up_next": "Up Next",
  "ui.up_next_2": "Up next",
  "ui.up_next_title": "Up next: {title}",
  "ui.upbeat_and_moving": "Upbeat and moving",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Use any Home Assistant path. Example: /lovelace/home",
  "ui.use_current_view_for_home_button": "Use current view for Home button",
  "ui.use_expand_to_open_the_full_player": "Use expand to open the full player",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Uses the same base country list shown in the in-card settings screen.",
  "ui.visible_tiles": "visible tiles",
  "ui.visible_tiles_2": "Visible tiles",
  "ui.visual_editor": "Visual editor",
  "ui.assist_agent": "Assist agent",
  "ui.assist_agent_id": "Assist agent ID",
  "ui.assist_only": "Assist only",
  "ui.default_assist_agent": "Default Home Assistant Assist",
  "ui.default_order": "Default order",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Recommended: music commands run locally on the selected player. Anything the card does not understand is sent to Home Assistant Assist.",
  "ui.hybrid_music_plus_assist": "Hybrid: Music + Assist",
  "ui.icon_size": "Icon size",
  "ui.music_only": "Music only",
  "ui.music_plus_assist": "Music + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "Optional Assist agent ID. Leave empty to use Home Assistant's default Assist agent.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Optional. Leave this on the default agent unless you have multiple Assist agents and want this card to use one specific agent.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Shows a push-to-talk button for music commands and Assist fallback.",
  "ui.show_home_inside_quick_actions": "Shows Home as a Quick Action instead of a floating button.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Show plus and minus buttons next to the volume slider.",
  "ui.speak_voice_assistant_responses_out_loud": "Speak the recognized response out loud through this browser when supported.",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "Voice assistant",
  "ui.voice_assistant_disabled": "Voice assistant is disabled",
  "ui.voice_assistant_listening": "Listening for a command...",
  "ui.voice_assistant_mode": "Voice assistant mode",
  "ui.voice_command_executed": "Voice command executed",
  "ui.voice_command_completed_action": "Done: {action}",
  "ui.voice_command_failed": "Voice command failed",
  "ui.voice_command_no_player": "Select a player or say the player name.",
  "ui.voice_command_not_understood": "I could not understand that command.",
  "ui.voice_command_sent": "Voice command sent",
  "ui.voice_done_status": "Done",
  "ui.voice_error_status": "Needs attention",
  "ui.voice_feedback": "Voice feedback",
  "ui.voice_group_connect_needs_players": "Say at least two player names, or select one player and say the other.",
  "ui.voice_group_connected_players": "Connected {primary} with {members}",
  "ui.voice_group_disconnect_needs_player": "Say which player group to disconnect.",
  "ui.voice_group_disconnected_player": "Disconnected the group for {player}",
  "ui.voice_input_failed": "Voice input failed",
  "ui.voice_input_is_not_supported_on_this_device": "Voice input is not supported on this device",
  "ui.voice_language_is_not_supported": "This browser does not support the selected speech recognition language.",
  "ui.voice_match_not_confident": 'I found {title}, but it does not match "{query}" closely enough. Try saying the song and artist again.',
  "ui.voice_music_searching": "Searching music: {query}",
  "ui.voice_playing_result": "Playing {title}",
  "ui.voice_queue_transfer_needs_players": "Say the source and target player, for example: transfer queue from Living Room to Kitchen.",
  "ui.voice_queue_transferred_between": "Queue transferred from {source} to {target}",
  "ui.voice_starting_playback": "Starting {title}...",
  "ui.voice_processing_status": "Processing command...",
  "ui.voice_ready_status": "Ready",
  "ui.voice_recognition_network_failed": "Speech recognition service is unavailable. Microphone permission may be OK; check the browser, VPN, DNS, firewall, or network.",
  "ui.voice_response": "Response",
  "ui.voice_response_will_appear_here": "The response will appear here.",
  "ui.voice_search": "Voice search",
  "ui.voice_search_failed": "Voice search failed",
  "ui.voice_search_is_not_supported_on_this_device": "Voice search is not supported on this device",
  "ui.voice_transcript": "Transcript",
  "ui.voice_listening_status": "Listening...",
  "ui.waiting_for_speech": "Waiting for speech...",
  "ui.microphone_permission_or_browser_blocked": "The browser blocked speech recognition. Check microphone and browser speech settings.",
  "ui.volume": "Volume",
  "ui.volume_and_player_target": "Volume and player target",
  "ui.volume_boost": "Volume boost",
  "ui.volume_control_large_screen_only": "Volume control (large screen only)",
  "ui.volume_down": "Volume down",
  "ui.volume_mode": "Volume mode",
  "ui.volume_presets": "Volume presets",
  "ui.volume_step_between_1_and_10_percent_default_5": "Volume step between 1% and 10%. Default is 5%.",
  "ui.volume_step_buttons": "Volume +/- buttons",
  "ui.volume_step_percent": "Volume step",
  "ui.volume_up": "Volume up",
  "ui.waiting_for_this_device_player": "Waiting for this device player...",
  "ui.wake": "Wake",
  "ui.wake_schedules": "Wake schedules",
  "ui.wed": "Wed",
  "ui.week": "Week",
  "ui.what_would_you_like_to_listen_to": "What would you like to listen to?",
  "ui.will_play_next": "Will play next",
  "ui.will_play_next_in_studio": "Will play next in Studio",
  "ui.worldwide_popular": "Worldwide popular",
  "ui.add_player_light_pair": "Add pair",
  "ui.ambient_light": "Ambient light",
  "ui.ambient_light_brightness": "Light brightness",
  "ui.ambient_light_cooldown": "Light update cooldown",
  "ui.ambient_light_entities": "Default color lights",
  "ui.ambient_light_player_map": "Player -> light mapping",
  "ui.ambient_light_player_map_helper": "Use the player and color-light selectors below. Each completed row becomes a pair; clear a player or its lights to remove the pair.",
  "ui.ambient_light_transition": "Light transition",
  "ui.analog": "Analog",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Choose only color-capable lights that should follow the music when no player-specific pair exists.",
  "ui.digital": "Digital",
  "ui.discover_albums": "Discover albums",
  "ui.discover_music": "Discover",
  "ui.discover_subtitle": "Recommended playlists, albums and radio.",
  "ui.discovery": "Discovery",
  "ui.discovery_failed": "Discovery mode failed",
  "ui.discovery_mode": "Discovery mode",
  "ui.discovery_started": "Discovery started: {mood}",
  "ui.maximum_brightness_for_music_lighting": "Maximum brightness for music lighting.",
  "ui.minimum_seconds_between_light_updates": "Minimum seconds between light updates.",
  "ui.player_light_pair_lights": "Color lights {number}",
  "ui.player_light_pair_lights_helper": "Choose color-capable lights for this Music Assistant player. Clear this field or the player to remove the pair.",
  "ui.player_light_pair_player": "Music Assistant player {number}",
  "ui.player_light_pair_player_helper": "Choose the Music Assistant player for this light pair.",
  "ui.all": "All",
  "ui.all_genres": "All genres",
  "ui.genres_and_styles": "Genres and styles",
  "ui.compact_widget_mode": "Compact widget style",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Choose when compact mode uses the smaller two-row mobile widget.",
  "ui.full": "Full",
  "ui.mini_widget": "Mini widget",
  "ui.genre": "Genre",
  "ui.mood_and_genre": "Mood and genre",
  "ui.music_by_mood_and_genre": "Music by mood and genre",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "Optional entity for the auxiliary button. Leave empty to stop the active player.",
  "ui.optional_name_for_the_auxiliary_button": "Optional label shown as the auxiliary button tooltip.",
  "ui.choose_the_auxiliary_button_icon": "Choose the icon shown for the auxiliary button.",
  "ui.choose_how_players_are_sorted": "Choose default, alphabetical, or custom player order.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Choose Music Assistant players that should never appear in the card.",
  "ui.custom_order": "Custom order",
  "ui.no_visible_music_assistant_players_after_exclusions": "All Music Assistant players are hidden by the excluded players setting.",
  "ui.set_custom_player_order": "Set the custom player order. Empty slots are ignored.",
  "ui.show_auxiliary_button_in_quick_actions": "Shows this auxiliary button in the Quick Actions row.",
  "ui.power": "Power",
  "ui.power_action_failed": "Power action failed",
  "ui.power_action_sent": "Power action sent",
  "ui.power_button": "Auxiliary button",
  "ui.power_button_action": "Auxiliary button action",
  "ui.power_button_entity": "Auxiliary button entity",
  "ui.scene": "Scene",
  "ui.screensaver": "Screensaver",
  "ui.screensaver_buttons": "Screensaver buttons",
  "ui.screensaver_controls": "Screensaver controls",
  "ui.screensaver_clock_mode": "Screensaver clock mode",
  "ui.screensaver_clock_size": "Screensaver clock size",
  "ui.screensaver_clock_size_helper": "Scales the tablet screensaver clock without moving the artwork.",
  "ui.screensaver_clock_x": "Clock position - LEFT / RIGHT",
  "ui.screensaver_clock_x_helper": "Lower values move left, higher values move right.",
  "ui.screensaver_clock_y": "Clock position - UP / DOWN",
  "ui.screensaver_clock_y_helper": "Lower values move up, higher values move down.",
  "ui.screensaver_message": "Screensaver message",
  "ui.screensaver_timeout": "Screensaver timeout",
  "ui.script": "Script",
  "ui.short_optional_message_shown_on_the_screensaver": "Short optional message shown on the screensaver.",
  "ui.show_previous_next_controls_on_the_screensaver": "Show previous and next buttons on the screensaver.",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Choose exactly which buttons appear on the screensaver, including FLOW ASSISTANT, previous and next.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Show a calm clock and artwork display after idle time.",
  "ui.show_power_button_in_the_player_controls": "Show an auxiliary button in Quick Actions.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Show the fullscreen discovery mode in Actions.",
  "ui.smart_home": "Smart home",
  "ui.soft_transition_time_for_music_lighting": "Soft transition time for music lighting.",
  "ui.stop_player": "Stop player",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Sync selected lights to the current artwork color.",
  "ui.toggle": "Toggle",
  "ui.turn_on": "Turn on",
  "ui.choose_a_category_to_browse_provider_playlists": "Choose a category to browse provider playlists before playing.",
  "ui.new_and_recommended": "New and recommended",
  "ui.provider_playlists": "Provider playlists",
  "ui.hotel_mode": "Hotel Mode",
  "ui.hotel_mode_helper": "Minimal hotel-safe UI: player controls, volume, search, artwork browsing, and player selection only.",
  "ui.style_and_genres": "Style and genres"
});
const messages$6 = Object.freeze({
  "ui.a_guided_music_wizard": "En guidet musikguide",
  "ui.a_radio_station": "En radiostation",
  "ui.accent_color": "Accentuerende farve",
  "ui.actions": "Handlinger",
  "ui.actions_2": "Handlinger",
  "ui.active_days": "Aktive dage",
  "ui.active_player_helper": "Aktiv hjælper til Afspillere",
  "ui.active_player_helper_must_be_an_input_text_entity": "Aktiv Afspiller-hjælper skal være en input_text-enhed",
  "ui.active_players": "Aktive Afspillere",
  "ui.add": "Tilføj",
  "ui.add_to_queue": "Tilføj til Kø",
  "ui.add_to_selection": "Tilføj til udvalg",
  "ui.added_to_liked": "Tilføjet til liked",
  "ui.added_to_music_assistant_liked": "Tilføjet til Music Assistant syntes godt om",
  "ui.added_to_queue": "Tilføjet til Kø",
  "ui.added_to_studio_queue": "Føjet til Kø i Studio",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Tilføjer bevægelse til baggrundslagene. Subtle holder det roligt, Strong er mærkbart, og Extreme er det fedeste animerede look.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Tilføjer kun til den aktuelle lydstyrke under meddelelser og gendanner derefter den forrige lydstyrke.",
  "ui.after_run": "Efter kørsel",
  "ui.album": "Album",
  "ui.albums": "Albums",
  "ui.all_albums": "Alle Albums",
  "ui.all_artists": "Alle Kunstnere",
  "ui.all_countries": "Alle lande",
  "ui.all_player_groups_disconnected": "Alle grupper af Afspillere afbrudt",
  "ui.all_players": "Alle Afspillere",
  "ui.all_players_2": "Alle Afspillere",
  "ui.all_podcasts": "Alle Podcasts",
  "ui.all_tracks": "Alle spor",
  "ui.allow_local_likes": "Tillad lokale likes",
  "ui.always_visible": "Altid synlig",
  "ui.announce": "Annoncer",
  "ui.announce_to_all_players": "Giv besked til alle Afspillere",
  "ui.announcement": "Meddelelse",
  "ui.announcement_failed_with_error": "Meddelelse mislykkedes{error}",
  "ui.announcement_language": "Sprog for annoncering",
  "ui.announcement_presets": "Forudindstillinger for annoncering",
  "ui.announcement_sent_to_player": "Meddelelse sendt til {afspiller}.",
  "ui.announcement_studio": "announcement studio",
  "ui.announcement_studio_2": "Announcement Studio",
  "ui.announcement_target": "Mål for annoncering",
  "ui.announcement_to_player_preview": "Meddelelse til {afspiller}: {forhåndsvisning}",
  "ui.announcement_volume_boost": "Forøgelse af lydstyrken ved annoncering",
  "ui.announcements": "Meddelelser",
  "ui.appearance": "Udseende",
  "ui.alphabetical": "Alfabetisk",
  "ui.auxiliary_button": "Hjælpeknap",
  "ui.auxiliary_button_action": "Handling af hjælpeknap",
  "ui.auxiliary_button_entity": "Hjælpeknap-enhed",
  "ui.auxiliary_button_icon": "Ikon for hjælpeknap",
  "ui.auxiliary_button_name": "Navn på hjælpeknap",
  "ui.auxiliary_buttons": "Hjælpeknapper",
  "ui.apply_group": "Ansøg gruppe",
  "ui.apply_schedule": "Anvend tidsplan",
  "ui.artist": "Kunstner",
  "ui.artist_radio": "Kunstner radio",
  "ui.artists": "Kunstnere",
  "ui.artwork": "Kunstværk",
  "ui.artwork_swipe": "Swipe af illustrationer",
  "ui.ascending": "Stigende",
  "ui.at_least_one_player_must_stay_selected": "Mindst én Afspiller skal forblive valgt",
  "ui.australia": "Australien",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Auto vælger mobil eller tablet baseret på den faktiske bredde.",
  "ui.auto_cloud_default": "Auto / Cloud standard",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Auto udtrækker farver fra det aktuelle kunstværk og holder effekten subtil. Strong gør paletten rigere og lysere.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Auto lader Home Assistant Cloud stemmestandarder være uberørt. Manuelle valg sender et eksplicit sprog.",
  "ui.available": "Tilgængelig",
  "ui.back": "Tilbage",
  "ui.back_2": "Tilbage",
  "ui.back_to_countries": "Tilbage til lande",
  "ui.back_to_queue": "Tilbage til Kø",
  "ui.background_motion": "Baggrundsbevægelse",
  "ui.behavior": "Adfærd",
  "ui.browse_covers": "Gennemse omslag",
  "ui.browse_playlists_artists_albums_and_radio": "Gennemse playlister, Kunstnere, Albums og Radio",
  "ui.browser_players": "Afspillere i browseren",
  "ui.build_custom_mix": "Byg et tilpasset mix",
  "ui.building_studio_mix": "Opbygning af Studio-mix...",
  "ui.button": "Knap",
  "ui.by_mood": "Efter humør",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Cache-varighed i millisekunder for udvalgte dataforespørgsler.",
  "ui.cache_ttl": "Cache TTL",
  "ui.calm": "Rolig",
  "ui.canada": "Canada",
  "ui.cancel": "Annuller",
  "ui.cancel_2": "Annuller",
  "ui.cancel_selection": "Annuller valg",
  "ui.card_configuration": "Kortkonfiguration",
  "ui.card_height": "Kortets højde",
  "ui.card_height_in_pixels": "Kortets højde i pixels.",
  "ui.card_settings_are_managed_from_the_visual_editor": "Kortindstillingerne styres fra den visuelle editor",
  "ui.change_song": "Skift sang",
  "ui.change_track": "Skift spor",
  "ui.chill_mix": "Chill mix",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Vælg et land eller Alle lande for at gennemse alle lande på fanen Radio.",
  "ui.choose_a_queue_position": "Vælg en position i køen",
  "ui.choose_a_source_player": "Vælg en kildeafspiller",
  "ui.choose_a_target_player": "Vælg en Afspiller som mål",
  "ui.choose_at_least_one_player": "Vælg mindst én Afspiller",
  "ui.choose_icons_text_or_both_for_the_footer": "Vælg ikoner, tekst eller begge dele til sidefoden.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Vælg kun Music Assistant-afspillere. Kortets pinningliste viser kun Music Assistant-afspillere for at undgå forvirring.",
  "ui.choose_one_clear_option": "Vælg en klar mulighed",
  "ui.choose_player": "Vælg Afspiller",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Vælg noget fra den hurtige hylde, eller tryk på staven for at få en tilfældig playliste.",
  "ui.choose_source": "Vælg kilde",
  "ui.choose_target": "Vælg mål",
  "ui.choose_target_player": "Vælg målafspiller",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Vælg accentfarven til mobillayoutet.",
  "ui.choose_the_music": "Vælg musikken",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Vælg og bestil de små knapper, der vises under sangtitlen.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Vælg kilden først. Afspilning vil bruge det aktuelle mål.",
  "ui.choose_where_to_play": "Vælg, hvor du vil spille",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Vælg, om kunstværket skal skifte spor eller gennemse covers.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Vælg, om indstillingerne skal styres fra brugergrænsefladen på kortet eller fra kortkonfigurationen i dashboardet.",
  "ui.choose_which_actions_appear_in_the_main_bar": "Vælg, hvilke Handlinger der skal vises i hovedlinjen.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Vælg, hvilke dage vinduet Auto nattilstand gælder for.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Vælg, hvilke ikoner der vises i rækken med hurtige handlinger. Rens alle beder om bekræftelse, før du afbryder forbindelsen til aktive Afspillere.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Vælg, hvilke Afspillere der vises som fliser i rummet.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Vælg, hvilke Afspillere der bliver i det aktuelle kontrolvalg.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Vælg, hvilke faner der er tilgængelige i biblioteket.",
  "ui.choose_who_is_controlled": "Vælg, hvem der skal kontrolleres",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "Hvis du vælger Visual, deaktiveres skærmen med indstillinger på kortet.",
  "ui.clean_all": "Rengør alle",
  "ui.clean_all_confirm_action": "Rengør alle",
  "ui.clean_all_confirm_copy": "Dette vil afbryde forbindelsen til alle aktive afspillere, stoppe afspilningen og rydde deres køer. Fortsætte?",
  "ui.clean_all_confirm_title": "Er alle Afspillere rene?",
  "ui.clean_the_studio_wall": "Rengør væggen i Studio",
  "ui.clear": "Klar",
  "ui.clear_queue": "Ryd køen",
  "ui.clear_search": "Klar søgning",
  "ui.clone_queue": "Klon kø",
  "ui.close": "Luk",
  "ui.collapse_compact_player": "Skjul kompakt afspiller",
  "ui.compact_mode": "Kompakt tilstand",
  "ui.configure_ready_made_announcement_phrases": "Konfigurer færdige meddelelsesfraser.",
  "ui.connect_this_device": "Tilslut denne enhed",
  "ui.connect_this_device_2": "Tilslut denne enhed",
  "ui.connected": "Tilsluttet",
  "ui.connected_2": "Forbundet",
  "ui.connected_players": "Afspillere med forbindelse",
  "ui.connected_players_2": "Forbundne afspillere",
  "ui.connecting": "Forbindelse",
  "ui.connecting_2": "Forbindelse...",
  "ui.confirm": "Bekræft",
  "ui.connection_and_behavior": "Forbindelse og adfærd",
  "ui.connects_directly_from_this_card_using_sendspin": "Forbinder direkte fra dette kort ved hjælp af Sendspin",
  "ui.continue": "Fortsæt",
  "ui.continue_2": "Fortsæt",
  "ui.continue_from_recent_music_assistant_activity": "Fortsæt fra den seneste Music Assistant-aktivitet.",
  "ui.continue_listening": "Fortsæt med at lytte",
  "ui.continue_what_was_played_recently": "Fortsæt med det, der blev spillet for nylig",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Kontroller, om kortets baggrund bevæger sig blidt, og hvor stærk bevægelsen føles.",
  "ui.controlling": "Styring",
  "ui.could_not_build_quick_mix_from_the_current_song": "Kunne ikke opbygge Quick Mix fra den aktuelle sang",
  "ui.could_not_build_studio_mix": "Kunne ikke bygge Studio mix",
  "ui.could_not_choose_media": "Kunne ikke vælge medie",
  "ui.could_not_clone_the_queue": "Kunne ikke klone køen",
  "ui.could_not_find_music": "Kunne ikke finde musik",
  "ui.could_not_play_label": "Kunne ikke afspille: {label}",
  "ui.could_not_start_chill_mix": "Kunne ikke starte chill mix",
  "ui.could_not_start_playback": "Kunne ikke starte afspilning",
  "ui.could_not_start_playback_2": "Kunne ikke starte afspilning",
  "ui.could_not_start_playback_in_studio": "Kunne ikke starte afspilning i Studio",
  "ui.could_not_start_quick_mix": "Kunne ikke starte Quick Mix",
  "ui.could_not_start_radio_playback": "Kunne ikke starte afspilning af radio",
  "ui.could_not_transfer_the_queue": "Kunne ikke overføre køen",
  "ui.could_not_update_active_player_helper": "Kunne ikke opdatere aktiv Afspiller-hjælper",
  "ui.create_schedule": "Opret tidsplan",
  "ui.current_path": "Nuværende sti",
  "ui.custom": "Brugerdefineret",
  "ui.dark": "Mørk",
  "ui.delete_scene": "Slet scene",
  "ui.delete_schedule": "Slet tidsplan",
  "ui.descending": "Nedadgående",
  "ui.dictate": "Diktere",
  "ui.disabled": "Handicappet",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Deaktiverer sløring, animationer, dynamiske baggrunde og kraftige skygger. Anbefales til Nest Hub og svagere tablets.",
  "ui.disconnect": "Afbryd forbindelsen",
  "ui.disconnect_all": "Afbryd alle forbindelser",
  "ui.disconnect_groups": "Afbryd grupper",
  "ui.disconnect_player_groups": "Afbryd forbindelsen til grupper af Afspillere",
  "ui.disconnect_this_device": "Afbryd forbindelsen til denne enhed",
  "ui.disconnected": "Frakoblet",
  "ui.disconnecting": "Afbryder forbindelsen",
  "ui.disconnecting_player_groups": "Afbrydelse af grupper af afspillere.",
  "ui.discover": "Opdag",
  "ui.down": "Ned",
  "ui.dynamic_theme": "Dynamisk tema",
  "ui.edit_schedule": "Rediger tidsplan",
  "ui.enable": "Gør det muligt",
  "ui.enable_or_disable_the_footer_search_button": "Aktivér eller deaktivér søgeknappen i sidefoden.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Aktivér eller deaktivér Studio-knappen i sidefodsbjælken.",
  "ui.enable_the_floating_home_shortcut_button": "Aktivér genvejen Hjem i Hurtige Handlinger.",
  "ui.enabled": "Aktiveret",
  "ui.enables_switching_to_local_liked_mode": "Gør det muligt at skifte til lokal lignende tilstand.",
  "ui.end_time": "Sluttidspunkt",
  "ui.excluded_players": "Ekskluderede Afspillere",
  "ui.energetic": "Energisk",
  "ui.energy_and_rhythm": "Energi og rytme",
  "ui.enter_an_announcement_first": "Indtast en meddelelse først",
  "ui.example_quiet_jazz_greek_music_workout": "Eksempel: stille jazz, græsk musik, træning",
  "ui.existing_content": "Eksisterende indhold",
  "ui.expand_player": "Udvid Afspiller",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Udtræk farver fra det aktuelle kunstværk og anvend dem på grænsefladen.",
  "ui.extreme": "Ekstrem",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Fallback for opsætninger, der har brug for en alternativ køservicesti.",
  "ui.fallback_mode": "Fallback-tilstand",
  "ui.family_friendly": "Familievenlig",
  "ui.favorite": "Favorit",
  "ui.favorite_action_failed": "Favorithandling mislykkedes",
  "ui.favorite_action_sent": "Favorithandling sendt",
  "ui.favorite_button_entity": "Enhed med favoritknap",
  "ui.favorite_center": "Favoritcenter",
  "ui.favorite_center_2": "Favoritcenter",
  "ui.favorite_radio": "Favorit Radio",
  "ui.favorite_updated": "Favorit opdateret",
  "ui.favorites_and_liked_music": "Favoritter og ønsket musik",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Værktøjer til registrering af funktioner, Afspiller-kontekst og afspilning på denne enhed.",
  "ui.find_music": "Find musik",
  "ui.finding_music": "At finde musik",
  "ui.fixed": "Fast",
  "ui.flow_mix": "FLOW Mix",
  "ui.font_scale": "Skala for skrifttype",
  "ui.font_size": "Skriftstørrelse",
  "ui.footer_search": "Søg i sidefod",
  "ui.footer_style": "Stil til sidefod",
  "ui.footer_style_2": "Stil til sidefod",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "For eksempel /lovelace/home eller en anden sti til Home Assistant.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "For svagere skærme: Slår kraftig sløring, animationer, dynamisk udtræk af temaer og baggrundsbevægelse fra.",
  "ui.france": "Frankrig",
  "ui.free_style": "Fri stil",
  "ui.free_style_quiet_jazz_greek_music_workout": "Fri stil: stille jazz, græsk musik, workout...",
  "ui.fresh_start": "En ny start",
  "ui.fresh_start_2": "En ny start",
  "ui.fri": "Fri",
  "ui.friendly_and_simple": "Venlig og enkel",
  "ui.from": "Fra",
  "ui.general": "Generelt",
  "ui.general_and_display": "Generelt og display",
  "ui.germany": "Tyskland",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Global skala for hver interface-skrifttype. 1 er standardstørrelsen.",
  "ui.greece": "Grækenland",
  "ui.grid": "Gitter",
  "ui.group": "Gruppe",
  "ui.group_cleared": "Gruppe ryddet",
  "ui.group_cleared_2": "Gruppe ryddet",
  "ui.group_speakers": "Talere i grupper",
  "ui.group_speakers_2": "Talere i grupper",
  "ui.group_updated": "Gruppen er opdateret",
  "ui.group_volume": "Lydstyrke i gruppen",
  "ui.group_volume_up_energetic_mix": "Gruppe, lydstyrke op, energisk mix",
  "ui.grouped_players": "Afspillere i grupper",
  "ui.hebrew": "Hebraisk",
  "ui.history": "Historie",
  "ui.history_tabs": "Historiske faner",
  "ui.home": "Hjem",
  "ui.home_party_night_presets": "Forudindstillinger for hjem, fest og nat",
  "ui.home_path": "Sti til hjemmet",
  "ui.home_scene_prepared": "Hjemmescenen er forberedt",
  "ui.home_shortcut": "Genvej til start",
  "ui.home_shortcut_path": "Sti til genvej til hjemmet",
  "ui.icon_and_text": "Ikon og tekst",
  "ui.icon_only": "Kun ikon",
  "ui.icon_plus_text": "Ikon + tekst",
  "ui.idle": "Inaktiv",
  "ui.idle_2": "Inaktiv",
  "ui.in_card_ui": "Brugergrænseflade på kortet",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Inkluderer den tilpassede tema-tilstand fra skærmen med kortindstillinger.",
  "ui.info": "Info",
  "ui.israel": "Israel",
  "ui.israeli": "Israelsk",
  "ui.italy": "Italien",
  "ui.items": "elementer",
  "ui.join_selected_players": "Slut dig til udvalgte Afspillere",
  "ui.kids": "Børn",
  "ui.language": "Sprog",
  "ui.larger_lyrics": "Større tekster",
  "ui.layout_adapted_to_height": "Layout tilpasset højden",
  "ui.layout_mode": "Layout-tilstand",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Lad det være tomt, hvis kortet kun skal bruge Home Assistant.",
  "ui.library": "Bibliotek",
  "ui.library_2": "Bibliotek",
  "ui.library_pages": "Bibliotekets sider",
  "ui.default_library_layout": "Layout for standardbibliotek",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Vælg, hvordan bibliotekets sider åbnes. Du kan stadig skifte mellem Grid/List inde i biblioteket.",
  "ui.library_radio": "Radio på biblioteket",
  "ui.library_surprise": "Bibliotekets overraskelse",
  "ui.library_tabs": "Faner i biblioteket",
  "ui.library_tabs_2": "Bibliotekets faner",
  "ui.light": "Lys",
  "ui.like": "Ligesom",
  "ui.like_2": "Ligesom",
  "ui.like_3": "Ligesom",
  "ui.liked": "Kunne lide",
  "ui.liked_mode": "Synes godt om-tilstand",
  "ui.liked_sync": "Kunne lide synkronisering",
  "ui.list": "Liste",
  "ui.listening": "Lytter...",
  "ui.loading": "Indlæser...",
  "ui.loading_favorites": "Indlæser favoritter...",
  "ui.loading_library": "Indlæser bibliotek...",
  "ui.loading_lyrics": "Indlæsning af tekster...",
  "ui.loading_players": "Afspillere på vej...",
  "ui.loading_queue": "Indlæser kø...",
  "ui.loading_recent_items": "Indlæsning af seneste varer...",
  "ui.loading_schedules": "Indlæsning af tidsplaner...",
  "ui.local": "Lokalt",
  "ui.local_favorites": "Lokale favoritter",
  "ui.local_favorites_2": "Lokale favoritter",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "Lokal tilstand giver kun mening, når lokale likes er aktiveret.",
  "ui.low_light_listening": "Lytte i svagt lys",
  "ui.low_volume_and_quiet_mix": "Lav lydstyrke og stille mix",
  "ui.lower_volume_mood": "Stemning med lavere lydstyrke",
  "ui.lyrics": "Tekst",
  "ui.lyrics_earlier": "Tekst tidligere",
  "ui.lyrics_font_size": "Teksternes skriftstørrelse",
  "ui.lyrics_later": "Tekst senere",
  "ui.lyrics_timing": "Timing af tekster",
  "ui.lyrics_unavailable_right_now": "Teksten er ikke tilgængelig lige nu",
  "ui.ma_favorite_action_failed_with_error": "Music Assistant favorithandling mislykkedes{error}",
  "ui.ma_interface_path": "MA-grænsefladesti",
  "ui.main_bar": "Hovedbar",
  "ui.main_bar_items": "De vigtigste elementer i baren",
  "ui.main_bar_items_2": "De vigtigste elementer i baren",
  "ui.main_opacity": "Hovedopacitet",
  "ui.mainly_relevant_on_larger_layouts": "Hovedsageligt relevant på større layouts.",
  "ui.mass_queue_send_command_fallback": "mass_queue/send_command fallback",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Svarer til mikrofonindstillingen fra skærmen med kortindstillinger.",
  "ui.media": "Medier",
  "ui.media_2": "Medier",
  "ui.media_actions": "Handlinger i medierne",
  "ui.media_layout": "Medielayout",
  "ui.media_selected_target": "Udvalgt: {media} ? Mål: {mål}",
  "ui.menu": "Menu",
  "ui.message": "Besked",
  "ui.microphone": "Mikrofon",
  "ui.microphone_2": "Mikrofon",
  "ui.microphone_is_disabled": "Mikrofonen er deaktiveret",
  "ui.missing_url": "Manglende URL",
  "ui.mobile": "Mobil",
  "ui.mon": "Mon",
  "ui.mood_style_or_free_text": "Stemning, stil eller fri tekst",
  "ui.more_info": "Mere info",
  "ui.more_info_is_unavailable_for_this_player": "Mere information er ikke tilgængelig for denne Afspiller",
  "ui.morning": "Morgen",
  "ui.morning_mix": "Morgen-mix",
  "ui.move_to_next": "Gå videre til næste",
  "ui.move_to_position": "Flyt til position",
  "ui.music": "Musik",
  "ui.music_assistant_required": "Music Assistant påkrævet",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "Music Assistant config entry ID, hvis du vil have direkte integrationsopslag via Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "Music Assistant config entry blev ikke fundet",
  "ui.music_assistant_token": "Music Assistant token",
  "ui.music_assistant_url": "Music Assistant URL",
  "ui.music_hub": "Musikcenter",
  "ui.music_hub_2": "Musikcenter",
  "ui.mute": "Dæmpet",
  "ui.mute_command_failed": "Mute-kommando mislykkedes",
  "ui.my_studio_scene": "Min scene i studiet",
  "ui.name_or_keyword": "Navn eller nøgleord",
  "ui.name_this_studio_scene": "Nævn denne scene fra Studio.",
  "ui.netherlands": "Holland",
  "ui.new_schedule": "Ny tidsplan",
  "ui.new_wake_schedule": "Ny vagtplan",
  "ui.newest": "Nyeste",
  "ui.next": "Næste",
  "ui.next_track_failed": "Næste spor mislykkedes",
  "ui.night": "Nat",
  "ui.night_end_time": "Sluttidspunkt for natten",
  "ui.night_mode": "Nattilstand",
  "ui.night_mode_auto_window": "Nattilstand auto {start}-{slut}",
  "ui.night_mode_days": "Nattilstand dage",
  "ui.night_mode_is_always_on": "Nattilstand er altid slået til",
  "ui.night_mode_is_off": "Nattilstand er slået fra",
  "ui.night_mode_is_off_until_you_choose_another_mode": "Nattilstand er slået fra, indtil du vælger en anden tilstand.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "Nattilstand forbliver tændt, indtil du vælger en anden tilstand.",
  "ui.night_schedule_updated": "Aftenskema opdateret",
  "ui.night_start_time": "Starttidspunkt om natten",
  "ui.no_active_media": "Ingen aktive medier",
  "ui.no_active_players": "Ingen aktive Afspillere",
  "ui.no_available_players": "Ingen tilgængelige Afspillere",
  "ui.no_content_found": "Intet indhold fundet",
  "ui.no_dynamic_player_groups_to_disconnect": "Ingen dynamiske grupper af Afspillere til at afbryde forbindelsen",
  "ui.no_extra_ma_players": "Ingen ekstra MA-afspillere",
  "ui.no_extra_ma_players_2": "Ingen ekstra MA-afspillere",
  "ui.no_favorites_found": "Ingen favoritter fundet.",
  "ui.no_liked_media_yet": "Ingen likede medier endnu",
  "ui.no_liked_tracks_to_play": "Ingen foretrukne spor at spille",
  "ui.no_light_entities_found": "Ingen lysenheder fundet.",
  "ui.no_lyrics_found": "Ingen tekster fundet",
  "ui.no_matching_content_was_found": "Der blev ikke fundet noget matchende indhold",
  "ui.no_media_found": "Ingen medier fundet.",
  "ui.no_media_found_for_this_search": "Ingen medier fundet til denne søgning.",
  "ui.no_mix_content_found": "Intet blandet indhold fundet",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "Music Assistant er påkrævet. Aktivér Music Assistant-integrationen i Home Assistant og eksponer mindst én Music Assistant media_player. Andre Home Assistant-medieafspillere er med vilje skjult.",
  "ui.no_music_assistant_players_were_found_for_pinning": "Der blev ikke fundet nogen Music Assistant-afspillere til pinning.",
  "ui.no_next_track_in_queue": "Intet næste spor i køen",
  "ui.no_player_groups_to_disconnect": "Ingen grupper af Afspillere til at afbryde forbindelsen",
  "ui.no_player_light_pairs_yet": "Ingen Afspiller-light-par endnu.",
  "ui.no_player_selected": "Ingen Afspiller valgt",
  "ui.no_players_found": "Ingen Afspillere fundet",
  "ui.no_players_found_yet": "Ingen Afspillere fundet endnu",
  "ui.no_playlists_found": "Ingen Playlister fundet",
  "ui.no_queue": "Ingen kø",
  "ui.no_queue_item_to_rebuild": "Intet kø-element at genopbygge",
  "ui.no_queue_to_clone": "Ingen kø til at klone",
  "ui.no_radio_browser_countries_found": "Ingen Radio Browser-lande fundet",
  "ui.no_radio_stations_found": "Ingen radiostationer fundet",
  "ui.no_recent_listening_yet": "Ingen nylige lytninger endnu.",
  "ui.no_results": "Ingen resultater",
  "ui.no_results_found": "Ingen resultater fundet",
  "ui.no_saved_studio_scenes_yet": "Ingen gemte Studio-scener endnu.",
  "ui.no_scheduled_start_is_active": "Ingen planlagt start er aktiv",
  "ui.no_selected_tracks_to_play": "Ingen valgte spor at afspille",
  "ui.no_sleep_timer_is_active": "Ingen sleep-timer er aktiv",
  "ui.no_speech_was_captured": "Ingen tale blev optaget",
  "ui.no_target_players_available": "Ingen Afspillere til rådighed",
  "ui.no_target_players_available_2": "Ingen Afspillere til rådighed",
  "ui.no_tts_service_or_entity_is_configured": "Ingen TTS-tjeneste eller -enhed er konfigureret",
  "ui.no_wake_schedules_yet": "Ingen vagtplaner endnu",
  "ui.none": "Ingen",
  "ui.nothing_is_playing_right_now": "Der er ikke noget, der spiller lige nu",
  "ui.nothing_playing": "Intet spiller",
  "ui.now_playing": "Spiller nu",
  "ui.now_playing_2": "Spiller nu",
  "ui.off": "Fra",
  "ui.off_2": "Fra",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "Off deaktiverer den, On holder den aktiv, og Auto følger det konfigurerede tidsvindue.",
  "ui.oldest": "Ældste",
  "ui.on": "På",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "Et tryk forbereder afspillere, gruppering, lydstyrke og indhold. Gem dit eget aktuelle Studio-mål som en lokal scene.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Kun Handlinger for det aktuelle mål vises her.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Kun Music Assistant-afspillere vises her. Browser- og lokale Sendspin-afspillere er skjult for at undgå forvirring.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Kun nødvendigt, når du bruger en direkte Music Assistant URL.",
  "ui.opacity_for_popups_and_overlays": "Opacitet for popups og overlays.",
  "ui.opacity_for_the_main_card_background": "Opacitet for hovedkortets baggrund.",
  "ui.open_full_interface": "Åbn hele grænsefladen",
  "ui.open_full_queue": "Åbn hele Køen",
  "ui.open_interface_in": "Åben grænseflade i",
  "ui.open_ma": "Åben MA",
  "ui.open_music_assistant": "Åben Music Assistant",
  "ui.open_music_assistant_2": "Åben Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Åbn Music Assistant på denne enhed for at aktivere browserafspilleren",
  "ui.open_player_picker": "Åbn Afspiller-vælger",
  "ui.open_queue": "Åben Kø",
  "ui.open_saved_songs": "Åbn gemte sange",
  "ui.open_the_full_music_assistant_interface": "Åbne hele Music Assistant-grænsefladen?",
  "ui.open_the_full_music_assistant_interface_2": "Åbn hele Music Assistant-grænsefladen",
  "ui.opening_studio_library": "Åbning af Studio bibliotek",
  "ui.opening_timers": "Åbningstimere",
  "ui.option": "Mulighed",
  "ui.optional": "Valgfrit",
  "ui.optional_2": "Valgfrit",
  "ui.optional_entity_used_for_an_external_favorite_button": "Valgfri enhed, der bruges til en ekstern favoritknap.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Valgfri input_text-hjælper opdateret med den aktive afspillers entity_id til automatiseringer og skabeloner.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Valgfri input_text-hjælper opdateret med den aktive afspillers entity_id til automatiseringer og skabeloner.",
  "ui.other": "Andet",
  "ui.other_players": "Andre Afspillere",
  "ui.party": "Parti",
  "ui.path_used_when_opening_the_music_assistant_interface": "Sti, der bruges, når Music Assistant-grænsefladen åbnes.",
  "ui.pause": "Pause",
  "ui.paused": "Holdt pause",
  "ui.per_device_in_card": "Per enhed/på kort",
  "ui.performance_mode": "Performance-tilstand",
  "ui.performance_mode_for_weak_devices": "Performance-tilstand for svage enheder",
  "ui.performance_profile": "Performance-profil",
  "ui.performance_profile_helper": "Vælg, hvor meget visuelt arbejde kortet må udføre. Ultra Lite reducerer gennemsigtighed, sløring, dynamiske illustrationer og bevægelse for svage enheder.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Vælg en stemning, eller skriv din egen stil. Studio søger i Music Assistant og opbygger en kø.",
  "ui.pinned": "Fastgjort",
  "ui.pinned_players": "Afspillere, der sidder fast",
  "ui.play": "Spil",
  "ui.play_2": "Spil",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Spil et par spor, så vises de her.",
  "ui.play_a_saved_list": "Afspil en gemt liste",
  "ui.play_all": "Afspil alle",
  "ui.play_an_artist": "Spil en kunstner",
  "ui.play_artwork_action": "Spil kunstværk handling",
  "ui.play_everywhere": "Spil overalt",
  "ui.play_next": "Afspil næste",
  "ui.play_next_and_clear_queue": "Afspil næste og ryd køen",
  "ui.play_now": "Spil nu",
  "ui.play_now_and_clear_queue": "Spil nu og ryd køen",
  "ui.play_pause": "Afspil / Pause",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Afspil, sæt i kø, radio eller fjern favoritter direkte fra Studio.",
  "ui.play_selected": "Afspil valgt",
  "ui.playback_command_failed": "Afspilningskommando mislykkedes",
  "ui.playback_command_failed_2": "Afspilningskommando mislykkedes",
  "ui.playback_started": "Afspilning startet",
  "ui.player": "Afspiller",
  "ui.player_2": "Afspiller",
  "ui.player_count_many": "{Afspillere",
  "ui.player_count_one": "1 Afspiller",
  "ui.player_groups_could_not_be_disconnected": "Grupper af Afspillere kunne ikke afbrydes",
  "ui.player_is_pinned_from_settings": "Afspilleren er fastgjort fra indstillingerne",
  "ui.player_is_ready": "Afspilleren er klar",
  "ui.player_is_ready_nothing_is_playing_right_now": "Afspilleren er klar - intet spiller lige nu",
  "ui.player_order": "Afspillernes rækkefølge",
  "ui.player_on_this_device": "Afspiller på denne enhed",
  "ui.player_selected": "Afspiller udvalgt",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "Afspillerens indstillinger og DSP-forudindstillinger forbliver skrivebeskyttede, indtil Music Assistant udstiller et pålideligt skema for denne afspiller.",
  "ui.player_sort": "Sortering af Afspillere",
  "ui.players": "Afspillere",
  "ui.players_queue_and_groups": "Afspillere, kø og grupper",
  "ui.playing": "At spille",
  "ui.playing_a_random_playlist": "Afspilning af en tilfældig playliste",
  "ui.playing_a_random_radio_station": "Afspilning af en tilfældig radiostation",
  "ui.playing_on": "Spiller videre",
  "ui.playing_on_2": "Spiller videre",
  "ui.playlist": "Playliste",
  "ui.playlists": "Playlister",
  "ui.podcast": "Podcast",
  "ui.podcasts": "Podcasts",
  "ui.popup_opacity": "Popup-opacitet",
  "ui.preset_message": "Forudindstillet besked",
  "ui.previous": "Tidligere",
  "ui.primary": "Primær",
  "ui.primary_target": "Primært mål",
  "ui.pro_tools": "Pro-værktøjer",
  "ui.queue": "Kø",
  "ui.queue_2": "Kø",
  "ui.queue_action_failed": "Handling i Kø mislykkedes",
  "ui.queue_action_is_not_available_for_this_player": "Kø-handling er ikke tilgængelig for denne afspiller",
  "ui.queue_actions": "Handlinger i kø",
  "ui.queue_cleared": "Køen er ryddet",
  "ui.queue_cleared_and_item_moved_next": "Kø ryddet og næste emne flyttet",
  "ui.queue_cloned": "Kø klonet",
  "ui.queue_cockpit": "Kø cockpit",
  "ui.queue_cockpit_2": "Kø Cockpit",
  "ui.queue_is_empty": "Køen er tom",
  "ui.queue_is_unavailable_for_this_player": "Køen er ikke tilgængelig for denne afspiller",
  "ui.queue_item": "Emne i Kø",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "Køens ID er ikke klar endnu. Opdater Køen og prøv igen.",
  "ui.queue_results": "Resultater fra Kø",
  "ui.queue_transferred": "Kø overført",
  "ui.queues_refreshed": "Køer opdateres",
  "ui.quick_actions": "Hurtige Handlinger",
  "ui.quick_actions_2": "Hurtige Handlinger",
  "ui.quick_mix": "Hurtig blanding",
  "ui.quick_mix_started": "Hurtig blanding startet",
  "ui.quick_play": "Hurtig afspilning",
  "ui.radio": "Radio",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Radio Browser-lande",
  "ui.radio_browser_country": "Radio Browser land",
  "ui.radio_browser_country_2": "Radio Browser land",
  "ui.radio_mode_is_not_available_for_this_media_type": "Radio-tilstand er ikke tilgængelig for denne medietype",
  "ui.radio_mode_started": "Radio-tilstand startet",
  "ui.radio_station_changed": "Radiostation ændret",
  "ui.radio_stations": "Radio stationer",
  "ui.random": "Tilfældig",
  "ui.random_gentle_morning_mix": "Tilfældig blid morgenblanding",
  "ui.random_playlist": "Tilfældig playliste",
  "ui.random_radio": "Tilfældig radio",
  "ui.ready": "Klar",
  "ui.ready_to_play": "Klar til at spille",
  "ui.recent": "Seneste",
  "ui.recent_continue": "Seneste / Fortsæt",
  "ui.recent_listening": "Nylig lytning",
  "ui.recent_track": "Seneste spor",
  "ui.recently_added": "Nyligt tilføjet",
  "ui.recently_played": "Spillet for nylig",
  "ui.recently_played_2": "Spillet for nylig",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "Anbefalinger vises, når HOMEii Flow ser din kø eller seneste lytning.",
  "ui.recommended": "Anbefalet",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Anbefalet format: HH:MM som f.eks. 06:00. Krydsning af midnat understøttes.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Anbefalet format: HH:MM som f.eks. 22:00.",
  "ui.recommended_playlist": "Anbefalet playliste",
  "ui.recommended_track": "Anbefalet spor",
  "ui.reconnects_when_you_return_to_this_dashboard": "Opretter forbindelse igen, når du vender tilbage til dette dashboard",
  "ui.refresh": "Opdatering",
  "ui.remember_as_this_device": "Husk, at denne enhed",
  "ui.remove": "Fjerne",
  "ui.remove_from_selection": "Fjern fra valg",
  "ui.removed_from_liked": "Fjernet fra liked",
  "ui.removed_from_music_assistant_liked": "Fjernet fra Music Assistant kunne lide",
  "ui.reorder_queue": "Kø til genbestilling",
  "ui.repeat": "Gentag",
  "ui.repeat_all": "Gentag alle",
  "ui.repeat_off": "Gentag fra",
  "ui.repeat_one": "Gentag en",
  "ui.reset": "Nulstil",
  "ui.reset_lyrics_font_size": "Nulstil tekstens skriftstørrelse",
  "ui.reset_lyrics_timing": "Nulstil timing af tekster",
  "ui.results": "Resultater",
  "ui.rtl": "RTL",
  "ui.sat": "Lørdag",
  "ui.save_current_target": "Gem det aktuelle mål",
  "ui.save_schedule": "Gemme tidsplan",
  "ui.scene_players_are_not_available": "Afspillere på scenen er ikke tilgængelige",
  "ui.scene_presets": "forudindstillede scener",
  "ui.scene_presets_2": "Forudindstillede scener",
  "ui.scenes": "Scener",
  "ui.scales_interface_icons_without_changing_button_sizes": "Skalerer interface-ikoner uden at ændre knapstørrelser eller layout.",
  "ui.scheduled_start": "Planlagt start",
  "ui.scheduled_start_activated_label": "Planlagt start aktiveret: {label}.",
  "ui.scheduled_start_cleared": "Planlagt start ryddet",
  "ui.scheduled_start_failed": "Planlagt start mislykkedes",
  "ui.scheduled_start_saved": "Planlagt start gemt",
  "ui.scheduled_starts_active_count": "{antal} planlagte starter aktiv",
  "ui.schedules": "Skemaer",
  "ui.search": "Søg",
  "ui.search_and_choose_play_next_add_radio_or_like": "Søg og vælg Play, Next, Add, Radio eller Like.",
  "ui.search_everything": "Søg i alt...",
  "ui.search_failed": "Søgningen mislykkedes",
  "ui.search_in_queue": "Søg i Kø",
  "ui.search_queue_and_library": "Søg i kø og på biblioteket.",
  "ui.search_the_library": "Søg i biblioteket...",
  "ui.search_tracks_albums_artists_and_playlists": "Søg i spor, Albums, Kunstnere og Playlister",
  "ui.searching": "Søg...",
  "ui.searching_library": "Søg på biblioteket...",
  "ui.searching_smart_selection": "Søg efter et smart udvalg.",
  "ui.seek": "Søg efter",
  "ui.select": "Vælg",
  "ui.select_lights": "Vælg lys eller lys",
  "ui.select_player": "Vælg Afspiller",
  "ui.select_2": "Vælg",
  "ui.select_a_player_first": "Vælg en afspiller først",
  "ui.select_at_least_one_studio_player": "Vælg mindst én Studio-afspiller",
  "ui.select_at_least_two_players_to_create_a_group": "Vælg mindst to Afspillere til at danne en gruppe",
  "ui.selected": "Udvalgt",
  "ui.selected_music": "Udvalgt musik",
  "ui.selected_player": "Udvalgte Afspillere",
  "ui.selected_player_2": "Udvalgt Afspiller",
  "ui.selected_player_3": "Udvalgt Afspiller",
  "ui.selected_players_at_comfortable_volume": "Udvalgte afspillere med behagelig lydstyrke",
  "ui.selected_playlist": "Valgt Playliste",
  "ui.send_a_short_voice_message_or_announcement_url": "Send en kort talebesked eller meddelelses URL.",
  "ui.send_a_voice_message": "Send en talebesked",
  "ui.send_announcement": "Send meddelelse",
  "ui.sendspin_and_diagnostics": "Sendspin og diagnostik",
  "ui.set_sleep_timer": "Indstil sleep-timer",
  "ui.set_volume": "Indstil lydstyrken",
  "ui.set_the_visual_order_of_quick_actions": "Indstil den visuelle rækkefølge. Kun valgte Quick Handlinger vises; ikke-valgte elementer ignoreres.",
  "ui.settings": "Indstillinger",
  "ui.settings_source": "Kilde til indstillinger",
  "ui.show_ma_button": "Vis MA-knap",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Vis eller skjul den indbyggede række med næste spor i Spiller nu.",
  "ui.show_theme_toggle": "Skift til visning af tema",
  "ui.show_up_next": "Vis næste gang",
  "ui.show_up_next_2": "Vis næste gang",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Afspiller en selvstændig kompakt afspillerflise med illustrationer, grundlæggende kontroller, lydstyrke og en udvidelseshandling.",
  "ui.shuffle": "Shuffle",
  "ui.shuffle_all": "Bland alle",
  "ui.shuffle_favorites": "Bland favoritter",
  "ui.shuffle_play": "Shuffle-afspilning",
  "ui.skipped_to_up_next": "Sprang til næste side",
  "ui.sleep_timer": "Sleep-timer",
  "ui.sleep_timer_2": "Sleep-timer",
  "ui.sleep_timer_active_remaining": "Sleep-timer aktiv: {forbliver}.",
  "ui.sleep_timer_added_minutes": "Tilføjede {minutter} minutter til timeren",
  "ui.sleep_timer_and_morning_playback": "Sleep timer og morgenafspilning",
  "ui.sleep_timer_and_scheduled_playback": "Sleep-timer og planlagt afspilning",
  "ui.sleep_timer_cleared": "Sleep-timer ryddet",
  "ui.sleep_timer_finished": "Sleep-timer færdig",
  "ui.sleep_timer_presets": "Forudindstillede sleep-timer",
  "ui.sleep_timer_set_for_15_minutes": "Sleep-timer indstillet til 15 minutter",
  "ui.sleep_timer_set_minutes": "Sleep-timer indstillet til {minutter} minutter",
  "ui.sleep_timer_updated_minutes": "Sleep-timer opdateret til {minutter} minutter",
  "ui.smaller_lyrics": "Mindre tekster",
  "ui.smart": "Smart",
  "ui.smart_mix": "smart blanding",
  "ui.smart_mix_builder": "Smart Mix Builder",
  "ui.smart_voice_selection": "Smart valg af stemme",
  "ui.soft_and_relaxed": "Blød og afslappet",
  "ui.soft_relaxed_music": "Blød, afslappet musik",
  "ui.some_players_could_not_be_stopped_or_cleared": "Nogle Afspillere kunne ikke stoppes eller cleares",
  "ui.songs_around_an_artist": "Sange omkring en kunstner",
  "ui.sort": "Sortere",
  "ui.sort_ascending": "Sorter stigende",
  "ui.sort_descending": "Sorter nedadgående",
  "ui.source_queue": "Kø ved kilden",
  "ui.spain": "Spanien",
  "ui.speak_to_target_players": "Tal med Afspillere i målgruppen",
  "ui.start_radio_mode": "Start radio-tilstand",
  "ui.start_time": "Starttidspunkt",
  "ui.start_time_2": "Starttidspunkt",
  "ui.started_on_selected_player": "Startet på den valgte Afspiller",
  "ui.starting_a_chill_mix": "Starter en chill-mix",
  "ui.starting_quick_mix": "Starter Quick Mix...",
  "ui.stations": "Stationer",
  "ui.stay_active": "Hold dig aktiv",
  "ui.stays_active": "Forbliver aktiv",
  "ui.stop_all": "Stop alle",
  "ui.stop_all_players": "Stop alle Afspillere",
  "ui.stop_command_failed": "Stop-kommando mislykkedes",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Stop afspilning, ryd køer og afbryd grupper",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Stoppet alle afspillere, ryddet playlister og afbrudt grupper",
  "ui.stops_this_browser_player": "Stopper denne browser-afspiller",
  "ui.strong": "Stærk",
  "ui.studio": "Studio",
  "ui.studio_closed": "Studio lukket",
  "ui.studio_media_action_failed": "Studio media action mislykkedes",
  "ui.studio_mix_started": "Mix i Studio er startet",
  "ui.studio_opened": "Studio åbnede",
  "ui.studio_pro": "Studio Pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Scene i Studio",
  "ui.studio_scene_deleted": "Scene i studiet slettet",
  "ui.studio_scene_saved": "Studio-scene gemt",
  "ui.studio_scene_was_not_found": "Studio-scenen blev ikke fundet",
  "ui.studio_search": "Søg i studio",
  "ui.studio_shortcut": "Genvej til Studio",
  "ui.subtle": "Subtilt",
  "ui.sun": "Solen",
  "ui.surprise_me": "Overrask mig",
  "ui.sync": "Synkroniser",
  "ui.sync_lyrics": "Synkroniserede tekster",
  "ui.tablet": "Tablet",
  "ui.tap_to_start_a_sleep_timer": "Tryk for at starte en sleep-timer",
  "ui.target_players": "Afspillere som målgruppe",
  "ui.target_queue": "Mål kø",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Tekstmeddelelser bruger Home Assistant TTS i den valgte Music Assistant-afspiller. URL-meddelelser bruger Music Assistant-meddelelsesafspilning.",
  "ui.text_only": "Kun tekst",
  "ui.text_tone": "Tekst tone",
  "ui.theme": "Tema",
  "ui.theme_2": "Tema",
  "ui.theme_mode": "Tilstand Tema",
  "ui.theme_toggle": "Skift mellem temaer",
  "ui.theme_toggle_2": "Skift mellem temaer",
  "ui.this_device": "Denne enhed",
  "ui.this_device_2": "Denne enhed",
  "ui.this_device_player_connected": "Denne enhed Afspiller tilsluttet",
  "ui.this_device_player_disconnected": "Afspilleren af denne enhed er frakoblet",
  "ui.thu": "Torsdag",
  "ui.timer": "Timer",
  "ui.timer_active_remaining": "Timer aktiv: {resterende}",
  "ui.timers": "Timere",
  "ui.to": "Til",
  "ui.today": "I dag",
  "ui.track": "Spor",
  "ui.track_lyrics": "Spor Tekst",
  "ui.tracks": "Spor",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Overfør, klon, inspicer eller ryd køer uden at skjule Studio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "Overførsel er ikke tilgængelig, mens en fastgjort afspiller er konfigureret",
  "ui.transfer_queue": "Kø til overførsel",
  "ui.transfer_queue_2": "Kø til overførsel",
  "ui.transfer_queue_3": "Kø til overførsel",
  "ui.try_again": "Prøv igen",
  "ui.tts_entity": "TTS-enhed",
  "ui.tts_entity_used_by_the_announcement_screen": "TTS-enhed, der bruges af annonceringsskærmen.",
  "ui.tts_language": "TTS sprog",
  "ui.tue": "Tirsdag",
  "ui.turkey": "Tyrkiet",
  "ui.turn_off": "Sluk",
  "ui.turns_off_after_run": "Slukker efter kørsel",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Slår tunge billeder fra, så kortet kører mere jævnt på Nest Hub, ældre tablets og svagere skærme.",
  "ui.type_a_free_style_first": "Skriv en fri stil først",
  "ui.type_an_announcement": "Skriv en meddelelse...",
  "ui.type_anything": "Skriv hvad som helst",
  "ui.type_what_should_be_announced": "Skriv, hvad der skal annonceres...",
  "ui.ungroup": "Ungroup",
  "ui.ungroup_2": "Ungroup",
  "ui.united_kingdom": "Det Forenede Kongerige",
  "ui.united_states": "De Forenede Stater",
  "ui.unknown": "Ukendt",
  "ui.up": "Op",
  "ui.move_down": "Bevæg dig nedad",
  "ui.move_up": "Bevæg dig opad",
  "ui.up_next": "Næste gang",
  "ui.up_next_2": "Næste",
  "ui.up_next_title": "Næste gang: {titel}",
  "ui.upbeat_and_moving": "Upbeat og bevægende",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Brug en hvilken som helst sti til Home Assistant. Eksempel: /lovelace/hjem",
  "ui.use_current_view_for_home_button": "Brug den aktuelle visning som startknap",
  "ui.use_expand_to_open_the_full_player": "Brug udvid for at åbne hele afspilleren",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Bruger den samme basislandeliste, som vises på skærmen med kortindstillinger.",
  "ui.visible_tiles": "synlige fliser",
  "ui.visible_tiles_2": "Synlige fliser",
  "ui.visual_editor": "Visuel editor",
  "ui.assist_agent": "Hjælp agenten",
  "ui.assist_agent_id": "Hjælp agentens ID",
  "ui.assist_only": "Kun assistere",
  "ui.default_assist_agent": "Standard Home Assistant Assist",
  "ui.default_order": "Standardordre",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Anbefalet: Musikkommandoer kører lokalt på den valgte afspiller. Alt, hvad kortet ikke forstår, sendes til Home Assistant Assist.",
  "ui.hybrid_music_plus_assist": "Hybrid: Musik + Assist",
  "ui.icon_size": "Ikonstørrelse",
  "ui.music_only": "Kun musik",
  "ui.music_plus_assist": "Music + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "Valgfrit ID for Assist-agent. Lad det være tomt for at bruge Home Assistants standard Assist-agent.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Det er valgfrit. Lad dette være på standardagenten, medmindre du har flere Assist-agenter og ønsker, at dette kort skal bruge en bestemt agent.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Viser en push-to-talk-knap til musikkommandoer og Assist fallback.",
  "ui.show_home_inside_quick_actions": "Viser Hjem som en hurtig handling i stedet for en flydende knap.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Vis plus- og minusknapper ved siden af skyderen for lydstyrke.",
  "ui.speak_voice_assistant_responses_out_loud": "Tal det genkendte svar højt gennem denne browser, når det understøttes.",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "Stemmeassistent",
  "ui.voice_assistant_disabled": "Stemmeassistent er deaktiveret",
  "ui.voice_assistant_listening": "Lytter efter en kommando...",
  "ui.voice_assistant_mode": "Stemmeassistent-tilstand",
  "ui.voice_command_executed": "Stemmekommando udført",
  "ui.voice_command_completed_action": "Færdig: {Handlinger}",
  "ui.voice_command_failed": "Stemmekommando mislykkedes",
  "ui.voice_command_no_player": "Vælg en afspiller, eller sig afspillerens navn.",
  "ui.voice_command_not_understood": "Jeg kunne ikke forstå den kommando.",
  "ui.voice_command_sent": "Stemmekommando sendt",
  "ui.voice_done_status": "Færdig",
  "ui.voice_error_status": "Har brug for opmærksomhed",
  "ui.voice_feedback": "Stemme-feedback",
  "ui.voice_group_connect_needs_players": "Sig mindst to spilleres navne, eller vælg en spiller og sig den anden.",
  "ui.voice_group_connected_players": "Forbundet {primary} med {members}.",
  "ui.voice_group_disconnect_needs_player": "Sig, hvilken gruppe afspillere der skal afbrydes.",
  "ui.voice_group_disconnected_player": "Afbrød gruppen for {afspiller}.",
  "ui.voice_input_failed": "Stemmeinput mislykkedes",
  "ui.voice_input_is_not_supported_on_this_device": "Stemmeinput er ikke understøttet på denne enhed",
  "ui.voice_language_is_not_supported": "Denne browser understøtter ikke det valgte sprog til talegenkendelse.",
  "ui.voice_match_not_confident": 'Jeg fandt {title}, men den matcher ikke "{query}" tæt nok. Prøv at sige sangen og kunstneren igen.',
  "ui.voice_music_searching": "Søg efter musik: {query}",
  "ui.voice_playing_result": "Spiller {titel}",
  "ui.voice_queue_transfer_needs_players": "Sig kilde- og målafspiller, for eksempel: overfør kø fra stue til køkken.",
  "ui.voice_queue_transferred_between": "Kø overført fra {kilde} til {mål}.",
  "ui.voice_starting_playback": "Starter {titel}...",
  "ui.voice_processing_status": "Behandler kommando...",
  "ui.voice_ready_status": "Klar",
  "ui.voice_recognition_network_failed": "Talegenkendelsestjenesten er ikke tilgængelig. Mikrofontilladelsen er muligvis OK; tjek browseren, VPN, DNS, firewall eller netværk.",
  "ui.voice_response": "Svar",
  "ui.voice_response_will_appear_here": "Svaret vil blive vist her.",
  "ui.voice_search": "Stemmesøgning",
  "ui.voice_search_failed": "Stemmesøgning mislykkedes",
  "ui.voice_search_is_not_supported_on_this_device": "Stemmesøgning er ikke understøttet på denne enhed",
  "ui.voice_transcript": "Udskrift",
  "ui.voice_listening_status": "Lytter...",
  "ui.waiting_for_speech": "Venter på tale...",
  "ui.microphone_permission_or_browser_blocked": "Browseren har blokeret for talegenkendelse. Tjek mikrofonens og browserens indstillinger for tale.",
  "ui.volume": "Lydstyrke",
  "ui.volume_and_player_target": "Lydstyrke og mål for afspillere",
  "ui.volume_boost": "Øget lydstyrke",
  "ui.volume_control_large_screen_only": "Kontrol af Lydstyrke (kun stor skærm)",
  "ui.volume_down": "Lydstyrke ned",
  "ui.volume_mode": "Lydstyrke",
  "ui.volume_presets": "Forudindstillinger af lydstyrke",
  "ui.volume_step_between_1_and_10_percent_default_5": "Lydstyrke mellem 1 % og 10 %. Standard er 5%.",
  "ui.volume_step_buttons": "Lydstyrke +/- knapper",
  "ui.volume_step_percent": "Lydstyrke trin",
  "ui.volume_up": "Lydstyrke op",
  "ui.waiting_for_this_device_player": "Venter på denne Afspiller...",
  "ui.wake": "Vågn op",
  "ui.wake_schedules": "Vækningsplaner",
  "ui.wed": "Onsdag",
  "ui.week": "Uge",
  "ui.what_would_you_like_to_listen_to": "Hvad vil du gerne lytte til?",
  "ui.will_play_next": "Vil spille næste gang",
  "ui.will_play_next_in_studio": "Vil spille næste gang i Studio",
  "ui.worldwide_popular": "Populær på verdensplan",
  "ui.add_player_light_pair": "Tilføj par",
  "ui.ambient_light": "Omgivende lys",
  "ui.ambient_light_brightness": "Lysstyrke",
  "ui.ambient_light_cooldown": "Nedkøling af lysopdatering",
  "ui.ambient_light_entities": "Lys i standardfarve",
  "ui.ambient_light_player_map": "Afspiller -> kortlægning af lys",
  "ui.ambient_light_player_map_helper": "Brug afspilleren og farve-lysvælgerne nedenfor. Hver afsluttet række bliver et par; ryd en afspiller eller dens lys for at fjerne parret.",
  "ui.ambient_light_transition": "Let overgang",
  "ui.analog": "Analog",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Vælg kun farvekompatible lys, der skal følge musikken, når der ikke findes et afspiller-specifikt par.",
  "ui.digital": "Digital",
  "ui.discover_albums": "Opdag Albums",
  "ui.discover_music": "Opdag",
  "ui.discover_subtitle": "Anbefalede playlister, albums og radio.",
  "ui.discovery": "Opdagelse",
  "ui.discovery_failed": "Opdagelsestilstand mislykkedes",
  "ui.discovery_mode": "Opdagelsestilstand",
  "ui.discovery_started": "Opdagelsen er begyndt: {stemning}",
  "ui.maximum_brightness_for_music_lighting": "Maksimal lysstyrke til musikbelysning.",
  "ui.minimum_seconds_between_light_updates": "Minimum sekunder mellem lysopdateringer.",
  "ui.player_light_pair_lights": "Farve lys {antal}",
  "ui.player_light_pair_lights_helper": "Vælg farveegnede lys til denne Music Assistant-afspiller. Ryd dette felt eller Afspilleren for at fjerne parret.",
  "ui.player_light_pair_player": "Music Assistant afspiller {nummer}",
  "ui.player_light_pair_player_helper": "Vælg Music Assistant-afspilleren til dette lyspar.",
  "ui.all": "Alle",
  "ui.all_genres": "Alle genrer",
  "ui.genres_and_styles": "Genrer og stilarter",
  "ui.compact_widget_mode": "Kompakt widget-stil",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Vælg, hvornår kompakt tilstand bruger den mindre mobil-widget med to rækker.",
  "ui.full": "Fuld",
  "ui.mini_widget": "Mini widget",
  "ui.genre": "Genre",
  "ui.mood_and_genre": "Stemning og genre",
  "ui.music_by_mood_and_genre": "Musik efter humør og genre",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "Valgfri enhed til hjælpeknappen. Lad den være tom for at stoppe den aktive Afspiller.",
  "ui.optional_name_for_the_auxiliary_button": "Valgfri etiket, der vises som værktøjstip for hjælpeknappen.",
  "ui.choose_the_auxiliary_button_icon": "Vælg det viste ikon for hjælpeknappen.",
  "ui.choose_how_players_are_sorted": "Vælg standard, alfabetisk eller brugerdefineret rækkefølge for Afspillere.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Vælg Music Assistant-afspillere, der aldrig må optræde på kortet.",
  "ui.custom_order": "Brugerdefineret ordre",
  "ui.no_visible_music_assistant_players_after_exclusions": "Alle Music Assistant-afspillere er skjult af indstillingen for udelukkede afspillere.",
  "ui.set_custom_player_order": "Indstil den brugerdefinerede rækkefølge for afspillere. Tomme pladser ignoreres.",
  "ui.show_auxiliary_button_in_quick_actions": "Viser denne hjælpeknap i rækken med hurtige Handlinger.",
  "ui.power": "Kraft",
  "ui.power_action_failed": "Strømhandling mislykkedes",
  "ui.power_action_sent": "Power action sendt",
  "ui.power_button": "Hjælpeknap",
  "ui.power_button_action": "Handling af hjælpeknap",
  "ui.power_button_entity": "Hjælpeknap-enhed",
  "ui.scene": "Scene",
  "ui.screensaver": "Screensaver",
  "ui.screensaver_buttons": "Knapper til pauseskærme",
  "ui.screensaver_controls": "Kontrol af pauseskærme",
  "ui.screensaver_clock_mode": "Urtilstand for pauseskærm",
  "ui.screensaver_clock_size": "Størrelse på pauseskærm-ur",
  "ui.screensaver_clock_size_helper": "Skalerer uret på tablettens pauseskærm uden at flytte kunstværket.",
  "ui.screensaver_clock_x": "Urets position - VENSTRE / HØJRE",
  "ui.screensaver_clock_x_helper": "Lavere værdier flyttes til venstre, højere værdier til højre.",
  "ui.screensaver_clock_y": "Urets position - OP / NED",
  "ui.screensaver_clock_y_helper": "Lavere værdier rykker op, højere værdier rykker ned.",
  "ui.screensaver_message": "Besked om pauseskærm",
  "ui.screensaver_timeout": "Timeout for pauseskærm",
  "ui.script": "Manuskript",
  "ui.short_optional_message_shown_on_the_screensaver": "Kort valgfri besked, der vises på pauseskærmen.",
  "ui.show_previous_next_controls_on_the_screensaver": "Vis forrige og næste knap på pauseskærmen.",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Vælg præcis, hvilke knapper der vises på pauseskærmen, herunder FLOW ASSISTANT, forrige og næste.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Vis et roligt ur og illustrationer efter inaktiv tid.",
  "ui.show_power_button_in_the_player_controls": "Vis en hjælpeknap i hurtige Handlinger.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Vis opdagelsestilstanden i fuld skærm i Handlinger.",
  "ui.smart_home": "Smart hjem",
  "ui.soft_transition_time_for_music_lighting": "Blød overgangstid til musikbelysning.",
  "ui.stop_player": "Stop Afspilleren",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Synkroniser valgte lys med den aktuelle kunstfarve.",
  "ui.toggle": "Toggle",
  "ui.turn_on": "Tænd for",
  "ui.choose_a_category_to_browse_provider_playlists": "Vælg en kategori for at gennemse udbydernes playlister, før du spiller.",
  "ui.new_and_recommended": "Nyt og anbefalet",
  "ui.provider_playlists": "Udbyderens playlister",
  "ui.hotel_mode": "Hotel Mode",
  "ui.hotel_mode_helper": "Minimal hotelsikker brugergrænseflade: kun afspillerkontrol, lydstyrke, søgning, gennemsyn af kunstværker og valg af afspiller.",
  "ui.style_and_genres": "Stil og genrer"
});
const messages$5 = Object.freeze({
  "ui.a_guided_music_wizard": "Un asistente de música guiado",
  "ui.a_radio_station": "Una estación de radio",
  "ui.accent_color": "Color de acento",
  "ui.actions": "acciones",
  "ui.actions_2": "Acciones",
  "ui.active_days": "Días activos",
  "ui.active_player_helper": "Asistente de reproductor activo",
  "ui.active_player_helper_must_be_an_input_text_entity": "El asistente de reproductor activo debe ser una entidad input_text",
  "ui.active_players": "Reproductores activos",
  "ui.add": "Agregar",
  "ui.add_to_queue": "Agregar a la cola",
  "ui.add_to_selection": "Agregar a la selección",
  "ui.added_to_liked": "Agregado a Me gusta",
  "ui.added_to_music_assistant_liked": "Agregado a Me gusta de Music Assistant",
  "ui.added_to_queue": "Agregado a la cola",
  "ui.added_to_studio_queue": "Agregado a la cola del estudio",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Agrega movimiento a las capas de fondo. Sutil lo mantiene calmado, Fuerte es notorio y Extremo es el aspecto animado más audaz.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Aumenta el volumen actual solo durante los anuncios, luego restaura el volumen anterior.",
  "ui.after_run": "Después de ejecutar",
  "ui.album": "Álbum",
  "ui.albums": "Álbumes",
  "ui.all_albums": "Todos los álbumes",
  "ui.all_artists": "Todos los artistas",
  "ui.all_countries": "Todos los países",
  "ui.all_player_groups_disconnected": "Todos los grupos de reproductores desconectados",
  "ui.all_players": "Todos los reproductores",
  "ui.all_players_2": "todos los reproductores",
  "ui.all_podcasts": "Todos los podcasts",
  "ui.all_tracks": "Todas las canciones",
  "ui.allow_local_likes": "Permitir favoritos locales",
  "ui.always_visible": "Siempre visible",
  "ui.announce": "Anunciar",
  "ui.announce_to_all_players": "Anunciar a todos los reproductores",
  "ui.announcement": "Anuncio",
  "ui.announcement_failed_with_error": "Error en el anuncio: {error}",
  "ui.announcement_language": "Idioma del anuncio",
  "ui.announcement_presets": "Ajustes preestablecidos de anuncios",
  "ui.announcement_sent_to_player": "Anuncio enviado a {player}",
  "ui.announcement_studio": "estudio de anuncios",
  "ui.announcement_studio_2": "Estudio de anuncios",
  "ui.announcement_target": "Objetivo del anuncio",
  "ui.announcement_to_player_preview": "Anuncio para {player}: {preview}",
  "ui.announcement_volume_boost": "Amplificación de volumen del anuncio",
  "ui.announcements": "Anuncios",
  "ui.appearance": "Apariencia",
  "ui.apply_group": "Aplicar grupo",
  "ui.apply_schedule": "Aplicar programación",
  "ui.artist": "Artista",
  "ui.artist_radio": "Radio de artista",
  "ui.artists": "Artistas",
  "ui.artwork": "Arte de portada",
  "ui.artwork_swipe": "Deslizar arte de portada",
  "ui.ascending": "Ascendente",
  "ui.at_least_one_player_must_stay_selected": "Al menos un reproductor debe permanecer seleccionado",
  "ui.australia": "Australia",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Selecciona automáticamente móvil o tableta según el ancho real.",
  "ui.auto_cloud_default": "Automático / Predeterminado de la nube",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Extrae automáticamente los colores del arte actual y mantiene el efecto sutil. Fuerte hace que la paleta sea más rica y brillante.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Mantiene Automáticamente los valores predeterminados de voz de Home Assistant Cloud sin cambios. Las opciones manuales envían un idioma explícito.",
  "ui.available": "Disponible",
  "ui.back": "Atrás",
  "ui.back_2": "Atrás",
  "ui.back_to_countries": "Volver a países",
  "ui.back_to_queue": "Volver a la cola",
  "ui.background_motion": "Movimiento de fondo",
  "ui.behavior": "Comportamiento",
  "ui.browse_covers": "Explorar portadas",
  "ui.browse_playlists_artists_albums_and_radio": "Explorar listas de reproducción, artistas, álbumes y radio",
  "ui.browser_players": "Reproductores del navegador",
  "ui.build_custom_mix": "Crear mezcla personalizada",
  "ui.building_studio_mix": "Creando mezcla de estudio...",
  "ui.button": "Botón",
  "ui.by_mood": "Por estado de ánimo",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Duración de la caché en milisegundos para solicitudes de datos seleccionadas.",
  "ui.cache_ttl": "TTL de caché",
  "ui.calm": "Calma",
  "ui.canada": "Canadá",
  "ui.cancel": "Cancelar",
  "ui.cancel_2": "Cancelar",
  "ui.cancel_selection": "Cancelar selección",
  "ui.card_configuration": "Configuración de la tarjeta",
  "ui.card_height": "Altura de la tarjeta",
  "ui.card_height_in_pixels": "Altura de la tarjeta en píxeles.",
  "ui.card_settings_are_managed_from_the_visual_editor": "La configuración de la tarjeta se administra desde el editor visual",
  "ui.change_song": "Cambiar canción",
  "ui.change_track": "Cambiar pista",
  "ui.chill_mix": "Mezcla relajante",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Selecciona un país o 'Todos los países' para explorar todos los países dentro de la pestaña Radio.",
  "ui.choose_a_queue_position": "Selecciona una posición en la cola",
  "ui.choose_a_source_player": "Selecciona un reproductor de origen",
  "ui.choose_a_target_player": "Selecciona un reproductor de destino",
  "ui.choose_at_least_one_player": "Selecciona al menos un reproductor",
  "ui.choose_icons_text_or_both_for_the_footer": "Selecciona iconos, texto o ambos para el pie de página.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Selecciona solo reproductores de Music Assistant. La lista de fijados dentro de la tarjeta solo muestra reproductores de Music Assistant para evitar confusiones.",
  "ui.choose_one_clear_option": "Selecciona una opción para limpiar",
  "ui.choose_player": "Seleccionar reproductor",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Selecciona algo del estante rápido o toca la varita para una lista de reproducción aleatoria.",
  "ui.choose_source": "Seleccionar origen",
  "ui.choose_target": "Seleccionar destino",
  "ui.choose_target_player": "Seleccionar reproductor de destino",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Selecciona el color de acento para el diseño móvil.",
  "ui.choose_the_music": "Seleccionar la música",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Selecciona los botones pequeños que se muestran debajo del título de la canción. El temporizador aparece primero en hebreo para que se ubique en el lado derecho.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Selecciona primero el origen. La reproducción utilizará el destino actual.",
  "ui.choose_where_to_play": "Seleccionar dónde reproducir",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Selecciona si deslizar el arte de portada cambia de pista o explora portadas.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Selecciona si la configuración se controla desde la interfaz de usuario de la tarjeta o desde la configuración de la tarjeta en el panel.",
  "ui.choose_which_actions_appear_in_the_main_bar": "Selecciona qué acciones aparecen en la barra principal.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Selecciona a qué días se aplica la ventana del modo nocturno automático.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Selecciona qué iconos aparecen en la fila de acciones rápidas. Limpiar todo solicita confirmación antes de desconectar los reproductores activos.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Selecciona qué reproductores aparecen como mosaicos en la sala.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Selecciona qué reproductores permanecen en la selección de control actual.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Selecciona qué pestañas están disponibles en la pantalla de la biblioteca.",
  "ui.choose_who_is_controlled": "Selecciona a quién controla",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "Al seleccionar Visual, se desactiva la pantalla de configuración dentro de la tarjeta.",
  "ui.clean_all": "Limpiar todo",
  "ui.clean_all_confirm_action": "Limpiar todo",
  "ui.clean_all_confirm_copy": "Esto desconectará todos los reproductores activos, detendrá la reproducción y vaciará sus colas. ¿Deseas continuar?",
  "ui.clean_all_confirm_title": "¿Limpiar todos los reproductores?",
  "ui.clean_the_studio_wall": "Limpiar el muro del estudio",
  "ui.clear": "Limpiar",
  "ui.clear_queue": "Vaciar cola",
  "ui.clear_search": "Limpiar búsqueda",
  "ui.clone_queue": "Clonar cola",
  "ui.close": "Cerrar",
  "ui.collapse_compact_player": "Contraer reproductor compacto",
  "ui.compact_mode": "Modo compacto",
  "ui.configure_ready_made_announcement_phrases": "Configurar frases de anuncios preestablecidas.",
  "ui.connect_this_device": "Conectar este dispositivo",
  "ui.connect_this_device_2": "Conectar este dispositivo",
  "ui.connected": "Conectado",
  "ui.connected_2": "Conectado",
  "ui.connected_players": "reproductores conectados",
  "ui.connected_players_2": "Reproductores conectados",
  "ui.connecting": "Conectando",
  "ui.connecting_2": "Conectando...",
  "ui.confirm": "Confirmar",
  "ui.connection_and_behavior": "Conexión y comportamiento",
  "ui.connects_directly_from_this_card_using_sendspin": "Se conecta directamente desde esta tarjeta usando Sendspin",
  "ui.continue": "Continuar",
  "ui.continue_2": "Continuar",
  "ui.continue_from_recent_music_assistant_activity": "Continuar desde la actividad reciente de Music Assistant.",
  "ui.continue_listening": "Continuar escuchando",
  "ui.continue_what_was_played_recently": "Continuar lo que se reprodujo recientemente",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Controla si el fondo de la tarjeta se mueve suavemente y qué tan fuerte se siente el movimiento.",
  "ui.controlling": "Controlando",
  "ui.could_not_build_quick_mix_from_the_current_song": "No se pudo crear la mezcla rápida desde la canción actual",
  "ui.could_not_build_studio_mix": "No se pudo crear la mezcla de estudio",
  "ui.could_not_choose_media": "No se pudo seleccionar el medio",
  "ui.could_not_find_music": "No se pudo encontrar música",
  "ui.could_not_clone_the_queue": "No se pudo clonar la cola",
  "ui.could_not_play_label": "No se pudo reproducir: {label}",
  "ui.could_not_start_chill_mix": "No se pudo iniciar la mezcla relajante",
  "ui.could_not_start_playback": "No se pudo iniciar la reproducción",
  "ui.could_not_start_playback_2": "No se pudo iniciar la reproducción",
  "ui.could_not_start_playback_in_studio": "No se pudo iniciar la reproducción en el estudio",
  "ui.could_not_start_quick_mix": "No se pudo iniciar la mezcla rápida",
  "ui.could_not_start_radio_playback": "No se pudo iniciar la reproducción de radio",
  "ui.could_not_transfer_the_queue": "No se pudo transferir la cola",
  "ui.could_not_update_active_player_helper": "No se pudo actualizar el asistente de reproductor activo",
  "ui.create_schedule": "Crear programación",
  "ui.current_path": "Ruta actual",
  "ui.custom": "Personalizado",
  "ui.dark": "Oscuro",
  "ui.delete_scene": "Eliminar escena",
  "ui.delete_schedule": "Eliminar programación",
  "ui.descending": "Descendente",
  "ui.dictate": "Dictar",
  "ui.disabled": "Desactivado",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Desactiva el desenfoque, las animaciones, los fondos dinámicos y las sombras pesadas. Recomendado para Nest Hub y tabletas menos potentes.",
  "ui.disconnect": "Desconectar",
  "ui.disconnect_all": "Desconectar todos",
  "ui.disconnect_groups": "Desconectar grupos",
  "ui.disconnect_player_groups": "Desconectar grupos de reproductores",
  "ui.disconnect_this_device": "Desconectar este dispositivo",
  "ui.disconnected": "Desconectado",
  "ui.disconnecting": "Desconectando",
  "ui.disconnecting_player_groups": "Desconectando grupos de reproductores...",
  "ui.discover": "Descubrir",
  "ui.down": "Abajo",
  "ui.dynamic_theme": "Tema dinámico",
  "ui.edit_schedule": "Editar programación",
  "ui.enable": "Activar",
  "ui.enable_or_disable_the_footer_search_button": "Activa o desactiva el botón de búsqueda en el pie de página.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Activa o desactiva el botón del estudio en la barra del pie de página.",
  "ui.enable_the_floating_home_shortcut_button": "Activa el botón flotante de acceso directo de inicio.",
  "ui.enabled": "Activado",
  "ui.enables_switching_to_local_liked_mode": "Permite cambiar al modo de Me gusta locales.",
  "ui.end_time": "Hora de finalización",
  "ui.energetic": "Energético",
  "ui.energy_and_rhythm": "Energía y ritmo",
  "ui.enter_an_announcement_first": "Ingresa un anuncio primero",
  "ui.example_quiet_jazz_greek_music_workout": "Ejemplo: jazz tranquilo, música griega, entrenamiento",
  "ui.existing_content": "Contenido existente",
  "ui.expand_player": "Expandir reproductor",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Extrae los colores del arte de portada actual y los aplica a la interfaz.",
  "ui.extreme": "Extremo",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Alternativa para configuraciones que necesitan una ruta de servicio de cola diferente.",
  "ui.fallback_mode": "Modo alternativo",
  "ui.family_friendly": "Familiar",
  "ui.favorite": "Favorito",
  "ui.favorite_action_failed": "Error en la acción de favorito",
  "ui.favorite_action_sent": "Acción de favorito enviada",
  "ui.favorite_button_entity": "Entidad del botón de favorito",
  "ui.favorite_center": "centro de favoritos",
  "ui.favorite_center_2": "Centro de favoritos",
  "ui.favorite_radio": "Radio favorita",
  "ui.favorite_updated": "Favorito actualizado",
  "ui.favorites_and_liked_music": "Favoritos y música que te gusta",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Detección de características, contexto del reproductor y herramientas de reproducción para este dispositivo.",
  "ui.find_music": "Buscar música",
  "ui.finding_music": "Buscando música",
  "ui.fixed": "Fijo",
  "ui.flow_mix": "Mezcla FLOW",
  "ui.font_scale": "Escala de fuente",
  "ui.font_size": "Tamaño de fuente",
  "ui.footer_search": "Búsqueda en el pie de página",
  "ui.footer_style": "Estilo del pie de página",
  "ui.footer_style_2": "Estilo del pie de página",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "Por ejemplo /lovelace/home o cualquier otra ruta de Home Assistant.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "Para pantallas menos potentes: desactiva el desenfoque intenso, las animaciones, la extracción del tema dinámico y el movimiento de fondo.",
  "ui.france": "Francia",
  "ui.free_style": "Estilo libre",
  "ui.free_style_quiet_jazz_greek_music_workout": "Estilo libre: jazz tranquilo, música griega, entrenamiento...",
  "ui.fresh_start": "Nuevo comienzo",
  "ui.fresh_start_2": "Nuevo comienzo",
  "ui.fri": "Vie",
  "ui.friendly_and_simple": "Amigable y simple",
  "ui.from": "De",
  "ui.general": "General",
  "ui.general_and_display": "General y Pantalla",
  "ui.germany": "Alemania",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Escala global para todas las fuentes de la interfaz. 1 es el tamaño predeterminado.",
  "ui.greece": "Grecia",
  "ui.grid": "Cuadrícula",
  "ui.group": "Grupo",
  "ui.group_cleared": "Grupo eliminado",
  "ui.group_cleared_2": "Grupo eliminado",
  "ui.group_speakers": "Agrupar altavoces",
  "ui.group_speakers_2": "Agrupar altavoces",
  "ui.group_updated": "Grupo actualizado",
  "ui.group_volume": "Volumen del grupo",
  "ui.group_volume_up_energetic_mix": "Grupo, subir volumen, mezcla energética",
  "ui.grouped_players": "Reproductores agrupados",
  "ui.hebrew": "Hebreo",
  "ui.history": "Historial",
  "ui.history_tabs": "Pestañas de historial",
  "ui.home": "Inicio",
  "ui.home_party_night_presets": "Ajustes preestablecidos para el hogar, fiestas o la noche",
  "ui.home_path": "Ruta de inicio",
  "ui.home_scene_prepared": "Escena de inicio preparada",
  "ui.home_shortcut": "Acceso directo de inicio",
  "ui.home_shortcut_path": "Ruta del acceso directo de inicio",
  "ui.icon_and_text": "Icono y texto",
  "ui.icon_only": "Solo icono",
  "ui.icon_plus_text": "Icono + texto",
  "ui.idle": "Inactivo",
  "ui.idle_2": "Inactivo",
  "ui.in_card_ui": "Interfaz de la tarjeta",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Incluye el modo de tema personalizado desde la pantalla de configuración dentro de la tarjeta.",
  "ui.info": "Información",
  "ui.israel": "Israel",
  "ui.israeli": "Israelí",
  "ui.italy": "Italia",
  "ui.items": "elementos",
  "ui.join_selected_players": "Unir reproductores seleccionados",
  "ui.kids": "Niños",
  "ui.language": "Idioma",
  "ui.larger_lyrics": "Letras más grandes",
  "ui.layout_adapted_to_height": "Diseño adaptado a la altura",
  "ui.layout_mode": "Modo de diseño",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Dejar vacío si la tarjeta debe usar solo Home Assistant.",
  "ui.library": "Biblioteca",
  "ui.library_2": "Biblioteca",
  "ui.library_pages": "Páginas de la biblioteca",
  "ui.default_library_layout": "Diseño predeterminado de la biblioteca",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Elige cómo se abren las páginas de biblioteca. Aún puedes cambiar entre cuadrícula/lista dentro de la biblioteca.",
  "ui.library_radio": "Radio de la biblioteca",
  "ui.library_surprise": "Sorpresa de la biblioteca",
  "ui.library_tabs": "Pestañas de la biblioteca",
  "ui.library_tabs_2": "Pestañas de la biblioteca",
  "ui.light": "Claro",
  "ui.like": "Me gusta",
  "ui.like_2": "Me gusta",
  "ui.like_3": "Me gusta",
  "ui.liked": "Te gusta",
  "ui.liked_mode": "Modo de Me gusta",
  "ui.liked_sync": "Sincronización de Me gusta",
  "ui.list": "Lista",
  "ui.listening": "Escuchando...",
  "ui.loading": "Cargando...",
  "ui.loading_favorites": "Cargando favoritos...",
  "ui.loading_library": "Cargando biblioteca...",
  "ui.loading_lyrics": "Cargando letras...",
  "ui.loading_players": "Cargando reproductores...",
  "ui.loading_queue": "Cargando cola...",
  "ui.loading_recent_items": "Cargando recientes...",
  "ui.loading_schedules": "Cargando programaciones...",
  "ui.local": "Local",
  "ui.local_favorites": "Favoritos locales",
  "ui.local_favorites_2": "Favoritos locales",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "El modo local solo tiene sentido cuando los favoritos locales están activados.",
  "ui.low_light_listening": "Escucha con luz tenue",
  "ui.low_volume_and_quiet_mix": "Volumen bajo y mezcla tranquila",
  "ui.lower_volume_mood": "Ambiente de volumen bajo",
  "ui.lyrics": "Letras",
  "ui.lyrics_earlier": "Adelantar letras",
  "ui.lyrics_font_size": "Tamaño de fuente de las letras",
  "ui.lyrics_later": "Retrasar letras",
  "ui.lyrics_timing": "Sincronización de letras",
  "ui.lyrics_unavailable_right_now": "Letras no disponibles en este momento",
  "ui.ma_favorite_action_failed_with_error": "Error en la acción de favorito de Music Assistant: {error}",
  "ui.ma_interface_path": "Ruta de la interfaz de MA",
  "ui.main_bar": "Barra principal",
  "ui.main_bar_items": "Elementos de la barra principal",
  "ui.main_bar_items_2": "Elementos de la barra principal",
  "ui.main_opacity": "Opacidad principal",
  "ui.mainly_relevant_on_larger_layouts": "Relevante principalmente en diseños más grandes.",
  "ui.mass_queue_send_command_fallback": "Alternativa de mass_queue/send_command",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Coincide con la configuración del micrófono de la pantalla de configuración interna de la tarjeta.",
  "ui.media": "Medios",
  "ui.media_2": "Medios",
  "ui.media_actions": "Acciones de medios",
  "ui.media_layout": "Diseño de medios",
  "ui.media_selected_target": "Seleccionado: {media} | Destino: {target}",
  "ui.menu": "Menú",
  "ui.message": "Mensaje",
  "ui.microphone": "Micrófono",
  "ui.microphone_2": "Micrófono",
  "ui.microphone_is_disabled": "El micrófono está desactivado",
  "ui.missing_url": "Falta la URL",
  "ui.mobile": "Móvil",
  "ui.mon": "Lun",
  "ui.mood_style_or_free_text": "Estado de ánimo, estilo o texto libre",
  "ui.more_info": "Más información",
  "ui.more_info_is_unavailable_for_this_player": "La información detallada no está disponible para este reproductor",
  "ui.morning": "Mañana",
  "ui.morning_mix": "Mezcla matutina",
  "ui.move_to_next": "Mover a la posición siguiente",
  "ui.move_to_position": "Mover a la posición",
  "ui.music": "Música",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "ID de entrada de configuración de Music Assistant, si deseas la búsqueda de integración directa a través de Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "No se encontró la entrada de configuración de Music Assistant",
  "ui.music_assistant_token": "Token de Music Assistant",
  "ui.music_assistant_url": "URL de Music Assistant",
  "ui.music_hub": "centro de música",
  "ui.music_hub_2": "Centro de música",
  "ui.mute": "Silenciar",
  "ui.mute_command_failed": "Error en el comando de silencio",
  "ui.my_studio_scene": "Mi escena de estudio",
  "ui.name_or_keyword": "Nombre o palabra clave",
  "ui.name_this_studio_scene": "Nombra esta escena de estudio...",
  "ui.netherlands": "Países Bajos",
  "ui.new_schedule": "Nueva programación",
  "ui.new_wake_schedule": "Nueva programación de despertar",
  "ui.newest": "Más reciente",
  "ui.next": "Siguiente",
  "ui.next_track_failed": "Error al pasar a la pista siguiente",
  "ui.night": "Noche",
  "ui.night_end_time": "Hora de fin de la noche",
  "ui.night_mode": "Modo nocturno",
  "ui.night_mode_auto_window": "Modo nocturno automático {start}-{end}",
  "ui.night_mode_days": "Días del modo nocturno",
  "ui.night_mode_is_always_on": "El modo nocturno está siempre activado",
  "ui.night_mode_is_off": "El modo nocturno está desactivado",
  "ui.night_mode_is_off_until_you_choose_another_mode": "El modo nocturno estará desactivado hasta que selecciones otro modo.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "El modo nocturno permanecerá activado hasta que selecciones otro modo.",
  "ui.night_schedule_updated": "Programación nocturna actualizada",
  "ui.night_start_time": "Hora de inicio de la noche",
  "ui.no_active_media": "Sin medios activos",
  "ui.no_active_players": "Sin reproductores activos",
  "ui.no_available_players": "Sin reproductores disponibles",
  "ui.no_content_found": "No se encontró contenido",
  "ui.no_dynamic_player_groups_to_disconnect": "No hay grupos dinámicos de reproductores para desconectar",
  "ui.no_extra_ma_players": "Sin reproductores de MA adicionales",
  "ui.no_extra_ma_players_2": "Sin reproductores de MA adicionales",
  "ui.no_favorites_found": "No se encontraron favoritos.",
  "ui.no_liked_media_yet": "Aún no hay medios marcados como Me gusta",
  "ui.no_liked_tracks_to_play": "No hay pistas Me gusta para reproducir",
  "ui.no_light_entities_found": "No se encontraron entidades de luz.",
  "ui.no_lyrics_found": "No se encontraron letras",
  "ui.no_matching_content_was_found": "No se encontró contenido coincidente",
  "ui.no_media_found": "No se encontraron medios.",
  "ui.no_media_found_for_this_search": "No se encontraron medios para esta búsqueda.",
  "ui.no_mix_content_found": "No se encontró contenido para la mezcla",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "No se encontraron reproductores de Music Assistant. Verifica Music Assistant y las entidades media_player.",
  "ui.no_music_assistant_players_were_found_for_pinning": "No se encontraron reproductores de Music Assistant para fijar.",
  "ui.no_next_track_in_queue": "No hay una pista siguiente en la cola",
  "ui.no_player_groups_to_disconnect": "No hay grupos de reproductores para desconectar",
  "ui.no_player_light_pairs_yet": "Aún no hay pares de reproductor-luz.",
  "ui.no_player_selected": "Ningún reproductor seleccionado",
  "ui.no_players_found": "No se encontraron reproductores",
  "ui.no_players_found_yet": "Aún no se encontraron reproductores",
  "ui.no_playlists_found": "No se encontraron listas de reproducción",
  "ui.no_queue": "Sin cola",
  "ui.no_queue_item_to_rebuild": "No hay ningún elemento de la cola para reconstruir",
  "ui.no_queue_to_clone": "No hay ninguna cola para clonar",
  "ui.no_radio_browser_countries_found": "No se encontraron países en Radio Browser",
  "ui.no_radio_stations_found": "No se encontraron estaciones de radio",
  "ui.no_recent_listening_yet": "Aún no hay reproducciones recientes.",
  "ui.no_results": "Sin resultados",
  "ui.no_results_found": "No se encontraron resultados",
  "ui.no_saved_studio_scenes_yet": "Aún no hay escenas de estudio guardadas.",
  "ui.no_scheduled_start_is_active": "No hay ningún inicio programado activo",
  "ui.no_selected_tracks_to_play": "No hay pistas seleccionadas para reproducir",
  "ui.no_sleep_timer_is_active": "No hay ningún temporizador de apagado activo",
  "ui.no_speech_was_captured": "No se capturó voz",
  "ui.no_target_players_available": "No hay reproductores de destino disponibles",
  "ui.no_target_players_available_2": "No hay reproductores de destino disponibles",
  "ui.no_tts_service_or_entity_is_configured": "No se ha configurado ninguna entidad o servicio TTS",
  "ui.no_wake_schedules_yet": "Aún no hay programaciones de despertar",
  "ui.none": "Ninguno",
  "ui.nothing_is_playing_right_now": "No se está reproduciendo nada en este momento",
  "ui.nothing_playing": "Nada en reproducción",
  "ui.now_playing": "Reproduciendo ahora",
  "ui.now_playing_2": "Reproduciendo ahora",
  "ui.off": "Apagado",
  "ui.off_2": "Apagado",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "Apagado lo desactiva, Encendido lo mantiene activo y Automático sigue la ventana de tiempo configurada.",
  "ui.oldest": "Más antiguo",
  "ui.on": "Encendido",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "Un toque prepara los reproductores, la agrupación, el volumen y el contenido. Guarda tu destino actual del estudio como una escena local.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Aquí solo se muestran las acciones para el destino actual.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Aquí solo se muestran los reproductores de Music Assistant. Los reproductores del navegador y locales de Sendspin están ocultos para evitar confusiones.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Solo es necesario cuando se utiliza una URL directa de Music Assistant.",
  "ui.opacity_for_popups_and_overlays": "Opacidad para ventanas emergentes y superposiciones.",
  "ui.opacity_for_the_main_card_background": "Opacidad para el fondo de la tarjeta principal.",
  "ui.open_full_interface": "Abrir interfaz completa",
  "ui.open_full_queue": "Abrir cola completa",
  "ui.open_interface_in": "Abrir interfaz en",
  "ui.open_ma": "Abrir MA",
  "ui.open_music_assistant": "Abrir Music Assistant",
  "ui.open_music_assistant_2": "¿Abrir Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Abre Music Assistant en este dispositivo para activar el reproductor del navegador",
  "ui.open_player_picker": "Abrir selector de reproductor",
  "ui.open_queue": "Abrir cola",
  "ui.open_saved_songs": "Abrir canciones guardadas",
  "ui.open_the_full_music_assistant_interface": "¿Abrir la interfaz completa de Music Assistant?",
  "ui.open_the_full_music_assistant_interface_2": "Abrir la interfaz completa de Music Assistant",
  "ui.opening_studio_library": "Abriendo biblioteca del estudio",
  "ui.opening_timers": "Abriendo temporizadores",
  "ui.option": "Opción",
  "ui.optional": "Opcional",
  "ui.optional_2": "Opcional",
  "ui.optional_entity_used_for_an_external_favorite_button": "Entidad opcional utilizada para un botón de favorito externo.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Asistente input_text opcional actualizado con el ID de entidad del reproductor activo para automatizaciones y plantillas.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Asistente input_text opcional actualizado con el ID de entidad del reproductor activo para automatizaciones y plantillas.",
  "ui.other": "Otro",
  "ui.other_players": "Otros reproductores",
  "ui.party": "Fiesta",
  "ui.path_used_when_opening_the_music_assistant_interface": "Ruta utilizada al abrir la interfaz de Music Assistant.",
  "ui.pause": "Pausar",
  "ui.paused": "Pausado",
  "ui.per_device_in_card": "Por dispositivo / en la tarjeta",
  "ui.performance_mode": "Modo rendimiento",
  "ui.performance_mode_for_weak_devices": "Modo rendimiento para dispositivos poco potentes",
  "ui.performance_profile": "Perfil de rendimiento",
  "ui.performance_profile_helper": "Elige cuánta carga visual puede usar la tarjeta. Ultra Lite reduce transparencia, desenfoque, fondos dinámicos y movimiento para dispositivos poco potentes.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Elige un estado de ánimo o escribe tu propio estilo. El estudio buscará en Music Assistant y creará una cola.",
  "ui.pinned": "Fijado",
  "ui.pinned_players": "Reproductores fijados",
  "ui.play": "Reproducir",
  "ui.play_2": "Reproducir",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Reproduce algunas pistas y aparecerán aquí.",
  "ui.play_a_saved_list": "Reproducir una lista guardada",
  "ui.play_all": "Reproducir todo",
  "ui.play_an_artist": "Reproducir un artista",
  "ui.play_artwork_action": "Acción al tocar el arte de portada",
  "ui.play_everywhere": "Reproducir en todas partes",
  "ui.play_next": "Reproducir a continuación",
  "ui.play_next_and_clear_queue": "Reproducir a continuación y vaciar cola",
  "ui.play_now": "Reproducir ahora",
  "ui.play_now_and_clear_queue": "Reproducir ahora y vaciar cola",
  "ui.play_pause": "Reproducir / Pausar",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Reproduce, añade a la cola, inicia radio o elimina favoritos directamente desde el estudio.",
  "ui.play_selected": "Reproducir seleccionados",
  "ui.playback_command_failed": "Error en el comando de reproducción",
  "ui.playback_command_failed_2": "Error en el comando de reproducción",
  "ui.playback_started": "Reproducción iniciada",
  "ui.player": "reproductor",
  "ui.player_2": "Reproductor",
  "ui.player_count_many": "{count} reproductores",
  "ui.player_count_one": "1 reproductor",
  "ui.player_groups_could_not_be_disconnected": "No se pudieron desconectar los grupos de reproductores",
  "ui.player_is_pinned_from_settings": "El reproductor está fijado desde la configuración",
  "ui.player_is_ready": "El reproductor está listo",
  "ui.player_is_ready_nothing_is_playing_right_now": "El reproductor está listo - no se está reproduciendo nada en este momento",
  "ui.player_on_this_device": "Reproductor en este dispositivo",
  "ui.player_selected": "Reproductor seleccionado",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "La configuración del reproductor y los ajustes preestablecidos de DSP permanecen de solo lectura hasta que Music Assistant exponga un esquema confiable para este reproductor.",
  "ui.players": "Reproductores",
  "ui.players_queue_and_groups": "Reproductores, cola y grupos",
  "ui.playing": "Reproduciendo",
  "ui.playing_a_random_playlist": "Reproduciendo una lista de reproducción aleatoria",
  "ui.playing_a_random_radio_station": "Reproduciendo una estación de radio aleatoria",
  "ui.playing_on": "Reproduciendo en",
  "ui.playing_on_2": "Reproduciendo en",
  "ui.playlist": "Lista de reproducción",
  "ui.playlists": "Listas de reproducción",
  "ui.podcast": "Podcast",
  "ui.podcasts": "Podcasts",
  "ui.popup_opacity": "Opacidad de ventanas emergentes",
  "ui.preset_message": "Mensaje preestablecido",
  "ui.previous": "Anterior",
  "ui.primary": "Primario",
  "ui.primary_target": "Destino primario",
  "ui.pro_tools": "Herramientas pro",
  "ui.queue": "Cola",
  "ui.queue_2": "Cola",
  "ui.queue_action_failed": "Error en la acción de la cola",
  "ui.queue_action_is_not_available_for_this_player": "La acción de la cola no está disponible para este reproductor",
  "ui.queue_actions": "Acciones de la cola",
  "ui.queue_cleared": "Cola vaciada",
  "ui.queue_cleared_and_item_moved_next": "Cola vaciada y elemento movido a continuación",
  "ui.queue_cloned": "Cola clonada",
  "ui.queue_cockpit": "cabina de la cola",
  "ui.queue_cockpit_2": "Cabina de la cola",
  "ui.queue_is_empty": "La cola está vacía",
  "ui.queue_is_unavailable_for_this_player": "La cola no está disponible para este reproductor",
  "ui.queue_item": "Elemento de la cola",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "El ID del elemento de la cola aún no está listo. Actualiza la cola e inténtalo de nuevo.",
  "ui.queue_results": "Resultados de la cola",
  "ui.queue_transferred": "Cola transferida",
  "ui.queues_refreshed": "Colas actualizadas",
  "ui.quick_actions": "Acciones rápidas",
  "ui.quick_actions_2": "Acciones rápidas",
  "ui.quick_mix": "Mezcla rápida",
  "ui.quick_mix_started": "Mezcla rápida iniciada",
  "ui.quick_play": "Reproducción rápida",
  "ui.radio": "Radio",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Países de Radio Browser",
  "ui.radio_browser_country": "País de Radio Browser",
  "ui.radio_browser_country_2": "País de Radio Browser",
  "ui.radio_mode_is_not_available_for_this_media_type": "El modo de radio no está disponible para este tipo de medio",
  "ui.radio_mode_started": "Modo de radio iniciado",
  "ui.radio_station_changed": "Estación de radio cambiada",
  "ui.radio_stations": "Estaciones de radio",
  "ui.random": "Aleatorio",
  "ui.random_gentle_morning_mix": "Mezcla matutina suave aleatoria",
  "ui.random_playlist": "Lista de reproducción aleatoria",
  "ui.random_radio": "Radio aleatoria",
  "ui.ready": "Listo",
  "ui.ready_to_play": "Listo para reproducir",
  "ui.recent": "Reciente",
  "ui.recent_continue": "Reciente / Continuar",
  "ui.recent_listening": "reproducciones recientes",
  "ui.recent_track": "Pista reciente",
  "ui.recently_added": "Agregado recientemente",
  "ui.recently_played": "Reproducido recientemente",
  "ui.recently_played_2": "Reproducido recientemente",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "Las recomendaciones aparecerán una vez que HOMEii Flow detecte tu cola o tus reproducciones recientes.",
  "ui.recommended": "Recomendado",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Formato recomendado: HH:MM como 06:00. Se admite el cruce de medianoche.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Formato recomendado: HH:MM como 22:00.",
  "ui.recommended_playlist": "Lista de reproducción recomendada",
  "ui.recommended_track": "Pista recomendada",
  "ui.reconnects_when_you_return_to_this_dashboard": "Se reconecta cuando regresas a este panel",
  "ui.refresh": "Actualizar",
  "ui.remember_as_this_device": "Recordar como este dispositivo",
  "ui.remove": "Quitar",
  "ui.remove_from_selection": "Quitar de la selección",
  "ui.removed_from_liked": "Quitado de Me gusta",
  "ui.removed_from_music_assistant_liked": "Quitado de Me gusta de Music Assistant",
  "ui.reorder_queue": "Reordenar cola",
  "ui.repeat": "Repetir",
  "ui.repeat_all": "Repetir todo",
  "ui.repeat_off": "Repetición desactivada",
  "ui.repeat_one": "Repetir una",
  "ui.reset": "Restablecer",
  "ui.reset_lyrics_font_size": "Restablecer tamaño de fuente de letras",
  "ui.reset_lyrics_timing": "Restablecer sincronización de letras",
  "ui.results": "Resultados",
  "ui.rtl": "RTL",
  "ui.sat": "Sáb",
  "ui.save_current_target": "Guardar destino actual",
  "ui.save_schedule": "Guardar programación",
  "ui.scene_players_are_not_available": "Los reproductores de la escena no están disponibles",
  "ui.scene_presets": "ajustes preestablecidos de escena",
  "ui.scene_presets_2": "Ajustes preestablecidos de escena",
  "ui.scenes": "Escenas",
  "ui.scales_interface_icons_without_changing_button_sizes": "Escala los iconos de la interfaz sin cambiar el tamaño de los botones ni el diseño.",
  "ui.scheduled_start": "Inicio programado",
  "ui.scheduled_start_activated_label": "Inicio programado activado: {label}",
  "ui.scheduled_start_cleared": "Inicio programado cancelado",
  "ui.scheduled_start_failed": "Error en el inicio programado",
  "ui.scheduled_start_saved": "Inicio programado guardado",
  "ui.scheduled_starts_active_count": "{count} inicios programados activos",
  "ui.schedules": "Programaciones",
  "ui.search": "Buscar",
  "ui.search_and_choose_play_next_add_radio_or_like": "Busca y selecciona Reproducir, Siguiente, Agregar, Radio o Me gusta.",
  "ui.search_everything": "Buscar todo...",
  "ui.search_failed": "Búsqueda fallida",
  "ui.search_in_queue": "Buscar en la cola",
  "ui.search_queue_and_library": "Buscar en la cola y en la biblioteca...",
  "ui.search_the_library": "Buscar en la biblioteca...",
  "ui.search_tracks_albums_artists_and_playlists": "Buscar pistas, álbumes, artistas y listas de reproducción",
  "ui.searching": "Buscando...",
  "ui.searching_library": "Buscando en la biblioteca...",
  "ui.searching_smart_selection": "Buscando en la selección inteligente...",
  "ui.seek": "Posición",
  "ui.select": "Seleccionar",
  "ui.select_lights": "Seleccionar luz o luces",
  "ui.select_player": "Seleccionar reproductor",
  "ui.select_2": "Seleccionar",
  "ui.select_a_player_first": "Selecciona un reproductor primero",
  "ui.select_at_least_one_studio_player": "Selecciona al menos un reproductor del estudio",
  "ui.select_at_least_two_players_to_create_a_group": "Selecciona al menos dos reproductores para crear un grupo",
  "ui.selected": "Seleccionado",
  "ui.selected_music": "Música seleccionada",
  "ui.selected_player": "Reproductor seleccionado",
  "ui.selected_player_2": "Reproductor seleccionado",
  "ui.selected_player_3": "Reproductor seleccionado",
  "ui.selected_players_at_comfortable_volume": "Reproductores seleccionados a un volumen cómodo",
  "ui.selected_playlist": "Lista de reproducción seleccionada",
  "ui.send_a_short_voice_message_or_announcement_url": "Envía un mensaje de voz corto o una URL de anuncio.",
  "ui.send_a_voice_message": "Enviar un mensaje de voz",
  "ui.send_announcement": "Enviar anuncio",
  "ui.sendspin_and_diagnostics": "Sendspin y diagnósticos",
  "ui.set_sleep_timer": "Configurar temporizador de apagado",
  "ui.set_volume": "Configurar volumen",
  "ui.settings": "Configuración",
  "ui.settings_source": "Origen de la configuración",
  "ui.show_ma_button": "Mostrar botón de MA",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Muestra u oculta la fila integrada de la siguiente pista en Reproduciendo ahora.",
  "ui.show_theme_toggle": "Mostrar alternancia de tema",
  "ui.show_up_next": "Mostrar a continuación",
  "ui.show_up_next_2": "Mostrar a continuación",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Muestra un mosaico independiente de reproductor compacto con arte de portada, controles básicos, volumen y una acción de expansión.",
  "ui.shuffle": "Aleatorio",
  "ui.shuffle_all": "Todo en aleatorio",
  "ui.shuffle_favorites": "Favoritos en aleatorio",
  "ui.shuffle_play": "Reproducción aleatoria",
  "ui.skipped_to_up_next": "Saltado a lo siguiente",
  "ui.sleep_timer": "Temporizador de apagado",
  "ui.sleep_timer_2": "Temporizador de apagado",
  "ui.sleep_timer_active_remaining": "Temporizador de apagado activo: {remaining}",
  "ui.sleep_timer_added_minutes": "Se agregaron {minutes} minutos al temporizador",
  "ui.sleep_timer_and_morning_playback": "Temporizador de apagado y reproducción matutina",
  "ui.sleep_timer_and_scheduled_playback": "Temporizador de apagado y reproducción programada",
  "ui.sleep_timer_cleared": "Temporizador de apagado cancelado",
  "ui.sleep_timer_finished": "Temporizador de apagado finalizado",
  "ui.sleep_timer_presets": "Ajustes preestablecidos del temporizador de apagado",
  "ui.sleep_timer_set_for_15_minutes": "Temporizador de apagado configurado para 15 minutos",
  "ui.sleep_timer_set_minutes": "Temporizador de apagado configurado para {minutes} minutos",
  "ui.sleep_timer_updated_minutes": "Temporizador de apagado actualizado a {minutes} minutos",
  "ui.smaller_lyrics": "Letras más pequeñas",
  "ui.smart": "Inteligente",
  "ui.smart_mix": "mezcla inteligente",
  "ui.smart_mix_builder": "Creador de mezclas inteligentes",
  "ui.smart_voice_selection": "Selección de voz inteligente",
  "ui.soft_and_relaxed": "Suave y relajado",
  "ui.soft_relaxed_music": "Música suave y relajada",
  "ui.some_players_could_not_be_stopped_or_cleared": "Algunos reproductores no se pudieron detener o vaciar",
  "ui.songs_around_an_artist": "Canciones relacionadas con un artista",
  "ui.sort": "Ordenar",
  "ui.sort_ascending": "Ordenar de forma ascendente",
  "ui.sort_descending": "Ordenar de forma descendente",
  "ui.source_queue": "Cola de origen",
  "ui.spain": "España",
  "ui.speak_to_target_players": "Hablar a los reproductores de destino",
  "ui.start_radio_mode": "Iniciar modo de radio",
  "ui.start_time": "Hora de inicio",
  "ui.start_time_2": "Hora de inicio",
  "ui.started_on_selected_player": "Iniciado en el reproductor seleccionado",
  "ui.starting_a_chill_mix": "Iniciando una mezcla relajante",
  "ui.starting_quick_mix": "Iniciando mezcla rápida...",
  "ui.stations": "estaciones",
  "ui.stay_active": "Permanecer activo",
  "ui.stays_active": "Permanece activo",
  "ui.stop_all": "Detener todos",
  "ui.stop_all_players": "Detener todos los reproductores",
  "ui.stop_command_failed": "Error en el comando de detención",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Detener la reproducción, vaciar las colas y desconectar los grupos",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Se detuvieron todos los reproductores, se vaciaron las listas de reproducción y se desconectaron los grupos",
  "ui.stops_this_browser_player": "Detiene este reproductor del navegador",
  "ui.strong": "Fuerte",
  "ui.studio": "Estudio",
  "ui.studio_closed": "Estudio cerrado",
  "ui.studio_media_action_failed": "Error en la acción de medios del estudio",
  "ui.studio_mix_started": "Mezcla de estudio iniciada",
  "ui.studio_opened": "Estudio abierto",
  "ui.studio_pro": "estudio pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Escena de estudio",
  "ui.studio_scene_deleted": "Escena de estudio eliminada",
  "ui.studio_scene_saved": "Escena de estudio guardada",
  "ui.studio_scene_was_not_found": "No se encontró la escena de estudio",
  "ui.studio_search": "búsqueda de estudio",
  "ui.studio_shortcut": "Acceso directo del estudio",
  "ui.subtle": "Sutil",
  "ui.sun": "Dom",
  "ui.surprise_me": "Sorpréndeme",
  "ui.sync": "Sincronizar",
  "ui.sync_lyrics": "Sincronizar letras",
  "ui.tablet": "Tableta",
  "ui.tap_to_start_a_sleep_timer": "Toca para iniciar un temporizador de apagado",
  "ui.target_players": "Reproductores de destino",
  "ui.target_queue": "Cola de destino",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Los anuncios de texto utilizan el TTS de Home Assistant en el reproductor de Music Assistant seleccionado. Los anuncios de URL utilizan la reproducción de anuncios de Music Assistant.",
  "ui.text_only": "Solo texto",
  "ui.text_tone": "Tono del texto",
  "ui.theme": "Tema",
  "ui.theme_2": "Tema",
  "ui.theme_mode": "Modo del tema",
  "ui.theme_toggle": "Alternancia de tema",
  "ui.theme_toggle_2": "Alternancia de tema",
  "ui.this_device": "Este dispositivo",
  "ui.this_device_2": "Este dispositivo",
  "ui.this_device_player_connected": "Reproductor de este dispositivo conectado",
  "ui.this_device_player_disconnected": "Reproductor de este dispositivo desconectado",
  "ui.thu": "Jue",
  "ui.timer": "Temporizador",
  "ui.timer_active_remaining": "Temporizador activo: {remaining}",
  "ui.timers": "Temporizadores",
  "ui.to": "A",
  "ui.today": "Hoy",
  "ui.track": "Pista",
  "ui.track_lyrics": "Letras de la pista",
  "ui.tracks": "Pistas",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Transfiere, clona, inspecciona o vacía colas sin ocultar el estudio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "La transferencia no está disponible mientras haya un reproductor fijado configurado",
  "ui.transfer_queue": "Transferir cola",
  "ui.transfer_queue_2": "Transferir cola",
  "ui.transfer_queue_3": "Transferir cola",
  "ui.try_again": "Intentar de nuevo",
  "ui.tts_entity": "Entidad TTS",
  "ui.tts_entity_used_by_the_announcement_screen": "Entidad TTS utilizada por la pantalla de anuncios.",
  "ui.tts_language": "Idioma TTS",
  "ui.tue": "Mar",
  "ui.turkey": "Turquía",
  "ui.turn_off": "Apagar",
  "ui.turns_off_after_run": "Se apaga después de ejecutar",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Desactiva los elementos visuales pesados para que la tarjeta funcione de manera más fluida en Nest Hub, tabletas más antiguas y pantallas menos potentes.",
  "ui.type_a_free_style_first": "Escribe un estilo libre primero",
  "ui.type_an_announcement": "Escribe un anuncio...",
  "ui.type_anything": "Escribe algo",
  "ui.type_what_should_be_announced": "Escribe lo que se debe anunciar...",
  "ui.ungroup": "Desagrupar",
  "ui.ungroup_2": "Desagrupar",
  "ui.united_kingdom": "Reino Unido",
  "ui.united_states": "Estados Unidos",
  "ui.unknown": "Desconocido",
  "ui.up": "Arriba",
  "ui.up_next": "A continuación",
  "ui.up_next_2": "A continuación",
  "ui.up_next_title": "A continuación: {title}",
  "ui.upbeat_and_moving": "Alegre y enérgico",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Usa cualquier ruta de Home Assistant. Ejemplo: /lovelace/home",
  "ui.use_current_view_for_home_button": "Usar la vista actual para el botón de inicio",
  "ui.use_expand_to_open_the_full_player": "Usa expandir para abrir el reproductor completo",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Utiliza la misma lista de países base que se muestra en la pantalla de configuración interna de la tarjeta.",
  "ui.visible_tiles": "mosaicos visibles",
  "ui.visible_tiles_2": "Mosaicos visibles",
  "ui.visual_editor": "Editor visual",
  "ui.assist_agent": "Agente de Assist",
  "ui.assist_agent_id": "ID del agente de Assist",
  "ui.assist_only": "Solo Assist",
  "ui.default_assist_agent": "Assist predeterminado de Home Assistant",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Recomendado: los comandos de música se ejecutan localmente en el reproductor seleccionado. Cualquier comando que la tarjeta no entienda se envía a Assist de Home Assistant.",
  "ui.hybrid_music_plus_assist": "Híbrido: Música + Assist",
  "ui.icon_size": "Tamaño del icono",
  "ui.music_only": "Solo música",
  "ui.music_plus_assist": "Música + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "ID del agente de Assist opcional. Deja vacío para usar el agente de Assist predeterminado de Home Assistant.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Opcional. Déjalo en el agente predeterminado a menos que tengas múltiples agentes de Assist y quieras que esta tarjeta use uno específico.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Muestra un botón de presionar para hablar para comandos de música y alternativa de Assist.",
  "ui.speak_voice_assistant_responses_out_loud": "Reproducir en voz alta las respuestas reconocidas a través de este navegador cuando sea compatible.",
  "ui.flow_assistant": "ASISTENTE FLOW",
  "ui.voice_assistant": "Asistente de voz",
  "ui.voice_assistant_disabled": "El asistente de voz está desactivado",
  "ui.voice_assistant_listening": "Escuchando un comando...",
  "ui.voice_assistant_mode": "Modo del asistente de voz",
  "ui.voice_command_executed": "Comando de voz ejecutado",
  "ui.voice_command_completed_action": "Listo: {action}",
  "ui.voice_command_failed": "Error en el comando de voz",
  "ui.voice_command_no_player": "Selecciona o di el nombre del reproductor.",
  "ui.voice_command_not_understood": "No entendí el comando.",
  "ui.voice_command_sent": "Comando de voz enviado",
  "ui.voice_done_status": "Listo",
  "ui.voice_error_status": "Requiere atención",
  "ui.voice_feedback": "Retroalimentación de voz",
  "ui.voice_group_connect_needs_players": "Di al menos dos nombres de reproductores, o selecciona uno y di el otro.",
  "ui.voice_group_connected_players": "Se conectó {primary} con {members}",
  "ui.voice_group_disconnect_needs_player": "Di qué grupo de reproductores deseas desconectar.",
  "ui.voice_group_disconnected_player": "Se desconectó el grupo para {player}",
  "ui.voice_input_failed": "Error en la entrada de voz",
  "ui.voice_input_is_not_supported_on_this_device": "La entrada de voz no es compatible con este dispositivo",
  "ui.voice_language_is_not_supported": "Este navegador no admite el idioma de reconocimiento de voz seleccionado.",
  "ui.voice_match_not_confident": 'Encontré {title}, pero no coincide lo suficiente con "{query}". Intenta decir la canción y el artista de nuevo.',
  "ui.voice_music_searching": "Buscando música: {query}",
  "ui.voice_playing_result": "Reproduciendo {title}",
  "ui.voice_queue_transfer_needs_players": "Indica el origen y destino. Ejemplo: transferir cola de Sala a Cocina.",
  "ui.voice_queue_transferred_between": "Cola transferida de {source} a {target}",
  "ui.voice_starting_playback": "Iniciando {title}...",
  "ui.voice_processing_status": "Procesando comando...",
  "ui.voice_ready_status": "Listo",
  "ui.voice_recognition_network_failed": "El servicio de reconocimiento de voz no está disponible. El permiso del micrófono puede estar bien; verifica el navegador, VPN, DNS, firewall o la red.",
  "ui.voice_response": "Respuesta",
  "ui.voice_response_will_appear_here": "La respuesta aparecerá aquí.",
  "ui.voice_search": "Búsqueda por voz",
  "ui.voice_search_failed": "Error en la búsqueda por voz",
  "ui.voice_search_is_not_supported_on_this_device": "La búsqueda por voz no es compatible con este dispositivo",
  "ui.voice_transcript": "Transcripción",
  "ui.voice_listening_status": "Escuchando...",
  "ui.waiting_for_speech": "Esperando voz...",
  "ui.microphone_permission_or_browser_blocked": "El navegador bloqueó el reconocimiento de voz. Verifica la configuración del micrófono y de voz del navegador.",
  "ui.volume": "Volumen",
  "ui.volume_and_player_target": "Volumen y reproductor de destino",
  "ui.volume_boost": "Amplificación de volumen",
  "ui.volume_control_large_screen_only": "Control de volumen (solo pantallas grandes)",
  "ui.volume_mode": "Modo de volumen",
  "ui.volume_presets": "Ajustes preestablecidos de volumen",
  "ui.waiting_for_this_device_player": "Esperando al reproductor de este dispositivo...",
  "ui.wake": "Despertar",
  "ui.wake_schedules": "Programaciones de despertar",
  "ui.wed": "Mié",
  "ui.week": "Semana",
  "ui.what_would_you_like_to_listen_to": "¿Qué te gustaría escuchar?",
  "ui.will_play_next": "Se reproducirá a continuación",
  "ui.will_play_next_in_studio": "Se reproducirá a continuación en el estudio",
  "ui.worldwide_popular": "Popular en todo el mundo",
  "ui.add_player_light_pair": "Agregar par",
  "ui.ambient_light": "Luz ambiental",
  "ui.ambient_light_brightness": "Brillo de la luz",
  "ui.ambient_light_cooldown": "Tiempo de espera de actualización de luz",
  "ui.ambient_light_entities": "Luces de color predeterminadas",
  "ui.ambient_light_player_map": "Asignación de reproductor -> luz",
  "ui.ambient_light_player_map_helper": "Usa los selectores de reproductor y luces de color a continuación. Cada fila completada se convierte en un par; limpia un reproductor o sus luces para eliminar el par.",
  "ui.ambient_light_transition": "Transición de la luz",
  "ui.analog": "Analógico",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Selecciona solo luces con capacidad de color que deban seguir la música cuando no exista un par específico para el reproductor.",
  "ui.digital": "Digital",
  "ui.discover_albums": "Descubrir álbumes",
  "ui.discover_music": "Descubrir",
  "ui.discover_subtitle": "Listas, álbumes y radio recomendados.",
  "ui.discovery": "Descubrimiento",
  "ui.discovery_failed": "Error en el modo de descubrimiento",
  "ui.discovery_mode": "Modo de descubrimiento",
  "ui.discovery_started": "Descubrimiento iniciado: {mood}",
  "ui.maximum_brightness_for_music_lighting": "Brillo máximo para la iluminación musical.",
  "ui.minimum_seconds_between_light_updates": "Segundos mínimos entre actualizaciones de luz.",
  "ui.player_light_pair_lights": "Luces de color {number}",
  "ui.player_light_pair_lights_helper": "Selecciona luces con capacidad de color para este reproductor de Music Assistant. Limpia este campo o el reproductor para eliminar el par.",
  "ui.player_light_pair_player": "Reproductor de Music Assistant {number}",
  "ui.player_light_pair_player_helper": "Selecciona el reproductor de Music Assistant para este par de luces.",
  "ui.all": "Todo",
  "ui.all_genres": "Todos los géneros",
  "ui.genres_and_styles": "Géneros y estilos",
  "ui.compact_widget_mode": "Estilo del widget compacto",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Elige cuándo el modo compacto usa el widget móvil pequeño de dos filas.",
  "ui.full": "Completo",
  "ui.mini_widget": "Mini widget",
  "ui.genre": "Género",
  "ui.mood_and_genre": "Estado de ánimo y género",
  "ui.music_by_mood_and_genre": "Música por estado de ánimo y género",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "Entidad opcional para el botón de ENCENDIDO. Deja vacío para detener el reproductor activo.",
  "ui.power": "Encendido",
  "ui.power_action_failed": "Error en la acción de encendido",
  "ui.power_action_sent": "Acción de encendido enviada",
  "ui.power_button": "Botón de ENCENDIDO",
  "ui.power_button_action": "Acción del botón de ENCENDIDO",
  "ui.power_button_entity": "Entidad del botón de ENCENDIDO",
  "ui.scene": "Escena",
  "ui.screensaver": "Salvapantallas",
  "ui.screensaver_controls": "Controles del salvapantallas",
  "ui.screensaver_clock_mode": "Modo de reloj del salvapantallas",
  "ui.screensaver_clock_size": "Tamaño del reloj del salvapantallas",
  "ui.screensaver_clock_size_helper": "Escala el reloj del salvapantallas para tabletas sin mover el arte de portada.",
  "ui.screensaver_clock_x": "Posición del reloj - IZQUIERDA / DERECHA",
  "ui.screensaver_clock_x_helper": "Valores más bajos lo mueven a la izquierda, valores más altos lo mueven a la derecha.",
  "ui.screensaver_clock_y": "Posición del reloj - ARRIBA / ABAJO",
  "ui.screensaver_clock_y_helper": "Valores más bajos lo mueven hacia arriba, valores más altos lo mueven hacia abajo.",
  "ui.screensaver_message": "Mensaje del salvapantallas",
  "ui.screensaver_timeout": "Tiempo de espera del salvapantallas",
  "ui.script": "Script",
  "ui.short_optional_message_shown_on_the_screensaver": "Mensaje corto opcional que se muestra en el salvapantallas.",
  "ui.show_previous_next_controls_on_the_screensaver": "Mostrar botones de anterior y siguiente en el salvapantallas.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Mostrar un reloj tranquilo y el arte de portada después del tiempo de inactividad.",
  "ui.show_power_button_in_the_player_controls": "Mostrar el botón de ENCENDIDO en los controles del reproductor.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Mostrar el modo de descubrimiento a pantalla completa en Acciones.",
  "ui.smart_home": "Hogar inteligente",
  "ui.soft_transition_time_for_music_lighting": "Tiempo de transición suave para la iluminación musical.",
  "ui.stop_player": "Detener reproductor",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Sincronizar las luces seleccionadas con el color del arte de portada actual.",
  "ui.toggle": "Alternar",
  "ui.turn_on": "Encender",
  "ui.choose_a_category_to_browse_provider_playlists": "Selecciona una categoría para explorar las listas de reproducción del proveedor antes de reproducir.",
  "ui.new_and_recommended": "Novedades y recomendados",
  "ui.provider_playlists": "Listas de reproducción del proveedor",
  "ui.alphabetical": "Alfabético",
  "ui.auxiliary_button": "Botón auxiliar",
  "ui.auxiliary_button_action": "Acción del botón auxiliar",
  "ui.auxiliary_button_entity": "Entidad del botón auxiliar",
  "ui.auxiliary_button_icon": "Icono del botón auxiliar",
  "ui.auxiliary_button_name": "Nombre del botón auxiliar",
  "ui.auxiliary_buttons": "Botones auxiliares",
  "ui.excluded_players": "Reproductores excluidos",
  "ui.music_assistant_required": "Se necesita Music Assistant",
  "ui.player_order": "Orden de reproductores",
  "ui.player_sort": "Orden de reproductores",
  "ui.set_the_visual_order_of_quick_actions": "Establece el orden visual. Solo se muestran las acciones rápidas seleccionadas; los elementos no seleccionados se ignoran.",
  "ui.move_down": "Mover hacia abajo",
  "ui.move_up": "Mover hacia arriba",
  "ui.default_order": "Orden predeterminado",
  "ui.show_home_inside_quick_actions": "Muestra Inicio como una acción rápida en lugar de un botón flotante.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Muestra botones de más y menos junto al control deslizante de volumen.",
  "ui.volume_down": "Bajar volumen",
  "ui.volume_step_between_1_and_10_percent_default_5": "Paso de volumen entre 1% y 10%. El predeterminado es 5%.",
  "ui.volume_step_buttons": "Botones de paso de volumen",
  "ui.volume_step_percent": "Paso de volumen",
  "ui.volume_up": "Subir volumen",
  "ui.optional_name_for_the_auxiliary_button": "Etiqueta opcional mostrada como descripción emergente del botón auxiliar.",
  "ui.choose_the_auxiliary_button_icon": "Elige el icono que se muestra para el botón auxiliar.",
  "ui.choose_how_players_are_sorted": "Elige el orden de los reproductores: predeterminado, alfabético o personalizado.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Elige los reproductores de Music Assistant que nunca deberían aparecer en la tarjeta.",
  "ui.custom_order": "Orden personalizado",
  "ui.no_visible_music_assistant_players_after_exclusions": "Todos los reproductores de Music Assistant quedan ocultos por la configuración de reproductores excluidos.",
  "ui.set_custom_player_order": "Establece el orden personalizado de reproductores. Los espacios vacíos se ignoran.",
  "ui.show_auxiliary_button_in_quick_actions": "Muestra este botón auxiliar en la fila de acciones rápidas.",
  "ui.screensaver_buttons": "Botones del salvapantallas",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Elige exactamente qué botones aparecen en el salvapantallas, incluidos FLOW ASSISTANT y los botones anterior y siguiente.",
  "ui.hotel_mode": "Modo hotel",
  "ui.hotel_mode_helper": "Interfaz mínima segura para hotel: solo controles del reproductor, volumen, búsqueda, navegación por carátulas y selección de reproductor.",
  "ui.style_and_genres": "Estilos y géneros"
});
const messages$4 = Object.freeze({
  "ui.a_guided_music_wizard": "Un assistant musical guidé",
  "ui.a_radio_station": "Une station de radio",
  "ui.accent_color": "Couleur d'accentuation",
  "ui.actions": "actions",
  "ui.actions_2": "Actions",
  "ui.active_days": "Jours actifs",
  "ui.active_player_helper": "Entité du lecteur actif",
  "ui.active_player_helper_must_be_an_input_text_entity": "L'entité du lecteur actif doit être un input_text entity",
  "ui.active_players": "Lecteurs actifs",
  "ui.add": "Ajouter",
  "ui.add_to_queue": "Ajouter à la file d'attente",
  "ui.add_to_selection": "Ajouter à la sélection",
  "ui.added_to_liked": "Ajouter aux titres aimés",
  "ui.added_to_music_assistant_liked": "Ajouter aux favoris de Music Assistant",
  "ui.added_to_queue": "Ajouté à la file d'attente",
  "ui.added_to_studio_queue": "Ajouté à la file d'attente du Studio",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Ajoute une animation à l'arrière-plan. Subtil est doux, Fort est perceptible, et Extrême donne un mouvement plus marqué.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Augmente le volume actuel seulement pendant les annonces, puis rétablit le volume précédent",
  "ui.after_run": "Après exécution",
  "ui.album": "Album",
  "ui.albums": "Albums",
  "ui.all_albums": "Tous les Albums",
  "ui.all_artists": "Tous les Artistes",
  "ui.all_countries": "Tous les Pays",
  "ui.all_player_groups_disconnected": "Tous les groupes de lecteurs déconnectés",
  "ui.all_players": "Tous les lecteurs",
  "ui.all_players_2": "tous les lecteurs",
  "ui.all_podcasts": "Tous les Podcasts",
  "ui.all_tracks": "Toutes les pistes",
  "ui.allow_local_likes": "Autoriser les titres préférés locaux",
  "ui.always_visible": "Toujours visible",
  "ui.announce": "Faire une annonce",
  "ui.announce_to_all_players": "Faire une annonce à tous les lecteurs",
  "ui.announcement": "Annonce",
  "ui.announcement_failed_with_error": "Échec de l'annonce{error}",
  "ui.announcement_language": "Langue des annonces",
  "ui.announcement_presets": "Annonces prédéfinies",
  "ui.announcement_sent_to_player": "Annonce envoyée à {player}",
  "ui.announcement_studio": "studio d'annonces",
  "ui.announcement_studio_2": "Studio d'annonces",
  "ui.announcement_target": "Lecteur cible",
  "ui.announcement_to_player_preview": "Annoncer sur {player}: {preview}",
  "ui.announcement_volume_boost": "Augmentation du volume pendant les annonces",
  "ui.announcements": "Annonces",
  "ui.appearance": "Apparence",
  "ui.alphabetical": "Alphabétique",
  "ui.auxiliary_button": "Bouton auxiliaire",
  "ui.auxiliary_button_action": "Action du bouton auxiliaire",
  "ui.auxiliary_button_entity": "Entité du bouton auxiliaire",
  "ui.auxiliary_button_icon": "Icône du bouton auxiliaire",
  "ui.auxiliary_button_name": "Nom du bouton auxiliaire",
  "ui.auxiliary_buttons": "Boutons auxiliaires",
  "ui.apply_group": "Créer le groupe",
  "ui.apply_schedule": "Appliquer la programmation",
  "ui.artist": "Artiste",
  "ui.artist_radio": "Radio de l'artiste",
  "ui.artists": "Artistes",
  "ui.artwork": "Pochette d'album",
  "ui.artwork_swipe": "Défilement de la couverture",
  "ui.ascending": "Croissant",
  "ui.at_least_one_player_must_stay_selected": "Au minimum un lecteur doit être sélectionner",
  "ui.australia": "Australie",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Sélectionne automatiquement l'affichage mobile ou tablette selon la largeur réelle de l'appareil",
  "ui.auto_cloud_default": "Auto / Cloud par défaut",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Extrait automatiquement les couleurs de la pochette actuelle et garde un effet subtil. Le mode Fort rend la palette plus riche et lumineuse.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Auto ne modifie pas la configuration par défaut de l'assistant vocal Home Assistant Cloud. Sélection Manuelle envoie une langue spécifique",
  "ui.available": "Disponible",
  "ui.back": "Retour",
  "ui.back_2": "Retour",
  "ui.back_to_countries": "Retour aux Pays",
  "ui.back_to_queue": "Retour vers la file d'attente",
  "ui.background_motion": "Mouvement de l'arrière-plan",
  "ui.behavior": "Comportement",
  "ui.browse_covers": "Parcourir les pochettes d'album",
  "ui.browse_playlists_artists_albums_and_radio": "Parcourir les listes de lectures, artistes, albums et radio",
  "ui.browser_players": "Parcourir les lecteurs",
  "ui.build_custom_mix": "Créer un mix personnalisé",
  "ui.building_studio_mix": "Création du mix Studio...",
  "ui.button": "Bouton",
  "ui.by_mood": "Par ambiance",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Durée du cache en millisecondes pour les requêtes de données sélectionnées",
  "ui.cache_ttl": "Cache TTL",
  "ui.calm": "Calme",
  "ui.canada": "Canada",
  "ui.cancel": "Annuler",
  "ui.cancel_2": "Annuler",
  "ui.cancel_selection": "Annuler la sélection",
  "ui.card_configuration": "Configuration de la carte",
  "ui.card_height": "Hauteur de la carte",
  "ui.card_height_in_pixels": "Hauteur de la carte en pixels.",
  "ui.card_settings_are_managed_from_the_visual_editor": "Les paramètres de la carte sont configurés à partir de l'éditeur visuel",
  "ui.change_song": "Changer de chanson",
  "ui.change_track": "Changer de piste",
  "ui.chill_mix": "Mix Détente",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Choisir un pays ou définir sur Tous les pays pour parcourir tous les pays de l'onglet Radio",
  "ui.choose_a_queue_position": "Choisir une position dans la file d'attente",
  "ui.choose_a_source_player": "Choisir la source du lecteur",
  "ui.choose_a_target_player": "Choisir le lecteur cible",
  "ui.choose_at_least_one_player": "Choisir au moins un lecteur",
  "ui.choose_icons_text_or_both_for_the_footer": "Choisir des icônes, du texte ou les deux pour le pied de page.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Choisissez uniquement des lecteurs MA. La liste d’épinglage affiche seulement les lecteurs MA afin d’éviter toute confusion.",
  "ui.choose_one_clear_option": "Choisissez une seule option",
  "ui.choose_player": "Choisir un lecteur",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Choisissez une liste de lecture rapide ou cliquez sur la baguette magique pour une liste aléatoire",
  "ui.choose_source": "Choisissez la source",
  "ui.choose_target": "Choisissez la cible",
  "ui.choose_target_player": "Choisissez le lecteur cible",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Choisissez une couleur d'accentuation pour la disposition mobile.",
  "ui.choose_the_music": "Choisissez votre musique",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Choisissez les petits boutons affichés sous le titre de la chanson. En hébreu, le minuteur apparaît en premier afin d’être placé du côté droit.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Choisissez la source en premier. La lecture démarrera sur la cible actuelle.",
  "ui.choose_where_to_play": "Choisissez où faire jouer",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Choisir si le défilement de la pochette d'album change la piste ou parcourt les pochettes.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Choisir si les paramètres sont contrôlés à partir de l'éditeur visuel intégré ou à partir la configuration de la carte dans le tableau de bord",
  "ui.choose_which_actions_appear_in_the_main_bar": "Choisir quelles actions apparaissent sur la barre d'outil principale",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Choisir quels jours sont concernés par le mode Sombre Auto",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Choisir quelles icônes apparaissent dans la barre d'actions rapides. Tout réinitialiser demande une confirmation avant de déconnecter tous les lecteurs actifs",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Choisir quels lecteurs apparaissent comme des tuiles dans les pièces",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Choisissez les lecteurs à conserver dans la sélection actuelle.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Choisir quels onglets sont visibles sur la page de bibliothèque",
  "ui.choose_who_is_controlled": "Choisir quel lecteurs sont contrôlés",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "Visuel désactive la page de configuration de la carte intégrée",
  "ui.clean_all": "Tout réinitialiser",
  "ui.clean_all_confirm_action": "Tout réinitialisé",
  "ui.clean_all_confirm_copy": "Cette action déconnectera tous les lecteurs actifs, arrêtera la lecture et supprimera leur file d'attente. Continuer?",
  "ui.clean_all_confirm_title": "Réinitialiser tous les lecteurs?",
  "ui.clean_the_studio_wall": "Réinitialiser le Studio",
  "ui.clear": "Réinitialiser",
  "ui.clear_queue": "Réinitialiser la file d'attente",
  "ui.clear_search": "Réinitialiser la recherche",
  "ui.clone_queue": "Dupliquer la file d'attente",
  "ui.close": "Fermer",
  "ui.collapse_compact_player": "Réduire le lecteur compact",
  "ui.compact_mode": "Mode compact",
  "ui.configure_ready_made_announcement_phrases": "Configurer des phrases prédéfinies d'annonces.",
  "ui.connect_this_device": "Connecter cet appareil",
  "ui.connect_this_device_2": "Connecter cet appareil",
  "ui.connected": "Connecté",
  "ui.connected_2": "Connecté",
  "ui.connected_players": "lecteurs connectés",
  "ui.connected_players_2": "Lecteurs connectés",
  "ui.connecting": "Connexion",
  "ui.connecting_2": "Connexion...",
  "ui.confirm": "Confirmer",
  "ui.connection_and_behavior": "Connection et Comportement",
  "ui.connects_directly_from_this_card_using_sendspin": "Connecter directement à partir de cette carte en utilisant SendspinConnecter",
  "ui.continue": "Continuer",
  "ui.continue_2": "Continuer",
  "ui.continue_from_recent_music_assistant_activity": "Continuer à partir des activités récentes de Music Assistant",
  "ui.continue_listening": "Continuer la lecture",
  "ui.continue_what_was_played_recently": "Continuer la lecture récente",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Définit si l'arrière-plan est légèrement animé et la force de l'animation",
  "ui.controlling": "Lecteurs sélectionnés",
  "ui.could_not_build_quick_mix_from_the_current_song": "Échec de la création du Mix Rapide à partir de cette chanson",
  "ui.could_not_build_studio_mix": "Échec de la création du mix Studio",
  "ui.could_not_choose_media": "Échec de la sélection du média",
  "ui.could_not_clone_the_queue": "Échec de la duplication de la file d'attente",
  "ui.could_not_find_music": "Aucune musique trouvée",
  "ui.could_not_play_label": "Échec de la lecture: {label}",
  "ui.could_not_start_chill_mix": "Échec de la lecture du mix Détente",
  "ui.could_not_start_playback": "Impossible de démarrer la lecture",
  "ui.could_not_start_playback_2": "Impossible de démarrer la lecture",
  "ui.could_not_start_playback_in_studio": "Impossible démarrer la lecture dans le Studio",
  "ui.could_not_start_quick_mix": "Impossible de démarrer le Mix Rapide",
  "ui.could_not_start_radio_playback": "Impossible de démarrer la radio",
  "ui.could_not_transfer_the_queue": "Échec du transfert de la file d'attente",
  "ui.could_not_update_active_player_helper": "Impossible d'actualiser l'entité du lecteur actif",
  "ui.create_schedule": "Créer une programmation",
  "ui.current_path": "Chemin actuel",
  "ui.custom": "Personnalisé",
  "ui.dark": "Sombre",
  "ui.delete_scene": "Supprimer la scène",
  "ui.delete_schedule": "Supprimer la programmation",
  "ui.descending": "Décroissant",
  "ui.dictate": "Dicter",
  "ui.disabled": "Désactivé",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Désactive le flou, les animations, l'arrière-plan dynamique et les ombres. Recommandé pour les appareils Nest et les tablettes moins performantes",
  "ui.disconnect": "Déconnecter",
  "ui.disconnect_all": "Tout déconnecter",
  "ui.disconnect_groups": "Déconnecter les groupes",
  "ui.disconnect_player_groups": "Déconnecter les groupes de lecteurs",
  "ui.disconnect_this_device": "Déconnecter cet appareil",
  "ui.disconnected": "Déconnecté",
  "ui.disconnecting": "Déconnexion",
  "ui.disconnecting_player_groups": "Déconnexion des groupes de lecteurs...",
  "ui.discover": "Découvertes",
  "ui.down": "Bas",
  "ui.dynamic_theme": "Thème dynamique",
  "ui.edit_schedule": "Modifier la programmation",
  "ui.enable": "Activer",
  "ui.enable_or_disable_the_footer_search_button": "Activer/désactiver la barre de recherche inférieure.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Activer/désactiver le bouton Studio dans la barre inférieure.",
  "ui.enable_the_floating_home_shortcut_button": "Activer le bouton flottant du raccourci pour le menu principal.",
  "ui.enabled": "Activé",
  "ui.enables_switching_to_local_liked_mode": "Autoriser le mode local pour les titres préférés.",
  "ui.end_time": "Heure de fin",
  "ui.excluded_players": "Lecteurs exclus",
  "ui.energetic": "Énergique",
  "ui.energy_and_rhythm": "Énergie et rythme",
  "ui.enter_an_announcement_first": "Entrer d'abord une annonce",
  "ui.example_quiet_jazz_greek_music_workout": "Exemple: smooth jazz, musique Grèque, entrainement",
  "ui.existing_content": "Contenu existant",
  "ui.expand_player": "Maximiser le lecteur",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Extraire les couleurs à partir de la pochette actuelle et appliquer celles-ci à l'interface.",
  "ui.extreme": "Extrême",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Fallback pour les configurations nécessitant un chemin alternatif pour le service de file d’attente.",
  "ui.fallback_mode": "Mode Fallback",
  "ui.family_friendly": "Adapté pour toute la famille",
  "ui.favorite": "Favoris",
  "ui.favorite_action_failed": "Échec de l'ajout aux favoris",
  "ui.favorite_action_sent": "Action d'ajout aux favoris envoyées",
  "ui.favorite_button_entity": "Entité du bouton favoris",
  "ui.favorite_center": "centre des favoris",
  "ui.favorite_center_2": "Centre des Favoris",
  "ui.favorite_radio": "Radio préférée",
  "ui.favorite_updated": "Favoris actualisés",
  "ui.favorites_and_liked_music": "Favoris et titres préférés",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Détection des fonctionnalités, contexte du lecteur et outils de lecture sur cet appareil.",
  "ui.find_music": "Trouver de la musique",
  "ui.finding_music": "Recherche de musique",
  "ui.fixed": "Fixe",
  "ui.flow_mix": "FLOW Mix",
  "ui.font_scale": "Ratio de la police",
  "ui.font_size": "Taille de la police",
  "ui.footer_search": "Barre de recherche inférieure",
  "ui.footer_style": "Style de la barre inférieure",
  "ui.footer_style_2": "Style de la barre inférieure",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "Par exemple /lovelace/home ou n'importe quel chemin Home Assistant.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "Pour les écrans de faible performance: désactive le flou agressif, les animations, l'extraction dynamique du thème et les animations de l'arrière-plan.",
  "ui.france": "France",
  "ui.free_style": "Free style",
  "ui.free_style_quiet_jazz_greek_music_workout": "Free style: smooth jazz, musique Grèque, entrainement...",
  "ui.fresh_start": "Nouvelle écoute",
  "ui.fresh_start_2": "Nouvelle écoute",
  "ui.fri": "Ven",
  "ui.friendly_and_simple": "Amical et simple",
  "ui.from": "De",
  "ui.general": "Général",
  "ui.general_and_display": "Général et Affichage",
  "ui.germany": "Allemagne",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Résolution globale de la police sur toute l'interface. 1 est la taille par défaut.",
  "ui.greece": "Grèce",
  "ui.grid": "Grille",
  "ui.group": "Groupe",
  "ui.group_cleared": "Groupe réinitialisé",
  "ui.group_cleared_2": "Groupe réinitialisé",
  "ui.group_speakers": "Groupes d'enceintes",
  "ui.group_speakers_2": "Groupes d'enceintes",
  "ui.group_updated": "Groupe actualisé",
  "ui.group_volume": "Volume du groupe",
  "ui.group_volume_up_energetic_mix": "Groupe, augmenter le volume, mix énergique",
  "ui.grouped_players": "Lecteurs groupés",
  "ui.hebrew": "Hébreu",
  "ui.history": "Historique",
  "ui.history_tabs": "Onglet Historique",
  "ui.home": "Accueil",
  "ui.home_party_night_presets": "Accueil, fête, préréglage de nuit",
  "ui.home_path": "Chemin d'Accueil",
  "ui.home_scene_prepared": "Scène d'accueil préparée",
  "ui.home_shortcut": "Raccourci d'accueil",
  "ui.home_shortcut_path": "Chemin du raccourci d'accueil",
  "ui.icon_and_text": "Icône et texte",
  "ui.icon_only": "Icône seulement",
  "ui.icon_plus_text": "Icône + texte",
  "ui.idle": "En veille",
  "ui.idle_2": "En veille",
  "ui.in_card_ui": "UI intégrée",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Inclure le mode de thème personnalisé depuis l’écran des paramètres intégrés.",
  "ui.info": "Info",
  "ui.israel": "Israel",
  "ui.israeli": "Israeli",
  "ui.italy": "Italie",
  "ui.items": "items",
  "ui.join_selected_players": "Grouper les lecteurs sélectionnés",
  "ui.kids": "Kids",
  "ui.language": "Langue",
  "ui.larger_lyrics": "Agrandir les paroles",
  "ui.layout_adapted_to_height": "Disposition adaptée à la hauteur",
  "ui.layout_mode": "Style de disposition",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Laisser vide si la carte doit utiliser Home Assistant seulement.",
  "ui.library": "Bibliothèque",
  "ui.library_2": "Bibliothèque",
  "ui.library_pages": "Sections de la bibliothèque",
  "ui.default_library_layout": "Affichage par défaut de la bibliothèque",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Choisissez la disposition de la bibliothèque. Vous pouvez toujours basculer entre Grille et Liste dans la bibliothèque.",
  "ui.library_radio": "Bibliothèque de radios",
  "ui.library_surprise": "Bibliothèque surprise",
  "ui.library_tabs": "Onglet Bibliothèque",
  "ui.library_tabs_2": "Onglet Bibliothèque",
  "ui.light": "Clair",
  "ui.like": "Aimer",
  "ui.like_2": "Aimer",
  "ui.like_3": "Aimer",
  "ui.liked": "Titres préférés",
  "ui.liked_mode": "Mode des titres préférés",
  "ui.liked_sync": "Synchronisation des titres préférés",
  "ui.list": "Liste",
  "ui.listening": "Écoute en cours...",
  "ui.loading": "Chargement...",
  "ui.loading_favorites": "Chargement des favoris...",
  "ui.loading_library": "Chargement de la bibliothèque...",
  "ui.loading_lyrics": "Chargement des paroles...",
  "ui.loading_players": "Chargement des lecteurs...",
  "ui.loading_queue": "Chargement de la file d'attente...",
  "ui.loading_recent_items": "Chargement des items récents...",
  "ui.loading_schedules": "Chargement des programmations...",
  "ui.local": "Local",
  "ui.local_favorites": "Favoris locaux",
  "ui.local_favorites_2": "Favoris locaux",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "Le mode Local n'est applicable que si les titres préférés locaux sont activés.",
  "ui.low_light_listening": "Écoute en faible luminosité",
  "ui.low_volume_and_quiet_mix": "Mix calme à faible volume",
  "ui.lower_volume_mood": "Ambiance douce",
  "ui.lyrics": "Paroles",
  "ui.lyrics_earlier": "Paroles précédentes",
  "ui.lyrics_font_size": "Taille de police des paroles",
  "ui.lyrics_later": "Paroles suivantes",
  "ui.lyrics_timing": "Synchronisation des paroles",
  "ui.lyrics_unavailable_right_now": "Paroles indisponibles actuellement",
  "ui.ma_favorite_action_failed_with_error": "Échec de l'ajout aux favoris de Music Assistant{error}",
  "ui.ma_interface_path": "Chemin de l'interface MA",
  "ui.main_bar": "Barre principale",
  "ui.main_bar_items": "Items de la barre principale",
  "ui.main_bar_items_2": "Items de la barre principale",
  "ui.main_opacity": "Transparence générale",
  "ui.mainly_relevant_on_larger_layouts": "Applicable principalement aux affichages larges.",
  "ui.mass_queue_send_command_fallback": "mass_queue/send_command fallback",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Utilise le même réglage de microphone que les paramètres intégrés.",
  "ui.media": "Média",
  "ui.media_2": "Média",
  "ui.media_actions": "Actions des Médias",
  "ui.media_layout": "Disposition des Médias",
  "ui.media_selected_target": "Sélectionné: {media} ? Cible: {target}",
  "ui.menu": "Menu",
  "ui.message": "Message",
  "ui.microphone": "Microphone",
  "ui.microphone_2": "Microphone",
  "ui.microphone_is_disabled": "Le microphone est désactivé.",
  "ui.missing_url": "URL manquante",
  "ui.mobile": "Mobile",
  "ui.mon": "Lun",
  "ui.mood_style_or_free_text": "Ambiance, style ou texte libre",
  "ui.more_info": "Plus d'infos",
  "ui.more_info_is_unavailable_for_this_player": "Infos indisponibles pour ce lecteur.",
  "ui.morning": "Matin",
  "ui.morning_mix": "Mix matinal",
  "ui.move_to_next": "Lire ensuite",
  "ui.move_to_position": "Déplacer à la position",
  "ui.music": "Musique",
  "ui.music_assistant_required": "Music Assistant requis",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "ID de l’entrée de configuration Music Assistant, si vous souhaitez une détection directe de l’intégration via Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "L'entrée de configuration de Music Assistant n'a pas été trouvé.",
  "ui.music_assistant_token": "Music Assistant token",
  "ui.music_assistant_url": "Music Assistant URL",
  "ui.music_hub": "hub musical",
  "ui.music_hub_2": "Hub Musical",
  "ui.mute": "Sourdine",
  "ui.mute_command_failed": "Échec de la mise en sourdine",
  "ui.my_studio_scene": "Ma scène du Studio",
  "ui.name_or_keyword": "Nom ou mot-clé",
  "ui.name_this_studio_scene": "Nommer cette scène du Studio...",
  "ui.netherlands": "Pays-bas",
  "ui.new_schedule": "Nouvelle programmation",
  "ui.new_wake_schedule": "Nouveau démarrage programmé",
  "ui.newest": "Les plus récents",
  "ui.next": "Suivant",
  "ui.next_track_failed": "Impossible de passer à la piste suivante",
  "ui.night": "Nuit",
  "ui.night_end_time": "Heure de la fin de la nuit",
  "ui.night_mode": "Mode sombre",
  "ui.night_mode_auto_window": "Mode sombre automatique {start}-{end}",
  "ui.night_mode_days": "Jours actifs du mode sombre",
  "ui.night_mode_is_always_on": "Mode sombre toujours actif",
  "ui.night_mode_is_off": "Mode sombre désactivé",
  "ui.night_mode_is_off_until_you_choose_another_mode": "Mode sombre désactivé jusqu'à changement.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "Mode sombre activé jusqu'à changement.",
  "ui.night_schedule_updated": "Programmation du mode sombre mise à jour",
  "ui.night_start_time": "Heure du début de la nuit",
  "ui.no_active_media": "Aucun média actif",
  "ui.no_active_players": "Aucun lecteur actif",
  "ui.no_available_players": "Aucun lecteur disponible",
  "ui.no_content_found": "Aucun contenu trouvé",
  "ui.no_dynamic_player_groups_to_disconnect": "Aucun groupe de lecteurs à déconnecter",
  "ui.no_extra_ma_players": "Aucun lecteur MA supplémentaire",
  "ui.no_extra_ma_players_2": "Aucun lecteur MA supplémentaire",
  "ui.no_favorites_found": "Aucun favori trouvé",
  "ui.no_liked_media_yet": "Aucun titre préféré actuellement",
  "ui.no_liked_tracks_to_play": "Aucun titre préféré à jouer",
  "ui.no_light_entities_found": "Aucune entité lumière trouvée",
  "ui.no_lyrics_found": "Aucune parole trouvée",
  "ui.no_matching_content_was_found": "Aucun contenu correspondant trouvé",
  "ui.no_media_found": "Aucun média trouvé",
  "ui.no_media_found_for_this_search": "Aucun média trouvé pour cette recherche",
  "ui.no_mix_content_found": "Aucun contenu de mix trouvé",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "Aucun lecteur Music Assistant ont été trouvé. Vérifier Music Assistant et les entités media_player.",
  "ui.no_music_assistant_players_were_found_for_pinning": "Aucun lecteur Music Assistant trouvé à épingler",
  "ui.no_next_track_in_queue": "Aucune chanson restant dans la liste d'attente",
  "ui.no_player_groups_to_disconnect": "Aucun groupe de lecteurs à déconnecter",
  "ui.no_player_light_pairs_yet": "Aucune paire lecteur-lumière actuellement",
  "ui.no_player_selected": "Aucun lecteur sélectionné",
  "ui.no_players_found": "Aucun lecteur trouvé",
  "ui.no_players_found_yet": "Aucun lecteur trouvé actuellement",
  "ui.no_playlists_found": "Aucune liste de lecture trouvée",
  "ui.no_queue": "Aucune file d'attente",
  "ui.no_queue_item_to_rebuild": "Aucun item de file d'attente à reconstruire",
  "ui.no_queue_to_clone": "Aucune file d'attente à dupliquer",
  "ui.no_radio_browser_countries_found": "Aucun pays trouvé dans le gestionnaire de radios",
  "ui.no_radio_stations_found": "Aucune station de radio trouvée",
  "ui.no_recent_listening_yet": "Aucune lecture récente actuellement",
  "ui.no_results": "Aucun résultat",
  "ui.no_results_found": "Aucun résultat trouvé",
  "ui.no_saved_studio_scenes_yet": "Aucun Studio enregistré actuellement",
  "ui.no_scheduled_start_is_active": "Aucune programmation n'est active",
  "ui.no_selected_tracks_to_play": "Aucune piste sélectionnée à jouer",
  "ui.no_sleep_timer_is_active": "Aucune minuterie de veille n'est active",
  "ui.no_speech_was_captured": "Aucune parole n'a été capturée",
  "ui.no_target_players_available": "Aucun lecteur cible disponible",
  "ui.no_target_players_available_2": "Aucun lecteur cible disponible",
  "ui.no_tts_service_or_entity_is_configured": "Aucun service TTS ou entité configuré",
  "ui.no_wake_schedules_yet": "Aucune démarrage programmé actuellement",
  "ui.none": "Aucun",
  "ui.nothing_is_playing_right_now": "Rien n'est en lecture présentement",
  "ui.nothing_playing": "Aucune lecture",
  "ui.now_playing": "Joue présentement",
  "ui.now_playing_2": "Joue présentement",
  "ui.off": "Éteint",
  "ui.off_2": "Éteint",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "Désactivé coupe la fonction, Activé la maintient active et Auto suit la plage horaire configurée.",
  "ui.oldest": "Les plus vieux",
  "ui.on": "Actif",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "Taper une fois prépare les lecteurs, groupes, volume et contenu. Enregistre votre Studio actuel comme étant une scène locale.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Seules les actions concernant la cible actuelle sont affichées ici.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Seuls les lecteurs de Music Assistant sont affichés ici. Les lecteurs Web et Sendspin locaux sont cachés pour afin d'éviter la confusion.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Nécessaire seulement lorsque l'URL directe de Music Assistant est utilisée.",
  "ui.opacity_for_popups_and_overlays": "Transparence des popups et des superpositions.",
  "ui.opacity_for_the_main_card_background": "Transparence de l'arrière-plan de la page principale.",
  "ui.open_full_interface": "Ouvrir l'interface complète",
  "ui.open_full_queue": "Afficher la file d'attente complète",
  "ui.open_interface_in": "Ouvrir l'interface dans",
  "ui.open_ma": "Ouvrir MA",
  "ui.open_music_assistant": "Ouvrir Music Assistant",
  "ui.open_music_assistant_2": "Ouvrir Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Ouvrir Music Assistant sur cet appareil pour activer le lecteur Web",
  "ui.open_player_picker": "Ouvrir le sélecteur de lecteur",
  "ui.open_queue": "Afficher la file d'attente",
  "ui.open_saved_songs": "Afficher les chansons enregistrées",
  "ui.open_the_full_music_assistant_interface": "Ouvrir l'interface complète de Music Assistant?",
  "ui.open_the_full_music_assistant_interface_2": "Ouvrir l'interface complète de Music Assistant",
  "ui.opening_studio_library": "Ouverture de la bibliothèque du Studio",
  "ui.opening_timers": "Minuterie d'ouverture",
  "ui.option": "Option",
  "ui.optional": "Optionnel",
  "ui.optional_2": "Optionnel",
  "ui.optional_entity_used_for_an_external_favorite_button": "Entité optionnelle utilisée pour le bouton de favoris externes.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Entrée input_text optionnel mis à jour avec l’entity_id du lecteur actif pour les automatisations et les modèles.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Entrée input_text optionnel mis à jour avec l’entity_id du lecteur actif pour les automatisations et les modèles.",
  "ui.other": "Autre",
  "ui.other_players": "Autres lecteurs",
  "ui.party": "Fête",
  "ui.path_used_when_opening_the_music_assistant_interface": "Chemin utilisé pour ouvrir l'interface de Music Assistant.",
  "ui.pause": "Pause",
  "ui.paused": "En pause",
  "ui.per_device_in_card": "Par appareil / intégré à la carte",
  "ui.performance_mode": "Mode performance",
  "ui.performance_mode_for_weak_devices": "Mode performance pour les appareils moins performants.",
  "ui.performance_profile": "Profil de performance",
  "ui.performance_profile_helper": "Choisissez le profil de performance pour les effets visuels. Ultra Lite réduit la transparence, le flou, les pochettes dynamiques et les animations pour les appareils peu puissants.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Choisissez une ambiance ou tapez votre propre style. Studio cherchera dans Music Assistant et créera une file d'attente.",
  "ui.pinned": "Épinglé",
  "ui.pinned_players": "Lecteurs épinglés",
  "ui.play": "Lecture",
  "ui.play_2": "Lecture",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Lisez quelques titres et ils apparaîtront ici.",
  "ui.play_a_saved_list": "Lire une liste enregistrée",
  "ui.play_all": "Tout lire",
  "ui.play_an_artist": "Lire un artiste",
  "ui.play_artwork_action": "Action sur la pochette",
  "ui.play_everywhere": "Lire partout",
  "ui.play_next": "Lire ensuite",
  "ui.play_next_and_clear_queue": "Lire ensuite et vider la file d'attente",
  "ui.play_now": "Lire maintenant",
  "ui.play_now_and_clear_queue": "Lire maintenant et vider la file d'attente",
  "ui.play_pause": "Lecture / Pause",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Lire, ajouter à la file d’attente, lancer la radio ou retirer des favoris directement depuis le Studio.",
  "ui.play_selected": "Lire la sélection",
  "ui.playback_command_failed": "Impossible d’exécuter la commande de lecture",
  "ui.playback_command_failed_2": "Impossible d’exécuter la commande de lecture",
  "ui.playback_started": "Lecture démarrée",
  "ui.player": "lecteur",
  "ui.player_2": "Lecteur",
  "ui.player_count_many": "{count} lecteurs",
  "ui.player_count_one": "1 lecteur",
  "ui.player_groups_could_not_be_disconnected": "Le groupe de lecteurs n'a pu être déconnecté",
  "ui.player_is_pinned_from_settings": "Le lecteur est épinglé à partir des paramètres",
  "ui.player_is_ready": "Le lecteur est prêt",
  "ui.player_is_ready_nothing_is_playing_right_now": "Le lecteur est prêt - Rien en cours de lecture actuellement",
  "ui.player_order": "Ordre des lecteurs",
  "ui.player_on_this_device": "Lecteur sur cet appareil",
  "ui.player_selected": "Lecteur sélectionné",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "Les paramètres du lecteur et les préréglages DSP restent en lecture seule jusqu’à ce que Music Assistant fournisse un schéma fiable pour ce lecteur.",
  "ui.player_sort": "Tri des lecteurs",
  "ui.players": "Lecteurs",
  "ui.players_queue_and_groups": "Lecteurs, file d'attente et groupes",
  "ui.playing": "En cours de lecture",
  "ui.playing_a_random_playlist": "Lecture d'une liste de lecture aléatoire",
  "ui.playing_a_random_radio_station": "Lecture d'une station de radio aléatoire",
  "ui.playing_on": "En cours de lecture sur",
  "ui.playing_on_2": "En cours de lecture sur",
  "ui.playlist": "Liste de lecture",
  "ui.playlists": "Listes de lecture",
  "ui.podcast": "Podcast",
  "ui.podcasts": "Podcasts",
  "ui.popup_opacity": "Transparence du popup",
  "ui.preset_message": "Message prédéfini",
  "ui.previous": "Précédent",
  "ui.primary": "Principale",
  "ui.primary_target": "Cible principale",
  "ui.pro_tools": "Outils Pro",
  "ui.queue": "File d'attente",
  "ui.queue_2": "File d'attente",
  "ui.queue_action_failed": "Échec de l'action sur la file d'attente",
  "ui.queue_action_is_not_available_for_this_player": "L'action sur la file d'attente est indisponible sur ce lecteur",
  "ui.queue_actions": "Actions de la file d'attente",
  "ui.queue_cleared": "File d'attente vidée",
  "ui.queue_cleared_and_item_moved_next": "File d'attente vidée et élément déplacé en position suivante ",
  "ui.queue_cloned": "File d'attente dupliquée",
  "ui.queue_cockpit": "centre de contrôle de la file d'attente",
  "ui.queue_cockpit_2": "Centre de contrôle de la file d'attente",
  "ui.queue_is_empty": "La file d'attente est vide",
  "ui.queue_is_unavailable_for_this_player": "La file d'attente est indisponible sur ce lecteur",
  "ui.queue_item": "Éléments de la file d'attente",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "L'ID d'élément pour la file d'attente n'est pas prêt. Actualisez la file d'attente et réessayez.",
  "ui.queue_results": "Résultats de la file d'attente",
  "ui.queue_transferred": "File d'attente transférée",
  "ui.queues_refreshed": "Files d'attente actualisées",
  "ui.quick_actions": "Actions rapides",
  "ui.quick_actions_2": "Actions rapides",
  "ui.quick_mix": "Mix rapide",
  "ui.quick_mix_started": "Mix rapide démarré",
  "ui.quick_play": "Lecture rapide",
  "ui.radio": "Radio",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Parcourir par pays",
  "ui.radio_browser_country": "Parcourir par pays",
  "ui.radio_browser_country_2": "Parcourir par pays",
  "ui.radio_mode_is_not_available_for_this_media_type": "Le mode radio n'est pas disponible pour ce type de média",
  "ui.radio_mode_started": "Mode radio démarré",
  "ui.radio_station_changed": "Station de radio changée",
  "ui.radio_stations": "Stations de radio",
  "ui.random": "Aléatoire",
  "ui.random_gentle_morning_mix": "Mix matinal aléatoire",
  "ui.random_playlist": "Liste de lecture aléatoire",
  "ui.random_radio": "Radio aléatoire",
  "ui.ready": "Prêt",
  "ui.ready_to_play": "Prêt à lire",
  "ui.recent": "Récent",
  "ui.recent_continue": "Récent / Reprendre",
  "ui.recent_listening": "lecture récente",
  "ui.recent_track": "Piste récente",
  "ui.recently_added": "Récemment ajouté",
  "ui.recently_played": "Récemment lu",
  "ui.recently_played_2": "Récemment lu",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "Les recommendations apparaîtront lorsque HOMEii Flow verra votre liste d'attente et vos lectures récentes.",
  "ui.recommended": "Recommandés",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Format recommandé: HH:MM, par exemple 06:00. Le passage après minuit est pris en charge.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Format recommandé: HH:MM, par exemple 22:00.",
  "ui.recommended_playlist": "Liste de lecture recommandée",
  "ui.recommended_track": "Piste recommandé",
  "ui.reconnects_when_you_return_to_this_dashboard": "Se connecte à nouveau lorsque vous retournez sur ce tableau de bord",
  "ui.refresh": "Actualiser",
  "ui.remember_as_this_device": "Mémoriser comme cet appareil",
  "ui.remove": "Retirer",
  "ui.remove_from_selection": "Retirer de la sélection",
  "ui.removed_from_liked": "Retiré des titres préférés",
  "ui.removed_from_music_assistant_liked": "Retiré des titres préférés sur Music Assistant",
  "ui.reorder_queue": "Réorganiser la file d'attente",
  "ui.repeat": "Répéter",
  "ui.repeat_all": "Tout répéter",
  "ui.repeat_off": "Répétition désactivée",
  "ui.repeat_one": "Répéter la piste",
  "ui.reset": "Réinitialiser",
  "ui.reset_lyrics_font_size": "Réinitialiser la taille de police",
  "ui.reset_lyrics_timing": "Réinitialiser la synchronisation des paroles",
  "ui.results": "Résultats",
  "ui.rtl": "RTL",
  "ui.sat": "Sam",
  "ui.save_current_target": "Enregistrer la cible actuelle",
  "ui.save_schedule": "Enregistrer la programmation",
  "ui.scene_players_are_not_available": "Les lecteurs de Scène sont indisponibles",
  "ui.scene_presets": "scènes prédéfinies",
  "ui.scene_presets_2": "Scènes Prédéfinies",
  "ui.scenes": "Scènes",
  "ui.scales_interface_icons_without_changing_button_sizes": "Ajuster la taille des icônes de l'interface sans changer la taille des boutons ni la disposition.",
  "ui.scheduled_start": "Démarrage programmé",
  "ui.scheduled_start_activated_label": "Démarrage programmé activé: {label}",
  "ui.scheduled_start_cleared": "Démarrage programmé désactivé",
  "ui.scheduled_start_failed": "Échec du démarrage programmé",
  "ui.scheduled_start_saved": "Démarrage programmé enregistré",
  "ui.scheduled_starts_active_count": "{count} démarrages programmés actifs",
  "ui.schedules": "Programmation",
  "ui.search": "Recherche",
  "ui.search_and_choose_play_next_add_radio_or_like": "Rechercher et choisissez Lire, Lire ensuite, Ajouter, Radio ou Aimer.",
  "ui.search_everything": "Rechercher partout...",
  "ui.search_failed": "Échec de la recherche",
  "ui.search_in_queue": "Rechercher dans la file d'attente",
  "ui.search_queue_and_library": "Rechercher dans la file d'attente et la bibliothèque...",
  "ui.search_the_library": "Rechercher dans la bibliothèque...",
  "ui.search_tracks_albums_artists_and_playlists": "Rechercher des pistes, albums, artistes ou listes de lecture",
  "ui.searching": "Recherche...",
  "ui.searching_library": "Recherche dans la bibliothèque...",
  "ui.searching_smart_selection": "Recherche d'une sélection intelligente...",
  "ui.seek": "Navigation",
  "ui.select": "Selectionner",
  "ui.select_lights": "Sélectionnez une ou plusieurs lumières",
  "ui.select_player": "Sélectionnez un lecteur",
  "ui.select_2": "Sélectionner",
  "ui.select_a_player_first": "Sélectionnez d'abord un lecteur",
  "ui.select_at_least_one_studio_player": "Sélectionnez au moins un lecteur Studio",
  "ui.select_at_least_two_players_to_create_a_group": "Sélectionnez au moins deux lecteurs pour créer un groupe",
  "ui.selected": "Sélectionné",
  "ui.selected_music": "Musique sélectionnée",
  "ui.selected_player": "Lecteur sélectionné",
  "ui.selected_player_2": "Lecteur sélectionné",
  "ui.selected_player_3": "Lecteur sélectionné",
  "ui.selected_players_at_comfortable_volume": "Lecteurs sélectionnés à volume confortable",
  "ui.selected_playlist": "Liste de lecture sélectionnée",
  "ui.send_a_short_voice_message_or_announcement_url": "Envoyer un court message ou un URL d'annonce.",
  "ui.send_a_voice_message": "Envoyer un message vocal",
  "ui.send_announcement": "Envoyer l'annonce",
  "ui.sendspin_and_diagnostics": "Sendspin et diagnostics",
  "ui.set_sleep_timer": "Régler la minuterie de mise en veille",
  "ui.set_volume": "Régler le volume",
  "ui.set_the_visual_order_of_quick_actions": "Réorganiser. Seules les actions rapides sélectionnées sont affichées ; les éléments non sélectionnés sont ignorés.",
  "ui.settings": "Paramètres",
  "ui.settings_source": "Source des paramètres",
  "ui.show_ma_button": "Afficher le bouton MA",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Afficher ou masquer la piste suivante dans Lecture en cours.",
  "ui.show_theme_toggle": "Afficher le sélecteur de thème",
  "ui.show_up_next": "Afficher la section À suivre",
  "ui.show_up_next_2": "Afficher la section À suivre",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Affiche une tuile de lecteur compacte autonome avec la pochette, les contrôles de base, le volume et un bouton pour agrandir l’interface.",
  "ui.shuffle": "Aléatoire",
  "ui.shuffle_all": "Tout lire en aléatoire",
  "ui.shuffle_favorites": "Lecture aléatoire des favoris",
  "ui.shuffle_play": "Lecture aléatoire",
  "ui.skipped_to_up_next": "Passé à la piste suivante",
  "ui.sleep_timer": "Minuterie de mise en veille",
  "ui.sleep_timer_2": "Minuterie de mise en veille",
  "ui.sleep_timer_active_remaining": "Minuterie de mise en veille: {remaining}",
  "ui.sleep_timer_added_minutes": "{minutes} minutes ajoutées à la minuterie",
  "ui.sleep_timer_and_morning_playback": "Minuterie de mise en veille et démarrage programmé",
  "ui.sleep_timer_and_scheduled_playback": "Minuterie de mise en veille et démarrage programmé",
  "ui.sleep_timer_cleared": "Minuterie de mise en veille effacée",
  "ui.sleep_timer_finished": "Minuterie de mise en veille terminée",
  "ui.sleep_timer_presets": "Préréglages de minuterie de mise en veille",
  "ui.sleep_timer_set_for_15_minutes": "Mise en veille programmée dans 15 minutes",
  "ui.sleep_timer_set_minutes": "Mise en veille programmée dans {minutes} minutes",
  "ui.sleep_timer_updated_minutes": "Mise en veille re-programmée dans {minutes} minutes",
  "ui.smaller_lyrics": "Paroles plus petites",
  "ui.smart": "Intelligent",
  "ui.smart_mix": "mix intelligent",
  "ui.smart_mix_builder": "Générateur de mix intelligent",
  "ui.smart_voice_selection": "Sélection vocale intelligente",
  "ui.soft_and_relaxed": "Doux et relaxant",
  "ui.soft_relaxed_music": "Musique relaxante, douce",
  "ui.some_players_could_not_be_stopped_or_cleared": "Certains lecteurs n'ont pu être arrêté ou vidé",
  "ui.songs_around_an_artist": "Chansons d'un artiste",
  "ui.sort": "Trier",
  "ui.sort_ascending": "Par ordre croissant",
  "ui.sort_descending": "Par ordre décroissant",
  "ui.source_queue": "Source de la file d'attente",
  "ui.spain": "Espagne",
  "ui.speak_to_target_players": "Communiquer dans les lecteurs cibles",
  "ui.start_radio_mode": "Démarrer le mode radio",
  "ui.start_time": "Heure du début",
  "ui.start_time_2": "Heure du début",
  "ui.started_on_selected_player": "Démarré sur les lecteurs sélectionnés",
  "ui.starting_a_chill_mix": "Démarrage d'un mix chill",
  "ui.starting_quick_mix": "Démarrage du Mix Rapide...",
  "ui.stations": "stations",
  "ui.stay_active": "Rester actif",
  "ui.stays_active": "Reste actif",
  "ui.stop_all": "Tout arrêter",
  "ui.stop_all_players": "Arrêter tous les lecteurs",
  "ui.stop_command_failed": "Échec de la commande Arrêter",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Arrête la lecture, vide la file d'attente et déconnecte les groupes",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Lecteurs arrêtés, file d'attente vidée et groupes déconnectés.",
  "ui.stops_this_browser_player": "Arrête ce lecteur Web",
  "ui.strong": "Fort",
  "ui.studio": "Studio",
  "ui.studio_closed": "Studio fermé",
  "ui.studio_media_action_failed": "Échec de l'action du Studio",
  "ui.studio_mix_started": "Mix Studio démarré",
  "ui.studio_opened": "Studio ouvert",
  "ui.studio_pro": "studio pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Scène Studio",
  "ui.studio_scene_deleted": "Scène Studio supprimée",
  "ui.studio_scene_saved": "Scène Studio enregistrée",
  "ui.studio_scene_was_not_found": "La scène Studio n'a pas été trouvé",
  "ui.studio_search": "rechercher studio",
  "ui.studio_shortcut": "Raccourci Studio",
  "ui.subtle": "Subtil",
  "ui.sun": "Dim",
  "ui.surprise_me": "Surprenez moi",
  "ui.sync": "Synchronisation",
  "ui.sync_lyrics": "Synchroniser les paroles",
  "ui.tablet": "Tablette",
  "ui.tap_to_start_a_sleep_timer": "Toucher pour démarrer une minuterie de mise en veille",
  "ui.target_players": "Lecteurs cibles",
  "ui.target_queue": "File d'attente cible",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Les annonces textuelles utilisent le TTS de Home Assistant sur le lecteur Music Assistant sélectionné. Les annonces par URL utilisent la lecture d’annonces de Music Assistant.",
  "ui.text_only": "Texte seulement",
  "ui.text_tone": "Couleur du texte",
  "ui.theme": "Thème",
  "ui.theme_2": "Thème",
  "ui.theme_mode": "Mode du thème",
  "ui.theme_toggle": "Sélecteur de thème",
  "ui.theme_toggle_2": "Sélecteur de thème",
  "ui.this_device": "Cet appareil",
  "ui.this_device_2": "Cet appareil",
  "ui.this_device_player_connected": "Lecteur de cet appareil connecté",
  "ui.this_device_player_disconnected": "Lecteur de cet appareil déconnecté",
  "ui.thu": "Jeu",
  "ui.timer": "Minuterie",
  "ui.timer_active_remaining": "Minuterie active: {remaining}",
  "ui.timers": "Minuteries",
  "ui.to": "À",
  "ui.today": "Aujourd'hui",
  "ui.track": "Piste",
  "ui.track_lyrics": "Paroles",
  "ui.tracks": "Pistes",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Transférer, dupliquer, inspecter ou vider les files d'attente sans masquer le Studio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "Le transfert n'est pas disponible lorsqu'un lecteur épinglé est configuré",
  "ui.transfer_queue": "Transférer la file d'attente",
  "ui.transfer_queue_2": "Transférer la file d'attente",
  "ui.transfer_queue_3": "Transférer la file d'attente",
  "ui.try_again": "Réessayer",
  "ui.tts_entity": "Entité TTS",
  "ui.tts_entity_used_by_the_announcement_screen": "Entité TTS utilisée par l'écran d'annonce.",
  "ui.tts_language": "Langue du TTS",
  "ui.tue": "Mar",
  "ui.turkey": "Turquie",
  "ui.turn_off": "Désactiver",
  "ui.turns_off_after_run": "Se désactive après l'exécution",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Désactive les effets visuels intensifs pour une meilleure performance sur les Nest Hub et les tablettes moins puissantes.",
  "ui.type_a_free_style_first": "Saisissez d'abord un style",
  "ui.type_an_announcement": "Saisissez une annonce...",
  "ui.type_anything": "Saisissez n'importe quoi",
  "ui.type_what_should_be_announced": "Saisissez ce qui doit être annoncé...",
  "ui.ungroup": "Dissocier",
  "ui.ungroup_2": "Dissocier",
  "ui.united_kingdom": "Royaume-Uni",
  "ui.united_states": "États-Unis",
  "ui.unknown": "Inconnu",
  "ui.up": "Haut",
  "ui.move_down": "Descendre",
  "ui.move_up": "Monter",
  "ui.up_next": "À suivre",
  "ui.up_next_2": "À suivre",
  "ui.up_next_title": "À suivre: {title}",
  "ui.upbeat_and_moving": "Énergique et entraînant",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Utilisez n'importe quel chemin Home Assistant, par exemple /lovelace/home",
  "ui.use_current_view_for_home_button": "Utiliser la vue actuelle pour le bouton Accueil",
  "ui.use_expand_to_open_the_full_player": "Utilisez le bouton d'agrandissement pour ouvrir le lecteur complet",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Utilise la même liste de pays de base affichée dans l’écran des paramètres intégrés à la carte.",
  "ui.visible_tiles": "tuiles visibles",
  "ui.visible_tiles_2": "Tuiles visibles",
  "ui.visual_editor": "Éditeur visuel",
  "ui.assist_agent": "Agent Assist",
  "ui.assist_agent_id": "ID de l'agent Assist",
  "ui.assist_only": "Assist seulement",
  "ui.default_assist_agent": "Home Assistant Assist par défaut",
  "ui.default_order": "Ordre par d?faut",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Recommandé : les commandes musicales sont exécutées localement. Tout ce que la carte ne comprend pas est envoyé à Home Assistant Assist.",
  "ui.hybrid_music_plus_assist": "Hybride: Musique + Assist",
  "ui.icon_size": "Taille des icônes",
  "ui.music_only": "Musique seulement",
  "ui.music_plus_assist": "Musique + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "ID optionnel de l’agent Assist. Laissez vide pour utiliser l’agent Assist par défaut de Home Assistant.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Optionnel. Laissez l’agent par défaut sauf si vous utilisez plusieurs agents Assist et souhaitez que cette carte en utilise un spécifique.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Affiche un bouton push-to-talk pour les commandes musicales et de l'agent Assist.",
  "ui.show_home_inside_quick_actions": "Affiche Accueil comme une action rapide au lieu d'un bouton flottant.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Afficher les boutons plus et moins avec le curseur de volume.",
  "ui.speak_voice_assistant_responses_out_loud": "Lit les réponses reconnues de l’assistant vocal à voix haute dans ce navigateur lorsque cette fonction est prise en charge.",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "Assistant vocal",
  "ui.voice_assistant_disabled": "Assistant vocal désactivé",
  "ui.voice_assistant_listening": "Écoute en cours...",
  "ui.voice_assistant_mode": "Mode de l’assistant vocal",
  "ui.voice_command_executed": "Commande vocale exécutée",
  "ui.voice_command_completed_action": "Terminé : {action}",
  "ui.voice_command_failed": "Échec de la commande vocale",
  "ui.voice_command_no_player": "Sélectionnez un lecteur ou dites son nom à voix haute.",
  "ui.voice_command_not_understood": "Je n’ai pas compris cette commande.",
  "ui.voice_command_sent": "Commande vocale envoyée",
  "ui.voice_done_status": "Terminé",
  "ui.voice_error_status": "Attention requise",
  "ui.voice_feedback": "Retour vocal",
  "ui.voice_group_connect_needs_players": "Dites au moins deux noms de lecteurs, ou sélectionnez un lecteur puis dites le nom de l’autre.",
  "ui.voice_group_connected_players": "{primary} connecté à {members}",
  "ui.voice_group_disconnect_needs_player": "Indiquez quel groupe de lecteurs doit être déconnecté.",
  "ui.voice_group_disconnected_player": "Le groupe de {player} a été déconnecté",
  "ui.voice_input_failed": "Échec de l'entrée vocale",
  "ui.voice_input_is_not_supported_on_this_device": "L'entrée vocale n'est pas prise en charge sur cet appareil",
  "ui.voice_language_is_not_supported": "La langue de la reconnaissance vocale sélectionnée n'est pas prise en charge par ce navigateur.",
  "ui.voice_match_not_confident": "J’ai trouvé {title}, mais cela ne correspond pas assez à « {query} ». Réessayez en disant le titre et l’artiste.",
  "ui.voice_music_searching": "Recherche de musique en cours: {query}",
  "ui.voice_playing_result": "Lecture de {title}",
  "ui.voice_queue_transfer_needs_players": "Dites la source et la cible du lecteur. Par exemple: transfère la file d'attente de Salon vers Cuisine.",
  "ui.voice_queue_transferred_between": "File d'attente transférée de {source} vers {target}",
  "ui.voice_starting_playback": "Démarrage de la lecture: {title}...",
  "ui.voice_processing_status": "Traitement en cours...",
  "ui.voice_ready_status": "Prêt",
  "ui.voice_recognition_network_failed": "Le service de reconnaissance vocale est indisponible. Les permissions du microphone peuvent être correctes ; vérifiez le navigateur, le VPN, le DNS, le pare-feu ou le réseau.",
  "ui.voice_response": "Réponse",
  "ui.voice_response_will_appear_here": "La réponse apparaîtra ici.",
  "ui.voice_search": "Recherche vocale",
  "ui.voice_search_failed": "Échec de la recherche vocale",
  "ui.voice_search_is_not_supported_on_this_device": "La recherche vocale n'est pas prise en charge sur cet appareil",
  "ui.voice_transcript": "Transcription",
  "ui.voice_listening_status": "Écoute...",
  "ui.waiting_for_speech": "En attente d'une commande vocale...",
  "ui.microphone_permission_or_browser_blocked": "Le navigateur empêche la reconnaissance vocale. Vérifiez les paramètres du navigateur.",
  "ui.volume": "Volume",
  "ui.volume_and_player_target": "Volume et lecteur cible",
  "ui.volume_boost": "Gain de volume",
  "ui.volume_control_large_screen_only": "Contrôle du volume (écrans larges seulement)",
  "ui.volume_down": "Baisser le volume",
  "ui.volume_mode": "Mode du volume",
  "ui.volume_presets": "Préréglages de volume",
  "ui.volume_step_between_1_and_10_percent_default_5": "Incrémentation du volume entre 1 % et 10 %. Par défaut : 5 %.",
  "ui.volume_step_buttons": "Boutons volume +/-",
  "ui.volume_step_percent": "Incrémentation du volume",
  "ui.volume_up": "Augmenter le volume",
  "ui.waiting_for_this_device_player": "En attente du lecteur de cet appareil...",
  "ui.wake": "Démarrage",
  "ui.wake_schedules": "Démarrage programmé",
  "ui.wed": "Mer",
  "ui.week": "Semaine",
  "ui.what_would_you_like_to_listen_to": "Qu'aimeriez-vous écouter?",
  "ui.will_play_next": "Sera lu ensuite",
  "ui.will_play_next_in_studio": "Sera lu ensuite dans le Studio",
  "ui.worldwide_popular": "Populaire dans le monde entier",
  "ui.add_player_light_pair": "Ajouter une paire",
  "ui.ambient_light": "Lumière ambiante",
  "ui.ambient_light_brightness": "Luminosité",
  "ui.ambient_light_cooldown": "Délai entre les mises à jour",
  "ui.ambient_light_entities": "Couleur par défaut",
  "ui.ambient_light_player_map": "Association lecteur/lumière",
  "ui.ambient_light_player_map_helper": "Créez des paires lumières-lecteurs pour la lumière ambiante. Une paire par ligne ; effacez un lecteur ou ses lumières pour supprimer la paire.",
  "ui.ambient_light_transition": "Transition",
  "ui.analog": "Analogue",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Choisissez uniquement les lumières compatibles avec les couleurs qui doivent suivre la musique lorsqu’aucune paire spécifique à un lecteur n’existe.",
  "ui.digital": "Numérique",
  "ui.discover_albums": "Découvrir des albums",
  "ui.discover_music": "Découvrir",
  "ui.discover_subtitle": "Playlists, albums et radios recommandés.",
  "ui.discovery": "Découverte",
  "ui.discovery_failed": "Échec du mode Découverte",
  "ui.discovery_mode": "Mode Découverte",
  "ui.discovery_started": "Découverte démarrée: {mood}",
  "ui.maximum_brightness_for_music_lighting": "Luminosité maximale de l'éclairage musical",
  "ui.minimum_seconds_between_light_updates": "Délai minimum en secondes entre les mises à jour",
  "ui.player_light_pair_lights": "Lumières colorées {number}",
  "ui.player_light_pair_lights_helper": "Choisissez les lumières compatibles avec les couleurs pour ce lecteur Music Assistant. Effacez ce champ ou le lecteur pour supprimer la paire.",
  "ui.player_light_pair_player": "Lecteur Music Assistant {number}",
  "ui.player_light_pair_player_helper": "Choisissez le lecteur MA pour cette lumière.",
  "ui.all": "Tous",
  "ui.all_genres": "Tous les styles",
  "ui.genres_and_styles": "Styles et genres",
  "ui.compact_widget_mode": "Style du widget compact",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Choisissez le mode du widget. Mini widget affiche une carte compacte adaptée pour mobile.",
  "ui.full": "Complet",
  "ui.mini_widget": "Mini widget",
  "ui.genre": "Style",
  "ui.mood_and_genre": "Ambiance et style",
  "ui.music_by_mood_and_genre": "Musique par ambiance et style",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "(Optionnel) Entité pour le bouton d'alimentation. Laissez vide pour arrêter le lecteur actif.",
  "ui.optional_name_for_the_auxiliary_button": "(Optionnel) Nom du bouton auxiliaire.",
  "ui.choose_the_auxiliary_button_icon": "Choisissez l'icône affichée sur le bouton auxiliaire.",
  "ui.choose_how_players_are_sorted": "Choisissez l'ordre des lecteurs : par défaut, alphabétique ou personnalisé.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Choisissez les lecteurs Music Assistant qui ne doivent jamais apparaître sur la carte.",
  "ui.custom_order": "Ordre personnalisé",
  "ui.no_visible_music_assistant_players_after_exclusions": "Tous les lecteurs Music Assistant sont masqués par le paramètre des lecteurs exclus.",
  "ui.set_custom_player_order": "Définissez l'ordre des lecteurs. Les emplacements vides sont ignorés.",
  "ui.show_auxiliary_button_in_quick_actions": "Affiche ce bouton auxiliaire sur la ligne des actions rapides.",
  "ui.power": "Alimentation",
  "ui.power_action_failed": "Échec de l'action d'alimentation",
  "ui.power_action_sent": "Action d'alimentation envoyé",
  "ui.power_button": "Bouton d'alimentation",
  "ui.power_button_action": "Action du bouton d'alimentation",
  "ui.power_button_entity": "Entité du bouton d'alimentation",
  "ui.scene": "Scène",
  "ui.screensaver": "Économiseur d'écran",
  "ui.screensaver_buttons": "Boutons de l'économiseur d'écran",
  "ui.screensaver_controls": "Contrôles de l'économiseur d'écran",
  "ui.screensaver_clock_mode": "Mode de l'horloge de l'économiseur d'écran",
  "ui.screensaver_clock_size": "Taille de l'horloge",
  "ui.screensaver_clock_size_helper": "Ajuste la taille de l'horloge sur l'économiseur d'écran sans déplacer la pochette.",
  "ui.screensaver_clock_x": "Position de l'horloge - GAUCHE / DROITE",
  "ui.screensaver_clock_x_helper": "Diminuez la valeur pour déplacer à gauche, augmentez pour déplacer à droite.",
  "ui.screensaver_clock_y": "Position de l'horloge - HAUT / BAS",
  "ui.screensaver_clock_y_helper": "Diminuez la valeur pour déplacer vers le haut, augmentez la valeur pour déplacer vers le bas.",
  "ui.screensaver_message": "Message sur l'économiseur d'écran",
  "ui.screensaver_timeout": "Délai de l'économiseur d'écran",
  "ui.script": "Script",
  "ui.short_optional_message_shown_on_the_screensaver": "Court message affiché sur l'économiseur d'écran. (optionnel)",
  "ui.show_previous_next_controls_on_the_screensaver": "Afficher les boutons suivant/précédent sur l'économiseur d'écran.",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Choisissez pr?cis?ment quels boutons apparaissent sur l'économiseur d'écran, y compris FLOW ASSISTANT, précédent et suivant.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Afficher une horloge discrète et la pochette après un temps d'inactivité.",
  "ui.show_power_button_in_the_player_controls": "Afficher le bouton d'alimentation dans les contrôles du lecteur.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Afficher le mode Découverte en plein écran dans les Actions.",
  "ui.smart_home": "Maison intelligente",
  "ui.soft_transition_time_for_music_lighting": "Transitions douces pour la l'éclairage musical.",
  "ui.stop_player": "Arrêter le lecteur",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Synchroniser les lumières sélectionnées avec la couleur de pochette du média actif.",
  "ui.toggle": "Basculer",
  "ui.turn_on": "Activer",
  "ui.choose_a_category_to_browse_provider_playlists": "Choisissez une catégorie pour parcourir les listes de lecture du fournisseur avant de lancer la lecture.",
  "ui.new_and_recommended": "Nouveau et recommandé",
  "ui.provider_playlists": "Listes de lecture du fournisseur",
  "ui.hotel_mode": "Mode hôtel",
  "ui.hotel_mode_helper": "Interface minimale sécurisée pour l'hôtel : commandes du lecteur, volume, recherche, navigation dans les pochettes et sélection du lecteur uniquement.",
  "ui.style_and_genres": "Styles et genres"
});
const messages$3 = Object.freeze({
  "ui.a_guided_music_wizard": "אשף מוזיקה ברור ומהיר",
  "ui.a_radio_station": "תחנה זמינה",
  "ui.accent_color": "צבע מוביל",
  "ui.actions": "פעולות",
  "ui.actions_2": "פעולות",
  "ui.active_days": "ימים פעילים",
  "ui.active_player_helper": "Helper לנגן פעיל",
  "ui.active_player_helper_must_be_an_input_text_entity": "ה־helper של הנגן הפעיל חייב להיות מסוג input_text",
  "ui.active_players": "נגנים פעילים",
  "ui.add": "הוסף",
  "ui.add_to_queue": "הוסף לתור",
  "ui.add_to_selection": "הוסף לבחירה",
  "ui.added_to_liked": "נוסף לאהבתי",
  "ui.added_to_music_assistant_liked": "נוסף לאהבתי של מיוזיק אסיסטנט",
  "ui.added_to_queue": "נוסף לתור",
  "ui.added_to_studio_queue": "נוסף לתור הסטודיו",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "מוסיף תנועה לשכבות הרקע. מצב עדין שומר על מראה רגוע, מצב חזק כבר מורגש, ואקסטרים נותן את התנועה הכי נועזת.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "מוסיף אחוזים לווליום הקיים רק בזמן הכריזה ואז מחזיר למה שהיה לפני.",
  "ui.after_run": "אחרי הפעלה",
  "ui.album": "אלבום",
  "ui.albums": "אלבומים",
  "ui.all_albums": "כל האלבומים",
  "ui.all_artists": "כל האמנים",
  "ui.all_countries": "כל המדינות",
  "ui.all_player_groups_disconnected": "כל קבוצות הנגנים נותקו",
  "ui.all_players": "כל הנגנים",
  "ui.all_players_2": "כל הנגנים",
  "ui.all_podcasts": "כל הפודקאסטים",
  "ui.all_tracks": "כל השירים",
  "ui.allow_local_likes": "אהבתי מקומי",
  "ui.always_visible": "פעיל תמיד",
  "ui.announce": "כרוז",
  "ui.announce_to_all_players": "כרוז לכולם",
  "ui.announcement": "כריזה",
  "ui.announcement_failed_with_error": "?????? ?????{error}",
  "ui.announcement_language": "שפת כריזה",
  "ui.announcement_presets": "משפטי כריזה",
  "ui.announcement_sent_to_player": "?????? ????? ?? {player}",
  "ui.announcement_studio": "סטודיו כריזה",
  "ui.announcement_studio_2": "סטודיו כריזה",
  "ui.announcement_target": "יעד כריזה",
  "ui.announcement_to_player_preview": "????? ?? {player}: {preview}",
  "ui.announcement_volume_boost": "תוספת ווליום כריזה",
  "ui.announcements": "כריזה",
  "ui.appearance": "מראה",
  "ui.alphabetical": "אלפביתי",
  "ui.auxiliary_button": "לחצן עזר",
  "ui.auxiliary_button_action": "פעולת לחצן עזר",
  "ui.auxiliary_button_entity": "ישות לחצן עזר",
  "ui.auxiliary_button_icon": "אייקון לחצן עזר",
  "ui.auxiliary_button_name": "שם לחצן עזר",
  "ui.auxiliary_buttons": "לחצני עזר",
  "ui.apply_group": "חבר קבוצה",
  "ui.apply_schedule": "אישור",
  "ui.artist": "אמן",
  "ui.artist_radio": "רדיו אמן",
  "ui.artists": "אמנים",
  "ui.artwork": "עטיפה",
  "ui.artwork_swipe": "סוויפ על עטיפה",
  "ui.ascending": "סדר עולה",
  "ui.at_least_one_player_must_stay_selected": "נגן אחד חייב להישאר נבחר",
  "ui.australia": "אוסטרליה",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Auto בוחר מובייל או טאבלט לפי רוחב בפועל.",
  "ui.auto_cloud_default": "אוטומטי / ברירת מחדל בענן",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "מצב Auto מחלץ צבעים מהעטיפה הפעילה ושומר על מראה עדין. מצב Strong נותן נוכחות עשירה ובולטת יותר.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Auto לא שולח שפה ל־Home Assistant Cloud כדי להשתמש בברירת המחדל שלך. בחירה ידנית תשלח language מפורש.",
  "ui.available": "זמין",
  "ui.back": "חזרה",
  "ui.back_2": "חזור",
  "ui.back_to_countries": "חזרה למדינות",
  "ui.back_to_queue": "חזרה לתור",
  "ui.background_motion": "תנועת רקע",
  "ui.behavior": "התנהגות",
  "ui.browse_covers": "מדפדף עטיפות",
  "ui.browse_playlists_artists_albums_and_radio": "דפדוף בפלייליסטים, אמנים, אלבומים ורדיו",
  "ui.browser_players": "נגני דפדפן",
  "ui.build_custom_mix": "בנה מיקס חופשי",
  "ui.building_studio_mix": "בונה מיקס סטודיו...",
  "ui.button": "כפתור",
  "ui.by_mood": "לפי סגנון",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "משך הקאש במילישניות עבור קריאות נתונים מסוימות.",
  "ui.cache_ttl": "זמן קאש",
  "ui.calm": "רגוע",
  "ui.canada": "קנדה",
  "ui.cancel": "ביטול",
  "ui.cancel_2": "בטל",
  "ui.cancel_selection": "בטל בחירה",
  "ui.card_configuration": "מהגדרות הכרטיס",
  "ui.card_height": "גובה הכרטיס",
  "ui.card_height_in_pixels": "גובה הכרטיס בפיקסלים.",
  "ui.card_settings_are_managed_from_the_visual_editor": "הגדרות הכרטיס מנוהלות מתוך העורך הוויזואלי",
  "ui.change_song": "מעביר שיר",
  "ui.change_track": "מעביר שיר",
  "ui.chill_mix": "מיקס רגוע",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "בחר מדינה, או כל המדינות כדי לדפדף בכל המדינות מתוך טאב הרדיו.",
  "ui.choose_a_queue_position": "בחר מיקום בתור",
  "ui.choose_a_source_player": "בחר נגן מקור",
  "ui.choose_a_target_player": "בחר נגן יעד",
  "ui.choose_at_least_one_player": "בחר לפחות נגן אחד",
  "ui.choose_icons_text_or_both_for_the_footer": "קובע אם הפוטר יוצג כאייקונים, טקסט או שניהם.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "בחר רק נגני Music Assistant. בתוך הכרטיס יוצגו לקיבוע רק נגני Music Assistant כדי למנוע בלבול.",
  "ui.choose_one_clear_option": "בחר אפשרות אחת ברורה",
  "ui.choose_player": "בחר נגן",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "בחר משהו מהמדף המהיר או לחץ על השרביט לפלייליסט אקראי.",
  "ui.choose_source": "בחר מקור",
  "ui.choose_target": "בחר יעד",
  "ui.choose_target_player": "בחר נגן יעד",
  "ui.choose_the_accent_color_for_the_mobile_layout": "בחר צבע בסיס לעיצוב המובייל.",
  "ui.choose_the_music": "איזו מוזיקה?",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "בחר וסדר את הכפתורים הקטנים שיופיעו מתחת לשם השיר.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "בחר מקור מוזיקה. הניגון יישלח ליעד הנוכחי.",
  "ui.choose_where_to_play": "איפה לנגן?",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "קובע האם סוויפ על עטיפת האלבום יעביר שיר או ידפדף עטיפות.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "בחר אם ההגדרות נשלטות מתוך ה־UI בכרטיס או מתוך הגדרות הכרטיס בדשבורד.",
  "ui.choose_which_actions_appear_in_the_main_bar": "בחר אילו כפתורים יופיעו בסרגל הראשי.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "בחר באילו ימים חלון מצב הלילה יהיה פעיל במצב Auto.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "בחר אילו אייקונים יופיעו בשורת הפעולות המהירות. נקה הכל יבקש אישור לפני ניתוק הנגנים הפעילים.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "בחר אילו נגנים יופיעו כאריחים בחדר.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "בחר אילו נגנים יישארו בבחירה הנוכחית.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "בחר אילו טאבים יהיו זמינים במסך הספרייה.",
  "ui.choose_who_is_controlled": "בחר על מי שולטים",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "בחירה ב־Visual מבטלת את מסך ההגדרות מתוך הכרטיס.",
  "ui.clean_all": "נקה הכל",
  "ui.clean_all_confirm_action": "נקה הכל",
  "ui.clean_all_confirm_copy": "הפעולה תנתק את כל הנגנים הפעילים, תעצור נגינה ותנקה את התורים שלהם. להמשיך?",
  "ui.clean_all_confirm_title": "לנקות את כל הנגנים?",
  "ui.clean_the_studio_wall": "סדר את מסך הסטודיו",
  "ui.clear": "נקה",
  "ui.clear_queue": "נקה תור",
  "ui.clear_search": "נקה חיפוש",
  "ui.clone_queue": "העתק תור",
  "ui.close": "סגור",
  "ui.collapse_compact_player": "חזרה למצב קומפקטי",
  "ui.compact_mode": "מצב קומפקטי",
  "ui.configure_ready_made_announcement_phrases": "אפשר להגדיר כמה משפטי כריזה מוכנים מראש.",
  "ui.connect_this_device": "חבר את המכשיר הזה",
  "ui.connect_this_device_2": "חבר מכשיר זה",
  "ui.connected": "מחובר",
  "ui.connected_2": "מחוברים",
  "ui.connected_players": "נגנים מחוברים",
  "ui.connected_players_2": "נגנים מחוברים",
  "ui.connecting": "מתחבר",
  "ui.connecting_2": "מתחבר...",
  "ui.confirm": "אישור",
  "ui.connection_and_behavior": "חיבור והתנהגות",
  "ui.connects_directly_from_this_card_using_sendspin": "מתחבר ישירות מתוך הכרטיס דרך Sendspin",
  "ui.continue": "מעבר",
  "ui.continue_2": "המשך",
  "ui.continue_from_recent_music_assistant_activity": "המשך מהאזנות אחרונות ב־Music Assistant.",
  "ui.continue_listening": "המשך האזנה",
  "ui.continue_what_was_played_recently": "המשך ממה שהתנגן לאחרונה",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "שולט אם רקע הכרטיס נע בעדינות, ובאיזו עוצמה.",
  "ui.controlling": "שולט עכשיו על",
  "ui.could_not_build_quick_mix_from_the_current_song": "לא הצלחתי לבנות Quick Mix מהשיר הנוכחי",
  "ui.could_not_build_studio_mix": "לא הצלחתי לבנות מיקס סטודיו",
  "ui.could_not_choose_media": "לא ניתן לבחור מדיה",
  "ui.could_not_clone_the_queue": "לא הצלחתי להעתיק את התור",
  "ui.could_not_find_music": "לא הצלחתי למצוא מוזיקה",
  "ui.could_not_play_label": "?? ?????? ????: {label}",
  "ui.could_not_start_chill_mix": "לא ניתן להפעיל מיקס רגוע",
  "ui.could_not_start_playback": "לא הצלחתי להתחיל ניגון",
  "ui.could_not_start_playback_2": "לא ניתן להתחיל ניגון",
  "ui.could_not_start_playback_in_studio": "לא הצלחתי להפעיל ניגון בסטודיו",
  "ui.could_not_start_quick_mix": "לא ניתן להפעיל Quick Mix",
  "ui.could_not_start_radio_playback": "לא ניתן להתחיל ניגון רדיו",
  "ui.could_not_transfer_the_queue": "לא הצלחתי להעביר את התור",
  "ui.could_not_update_active_player_helper": "לא ניתן לעדכן helper לנגן פעיל",
  "ui.create_schedule": "צור תזמון",
  "ui.current_path": "נתיב נוכחי",
  "ui.custom": "אישי",
  "ui.dark": "כהה",
  "ui.delete_scene": "מחק סצנה",
  "ui.delete_schedule": "מחק תזמון",
  "ui.descending": "סדר יורד",
  "ui.dictate": "הכתבה קולית",
  "ui.disabled": "כבוי",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "מכבה טשטושים, אנימציות, רקעים דינמיים וצללים כבדים. מומלץ ל־Nest Hub וטאבלטים חלשים.",
  "ui.disconnect": "נתק",
  "ui.disconnect_all": "נתק הכל",
  "ui.disconnect_groups": "נתק קבוצות",
  "ui.disconnect_player_groups": "נתק קבוצות נגנים",
  "ui.disconnect_this_device": "נתק את המכשיר הזה",
  "ui.disconnected": "מנותק",
  "ui.disconnecting": "מתנתק",
  "ui.disconnecting_player_groups": "מנתק קבוצות נגנים...",
  "ui.discover": "גלה",
  "ui.down": "למטה",
  "ui.dynamic_theme": "ערכת נושא דינמית",
  "ui.edit_schedule": "עריכת תזמון",
  "ui.enable": "הפעל",
  "ui.enable_or_disable_the_footer_search_button": "מפעיל או מכבה את כפתור החיפוש בפוטר.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "מפעיל או מכבה את כפתור הסטודיו בסרגל התחתון.",
  "ui.enable_the_floating_home_shortcut_button": "מפעיל את כפתור הבית הצף.",
  "ui.enabled": "פעיל",
  "ui.enables_switching_to_local_liked_mode": "מפעיל אפשרות לעבור ל־Liked mode מקומי.",
  "ui.end_time": "שעת סיום",
  "ui.excluded_players": "נגנים מוחרגים",
  "ui.energetic": "קצבי",
  "ui.energy_and_rhythm": "אנרגיה וקצב",
  "ui.enter_an_announcement_first": "קודם צריך להזין הודעת כריזה",
  "ui.example_quiet_jazz_greek_music_workout": "לדוגמה: ג'אז רגוע, מוזיקה יוונית, אימון",
  "ui.existing_content": "תוכן קיים",
  "ui.expand_player": "הרחב נגן",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "מחלץ צבעים מעטיפת האלבום ומחיל אותם על הממשק.",
  "ui.extreme": "אקסטרים",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Fallback למערכות שבהן תור ההשמעה דורש נתיב service חלופי.",
  "ui.fallback_mode": "מצב בסיסי",
  "ui.family_friendly": "ידידותי למשפחה",
  "ui.favorite": "אהבתי",
  "ui.favorite_action_failed": "פעולת אהבתי נכשלה",
  "ui.favorite_action_sent": "פעולת אהבתי נשלחה",
  "ui.favorite_button_entity": "ישות כפתור Like",
  "ui.favorite_center": "מרכז אהבתי",
  "ui.favorite_center_2": "מרכז אהבתי",
  "ui.favorite_radio": "תחנות מועדפות",
  "ui.favorite_updated": "אהבתי עודכן",
  "ui.favorites_and_liked_music": "מועדפים ומוזיקה אהובה",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "זיהוי יכולות, הקשר נגן וכלים לנגן המכשיר הזה.",
  "ui.find_music": "מצא מוזיקה",
  "ui.finding_music": "מחפש מוזיקה",
  "ui.fixed": "קבוע",
  "ui.flow_mix": "מיקס FLOW",
  "ui.font_scale": "סקייל פונט",
  "ui.font_size": "גודל פונט",
  "ui.footer_search": "חיפוש בפוטר",
  "ui.footer_style": "מראה פוטר",
  "ui.footer_style_2": "סגנון פוטר",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "למשל /lovelace/home או כל נתיב אחר ב-Home Assistant.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "למסכים חלשים: מכבה טשטושים כבדים, אנימציות, חילוץ צבעים מהעטיפה ותנועת רקע.",
  "ui.france": "צרפת",
  "ui.free_style": "סגנון חופשי",
  "ui.free_style_quiet_jazz_greek_music_workout": "סגנון חופשי: ג'אז רגוע, יוונית, אימון...",
  "ui.fresh_start": "פתיחה נעימה ליום",
  "ui.fresh_start_2": "פתיחה קלילה",
  "ui.fri": "ו׳",
  "ui.friendly_and_simple": "שמח וברור",
  "ui.from": "מ־",
  "ui.general": "כללי",
  "ui.general_and_display": "כללי ותצוגה",
  "ui.germany": "גרמניה",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "סקייל כללי לכל הפונטים בממשק. 1 הוא ברירת המחדל.",
  "ui.greece": "יוון",
  "ui.grid": "גריד",
  "ui.group": "קבוצה",
  "ui.group_cleared": "הקבוצה פורקה",
  "ui.group_cleared_2": "הקבוצה נותקה",
  "ui.group_speakers": "חיבור רמקולים",
  "ui.group_speakers_2": "קבוצת נגנים",
  "ui.group_updated": "הקבוצה עודכנה",
  "ui.group_volume": "ווליום קבוצה",
  "ui.group_volume_up_energetic_mix": "קבוצה, ווליום גבוה ומיקס קצבי",
  "ui.grouped_players": "נגנים בקבוצה",
  "ui.hebrew": "עברית",
  "ui.history": "היסטוריה",
  "ui.history_tabs": "טאבי היסטוריה",
  "ui.home": "בית",
  "ui.home_party_night_presets": "בית, מסיבה, לילה",
  "ui.home_path": "נתיב הבית",
  "ui.home_scene_prepared": "סצנת בית מוכנה",
  "ui.home_shortcut": "כפתור בית",
  "ui.home_shortcut_path": "נתיב כפתור בית",
  "ui.icon_and_text": "אייקון ומלל",
  "ui.icon_only": "אייקון בלבד",
  "ui.icon_plus_text": "אייקון ומלל",
  "ui.idle": "פנוי",
  "ui.idle_2": "ממתין",
  "ui.in_card_ui": "מה־UI בתוך הכרטיס",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "כולל גם מצב אישי כמו במסך ההגדרות של הכרטיס.",
  "ui.info": "מידע",
  "ui.israel": "ישראל",
  "ui.israeli": "ישראלי",
  "ui.italy": "איטליה",
  "ui.items": "פריטים",
  "ui.join_selected_players": "חבר נגנים נבחרים",
  "ui.kids": "ילדים",
  "ui.language": "שפה",
  "ui.larger_lyrics": "מילים גדולות יותר",
  "ui.layout_adapted_to_height": "הפריסה הותאמה לגובה",
  "ui.layout_mode": "מצב פריסה",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "השאר ריק אם הכרטיס ניגש ל־Music Assistant דרך Home Assistant בלבד.",
  "ui.library": "ספריה",
  "ui.library_2": "ספרייה",
  "ui.library_pages": "דפי ספריה",
  "ui.default_library_layout": "תצוגת ברירת מחדל לספריה",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "בחר איך דפי הספריה ייפתחו. עדיין אפשר להחליף Grid/List ידנית בתוך הספריה.",
  "ui.library_radio": "רדיו מהספריה",
  "ui.library_surprise": "הפתעה מהספריה",
  "ui.library_tabs": "דפי ספרייה",
  "ui.library_tabs_2": "טאבי ספרייה",
  "ui.light": "בהיר",
  "ui.like": "לייק",
  "ui.like_2": "אהבתי",
  "ui.like_3": "סמן אהבתי",
  "ui.liked": "אהבתי",
  "ui.liked_mode": "מצב אהבתי",
  "ui.liked_sync": "סנכרון אהבתי",
  "ui.list": "רשימה",
  "ui.listening": "מקשיב...",
  "ui.loading": "טוען...",
  "ui.loading_favorites": "טוען אהובים...",
  "ui.loading_library": "טוען ספרייה...",
  "ui.loading_lyrics": "טוען מילים...",
  "ui.loading_players": "טוען נגנים...",
  "ui.loading_queue": "טוען תור...",
  "ui.loading_recent_items": "טוען אחרונים...",
  "ui.loading_schedules": "טוען תזמונים...",
  "ui.local": "מקומי",
  "ui.local_favorites": "מוזיקה מקומית",
  "ui.local_favorites_2": "מוזיקה ישראלית",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "מצב Local שימושי רק אם Local likes מופעל.",
  "ui.low_light_listening": "שקט לערב",
  "ui.low_volume_and_quiet_mix": "ווליום נמוך ומיקס רגוע",
  "ui.lower_volume_mood": "אווירת לילה רגועה",
  "ui.lyrics": "מילים",
  "ui.lyrics_earlier": "מילים מוקדם יותר",
  "ui.lyrics_font_size": "גודל פונט מילים",
  "ui.lyrics_later": "מילים מאוחר יותר",
  "ui.lyrics_timing": "תזמון מילים",
  "ui.lyrics_unavailable_right_now": "המילים לא זמינות כרגע",
  "ui.ma_favorite_action_failed_with_error": "????? ????? ?? ?????? ??????? ?????{error}",
  "ui.ma_interface_path": "נתיב ממשק MA",
  "ui.main_bar": "סרגל ראשי",
  "ui.main_bar_items": "פריטים בסרגל הראשי",
  "ui.main_bar_items_2": "פריטי סרגל ראשי",
  "ui.main_opacity": "שקיפות ראשית",
  "ui.mainly_relevant_on_larger_layouts": "רלוונטי במיוחד במסכים גדולים.",
  "ui.mass_queue_send_command_fallback": "Fallback mass_queue/send_command",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "זהה לבחירת המיקרופון במסך ההגדרות.",
  "ui.media": "מדיה",
  "ui.media_2": "Media",
  "ui.media_actions": "פעולות מדיה",
  "ui.media_layout": "תצוגת מדיה",
  "ui.media_selected_target": "????: {media} ? ???: {target}",
  "ui.menu": "תפריט",
  "ui.message": "הודעה",
  "ui.microphone": "שימוש במיקרופון",
  "ui.microphone_2": "מיקרופון",
  "ui.microphone_is_disabled": "המיקרופון מנותק",
  "ui.missing_url": "חסר URL",
  "ui.mobile": "מובייל",
  "ui.mon": "ב׳",
  "ui.mood_style_or_free_text": "מצב רוח, סגנון או חיפוש חופשי",
  "ui.more_info": "מידע נוסף",
  "ui.more_info_is_unavailable_for_this_player": "מידע נוסף לא זמין לנגן הזה",
  "ui.morning": "בוקר",
  "ui.morning_mix": "מיקס בוקר",
  "ui.move_to_next": "העבר לבא בתור",
  "ui.move_to_position": "העבר למיקום",
  "ui.music": "מוזיקה",
  "ui.music_assistant_required": "נדרשת אינטגרציית Music Assistant",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "מזהה ה־config entry של Music Assistant, אם רוצים קישור ישיר דרך Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "לא נמצא Config Entry של Music Assistant",
  "ui.music_assistant_token": "טוקן Music Assistant",
  "ui.music_assistant_url": "כתובת Music Assistant",
  "ui.music_hub": "מרכז מוזיקה",
  "ui.music_hub_2": "מרכז מוזיקה",
  "ui.mute": "השתק",
  "ui.mute_command_failed": "פקודת ההשתקה נכשלה",
  "ui.my_studio_scene": "סצנת סטודיו שלי",
  "ui.name_or_keyword": "שם או מילת חיפוש",
  "ui.name_this_studio_scene": "שם לסצנת הסטודיו...",
  "ui.netherlands": "הולנד",
  "ui.new_schedule": "תזמון חדש",
  "ui.new_wake_schedule": "תזמון השכמה חדש",
  "ui.newest": "הכי חדש",
  "ui.next": "הבא",
  "ui.next_track_failed": "מעבר לשיר הבא נכשל",
  "ui.night": "לילה",
  "ui.night_end_time": "שעת סיום ללילה",
  "ui.night_mode": "מצב לילה",
  "ui.night_mode_auto_window": "??? ???? ??????? {start}-{end}",
  "ui.night_mode_days": "ימי מצב לילה",
  "ui.night_mode_is_always_on": "מצב לילה פעיל תמיד",
  "ui.night_mode_is_off": "מצב לילה כבוי",
  "ui.night_mode_is_off_until_you_choose_another_mode": "מצב לילה כבוי עד שתבחר מצב אחר.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "מצב לילה יישאר פעיל עד שתבחר מצב אחר.",
  "ui.night_schedule_updated": "לוח הזמנים של מצב לילה עודכן",
  "ui.night_start_time": "שעת התחלה ללילה",
  "ui.no_active_media": "אין מדיה פעילה",
  "ui.no_active_players": "אין נגנים פעילים",
  "ui.no_available_players": "אין נגנים זמינים",
  "ui.no_content_found": "לא נמצא תוכן",
  "ui.no_dynamic_player_groups_to_disconnect": "אין קבוצות נגנים דינמיות לניתוק",
  "ui.no_extra_ma_players": "אין נגני MA נוספים",
  "ui.no_extra_ma_players_2": "אין נגנים נוספים",
  "ui.no_favorites_found": "לא נמצאו אהובים.",
  "ui.no_liked_media_yet": "עדיין לא נשמרו מועדפים",
  "ui.no_liked_tracks_to_play": "אין שירים מועדפים לנגן",
  "ui.no_light_entities_found": "לא נמצאו ישויות תאורה.",
  "ui.no_lyrics_found": "לא נמצאו מילים לשיר הזה",
  "ui.no_matching_content_was_found": "לא נמצא תוכן מתאים",
  "ui.no_media_found": "לא נמצאה מדיה.",
  "ui.no_media_found_for_this_search": "לא נמצאה מדיה לחיפוש הזה.",
  "ui.no_mix_content_found": "לא נמצא תוכן למיקס",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "נדרשת אינטגרציית Music Assistant. הפעל את Music Assistant ב-Home Assistant וודא שיש לפחות נגן Music Assistant אחד. נגני Home Assistant רגילים מוסתרים בכוונה.",
  "ui.no_music_assistant_players_were_found_for_pinning": "לא נמצאו נגני Music Assistant לקיבוע.",
  "ui.no_next_track_in_queue": "אין שיר הבא בתור",
  "ui.no_player_groups_to_disconnect": "אין קבוצות נגנים לניתוק",
  "ui.no_player_light_pairs_yet": "עדיין לא נוספו צמדי נגן-תאורה.",
  "ui.no_player_selected": "לא נבחר נגן",
  "ui.no_players_found": "לא נמצאו נגנים",
  "ui.no_players_found_yet": "עדיין לא נמצאו נגנים",
  "ui.no_playlists_found": "לא נמצאה מוזיקה אקראית",
  "ui.no_queue": "אין תור",
  "ui.no_queue_item_to_rebuild": "לא נמצא פריט פעיל לתור",
  "ui.no_queue_to_clone": "אין תור להעתקה",
  "ui.no_radio_browser_countries_found": "לא נמצאו מדינות Radio Browser",
  "ui.no_radio_stations_found": "לא נמצאו תחנות רדיו",
  "ui.no_recent_listening_yet": "אין האזנות אחרונות עדיין.",
  "ui.no_results": "אין תוצאות",
  "ui.no_results_found": "לא נמצאו תוצאות",
  "ui.no_saved_studio_scenes_yet": "עדיין אין סצנות סטודיו שמורות.",
  "ui.no_scheduled_start_is_active": "אין כרגע תזמון הפעלה פעיל",
  "ui.no_selected_tracks_to_play": "אין שירים נבחרים לנגן",
  "ui.no_sleep_timer_is_active": "אין כרגע טיימר שינה פעיל",
  "ui.no_speech_was_captured": "לא נקלט דיבור",
  "ui.no_target_players_available": "אין נגנים זמינים להעברה",
  "ui.no_target_players_available_2": "אין נגני יעד זמינים",
  "ui.no_tts_service_or_entity_is_configured": "לא הוגדר שירות או ישות TTS",
  "ui.no_wake_schedules_yet": "עדיין אין תזמוני השכמה",
  "ui.none": "אין",
  "ui.nothing_is_playing_right_now": "לא מתנגן כרגע",
  "ui.nothing_playing": "לא מתנגן",
  "ui.now_playing": "מתנגן עכשיו",
  "ui.now_playing_2": "מתנגן עכשיו",
  "ui.off": "כבוי",
  "ui.off_2": "מנותק",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "Off מכבה, On מפעיל תמיד, ו־Auto פועל לפי חלון השעות שהגדרת.",
  "ui.oldest": "הכי ישן",
  "ui.on": "פעיל",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "לחיצה אחת מכינה נגנים, קבוצה, ווליום ותוכן. אפשר לשמור את יעד הסטודיו הנוכחי כסצנה מקומית.",
  "ui.only_actions_for_the_current_target_are_shown_here": "כאן מופיעות רק פעולות ליעד השליטה הנוכחי.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "מוצגים כאן רק נגני Music Assistant. נגני דפדפן ו-Sendspin מקומי מוסתרים כדי למנוע בלבול.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "נדרש רק אם אתה עובד מול כתובת MA ישירה.",
  "ui.opacity_for_popups_and_overlays": "שקיפות חלונות וקופצים.",
  "ui.opacity_for_the_main_card_background": "שקיפות הרקע הראשי של הכרטיס.",
  "ui.open_full_interface": "פתיחת ממשק מלא",
  "ui.open_full_queue": "פתח תור מלא",
  "ui.open_interface_in": "פתיחת ממשק",
  "ui.open_ma": "פתח MA",
  "ui.open_music_assistant": "פתח Music Assistant",
  "ui.open_music_assistant_2": "מעבר ל-Music Assistant",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "פתח את Music Assistant במכשיר הזה כדי להפעיל את נגן הדפדפן",
  "ui.open_player_picker": "בחר נגן",
  "ui.open_queue": "פתח תור",
  "ui.open_saved_songs": "פתח שירים שמורים",
  "ui.open_the_full_music_assistant_interface": "האם לפתוח את הממשק המלא של Music Assistant?",
  "ui.open_the_full_music_assistant_interface_2": "פתח את ממשק Music Assistant המלא",
  "ui.opening_studio_library": "פותח את ספריית הסטודיו",
  "ui.opening_timers": "פותח טיימרים",
  "ui.option": "אפשרות",
  "ui.optional": "אופציונלי",
  "ui.optional_2": "לא חובה",
  "ui.optional_entity_used_for_an_external_favorite_button": "ישות אופציונלית לכפתור Like חיצוני.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "אופציונלי: input_text שהכרטיס יעדכן עם entity_id של הנגן הפעיל, עבור אוטומציות ותבניות.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "אופציונלי: בחר input_text שהכרטיס יעדכן עם entity_id של הנגן הפעיל עבור אוטומציות ותבניות.",
  "ui.other": "אחר",
  "ui.other_players": "נגנים נוספים",
  "ui.party": "מסיבה",
  "ui.path_used_when_opening_the_music_assistant_interface": "נתיב לפתיחת ממשק Music Assistant.",
  "ui.pause": "השהה",
  "ui.paused": "מושהה",
  "ui.per_device_in_card": "מהמכשיר / הממשק",
  "ui.performance_mode": "מצב ביצועים",
  "ui.performance_mode_for_weak_devices": "מצב ביצועים למכשירים חלשים",
  "ui.performance_profile": "פרופיל ביצועים",
  "ui.performance_profile_helper": "בחר כמה עבודה חזותית הכרטיס יבצע. Ultra Lite מפחית שקיפות, טשטוש, רקעי עטיפה דינמיים ותנועה עבור מכשירים חלשים.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "בחר מצב רוח או כתוב סגנון חופשי. הסטודיו יחפש ב־Music Assistant ויבנה תור.",
  "ui.pinned": "מקובע",
  "ui.pinned_players": "נגנים מקובעים",
  "ui.play": "נגן",
  "ui.play_2": "ניגון",
  "ui.play_a_few_tracks_and_they_will_appear_here": "נגן כמה שירים והם יופיעו כאן.",
  "ui.play_a_saved_list": "רשימה מוכנה",
  "ui.play_all": "נגן הכל",
  "ui.play_an_artist": "ניגון אמן",
  "ui.play_artwork_action": "פעולת עטיפה",
  "ui.play_everywhere": "ניגון בכל הבית",
  "ui.play_next": "נגן הבא",
  "ui.play_next_and_clear_queue": "נגן הבא ונקה תור",
  "ui.play_now": "נגן עכשיו",
  "ui.play_now_and_clear_queue": "נגן עכשיו נקה תור",
  "ui.play_pause": "נגן / השהה",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "נגן, הוסף, הפעל רדיו או נהל אהובים ישירות מהסטודיו.",
  "ui.play_selected": "נגן נבחרים",
  "ui.playback_command_failed": "Playback command failed",
  "ui.playback_command_failed_2": "פקודת הניגון נכשלה",
  "ui.playback_started": "הניגון התחיל",
  "ui.player": "נגן",
  "ui.player_2": "נגן",
  "ui.player_count_many": "{count} ?????",
  "ui.player_count_one": "??? ???",
  "ui.player_groups_could_not_be_disconnected": "קבוצות הנגנים לא נותקו",
  "ui.player_is_pinned_from_settings": "הנגן מקובע מתוך ההגדרות",
  "ui.player_is_ready": "הנגן מוכן",
  "ui.player_is_ready_nothing_is_playing_right_now": "הנגן זמין – לא מתנגן כרגע כלום",
  "ui.player_order": "סדר נגנים",
  "ui.player_on_this_device": "נגן במכשיר זה",
  "ui.player_selected": "הנגן נבחר",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "הגדרות נגן ו־DSP נשארים בשלב זה כזיהוי יכולות בלבד עד ש־Music Assistant מחזיר schema אמין לנגן.",
  "ui.player_sort": "מיון נגנים",
  "ui.players": "נגנים",
  "ui.players_queue_and_groups": "נגנים, תור וקבוצות",
  "ui.playing": "מנגן",
  "ui.playing_a_random_playlist": "מנגן פלייליסט אקראי",
  "ui.playing_a_random_radio_station": "מנגן תחנת רדיו אקראית",
  "ui.playing_on": "מתנגן על",
  "ui.playing_on_2": "מנגן ב",
  "ui.playlist": "פלייליסט",
  "ui.playlists": "פלייליסטים",
  "ui.podcast": "פודקאסט",
  "ui.podcasts": "פודקאסטים",
  "ui.popup_opacity": "שקיפות חלונות",
  "ui.preset_message": "משפט מוגדר מראש",
  "ui.previous": "הקודם",
  "ui.primary": "ראשי",
  "ui.primary_target": "יעד ראשי",
  "ui.pro_tools": "כלי Pro",
  "ui.queue": "תור ניגון",
  "ui.queue_2": "תור",
  "ui.queue_action_failed": "פעולה על התור נכשלה",
  "ui.queue_action_is_not_available_for_this_player": "פעולת התור לא זמינה לנגן הזה",
  "ui.queue_actions": "פעולות תור",
  "ui.queue_cleared": "התור נוקה",
  "ui.queue_cleared_and_item_moved_next": "התור נוקה והפריט הוגדר לבא בתור",
  "ui.queue_cloned": "התור הועתק",
  "ui.queue_cockpit": "ניהול תור",
  "ui.queue_cockpit_2": "ניהול תור",
  "ui.queue_is_empty": "התור ריק",
  "ui.queue_is_unavailable_for_this_player": "התור לא זמין לנגן הזה",
  "ui.queue_item": "פריט בתור",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "מזהה פריט התור עדיין לא מוכן. רענן את התור ונסה שוב.",
  "ui.queue_results": "תוצאות בעמודת התור",
  "ui.queue_transferred": "התור הועבר",
  "ui.queues_refreshed": "התורים רועננו",
  "ui.quick_actions": "פעולות מהירות",
  "ui.quick_actions_2": "פעולות מהירות",
  "ui.quick_mix": "מיקס מהיר",
  "ui.quick_mix_started": "המיקס המהיר הופעל",
  "ui.quick_play": "ניגון מהיר",
  "ui.radio": "רדיו",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "מדינות Radio Browser",
  "ui.radio_browser_country": "מדינת Radio Browser",
  "ui.radio_browser_country_2": "מדינת רדיו",
  "ui.radio_mode_is_not_available_for_this_media_type": "Radio לא זמין לסוג המדיה הזה",
  "ui.radio_mode_started": "Radio הופעל",
  "ui.radio_station_changed": "תחנת הרדיו הוחלפה",
  "ui.radio_stations": "תחנות רדיו",
  "ui.random": "אקראי",
  "ui.random_gentle_morning_mix": "מיקס בוקר נעים ואקראי",
  "ui.random_playlist": "פלייליסט אקראי",
  "ui.random_radio": "רדיו אקראי",
  "ui.ready": "מוכן",
  "ui.ready_to_play": "מוכן לניגון",
  "ui.recent": "אחרונים",
  "ui.recent_continue": "אחרונים / המשך",
  "ui.recent_listening": "האזנות אחרונות",
  "ui.recent_track": "שיר קודם",
  "ui.recently_added": "נוספו לאחרונה",
  "ui.recently_played": "נוגן לאחרונה",
  "ui.recently_played_2": "נוגן לאחרונה",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "המלצות יופיעו אחרי ש-HOMEii Flow יזהה את התור או ההאזנות האחרונות.",
  "ui.recommended": "המלצות",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "פורמט מומלץ: HH:MM כמו 06:00. אפשר לחצות חצות.",
  "ui.recommended_format_hh_mm_such_as_22_00": "פורמט מומלץ: HH:MM כמו 22:00.",
  "ui.recommended_playlist": "פלייליסט מומלץ",
  "ui.recommended_track": "המלצה",
  "ui.reconnects_when_you_return_to_this_dashboard": "יתחבר מחדש כשתחזור לדשבורד הזה",
  "ui.refresh": "רענן",
  "ui.remember_as_this_device": "שייך למכשיר הזה",
  "ui.remove": "הסר",
  "ui.remove_from_selection": "הסר מהבחירה",
  "ui.removed_from_liked": "הוסר מאהבתי",
  "ui.removed_from_music_assistant_liked": "הוסר מאהבתי של מיוזיק אסיסטנט",
  "ui.reorder_queue": "סידור תור",
  "ui.repeat": "חזרה",
  "ui.repeat_all": "חזרה על הכל",
  "ui.repeat_off": "חזרה כבויה",
  "ui.repeat_one": "חזרה על שיר אחד",
  "ui.reset": "איפוס",
  "ui.reset_lyrics_font_size": "איפוס גודל מילים",
  "ui.reset_lyrics_timing": "איפוס תזמון מילים",
  "ui.results": "תוצאות",
  "ui.rtl": "RTL",
  "ui.sat": "ש׳",
  "ui.save_current_target": "שמור יעד נוכחי",
  "ui.save_schedule": "שמור תזמון",
  "ui.scene_players_are_not_available": "הנגנים של הסצנה לא זמינים",
  "ui.scene_presets": "סצנות",
  "ui.scene_presets_2": "סצנות",
  "ui.scenes": "סצנות",
  "ui.scales_interface_icons_without_changing_button_sizes": "מגדיל או מקטין אייקונים בלי לשנות את גודל הכפתורים או המבנה.",
  "ui.scheduled_start": "תזמון הפעלה",
  "ui.scheduled_start_activated_label": "????? ?????? ?????: {label}",
  "ui.scheduled_start_cleared": "תזמון ההפעלה בוטל",
  "ui.scheduled_start_failed": "תזמון ההפעלה נכשל",
  "ui.scheduled_start_saved": "תזמון ההפעלה נשמר",
  "ui.scheduled_starts_active_count": "{count} ?????? ????? ??????",
  "ui.schedules": "תזמונים",
  "ui.search": "חיפוש",
  "ui.search_and_choose_play_next_add_radio_or_like": "חפש ובחר נגן, הבא, הוסף, רדיו או אהבתי.",
  "ui.search_everything": "חפש בכל מקום...",
  "ui.search_failed": "החיפוש נכשל",
  "ui.search_in_queue": "חיפוש בעמודת התור",
  "ui.search_queue_and_library": "חפש בתור ובספרייה...",
  "ui.search_the_library": "חפש בספריה...",
  "ui.search_tracks_albums_artists_and_playlists": "חיפוש שירים, אלבומים, אמנים ופלייליסטים",
  "ui.searching": "מחפש...",
  "ui.searching_library": "מחפש בספריה...",
  "ui.searching_smart_selection": "מחפש בחירה חכמה...",
  "ui.seek": "קפיצה",
  "ui.select": "בחר",
  "ui.select_lights": "בחירת תאורה או תאורות",
  "ui.select_player": "בחירת נגן",
  "ui.select_2": "בחירה",
  "ui.select_a_player_first": "בחר נגן קודם",
  "ui.select_at_least_one_studio_player": "בחר לפחות נגן אחד בסטודיו",
  "ui.select_at_least_two_players_to_create_a_group": "בחר לפחות שני נגנים כדי ליצור קבוצה",
  "ui.selected": "נבחר",
  "ui.selected_music": "מוזיקה נבחרת",
  "ui.selected_player": "נגן נבחר",
  "ui.selected_player_2": "נגן נבחר",
  "ui.selected_player_3": "הנגן הנבחר",
  "ui.selected_players_at_comfortable_volume": "נגנים נבחרים בווליום נוח",
  "ui.selected_playlist": "פלייליסט נבחר",
  "ui.send_a_short_voice_message_or_announcement_url": "שלח הודעה קולית קצרה או קישור כריזה.",
  "ui.send_a_voice_message": "שלח כריזה קולית",
  "ui.send_announcement": "שלח כריזה",
  "ui.sendspin_and_diagnostics": "Sendspin ודיאגנוסטיקה",
  "ui.set_sleep_timer": "הפעל טיימר שינה",
  "ui.set_volume": "קבע ווליום",
  "ui.set_the_visual_order_of_quick_actions": "קובע את הסדר הוויזואלי. רק פעולות מהירות שסומנו יוצגו; פריטים שלא סומנו יתעלמו.",
  "ui.settings": "הגדרות",
  "ui.settings_source": "מקור ההגדרות",
  "ui.show_ma_button": "כפתור MA",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "מציג או מסתיר את שורת השיר הבא במסך הניגון.",
  "ui.show_theme_toggle": "כפתור ערכת נושא",
  "ui.show_up_next": "הצג הבא בתור",
  "ui.show_up_next_2": "הצגת הבא בתור",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "מציג נגן אריח עצמאי וקומפקטי עם עטיפה, פקדי בסיס, ווליום וכפתור הרחבה.",
  "ui.shuffle": "ערבוב",
  "ui.shuffle_all": "ערבב הכל",
  "ui.shuffle_favorites": "ערבוב אהובים",
  "ui.shuffle_play": "נגן בערבוב",
  "ui.skipped_to_up_next": "עבר לשיר הבא בתור",
  "ui.sleep_timer": "טיימר שינה",
  "ui.sleep_timer_2": "טיימר שינה",
  "ui.sleep_timer_active_remaining": "????? ???? ????: {remaining}",
  "ui.sleep_timer_added_minutes": "????? {minutes} ???? ??????",
  "ui.sleep_timer_and_morning_playback": "טיימר שינה ותזמון הפעלה",
  "ui.sleep_timer_and_scheduled_playback": "טיימר שינה ותזמון הפעלה",
  "ui.sleep_timer_cleared": "טיימר השינה בוטל",
  "ui.sleep_timer_finished": "טיימר השינה הסתיים",
  "ui.sleep_timer_presets": "בחירת זמן טיימר שינה",
  "ui.sleep_timer_set_for_15_minutes": "טיימר שינה הופעל ל־15 דקות",
  "ui.sleep_timer_set_minutes": "????? ???? ????? ??{minutes} ????",
  "ui.sleep_timer_updated_minutes": "????? ???? ????? ??{minutes} ????",
  "ui.smaller_lyrics": "מילים קטנות יותר",
  "ui.smart": "חכם",
  "ui.smart_mix": "מיקס חכם",
  "ui.smart_mix_builder": "בונה מיקס חכם",
  "ui.smart_voice_selection": "בחירה קולית חכמה",
  "ui.soft_and_relaxed": "רך ונעים",
  "ui.soft_relaxed_music": "מוזיקה רכה ורגועה",
  "ui.some_players_could_not_be_stopped_or_cleared": "חלק מהנגנים לא נעצרו או לא נוקו",
  "ui.songs_around_an_artist": "מוזיקה סביב אמן",
  "ui.sort": "מיון",
  "ui.sort_ascending": "סדר עולה",
  "ui.sort_descending": "סדר יורד",
  "ui.source_queue": "תור מקור",
  "ui.spain": "ספרד",
  "ui.speak_to_target_players": "שלח הודעה לנגנים",
  "ui.start_radio_mode": "הפעל Radio",
  "ui.start_time": "שעת הפעלה",
  "ui.start_time_2": "שעת התחלה",
  "ui.started_on_selected_player": "הופעל על הנגן הנבחר",
  "ui.starting_a_chill_mix": "מפעיל מיקס רגוע",
  "ui.starting_quick_mix": "מפעיל מיקס מהיר...",
  "ui.stations": "תחנות",
  "ui.stay_active": "יישאר פעיל",
  "ui.stays_active": "נשאר פעיל",
  "ui.stop_all": "עצור הכל",
  "ui.stop_all_players": "עצור את כל הנגנים",
  "ui.stop_command_failed": "פקודת העצירה נכשלה",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "עצירה, ניקוי תורים וניתוק קבוצות",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "כל הנגנים נעצרו, התורים נוקו והקבוצות נותקו",
  "ui.stops_this_browser_player": "עוצר את נגן הדפדפן הזה",
  "ui.strong": "חזק",
  "ui.studio": "סטודיו",
  "ui.studio_closed": "סטודיו נסגר",
  "ui.studio_media_action_failed": "פעולת המדיה בסטודיו נכשלה",
  "ui.studio_mix_started": "מיקס הסטודיו הופעל",
  "ui.studio_opened": "סטודיו נפתח",
  "ui.studio_pro": "סטודיו Pro",
  "ui.studio_pro_2": "סטודיו Pro",
  "ui.studio_scene": "סצנת סטודיו",
  "ui.studio_scene_deleted": "סצנת סטודיו נמחקה",
  "ui.studio_scene_saved": "סצנת סטודיו נשמרה",
  "ui.studio_scene_was_not_found": "סצנת הסטודיו לא נמצאה",
  "ui.studio_search": "חיפוש סטודיו",
  "ui.studio_shortcut": "כפתור סטודיו",
  "ui.subtle": "עדין",
  "ui.sun": "א׳",
  "ui.surprise_me": "תפתיע אותי",
  "ui.sync": "סנכרון",
  "ui.sync_lyrics": "סנכרון מילים",
  "ui.tablet": "טאבלט",
  "ui.tap_to_start_a_sleep_timer": "לחץ כדי להפעיל טיימר שינה",
  "ui.target_players": "נגני יעד",
  "ui.target_queue": "תור יעד",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "כריזה מטקסט משתמשת ב־TTS של Home Assistant אל נגן Music Assistant שנבחר. כריזה מקישור משתמשת בניגון הכרזה של Music Assistant.",
  "ui.text_only": "מלל בלבד",
  "ui.text_tone": "טון טקסט",
  "ui.theme": "ערכת תצוגה",
  "ui.theme_2": "ערכת נושא",
  "ui.theme_mode": "ערכת נושא",
  "ui.theme_toggle": "מצב כהה/בהיר",
  "ui.theme_toggle_2": "שמש / ירח",
  "ui.this_device": "המכשיר הזה",
  "ui.this_device_2": "מכשיר זה",
  "ui.this_device_player_connected": "נגן המכשיר חובר",
  "ui.this_device_player_disconnected": "נגן המכשיר נותק",
  "ui.thu": "ה׳",
  "ui.timer": "טיימר",
  "ui.timer_active_remaining": "????? ????: {remaining}",
  "ui.timers": "טיימרים",
  "ui.to": "אל",
  "ui.today": "היום",
  "ui.track": "שיר",
  "ui.track_lyrics": "מילות השיר",
  "ui.tracks": "שירים",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "העבר, העתק, בדוק או נקה תורים בלי להסתיר את הסטודיו.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "העברת ניגון אינה זמינה כשמוגדר נגן מקובע",
  "ui.transfer_queue": "העבר תור",
  "ui.transfer_queue_2": "העבר תור",
  "ui.transfer_queue_3": "העברת התור",
  "ui.try_again": "נסה שוב",
  "ui.tts_entity": "ישות TTS",
  "ui.tts_entity_used_by_the_announcement_screen": "ישות TTS שתשמש למסך הכריזה.",
  "ui.tts_language": "שפת TTS",
  "ui.tue": "ג׳",
  "ui.turkey": "טורקיה",
  "ui.turn_off": "יכבה",
  "ui.turns_off_after_run": "נכבה אחרי הפעלה",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "מכבה אפקטים כבדים כדי שהכרטיס ירוץ חלק יותר על Nest Hub, טאבלטים ישנים ומסכים חלשים.",
  "ui.type_a_free_style_first": "כתוב קודם סגנון חופשי",
  "ui.type_an_announcement": "הקלד הודעת כריזה...",
  "ui.type_anything": "כתוב מה שבא לך",
  "ui.type_what_should_be_announced": "כתוב מה להכריז...",
  "ui.ungroup": "נתק קבוצה",
  "ui.ungroup_2": "נתק",
  "ui.united_kingdom": "בריטניה",
  "ui.united_states": "ארצות הברית",
  "ui.unknown": "לא ידוע",
  "ui.up": "למעלה",
  "ui.move_down": "הזז למטה",
  "ui.move_up": "הזז למעלה",
  "ui.up_next": "הבא בתור",
  "ui.up_next_2": "הבא בתור",
  "ui.up_next_title": "??? ????: {title}",
  "ui.upbeat_and_moving": "קצב ותנועה",
  "ui.use_any_home_assistant_path_example_lovelace_home": "אפשר להשתמש בכל נתיב ב-Home Assistant. לדוגמה: /lovelace/home",
  "ui.use_current_view_for_home_button": "השתמש במסך הנוכחי עבור כפתור הבית",
  "ui.use_expand_to_open_the_full_player": "השתמש בהרחבה כדי לפתוח את הנגן המלא",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "אותה רשימת מדינות בסיסית שמופיעה במסך ההגדרות.",
  "ui.visible_tiles": "אריחים מוצגים",
  "ui.visible_tiles_2": "אריחים מוצגים",
  "ui.visual_editor": "מהעורך הוויזואלי",
  "ui.voice_input_failed": "זיהוי קולי נכשל",
  "ui.voice_input_is_not_supported_on_this_device": "זיהוי קולי לא נתמך במכשיר הזה",
  "ui.voice_search": "חיפוש קולי",
  "ui.voice_search_failed": "החיפוש הקולי נכשל",
  "ui.voice_search_is_not_supported_on_this_device": "חיפוש קולי לא נתמך במכשיר הזה",
  "ui.volume": "ווליום",
  "ui.volume_and_player_target": "ווליום ויעד נגנים",
  "ui.volume_boost": "חיזוק ווליום",
  "ui.volume_control_large_screen_only": "בקרת ווליום (זמין רק במסך גדול)",
  "ui.volume_down": "הורדת ווליום",
  "ui.volume_mode": "בקרת ווליום",
  "ui.volume_presets": "בחירת ווליום",
  "ui.volume_step_between_1_and_10_percent_default_5": "קפיצת ווליום בין 1% ל-10%. ברירת המחדל היא 5%.",
  "ui.volume_step_buttons": "כפתורי ווליום +/-",
  "ui.volume_step_percent": "קפיצת ווליום",
  "ui.volume_up": "העלאת ווליום",
  "ui.waiting_for_this_device_player": "ממתין לנגן של המכשיר הזה...",
  "ui.wake": "השכמה",
  "ui.wake_schedules": "תזמוני השכמה",
  "ui.wed": "ד׳",
  "ui.week": "השבוע",
  "ui.what_would_you_like_to_listen_to": "לאיזה תוכן תרצו להאזין? ",
  "ui.will_play_next": "ינוגן הבא",
  "ui.will_play_next_in_studio": "ינוגן הבא בסטודיו",
  "ui.worldwide_popular": "פופולרי בעולם",
  "ui.add_player_light_pair": "הוספת צמד",
  "ui.ambient_light": "תאורה לפי עטיפה",
  "ui.ambient_light_brightness": "עוצמת תאורה",
  "ui.ambient_light_cooldown": "השהיית עדכון תאורה",
  "ui.ambient_light_entities": "תאורות צבע כברירת מחדל",
  "ui.ambient_light_player_map": "שיוך נגן -> תאורה",
  "ui.ambient_light_player_map_helper": "השתמש בסלקטורים של נגן ותאורות צבע למטה. כל שורה מלאה נשמרת כצמד; ניקוי הנגן או התאורות מסיר את הצמד.",
  "ui.ambient_light_transition": "מעבר תאורה",
  "ui.analog": "אנלוגי",
  "ui.choose_only_the_lights_that_should_follow_the_music": "בחר רק תאורות עם יכולת צבע שישמשו כברירת מחדל כשאין צמד ייעודי לנגן.",
  "ui.digital": "דיגיטלי",
  "ui.discover_albums": "גילוי אלבומים",
  "ui.discover_music": "גלה מוזיקה",
  "ui.discover_subtitle": "המלצות, אלבומים ורדיו לפי הסגנון שבחרת.",
  "ui.discovery": "גילוי מוזיקה",
  "ui.discovery_failed": "מצב ההמלצות נכשל",
  "ui.discovery_mode": "מצב המלצות",
  "ui.discovery_started": "התחלתי המלצה: {mood}",
  "ui.maximum_brightness_for_music_lighting": "עוצמת התאורה המקסימלית למוזיקה.",
  "ui.minimum_seconds_between_light_updates": "מספר שניות מינימלי בין עדכוני תאורה.",
  "ui.player_light_pair_lights": "תאורות צבע {number}",
  "ui.player_light_pair_lights_helper": "בחר תאורות עם יכולת צבע עבור נגן Music Assistant זה. ניקוי השדה או הנגן מסיר את הצמד.",
  "ui.player_light_pair_player": "נגן Music Assistant {number}",
  "ui.player_light_pair_player_helper": "בחר את נגן Music Assistant עבור צמד התאורה הזה.",
  "ui.all": "הכל",
  "ui.all_genres": "כל הז׳אנרים",
  "ui.genres_and_styles": "ז'אנרים וסגנונות",
  "ui.compact_widget_mode": "סגנון ווידג׳ט קומפקטי",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "בחר מתי מצב קומפקטי משתמש בווידג׳ט מובייל קטן בשתי שורות.",
  "ui.full": "רגיל",
  "ui.mini_widget": "ווידג׳ט קטן",
  "ui.genre": "ז׳אנר",
  "ui.mood_and_genre": "מצב רוח וז׳אנר",
  "ui.music_by_mood_and_genre": "מוזיקה לפי מצב רוח וז׳אנר",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "ישות אופציונלית ללחצן עזר. השאר ריק כדי לעצור את הנגן הפעיל.",
  "ui.optional_name_for_the_auxiliary_button": "שם אופציונלי שיוצג כטולטיפ ללחצן העזר.",
  "ui.choose_the_auxiliary_button_icon": "בחר את האייקון שיוצג ללחצן העזר.",
  "ui.choose_how_players_are_sorted": "בחר סדר ברירת מחדל, אלפביתי או סדר מותאם אישית.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "בחר נגני Music Assistant שלא יופיעו בכרטיס.",
  "ui.custom_order": "סדר מותאם",
  "ui.no_visible_music_assistant_players_after_exclusions": "כל נגני Music Assistant מוסתרים בהגדרת הנגנים המוחרגים.",
  "ui.set_custom_player_order": "קובע את סדר הנגנים המותאם. שדות ריקים יתעלמו.",
  "ui.show_auxiliary_button_in_quick_actions": "מציג את לחצן העזר הזה בשורת הפעולות המהירות.",
  "ui.power": "כיבוי",
  "ui.power_action_failed": "פעולת POWER נכשלה",
  "ui.power_action_sent": "פעולת POWER נשלחה",
  "ui.power_button": "לחצן עזר",
  "ui.power_button_action": "פעולת לחצן עזר",
  "ui.power_button_entity": "ישות לחצן עזר",
  "ui.scene": "סצנה",
  "ui.screensaver": "שומר מסך",
  "ui.screensaver_buttons": "כפתורי שומר מסך",
  "ui.screensaver_controls": "פקדי שומר מסך",
  "ui.screensaver_clock_mode": "סוג שעון בשומר מסך",
  "ui.screensaver_clock_size": "גודל שעון בשומר מסך",
  "ui.screensaver_clock_size_helper": "מגדיל או מקטין את השעון בשומר המסך בטאבלט בלי להזיז את העטיפה.",
  "ui.screensaver_clock_x": "מיקום שעון - LEFT / RIGHT",
  "ui.screensaver_clock_x_helper": "ערך נמוך מזיז שמאלה, ערך גבוה מזיז ימינה.",
  "ui.screensaver_clock_y": "מיקום שעון - UP / DOWN",
  "ui.screensaver_clock_y_helper": "ערך נמוך מזיז למעלה, ערך גבוה מזיז למטה.",
  "ui.screensaver_message": "הודעת שומר מסך",
  "ui.screensaver_timeout": "זמן עד שומר מסך",
  "ui.script": "סקריפט",
  "ui.short_optional_message_shown_on_the_screensaver": "הודעה קצרה ואופציונלית שתוצג בשומר המסך.",
  "ui.show_previous_next_controls_on_the_screensaver": "מציג כפתורי קודם והבא עדינים בשומר המסך.",
  "ui.choose_which_buttons_appear_on_the_screensaver": "בחירה מדויקת של הכפתורים שיופיעו בשומר המסך, כולל FLOW ASSISTANT, קודם והבא.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "מציג שעון רגוע ועטיפה אחרי זמן ללא שימוש.",
  "ui.show_power_button_in_the_player_controls": "מציג לחצן עזר בשורת הפעולות המהירות.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "מציג מצב המלצות במסך מלא תחת פעולות.",
  "ui.smart_home": "בית חכם",
  "ui.soft_transition_time_for_music_lighting": "זמן מעבר רך לתאורת המוזיקה.",
  "ui.stop_player": "עצור נגן",
  "ui.sync_selected_lights_to_the_current_artwork_color": "מסנכרן אורות נבחרים לצבעי העטיפה הנוכחית.",
  "ui.toggle": "החלפה",
  "ui.turn_on": "הפעל",
  "ui.choose_a_category_to_browse_provider_playlists": "בחר קטגוריה כדי לראות פלייליסטים מהספקים לפני ניגון.",
  "ui.new_and_recommended": "חדש ומומלץ",
  "ui.provider_playlists": "פלייליסטים מהספקים",
  "ui.assist_agent": "סוכן Assist",
  "ui.assist_agent_id": "מזהה סוכן Assist",
  "ui.assist_only": "Assist בלבד",
  "ui.default_assist_agent": "Assist ברירת מחדל של Home Assistant",
  "ui.default_order": "סדר ברירת מחדל",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "מומלץ: פקודות מוזיקה מתבצעות מקומית על הנגן הנבחר. כל דבר שהכרטיס לא מבין נשלח ל-Home Assistant Assist.",
  "ui.hybrid_music_plus_assist": "משולב: מוזיקה + Assist",
  "ui.icon_size": "גודל אייקונים",
  "ui.music_only": "מוזיקה בלבד",
  "ui.music_plus_assist": "מוזיקה + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "מזהה סוכן Assist אופציונלי. השאר ריק כדי להשתמש בסוכן ברירת המחדל של Home Assistant.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "אופציונלי. השאר על סוכן ברירת המחדל אלא אם יש לך כמה סוכני Assist ואתה רוצה שהכרטיס ישתמש בסוכן מסוים.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "מציג כפתור דיבור לפקודות מוזיקה ולגיבוי Assist.",
  "ui.show_home_inside_quick_actions": "מציג את הבית בתוך פעולות מהירות במקום כפתור צף.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "מציג כפתורי פלוס ומינוס ליד סליידר הווליום.",
  "ui.speak_voice_assistant_responses_out_loud": "מקריא את התגובה שזוהתה דרך הדפדפן הזה כאשר הדבר נתמך.",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "עוזר קולי",
  "ui.voice_assistant_disabled": "העוזר הקולי כבוי",
  "ui.voice_assistant_listening": "מקשיב לפקודה...",
  "ui.voice_assistant_mode": "מצב עוזר קולי",
  "ui.voice_command_executed": "הפקודה הקולית בוצעה",
  "ui.voice_command_completed_action": "בוצע: {action}",
  "ui.voice_command_failed": "הפקודה הקולית נכשלה",
  "ui.voice_command_no_player": "בחר נגן או אמור את שם הנגן.",
  "ui.voice_command_not_understood": "לא הצלחתי להבין את הפקודה.",
  "ui.voice_command_sent": "הפקודה הקולית נשלחה",
  "ui.voice_done_status": "בוצע",
  "ui.voice_error_status": "צריך בדיקה",
  "ui.voice_feedback": "פידבק קולי",
  "ui.voice_group_connect_needs_players": "אמור לפחות שני שמות נגנים, או בחר נגן ואמור את שם הנגן השני.",
  "ui.voice_group_connected_players": "{primary} חובר עם {members}",
  "ui.voice_group_disconnect_needs_player": "אמור איזו קבוצת נגן לנתק.",
  "ui.voice_group_disconnected_player": "הקבוצה של {player} נותקה",
  "ui.voice_music_searching": "מחפש מוזיקה: {query}",
  "ui.voice_language_is_not_supported": "הדפדפן לא תומך בשפת הזיהוי הקולי שנבחרה.",
  "ui.voice_match_not_confident": 'מצאתי את {title}, אבל זה לא מספיק תואם ל־"{query}". נסה לומר שוב את שם השיר והאמן.',
  "ui.voice_playing_result": "מנגן את {title}",
  "ui.voice_queue_transfer_needs_players": "אמור מקור ויעד, למשל: העבר את התור מהסלון למטבח.",
  "ui.voice_queue_transferred_between": "התור הועבר מ־{source} אל {target}",
  "ui.voice_starting_playback": "מפעיל את {title}...",
  "ui.voice_processing_status": "מעבד פקודה...",
  "ui.voice_ready_status": "מוכן",
  "ui.voice_recognition_network_failed": "שירות זיהוי הדיבור לא זמין. ייתכן שהרשאת המיקרופון תקינה; בדוק דפדפן, VPN, DNS, חומת אש או רשת.",
  "ui.voice_response": "תגובה",
  "ui.voice_response_will_appear_here": "התגובה תופיע כאן.",
  "ui.voice_transcript": "תמלול",
  "ui.voice_listening_status": "מקשיב...",
  "ui.waiting_for_speech": "ממתין לדיבור...",
  "ui.microphone_permission_or_browser_blocked": "הדפדפן חסם את זיהוי הדיבור. בדוק הרשאות מיקרופון והגדרות זיהוי בדפדפן.",
  "ui.hotel_mode": "מצב מלון",
  "ui.hotel_mode_helper": "ממשק מצב מלון מינימלי: פקדי נגן, ווליום, חיפוש, דפדוף בעטיפות ובחירת נגן בלבד.",
  "ui.style_and_genres": "סגנונות וז'אנרים"
});
const messages$2 = Object.freeze({
  "ui.a_guided_music_wizard": "Un assistente musicale guidato",
  "ui.a_radio_station": "Una stazione radio",
  "ui.accent_color": "Colore d'accento",
  "ui.actions": "azioni",
  "ui.actions_2": "Azioni",
  "ui.active_days": "Giorni attivi",
  "ui.active_player_helper": "Aiutante riproduttore attivo",
  "ui.active_player_helper_must_be_an_input_text_entity": "L'aitante del riproduttore attivo deve essere un'entità input_text",
  "ui.active_players": "Riproduttori attivi",
  "ui.add": "Aggiungi",
  "ui.add_to_queue": "Aggiungi alla coda",
  "ui.add_to_selection": "Aggiungi alla selezione",
  "ui.added_to_liked": "Aggiunto ai preferiti",
  "ui.added_to_music_assistant_liked": "Aggiunto ai preferiti di Music Assistant",
  "ui.added_to_queue": "Aggiunto alla coda",
  "ui.added_to_studio_queue": "Aggiunto alla coda di Studio",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Aggiunge movimento ai livelli di sfondo. 'Tenue' lo mantiene calmo, 'Forte' è evidente ed 'Estremo' offre l'aspetto animato più audace.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Aumenta il volume attuale solo durante gli annunci, ripristinando poi il volume precedente.",
  "ui.after_run": "Dopo l'esecuzione",
  "ui.album": "Album",
  "ui.albums": "Album",
  "ui.all_albums": "Tutti gli album",
  "ui.all_artists": "Tutti gli artisti",
  "ui.all_countries": "Tutti i paesi",
  "ui.all_player_groups_disconnected": "Tutti i gruppi di riproduttori disconnessi",
  "ui.all_players": "Tutti i riproduttori",
  "ui.all_players_2": "tutti i riproduttori",
  "ui.all_podcasts": "Tutti i podcast",
  "ui.all_tracks": "Tutti i brani",
  "ui.allow_local_likes": "Consenti preferiti locali",
  "ui.always_visible": "Sempre visibile",
  "ui.announce": "Annuncia",
  "ui.announce_to_all_players": "Annuncia a tutti i riproduttori",
  "ui.announcement": "Annuncio",
  "ui.announcement_failed_with_error": "Annuncio fallito{error}",
  "ui.announcement_language": "Lingua dell'annuncio",
  "ui.announcement_presets": "Preimpostazioni annuncio",
  "ui.announcement_sent_to_player": "Annuncio inviato a {player}",
  "ui.announcement_studio": "studio annunci",
  "ui.announcement_studio_2": "Studio Annunci",
  "ui.announcement_target": "Target dell'annuncio",
  "ui.announcement_to_player_preview": "Annuncio a {player}: {preview}",
  "ui.announcement_volume_boost": "Incremento volume annuncio",
  "ui.announcements": "Annunci",
  "ui.appearance": "Aspetto",
  "ui.alphabetical": "Alfabetico",
  "ui.auxiliary_button": "Pulsante ausiliario",
  "ui.auxiliary_button_action": "Azione pulsante ausiliario",
  "ui.auxiliary_button_entity": "Entità pulsante ausiliario",
  "ui.auxiliary_button_icon": "Icona pulsante ausiliario",
  "ui.auxiliary_button_name": "Nome pulsante ausiliario",
  "ui.auxiliary_buttons": "Pulsanti ausiliari",
  "ui.apply_group": "Applica gruppo",
  "ui.apply_schedule": "Applica programmazione",
  "ui.artist": "Artista",
  "ui.artist_radio": "Radio artista",
  "ui.artists": "Artisti",
  "ui.artwork": "Copertina",
  "ui.artwork_swipe": "Scorrimento copertina",
  "ui.ascending": "Crescente",
  "ui.at_least_one_player_must_stay_selected": "Almeno un riproduttore deve rimanere selezionato",
  "ui.australia": "Australia",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Sceglie automaticamente tra smartphone o tablet in base alla larghezza effettiva.",
  "ui.auto_cloud_default": "Automatico / Predefinito Cloud",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Estrae automaticamente i colori dalla copertina corrente mantenendo l'effetto tenue. 'Forte' rende la tavolozza più ricca e luminosa.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "Automatico lascia invariati i valori predefiniti della voce di Home Assistant Cloud. Le scelte manuali inviano una lingua esplicita.",
  "ui.available": "Disponibile",
  "ui.back": "Indietro",
  "ui.back_2": "Indietro",
  "ui.back_to_countries": "Torna ai paesi",
  "ui.back_to_queue": "Torna alla coda",
  "ui.background_motion": "Movimento di sfondo",
  "ui.behavior": "Comportamento",
  "ui.browse_covers": "Sfoglia copertine",
  "ui.browse_playlists_artists_albums_and_radio": "Sfoglia playlist, artisti, album e radio",
  "ui.browser_players": "Riproduttori del browser",
  "ui.build_custom_mix": "Crea mix personalizzato",
  "ui.building_studio_mix": "Creazione del mix di Studio...",
  "ui.button": "Pulsante",
  "ui.by_mood": "Per stato d'animo",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Durata della cache in millisecondi per le richieste di dati selezionate.",
  "ui.cache_ttl": "TTL della cache",
  "ui.calm": "Calmo",
  "ui.canada": "Canada",
  "ui.cancel": "Annulla",
  "ui.cancel_2": "Annulla",
  "ui.cancel_selection": "Annulla selezione",
  "ui.card_configuration": "Configurazione della scheda",
  "ui.card_height": "Altezza della scheda",
  "ui.card_height_in_pixels": "Altezza della scheda in pixel.",
  "ui.card_settings_are_managed_from_the_visual_editor": "Le impostazioni della scheda sono gestite dall'editor visuale",
  "ui.change_song": "Cambia canzone",
  "ui.change_track": "Cambia brano",
  "ui.chill_mix": "Mix relax",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Scegli un paese, o 'Tutti i paesi' per sfogliare ogni stato all'interno della scheda Radio.",
  "ui.choose_a_queue_position": "Scegli una posizione in coda",
  "ui.choose_a_source_player": "Scegli un riproduttore sorgente",
  "ui.choose_a_target_player": "Scegli un riproduttore di destinazione",
  "ui.choose_at_least_one_player": "Scegli almeno un riproduttore",
  "ui.choose_icons_text_or_both_for_the_footer": "Scegli icone, testo o entrambi per il piè di pagina.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Scegli solo riproduttori di Music Assistant. L'elenco di fissaggio nella scheda mostra solo i riproduttori di Music Assistant per evitare confusione.",
  "ui.choose_one_clear_option": "Scegli un'opzione di svuotamento",
  "ui.choose_player": "Scegli riproduttore",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Scegli qualcosa dallo scaffale rapido o tocca la bacchetta per una playlist casuale.",
  "ui.choose_source": "Scegli sorgente",
  "ui.choose_target": "Scegli destinazione",
  "ui.choose_target_player": "Scegli riproduttore di destinazione",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Scegli il colore d'accento per il layout mobile.",
  "ui.choose_the_music": "Scegli la musica",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Scegli e ordina i piccoli pulsanti mostrati sotto il titolo della canzone.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Scegli prima la sorgente. La riproduzione utilizzerà la destinazione corrente.",
  "ui.choose_where_to_play": "Scegli dove riprodurre",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Scegli se lo scorrimento della copertina cambia brano o sfoglia le copertine.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Scegli se le impostazioni sono controllate dall'interfaccia utente nella scheda o dalla configurazione della scheda nella dashboard.",
  "ui.choose_which_actions_appear_in_the_main_bar": "Scegli quali azioni appaiono nella barra principale.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Scegli a quali giorni si applica la finestra della modalità notturna automatica.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Scegli quali icone appaiono nella riga delle azioni rapide. 'Svuota tutto' chiede conferma prima di disconnettere i riproduttori attivi.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Scegli quali riproduttori appaiono come riquadri nella stanza.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Scegli quali riproduttori rimangono nella selezione di controllo corrente.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Scegli quali schede sono disponibili nella schermata della libreria.",
  "ui.choose_who_is_controlled": "Scegli chi controllare",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "La scelta di 'Visuale' disabilita la schermata delle impostazioni all'interno della scheda.",
  "ui.clean_all": "Svuota tutto",
  "ui.clean_all_confirm_action": "Svuota tutto",
  "ui.clean_all_confirm_copy": "Questo disconnetterà tutti i riproduttori attivi, interromperà la riproduzione e svuoterà le loro code. Continuare?",
  "ui.clean_all_confirm_title": "Svuotare tutti i riproduttori?",
  "ui.clean_the_studio_wall": "Pulisci la parete dello Studio",
  "ui.clear": "Cancella",
  "ui.clear_queue": "Svuota coda",
  "ui.clear_search": "Cancella ricerca",
  "ui.clone_queue": "Clona coda",
  "ui.close": "Chiudi",
  "ui.collapse_compact_player": "Riduci riproduttore compatto",
  "ui.compact_mode": "Modalità compatta",
  "ui.configure_ready_made_announcement_phrases": "Configura frasi di annuncio pronte all'uso.",
  "ui.connect_this_device": "Connetti questo dispositivo",
  "ui.connect_this_device_2": "Connetti questo dispositivo",
  "ui.connected": "Connesso",
  "ui.connected_2": "Connesso",
  "ui.connected_players": "riproduttori connessi",
  "ui.connected_players_2": "Riproduttori connessi",
  "ui.connecting": "Connessione in corso",
  "ui.connecting_2": "Connessione in corso...",
  "ui.confirm": "Conferma",
  "ui.connection_and_behavior": "Connessione e comportamento",
  "ui.connects_directly_from_this_card_using_sendspin": "Si connette direttamente da questa scheda usando Sendspin",
  "ui.continue": "Continua",
  "ui.continue_2": "Continua",
  "ui.continue_from_recent_music_assistant_activity": "Continua dalle recenti attività di Music Assistant.",
  "ui.continue_listening": "Continua ad ascoltare",
  "ui.continue_what_was_played_recently": "Continua quello che è stato riprodotto di recente",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Controlla se lo sfondo della scheda si muove delicatamente e l'intensità del movimento.",
  "ui.controlling": "Controllo di",
  "ui.could_not_build_quick_mix_from_the_current_song": "Impossibile creare un Mix Rapido dalla canzone corrente",
  "ui.could_not_build_studio_mix": "Impossibile creare il mix di Studio",
  "ui.could_not_choose_media": "Impossibile scegliere i contenuti multimediali",
  "ui.could_not_clone_the_queue": "Impossibile clonare la coda",
  "ui.could_not_find_music": "Impossibile trovare musica",
  "ui.could_not_play_label": "Impossibile riprodurre: {label}",
  "ui.could_not_start_chill_mix": "Impossibile avviare il mix relax",
  "ui.could_not_start_playback": "Impossibile avviare la riproduzione",
  "ui.could_not_start_playback_2": "Impossibile avviare la riproduzione",
  "ui.could_not_start_playback_in_studio": "Impossibile avviare la riproduzione in Studio",
  "ui.could_not_start_quick_mix": "Impossibile avviare il Mix Rapido",
  "ui.could_not_start_radio_playback": "Impossibile avviare la riproduzione radio",
  "ui.could_not_transfer_the_queue": "Impossibile trasferire la coda",
  "ui.could_not_update_active_player_helper": "Impossibile aggiornare l'aiutante del riproduttore attivo",
  "ui.create_schedule": "Crea programmazione",
  "ui.current_path": "Percorso corrente",
  "ui.custom": "Personalizzato",
  "ui.dark": "Scuro",
  "ui.delete_scene": "Elimina scena",
  "ui.delete_schedule": "Elimina programmazione",
  "ui.descending": "Decrescente",
  "ui.dictate": "Detta",
  "ui.disabled": "Disabilitato",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Disabilita sfocatura, animazioni, sfondi dinamici e ombre intense. Consigliato per Nest Hub e tablet meno potenti.",
  "ui.disconnect": "Disconnetti",
  "ui.disconnect_all": "Disconnetti tutto",
  "ui.disconnect_groups": "Disconnetti gruppi",
  "ui.disconnect_player_groups": "Disconnetti gruppi di riproduttori",
  "ui.disconnect_this_device": "Disconnetti questo dispositivo",
  "ui.disconnected": "Disconnesso",
  "ui.disconnecting": "Disconnessione in corso",
  "ui.disconnecting_player_groups": "Disconnessione dei gruppi di riproduttori...",
  "ui.discover": "Scopri",
  "ui.down": "Giù",
  "ui.dynamic_theme": "Tema dinamico",
  "ui.edit_schedule": "Modifica programmazione",
  "ui.enable": "Abilita",
  "ui.enable_or_disable_the_footer_search_button": "Abilita o disabilita il pulsante di ricerca nel piè di pagina.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Abilita o disabilita il pulsante Studio nella barra del piè di pagina.",
  "ui.enable_the_floating_home_shortcut_button": "Abilita la scorciatoia Home nelle Azioni Rapide.",
  "ui.enabled": "Abilitato",
  "ui.enables_switching_to_local_liked_mode": "Abilita il passaggio alla modalità dei preferiti locali.",
  "ui.end_time": "Ora di fine",
  "ui.excluded_players": "Riproduttori esclusi",
  "ui.energetic": "Energetico",
  "ui.energy_and_rhythm": "Energia e ritmo",
  "ui.enter_an_announcement_first": "Inserisci prima un annuncio",
  "ui.example_quiet_jazz_greek_music_workout": "Esempio: jazz calmo, musica greca, allenamento",
  "ui.existing_content": "Contenuto esistente",
  "ui.expand_player": "Espandi riproduttore",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Estrai i colori dalla copertina corrente e applicali all'interfaccia.",
  "ui.extreme": "Estremo",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Fallback per configurazioni che necessitano di un percorso alternativo per il servizio di coda.",
  "ui.fallback_mode": "Modalità di fallback",
  "ui.family_friendly": "Adatto alle famiglie",
  "ui.favorite": "Preferito",
  "ui.favorite_action_failed": "Azione preferito fallita",
  "ui.favorite_action_sent": "Azione preferito inviata",
  "ui.favorite_button_entity": "Entità pulsante preferito",
  "ui.favorite_center": "centro preferiti",
  "ui.favorite_center_2": "Centro Preferiti",
  "ui.favorite_radio": "Radio preferita",
  "ui.favorite_updated": "Preferito aggiornato",
  "ui.favorites_and_liked_music": "Preferiti e musica gradita",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Rilevamento delle funzionalità, contesto del riproduttore e strumenti di riproduzione per questo dispositivo.",
  "ui.find_music": "Trova musica",
  "ui.finding_music": "Ricerca musica in corso",
  "ui.fixed": "Fisso",
  "ui.flow_mix": "FLOW Mix",
  "ui.font_scale": "Scala carattere",
  "ui.font_size": "Dimensione carattere",
  "ui.footer_search": "Ricerca nel piè di pagina",
  "ui.footer_style": "Stile piè di pagina",
  "ui.footer_style_2": "Stile piè di pagina",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "Per esempio /lovelace/home o qualsiasi altro percorso di Home Assistant.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "Per schermi meno potenti: disattiva sfocatura intensa, animazioni, estrazione del tema dinamico e movimento dello sfondo.",
  "ui.france": "Francia",
  "ui.free_style": "Stile libero",
  "ui.free_style_quiet_jazz_greek_music_workout": "Stile libero: jazz calmo, musica greca, allenamento...",
  "ui.fresh_start": "Nuovo inizio",
  "ui.fresh_start_2": "Nuovo inizio",
  "ui.fri": "Ven",
  "ui.friendly_and_simple": "Intuitivo e semplice",
  "ui.from": "Da",
  "ui.general": "Generale",
  "ui.general_and_display": "Generale e Display",
  "ui.germany": "Germania",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Scala globale per tutti i caratteri dell'interfaccia. 1 è la dimensione predefinita.",
  "ui.greece": "Grecia",
  "ui.grid": "Griglia",
  "ui.group": "Gruppo",
  "ui.group_cleared": "Gruppo svuotato",
  "ui.group_cleared_2": "Gruppo svuotato",
  "ui.group_speakers": "Raggruppa altoparlanti",
  "ui.group_speakers_2": "Raggruppa altoparlanti",
  "ui.group_updated": "Gruppo aggiornato",
  "ui.group_volume": "Volume di gruppo",
  "ui.group_volume_up_energetic_mix": "Gruppo, alza il volume, mix energetico",
  "ui.grouped_players": "Riproduttori raggruppati",
  "ui.hebrew": "Ebraico",
  "ui.history": "Cronologia",
  "ui.history_tabs": "Schede cronologia",
  "ui.home": "Home",
  "ui.home_party_night_presets": "Preimpostazioni casa, festa, notte",
  "ui.home_path": "Percorso Home",
  "ui.home_scene_prepared": "Scena Home preparata",
  "ui.home_shortcut": "Scorciatoia Home",
  "ui.home_shortcut_path": "Percorso scorciatoia Home",
  "ui.icon_and_text": "Icona e testo",
  "ui.icon_only": "Solo icona",
  "ui.icon_plus_text": "Icona + testo",
  "ui.idle": "Inattivo",
  "ui.idle_2": "Inattivo",
  "ui.in_card_ui": "UI nella scheda",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Include la modalità del tema personalizzato dalla schermata delle impostazioni della scheda.",
  "ui.info": "Info",
  "ui.israel": "Israele",
  "ui.israeli": "Israeliano",
  "ui.italy": "Italia",
  "ui.items": "elementi",
  "ui.join_selected_players": "Unisci i riproduttori selezionati",
  "ui.kids": "Bambini",
  "ui.language": "Lingua",
  "ui.larger_lyrics": "Testo più grande",
  "ui.layout_adapted_to_height": "Layout adattato all'altezza",
  "ui.layout_mode": "Modalità layout",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Lascia vuoto se la scheda deve utilizzare solo Home Assistant.",
  "ui.library": "Libreria",
  "ui.library_2": "Libreria",
  "ui.library_pages": "Pagine della libreria",
  "ui.default_library_layout": "Layout predefinito della libreria",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Scegli come aprire le pagine della libreria. Puoi comunque passare da Griglia a Elenco all'interno della libreria.",
  "ui.library_radio": "Radio della libreria",
  "ui.library_surprise": "Sorpresa della libreria",
  "ui.library_tabs": "Schede Libreria",
  "ui.library_tabs_2": "Schede libreria",
  "ui.light": "Chiaro",
  "ui.like": "Metti mi piace",
  "ui.like_2": "Metti mi piace",
  "ui.like_3": "Metti mi piace",
  "ui.liked": "Piaciuti",
  "ui.liked_mode": "Modalità preferiti",
  "ui.liked_sync": "Sincronizzazione preferiti",
  "ui.list": "Elenco",
  "ui.listening": "Ascolto in corso...",
  "ui.loading": "Caricamento...",
  "ui.loading_favorites": "Caricamento preferiti...",
  "ui.loading_library": "Caricamento libreria...",
  "ui.loading_lyrics": "Caricamento testo...",
  "ui.loading_players": "Caricamento riproduttori...",
  "ui.loading_queue": "Caricamento coda...",
  "ui.loading_recent_items": "Caricamento elementi recenti...",
  "ui.loading_schedules": "Caricamento programmazioni...",
  "ui.local": "Locale",
  "ui.local_favorites": "Preferiti locali",
  "ui.local_favorites_2": "Preferiti locali",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "La modalità locale ha senso solo quando i preferiti locali sono abilitati.",
  "ui.low_light_listening": "Ascolto a bassa luminosità",
  "ui.low_volume_and_quiet_mix": "Volume basso e mix tranquillo",
  "ui.lower_volume_mood": "Stato d'animo a volume ridotto",
  "ui.lyrics": "Testo",
  "ui.lyrics_earlier": "Anticipa testo",
  "ui.lyrics_font_size": "Dimensione carattere del testo",
  "ui.lyrics_later": "Ritarda testo",
  "ui.lyrics_timing": "Sincronizzazione testo",
  "ui.lyrics_unavailable_right_now": "Testo non disponibile in questo momento",
  "ui.ma_favorite_action_failed_with_error": "Azione preferito di Music Assistant fallita{error}",
  "ui.ma_interface_path": "Percorso interfaccia MA",
  "ui.main_bar": "Barra principale",
  "ui.main_bar_items": "Elementi barra principale",
  "ui.main_bar_items_2": "Elementi barra principale",
  "ui.main_opacity": "Opacità principale",
  "ui.mainly_relevant_on_larger_layouts": "Rilevante principalmente su layout più grandi.",
  "ui.mass_queue_send_command_fallback": "Fallback mass_queue/send_command",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Corrisponde all'impostazione del microfono dalla schermata delle impostazioni della scheda.",
  "ui.media": "Media",
  "ui.media_2": "Media",
  "ui.media_actions": "Azioni multimediali",
  "ui.media_layout": "Layout multimediale",
  "ui.media_selected_target": "Selezionato: {media} ? Destinazione: {target}",
  "ui.menu": "Menu",
  "ui.message": "Messaggio",
  "ui.microphone": "Microfono",
  "ui.microphone_2": "Microfono",
  "ui.microphone_is_disabled": "Il microfono è disabilitato",
  "ui.missing_url": "URL mancante",
  "ui.mobile": "Mobile",
  "ui.mon": "Lun",
  "ui.mood_style_or_free_text": "Stato d'animo, stile o testo libero",
  "ui.more_info": "Maggiori informazioni",
  "ui.more_info_is_unavailable_for_this_player": "Maggiori informazioni non disponibili per questo riproduttore",
  "ui.morning": "Mattina",
  "ui.morning_mix": "Mix mattutino",
  "ui.move_to_next": "Sposta al successivo",
  "ui.move_to_position": "Sposta alla posizione",
  "ui.music": "Musica",
  "ui.music_assistant_required": "Music Assistant richiesto",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "ID voce di configurazione di Music Assistant, se desideri la ricerca dell'integrazione diretta tramite Home Assistant.",
  "ui.music_assistant_config_entry_was_not_found": "La voce di configurazione di Music Assistant non è stata trovata",
  "ui.music_assistant_token": "Token di Music Assistant",
  "ui.music_assistant_url": "URL di Music Assistant",
  "ui.music_hub": "hub musicale",
  "ui.music_hub_2": "Hub Musicale",
  "ui.mute": "Silenzia",
  "ui.mute_command_failed": "Comando di silenziamento fallito",
  "ui.my_studio_scene": "La mia scena di Studio",
  "ui.name_or_keyword": "Nome o parola chiave",
  "ui.name_this_studio_scene": "Dai un nome a questa scena di Studio...",
  "ui.netherlands": "Paesi Bassi",
  "ui.new_schedule": "Nuova programmazione",
  "ui.new_wake_schedule": "Nuova programmazione risveglio",
  "ui.newest": "Più recenti",
  "ui.next": "Successivo",
  "ui.next_track_failed": "Brano successivo fallito",
  "ui.night": "Notte",
  "ui.night_end_time": "Ora di fine notte",
  "ui.night_mode": "Modalità notturna",
  "ui.night_mode_auto_window": "Modalità notturna auto {start}-{end}",
  "ui.night_mode_days": "Giorni modalità notturna",
  "ui.night_mode_is_always_on": "La modalità notturna è sempre attiva",
  "ui.night_mode_is_off": "La modalità notturna è disattivata",
  "ui.night_mode_is_off_until_you_choose_another_mode": "La modalità notturna è disattivata finché non si sceglie un'altra modalità.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "La modalità notturna rimane attiva finché non si sceglie un'altra modalità.",
  "ui.night_schedule_updated": "Programmazione notturna aggiornata",
  "ui.night_start_time": "Ora di inizio notte",
  "ui.no_active_media": "Nessun contenuto multimediale attivo",
  "ui.no_active_players": "Nessun riproduttore attivo",
  "ui.no_available_players": "Nessun riproduttore disponibile",
  "ui.no_content_found": "Nessun contenuto trovato",
  "ui.no_dynamic_player_groups_to_disconnect": "Nessun gruppo di riproduttori dinamici da disconnettere",
  "ui.no_extra_ma_players": "Nessun riproduttore MA aggiuntivo",
  "ui.no_extra_ma_players_2": "Nessun riproduttore MA aggiuntivo",
  "ui.no_favorites_found": "Nessun preferito trovato.",
  "ui.no_liked_media_yet": "Ancore nessun contenuto tra i preferiti",
  "ui.no_liked_tracks_to_play": "Nessun brano preferito da riprodurre",
  "ui.no_light_entities_found": "Nessuna entità di illuminazione trovata.",
  "ui.no_lyrics_found": "Testo non trovato",
  "ui.no_matching_content_was_found": "Nessun contenuto corrispondente trovato",
  "ui.no_media_found": "Nessun contenuto multimediale trovato.",
  "ui.no_media_found_for_this_search": "Nessun contenuto multimediale trovato per questa ricerca.",
  "ui.no_mix_content_found": "Nessun contenuto mix trovato",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "Music Assistant è richiesto. Abilita l'integrazione di Music Assistant in Home Assistant ed esponi almeno un riproduttore multimediale Music Assistant. Gli altri riproduttori di Home Assistant sono intenzionalmente nascosti.",
  "ui.no_music_assistant_players_were_found_for_pinning": "Nessun riproduttore di Music Assistant trovato da fissare.",
  "ui.no_next_track_in_queue": "Nessun brano successivo in coda",
  "ui.no_player_groups_to_disconnect": "Nessun gruppo di riproduttori da disconnettere",
  "ui.no_player_light_pairs_yet": "Nessuna associazione riproduttore-luce definita.",
  "ui.no_player_selected": "Nessun riproduttore selezionato",
  "ui.no_players_found": "Nessun riproduttore trovato",
  "ui.no_players_found_yet": "Nessun riproduttore ancora trovato",
  "ui.no_playlists_found": "Nessuna playlist trovata",
  "ui.no_queue": "Nessuna coda",
  "ui.no_queue_item_to_rebuild": "Nessun elemento in coda da ricostruire",
  "ui.no_queue_to_clone": "Nessuna coda da clonare",
  "ui.no_radio_browser_countries_found": "Nessun paese trovato in Radio Browser",
  "ui.no_radio_stations_found": "Nessuna stazione radio trovata",
  "ui.no_recent_listening_yet": "Nessun ascolto recente.",
  "ui.no_results": "Nessun risultato",
  "ui.no_results_found": "Nessun risultato trovato",
  "ui.no_saved_studio_scenes_yet": "Nessuna scena di Studio salvata.",
  "ui.no_scheduled_start_is_active": "Nessun avvio programmato attivo",
  "ui.no_selected_tracks_to_play": "Nessun brano selezionato da riprodurre",
  "ui.no_sleep_timer_is_active": "Nessun timer di spegnimento attivo",
  "ui.no_speech_was_captured": "Nessun discorso è stato acquisito",
  "ui.no_target_players_available": "Nessun riproduttore di destinazione disponibile",
  "ui.no_target_players_available_2": "Nessun riproduttore di destinazione disponibile",
  "ui.no_tts_service_or_entity_is_configured": "Nessun servizio o entità TTS configurata",
  "ui.no_wake_schedules_yet": "Nessuna programmazione risveglio presente",
  "ui.none": "Nessuno",
  "ui.nothing_is_playing_right_now": "Nulla è in riproduzione in questo momento",
  "ui.nothing_playing": "Nulla in riproduzione",
  "ui.now_playing": "In riproduzione",
  "ui.now_playing_2": "In riproduzione",
  "ui.off": "Spento",
  "ui.off_2": "Spento",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "'Spento' lo disabilita, 'Acceso' lo mantiene attivo e 'Auto' segue la finestra temporale configurata.",
  "ui.oldest": "Più datati",
  "ui.on": "Acceso",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "Un solo tocco prepara i riproduttori, il raggruppamento, il volume e il contenuto. Salva la tua destinazione Studio corrente come scena locale.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Qui vengono mostrate solo le azioni per la destinazione corrente.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Qui vengono mostrati solo i riproduttori di Music Assistant. I riproduttori del browser e quelli locali di Sendspin sono nascosti per evitare confusione.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Necessario solo quando si utilizza un URL diretto di Music Assistant.",
  "ui.opacity_for_popups_and_overlays": "Opacità per finestre a comparsa e sovrapposizioni.",
  "ui.opacity_for_the_main_card_background": "Opacità per lo sfondo della scheda principale.",
  "ui.open_full_interface": "Apri l'interfaccia completa",
  "ui.open_full_queue": "Apri la coda completa",
  "ui.open_interface_in": "Apri l'interfaccia in",
  "ui.open_ma": "Apri MA",
  "ui.open_music_assistant": "Apri Music Assistant",
  "ui.open_music_assistant_2": "Apri Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Apri Music Assistant su questo dispositivo per attivare il riproduttore del browser",
  "ui.open_player_picker": "Apri selettore riproduttori",
  "ui.open_queue": "Apri coda",
  "ui.open_saved_songs": "Apri canzoni salvate",
  "ui.open_the_full_music_assistant_interface": "Apri l'interfaccia completa di Music Assistant?",
  "ui.open_the_full_music_assistant_interface_2": "Apri l'interfaccia completa di Music Assistant",
  "ui.opening_studio_library": "Apertura libreria di Studio",
  "ui.opening_timers": "Apertura timer",
  "ui.option": "Opzione",
  "ui.optional": "Opzionale",
  "ui.optional_2": "Opzionale",
  "ui.optional_entity_used_for_an_external_favorite_button": "Entità opzionale utilizzata per un pulsante dei preferiti esterno.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Aiutante input_text opzionale aggiornato con l'entity_id del riproduttore attivo per automazioni e modelli.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Aiutante input_text opzionale aggiornato con l'entity_id del riproduttore attivo per automazioni e modelli.",
  "ui.other": "Altro",
  "ui.other_players": "Altri riproduttori",
  "ui.party": "Festa",
  "ui.path_used_when_opening_the_music_assistant_interface": "Percorso utilizzato per l'apertura dell'interfaccia di Music Assistant.",
  "ui.pause": "Pausa",
  "ui.paused": "In pausa",
  "ui.per_device_in_card": "Per dispositivo / nella scheda",
  "ui.performance_mode": "Modalità prestazioni",
  "ui.performance_mode_for_weak_devices": "Modalità prestazioni per dispositivi deboli",
  "ui.performance_profile": "Profilo prestazioni",
  "ui.performance_profile_helper": "Scegli quanto carico grafico consentire alla scheda. 'Ultra leggero' riduce trasparenze, sfocature, copertine dinamiche e movimenti per i dispositivi meno potenti.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Scegli uno stato d'animo o digita il tuo stile. Studio cercherà su Music Assistant e creerà una coda.",
  "ui.pinned": "Fissati",
  "ui.pinned_players": "Riproduttori fissati",
  "ui.play": "Riproduci",
  "ui.play_2": "Riproduci",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Riproduci qualche brano e compariranno qui.",
  "ui.play_a_saved_list": "Riproduci un elenco salvato",
  "ui.play_all": "Riproduci tutto",
  "ui.play_an_artist": "Riproduci un artista",
  "ui.play_artwork_action": "Azione di riproduzione copertina",
  "ui.play_everywhere": "Riproduci ovunque",
  "ui.play_next": "Riproduci successivo",
  "ui.play_next_and_clear_queue": "Riproduci successivo e svuota coda",
  "ui.play_now": "Riproduci ora",
  "ui.play_now_and_clear_queue": "Riproduci ora e svuota coda",
  "ui.play_pause": "Riproduci / Pausa",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Riproduci, metti in coda, avvia la radio o rimuovi i preferiti direttamente da Studio.",
  "ui.play_selected": "Riproduci selezionati",
  "ui.playback_command_failed": "Comando di riproduzione fallito",
  "ui.playback_command_failed_2": "Comando di riproduzione fallito",
  "ui.playback_started": "Riproduzione avviata",
  "ui.player": "riproduttore",
  "ui.player_2": "Riproduttore",
  "ui.player_count_many": "{count} riproduttori",
  "ui.player_count_one": "1 riproduttore",
  "ui.player_groups_could_not_be_disconnected": "Impossibile disconnettere i gruppi di riproduttori",
  "ui.player_is_pinned_from_settings": "Il riproduttore è fissato dalle impostazioni",
  "ui.player_is_ready": "Il riproduttore è pronto",
  "ui.player_is_ready_nothing_is_playing_right_now": "Il riproduttore è pronto - nulla è in riproduzione al momento",
  "ui.player_order": "Ordine riproduttori",
  "ui.player_on_this_device": "Riproduttore su questo dispositivo",
  "ui.player_selected": "Riproduttore selezionato",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "Le impostazioni del riproduttore e i preset DSP rimangono in sola lettura finché Music Assistant non espone uno schema affidabile per questo riproduttore.",
  "ui.player_sort": "Ordinamento riproduttori",
  "ui.players": "Riproduttori",
  "ui.players_queue_and_groups": "Riproduttori, coda e gruppi",
  "ui.playing": "In riproduzione",
  "ui.playing_a_random_playlist": "Riproduzione di una playlist casuale",
  "ui.playing_a_random_radio_station": "Riproduzione di una stazione radio casuale",
  "ui.playing_on": "In riproduzione su",
  "ui.playing_on_2": "In riproduzione su",
  "ui.playlist": "Playlist",
  "ui.playlists": "Playlist",
  "ui.podcast": "Podcast",
  "ui.podcasts": "Podcast",
  "ui.popup_opacity": "Opacità popup",
  "ui.preset_message": "Messaggio preimpostato",
  "ui.previous": "Precedente",
  "ui.primary": "Primario",
  "ui.primary_target": "Destinazione primaria",
  "ui.pro_tools": "Strumenti Pro",
  "ui.queue": "Coda",
  "ui.queue_2": "Coda",
  "ui.queue_action_failed": "Azione sulla coda fallita",
  "ui.queue_action_is_not_available_for_this_player": "L'azione sulla coda non è disponibile per questo riproduttore",
  "ui.queue_actions": "Azioni sulla coda",
  "ui.queue_cleared": "Coda svuotata",
  "ui.queue_cleared_and_item_moved_next": "Coda svuotata ed elemento spostato al successivo",
  "ui.queue_cloned": "Coda clonata",
  "ui.queue_cockpit": "cabina di pilotaggio coda",
  "ui.queue_cockpit_2": "Pannello della Coda",
  "ui.queue_is_empty": "La coda è vuota",
  "ui.queue_is_unavailable_for_this_player": "La coda non è disponibile per questo riproduttore",
  "ui.queue_item": "Elemento in coda",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "L'ID dell'elemento in coda non è ancora pronto. Aggiorna la coda e riprova.",
  "ui.queue_results": "Risultati della coda",
  "ui.queue_transferred": "Coda trasferita",
  "ui.queues_refreshed": "Code aggiornate",
  "ui.quick_actions": "Azioni rapide",
  "ui.quick_actions_2": "Azioni Rapide",
  "ui.quick_mix": "Mix rapido",
  "ui.quick_mix_started": "Mix rapido avviato",
  "ui.quick_play": "Riproduzione rapida",
  "ui.radio": "Radio",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Paesi di Radio Browser",
  "ui.radio_browser_country": "Paese di Radio Browser",
  "ui.radio_browser_country_2": "Paese di Radio Browser",
  "ui.radio_mode_is_not_available_for_this_media_type": "La modalità radio non è disponibile per questo tipo di contenuto multimediale",
  "ui.radio_mode_started": "Modalità radio avviata",
  "ui.radio_station_changed": "Stazione radio cambiata",
  "ui.radio_stations": "Stazioni Radio",
  "ui.random": "Casuale",
  "ui.random_gentle_morning_mix": "Mix mattutino delicato casuale",
  "ui.random_playlist": "Playlist casuale",
  "ui.random_radio": "Radio casuale",
  "ui.ready": "Pronto",
  "ui.ready_to_play": "Pronto a riprodurre",
  "ui.recent": "Recenti",
  "ui.recent_continue": "Recenti / Continua",
  "ui.recent_listening": "ascolti recenti",
  "ui.recent_track": "Brano recente",
  "ui.recently_added": "Aggiunti di recente",
  "ui.recently_played": "Riprodotti di recente",
  "ui.recently_played_2": "Riprodotti di recente",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "I consigli appariranno non appena HOMEii Flow rileverà la tua coda o gli ascolti recenti.",
  "ui.recommended": "Consigliati",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Formato consigliato: HH:MM ad esempio 06:00. Il passaggio oltre la mezzanotte è supportato.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Formato consigliato: HH:MM ad esempio 22:00.",
  "ui.recommended_playlist": "Playlist consigliata",
  "ui.recommended_track": "Brano consigliato",
  "ui.reconnects_when_you_return_to_this_dashboard": "Si riconnette quando ritorni in questa dashboard",
  "ui.refresh": "Aggiorna",
  "ui.remember_as_this_device": "Ricorda come questo dispositivo",
  "ui.remove": "Rimuovi",
  "ui.remove_from_selection": "Rimuovi dalla selezione",
  "ui.removed_from_liked": "Rimosso dai preferiti",
  "ui.removed_from_music_assistant_liked": "Rimosso dai preferiti di Music Assistant",
  "ui.reorder_queue": "Riordina coda",
  "ui.repeat": "Ripeti",
  "ui.repeat_all": "Ripeti tutti",
  "ui.repeat_off": "Ripetizione disattivata",
  "ui.repeat_one": "Ripeti uno",
  "ui.reset": "Ripristina",
  "ui.reset_lyrics_font_size": "Ripristina dimensione carattere del testo",
  "ui.reset_lyrics_timing": "Ripristina sincronizzazione del testo",
  "ui.results": "Risultati",
  "ui.rtl": "RTL",
  "ui.sat": "Sab",
  "ui.save_current_target": "Salva destinazione corrente",
  "ui.save_schedule": "Salva programmazione",
  "ui.scene_players_are_not_available": "I riproduttori della scena non sono disponibili",
  "ui.scene_presets": "preimpostazioni della scena",
  "ui.scene_presets_2": "Preimpostazioni della Scena",
  "ui.scenes": "Scene",
  "ui.scales_interface_icons_without_changing_button_sizes": "Scala le icone dell'interfaccia senza modificare la dimensione dei pulsanti o il layout.",
  "ui.scheduled_start": "Avvio programmato",
  "ui.scheduled_start_activated_label": "Avvio programmato attivato: {label}",
  "ui.scheduled_start_cleared": "Avvio programmato annullato",
  "ui.scheduled_start_failed": "Avvio programmato fallito",
  "ui.scheduled_start_saved": "Avvio programmato salvato",
  "ui.scheduled_starts_active_count": "{count} avvii programmati attivi",
  "ui.schedules": "Programmazioni",
  "ui.search": "Cerca",
  "ui.search_and_choose_play_next_add_radio_or_like": "Cerca e scegli Riproduci, Successivo, Aggiungi, Radio o Mi Piace.",
  "ui.search_everything": "Cerca ovunque...",
  "ui.search_failed": "Ricerca fallita",
  "ui.search_in_queue": "Cerca nella coda",
  "ui.search_queue_and_library": "Cerca nella coda e nella libreria...",
  "ui.search_the_library": "Cerca nella libreria...",
  "ui.search_tracks_albums_artists_and_playlists": "Cerca brani, album, artisti e playlist",
  "ui.searching": "Ricerca in corso...",
  "ui.searching_library": "Ricerca nella libreria...",
  "ui.searching_smart_selection": "Ricerca selezione intelligente...",
  "ui.seek": "Posiziona",
  "ui.select": "Seleziona",
  "ui.select_lights": "Seleziona una o più luci",
  "ui.select_player": "Seleziona riproduttore",
  "ui.select_2": "Seleziona",
  "ui.select_a_player_first": "Seleziona prima un riproduttore",
  "ui.select_at_least_one_studio_player": "Seleziona almeno un riproduttore di Studio",
  "ui.select_at_least_two_players_to_create_a_group": "Seleziona almeno due riproduttori per creare un gruppo",
  "ui.selected": "Selezionato",
  "ui.selected_music": "Musica selezionata",
  "ui.selected_player": "Riproduttore Selezionato",
  "ui.selected_player_2": "Riproduttore selezionato",
  "ui.selected_player_3": "Riproduttore selezionato",
  "ui.selected_players_at_comfortable_volume": "Riproduttori selezionati a volume confortevole",
  "ui.selected_playlist": "Playlist selezionata",
  "ui.send_a_short_voice_message_or_announcement_url": "Invia un breve messaggio vocale o un URL di annuncio.",
  "ui.send_a_voice_message": "Invia un messaggio vocale",
  "ui.send_announcement": "Invia annuncio",
  "ui.sendspin_and_diagnostics": "Sendspin e diagnostica",
  "ui.set_sleep_timer": "Imposta timer di spegnimento",
  "ui.set_volume": "Imposta volume",
  "ui.set_the_visual_order_of_quick_actions": "Imposta l'ordine visivo. Verranno mostrate solo le Azioni Rapide selezionate; gli elementi non selezionati saranno ignorati.",
  "ui.settings": "Impostazioni",
  "ui.settings_source": "Sorgente impostazioni",
  "ui.show_ma_button": "Mostra pulsante MA",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Mostra o nascondi la riga integrata del brano successivo in 'In Riproduzione'.",
  "ui.show_theme_toggle": "Mostra interruttore tema",
  "ui.show_up_next": "Mostra successivo",
  "ui.show_up_next_2": "Mostra successivo",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Mostra un riquadro indipendente del riproduttore compatto con copertina, controlli di base, volume e un'azione di espansione.",
  "ui.shuffle": "Riproduzione casuale",
  "ui.shuffle_all": "Riproduzione casuale tutto",
  "ui.shuffle_favorites": "Riproduzione casuale preferiti",
  "ui.shuffle_play": "Riproduci in modo casuale",
  "ui.skipped_to_up_next": "Passato al brano successivo",
  "ui.sleep_timer": "Timer di spegnimento",
  "ui.sleep_timer_2": "Timer di Spegnimento",
  "ui.sleep_timer_active_remaining": "Timer di spegnimento attivo: {remaining}",
  "ui.sleep_timer_added_minutes": "Aggiunti {minutes} minuti al timer",
  "ui.sleep_timer_and_morning_playback": "Timer di spegnimento e riproduzione mattutina",
  "ui.sleep_timer_and_scheduled_playback": "Timer di spegnimento e riproduzione programmata",
  "ui.sleep_timer_cleared": "Timer di spegnimento annullato",
  "ui.sleep_timer_finished": "Timer di spegnimento terminato",
  "ui.sleep_timer_presets": "Preimpostazioni timer di spegnimento",
  "ui.sleep_timer_set_for_15_minutes": "Timer di spegnimento impostato su 15 minuti",
  "ui.sleep_timer_set_minutes": "Timer di spegnimento impostato su {minutes} minuti",
  "ui.sleep_timer_updated_minutes": "Timer di spegnimento aggiornato a {minutes} minuti",
  "ui.smaller_lyrics": "Testo più piccolo",
  "ui.smart": "Intelligente",
  "ui.smart_mix": "mix intelligente",
  "ui.smart_mix_builder": "Creatore Mix Intelligente",
  "ui.smart_voice_selection": "Selezione vocale intelligente",
  "ui.soft_and_relaxed": "Morbida e rilassante",
  "ui.soft_relaxed_music": "Musica morbida e rilassante",
  "ui.some_players_could_not_be_stopped_or_cleared": "Impossibile arrestare o svuotare alcuni riproduttori",
  "ui.songs_around_an_artist": "Brani correlati a un artista",
  "ui.sort": "Ordina",
  "ui.sort_ascending": "Ordina in modo crescente",
  "ui.sort_descending": "Ordina in modo decrescente",
  "ui.source_queue": "Coda sorgente",
  "ui.spain": "Spagna",
  "ui.speak_to_target_players": "Parla ai riproduttori di destinazione",
  "ui.start_radio_mode": "Avvia modalità radio",
  "ui.start_time": "Ora di inizio",
  "ui.start_time_2": "Ora di inizio",
  "ui.started_on_selected_player": "Avviato sul riproduttore selezionato",
  "ui.starting_a_chill_mix": "Avvio di un mix relax",
  "ui.starting_quick_mix": "Avvio Mix Rapido...",
  "ui.stations": "stazioni",
  "ui.stay_active": "Rimani attivo",
  "ui.stays_active": "Rimane attivo",
  "ui.stop_all": "Ferma tutto",
  "ui.stop_all_players": "Ferma tutti i riproduttori",
  "ui.stop_command_failed": "Comando di arresto fallito",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Interrompi la riproduzione, svuota le code e disconnetti i gruppi",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Arrestati tutti i riproduttori, svuotate le playlist e disconnessi i gruppi",
  "ui.stops_this_browser_player": "Arresta questo riproduttore del browser",
  "ui.strong": "Forte",
  "ui.studio": "Studio",
  "ui.studio_closed": "Studio chiuso",
  "ui.studio_media_action_failed": "Azione multimediale Studio fallita",
  "ui.studio_mix_started": "Mix di Studio avviato",
  "ui.studio_opened": "Studio aperto",
  "ui.studio_pro": "studio pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Scena di Studio",
  "ui.studio_scene_deleted": "Scena di Studio eliminata",
  "ui.studio_scene_saved": "Scena di Studio salvata",
  "ui.studio_scene_was_not_found": "La scena di Studio non è stata trovata",
  "ui.studio_search": "ricerca studio",
  "ui.studio_shortcut": "Scorciatoia Studio",
  "ui.subtle": "Tenue",
  "ui.sun": "Dom",
  "ui.surprise_me": "Sorprendimi",
  "ui.sync": "Sincronizza",
  "ui.sync_lyrics": "Sincronizza testo",
  "ui.tablet": "Tablet",
  "ui.tap_to_start_a_sleep_timer": "Tocca per avviare un timer di spegnimento",
  "ui.target_players": "Riproduttori di destinazione",
  "ui.target_queue": "Coda di destinazione",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Gli annunci di testo utilizzano il TTS di Home Assistant sul riproduttore Music Assistant selezionato. Gli annunci via URL utilizzano la riproduzione degli annunci di Music Assistant.",
  "ui.text_only": "Solo testo",
  "ui.text_tone": "Tono del testo",
  "ui.theme": "Tema",
  "ui.theme_2": "Tema",
  "ui.theme_mode": "Modalità tema",
  "ui.theme_toggle": "Interruttore tema",
  "ui.theme_toggle_2": "Interruttore tema",
  "ui.this_device": "Questo Dispositivo",
  "ui.this_device_2": "Questo dispositivo",
  "ui.this_device_player_connected": "Riproduttore di questo dispositivo connesso",
  "ui.this_device_player_disconnected": "Riproduttore di questo dispositivo disconnesso",
  "ui.thu": "Gio",
  "ui.timer": "Timer",
  "ui.timer_active_remaining": "Timer attivo: {remaining}",
  "ui.timers": "Timer",
  "ui.to": "A",
  "ui.today": "Oggi",
  "ui.track": "Brano",
  "ui.track_lyrics": "Testo del brano",
  "ui.tracks": "Brani",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Trasferisci, clona, ispeziona o svuota le code senza nascondere lo Studio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "Il trasferimento non è disponibile quando è configurato un riproduttore fissato",
  "ui.transfer_queue": "Trasferisci Coda",
  "ui.transfer_queue_2": "Trasferisci coda",
  "ui.transfer_queue_3": "Trasferisci coda",
  "ui.try_again": "Riprova",
  "ui.tts_entity": "Entità TTS",
  "ui.tts_entity_used_by_the_announcement_screen": "Entità TTS utilizzata dalla schermata degli annunci.",
  "ui.tts_language": "Lingua TTS",
  "ui.tue": "Mar",
  "ui.turkey": "Turchia",
  "ui.turn_off": "Spegni",
  "ui.turns_off_after_run": "Si spegne dopo l'esecuzione",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Disattiva gli elementi grafici complessi per rendere la scheda più fluida su Nest Hub, tablet datati e schermi meno potenti.",
  "ui.type_a_free_style_first": "Digita prima uno stile libero",
  "ui.type_an_announcement": "Digita un annuncio...",
  "ui.type_anything": "Digita qualsiasi cosa",
  "ui.type_what_should_be_announced": "Digita cosa annunciare...",
  "ui.ungroup": "Separa",
  "ui.ungroup_2": "Separa",
  "ui.united_kingdom": "Regno Unito",
  "ui.united_states": "Stati Uniti",
  "ui.unknown": "Sconosciuto",
  "ui.up": "Su",
  "ui.move_down": "Sposta giù",
  "ui.move_up": "Sposta su",
  "ui.up_next": "Prossimo brano",
  "ui.up_next_2": "Prossimo brano",
  "ui.up_next_title": "Prossimo brano: {title}",
  "ui.upbeat_and_moving": "Incalzante e movimentata",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Usa qualsiasi percorso di Home Assistant. Esempio: /lovelace/home",
  "ui.use_current_view_for_home_button": "Usa la vista corrente per il pulsante Home",
  "ui.use_expand_to_open_the_full_player": "Usa espandi per aprire il riproduttore completo",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Utilizza lo stesso elenco di paesi base mostrato nella schermata delle impostazioni della scheda.",
  "ui.visible_tiles": "riquadri visibili",
  "ui.visible_tiles_2": "Riquadri visibili",
  "ui.visual_editor": "Editor visuale",
  "ui.assist_agent": "Agente Assist",
  "ui.assist_agent_id": "ID agente Assist",
  "ui.assist_only": "Solo Assist",
  "ui.default_assist_agent": "Assistente predefinito di Home Assistant",
  "ui.default_order": "Ordine predefinito",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Consigliato: i comandi musicali vengono eseguiti localmente sul riproduttore selezionato. Tutto ciò che la scheda non comprende viene inviato ad Assist di Home Assistant.",
  "ui.hybrid_music_plus_assist": "Ibrido: Musica + Assist",
  "ui.icon_size": "Dimensione icona",
  "ui.music_only": "Solo musica",
  "ui.music_plus_assist": "Musica + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "ID agente Assist opzionale. Lascia vuoto per utilizzare l'agente predefinito di Home Assistant.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Opzionale. Lascia l'agente predefinito a meno che tu non disponga di più agenti Assist e desideri che questa scheda ne utilizzi uno specifico.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Mostra un pulsante push-to-talk per i comandi musicali e il fallback di Assist.",
  "ui.show_home_inside_quick_actions": "Mostra Home come Azione Rapida invece di un pulsante fluttuante.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Mostra i pulsanti più e meno accanto al cursore del volume.",
  "ui.speak_voice_assistant_responses_out_loud": "Pronuncia la risposta riconosciuta ad alta voce tramite questo browser quando supportato.",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "Assistente vocale",
  "ui.voice_assistant_disabled": "L'assistente vocale è disabilitato",
  "ui.voice_assistant_listening": "In ascolto di un comando...",
  "ui.voice_assistant_mode": "Modalità assistente vocale",
  "ui.voice_command_executed": "Comando vocale eseguito",
  "ui.voice_command_completed_action": "Fatto: {action}",
  "ui.voice_command_failed": "Comando vocale fallito",
  "ui.voice_command_no_player": "Seleziona un riproduttore o pronuncia il nome del riproduttore.",
  "ui.voice_command_not_understood": "Non ho capito questo comando.",
  "ui.voice_command_sent": "Comando vocale inviato",
  "ui.voice_done_status": "Fatto",
  "ui.voice_error_status": "Richiede attenzione",
  "ui.voice_feedback": "Feedback vocale",
  "ui.voice_group_connect_needs_players": "Pronuncia almeno due nomi di riproduttori, oppure selezionane uno e pronuncia l'altro.",
  "ui.voice_group_connected_players": "Connesso {primary} con {members}",
  "ui.voice_group_disconnect_needs_player": "Pronuncia quale gruppo di riproduttori disconnettere.",
  "ui.voice_group_disconnected_player": "Disconnesso il gruppo per {player}",
  "ui.voice_input_failed": "Input vocale fallito",
  "ui.voice_input_is_not_supported_on_this_device": "L'input vocale non è supportato su questo dispositivo",
  "ui.voice_language_is_not_supported": "Questo browser non supporta la lingua di riconoscimento vocale selezionata.",
  "ui.voice_match_not_confident": `Ho trovato {title}, ma non corrisponde abbastanza a "{query}". Prova a pronunciare nuovamente il brano e l'artista.`,
  "ui.voice_music_searching": "Ricerca musica: {query}",
  "ui.voice_playing_result": "Riproduzione di {title}",
  "ui.voice_queue_transfer_needs_players": "Pronuncia il riproduttore di origine e di destinazione, ad esempio: trasferisci la coda dal soggiorno alla cucina.",
  "ui.voice_queue_transferred_between": "Coda trasferita da {source} a {target}",
  "ui.voice_starting_playback": "Avvio di {title}...",
  "ui.voice_processing_status": "Elaborazione del comando...",
  "ui.voice_ready_status": "Pronto",
  "ui.voice_recognition_network_failed": "Il servizio di riconoscimento vocale non è disponibile. I permessi del microfono potrebbero essere corretti; verifica il browser, VPN, DNS, firewall o la rete.",
  "ui.voice_response": "Risposta",
  "ui.voice_response_will_appear_here": "La risposta apparirà qui.",
  "ui.voice_search": "Ricerca vocale",
  "ui.voice_search_failed": "Ricerca vocale fallita",
  "ui.voice_search_is_not_supported_on_this_device": "La ricerca vocale non è supportata su questo dispositivo",
  "ui.voice_transcript": "Trascrizione",
  "ui.voice_listening_status": "In ascolto...",
  "ui.waiting_for_speech": "In attesa del parlato...",
  "ui.microphone_permission_or_browser_blocked": "Il browser ha bloccato il riconoscimento vocale. Controlla le impostazioni del microfono e della voce del browser.",
  "ui.volume": "Volume",
  "ui.volume_and_player_target": "Volume e riproduttore di destinazione",
  "ui.volume_boost": "Incremento volume",
  "ui.volume_control_large_screen_only": "Controllo volume (solo schermi grandi)",
  "ui.volume_down": "Abbassa volume",
  "ui.volume_mode": "Modalità volume",
  "ui.volume_presets": "Preimpostazioni volume",
  "ui.volume_step_between_1_and_10_percent_default_5": "Passo del volume tra 1% e 10%. Il valore predefinito è 5%.",
  "ui.volume_step_buttons": "Pulsanti volume +/-",
  "ui.volume_step_percent": "Passo del volume",
  "ui.volume_up": "Alza volume",
  "ui.waiting_for_this_device_player": "In attesa del riproduttore di questo dispositivo...",
  "ui.wake": "Risveglio",
  "ui.wake_schedules": "Programmazioni risveglio",
  "ui.wed": "Mer",
  "ui.week": "Settimana",
  "ui.what_would_you_like_to_listen_to": "Cosa ti piacerebbe ascoltare?",
  "ui.will_play_next": "Sarà riprodotto successivamente",
  "ui.will_play_next_in_studio": "Sarà riprodotto successivamente in Studio",
  "ui.worldwide_popular": "Popolari in tutto il mondo",
  "ui.add_player_light_pair": "Aggiungi associazione",
  "ui.ambient_light": "Luce d'ambiente",
  "ui.ambient_light_brightness": "Luminosità della luce",
  "ui.ambient_light_cooldown": "Tempo di ricarica aggiornamento luce",
  "ui.ambient_light_entities": "Luci colorate predefinite",
  "ui.ambient_light_player_map": "Mappatura Riproduttore -> luce",
  "ui.ambient_light_player_map_helper": "Usa i selettori di riproduttori e luci colorate riportati di seguito. Ogni riga completata diventa un'associazione; cancella un riproduttore o le sue luci per rimuovere l'associazione.",
  "ui.ambient_light_transition": "Transizione della luce",
  "ui.analog": "Analogico",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Scegli solo le luci in grado di gestire il colore che devono seguire la musica quando non esiste un'associazione specifica per il riproduttore.",
  "ui.digital": "Digitale",
  "ui.discover_albums": "Scopri album",
  "ui.discover_music": "Scopri",
  "ui.discover_subtitle": "Playlist, album e radio consigliati.",
  "ui.discovery": "Scoperta",
  "ui.discovery_failed": "Modalità scoperta fallita",
  "ui.discovery_mode": "Modalità scoperta",
  "ui.discovery_started": "Scoperta avviata: {mood}",
  "ui.maximum_brightness_for_music_lighting": "Luminosità massima per l'illuminazione musicale.",
  "ui.minimum_seconds_between_light_updates": "Secondi minimi tra gli aggiornamenti della luce.",
  "ui.player_light_pair_lights": "Luci colorate {number}",
  "ui.player_light_pair_lights_helper": "Scegli le luci colorate per questo riproduttore di Music Assistant. Cancella questo campo o il riproduttore per rimuovere l'associazione.",
  "ui.player_light_pair_player": "Riproduttore Music Assistant {number}",
  "ui.player_light_pair_player_helper": "Scegli il riproduttore di Music Assistant per questa associazione di luci.",
  "ui.all": "Tutti",
  "ui.all_genres": "Tutti i generi",
  "ui.genres_and_styles": "Generi e stili",
  "ui.compact_widget_mode": "Stile widget compatto",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Scegli quando la modalità compatta deve utilizzare il widget mobile più piccolo a due righe.",
  "ui.full": "Completo",
  "ui.mini_widget": "Mini widget",
  "ui.genre": "Genere",
  "ui.mood_and_genre": "Stato d'animo e genere",
  "ui.music_by_mood_and_genre": "Musica per stato d'animo e genere",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "Entità opzionale per il pulsante ausiliario. Lascia vuoto per arrestare il riproduttore attivo.",
  "ui.optional_name_for_the_auxiliary_button": "Nome opzionale mostrato come suggerimento per il pulsante ausiliario.",
  "ui.choose_the_auxiliary_button_icon": "Scegli l'icona mostrata per il pulsante ausiliario.",
  "ui.choose_how_players_are_sorted": "Scegli l'ordine dei riproduttori tra predefinito, alfabetico o personalizzato.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Scegli i riproduttori di Music Assistant da non mostrare mai nella scheda.",
  "ui.custom_order": "Ordine personalizzato",
  "ui.no_visible_music_assistant_players_after_exclusions": "Tutti i riproduttori di Music Assistant sono nascosti dall'impostazione dei riproduttori esclusi.",
  "ui.set_custom_player_order": "Imposta l'ordine personalizzato dei riproduttori. Gli slot vuoti verranno ignorati.",
  "ui.show_auxiliary_button_in_quick_actions": "Mostra questo pulsante ausiliario nella riga delle Azioni Rapide.",
  "ui.power": "Alimentazione",
  "ui.power_action_failed": "Azione di accensione/spegnimento fallita",
  "ui.power_action_sent": "Azione di accensione/spegnimento inviata",
  "ui.power_button": "Pulsante ausiliario",
  "ui.power_button_action": "Azione pulsante ausiliario",
  "ui.power_button_entity": "Entità pulsante ausiliario",
  "ui.scene": "Scena",
  "ui.screensaver": "Salvaschermo",
  "ui.screensaver_buttons": "Pulsanti salvaschermo",
  "ui.screensaver_controls": "Controlli salvaschermo",
  "ui.screensaver_clock_mode": "Modalità orologio salvaschermo",
  "ui.screensaver_clock_size": "Dimensione orologio salvaschermo",
  "ui.screensaver_clock_size_helper": "Scala l'orologio del salvaschermo del tablet senza spostare la copertina.",
  "ui.screensaver_clock_x": "Posizione orologio - SINISTRA / DESTRA",
  "ui.screensaver_clock_x_helper": "Valori più bassi spostano a sinistra, valori più alti spostano a destra.",
  "ui.screensaver_clock_y": "Posizione orologio - SU / GIÙ",
  "ui.screensaver_clock_y_helper": "Valori più bassi spostano verso l'alto, valori più alti spostano verso il basso.",
  "ui.screensaver_message": "Messaggio salvaschermo",
  "ui.screensaver_timeout": "Timeout salvaschermo",
  "ui.script": "Script",
  "ui.short_optional_message_shown_on_the_screensaver": "Breve messaggio opzionale mostrato sul salvaschermo.",
  "ui.show_previous_next_controls_on_the_screensaver": "Mostra i pulsanti precedente e successivo sul salvaschermo.",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Scegli esattamente quali pulsanti mostrare sul salvaschermo, inclusi FLOW ASSISTANT, precedente e successivo.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Mostra un orologio discreto e la copertina dopo un periodo di inattività.",
  "ui.show_power_button_in_the_player_controls": "Mostra un pulsante ausiliario nelle Azioni Rapide.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Mostra la modalità scoperta a schermo intero nelle Azioni.",
  "ui.smart_home": "Smart home",
  "ui.soft_transition_time_for_music_lighting": "Tempo di transizione morbida per l'illuminazione musicale.",
  "ui.stop_player": "Ferma riproduttore",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Sincronizza le luci selezionate con il colore della copertina corrente.",
  "ui.toggle": "Inverti",
  "ui.turn_on": "Accendi",
  "ui.choose_a_category_to_browse_provider_playlists": "Scegli una categoria per sfogliare le playlist del fornitore prima di riprodurle.",
  "ui.new_and_recommended": "Novità e consigliati",
  "ui.provider_playlists": "Playlist del fornitore",
  "ui.hotel_mode": "Modalità hotel",
  "ui.hotel_mode_helper": "Interfaccia minima sicura per hotel: solo controlli del riproduttore, volume, ricerca, navigazione copertine e selezione del riproduttore.",
  "ui.style_and_genres": "Stili e generi"
});
const messages$1 = Object.freeze({
  "ui.a_guided_music_wizard": "Valdomas muzikos vedlys",
  "ui.a_radio_station": "Radijo stotis",
  "ui.accent_color": "Akcento spalva",
  "ui.actions": "veiksmai",
  "ui.actions_2": "Veiksmai",
  "ui.active_days": "Aktyvios dienos",
  "ui.active_player_helper": "Aktyvaus grotuvo pagalbininkas",
  "ui.active_player_helper_must_be_an_input_text_entity": "Aktyvaus grotuvo pagalbininkas turi būti „input_text“ objektas",
  "ui.active_players": "Aktyvūs grotuvai",
  "ui.add": "Pridėti",
  "ui.add_to_queue": "Pridėti į eilę",
  "ui.add_to_selection": "Pridėti prie pasirinkimo",
  "ui.added_to_liked": "Pridėta prie patinkančių",
  "ui.added_to_music_assistant_liked": "Pridėta prie „Music Assistant“ patinkančių",
  "ui.added_to_queue": "Pridėta į eilę",
  "ui.added_to_studio_queue": "Pridėta į „Studio“ eilę",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "Prideda judesio fono sluoksniams. „Subtilus“ išlaiko ramybę, „Stiprus“ yra pastebimas, o „Ekstremalus“ – pati drąsiausia animacija.",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "Garsumas padidinamas tik pranešimų metu, vėliau grąžinamas ankstesnis garso lygis.",
  "ui.after_run": "Po paleidimo",
  "ui.album": "Albumas",
  "ui.albums": "Albumai",
  "ui.all_albums": "Visi albumai",
  "ui.all_artists": "Visi atlikėjai",
  "ui.all_countries": "Visos šalys",
  "ui.all_player_groups_disconnected": "Visos grotuvų grupės atjungtos",
  "ui.all_players": "Visi grotuvai",
  "ui.all_players_2": "visi grotuvai",
  "ui.all_podcasts": "Visos tinklalaidės",
  "ui.all_tracks": "Visi kūriniai",
  "ui.allow_local_likes": "Leisti vietinius pamėgtus",
  "ui.always_visible": "Visada matoma",
  "ui.announce": "Pranešti",
  "ui.announce_to_all_players": "Pranešti per visus grotuvus",
  "ui.announcement": "Pranešimas",
  "ui.announcement_failed_with_error": "Pranešimo klaida: {error}",
  "ui.announcement_language": "Pranešimo kalba",
  "ui.announcement_presets": "Pranešimų šablonai",
  "ui.announcement_sent_to_player": "Pranešimas išsiųstas į {player}",
  "ui.announcement_studio": "pranešimų studija",
  "ui.announcement_studio_2": "Pranešimų studija",
  "ui.announcement_target": "Pranešimo tikslas",
  "ui.announcement_to_player_preview": "Pranešimas į {player}: {preview}",
  "ui.announcement_volume_boost": "Pranešimo garso padidinimas",
  "ui.announcements": "Pranešimai",
  "ui.appearance": "Išvaizda",
  "ui.apply_group": "Pritaikyti grupę",
  "ui.apply_schedule": "Pritaikyti tvarkaraštį",
  "ui.artist": "Atlikėjas",
  "ui.artist_radio": "Atlikėjo radijas",
  "ui.artists": "Atlikėjai",
  "ui.artwork": "Viršelis",
  "ui.artwork_swipe": "Braukimas per viršelį",
  "ui.ascending": "Didėjančiai",
  "ui.at_least_one_player_must_stay_selected": "Bent vienas grotuvas turi likti pasirinktas",
  "ui.australia": "Australija",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "Automatiškai parenka telefono arba planšetės režimą pagal plotį.",
  "ui.auto_cloud_default": "Auto / Debesies numatytasis",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "Automatiškai ištraukia spalvas iš viršelio ir išlaiko subtilų efektą. „Stiprus“ padaro spalvas sodresnes.",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "„Auto“ nekeičia Home Assistant Cloud balso nustatymų. Rankiniai pasirinkimai nurodo konkrečią kalbą.",
  "ui.available": "Pasiekiama",
  "ui.back": "Atgal",
  "ui.back_2": "Atgal",
  "ui.back_to_countries": "Atgal į šalis",
  "ui.back_to_queue": "Atgal į eilę",
  "ui.background_motion": "Fono judėjimas",
  "ui.behavior": "Elgsena",
  "ui.browse_covers": "Naršyti viršelius",
  "ui.browse_playlists_artists_albums_and_radio": "Naršyti grojaraščius, atlikėjus, albumus ir radiją",
  "ui.browser_players": "Naršyklės grotuvai",
  "ui.build_custom_mix": "Kurti asmeninį miksą",
  "ui.building_studio_mix": "Kuriamas „Studio“ miksas...",
  "ui.button": "Mygtukas",
  "ui.by_mood": "Pagal nuotaiką",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "Talpyklos trukmė (ms) pasirinktoms duomenų užklausoms.",
  "ui.cache_ttl": "Talpyklos trukmė (TTL)",
  "ui.calm": "Ramu",
  "ui.canada": "Kanada",
  "ui.cancel": "Atšaukti",
  "ui.cancel_2": "Atšaukti",
  "ui.cancel_selection": "Atšaukti pasirinkimą",
  "ui.card_configuration": "Kortelės konfigūracija",
  "ui.card_height": "Kortelės aukštis",
  "ui.card_height_in_pixels": "Kortelės aukštis pikseliais.",
  "ui.card_settings_are_managed_from_the_visual_editor": "Kortelės nustatymai valdomi per vizualų redaktorių",
  "ui.change_song": "Pakeisti dainą",
  "ui.change_track": "Pakeisti kūrinį",
  "ui.chill_mix": "Atpalaiduojantis miksas",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "Pasirinkite šalį arba „Visos šalys“, norėdami naršyti viską radijo skiltyje.",
  "ui.choose_a_queue_position": "Pasirinkite eilės poziciją",
  "ui.choose_a_source_player": "Pasirinkite šaltinio grotuvą",
  "ui.choose_a_target_player": "Pasirinkite tikslo grotuvą",
  "ui.choose_at_least_one_player": "Pasirinkite bent vieną grotuvą",
  "ui.choose_icons_text_or_both_for_the_footer": "Pasirinkite piktogramas, tekstą arba abu apatinėje juostoje.",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "Pasirinkite tik Music Assistant grotuvus. Sąrašas rodo tik juos, kad išvengtumėte painiavos.",
  "ui.choose_one_clear_option": "Pasirinkite vieną išvalymo parinktį",
  "ui.choose_player": "Pasirinkti grotuvą",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "Pasirinkite kažką iš greitojo sąrašo arba burtų lazdelę atsitiktiniam grojaraščiui.",
  "ui.choose_source": "Pasirinkite šaltinį",
  "ui.choose_target": "Pasirinkite tikslą",
  "ui.choose_target_player": "Pasirinkite tikslo grotuvą",
  "ui.choose_the_accent_color_for_the_mobile_layout": "Pasirinkite akcento spalvą mobiliajam vaizdui.",
  "ui.choose_the_music": "Pasirinkite muziką",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "Pasirinkite mažus mygtukus, rodomus po dainos pavadinimu.",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "Pirmiausia pasirinkite šaltinį. Grojimas naudos dabartinį tikslą.",
  "ui.choose_where_to_play": "Pasirinkite, kur groti",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "Pasirinkite, ar braukimas per viršelį perjungia dainą, ar naršo viršelius.",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "Pasirinkite, ar nustatymai valdomi kortelės lange, ar per skydelio konfigūraciją.",
  "ui.choose_which_actions_appear_in_the_main_bar": "Pasirinkite pagrindinės juostos veiksmus.",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "Pasirinkite, kuriomis dienomis veikia automatinis nakties režimas.",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "Pasirinkite greitųjų veiksmų piktogramas. „Išvalyti viską“ paprašys patvirtinimo.",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "Pasirinkite, kurie grotuvai rodomi kaip plytelės.",
  "ui.choose_which_players_stay_in_the_current_control_selection": "Pasirinkite, kurie grotuvai lieka aktyviame valdyme.",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "Pasirinkite matomus skirtukus bibliotekos lange.",
  "ui.choose_who_is_controlled": "Pasirinkite valdomą įrenginį",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "Vizualus režimas išjungia kortelės vidinių nustatymų langą.",
  "ui.clean_all": "Išvalyti viską",
  "ui.clean_all_confirm_action": "Išvalyti viską",
  "ui.clean_all_confirm_copy": "Tai atjungs visus aktyvius grotuvus, sustabdys muziką ir išvalys eiles. Tęsti?",
  "ui.clean_all_confirm_title": "Išvalyti visus grotuvus?",
  "ui.clean_the_studio_wall": "Išvalyti „Studio“ sieną",
  "ui.clear": "Išvalyti",
  "ui.clear_queue": "Išvalyti eilę",
  "ui.clear_search": "Išvalyti paiešką",
  "ui.clone_queue": "Klonuoti eilę",
  "ui.close": "Uždaryti",
  "ui.collapse_compact_player": "Suskleisti kompaktinį grotuvą",
  "ui.compact_mode": "Kompaktiškas režimas",
  "ui.configure_ready_made_announcement_phrases": "Konfigūruoti iš anksto paruoštas pranešimų frazes.",
  "ui.connect_this_device": "Prijungti šį įrenginį",
  "ui.connect_this_device_2": "Prijungti šį įrenginį",
  "ui.connected": "Prijungtas",
  "ui.connected_2": "Prijungtas",
  "ui.connected_players": "prijungti grotuvai",
  "ui.connected_players_2": "Prijungti grotuvai",
  "ui.connecting": "Jungiamasi",
  "ui.connecting_2": "Jungiamasi...",
  "ui.confirm": "Patvirtinti",
  "ui.connection_and_behavior": "Ryšys ir elgsena",
  "ui.connects_directly_from_this_card_using_sendspin": "Jungiasi tiesiogiai iš šios kortelės (Sendspin)",
  "ui.continue": "Tęsti",
  "ui.continue_2": "Tęsti",
  "ui.continue_from_recent_music_assistant_activity": "Tęsti neseną „Music Assistant“ veiklą.",
  "ui.continue_listening": "Tęsti klausymą",
  "ui.continue_what_was_played_recently": "Tęsti tai, kas neseniai grojo",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "Valdykite, ar kortelės fonas juda ir kaip stipriai.",
  "ui.controlling": "Valdoma",
  "ui.could_not_build_quick_mix_from_the_current_song": "Nepavyko sukurti greito mikso iš šios dainos",
  "ui.could_not_build_studio_mix": "Nepavyko sukurti „Studio“ mikso",
  "ui.could_not_choose_media": "Nepavyko pasirinkti medijos",
  "ui.could_not_clone_the_queue": "Nepavyko klonuoti eilės",
  "ui.could_not_find_music": "Muzika nerasta",
  "ui.could_not_play_label": "Nepavyko paleisti: {label}",
  "ui.could_not_start_chill_mix": "Nepavyko pradėti atpalaiduojančio mikso",
  "ui.could_not_start_playback": "Nepavyko pradėti grojimo",
  "ui.could_not_start_playback_2": "Nepavyko pradėti grojimo",
  "ui.could_not_start_playback_in_studio": "Nepavyko pradėti grojimo „Studio“ lange",
  "ui.could_not_start_quick_mix": "Nepavyko pradėti greito mikso",
  "ui.could_not_start_radio_playback": "Nepavyko pradėti radijo",
  "ui.could_not_transfer_the_queue": "Nepavyko perkelti eilės",
  "ui.could_not_update_active_player_helper": "Nepavyko atnaujinti aktyvaus grotuvo pagalbininko",
  "ui.create_schedule": "Sukurti tvarkaraštį",
  "ui.current_path": "Dabartinis kelias",
  "ui.custom": "Pasirinktinis",
  "ui.dark": "Tamsus",
  "ui.delete_scene": "Ištrinti sceną",
  "ui.delete_schedule": "Ištrinti tvarkaraštį",
  "ui.descending": "Mažėjančiai",
  "ui.dictate": "Diktuoti",
  "ui.disabled": "Išjungta",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "Išjungia suliejimą, animacijas ir šešėlius. Rekomenduojama silpnesniems įrenginiams.",
  "ui.disconnect": "Atjungti",
  "ui.disconnect_all": "Atjungti viską",
  "ui.disconnect_groups": "Atjungti grupes",
  "ui.disconnect_player_groups": "Atjungti grotuvų grupes",
  "ui.disconnect_this_device": "Atjungti šį įrenginį",
  "ui.disconnected": "Atjungta",
  "ui.disconnecting": "Atjungiama",
  "ui.disconnecting_player_groups": "Atjungiamos grotuvų grupės...",
  "ui.discover": "Atrasti",
  "ui.down": "Žemyn",
  "ui.dynamic_theme": "Dinaminė tema",
  "ui.edit_schedule": "Redaguoti tvarkaraštį",
  "ui.enable": "Įjungti",
  "ui.enable_or_disable_the_footer_search_button": "Įjungti/išjungti paieškos mygtuką apačioje.",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "Įjungti/išjungti „Studio“ mygtuką apačioje.",
  "ui.enable_the_floating_home_shortcut_button": "Įjungti plaukiojantį pradžios mygtuką.",
  "ui.enabled": "Įjungta",
  "ui.enables_switching_to_local_liked_mode": "Leidžia perjungti į vietinį patikdymų režimą.",
  "ui.end_time": "Pabaigos laikas",
  "ui.energetic": "Energingas",
  "ui.energy_and_rhythm": "Energija ir ritmas",
  "ui.enter_an_announcement_first": "Pirmiausia įveskite pranešimą",
  "ui.example_quiet_jazz_greek_music_workout": "Pavyzdžiui: ramus džiazas, graikiška muzika, treniruotė",
  "ui.existing_content": "Esamas turinys",
  "ui.expand_player": "Išplėsti grotuvą",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "Ištraukti spalvas iš viršelio ir pritaikyti jas sąsajai.",
  "ui.extreme": "Ekstremalus",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "Atsarginis variantas sistemoms su alternatyviu eilės keliu.",
  "ui.fallback_mode": "Atsarginis režimas",
  "ui.family_friendly": "Visai šeimai",
  "ui.favorite": "Mėgstamiausias",
  "ui.favorite_action_failed": "Mėgstamiausio veiksmas nepavyko",
  "ui.favorite_action_sent": "Mėgstamiausio veiksmas išsiųstas",
  "ui.favorite_button_entity": "Mėgstamiausių mygtuko objektas",
  "ui.favorite_center": "mėgstamiausių centras",
  "ui.favorite_center_2": "Mėgstamiausių centras",
  "ui.favorite_radio": "Mėgstamiausių radijas",
  "ui.favorite_updated": "Mėgstamiausi atnaujinti",
  "ui.favorites_and_liked_music": "Mėgstamiausia ir patinkanti muzika",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "Funkcijų aptikimas, grotuvo kontekstas ir šio įrenginio atkūrimo įrankiai.",
  "ui.find_music": "Rasti muziką",
  "ui.finding_music": "Ieškoma muzikos",
  "ui.fixed": "Fiksuotas",
  "ui.flow_mix": "FLOW miksas",
  "ui.font_scale": "Šrifto mastelis",
  "ui.font_size": "Šrifto dydis",
  "ui.footer_search": "Paieška apačioje",
  "ui.footer_style": "Apatinės juostos stilius",
  "ui.footer_style_2": "Apatinės juostos stilius",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "Pavyzdžiui: /lovelace/home ar kitas Home Assistant kelias.",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "Silpnesniems ekranams: išjungia foninius efektus ir animacijas.",
  "ui.france": "Prancūzija",
  "ui.free_style": "Laisvas stilius",
  "ui.free_style_quiet_jazz_greek_music_workout": "Laisvai: ramus džiazas, graikiška muzika...",
  "ui.fresh_start": "Nauja pradžia",
  "ui.fresh_start_2": "Nauja pradžia",
  "ui.fri": "Pen",
  "ui.friendly_and_simple": "Draugiškas ir paprastas",
  "ui.from": "Iš",
  "ui.general": "Bendra",
  "ui.general_and_display": "Bendra ir ekranas",
  "ui.germany": "Vokietija",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "Bendras šrifto mastelis. 1 yra numatytasis dydis.",
  "ui.greece": "Graikija",
  "ui.grid": "Tinklelis",
  "ui.group": "Grupė",
  "ui.group_cleared": "Grupė išvalyta",
  "ui.group_cleared_2": "Grupė išvalyta",
  "ui.group_speakers": "Grupuoti garsiakalbius",
  "ui.group_speakers_2": "Grupuoti garsiakalbius",
  "ui.group_updated": "Grupė atnaujinta",
  "ui.group_volume": "Grupės garsumas",
  "ui.group_volume_up_energetic_mix": "Grupė, garsiau, energingas miksas",
  "ui.grouped_players": "Sugrupuoti grotuvai",
  "ui.hebrew": "Hebrajų",
  "ui.history": "Istorija",
  "ui.history_tabs": "Istorijos skirtukai",
  "ui.home": "Pradžia",
  "ui.home_party_night_presets": "Pradžia, vakarėlis, nakties šablonai",
  "ui.home_path": "Pradžios kelias",
  "ui.home_scene_prepared": "Namų scena paruošta",
  "ui.home_shortcut": "Pradžios nuoroda",
  "ui.home_shortcut_path": "Pradžios nuorodos kelias",
  "ui.icon_and_text": "Piktograma ir tekstas",
  "ui.icon_only": "Tik piktograma",
  "ui.icon_plus_text": "Piktograma + tekstas",
  "ui.idle": "Neveiklus",
  "ui.idle_2": "Neveiklus",
  "ui.in_card_ui": "Kortelės sąsaja",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "Apima pasirinktinį temos režimą iš kortelės nustatymų.",
  "ui.info": "Informacija",
  "ui.israel": "Izraelis",
  "ui.israeli": "Izraelietiška",
  "ui.italy": "Italija",
  "ui.items": "elementai",
  "ui.join_selected_players": "Sujungti pasirinktus grotuvus",
  "ui.kids": "Vaikams",
  "ui.language": "Kalba",
  "ui.larger_lyrics": "Didesni žodžiai",
  "ui.layout_adapted_to_height": "Išdėstymas pritaikytas aukščiui",
  "ui.layout_mode": "Išdėstymo režimas",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "Palikite tuščią, jei kortelė naudos tik Home Assistant.",
  "ui.library": "Biblioteka",
  "ui.library_2": "Biblioteka",
  "ui.library_pages": "Bibliotekos puslapiai",
  "ui.default_library_layout": "Numatytasis bibliotekos išdėstymas",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "Pasirinkite, kaip atidaromi bibliotekos puslapiai. Bibliotekoje vis tiek galite rankiniu būdu perjungti tinklelį / sąrašą.",
  "ui.library_radio": "Bibliotekos radijas",
  "ui.library_surprise": "Bibliotekos staigmena",
  "ui.library_tabs": "Bibliotekos skirtukai",
  "ui.library_tabs_2": "Bibliotekos skirtukai",
  "ui.light": "Šviesus",
  "ui.like": "Patinka",
  "ui.like_2": "Patinka",
  "ui.like_3": "Patinka",
  "ui.liked": "Pamėgti",
  "ui.liked_mode": "Pamėgtų režimas",
  "ui.liked_sync": "Pamėgtų sinchronizacija",
  "ui.list": "Sąrašas",
  "ui.listening": "Klausomasi...",
  "ui.loading": "Kraunama...",
  "ui.loading_favorites": "Kraunami mėgstamiausi...",
  "ui.loading_library": "Kraunama biblioteka...",
  "ui.loading_lyrics": "Kraunami žodžiai...",
  "ui.loading_players": "Kraunami grotuvai...",
  "ui.loading_queue": "Kraunama eilė...",
  "ui.loading_recent_items": "Kraunami naujausi...",
  "ui.loading_schedules": "Kraunami tvarkaraščiai...",
  "ui.local": "Vietinis",
  "ui.local_favorites": "Vietiniai mėgstamiausi",
  "ui.local_favorites_2": "Vietiniai mėgstamiausi",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "Vietinis režimas prasmingas tik įjungus vietinius pamėgtus.",
  "ui.low_light_listening": "Klausymasis prietemoje",
  "ui.low_volume_and_quiet_mix": "Tylus garsas ir ramus miksas",
  "ui.lower_volume_mood": "Tylesnio garso nuotaika",
  "ui.lyrics": "Dainų žodžiai",
  "ui.lyrics_earlier": "Žodžiai anksčiau",
  "ui.lyrics_font_size": "Žodžių šrifto dydis",
  "ui.lyrics_later": "Žodžiai vėliau",
  "ui.lyrics_timing": "Žodžių laikas",
  "ui.lyrics_unavailable_right_now": "Žodžiai šiuo metu nepasiekiami",
  "ui.ma_favorite_action_failed_with_error": "Music Assistant mėgstamiausių veiksmo klaida: {error}",
  "ui.ma_interface_path": "MA sąsajos kelias",
  "ui.main_bar": "Pagrindinė juosta",
  "ui.main_bar_items": "Pagrindinės juostos elementai",
  "ui.main_bar_items_2": "Pagrindinės juostos elementai",
  "ui.main_opacity": "Pagrindinis permatomumas",
  "ui.mainly_relevant_on_larger_layouts": "Aktualu didesniuose ekranuose.",
  "ui.mass_queue_send_command_fallback": "mass_queue/send_command atsarginis variantas",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "Atitinka mikrofono nustatymą iš kortelės nustatymų.",
  "ui.media": "Medija",
  "ui.media_2": "Medija",
  "ui.media_actions": "Medijos veiksmai",
  "ui.media_layout": "Medijos išdėstymas",
  "ui.media_selected_target": "Pasirinkta: {media} ? Tikslas: {target}",
  "ui.menu": "Meniu",
  "ui.message": "Pranešimas",
  "ui.microphone": "Mikrofonas",
  "ui.microphone_2": "Mikrofonas",
  "ui.microphone_is_disabled": "Mikrofonas išjungtas",
  "ui.missing_url": "Trūksta URL",
  "ui.mobile": "Mobilus",
  "ui.mon": "Pir",
  "ui.mood_style_or_free_text": "Nuotaika, stilius ar laisvas tekstas",
  "ui.more_info": "Daugiau informacijos",
  "ui.more_info_is_unavailable_for_this_player": "Šiam grotuvui papildomos informacijos nėra",
  "ui.morning": "Rytas",
  "ui.morning_mix": "Ryto miksas",
  "ui.move_to_next": "Perkelti į kitą",
  "ui.move_to_position": "Perkelti į poziciją",
  "ui.music": "Muzika",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "Music Assistant integracijos ID.",
  "ui.music_assistant_config_entry_was_not_found": "Music Assistant integracija nerasta",
  "ui.music_assistant_token": "Music Assistant raktas (Token)",
  "ui.music_assistant_url": "Music Assistant URL",
  "ui.music_hub": "muzikos centras",
  "ui.music_hub_2": "Muzikos centras",
  "ui.mute": "Nutildyti",
  "ui.mute_command_failed": "Nutildyti nepavyko",
  "ui.my_studio_scene": "Mano Studio scena",
  "ui.name_or_keyword": "Pavadinimas ar raktažodis",
  "ui.name_this_studio_scene": "Pavadinkite šią Studio sceną...",
  "ui.netherlands": "Nyderlandai",
  "ui.new_schedule": "Naujas tvarkaraštis",
  "ui.new_wake_schedule": "Naujas žadinimo tvarkaraštis",
  "ui.newest": "Naujausi",
  "ui.next": "Kitas",
  "ui.next_track_failed": "Nepavyko perjungti dainos",
  "ui.night": "Naktis",
  "ui.night_end_time": "Nakties pabaigos laikas",
  "ui.night_mode": "Nakties režimas",
  "ui.night_mode_auto_window": "Automatinė naktis {start}-{end}",
  "ui.night_mode_days": "Nakties režimo dienos",
  "ui.night_mode_is_always_on": "Nakties režimas visada įjungtas",
  "ui.night_mode_is_off": "Nakties režimas išjungtas",
  "ui.night_mode_is_off_until_you_choose_another_mode": "Nakties režimas išjungtas, kol nepasirinksite kito.",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "Nakties režimas įjungtas, kol nepasirinksite kito.",
  "ui.night_schedule_updated": "Nakties tvarkaraštis atnaujintas",
  "ui.night_start_time": "Nakties pradžios laikas",
  "ui.no_active_media": "Nėra aktyvios medijos",
  "ui.no_active_players": "Nėra aktyvių grotuvų",
  "ui.no_available_players": "Nėra pasiekiamų grotuvų",
  "ui.no_content_found": "Turinys nerastas",
  "ui.no_dynamic_player_groups_to_disconnect": "Nėra dinaminių grupių, kurias būtų galima atjungti",
  "ui.no_extra_ma_players": "Nėra papildomų MA grotuvų",
  "ui.no_extra_ma_players_2": "Nėra papildomų MA grotuvų",
  "ui.no_favorites_found": "Mėgstamiausių nerasta.",
  "ui.no_liked_media_yet": "Dar nėra patinkančios medijos",
  "ui.no_liked_tracks_to_play": "Nėra patinkančių kūrinių grojimui",
  "ui.no_light_entities_found": "Šviesos objektų nerasta.",
  "ui.no_lyrics_found": "Žodžių nerasta",
  "ui.no_matching_content_was_found": "Atitinkančio turinio nerasta",
  "ui.no_media_found": "Medijos nerasta.",
  "ui.no_media_found_for_this_search": "Pagal šią paiešką medijos nerasta.",
  "ui.no_mix_content_found": "Mikso turinio nerasta",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "Music Assistant grotuvų nerasta. Patikrinkite media_player objektus.",
  "ui.no_music_assistant_players_were_found_for_pinning": "MA grotuvų nerasta prisegimui.",
  "ui.no_next_track_in_queue": "Eilėje nėra kito kūrinio",
  "ui.no_player_groups_to_disconnect": "Nėra grotuvų grupių atjungimui",
  "ui.no_player_light_pairs_yet": "Dar nėra grotuvo-šviesos porų.",
  "ui.no_player_selected": "Grotuvas nepasirinktas",
  "ui.no_players_found": "Grotuvų nerasta",
  "ui.no_players_found_yet": "Grotuvų dar nerasta",
  "ui.no_playlists_found": "Grojaraščių nerasta",
  "ui.no_queue": "Nėra eilės",
  "ui.no_queue_item_to_rebuild": "Nėra eilės elemento atstatymui",
  "ui.no_queue_to_clone": "Nėra eilės klonavimui",
  "ui.no_radio_browser_countries_found": "Radijo šalių nerasta",
  "ui.no_radio_stations_found": "Radijo stočių nerasta",
  "ui.no_recent_listening_yet": "Dar nėra neseniai klausytų.",
  "ui.no_results": "Nėra rezultatų",
  "ui.no_results_found": "Rezultatų nerasta",
  "ui.no_saved_studio_scenes_yet": "Dar nėra išsaugotų Studio scenų.",
  "ui.no_scheduled_start_is_active": "Nėra aktyvaus suplanuoto starto",
  "ui.no_selected_tracks_to_play": "Nepasirinkta kūrinių grojimui",
  "ui.no_sleep_timer_is_active": "Išsijungimo laikmatis neaktyvus",
  "ui.no_speech_was_captured": "Kalba neužfiksuota",
  "ui.no_target_players_available": "Nėra pasiekiamų tikslo grotuvų",
  "ui.no_target_players_available_2": "Nėra pasiekiamų tikslo grotuvų",
  "ui.no_tts_service_or_entity_is_configured": "TTS paslauga arba objektas nesukonfigūruotas",
  "ui.no_wake_schedules_yet": "Dar nėra žadinimo tvarkaraščių",
  "ui.none": "Nėra",
  "ui.nothing_is_playing_right_now": "Šiuo metu nieko negroja",
  "ui.nothing_playing": "Nieko negroja",
  "ui.now_playing": "Dabar groja",
  "ui.now_playing_2": "Dabar groja",
  "ui.off": "Išjungta",
  "ui.off_2": "Išjungta",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "„Išjungta“ atjungia, „Įjungta“ palieka aktyvų, „Auto“ seka pagal nustatytą laiką.",
  "ui.oldest": "Seniausi",
  "ui.on": "Įjungta",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "Vienu paspaudimu paruoškite grotuvus ir garsą. Išsaugokite kaip vietinę sceną.",
  "ui.only_actions_for_the_current_target_are_shown_here": "Čia rodomi tik dabartinio tikslo veiksmai.",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Rodomi tik MA grotuvai. Naršyklės ir vietiniai grotuvai slepiami.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "Reikalinga tik naudojant tiesioginį Music Assistant URL.",
  "ui.opacity_for_popups_and_overlays": "Iššokančių langų permatomumas.",
  "ui.opacity_for_the_main_card_background": "Pagrindinio kortelės fono permatomumas.",
  "ui.open_full_interface": "Atidaryti pilną sąsają",
  "ui.open_full_queue": "Atidaryti pilną eilę",
  "ui.open_interface_in": "Atidaryti sąsają",
  "ui.open_ma": "Atidaryti MA",
  "ui.open_music_assistant": "Atidaryti Music Assistant",
  "ui.open_music_assistant_2": "Atidaryti Music Assistant?",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "Atidarykite MA šiame įrenginyje, kad aktyvuotumėte naršyklės grotuvą",
  "ui.open_player_picker": "Atidaryti grotuvo parinkiklį",
  "ui.open_queue": "Atidaryti eilę",
  "ui.open_saved_songs": "Atidaryti išsaugotas dainas",
  "ui.open_the_full_music_assistant_interface": "Atidaryti pilną Music Assistant sąsają?",
  "ui.open_the_full_music_assistant_interface_2": "Atidaryti pilną Music Assistant sąsają",
  "ui.opening_studio_library": "Atidaroma Studio biblioteka",
  "ui.opening_timers": "Atidaromi laikmačiai",
  "ui.option": "Parinktis",
  "ui.optional": "Neprivaloma",
  "ui.optional_2": "Neprivaloma",
  "ui.optional_entity_used_for_an_external_favorite_button": "Neprivalomas objektas išoriniam mėgstamiausių mygtukui.",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "Neprivalomas pagalbininkas atnaujinamas pagal aktyvų grotuvą (automatizacijoms).",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "Neprivalomas pagalbininkas atnaujinamas pagal aktyvų grotuvą.",
  "ui.other": "Kiti",
  "ui.other_players": "Kiti grotuvai",
  "ui.party": "Vakarėlis",
  "ui.path_used_when_opening_the_music_assistant_interface": "Kelias naudojamas atidarant Music Assistant sąsają.",
  "ui.pause": "Pauzė",
  "ui.paused": "Pauzė",
  "ui.per_device_in_card": "Šiame įrenginyje / kortelėje",
  "ui.performance_mode": "Našumo režimas",
  "ui.performance_mode_for_weak_devices": "Našumo režimas silpniems įrenginiams",
  "ui.performance_profile": "Našumo profilis",
  "ui.performance_profile_helper": "Pasirinkite, kiek vizualinių efektų kortelė gali naudoti. Ultra Lite sumažina skaidrumą, suliejimą, dinaminius fonus ir judėjimą silpniems įrenginiams.",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "Pasirinkite nuotaiką arba įveskite savo. Studio suras ir sukurs eilę.",
  "ui.pinned": "Prisegtas",
  "ui.pinned_players": "Prisegti grotuvai",
  "ui.play": "Groti",
  "ui.play_2": "Groti",
  "ui.play_a_few_tracks_and_they_will_appear_here": "Paklausykite kelis kūrinius ir jie atsiras čia.",
  "ui.play_a_saved_list": "Paleisti išsaugotą sąrašą",
  "ui.play_all": "Groti viską",
  "ui.play_an_artist": "Paleisti atlikėją",
  "ui.play_artwork_action": "Viršelio paspaudimo veiksmas",
  "ui.play_everywhere": "Groti visur",
  "ui.play_next": "Groti kitą",
  "ui.play_next_and_clear_queue": "Groti kitą ir išvalyti eilę",
  "ui.play_now": "Groti dabar",
  "ui.play_now_and_clear_queue": "Groti dabar ir išvalyti eilę",
  "ui.play_pause": "Groti / Pauzė",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "Grokite, eilė, radijas arba šalinkite iš mėgstamiausių tiesiai per Studio.",
  "ui.play_selected": "Groti pasirinktus",
  "ui.playback_command_failed": "Grojimo komanda nepavyko",
  "ui.playback_command_failed_2": "Grojimo komanda nepavyko",
  "ui.playback_started": "Grojimas pradėtas",
  "ui.player": "grotuvas",
  "ui.player_2": "Grotuvas",
  "ui.player_count_many": "{count} grotuvai",
  "ui.player_count_one": "1 grotuvas",
  "ui.player_groups_could_not_be_disconnected": "Nepavyko atjungti grotuvų grupių",
  "ui.player_is_pinned_from_settings": "Grotuvas prisegtas iš nustatymų",
  "ui.player_is_ready": "Grotuvas paruoštas",
  "ui.player_is_ready_nothing_is_playing_right_now": "Grotuvas paruoštas - šiuo metu nieko negroja",
  "ui.player_on_this_device": "Grotuvas šiame įrenginyje",
  "ui.player_selected": "Grotuvas pasirinktas",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "Grotuvo nustatymai rodomi tik skaitymui, kol bus palaikoma modifikacija.",
  "ui.players": "Grotuvai",
  "ui.players_queue_and_groups": "Grotuvai, eilė ir grupės",
  "ui.playing": "Groja",
  "ui.playing_a_random_playlist": "Grojamas atsitiktinis grojaraštis",
  "ui.playing_a_random_radio_station": "Grojama atsitiktinė radijo stotis",
  "ui.playing_on": "Groja per",
  "ui.playing_on_2": "Groja per",
  "ui.playlist": "Grojaraštis",
  "ui.playlists": "Grojaraščiai",
  "ui.podcast": "Tinklalaidė",
  "ui.podcasts": "Tinklalaidės",
  "ui.popup_opacity": "Iššokančio lango permatomumas",
  "ui.preset_message": "Šablono pranešimas",
  "ui.previous": "Ankstesnis",
  "ui.primary": "Pagrindinis",
  "ui.primary_target": "Pagrindinis tikslas",
  "ui.pro_tools": "Pro įrankiai",
  "ui.queue": "Eilė",
  "ui.queue_2": "Eilė",
  "ui.queue_action_failed": "Nepavyko atlikti veiksmo su eile",
  "ui.queue_action_is_not_available_for_this_player": "Eilės veiksmai šiam grotuvui negalimi",
  "ui.queue_actions": "Eilės veiksmai",
  "ui.queue_cleared": "Eilė išvalyta",
  "ui.queue_cleared_and_item_moved_next": "Eilė išvalyta ir elementas perkeltas į priekį",
  "ui.queue_cloned": "Eilė klonuota",
  "ui.queue_cockpit": "Eilės valdymas",
  "ui.queue_cockpit_2": "Eilės valdymas",
  "ui.queue_is_empty": "Eilė tuščia",
  "ui.queue_is_unavailable_for_this_player": "Eilė nepasiekiama šiam grotuvui",
  "ui.queue_item": "Eilės elementas",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "Elemento ID dar neparuoštas. Atnaujinkite eilę ir bandykite dar kartą.",
  "ui.queue_results": "Eilės rezultatai",
  "ui.queue_transferred": "Eilė pervesta",
  "ui.queues_refreshed": "Eilės atnaujintos",
  "ui.quick_actions": "Greitieji veiksmai",
  "ui.quick_actions_2": "Greitieji veiksmai",
  "ui.quick_mix": "Greitas miksas",
  "ui.quick_mix_started": "Greitas miksas pradėtas",
  "ui.quick_play": "Greitas grojimas",
  "ui.radio": "Radijas",
  "ui.radio_browser": "Radijo naršyklė",
  "ui.radio_browser_countries": "Radijo šalys",
  "ui.radio_browser_country": "Radijo šalis",
  "ui.radio_browser_country_2": "Radijo šalis",
  "ui.radio_mode_is_not_available_for_this_media_type": "Radijo režimas nepasiekiamas šiam medijos tipui",
  "ui.radio_mode_started": "Radijo režimas pradėtas",
  "ui.radio_station_changed": "Radijo stotis pakeista",
  "ui.radio_stations": "Radijo stotys",
  "ui.random": "Atsitiktinis",
  "ui.random_gentle_morning_mix": "Atsitiktinis švelnus ryto miksas",
  "ui.random_playlist": "Atsitiktinis grojaraštis",
  "ui.random_radio": "Atsitiktinis radijas",
  "ui.ready": "Paruošta",
  "ui.ready_to_play": "Paruošta groti",
  "ui.recent": "Naujausi",
  "ui.recent_continue": "Naujausi / Tęsti",
  "ui.recent_listening": "neseniai klausyta",
  "ui.recent_track": "Naujausias kūrinys",
  "ui.recently_added": "Neseniai pridėta",
  "ui.recently_played": "Neseniai grota",
  "ui.recently_played_2": "Neseniai grota",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "Rekomendacijos atsiras, kai HOMEii Flow matys jūsų eilę arba klausymo istoriją.",
  "ui.recommended": "Rekomenduojama",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "Rekomenduojamas formatas: HH:MM, pvz. 06:00. Vidurnakčio peržengimas palaikomas.",
  "ui.recommended_format_hh_mm_such_as_22_00": "Rekomenduojamas formatas: HH:MM, pvz. 22:00.",
  "ui.recommended_playlist": "Rekomenduojamas grojaraštis",
  "ui.recommended_track": "Rekomenduojamas kūrinys",
  "ui.reconnects_when_you_return_to_this_dashboard": "Prisijungia iš naujo, kai grįžtate į šį skydelį",
  "ui.refresh": "Atnaujinti",
  "ui.remember_as_this_device": "Prisiminti kaip šį įrenginį",
  "ui.remove": "Pašalinti",
  "ui.remove_from_selection": "Pašalinti iš pasirinkimo",
  "ui.removed_from_liked": "Pašalinta iš patinkančių",
  "ui.removed_from_music_assistant_liked": "Pašalinta iš Music Assistant patinkančių",
  "ui.reorder_queue": "Perrūšiuoti eilę",
  "ui.repeat": "Kartoti",
  "ui.repeat_all": "Kartoti viską",
  "ui.repeat_off": "Kartoti išjungta",
  "ui.repeat_one": "Kartoti vieną",
  "ui.reset": "Atstatyti",
  "ui.reset_lyrics_font_size": "Atstatyti žodžių šrifto dydį",
  "ui.reset_lyrics_timing": "Atstatyti žodžių laiką",
  "ui.results": "Rezultatai",
  "ui.rtl": "Iš dešinės į kairę (RTL)",
  "ui.sat": "Šeš",
  "ui.save_current_target": "Išsaugoti dabartinį tikslą",
  "ui.save_schedule": "Išsaugoti tvarkaraštį",
  "ui.scene_players_are_not_available": "Scenos grotuvai nepasiekiami",
  "ui.scene_presets": "scenų šablonai",
  "ui.scene_presets_2": "Scenų šablonai",
  "ui.scenes": "Scenos",
  "ui.scales_interface_icons_without_changing_button_sizes": "Keičia sąsajos piktogramų mastelį nekeičiant mygtukų dydžio.",
  "ui.scheduled_start": "Suplanuotas startas",
  "ui.scheduled_start_activated_label": "Suplanuotas startas aktyvuotas: {label}",
  "ui.scheduled_start_cleared": "Suplanuotas startas išvalytas",
  "ui.scheduled_start_failed": "Suplanuotas startas nepavyko",
  "ui.scheduled_start_saved": "Suplanuotas startas išsaugotas",
  "ui.scheduled_starts_active_count": "Aktyvūs suplanuoti startai: {count}",
  "ui.schedules": "Tvarkaraščiai",
  "ui.search": "Ieškoti",
  "ui.search_and_choose_play_next_add_radio_or_like": "Ieškokite ir pasirinkite: Groti, Kitas, Pridėti, Radijas arba Patinka.",
  "ui.search_everything": "Ieškoti visko...",
  "ui.search_failed": "Paieška nepavyko",
  "ui.search_in_queue": "Ieškoti eilėje",
  "ui.search_queue_and_library": "Ieškoti eilėje ir bibliotekoje...",
  "ui.search_the_library": "Ieškoti bibliotekoje...",
  "ui.search_tracks_albums_artists_and_playlists": "Ieškoti kūrinių, albumų, atlikėjų ir grojaraščių",
  "ui.searching": "Ieškoma...",
  "ui.searching_library": "Ieškoma bibliotekoje...",
  "ui.searching_smart_selection": "Ieškoma išmaniajame pasirinkime...",
  "ui.seek": "Slinkti",
  "ui.select": "Pasirinkti",
  "ui.select_lights": "Pasirinkti apšvietimą",
  "ui.select_player": "Pasirinkti grotuvą",
  "ui.select_2": "Pasirinkti",
  "ui.select_a_player_first": "Pirmiausia pasirinkite grotuvą",
  "ui.select_at_least_one_studio_player": "Pasirinkite bent vieną Studio grotuvą",
  "ui.select_at_least_two_players_to_create_a_group": "Pasirinkite bent du grotuvus grupei sukurti",
  "ui.selected": "Pasirinkta",
  "ui.selected_music": "Pasirinkta muzika",
  "ui.selected_player": "Pasirinktas grotuvas",
  "ui.selected_player_2": "Pasirinktas grotuvas",
  "ui.selected_player_3": "Pasirinktas grotuvas",
  "ui.selected_players_at_comfortable_volume": "Pasirinkti grotuvai patogiu garsumu",
  "ui.selected_playlist": "Pasirinktas grojaraštis",
  "ui.send_a_short_voice_message_or_announcement_url": "Siųsti trumpą balso pranešimą arba URL.",
  "ui.send_a_voice_message": "Siųsti balso pranešimą",
  "ui.send_announcement": "Siųsti pranešimą",
  "ui.sendspin_and_diagnostics": "Sendspin ir diagnostika",
  "ui.set_sleep_timer": "Nustatyti išsijungimo laikmatį",
  "ui.set_volume": "Nustatyti garsumą",
  "ui.settings": "Nustatymai",
  "ui.settings_source": "Nustatymų šaltinis",
  "ui.show_ma_button": "Rodyti MA mygtuką",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "Rodyti/slėpti sekančio kūrinio eilutę grojimo lange.",
  "ui.show_theme_toggle": "Rodyti temos perjungiklį",
  "ui.show_up_next": "Rodyti „Kitas“",
  "ui.show_up_next_2": "Rodyti „Kitas“",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "Rodo atskirą kompaktinio grotuvo plytelę su valdymu.",
  "ui.shuffle": "Maišyti",
  "ui.shuffle_all": "Maišyti viską",
  "ui.shuffle_favorites": "Maišyti mėgstamiausius",
  "ui.shuffle_play": "Maišyti ir groti",
  "ui.skipped_to_up_next": "Peršokta prie sekančio",
  "ui.sleep_timer": "Išsijungimo laikmatis",
  "ui.sleep_timer_2": "Išsijungimo laikmatis",
  "ui.sleep_timer_active_remaining": "Laikmatis aktyvus: {remaining}",
  "ui.sleep_timer_added_minutes": "Pridėta {minutes} min. prie laikmačio",
  "ui.sleep_timer_and_morning_playback": "Laikmatis ir rytinis grojimas",
  "ui.sleep_timer_and_scheduled_playback": "Laikmatis ir suplanuotas grojimas",
  "ui.sleep_timer_cleared": "Laikmatis išvalytas",
  "ui.sleep_timer_finished": "Laikmatis baigėsi",
  "ui.sleep_timer_presets": "Laikmačio šablonai",
  "ui.sleep_timer_set_for_15_minutes": "Laikmatis nustatytas 15 minučių",
  "ui.sleep_timer_set_minutes": "Laikmatis nustatytas {minutes} minutėms",
  "ui.sleep_timer_updated_minutes": "Laikmatis atnaujintas į {minutes} minučių",
  "ui.smaller_lyrics": "Mažesni žodžiai",
  "ui.smart": "Išmanusis",
  "ui.smart_mix": "išmanusis miksas",
  "ui.smart_mix_builder": "Išmaniojo mikso kūrėjas",
  "ui.smart_voice_selection": "Išmanusis balso pasirinkimas",
  "ui.soft_and_relaxed": "Švelnus ir atsipalaidavęs",
  "ui.soft_relaxed_music": "Švelni, atsipalaidavimo muzika",
  "ui.some_players_could_not_be_stopped_or_cleared": "Kai kurių grotuvų nepavyko sustabdyti ar išvalyti",
  "ui.songs_around_an_artist": "Dainos pagal atlikėją",
  "ui.sort": "Rūšiuoti",
  "ui.sort_ascending": "Rūšiuoti didėjančiai",
  "ui.sort_descending": "Rūšiuoti mažėjančiai",
  "ui.source_queue": "Šaltinio eilė",
  "ui.spain": "Ispanija",
  "ui.speak_to_target_players": "Kalbėti per tikslo grotuvus",
  "ui.start_radio_mode": "Pradėti radijo režimą",
  "ui.start_time": "Pradžios laikas",
  "ui.start_time_2": "Pradžios laikas",
  "ui.started_on_selected_player": "Pradėta pasirinktame grotuve",
  "ui.starting_a_chill_mix": "Pradedamas atpalaiduojantis miksas",
  "ui.starting_quick_mix": "Pradedamas greitas miksas...",
  "ui.stations": "stotys",
  "ui.stay_active": "Likti aktyviam",
  "ui.stays_active": "Lieka aktyvus",
  "ui.stop_all": "Sustabdyti viską",
  "ui.stop_all_players": "Sustabdyti visus grotuvus",
  "ui.stop_command_failed": "Sustabdymo komanda nepavyko",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "Sustabdyti grojimą, išvalyti eiles ir atjungti grupes",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "Sustabdyti visi grotuvai, išvalyti grojaraščiai ir atjungtos grupės",
  "ui.stops_this_browser_player": "Sustabdo šį naršyklės grotuvą",
  "ui.strong": "Stiprus",
  "ui.studio": "Studio",
  "ui.studio_closed": "Studio uždaryta",
  "ui.studio_media_action_failed": "Studio medijos veiksmas nepavyko",
  "ui.studio_mix_started": "Studio miksas pradėtas",
  "ui.studio_opened": "Studio atidaryta",
  "ui.studio_pro": "studio pro",
  "ui.studio_pro_2": "Studio Pro",
  "ui.studio_scene": "Studio scena",
  "ui.studio_scene_deleted": "Studio scena ištrinta",
  "ui.studio_scene_saved": "Studio scena išsaugota",
  "ui.studio_scene_was_not_found": "Studio scena nerasta",
  "ui.studio_search": "studio paieška",
  "ui.studio_shortcut": "Studio nuoroda",
  "ui.subtle": "Subtilus",
  "ui.sun": "Sek",
  "ui.surprise_me": "Nustebink mane",
  "ui.sync": "Sinchronizuoti",
  "ui.sync_lyrics": "Sinchronizuoti žodžius",
  "ui.tablet": "Planšetė",
  "ui.tap_to_start_a_sleep_timer": "Bakstelėkite laikmačiui pradėti",
  "ui.target_players": "Tikslo grotuvai",
  "ui.target_queue": "Tikslo eilė",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "Tekstiniai pranešimai naudoja Home Assistant TTS, nukreiptą į Music Assistant grotuvus.",
  "ui.text_only": "Tik tekstas",
  "ui.text_tone": "Teksto tonas",
  "ui.theme": "Tema",
  "ui.theme_2": "Tema",
  "ui.theme_mode": "Temos režimas",
  "ui.theme_toggle": "Temos perjungiklis",
  "ui.theme_toggle_2": "Temos perjungiklis",
  "ui.this_device": "Šis įrenginys",
  "ui.this_device_2": "Šis įrenginys",
  "ui.this_device_player_connected": "Šio įrenginio grotuvas prijungtas",
  "ui.this_device_player_disconnected": "Šio įrenginio grotuvas atjungtas",
  "ui.thu": "Ket",
  "ui.timer": "Laikmatis",
  "ui.timer_active_remaining": "Laikmatis aktyvus: {remaining}",
  "ui.timers": "Laikmačiai",
  "ui.to": "Iki",
  "ui.today": "Šiandien",
  "ui.track": "Kūrinys",
  "ui.track_lyrics": "Kūrinio žodžiai",
  "ui.tracks": "Kūriniai",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "Perkelkite, klonuokite arba išvalykite eiles nepaslėpdami Studio.",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "Perkėlimas nepasiekiamas, kol konfigūruotas prisegtas grotuvas",
  "ui.transfer_queue": "Perkelti eilę",
  "ui.transfer_queue_2": "Perkelti eilę",
  "ui.transfer_queue_3": "Perkelti eilę",
  "ui.try_again": "Bandykite dar kartą",
  "ui.tts_entity": "TTS objektas",
  "ui.tts_entity_used_by_the_announcement_screen": "TTS objektas pranešimų ekranui.",
  "ui.tts_language": "TTS kalba",
  "ui.tue": "Ant",
  "ui.turkey": "Turkija",
  "ui.turn_off": "Išjungti",
  "ui.turns_off_after_run": "Išsijungia po paleidimo",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "Išjungia sunkius vaizdus sklandesniam veikimui Nest Hub ir silpnesnėse planšetėse.",
  "ui.type_a_free_style_first": "Pirmiausia įveskite laisvą stilių",
  "ui.type_an_announcement": "Įveskite pranešimą...",
  "ui.type_anything": "Įveskite bet ką",
  "ui.type_what_should_be_announced": "Įveskite, kas turėtų būti pranešta...",
  "ui.ungroup": "Išgrupuoti",
  "ui.ungroup_2": "Išgrupuoti",
  "ui.united_kingdom": "Jungtinė Karalystė",
  "ui.united_states": "Jungtinės Valstijos",
  "ui.unknown": "Nežinoma",
  "ui.up": "Aukštyn",
  "ui.up_next": "Kitas",
  "ui.up_next_2": "Kitas",
  "ui.up_next_title": "Kitas: {title}",
  "ui.upbeat_and_moving": "Ritmingas ir judrus",
  "ui.use_any_home_assistant_path_example_lovelace_home": "Naudokite bet kurį Home Assistant kelią. Pvz.: /lovelace/home",
  "ui.use_current_view_for_home_button": "Naudoti esamą vaizdą pradžios mygtukui",
  "ui.use_expand_to_open_the_full_player": "Naudoti išplėtimą atidarant pilną grotuvą",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "Naudoja tą patį šalių sąrašą kaip ir kortelės nustatymuose.",
  "ui.visible_tiles": "matomos plytelės",
  "ui.visible_tiles_2": "Matomos plytelės",
  "ui.visual_editor": "Vizualus redaktorius",
  "ui.assist_agent": "Assist agentas",
  "ui.assist_agent_id": "Assist agento ID",
  "ui.assist_only": "Tik Assist",
  "ui.default_assist_agent": "Numatytasis Home Assistant agentas",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "Rekomenduojama: muzikos komandos vykdomos lokaliai. Kitos komandos siunčiamos į Assist.",
  "ui.hybrid_music_plus_assist": "Hibridinis: Muzika + Assist",
  "ui.icon_size": "Piktogramos dydis",
  "ui.music_only": "Tik muzika",
  "ui.music_plus_assist": "Muzika + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "Neprivalomas Assist agento ID. Palikite tuščią numatytajam Home Assistant agentui.",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "Neprivaloma. Palikite numatytąjį agentą.",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "Rodyti „spausk ir kalbėk“ mygtuką muzikos komandoms ir Assist.",
  "ui.speak_voice_assistant_responses_out_loud": "Skaityti atpažintus atsakymus garsiai per šią naršyklę.",
  "ui.flow_assistant": "FLOW ASISTENTAS",
  "ui.voice_assistant": "Balso asistentas",
  "ui.voice_assistant_disabled": "Balso asistentas išjungtas",
  "ui.voice_assistant_listening": "Klausomasi komandos...",
  "ui.voice_assistant_mode": "Balso asistento režimas",
  "ui.voice_command_executed": "Balso komanda įvykdyta",
  "ui.voice_command_completed_action": "Atlikta: {action}",
  "ui.voice_command_failed": "Balso komanda nepavyko",
  "ui.voice_command_no_player": "Pasirinkite grotuvą arba pasakykite jo pavadinimą.",
  "ui.voice_command_not_understood": "Nesulaukiau jokios komandos.",
  "ui.voice_command_sent": "Balso komanda išsiųsta",
  "ui.voice_done_status": "Atlikta",
  "ui.voice_error_status": "Reikia dėmesio",
  "ui.voice_feedback": "Balso atgalinis ryšys",
  "ui.voice_group_connect_needs_players": "Pasakykite bent dviejų grotuvų pavadinimus.",
  "ui.voice_group_connected_players": "Sujungta {primary} su {members}",
  "ui.voice_group_disconnect_needs_player": "Pasakykite, kurią grupę atjungti.",
  "ui.voice_group_disconnected_player": "Atjungta grupė grotuvui {player}",
  "ui.voice_input_failed": "Balso įvestis nepavyko",
  "ui.voice_input_is_not_supported_on_this_device": "Balso įvestis nepalaikoma šiame įrenginyje",
  "ui.voice_language_is_not_supported": "Ši naršyklė nepalaiko pasirinktos kalbos atpažinimo.",
  "ui.voice_match_not_confident": 'Radau {title}, bet tai neatitinka "{query}". Bandykite dar kartą.',
  "ui.voice_music_searching": "Ieškoma muzikos: {query}",
  "ui.voice_playing_result": "Groja {title}",
  "ui.voice_queue_transfer_needs_players": "Pasakykite šaltinio ir tikslo grotuvus.",
  "ui.voice_queue_transferred_between": "Eilė pervesta iš {source} į {target}",
  "ui.voice_starting_playback": "Pradedama {title}...",
  "ui.voice_processing_status": "Apdorojama komanda...",
  "ui.voice_ready_status": "Paruošta",
  "ui.voice_recognition_network_failed": "Kalbos atpažinimo paslauga nepasiekiama. Patikrinkite tinklą.",
  "ui.voice_response": "Atsakymas",
  "ui.voice_response_will_appear_here": "Atsakymas atsiras čia.",
  "ui.voice_search": "Paieška balsu",
  "ui.voice_search_failed": "Paieška balsu nepavyko",
  "ui.voice_search_is_not_supported_on_this_device": "Paieška balsu šiame įrenginyje nepalaikoma",
  "ui.voice_transcript": "Tekstas",
  "ui.voice_listening_status": "Klausomasi...",
  "ui.waiting_for_speech": "Laukiama kalbos...",
  "ui.microphone_permission_or_browser_blocked": "Naršyklė užblokavo kalbos atpažinimą. Patikrinkite mikrofono leidimus.",
  "ui.volume": "Garsumas",
  "ui.volume_and_player_target": "Garsumas ir grotuvo tikslas",
  "ui.volume_boost": "Garso padidinimas",
  "ui.volume_control_large_screen_only": "Garso valdymas (tik dideliame ekrane)",
  "ui.volume_mode": "Garsumo režimas",
  "ui.volume_presets": "Garsumo šablonai",
  "ui.waiting_for_this_device_player": "Laukiama šio įrenginio grotuvo...",
  "ui.wake": "Pabusti",
  "ui.wake_schedules": "Žadinimo tvarkaraščiai",
  "ui.wed": "Tre",
  "ui.week": "Savaitė",
  "ui.what_would_you_like_to_listen_to": "Ko norėtumėte paklausyti?",
  "ui.will_play_next": "Gros po to",
  "ui.will_play_next_in_studio": "Gros po to (Studio)",
  "ui.worldwide_popular": "Populiaru pasaulyje",
  "ui.add_player_light_pair": "Pridėti porą",
  "ui.ambient_light": "Foninis apšvietimas",
  "ui.ambient_light_brightness": "Apšvietimo ryškumas",
  "ui.ambient_light_cooldown": "Apšvietimo atnaujinimo vėlinimas",
  "ui.ambient_light_entities": "Numatytieji spalvų žibintai",
  "ui.ambient_light_player_map": "Grotuvo -> šviesos susiejimas",
  "ui.ambient_light_player_map_helper": "Naudokite grotuvo ir šviesos parinkiklius. Išvalykite norėdami pašalinti porą.",
  "ui.ambient_light_transition": "Apšvietimo perėjimas",
  "ui.analog": "Analoginis",
  "ui.choose_only_the_lights_that_should_follow_the_music": "Pasirinkite šviesas, kurios seks muziką, kai nėra konkrečios grotuvo poros.",
  "ui.digital": "Skaitmeninis",
  "ui.discover_albums": "Atrasti albumus",
  "ui.discover_music": "Atrasti",
  "ui.discover_subtitle": "Rekomenduojami grojaraščiai, albumai ir radijas.",
  "ui.discovery": "Atradimas",
  "ui.discovery_failed": "Atradimo režimas nepavyko",
  "ui.discovery_mode": "Atradimo režimas",
  "ui.discovery_started": "Atradimas pradėtas: {mood}",
  "ui.maximum_brightness_for_music_lighting": "Maksimalus ryškumas muzikos apšvietimui.",
  "ui.minimum_seconds_between_light_updates": "Minimalus laikas sekundėmis tarp apšvietimo atnaujinimų.",
  "ui.player_light_pair_lights": "Spalvų žibintai {number}",
  "ui.player_light_pair_lights_helper": "Pasirinkite žibintus šiam Music Assistant grotuvui.",
  "ui.player_light_pair_player": "Music Assistant grotuvas {number}",
  "ui.player_light_pair_player_helper": "Pasirinkite Music Assistant grotuvą šiai šviesų porai.",
  "ui.all": "Visi",
  "ui.all_genres": "Visi žanrai",
  "ui.genres_and_styles": "Žanrai ir stiliai",
  "ui.compact_widget_mode": "Kompaktiško valdiklio stilius",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "Pasirinkite, kada kompaktiškas režimas naudoja mažesnį dviejų eilučių mobilųjį valdiklį.",
  "ui.full": "Pilnas",
  "ui.mini_widget": "Mini valdiklis",
  "ui.genre": "Žanras",
  "ui.mood_and_genre": "Nuotaika ir žanras",
  "ui.music_by_mood_and_genre": "Muzika pagal nuotaiką ir žanrą",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "Neprivalomas objektas maitinimo mygtukui. Palikite tuščią, kad sustabdytumėte aktyvų grotuvą.",
  "ui.power": "Maitinimas",
  "ui.power_action_failed": "Maitinimo veiksmas nepavyko",
  "ui.power_action_sent": "Maitinimo veiksmas išsiųstas",
  "ui.power_button": "Maitinimo mygtukas",
  "ui.power_button_action": "Maitinimo mygtuko veiksmas",
  "ui.power_button_entity": "Maitinimo mygtuko objektas",
  "ui.scene": "Scena",
  "ui.screensaver": "Ekrano užsklanda",
  "ui.screensaver_controls": "Ekrano užsklandos valdymas",
  "ui.screensaver_clock_mode": "Laikrodžio režimas",
  "ui.screensaver_clock_size": "Laikrodžio dydis",
  "ui.screensaver_clock_size_helper": "Keičia laikrodžio mastelį nekeičiant viršelio.",
  "ui.screensaver_clock_x": "Laikrodžio pozicija - KAIRĖ / DEŠINĖ",
  "ui.screensaver_clock_x_helper": "Mažesnės reikšmės perkelia į kairę, didesnės į dešinę.",
  "ui.screensaver_clock_y": "Laikrodžio pozicija - VIRŠUS / APAČIA",
  "ui.screensaver_clock_y_helper": "Mažesnės reikšmės perkelia į viršų, didesnės į apačią.",
  "ui.screensaver_message": "Ekrano užsklandos pranešimas",
  "ui.screensaver_timeout": "Ekrano užsklandos laukimo laikas",
  "ui.script": "Skriptas",
  "ui.short_optional_message_shown_on_the_screensaver": "Trumpas papildomas pranešimas ekrano užsklandoje.",
  "ui.show_previous_next_controls_on_the_screensaver": "Rodyti ankstesnio ir kito mygtukus ekrano užsklandoje.",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "Rodyti ramų laikrodį ir viršelį po ramybės laiko.",
  "ui.show_power_button_in_the_player_controls": "Rodyti maitinimo mygtuką grotuvo valdyme.",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "Rodyti pilno ekrano atradimo režimą veiksmuose.",
  "ui.smart_home": "Išmanieji namai",
  "ui.soft_transition_time_for_music_lighting": "Minkštas perėjimo laikas muzikos apšvietimui.",
  "ui.stop_player": "Sustabdyti grotuvą",
  "ui.sync_selected_lights_to_the_current_artwork_color": "Sinchronizuoti pasirinktas šviesas su viršelio spalva.",
  "ui.toggle": "Perjungti",
  "ui.turn_on": "Įjungti",
  "ui.choose_a_category_to_browse_provider_playlists": "Pasirinkite kategoriją tiekėjo grojaraščiams naršyti.",
  "ui.new_and_recommended": "Nauja ir rekomenduojama",
  "ui.provider_playlists": "Tiekėjo grojaraščiai",
  "ui.alphabetical": "Alphabetical",
  "ui.auxiliary_button": "Auxiliary button",
  "ui.auxiliary_button_action": "Auxiliary button action",
  "ui.auxiliary_button_entity": "Auxiliary button entity",
  "ui.auxiliary_button_icon": "Auxiliary button icon",
  "ui.auxiliary_button_name": "Auxiliary button name",
  "ui.auxiliary_buttons": "Auxiliary buttons",
  "ui.excluded_players": "Excluded players",
  "ui.music_assistant_required": "Music Assistant required",
  "ui.player_order": "Player order",
  "ui.player_sort": "Player sort",
  "ui.set_the_visual_order_of_quick_actions": "Set the visual order. Only selected Quick Actions are shown; unselected items are ignored.",
  "ui.move_down": "Move down",
  "ui.move_up": "Move up",
  "ui.default_order": "Default order",
  "ui.show_home_inside_quick_actions": "Shows Home as a Quick Action instead of a floating button.",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "Show plus and minus buttons next to the volume slider.",
  "ui.volume_down": "Volume down",
  "ui.volume_step_between_1_and_10_percent_default_5": "Volume step between 1% and 10%. Default is 5%.",
  "ui.volume_step_buttons": "Volume +/- buttons",
  "ui.volume_step_percent": "Volume step",
  "ui.volume_up": "Volume up",
  "ui.optional_name_for_the_auxiliary_button": "Optional label shown as the auxiliary button tooltip.",
  "ui.choose_the_auxiliary_button_icon": "Choose the icon shown for the auxiliary button.",
  "ui.choose_how_players_are_sorted": "Choose default, alphabetical, or custom player order.",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "Choose Music Assistant players that should never appear in the card.",
  "ui.custom_order": "Custom order",
  "ui.no_visible_music_assistant_players_after_exclusions": "All Music Assistant players are hidden by the excluded players setting.",
  "ui.set_custom_player_order": "Set the custom player order. Empty slots are ignored.",
  "ui.show_auxiliary_button_in_quick_actions": "Shows this auxiliary button in the Quick Actions row.",
  "ui.screensaver_buttons": "Screensaver buttons",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Choose exactly which buttons appear on the screensaver, including FLOW ASSISTANT, previous and next.",
  "ui.hotel_mode": "Viešbučio režimas",
  "ui.hotel_mode_helper": "Minimali viešbučiui saugi sąsaja: tik grotuvo valdymas, garsumas, paieška, viršelių naršymas ir grotuvo pasirinkimas.",
  "ui.style_and_genres": "Stiliai ir žanrai"
});
const messages = Object.freeze({
  "ui.a_guided_music_wizard": "引导式音乐向导",
  "ui.a_radio_station": "一个电台",
  "ui.accent_color": "强调色",
  "ui.actions": "操作",
  "ui.actions_2": "操作",
  "ui.active_days": "生效日期",
  "ui.active_player_helper": "活动播放器辅助实体",
  "ui.active_player_helper_must_be_an_input_text_entity": "活动播放器辅助实体必须是 input_text",
  "ui.active_players": "活动播放器",
  "ui.add": "添加",
  "ui.add_to_queue": "加入队列",
  "ui.add_to_selection": "加入选择",
  "ui.added_to_liked": "已加入喜欢",
  "ui.added_to_music_assistant_liked": "已加入 Music Assistant 喜欢列表",
  "ui.added_to_queue": "已加入队列",
  "ui.added_to_studio_queue": "已加入音乐中枢队列",
  "ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti": "为背景层添加动效。柔和模式较平静，强模式更明显，极强模式最有动感。",
  "ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p": "仅在播报期间提高当前音量，播报结束后恢复原有音量。",
  "ui.after_run": "执行后",
  "ui.album": "专辑",
  "ui.albums": "专辑",
  "ui.all_albums": "全部专辑",
  "ui.all_artists": "全部艺人",
  "ui.all_countries": "所有国家",
  "ui.all_player_groups_disconnected": "所有播放器分组已断开",
  "ui.all_players": "所有播放器",
  "ui.all_players_2": "所有播放器",
  "ui.all_podcasts": "全部播客",
  "ui.all_tracks": "全部歌曲",
  "ui.allow_local_likes": "允许本地喜欢",
  "ui.always_visible": "始终显示",
  "ui.announce": "播报",
  "ui.announce_to_all_players": "向所有播放器播报",
  "ui.announcement": "播报",
  "ui.announcement_failed_with_error": "播报失败{error}",
  "ui.announcement_language": "播报语言",
  "ui.announcement_presets": "播报预设",
  "ui.announcement_sent_to_player": "已向 {player} 发送播报",
  "ui.announcement_studio": "播报中心",
  "ui.announcement_studio_2": "播报中心",
  "ui.announcement_target": "播报目标",
  "ui.announcement_to_player_preview": "播报到 {player}：{preview}",
  "ui.announcement_volume_boost": "播报音量提升",
  "ui.announcements": "播报",
  "ui.appearance": "外观",
  "ui.alphabetical": "按字母排序",
  "ui.auxiliary_button": "辅助按钮",
  "ui.auxiliary_button_action": "辅助按钮动作",
  "ui.auxiliary_button_entity": "辅助按钮实体",
  "ui.auxiliary_button_icon": "辅助按钮图标",
  "ui.auxiliary_button_name": "辅助按钮名称",
  "ui.auxiliary_buttons": "辅助按钮",
  "ui.apply_group": "应用分组",
  "ui.apply_schedule": "应用计划",
  "ui.artist": "艺人",
  "ui.artist_radio": "艺人电台",
  "ui.artists": "艺人",
  "ui.artwork": "封面",
  "ui.artwork_swipe": "封面滑动",
  "ui.ascending": "升序",
  "ui.at_least_one_player_must_stay_selected": "至少保留一个已选播放器",
  "ui.australia": "澳大利亚",
  "ui.auto_chooses_mobile_or_tablet_based_on_actual_width": "自动模式会根据实际宽度选择手机或平板布局。",
  "ui.auto_cloud_default": "自动 / 云端默认",
  "ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl": "自动模式会从当前封面提取颜色并保持较柔和的效果；强模式会让配色更饱满、更明亮。",
  "ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices": "自动模式会保留 Home Assistant Cloud 的默认语音设置；手动选择时会显式发送语言。",
  "ui.available": "可用",
  "ui.back": "返回",
  "ui.back_2": "返回",
  "ui.back_to_countries": "返回国家列表",
  "ui.back_to_queue": "返回队列",
  "ui.background_motion": "背景动效",
  "ui.behavior": "行为",
  "ui.browse_covers": "浏览封面",
  "ui.browse_playlists_artists_albums_and_radio": "浏览播放列表、艺人、专辑和电台",
  "ui.browser_players": "浏览器播放器",
  "ui.build_custom_mix": "生成自定义混音",
  "ui.building_studio_mix": "正在生成音乐中枢混音...",
  "ui.button": "按钮",
  "ui.by_mood": "按心情",
  "ui.cache_duration_in_milliseconds_for_selected_data_requests": "选定数据请求的缓存时长（毫秒）。",
  "ui.cache_ttl": "缓存 TTL",
  "ui.calm": "舒缓",
  "ui.canada": "加拿大",
  "ui.cancel": "取消",
  "ui.cancel_2": "取消",
  "ui.cancel_selection": "取消选择",
  "ui.card_configuration": "卡片配置",
  "ui.card_height": "卡片高度",
  "ui.card_height_in_pixels": "卡片高度（像素）。",
  "ui.card_settings_are_managed_from_the_visual_editor": "卡片设置由可视化编辑器管理",
  "ui.change_song": "切换歌曲",
  "ui.change_track": "切换歌曲",
  "ui.chill_mix": "轻松混音",
  "ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad": "选择一个国家，或选择“所有国家”以在电台标签中浏览全部国家。",
  "ui.choose_a_queue_position": "选择队列位置",
  "ui.choose_a_source_player": "选择源播放器",
  "ui.choose_a_target_player": "选择目标播放器",
  "ui.choose_at_least_one_player": "请至少选择一个播放器",
  "ui.choose_icons_text_or_both_for_the_footer": "选择底栏显示图标、文字或两者。",
  "ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only": "仅选择 Music Assistant 播放器。卡片内固定列表只显示 Music Assistant 播放器，以避免混淆。",
  "ui.choose_one_clear_option": "请选择一个明确选项",
  "ui.choose_player": "选择播放器",
  "ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl": "从快捷栏选择内容，或点击魔杖获取随机播放列表。",
  "ui.choose_source": "选择音源",
  "ui.choose_target": "选择目标",
  "ui.choose_target_player": "选择目标播放器",
  "ui.choose_the_accent_color_for_the_mobile_layout": "为移动布局选择强调色。",
  "ui.choose_the_music": "选择音乐",
  "ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first": "选择并排序歌曲标题下方显示的小按钮。",
  "ui.choose_the_source_first_playback_will_use_the_current_target": "请先选择音源，播放会发送到当前目标。",
  "ui.choose_where_to_play": "选择播放位置",
  "ui.choose_whether_artwork_swipe_changes_track_or_browses_covers": "选择封面滑动是切换歌曲还是浏览封面。",
  "ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c": "选择由卡片内界面控制设置，还是由仪表板中的卡片配置控制。",
  "ui.choose_which_actions_appear_in_the_main_bar": "选择主栏中显示哪些操作。",
  "ui.choose_which_days_the_auto_night_mode_window_applies_to": "选择自动夜间模式生效的星期。",
  "ui.choose_which_icons_appear_in_the_quick_action_row": "选择快捷操作栏中显示哪些图标。",
  "ui.choose_which_players_appear_as_tiles_in_the_room": "选择哪些播放器以卡片形式显示在房间中。",
  "ui.choose_which_players_stay_in_the_current_control_selection": "选择哪些播放器保留在当前控制范围内。",
  "ui.choose_which_tabs_are_available_in_the_library_screen": "选择资料库页面中可用的标签。",
  "ui.choose_who_is_controlled": "选择控制对象",
  "ui.choosing_visual_disables_the_in_card_settings_screen": "选择“可视化编辑器”后会禁用卡片内设置界面。",
  "ui.clean_the_studio_wall": "整理音乐中枢面板",
  "ui.clear": "清除",
  "ui.clear_queue": "清空队列",
  "ui.clear_search": "清除搜索",
  "ui.clone_queue": "克隆队列",
  "ui.close": "关闭",
  "ui.collapse_compact_player": "收起紧凑播放器",
  "ui.compact_mode": "紧凑模式",
  "ui.configure_ready_made_announcement_phrases": "配置常用播报短语。",
  "ui.connect_this_device": "连接本机",
  "ui.connect_this_device_2": "连接此设备",
  "ui.connected": "已连接",
  "ui.connected_2": "已连接",
  "ui.connected_players": "已连接播放器",
  "ui.connected_players_2": "已连接播放器",
  "ui.connecting": "连接中",
  "ui.connecting_2": "连接中...",
  "ui.confirm": "确认",
  "ui.connection_and_behavior": "连接与行为",
  "ui.connects_directly_from_this_card_using_sendspin": "通过 Sendspin 直接从此卡片连接",
  "ui.continue": "继续",
  "ui.continue_2": "继续",
  "ui.continue_from_recent_music_assistant_activity": "从 Music Assistant 最近活动中继续播放。",
  "ui.continue_listening": "继续收听",
  "ui.continue_what_was_played_recently": "继续最近播放过的内容",
  "ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti": "控制卡片背景是否轻微移动，以及动效强度。",
  "ui.controlling": "当前控制",
  "ui.could_not_build_quick_mix_from_the_current_song": "无法根据当前歌曲生成快速混音",
  "ui.could_not_build_studio_mix": "无法生成音乐中枢混音",
  "ui.could_not_choose_media": "无法选择媒体内容",
  "ui.could_not_clone_the_queue": "无法克隆队列",
  "ui.could_not_find_music": "无法找到音乐",
  "ui.could_not_play_label": "无法播放：{label}",
  "ui.could_not_start_chill_mix": "无法启动轻松混音",
  "ui.could_not_start_playback": "无法开始播放",
  "ui.could_not_start_playback_2": "无法开始播放",
  "ui.could_not_start_playback_in_studio": "无法在音乐中枢中开始播放",
  "ui.could_not_start_quick_mix": "无法启动快速混音",
  "ui.could_not_start_radio_playback": "无法开始电台播放",
  "ui.could_not_transfer_the_queue": "无法转移队列",
  "ui.could_not_update_active_player_helper": "无法更新活动播放器辅助实体",
  "ui.create_schedule": "创建计划",
  "ui.current_path": "当前路径",
  "ui.custom": "自定义",
  "ui.dark": "深色",
  "ui.delete_scene": "删除场景",
  "ui.delete_schedule": "删除计划",
  "ui.descending": "降序",
  "ui.dictate": "语音输入",
  "ui.disabled": "关闭",
  "ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend": "关闭模糊、动画、动态背景和重阴影。建议在 Nest Hub 或性能较弱的平板上开启。",
  "ui.disconnect": "断开连接",
  "ui.disconnect_all": "全部断开",
  "ui.disconnect_groups": "断开分组",
  "ui.disconnect_player_groups": "断开播放器分组",
  "ui.disconnect_this_device": "断开本机",
  "ui.disconnected": "已断开",
  "ui.disconnecting": "断开中",
  "ui.disconnecting_player_groups": "正在断开播放器分组...",
  "ui.discover": "发现",
  "ui.down": "下移",
  "ui.dynamic_theme": "动态主题",
  "ui.edit_schedule": "编辑计划",
  "ui.enable": "启用",
  "ui.enable_or_disable_the_footer_search_button": "启用或关闭底栏搜索按钮。",
  "ui.enable_or_disable_the_studio_button_in_the_footer_bar": "启用或关闭底栏中的音乐中枢按钮。",
  "ui.enable_the_floating_home_shortcut_button": "启用浮动主页快捷按钮。",
  "ui.enabled": "开启",
  "ui.enables_switching_to_local_liked_mode": "启用切换到本地喜欢模式。",
  "ui.end_time": "结束时间",
  "ui.excluded_players": "隐藏的播放器",
  "ui.energetic": "充满活力",
  "ui.energy_and_rhythm": "充满活力与节奏",
  "ui.enter_an_announcement_first": "请先输入播报内容",
  "ui.example_quiet_jazz_greek_music_workout": "例如：安静爵士、希腊音乐、运动",
  "ui.existing_content": "现有内容",
  "ui.expand_player": "展开播放器",
  "ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface": "从当前封面提取颜色并应用到界面中。",
  "ui.extreme": "极强",
  "ui.fallback_for_setups_that_need_an_alternate_queue_service_path": "用于需要备用队列服务路径的环境。",
  "ui.fallback_mode": "基础模式",
  "ui.family_friendly": "适合全家收听",
  "ui.favorite": "收藏",
  "ui.favorite_action_failed": "收藏操作失败",
  "ui.favorite_action_sent": "收藏操作已发送",
  "ui.favorite_button_entity": "收藏按钮实体",
  "ui.favorite_center": "收藏中心",
  "ui.favorite_center_2": "收藏中心",
  "ui.favorite_radio": "收藏电台",
  "ui.favorite_updated": "收藏已更新",
  "ui.favorites_and_liked_music": "收藏与喜欢的音乐",
  "ui.feature_detection_player_context_and_this_device_playback_tools": "功能检测、播放器上下文与本机播放工具。",
  "ui.find_music": "查找音乐",
  "ui.finding_music": "正在查找音乐",
  "ui.fixed": "固定",
  "ui.flow_mix": "FLOW 混音",
  "ui.font_scale": "字体缩放",
  "ui.font_size": "字体大小",
  "ui.footer_search": "底栏搜索",
  "ui.footer_style": "底栏样式",
  "ui.footer_style_2": "底栏样式",
  "ui.for_example_lovelace_home_or_any_other_home_assistant_path": "例如 /lovelace/home 或任何其他 Home Assistant 路径。",
  "ui.for_weaker_displays_turns_off_heavy_blur_animations_dynamic_theme_extrac": "适用于性能较弱的屏幕：关闭重模糊、动画、动态主题提取和背景动效。",
  "ui.france": "法国",
  "ui.free_style": "自由风格",
  "ui.free_style_quiet_jazz_greek_music_workout": "自由风格：安静爵士、希腊音乐、运动歌单……",
  "ui.fresh_start": "轻松开启一天",
  "ui.fresh_start_2": "轻松开启一天",
  "ui.fri": "周五",
  "ui.friendly_and_simple": "轻松简单",
  "ui.from": "从",
  "ui.general": "常规",
  "ui.general_and_display": "常规与显示",
  "ui.germany": "德国",
  "ui.global_scale_for_every_interface_font_1_is_the_default_size": "整个界面字体的全局缩放。1 为默认大小。",
  "ui.greece": "希腊",
  "ui.grid": "网格",
  "ui.group": "分组",
  "ui.group_cleared": "分组已清除",
  "ui.group_cleared_2": "分组已清除",
  "ui.group_speakers": "扬声器分组",
  "ui.group_speakers_2": "扬声器分组",
  "ui.group_updated": "分组已更新",
  "ui.group_volume": "分组音量",
  "ui.group_volume_up_energetic_mix": "分组、高音量、活力混音",
  "ui.grouped_players": "已分组播放器",
  "ui.hebrew": "希伯来语",
  "ui.history": "历史",
  "ui.history_tabs": "历史标签",
  "ui.home": "首页",
  "ui.home_party_night_presets": "家庭、派对、夜晚预设",
  "ui.home_path": "主页路径",
  "ui.home_scene_prepared": "家庭场景已准备好",
  "ui.home_shortcut": "主页快捷方式",
  "ui.home_shortcut_path": "主页快捷路径",
  "ui.icon_and_text": "图标和文字",
  "ui.icon_only": "仅图标",
  "ui.icon_plus_text": "图标 + 文字",
  "ui.idle": "空闲",
  "ui.idle_2": "空闲",
  "ui.in_card_ui": "卡片内界面",
  "ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen": "包含卡片内设置界面里的自定义主题模式。",
  "ui.info": "信息",
  "ui.israel": "以色列",
  "ui.israeli": "以色列",
  "ui.italy": "意大利",
  "ui.items": "项",
  "ui.join_selected_players": "将所选播放器加入分组",
  "ui.kids": "儿童",
  "ui.language": "语言",
  "ui.larger_lyrics": "放大歌词",
  "ui.layout_adapted_to_height": "布局已按高度调整",
  "ui.layout_mode": "布局模式",
  "ui.leave_empty_if_the_card_should_use_home_assistant_only": "如果卡片只通过 Home Assistant 访问，请留空。",
  "ui.library": "资料库",
  "ui.library_2": "资料库",
  "ui.library_pages": "资料库页面",
  "ui.default_library_layout": "默认资料库布局",
  "ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually": "选择资料库页面打开时的布局。你仍然可以在资料库内手动切换网格/列表。",
  "ui.library_radio": "资料库电台",
  "ui.library_surprise": "资料库惊喜",
  "ui.library_tabs": "资料库标签",
  "ui.library_tabs_2": "资料库标签",
  "ui.light": "浅色",
  "ui.like": "喜欢",
  "ui.like_2": "喜欢",
  "ui.like_3": "喜欢",
  "ui.liked": "喜欢",
  "ui.liked_mode": "喜欢模式",
  "ui.liked_sync": "喜欢同步",
  "ui.list": "列表",
  "ui.listening": "正在聆听...",
  "ui.loading": "加载中...",
  "ui.loading_favorites": "正在加载收藏...",
  "ui.loading_library": "正在加载资料库...",
  "ui.loading_lyrics": "正在加载歌词...",
  "ui.loading_players": "正在加载播放器...",
  "ui.loading_queue": "正在加载队列...",
  "ui.loading_recent_items": "正在加载最近内容...",
  "ui.loading_schedules": "正在加载计划...",
  "ui.local": "本地",
  "ui.local_favorites": "本地热门",
  "ui.local_favorites_2": "本地热门",
  "ui.local_mode_is_only_meaningful_when_local_likes_are_enabled": "只有启用本地喜欢时，本地模式才有意义。",
  "ui.low_light_listening": "适合夜晚静听",
  "ui.low_volume_and_quiet_mix": "低音量、安静混音",
  "ui.lower_volume_mood": "低音量夜间氛围",
  "ui.lyrics": "歌词",
  "ui.lyrics_earlier": "歌词提前",
  "ui.lyrics_font_size": "歌词字号",
  "ui.lyrics_later": "歌词延后",
  "ui.lyrics_timing": "歌词时序",
  "ui.lyrics_unavailable_right_now": "当前无法获取歌词",
  "ui.ma_favorite_action_failed_with_error": "Music Assistant 收藏操作失败{error}",
  "ui.ma_interface_path": "MA 界面路径",
  "ui.main_bar": "主栏",
  "ui.main_bar_items": "主栏项目",
  "ui.main_bar_items_2": "主栏项目",
  "ui.main_opacity": "主界面透明度",
  "ui.mainly_relevant_on_larger_layouts": "主要用于较大布局。",
  "ui.mass_queue_send_command_fallback": "mass_queue/send_command 回退",
  "ui.matches_the_microphone_setting_from_the_in_card_settings_screen": "与卡片内设置界面的麦克风设置保持一致。",
  "ui.media": "媒体",
  "ui.media_2": "媒体",
  "ui.media_actions": "媒体操作",
  "ui.media_layout": "媒体布局",
  "ui.media_selected_target": "已选：{media} · 目标：{target}",
  "ui.menu": "菜单",
  "ui.message": "消息",
  "ui.microphone": "麦克风",
  "ui.microphone_2": "麦克风",
  "ui.microphone_is_disabled": "麦克风已禁用",
  "ui.missing_url": "缺少 URL",
  "ui.mobile": "手机",
  "ui.mon": "周一",
  "ui.mood_style_or_free_text": "心情、风格或自由输入",
  "ui.more_info": "更多信息",
  "ui.more_info_is_unavailable_for_this_player": "此播放器没有更多信息",
  "ui.morning": "清晨",
  "ui.morning_mix": "晨间混音",
  "ui.move_to_next": "移到下一首",
  "ui.move_to_position": "移动到指定位置",
  "ui.music": "音乐",
  "ui.music_assistant_required": "需要 Music Assistant",
  "ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th": "如果你希望通过 Home Assistant 直接关联 Music Assistant，可填写其配置条目 ID。",
  "ui.music_assistant_config_entry_was_not_found": "未找到 Music Assistant 配置条目",
  "ui.music_assistant_token": "Music Assistant 令牌",
  "ui.music_assistant_url": "Music Assistant 地址",
  "ui.music_hub": "音乐中心",
  "ui.music_hub_2": "音乐中心",
  "ui.mute": "静音",
  "ui.mute_command_failed": "静音命令失败",
  "ui.my_studio_scene": "我的音乐中枢场景",
  "ui.name_or_keyword": "名称或关键词",
  "ui.name_this_studio_scene": "为这个音乐中枢场景命名...",
  "ui.netherlands": "荷兰",
  "ui.new_schedule": "新建计划",
  "ui.new_wake_schedule": "新建唤醒计划",
  "ui.newest": "最新",
  "ui.next": "下一首",
  "ui.next_track_failed": "切换下一首失败",
  "ui.night": "夜晚",
  "ui.night_end_time": "夜间结束时间",
  "ui.night_mode": "夜间模式",
  "ui.night_mode_auto_window": "夜间模式自动 {start}-{end}",
  "ui.night_mode_days": "夜间模式日期",
  "ui.night_mode_is_always_on": "夜间模式始终开启",
  "ui.night_mode_is_off": "夜间模式已关闭",
  "ui.night_mode_is_off_until_you_choose_another_mode": "夜间模式将保持关闭，直到你选择其他模式。",
  "ui.night_mode_stays_on_until_you_choose_another_mode": "夜间模式将保持开启，直到你选择其他模式。",
  "ui.night_schedule_updated": "夜间计划已更新",
  "ui.night_start_time": "夜间开始时间",
  "ui.no_active_media": "当前没有活动媒体",
  "ui.no_active_players": "没有活动播放器",
  "ui.no_available_players": "没有可用播放器",
  "ui.no_content_found": "未找到内容",
  "ui.no_dynamic_player_groups_to_disconnect": "没有可断开的动态播放器分组",
  "ui.no_extra_ma_players": "没有额外的 MA 播放器",
  "ui.no_extra_ma_players_2": "没有额外的 MA 播放器",
  "ui.no_favorites_found": "未找到收藏内容。",
  "ui.no_liked_media_yet": "还没有喜欢的媒体内容",
  "ui.no_liked_tracks_to_play": "没有可播放的喜欢歌曲",
  "ui.no_light_entities_found": "未找到灯光实体。",
  "ui.no_lyrics_found": "未找到歌词",
  "ui.no_matching_content_was_found": "未找到匹配内容",
  "ui.no_media_found": "未找到媒体内容。",
  "ui.no_media_found_for_this_search": "这次搜索没有找到媒体内容。",
  "ui.no_mix_content_found": "未找到可用于混音的内容",
  "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl": "需要 Music Assistant。请在 Home Assistant 中启用 Music Assistant 集成，并至少公开一个 Music Assistant media_player。其他 Home Assistant 媒体播放器会被隐藏。",
  "ui.no_music_assistant_players_were_found_for_pinning": "No Music Assistant players were found for pinning.",
  "ui.no_next_track_in_queue": "队列中没有下一首",
  "ui.no_player_groups_to_disconnect": "没有可断开的播放器分组",
  "ui.no_player_light_pairs_yet": "还没有播放器灯光配对。",
  "ui.no_player_selected": "未选择播放器",
  "ui.no_players_found": "未找到播放器",
  "ui.no_players_found_yet": "尚未发现播放器",
  "ui.no_playlists_found": "未找到播放列表",
  "ui.no_queue": "没有队列",
  "ui.no_queue_item_to_rebuild": "没有可重建的队列项",
  "ui.no_queue_to_clone": "没有可克隆的队列",
  "ui.no_radio_browser_countries_found": "未找到 Radio Browser 国家列表",
  "ui.no_radio_stations_found": "未找到电台",
  "ui.no_recent_listening_yet": "暂时还没有最近收听记录。",
  "ui.no_results": "没有结果",
  "ui.no_results_found": "未找到结果",
  "ui.no_saved_studio_scenes_yet": "还没有保存的音乐中枢场景。",
  "ui.no_scheduled_start_is_active": "当前没有启用定时启动",
  "ui.no_selected_tracks_to_play": "没有选中的可播放歌曲",
  "ui.no_sleep_timer_is_active": "当前没有启用睡眠定时器",
  "ui.no_speech_was_captured": "未捕获到语音",
  "ui.no_target_players_available": "没有可用的目标播放器",
  "ui.no_target_players_available_2": "没有可用的目标播放器",
  "ui.no_tts_service_or_entity_is_configured": "未配置 TTS 服务或实体",
  "ui.no_wake_schedules_yet": "还没有唤醒计划",
  "ui.none": "无",
  "ui.nothing_is_playing_right_now": "当前没有播放内容",
  "ui.nothing_playing": "当前没有播放内容",
  "ui.now_playing": "正在播放",
  "ui.now_playing_2": "正在播放",
  "ui.off": "关闭",
  "ui.off_2": "关闭",
  "ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time": "关闭表示禁用，开启表示始终启用，自动表示按设定时间段生效。",
  "ui.oldest": "最早",
  "ui.on": "开启",
  "ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre": "一键准备播放器、分组、音量和内容，也可以把当前音乐中枢目标保存为本地场景。",
  "ui.only_actions_for_the_current_target_are_shown_here": "这里只显示当前目标可用的操作。",
  "ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p": "Only Music Assistant players are shown here. Browser and local Sendspin players are hidden to avoid confusion.",
  "ui.only_needed_when_using_a_direct_music_assistant_url": "仅在使用直连 Music Assistant 地址时需要。",
  "ui.opacity_for_popups_and_overlays": "弹窗和覆盖层的透明度。",
  "ui.opacity_for_the_main_card_background": "卡片主背景透明度。",
  "ui.open_full_interface": "打开完整界面",
  "ui.open_full_queue": "打开完整队列",
  "ui.open_interface_in": "界面打开方式",
  "ui.open_ma": "打开 MA",
  "ui.open_music_assistant": "打开 Music Assistant",
  "ui.open_music_assistant_2": "打开 Music Assistant？",
  "ui.open_music_assistant_on_this_device_to_activate_the_browser_player": "请在本机打开 Music Assistant 以启用浏览器播放器",
  "ui.open_player_picker": "打开播放器选择器",
  "ui.open_queue": "打开队列",
  "ui.open_saved_songs": "打开已保存歌曲",
  "ui.open_the_full_music_assistant_interface": "要打开完整的 Music Assistant 界面吗？",
  "ui.open_the_full_music_assistant_interface_2": "打开完整 Music Assistant 界面",
  "ui.opening_studio_library": "正在打开中枢资料库",
  "ui.opening_timers": "正在打开定时器",
  "ui.option": "选项",
  "ui.optional": "可选",
  "ui.optional_2": "可选",
  "ui.optional_entity_used_for_an_external_favorite_button": "用于外部收藏按钮的可选实体。",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for": "可选的 input_text 辅助实体。卡片会写入当前活动播放器的 entity_id，便于自动化和模板使用。",
  "ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2": "可选的 input_text 辅助实体。卡片会写入当前活动播放器的 entity_id，便于自动化和模板使用。",
  "ui.other": "其他",
  "ui.other_players": "其他播放器",
  "ui.party": "派对",
  "ui.path_used_when_opening_the_music_assistant_interface": "打开 Music Assistant 界面时使用的路径。",
  "ui.pause": "暂停",
  "ui.paused": "已暂停",
  "ui.per_device_in_card": "按设备 / 卡片内",
  "ui.performance_mode": "性能模式",
  "ui.performance_mode_for_weak_devices": "弱设备性能模式",
  "ui.performance_profile": "性能配置",
  "ui.performance_profile_helper": "选择卡片允许使用的视觉负载。Ultra Lite 会为性能较弱的设备降低透明度、模糊、动态封面背景和动效。",
  "ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an": "选择一种心情，或输入你自己的风格。音乐中枢会搜索 Music Assistant 并生成队列。",
  "ui.pinned": "已固定",
  "ui.pinned_players": "固定播放器",
  "ui.play": "播放",
  "ui.play_2": "播放",
  "ui.play_a_few_tracks_and_they_will_appear_here": "先播放几首歌，推荐内容就会出现在这里。",
  "ui.play_a_saved_list": "播放已保存列表",
  "ui.play_all": "全部播放",
  "ui.play_an_artist": "播放艺人作品",
  "ui.play_artwork_action": "封面操作",
  "ui.play_everywhere": "全屋播放",
  "ui.play_next": "下一首播放",
  "ui.play_next_and_clear_queue": "下一首播放并清空队列",
  "ui.play_now": "立即播放",
  "ui.play_now_and_clear_queue": "立即播放并清空队列",
  "ui.play_pause": "播放 / 暂停",
  "ui.play_queue_radio_or_remove_favorites_directly_from_studio": "直接在音乐中枢中播放、加入队列、开启电台或移除收藏。",
  "ui.play_selected": "播放所选",
  "ui.playback_command_failed": "播放命令失败",
  "ui.playback_command_failed_2": "播放命令失败",
  "ui.playback_started": "已开始播放",
  "ui.player": "播放器",
  "ui.player_2": "播放器",
  "ui.player_count_many": "{count} 个播放器",
  "ui.player_count_one": "1 个播放器",
  "ui.player_groups_could_not_be_disconnected": "无法断开播放器分组",
  "ui.player_is_pinned_from_settings": "该播放器已在设置中固定",
  "ui.player_is_ready": "播放器已就绪",
  "ui.player_is_ready_nothing_is_playing_right_now": "播放器已就绪，但当前没有播放内容",
  "ui.player_order": "播放器顺序",
  "ui.player_on_this_device": "本设备上的播放器",
  "ui.player_selected": "播放器已选中",
  "ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp": "在 Music Assistant 为此播放器提供可靠 schema 之前，播放器设置和 DSP 预设将保持只读。",
  "ui.player_sort": "播放器排序",
  "ui.players": "播放器",
  "ui.players_queue_and_groups": "播放器、队列与分组",
  "ui.playing": "播放中",
  "ui.playing_a_random_playlist": "正在播放随机播放列表",
  "ui.playing_a_random_radio_station": "正在播放随机电台",
  "ui.playing_on": "当前播放设备",
  "ui.playing_on_2": "当前播放设备",
  "ui.playlist": "播放列表",
  "ui.playlists": "播放列表",
  "ui.podcast": "播客",
  "ui.podcasts": "播客",
  "ui.popup_opacity": "弹窗透明度",
  "ui.preset_message": "预设消息",
  "ui.previous": "上一首",
  "ui.primary": "主",
  "ui.primary_target": "主要目标",
  "ui.pro_tools": "专业工具",
  "ui.queue": "播放队列",
  "ui.queue_2": "播放队列",
  "ui.queue_action_failed": "队列操作失败",
  "ui.queue_action_is_not_available_for_this_player": "该播放器不支持此队列操作",
  "ui.queue_actions": "队列操作",
  "ui.queue_cleared": "队列已清空",
  "ui.queue_cleared_and_item_moved_next": "队列已清空，项目已移到下一首",
  "ui.queue_cloned": "队列已克隆",
  "ui.queue_cockpit": "队列控制台",
  "ui.queue_cockpit_2": "队列控制台",
  "ui.queue_is_empty": "播放队列为空",
  "ui.queue_is_unavailable_for_this_player": "该播放器暂不支持查看队列",
  "ui.queue_item": "队列项目",
  "ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again": "队列项目 ID 尚未准备好，请刷新队列后重试。",
  "ui.queue_results": "队列结果",
  "ui.queue_transferred": "队列已转移",
  "ui.queues_refreshed": "队列已刷新",
  "ui.quick_actions": "快捷操作",
  "ui.quick_actions_2": "快捷操作",
  "ui.quick_mix": "快速混音",
  "ui.quick_mix_started": "快速混音已开始",
  "ui.quick_play": "快速播放",
  "ui.radio": "电台",
  "ui.radio_browser": "Radio Browser",
  "ui.radio_browser_countries": "Radio Browser 国家",
  "ui.radio_browser_country": "Radio Browser 国家",
  "ui.radio_browser_country_2": "Radio Browser 国家",
  "ui.radio_mode_is_not_available_for_this_media_type": "此媒体类型不支持电台模式",
  "ui.radio_mode_started": "电台模式已启动",
  "ui.radio_station_changed": "电台已切换",
  "ui.radio_stations": "电台频道",
  "ui.random": "随机",
  "ui.random_gentle_morning_mix": "随机轻柔晨间混音",
  "ui.random_playlist": "随机播放列表",
  "ui.random_radio": "随机电台",
  "ui.ready": "就绪",
  "ui.ready_to_play": "准备播放",
  "ui.recent": "最近",
  "ui.recent_continue": "最近 / 继续",
  "ui.recent_listening": "最近收听",
  "ui.recent_track": "最近播放歌曲",
  "ui.recently_added": "最近新增",
  "ui.recently_played": "最近播放",
  "ui.recently_played_2": "最近播放",
  "ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l": "当 HOMEii Flow 检测到你的队列或最近收听记录后，这里会显示推荐内容。",
  "ui.recommended": "推荐",
  "ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported": "推荐格式：HH:MM，例如 06:00，支持跨午夜。",
  "ui.recommended_format_hh_mm_such_as_22_00": "推荐格式：HH:MM，例如 22:00。",
  "ui.recommended_playlist": "推荐播放列表",
  "ui.recommended_track": "推荐歌曲",
  "ui.reconnects_when_you_return_to_this_dashboard": "回到此仪表板后会自动重连",
  "ui.refresh": "刷新",
  "ui.remember_as_this_device": "记住为本机",
  "ui.remove": "移除",
  "ui.remove_from_selection": "从选择中移除",
  "ui.removed_from_liked": "已从喜欢中移除",
  "ui.removed_from_music_assistant_liked": "已从 Music Assistant 喜欢列表移除",
  "ui.reorder_queue": "重排队列",
  "ui.repeat": "循环",
  "ui.reset": "重置",
  "ui.reset_lyrics_font_size": "重置歌词字号",
  "ui.reset_lyrics_timing": "重置歌词时序",
  "ui.results": "结果",
  "ui.rtl": "RTL",
  "ui.sat": "周六",
  "ui.save_current_target": "保存当前目标",
  "ui.save_schedule": "保存计划",
  "ui.scene_players_are_not_available": "场景中的播放器当前不可用",
  "ui.scene_presets": "场景预设",
  "ui.scene_presets_2": "场景预设",
  "ui.scenes": "场景",
  "ui.scheduled_start": "定时启动",
  "ui.scheduled_start_activated_label": "定时启动已启用：{label}",
  "ui.scheduled_start_cleared": "定时启动已清除",
  "ui.scheduled_start_failed": "定时启动失败",
  "ui.scheduled_start_saved": "定时启动已保存",
  "ui.scheduled_starts_active_count": "{count} 个定时启动正在生效",
  "ui.schedules": "计划",
  "ui.search": "搜索",
  "ui.search_and_choose_play_next_add_radio_or_like": "搜索后可选择播放、下一首、添加、电台或喜欢。",
  "ui.search_everything": "搜索全部内容...",
  "ui.search_failed": "搜索失败",
  "ui.search_in_queue": "在队列中搜索",
  "ui.search_queue_and_library": "搜索队列和资料库...",
  "ui.search_the_library": "搜索资料库...",
  "ui.search_tracks_albums_artists_and_playlists": "搜索歌曲、专辑、艺人和播放列表",
  "ui.searching": "搜索中...",
  "ui.searching_library": "正在搜索资料库...",
  "ui.searching_smart_selection": "正在搜索智能选择...",
  "ui.seek": "拖动进度",
  "ui.select": "选择",
  "ui.select_lights": "选择灯或灯组",
  "ui.select_player": "选择播放器",
  "ui.select_2": "选择",
  "ui.select_a_player_first": "请先选择播放器",
  "ui.select_at_least_one_studio_player": "请至少选择一个音乐中枢播放器",
  "ui.select_at_least_two_players_to_create_a_group": "请至少选择两个播放器来创建分组",
  "ui.selected": "已选中",
  "ui.selected_music": "已选音乐",
  "ui.selected_player": "已选播放器",
  "ui.selected_player_2": "已选播放器",
  "ui.selected_player_3": "已选播放器",
  "ui.selected_players_at_comfortable_volume": "已选播放器，舒适音量",
  "ui.selected_playlist": "已选播放列表",
  "ui.send_a_short_voice_message_or_announcement_url": "发送简短语音消息或播报链接。",
  "ui.send_a_voice_message": "发送语音消息",
  "ui.send_announcement": "发送播报",
  "ui.sendspin_and_diagnostics": "Sendspin 与诊断",
  "ui.set_sleep_timer": "设置睡眠定时器",
  "ui.set_volume": "设置音量",
  "ui.set_the_visual_order_of_quick_actions": "设置显示顺序。只显示已勾选的快捷操作；未勾选的项目会被忽略。",
  "ui.settings": "设置",
  "ui.settings_source": "设置来源",
  "ui.show_ma_button": "显示 MA 按钮",
  "ui.show_or_hide_the_inline_next_track_row_in_now_playing": "显示或隐藏正在播放界面中的“即将播放”行。",
  "ui.show_theme_toggle": "显示主题切换",
  "ui.show_up_next": "显示即将播放",
  "ui.show_up_next_2": "显示即将播放",
  "ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum": "显示一个独立的紧凑播放器卡片，包含封面、基础控制、音量和展开操作。",
  "ui.shuffle": "随机播放",
  "ui.shuffle_all": "全部随机",
  "ui.shuffle_favorites": "随机播放收藏",
  "ui.shuffle_play": "随机播放",
  "ui.skipped_to_up_next": "已切换到下一首",
  "ui.sleep_timer": "睡眠定时器",
  "ui.sleep_timer_2": "睡眠定时器",
  "ui.sleep_timer_active_remaining": "睡眠定时器运行中：剩余 {remaining}",
  "ui.sleep_timer_added_minutes": "已为定时器增加 {minutes} 分钟",
  "ui.sleep_timer_and_morning_playback": "睡眠定时与晨间播放",
  "ui.sleep_timer_and_scheduled_playback": "睡眠定时与定时播放",
  "ui.sleep_timer_cleared": "睡眠定时器已清除",
  "ui.sleep_timer_finished": "睡眠定时器已结束",
  "ui.sleep_timer_presets": "睡眠定时预设",
  "ui.sleep_timer_set_for_15_minutes": "睡眠定时器已设为 15 分钟",
  "ui.sleep_timer_set_minutes": "睡眠定时器已设为 {minutes} 分钟",
  "ui.sleep_timer_updated_minutes": "睡眠定时器已更新为 {minutes} 分钟",
  "ui.smaller_lyrics": "缩小歌词",
  "ui.smart": "智能",
  "ui.smart_mix": "智能混音",
  "ui.smart_mix_builder": "智能混音生成器",
  "ui.smart_voice_selection": "智能语音选择",
  "ui.soft_and_relaxed": "柔和放松",
  "ui.soft_relaxed_music": "柔和轻松的音乐",
  "ui.some_players_could_not_be_stopped_or_cleared": "部分播放器未能停止或清空",
  "ui.songs_around_an_artist": "围绕艺人的歌曲",
  "ui.sort": "排序",
  "ui.sort_ascending": "升序",
  "ui.sort_descending": "降序",
  "ui.source_queue": "源队列",
  "ui.spain": "西班牙",
  "ui.speak_to_target_players": "向目标播放器发送语音播报",
  "ui.start_radio_mode": "启动电台模式",
  "ui.start_time": "开始时间",
  "ui.start_time_2": "开始时间",
  "ui.started_on_selected_player": "已在所选播放器上开始播放",
  "ui.starting_a_chill_mix": "正在启动轻松混音",
  "ui.starting_quick_mix": "正在启动快速混音...",
  "ui.stations": "个电台",
  "ui.stay_active": "保持启用",
  "ui.stays_active": "保持启用",
  "ui.stop_all": "全部停止",
  "ui.stop_all_players": "停止所有播放器",
  "ui.stop_command_failed": "停止命令失败",
  "ui.stop_playback_clear_queues_and_disconnect_groups": "停止播放、清空队列并断开分组",
  "ui.stopped_all_players_cleared_playlists_and_disconnected_groups": "已停止所有播放器、清空列表并断开分组",
  "ui.stops_this_browser_player": "停止此浏览器播放器",
  "ui.strong": "强",
  "ui.studio": "音乐中枢",
  "ui.studio_closed": "音乐中枢已关闭",
  "ui.studio_media_action_failed": "音乐中枢媒体操作失败",
  "ui.studio_mix_started": "音乐中枢混音已开始",
  "ui.studio_opened": "音乐中枢已打开",
  "ui.studio_pro": "音乐中枢 Pro",
  "ui.studio_pro_2": "音乐中枢 Pro",
  "ui.studio_scene": "音乐中枢场景",
  "ui.studio_scene_deleted": "音乐中枢场景已删除",
  "ui.studio_scene_saved": "音乐中枢场景已保存",
  "ui.studio_scene_was_not_found": "未找到该音乐中枢场景",
  "ui.studio_search": "音乐中枢搜索",
  "ui.studio_shortcut": "音乐中枢快捷方式",
  "ui.subtle": "柔和",
  "ui.sun": "周日",
  "ui.surprise_me": "给我惊喜",
  "ui.sync": "同步",
  "ui.sync_lyrics": "同步歌词",
  "ui.tablet": "平板",
  "ui.tap_to_start_a_sleep_timer": "点击以启动睡眠定时器",
  "ui.target_players": "目标播放器",
  "ui.target_queue": "目标队列",
  "ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist": "文本播报会通过 Home Assistant TTS 发送到所选的 Music Assistant 播放器；链接播报则使用 Music Assistant 的播报播放功能。",
  "ui.text_only": "仅文字",
  "ui.text_tone": "文字色调",
  "ui.theme": "主题",
  "ui.theme_2": "主题",
  "ui.theme_mode": "主题模式",
  "ui.theme_toggle": "主题切换",
  "ui.theme_toggle_2": "主题切换",
  "ui.this_device": "本机",
  "ui.this_device_2": "此设备",
  "ui.this_device_player_connected": "本机播放器已连接",
  "ui.this_device_player_disconnected": "本机播放器已断开",
  "ui.thu": "周四",
  "ui.timer": "定时器",
  "ui.timer_active_remaining": "定时器运行中：剩余 {remaining}",
  "ui.timers": "定时器",
  "ui.to": "到",
  "ui.today": "今天",
  "ui.track": "歌曲",
  "ui.track_lyrics": "歌曲歌词",
  "ui.tracks": "歌曲",
  "ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio": "无需离开音乐中枢即可转移、克隆、查看或清空队列。",
  "ui.transfer_is_unavailable_while_a_pinned_player_is_configured": "配置固定播放器后，暂不支持转移播放",
  "ui.transfer_queue": "转移队列",
  "ui.transfer_queue_2": "转移队列",
  "ui.transfer_queue_3": "转移队列",
  "ui.try_again": "重试",
  "ui.tts_entity": "TTS 实体",
  "ui.tts_entity_used_by_the_announcement_screen": "播报界面使用的 TTS 实体。",
  "ui.tts_language": "TTS 语言",
  "ui.tue": "周二",
  "ui.turkey": "土耳其",
  "ui.turn_off": "关闭",
  "ui.turns_off_after_run": "执行后关闭",
  "ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl": "关闭高负载视觉效果，让卡片在 Nest Hub、旧平板和性能较弱的屏幕上运行得更流畅。",
  "ui.type_a_free_style_first": "请先输入自由风格",
  "ui.type_an_announcement": "输入播报内容...",
  "ui.type_anything": "随意输入",
  "ui.type_what_should_be_announced": "输入要播报的内容...",
  "ui.ungroup": "取消分组",
  "ui.ungroup_2": "取消分组",
  "ui.united_kingdom": "英国",
  "ui.united_states": "美国",
  "ui.unknown": "未知",
  "ui.up": "上移",
  "ui.move_down": "下移",
  "ui.move_up": "上移",
  "ui.up_next": "即将播放",
  "ui.up_next_2": "即将播放",
  "ui.up_next_title": "即将播放：{title}",
  "ui.upbeat_and_moving": "轻快有动感",
  "ui.use_any_home_assistant_path_example_lovelace_home": "可使用任意 Home Assistant 路径，例如：/lovelace/home",
  "ui.use_current_view_for_home_button": "主页按钮使用当前视图",
  "ui.use_expand_to_open_the_full_player": "使用展开按钮打开完整播放器",
  "ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen": "使用与卡片内设置界面相同的基础国家列表。",
  "ui.visible_tiles": "显示卡片",
  "ui.visible_tiles_2": "显示卡片",
  "ui.visual_editor": "可视化编辑器",
  "ui.voice_input_failed": "语音输入失败",
  "ui.voice_input_is_not_supported_on_this_device": "此设备不支持语音输入",
  "ui.voice_search": "语音搜索",
  "ui.voice_search_failed": "语音搜索失败",
  "ui.voice_search_is_not_supported_on_this_device": "此设备不支持语音搜索",
  "ui.volume": "音量",
  "ui.volume_and_player_target": "音量与播放器目标",
  "ui.volume_boost": "音量增强",
  "ui.volume_control_large_screen_only": "音量控制（仅大屏可用）",
  "ui.volume_down": "降低音量",
  "ui.volume_mode": "音量模式",
  "ui.volume_presets": "音量预设",
  "ui.volume_step_between_1_and_10_percent_default_5": "音量步进范围为 1% 到 10%。默认 5%。",
  "ui.volume_step_buttons": "音量 +/- 按钮",
  "ui.volume_step_percent": "音量步进",
  "ui.volume_up": "提高音量",
  "ui.waiting_for_this_device_player": "正在等待本机播放器...",
  "ui.wake": "唤醒",
  "ui.wake_schedules": "唤醒计划",
  "ui.wed": "周三",
  "ui.week": "本周",
  "ui.what_would_you_like_to_listen_to": "你想听点什么？",
  "ui.will_play_next": "将作为下一首播放",
  "ui.will_play_next_in_studio": "将在音乐中枢中下一首播放",
  "ui.worldwide_popular": "全球热门",
  "ui.add_player_light_pair": "添加配对",
  "ui.ambient_light": "氛围灯光",
  "ui.ambient_light_brightness": "灯光亮度",
  "ui.ambient_light_cooldown": "灯光更新间隔",
  "ui.ambient_light_entities": "默认彩灯",
  "ui.ambient_light_player_map": "播放器到灯光映射",
  "ui.ambient_light_player_map_helper": "使用下方的播放器和彩灯选择器。每一行填写完整后会形成一个配对；清空播放器或灯光即可移除该配对。",
  "ui.ambient_light_transition": "灯光过渡",
  "ui.analog": "指针式",
  "ui.choose_only_the_lights_that_should_follow_the_music": "当没有针对播放器的专属配对时，只选择应跟随音乐变化的彩灯。",
  "ui.digital": "数字式",
  "ui.discover_albums": "发现专辑",
  "ui.discover_music": "发现",
  "ui.discover_subtitle": "推荐的歌单、专辑和电台。",
  "ui.discovery": "音乐发现",
  "ui.discovery_failed": "发现模式失败",
  "ui.discovery_mode": "发现模式",
  "ui.discovery_started": "发现模式已开始：{mood}",
  "ui.maximum_brightness_for_music_lighting": "音乐灯光的最大亮度。",
  "ui.minimum_seconds_between_light_updates": "两次灯光更新之间的最短间隔（秒）。",
  "ui.player_light_pair_lights": "彩灯 {number}",
  "ui.player_light_pair_lights_helper": "为这个 Music Assistant 播放器选择支持颜色的灯。清空此字段或播放器即可移除该配对。",
  "ui.player_light_pair_player": "Music Assistant 播放器 {number}",
  "ui.player_light_pair_player_helper": "为这个灯光配对选择 Music Assistant 播放器。",
  "ui.all": "全部",
  "ui.all_genres": "所有流派",
  "ui.genres_and_styles": "流派和风格",
  "ui.compact_widget_mode": "紧凑小组件样式",
  "ui.choose_when_compact_mode_uses_the_two_row_mobile_widget": "选择紧凑模式何时使用较小的两行移动小组件。",
  "ui.full": "完整",
  "ui.mini_widget": "迷你小组件",
  "ui.genre": "流派",
  "ui.mood_and_genre": "心情与流派",
  "ui.music_by_mood_and_genre": "按心情和流派选音乐",
  "ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player": "辅助按钮的可选实体。留空时将停止当前活动播放器。",
  "ui.power": "电源",
  "ui.power_action_failed": "电源操作失败",
  "ui.power_action_sent": "电源操作已发送",
  "ui.power_button": "辅助按钮",
  "ui.power_button_action": "辅助按钮动作",
  "ui.power_button_entity": "辅助按钮实体",
  "ui.scene": "场景",
  "ui.screensaver": "屏保",
  "ui.screensaver_clock_mode": "屏保时钟模式",
  "ui.screensaver_clock_size": "屏保时钟大小",
  "ui.screensaver_clock_size_helper": "调整平板屏保时钟的大小，不会移动封面。",
  "ui.screensaver_clock_x": "时钟位置 - 左 / 右",
  "ui.screensaver_clock_x_helper": "数值越小越靠左，数值越大越靠右。",
  "ui.screensaver_clock_y": "时钟位置 - 上 / 下",
  "ui.screensaver_clock_y_helper": "数值越小越靠上，数值越大越靠下。",
  "ui.screensaver_message": "屏保消息",
  "ui.screensaver_timeout": "屏保等待时间",
  "ui.script": "脚本",
  "ui.short_optional_message_shown_on_the_screensaver": "显示在屏保上的简短可选消息。",
  "ui.show_a_calm_clock_artwork_display_after_idle_time": "空闲一段时间后显示柔和的时钟与封面屏保。",
  "ui.show_power_button_in_the_player_controls": "在快捷操作中显示辅助按钮。",
  "ui.show_the_fullscreen_discovery_mode_in_actions": "在“操作”中显示全屏发现模式。",
  "ui.smart_home": "智能家居",
  "ui.soft_transition_time_for_music_lighting": "音乐灯光的柔和过渡时间。",
  "ui.stop_player": "停止播放器",
  "ui.sync_selected_lights_to_the_current_artwork_color": "将选中的灯光同步为当前封面颜色。",
  "ui.toggle": "切换",
  "ui.turn_on": "打开",
  "ui.choose_a_category_to_browse_provider_playlists": "播放前先选择一个分类浏览服务商播放列表。",
  "ui.new_and_recommended": "新品与推荐",
  "ui.provider_playlists": "服务商播放列表",
  "ui.clean_all": "清除全部",
  "ui.clean_all_confirm_action": "清除全部",
  "ui.clean_all_confirm_copy": "这会断开所有活动播放器、停止播放并清空它们的队列。继续吗？",
  "ui.clean_all_confirm_title": "清除所有播放器？",
  "ui.repeat_all": "全部循环",
  "ui.repeat_off": "关闭循环",
  "ui.repeat_one": "单曲循环",
  "ui.scales_interface_icons_without_changing_button_sizes": "缩放界面图标，不改变按钮大小或布局。",
  "ui.assist_agent": "Assist 助手",
  "ui.assist_agent_id": "Assist 助手 ID",
  "ui.assist_only": "仅 Assist",
  "ui.default_assist_agent": "默认 Home Assistant Assist",
  "ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist": "推荐：音乐命令会在选中的播放器上本地执行。卡片无法理解的内容会发送给 Home Assistant Assist。",
  "ui.hybrid_music_plus_assist": "混合：音乐 + Assist",
  "ui.icon_size": "图标大小",
  "ui.music_only": "仅音乐",
  "ui.music_plus_assist": "音乐 + Assist",
  "ui.optional_assist_agent_id_leave_empty_for_home_assistant_default": "可选的 Assist 助手 ID。留空将使用 Home Assistant 默认 Assist 助手。",
  "ui.optional_assist_agent_leave_empty_for_home_assistant_default": "可选。除非你有多个 Assist 助手并希望此卡片使用指定助手，否则保持默认即可。",
  "ui.show_a_push_to_talk_button_for_music_and_assist_commands": "显示用于音乐命令和 Assist 兜底的按住说话按钮。",
  "ui.show_home_inside_quick_actions": "在快捷操作中显示主页，而不是浮动按钮。",
  "ui.show_plus_minus_buttons_next_to_the_volume_slider": "在音量滑块旁显示加号和减号按钮。",
  "ui.speak_voice_assistant_responses_out_loud": "在支持时通过此浏览器朗读识别到的回复。",
  "ui.flow_assistant": "FLOW ASSISTANT",
  "ui.voice_assistant": "语音助手",
  "ui.voice_assistant_disabled": "语音助手已禁用",
  "ui.voice_assistant_listening": "正在聆听命令...",
  "ui.voice_assistant_mode": "语音助手模式",
  "ui.voice_command_executed": "语音命令已执行",
  "ui.voice_command_completed_action": "完成：{action}",
  "ui.voice_command_failed": "语音命令失败",
  "ui.voice_command_no_player": "请选择播放器，或说出播放器名称。",
  "ui.voice_command_not_understood": "我无法理解这个命令。",
  "ui.voice_command_sent": "语音命令已发送",
  "ui.voice_done_status": "完成",
  "ui.voice_error_status": "需要注意",
  "ui.voice_feedback": "语音反馈",
  "ui.voice_group_connect_needs_players": "请说出至少两个播放器名称，或先选择一个播放器再说出另一个。",
  "ui.voice_group_connected_players": "已将 {primary} 与 {members} 连接",
  "ui.voice_group_disconnect_needs_player": "请说出要断开的播放器组。",
  "ui.voice_group_disconnected_player": "已断开 {player} 的组",
  "ui.voice_language_is_not_supported": "此浏览器不支持所选语音识别语言。",
  "ui.voice_match_not_confident": "我找到了 {title}，但它与“{query}”的匹配度不够高。请再说一次歌曲和艺人。",
  "ui.voice_music_searching": "正在搜索音乐：{query}",
  "ui.voice_playing_result": "正在播放 {title}",
  "ui.voice_queue_transfer_needs_players": "请说出来源和目标播放器，例如：transfer queue from Living Room to Kitchen。",
  "ui.voice_queue_transferred_between": "队列已从 {source} 转移到 {target}",
  "ui.voice_starting_playback": "正在开始播放 {title}...",
  "ui.voice_processing_status": "正在处理命令...",
  "ui.voice_ready_status": "就绪",
  "ui.voice_recognition_network_failed": "语音识别服务不可用。麦克风权限可能没有问题；请检查浏览器、VPN、DNS、防火墙或网络。",
  "ui.voice_response": "响应",
  "ui.voice_response_will_appear_here": "响应会显示在这里。",
  "ui.voice_transcript": "转录",
  "ui.voice_listening_status": "正在聆听...",
  "ui.waiting_for_speech": "正在等待语音...",
  "ui.microphone_permission_or_browser_blocked": "浏览器阻止了语音识别。请检查麦克风和浏览器语音设置。",
  "ui.screensaver_controls": "屏保控制",
  "ui.show_previous_next_controls_on_the_screensaver": "在屏保上显示上一首和下一首按钮。",
  "ui.optional_name_for_the_auxiliary_button": "显示为辅助按钮提示的可选标签。",
  "ui.choose_the_auxiliary_button_icon": "选择辅助按钮显示的图标。",
  "ui.choose_how_players_are_sorted": "选择默认、按字母或自定义播放器顺序。",
  "ui.choose_music_assistant_players_to_hide_from_the_card": "选择不应出现在卡片中的 Music Assistant 播放器。",
  "ui.custom_order": "自定义顺序",
  "ui.default_order": "默认顺序",
  "ui.no_visible_music_assistant_players_after_exclusions": "所有 Music Assistant 播放器都被隐藏播放器设置隐藏。",
  "ui.set_custom_player_order": "设置自定义播放器顺序。空槽位会被忽略。",
  "ui.show_auxiliary_button_in_quick_actions": "在快捷操作栏中显示此辅助按钮。",
  "ui.screensaver_buttons": "Screensaver buttons",
  "ui.choose_which_buttons_appear_on_the_screensaver": "Choose exactly which buttons appear on the screensaver, including FLOW ASSISTANT, previous and next.",
  "ui.hotel_mode": "酒店模式",
  "ui.hotel_mode_helper": "适合酒店的极简安全界面：仅保留播放器控制、音量、搜索、封面浏览和播放器选择。",
  "ui.style_and_genres": "风格和流派"
});
const DEFAULT_LANGUAGE = "en";
const DICTIONARIES = Object.freeze({
  en: messages$7,
  da: messages$6,
  es: messages$5,
  fr: messages$4,
  he: messages$3,
  it: messages$2,
  lt: messages$1,
  zh: messages
});
const RTL_LANGUAGE_CODES = Object.freeze(["he"]);
const LANGUAGE_OPTIONS = Object.freeze([
  { value: "auto", label: "Auto" },
  { value: "en", label: "English" },
  { value: "da", label: "Dansk" },
  { value: "es", label: "Español" },
  { value: "fr", label: "Français" },
  { value: "he", label: "עברית / Hebrew" },
  { value: "it-IT", label: "Italiano" },
  { value: "lt", label: "Lithuanian / Lietuvių" },
  { value: "zh-CN", label: "简体中文 / Simplified Chinese" }
]);
Object.freeze(Object.keys(DICTIONARIES));
const ENGLISH_TEXT_TO_KEY = Object.freeze(
  Object.entries(messages$7).reduce((acc, [key, value]) => {
    if (typeof value === "string" && acc[value] == null) acc[value] = key;
    return acc;
  }, {})
);
function baseLanguageCode(value) {
  return String(value || "").trim().toLowerCase().replace("_", "-").split("-")[0];
}
function normalizeLanguageCode(value, fallback = DEFAULT_LANGUAGE) {
  const candidate = baseLanguageCode(value);
  if (!candidate || candidate === "auto") return fallback;
  return DICTIONARIES[candidate] ? candidate : fallback;
}
function detectLanguage({
  configLanguage,
  hass,
  doc = globalThis.document,
  nav = globalThis.navigator
} = {}) {
  const configured = String(configLanguage || "").trim().toLowerCase();
  if (configured && configured !== "auto") {
    return normalizeLanguageCode(configured, DEFAULT_LANGUAGE);
  }
  const candidates = [
    hass?.locale?.language,
    hass?.language,
    doc?.documentElement?.lang,
    nav?.language,
    ...Array.isArray(nav?.languages) ? nav.languages : []
  ];
  for (const candidate of candidates) {
    const normalized = normalizeLanguageCode(candidate, "");
    if (normalized) return normalized;
  }
  return DEFAULT_LANGUAGE;
}
function isRtlLanguage(value) {
  return RTL_LANGUAGE_CODES.includes(baseLanguageCode(value));
}
function keyForEnglishText(text) {
  return ENGLISH_TEXT_TO_KEY[String(text || "")] || "";
}
function interpolate(template, params = {}) {
  return String(template).replace(/\{([a-zA-Z0-9_]+)\}/g, (match, key) => params[key] == null ? match : String(params[key]));
}
function translate(language, key, params = {}, fallback = "") {
  const normalized = normalizeLanguageCode(language, DEFAULT_LANGUAGE);
  const dictionary = DICTIONARIES[normalized] || messages$7;
  const template = dictionary[key] ?? messages$7[key] ?? fallback ?? key;
  return interpolate(template, params);
}
function translateText(language, englishText, params = {}, fallback = englishText) {
  const key = keyForEnglishText(englishText);
  if (!key) return interpolate(fallback ?? englishText, params);
  return translate(language, key, params, fallback ?? englishText);
}
function isHebrewLanguageTag(value) {
  return String(value || "").trim().toLowerCase().startsWith("he");
}
function pickEditorLanguageCandidate({
  hassLocaleLanguage,
  hassLanguage,
  documentLanguage
} = {}) {
  return hassLocaleLanguage || hassLanguage || documentLanguage || "";
}
function detectEditorHebrew(doc = globalThis.document) {
  try {
    const homeAssistant = doc?.querySelector?.("home-assistant");
    const lang = pickEditorLanguageCandidate({
      hassLocaleLanguage: homeAssistant?.hass?.locale?.language,
      hassLanguage: homeAssistant?.hass?.language,
      documentLanguage: doc?.documentElement?.lang
    });
    return isHebrewLanguageTag(lang);
  } catch (_) {
    return false;
  }
}
function assertStringIfDefined(value, key) {
  if (value == null) return;
  if (typeof value !== "string") throw new Error(`${key} must be a string`);
}
function assertBooleanIfDefined(value, key) {
  if (value == null) return;
  if (typeof value !== "boolean") throw new Error(`${key} must be a boolean`);
}
function assertNumberIfDefined(value, key) {
  if (value == null) return;
  if (typeof value !== "number" || Number.isNaN(value)) {
    throw new Error(`${key} must be a number`);
  }
}
function assertStringArrayIfDefined(value, key) {
  if (value == null) return;
  if (!Array.isArray(value) || value.some((entry) => typeof entry !== "string")) {
    throw new Error(`${key} must be an array of strings`);
  }
}
function assertValueInList(value, key, allowedValues) {
  if (value == null) return;
  if (!allowedValues.includes(value)) {
    throw new Error(`${key} must be one of: ${allowedValues.join(", ")}`);
  }
}
function assertStringArrayValuesIfDefined(value, key, allowedValues) {
  assertStringArrayIfDefined(value, key);
  if (value == null) return;
  const invalid = value.find((entry) => !allowedValues.includes(entry));
  if (invalid !== void 0) {
    throw new Error(`${key} must contain only: ${allowedValues.join(", ")}`);
  }
}
function validateBaseCardEditorConfig(config) {
  if (!config || typeof config !== "object" || Array.isArray(config)) {
    throw new Error("Card config must be an object");
  }
  assertStringIfDefined(config.config_entry_id, "config_entry_id");
  assertStringIfDefined(config.ma_url, "ma_url");
  assertStringIfDefined(config.music_assistant_external_url, "music_assistant_external_url");
  assertStringIfDefined(config.ma_token, "ma_token");
  assertStringIfDefined(config.active_player_helper_entity, "active_player_helper_entity");
  assertStringIfDefined(config.ma_interface_url, "ma_interface_url");
  assertValueInList(config.ma_interface_target, "ma_interface_target", ["_self", "_blank"]);
  assertNumberIfDefined(config.height, "height");
  assertNumberIfDefined(config.main_opacity, "main_opacity");
  assertNumberIfDefined(config.popup_opacity, "popup_opacity");
  assertNumberIfDefined(config.cache_ttl, "cache_ttl");
  assertNumberIfDefined(config.music_assistant_timeout_ms, "music_assistant_timeout_ms");
  assertStringIfDefined(config.language, "language");
  assertValueInList(config.theme_mode, "theme_mode", ["auto", "dark", "light", "custom"]);
  assertBooleanIfDefined(config.rtl, "rtl");
  assertBooleanIfDefined(config.hotel_mode, "hotel_mode");
  assertBooleanIfDefined(config.performance_mode, "performance_mode");
  assertValueInList(config.performance_profile, "performance_profile", ["full", "high", "low", "ultra_lite"]);
  assertBooleanIfDefined(config.show_ma_button, "show_ma_button");
  assertBooleanIfDefined(config.show_theme_toggle, "show_theme_toggle");
  assertBooleanIfDefined(config.ambient_light_enabled, "ambient_light_enabled");
  assertStringArrayIfDefined(config.ambient_light_entities, "ambient_light_entities");
  assertStringArrayIfDefined(config.ambient_light_player_map, "ambient_light_player_map");
  assertNumberIfDefined(config.ambient_light_brightness, "ambient_light_brightness");
  assertNumberIfDefined(config.ambient_light_transition, "ambient_light_transition");
  assertNumberIfDefined(config.ambient_light_cooldown, "ambient_light_cooldown");
  assertBooleanIfDefined(config.screensaver_enabled, "screensaver_enabled");
  assertBooleanIfDefined(config.screensaver_controls_enabled, "screensaver_controls_enabled");
  assertStringArrayValuesIfDefined(config.screensaver_control_buttons, "screensaver_control_buttons", ["previous", "play_pause", "next", "mute", "power", "like", "lyrics", "voice"]);
  assertValueInList(config.screensaver_clock_mode, "screensaver_clock_mode", ["digital", "analog"]);
  assertNumberIfDefined(config.screensaver_timeout_seconds, "screensaver_timeout_seconds");
  assertStringIfDefined(config.screensaver_message, "screensaver_message");
  assertNumberIfDefined(config.screensaver_clock_size, "screensaver_clock_size");
  assertNumberIfDefined(config.screensaver_clock_x, "screensaver_clock_x");
  assertNumberIfDefined(config.screensaver_clock_y, "screensaver_clock_y");
  assertBooleanIfDefined(config.power_button_enabled, "power_button_enabled");
  assertStringIfDefined(config.power_button_name, "power_button_name");
  assertStringIfDefined(config.power_button_icon, "power_button_icon");
  assertValueInList(config.power_button_action, "power_button_action", ["stop_player", "toggle", "turn_on", "turn_off", "scene", "script"]);
  assertStringIfDefined(config.power_button_entity, "power_button_entity");
  for (let index = 2; index <= 4; index += 1) {
    assertBooleanIfDefined(config[`aux_button_${index}_enabled`], `aux_button_${index}_enabled`);
    assertStringIfDefined(config[`aux_button_${index}_name`], `aux_button_${index}_name`);
    assertStringIfDefined(config[`aux_button_${index}_icon`], `aux_button_${index}_icon`);
    assertValueInList(config[`aux_button_${index}_action`], `aux_button_${index}_action`, ["stop_player", "toggle", "turn_on", "turn_off", "scene", "script"]);
    assertStringIfDefined(config[`aux_button_${index}_entity`], `aux_button_${index}_entity`);
  }
  assertBooleanIfDefined(config.discovery_mode_enabled, "discovery_mode_enabled");
}
function validateMobileCardEditorConfig(config) {
  validateBaseCardEditorConfig(config);
  assertValueInList(config.layout_mode, "layout_mode", ["auto", "mobile", "tablet"]);
  assertValueInList(config.settings_source, "settings_source", ["device", "visual", "ui", "card"]);
  assertValueInList(config.night_mode, "night_mode", ["off", "auto", "on"]);
  assertStringIfDefined(config.night_mode_auto_start, "night_mode_auto_start");
  assertStringIfDefined(config.night_mode_auto_end, "night_mode_auto_end");
  assertStringIfDefined(config.favorite_button_entity, "favorite_button_entity");
  assertBooleanIfDefined(config.allow_local_likes, "allow_local_likes");
  assertBooleanIfDefined(config.use_mass_queue_send_command, "use_mass_queue_send_command");
  assertStringIfDefined(config.mobile_custom_color, "mobile_custom_color");
  assertValueInList(config.mobile_dynamic_theme_mode, "mobile_dynamic_theme_mode", ["off", "auto", "strong"]);
  assertValueInList(config.mobile_background_motion_mode, "mobile_background_motion_mode", ["off", "subtle", "strong", "extreme"]);
  assertValueInList(config.mobile_custom_text_tone, "mobile_custom_text_tone", ["light", "dark"]);
  assertNumberIfDefined(config.mobile_font_scale, "mobile_font_scale");
  assertNumberIfDefined(config.mobile_icon_scale, "mobile_icon_scale");
  assertBooleanIfDefined(config.mobile_footer_search_enabled, "mobile_footer_search_enabled");
  assertValueInList(config.mobile_footer_mode, "mobile_footer_mode", ["icon", "text", "both"]);
  assertBooleanIfDefined(config.mobile_studio_shortcut, "mobile_studio_shortcut");
  assertBooleanIfDefined(config.mobile_home_shortcut, "mobile_home_shortcut");
  assertStringIfDefined(config.mobile_home_shortcut_path, "mobile_home_shortcut_path");
  assertValueInList(config.mobile_volume_mode, "mobile_volume_mode", ["always", "button"]);
  assertBooleanIfDefined(config.mobile_volume_step_buttons, "mobile_volume_step_buttons");
  assertNumberIfDefined(config.mobile_volume_step_percent, "mobile_volume_step_percent");
  assertValueInList(config.mobile_mic_mode, "mobile_mic_mode", ["on", "off", "smart"]);
  assertBooleanIfDefined(config.voice_assistant_enabled, "voice_assistant_enabled");
  assertValueInList(config.voice_assistant_mode, "voice_assistant_mode", ["hybrid", "music", "assist"]);
  assertStringIfDefined(config.voice_assistant_agent_id, "voice_assistant_agent_id");
  assertBooleanIfDefined(config.voice_assistant_speak_feedback, "voice_assistant_speak_feedback");
  assertNumberIfDefined(config.flow_assistant_response_timeout_ms, "flow_assistant_response_timeout_ms");
  assertNumberIfDefined(config.flow_assistant_listen_timeout_ms, "flow_assistant_listen_timeout_ms");
  assertNumberIfDefined(config.flow_assistant_auto_close_ms, "flow_assistant_auto_close_ms");
  assertStringArrayIfDefined(config.mobile_library_tabs, "mobile_library_tabs");
  assertValueInList(config.mobile_library_default_layout, "mobile_library_default_layout", ["grid", "list"]);
  assertStringArrayIfDefined(config.mobile_main_bar_items, "mobile_main_bar_items");
  assertStringArrayIfDefined(config.mobile_quick_actions, "mobile_quick_actions");
  for (let index = 1; index <= 10; index += 1) {
    assertStringIfDefined(config[`mobile_quick_action_${index}`], `mobile_quick_action_${index}`);
  }
  assertValueInList(config.mobile_liked_mode, "mobile_liked_mode", ["ma", "local"]);
  assertValueInList(config.mobile_swipe_mode, "mobile_swipe_mode", ["play", "browse"]);
  assertStringIfDefined(config.mobile_radio_browser_country, "mobile_radio_browser_country");
  assertStringArrayIfDefined(config.mobile_announcement_presets, "mobile_announcement_presets");
  assertNumberIfDefined(config.mobile_announcement_volume, "mobile_announcement_volume");
  assertStringIfDefined(config.announcement_tts_entity, "announcement_tts_entity");
  assertStringIfDefined(config.announcement_tts_language, "announcement_tts_language");
  assertBooleanIfDefined(config.mobile_compact_mode, "mobile_compact_mode");
  assertValueInList(config.mobile_compact_widget_mode, "mobile_compact_widget_mode", ["auto", "full", "mini"]);
  assertBooleanIfDefined(config.mobile_compact_edge_to_edge, "mobile_compact_edge_to_edge");
  assertValueInList(config.mobile_layout_mode, "mobile_layout_mode", ["auto", "full", "compact"]);
  assertBooleanIfDefined(config.mobile_show_up_next, "mobile_show_up_next");
  assertStringIfDefined(config.pinned_player_entity, "pinned_player_entity");
  assertStringArrayIfDefined(config.pinned_player_entities, "pinned_player_entities");
  assertStringArrayIfDefined(config.excluded_player_entities, "excluded_player_entities");
  assertValueInList(config.player_sort_mode, "player_sort_mode", ["default", "alphabetical", "custom"]);
  assertStringArrayIfDefined(config.player_order_entities, "player_order_entities");
  for (let index = 1; index <= 20; index += 1) {
    assertStringIfDefined(config[`player_order_entity_${index}`], `player_order_entity_${index}`);
  }
}
let editorFormDeps = {
  homeiiEditorI18n: (key, _params = {}, fallback = "") => fallback || key,
  homeiiEditorLabelFor: (schema = {}, labels = {}) => labels?.[schema?.name] || schema?.label || schema?.title || schema?.name || "",
  homeiiEditorHelperFor: (schema = {}, helpers = {}) => helpers?.[schema?.name] || schema?.helper || "",
  detectEditorHebrew: () => false,
  visibleLanguageOptions: [],
  radioBrowserCountrySelectorOptions: (translateFn = null) => [{ value: "all", label: typeof translateFn === "function" ? translateFn("ui.all_countries") : "ui.all_countries" }]
};
function configureHomeiiEditorForms(deps = {}) {
  editorFormDeps = {
    ...editorFormDeps,
    ...deps
  };
}
function homeiiEditorI18n$1(key, params = {}, fallback = "") {
  return editorFormDeps.homeiiEditorI18n(key, params, fallback);
}
const HomeiiEditorLocale$1 = Object.freeze({
  detectEditorHebrew() {
    return editorFormDeps.detectEditorHebrew();
  }
});
function homeiiEditorLabelFor$1(schema = {}, labels = {}) {
  return editorFormDeps.homeiiEditorLabelFor(schema, labels);
}
function homeiiEditorHelperFor$1(schema = {}, helpers = {}) {
  return editorFormDeps.homeiiEditorHelperFor(schema, helpers);
}
function visibleLanguageOptions() {
  return Array.isArray(editorFormDeps.visibleLanguageOptions) ? editorFormDeps.visibleLanguageOptions : [];
}
function homeiiRadioBrowserCountrySelectorOptions(translateFn = null, language = "en") {
  return editorFormDeps.radioBrowserCountrySelectorOptions(translateFn, language);
}
function getBaseCardConfigForm$1() {
  const labels = {
    config_entry_id: "Config Entry ID",
    ma_url: homeiiEditorI18n$1("ui.music_assistant_url"),
    music_assistant_external_url: "Music Assistant external URL",
    ma_token: homeiiEditorI18n$1("ui.music_assistant_token"),
    active_player_helper_entity: homeiiEditorI18n$1("ui.active_player_helper"),
    ma_interface_url: homeiiEditorI18n$1("ui.ma_interface_path"),
    ma_interface_target: homeiiEditorI18n$1("ui.open_interface_in"),
    show_ma_button: homeiiEditorI18n$1("ui.show_ma_button"),
    show_theme_toggle: homeiiEditorI18n$1("ui.show_theme_toggle"),
    cache_ttl: homeiiEditorI18n$1("ui.cache_ttl"),
    music_assistant_timeout_ms: "Music Assistant timeout",
    language: homeiiEditorI18n$1("ui.language"),
    theme_mode: homeiiEditorI18n$1("ui.theme_mode"),
    hotel_mode: homeiiEditorI18n$1("ui.hotel_mode", {}, "Hotel Mode"),
    performance_profile: homeiiEditorI18n$1("ui.performance_profile"),
    performance_mode: homeiiEditorI18n$1("ui.performance_mode_for_weak_devices"),
    night_mode: homeiiEditorI18n$1("ui.night_mode"),
    night_mode_auto_start: homeiiEditorI18n$1("ui.night_start_time"),
    night_mode_auto_end: homeiiEditorI18n$1("ui.night_end_time"),
    rtl: "RTL",
    main_opacity: homeiiEditorI18n$1("ui.main_opacity"),
    popup_opacity: homeiiEditorI18n$1("ui.popup_opacity")
  };
  const helpers = {
    config_entry_id: homeiiEditorI18n$1("ui.music_assistant_config_entry_id_if_you_want_direct_integration_lookup_th"),
    ma_url: homeiiEditorI18n$1("ui.leave_empty_if_the_card_should_use_home_assistant_only"),
    music_assistant_external_url: "Optional HTTPS Music Assistant URL for HTTPS dashboards, Nabu Casa, and Companion App browser-player connections.",
    ma_token: homeiiEditorI18n$1("ui.only_needed_when_using_a_direct_music_assistant_url"),
    active_player_helper_entity: homeiiEditorI18n$1("ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for"),
    ma_interface_url: homeiiEditorI18n$1("ui.path_used_when_opening_the_music_assistant_interface"),
    cache_ttl: homeiiEditorI18n$1("ui.cache_duration_in_milliseconds_for_selected_data_requests"),
    music_assistant_timeout_ms: "Maximum milliseconds to wait for Music Assistant service responses before showing an error.",
    hotel_mode: homeiiEditorI18n$1("ui.hotel_mode_helper", {}, "Minimal hotel-safe UI: player controls, volume, search, artwork browsing, and player selection only."),
    performance_profile: homeiiEditorI18n$1("ui.performance_profile_helper"),
    performance_mode: homeiiEditorI18n$1("ui.disables_blur_animations_dynamic_backgrounds_and_heavy_shadows_recommend"),
    main_opacity: homeiiEditorI18n$1("ui.opacity_for_the_main_card_background"),
    popup_opacity: homeiiEditorI18n$1("ui.opacity_for_popups_and_overlays")
  };
  return {
    schema: [
      {
        type: "expandable",
        name: "general_section",
        title: homeiiEditorI18n$1("ui.general_and_display"),
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "general_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "language", selector: { select: { mode: "dropdown", options: visibleLanguageOptions() } } },
              { name: "theme_mode", selector: { select: { mode: "dropdown", options: [
                { value: "auto", label: "Auto" },
                { value: "dark", label: homeiiEditorI18n$1("ui.dark") },
                { value: "light", label: homeiiEditorI18n$1("ui.light") },
                { value: "custom", label: homeiiEditorI18n$1("ui.custom") }
              ] } } },
              { name: "performance_profile", selector: { select: { mode: "dropdown", options: [
                { value: "full", label: "Full" },
                { value: "high", label: "High" },
                { value: "low", label: "Low" },
                { value: "ultra_lite", label: "Ultra Lite" }
              ] } } },
              { name: "rtl", selector: { boolean: {} } },
              { name: "hotel_mode", selector: { boolean: {} } },
              { name: "main_opacity", selector: { number: { min: 0.3, max: 1, step: 0.02, mode: "box" } } },
              { name: "popup_opacity", selector: { number: { min: 0.4, max: 1, step: 0.02, mode: "box" } } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "connection_section",
        title: homeiiEditorI18n$1("ui.connection_and_behavior"),
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "connection_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "config_entry_id", selector: { text: {} } },
              { name: "ma_url", selector: { text: { type: "url" } } },
              { name: "music_assistant_external_url", selector: { text: { type: "url" } } },
              { name: "ma_token", selector: { text: {} } },
              { name: "active_player_helper_entity", selector: { entity: { multiple: false, filter: [{ domain: "input_text" }] } } },
              { name: "ma_interface_url", selector: { text: {} } },
              { name: "ma_interface_target", selector: { select: { mode: "dropdown", options: [
                { value: "_self", label: "_self" },
                { value: "_blank", label: "_blank" }
              ] } } },
              { name: "show_ma_button", selector: { boolean: {} } },
              { name: "show_theme_toggle", selector: { boolean: {} } },
              { name: "cache_ttl", selector: { number: { min: 0, max: 36e5, step: 1e3, mode: "box" } } },
              { name: "music_assistant_timeout_ms", selector: { number: { min: 3e3, max: 6e4, step: 1e3, mode: "box" } } }
            ]
          }
        ]
      }
    ],
    computeLabel: (schema) => homeiiEditorLabelFor$1(schema, labels),
    computeHelper: (schema) => homeiiEditorHelperFor$1(schema, helpers),
    assertConfig: (config) => {
      if (!config || typeof config !== "object" || Array.isArray(config)) {
        throw new Error("Card config must be an object");
      }
    }
  };
}
function getMobileEditorTexts() {
  const quickActionSlotLabels = {};
  const quickActionSlotHelpers = {};
  for (let index = 1; index <= 10; index += 1) {
    quickActionSlotLabels[`mobile_quick_action_${index}`] = `${homeiiEditorI18n$1("ui.quick_actions")} ${index}`;
    quickActionSlotHelpers[`mobile_quick_action_${index}`] = homeiiEditorI18n$1("ui.set_the_visual_order_of_quick_actions");
  }
  const playerOrderSlotLabels = {};
  const playerOrderSlotHelpers = {};
  for (let index = 1; index <= 20; index += 1) {
    playerOrderSlotLabels[`player_order_entity_${index}`] = `${homeiiEditorI18n$1("ui.player_order")} ${index}`;
    playerOrderSlotHelpers[`player_order_entity_${index}`] = homeiiEditorI18n$1("ui.set_custom_player_order");
  }
  const auxiliaryLabels = {};
  const auxiliaryHelpers = {};
  for (let index = 2; index <= 4; index += 1) {
    auxiliaryLabels[`aux_button_${index}_enabled`] = `${homeiiEditorI18n$1("ui.auxiliary_button")} ${index}`;
    auxiliaryLabels[`aux_button_${index}_name`] = `${homeiiEditorI18n$1("ui.auxiliary_button_name")} ${index}`;
    auxiliaryLabels[`aux_button_${index}_icon`] = `${homeiiEditorI18n$1("ui.auxiliary_button_icon")} ${index}`;
    auxiliaryLabels[`aux_button_${index}_action`] = `${homeiiEditorI18n$1("ui.auxiliary_button_action")} ${index}`;
    auxiliaryLabels[`aux_button_${index}_entity`] = `${homeiiEditorI18n$1("ui.auxiliary_button_entity")} ${index}`;
    auxiliaryHelpers[`aux_button_${index}_enabled`] = homeiiEditorI18n$1("ui.show_auxiliary_button_in_quick_actions");
  }
  const quickActionOptions = [
    { value: "home", label: homeiiEditorI18n$1("ui.home") },
    { value: "search", label: homeiiEditorI18n$1("ui.search") },
    { value: "timer", label: homeiiEditorI18n$1("ui.timer") },
    { value: "like", label: homeiiEditorI18n$1("ui.like_2") },
    { value: "lyrics", label: homeiiEditorI18n$1("ui.lyrics") },
    { value: "queue", label: homeiiEditorI18n$1("ui.queue_2") },
    { value: "radio", label: homeiiEditorI18n$1("ui.quick_mix") },
    { value: "voice", label: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT") },
    { value: "history", label: homeiiEditorI18n$1("ui.history") },
    { value: "info", label: homeiiEditorI18n$1("ui.info") },
    { value: "disconnect_all", label: homeiiEditorI18n$1("ui.clean_all", {}, HomeiiEditorLocale$1.detectEditorHebrew() ? "נקה הכל" : "Clean all") }
  ];
  return {
    sections: {
      general: homeiiEditorI18n$1("ui.general"),
      appearance: homeiiEditorI18n$1("ui.appearance"),
      behavior: homeiiEditorI18n$1("ui.behavior"),
      connection: homeiiEditorI18n$1("ui.connection_and_behavior"),
      mainbar: homeiiEditorI18n$1("ui.main_bar"),
      quickactions: homeiiEditorI18n$1("ui.quick_actions_2"),
      library: homeiiEditorI18n$1("ui.library_tabs"),
      announcements: homeiiEditorI18n$1("ui.announcements"),
      voice_assistant: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT"),
      players: homeiiEditorI18n$1("ui.players"),
      smart_home: homeiiEditorI18n$1("ui.smart_home")
    },
    labels: {
      settings_source: homeiiEditorI18n$1("ui.settings_source"),
      layout_mode: homeiiEditorI18n$1("ui.layout_mode"),
      main_opacity: homeiiEditorI18n$1("ui.main_opacity"),
      popup_opacity: homeiiEditorI18n$1("ui.popup_opacity"),
      height: homeiiEditorI18n$1("ui.card_height"),
      language: homeiiEditorI18n$1("ui.language"),
      theme_mode: homeiiEditorI18n$1("ui.theme_mode"),
      performance_profile: homeiiEditorI18n$1("ui.performance_profile"),
      performance_mode: homeiiEditorI18n$1("ui.performance_mode_for_weak_devices"),
      night_mode: homeiiEditorI18n$1("ui.night_mode"),
      night_mode_auto_start: homeiiEditorI18n$1("ui.night_start_time"),
      night_mode_auto_end: homeiiEditorI18n$1("ui.night_end_time"),
      night_mode_days: homeiiEditorI18n$1("ui.night_mode_days"),
      rtl: "RTL",
      mobile_custom_color: homeiiEditorI18n$1("ui.accent_color"),
      mobile_dynamic_theme_mode: homeiiEditorI18n$1("ui.dynamic_theme"),
      mobile_background_motion_mode: homeiiEditorI18n$1("ui.background_motion"),
      mobile_custom_text_tone: homeiiEditorI18n$1("ui.text_tone"),
      mobile_font_scale: homeiiEditorI18n$1("ui.font_scale"),
      mobile_icon_scale: homeiiEditorI18n$1("ui.icon_size"),
      mobile_compact_mode: homeiiEditorI18n$1("ui.compact_mode"),
      mobile_compact_widget_mode: homeiiEditorI18n$1("ui.compact_widget_mode", {}, "Compact widget style"),
      mobile_compact_edge_to_edge: homeiiEditorI18n$1("ui.compact_edge_to_edge", {}, "Compact edge-to-edge expand"),
      mobile_layout_mode: homeiiEditorI18n$1("ui.mobile_layout_mode", {}, "Phone layout mode"),
      mobile_swipe_mode: homeiiEditorI18n$1("ui.artwork_swipe"),
      mobile_footer_search_enabled: homeiiEditorI18n$1("ui.footer_search"),
      mobile_mic_mode: homeiiEditorI18n$1("ui.microphone_2"),
      mobile_footer_mode: homeiiEditorI18n$1("ui.footer_style_2"),
      mobile_studio_shortcut: homeiiEditorI18n$1("ui.studio_shortcut"),
      mobile_home_shortcut: homeiiEditorI18n$1("ui.home_shortcut"),
      mobile_home_shortcut_path: homeiiEditorI18n$1("ui.home_shortcut_path"),
      hotel_mode: homeiiEditorI18n$1("ui.hotel_mode", {}, "Hotel Mode"),
      mobile_volume_mode: homeiiEditorI18n$1("ui.volume_mode"),
      mobile_volume_step_buttons: homeiiEditorI18n$1("ui.volume_step_buttons"),
      mobile_volume_step_percent: homeiiEditorI18n$1("ui.volume_step_percent"),
      voice_assistant_enabled: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT"),
      voice_assistant_section: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT"),
      voice_assistant_grid: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT"),
      voice_assistant_mode: homeiiEditorI18n$1("ui.voice_assistant_mode"),
      voice_assistant_agent_id: homeiiEditorI18n$1("ui.assist_agent"),
      voice_assistant_speak_feedback: homeiiEditorI18n$1("ui.voice_feedback"),
      flow_assistant_response_timeout_ms: "Flow Assistant response timeout",
      flow_assistant_listen_timeout_ms: "Flow Assistant listen timeout",
      flow_assistant_auto_close_ms: "Flow Assistant auto close",
      mobile_liked_mode: homeiiEditorI18n$1("ui.liked_mode"),
      mobile_radio_browser_country: homeiiEditorI18n$1("ui.radio_browser_country_2"),
      mobile_main_bar_items: homeiiEditorI18n$1("ui.main_bar_items_2"),
      mobile_quick_actions: homeiiEditorI18n$1("ui.quick_actions"),
      ...quickActionSlotLabels,
      mobile_library_default_layout: homeiiEditorI18n$1("ui.default_library_layout", {}, "Default library layout"),
      mobile_library_tabs: homeiiEditorI18n$1("ui.library_tabs_2"),
      mobile_announcement_presets: homeiiEditorI18n$1("ui.announcement_presets"),
      mobile_announcement_volume: homeiiEditorI18n$1("ui.announcement_volume_boost"),
      announcement_tts_entity: homeiiEditorI18n$1("ui.tts_entity"),
      announcement_tts_language: homeiiEditorI18n$1("ui.tts_language"),
      ambient_light_enabled: homeiiEditorI18n$1("ui.ambient_light"),
      ambient_light_entities: homeiiEditorI18n$1("ui.ambient_light_entities"),
      ambient_light_player_map: homeiiEditorI18n$1("ui.ambient_light_player_map"),
      ambient_light_brightness: homeiiEditorI18n$1("ui.ambient_light_brightness"),
      ambient_light_transition: homeiiEditorI18n$1("ui.ambient_light_transition"),
      ambient_light_cooldown: homeiiEditorI18n$1("ui.ambient_light_cooldown"),
      screensaver_enabled: homeiiEditorI18n$1("ui.screensaver"),
      screensaver_controls_enabled: homeiiEditorI18n$1("ui.screensaver_controls"),
      screensaver_control_buttons: homeiiEditorI18n$1("ui.screensaver_buttons"),
      screensaver_clock_mode: homeiiEditorI18n$1("ui.screensaver_clock_mode"),
      screensaver_timeout_seconds: homeiiEditorI18n$1("ui.screensaver_timeout"),
      screensaver_message: homeiiEditorI18n$1("ui.screensaver_message"),
      screensaver_clock_size: homeiiEditorI18n$1("ui.screensaver_clock_size"),
      screensaver_clock_x: homeiiEditorI18n$1("ui.screensaver_clock_x"),
      screensaver_clock_y: homeiiEditorI18n$1("ui.screensaver_clock_y"),
      power_button_enabled: homeiiEditorI18n$1("ui.power_button"),
      power_button_name: homeiiEditorI18n$1("ui.auxiliary_button_name"),
      power_button_icon: homeiiEditorI18n$1("ui.auxiliary_button_icon"),
      power_button_action: homeiiEditorI18n$1("ui.power_button_action"),
      power_button_entity: homeiiEditorI18n$1("ui.power_button_entity"),
      ...auxiliaryLabels,
      discovery_mode_enabled: homeiiEditorI18n$1("ui.discovery_mode"),
      pinned_player_entities: homeiiEditorI18n$1("ui.pinned_players"),
      excluded_player_entities: homeiiEditorI18n$1("ui.excluded_players"),
      player_sort_mode: homeiiEditorI18n$1("ui.player_sort"),
      player_order_entities: homeiiEditorI18n$1("ui.player_order"),
      ...playerOrderSlotLabels,
      config_entry_id: "Config Entry ID",
      ma_url: homeiiEditorI18n$1("ui.music_assistant_url"),
      music_assistant_external_url: "Music Assistant external URL",
      ma_token: homeiiEditorI18n$1("ui.music_assistant_token"),
      active_player_helper_entity: homeiiEditorI18n$1("ui.active_player_helper"),
      favorite_button_entity: homeiiEditorI18n$1("ui.favorite_button_entity"),
      ma_interface_url: homeiiEditorI18n$1("ui.ma_interface_path"),
      ma_interface_target: homeiiEditorI18n$1("ui.open_interface_in"),
      show_ma_button: homeiiEditorI18n$1("ui.show_ma_button"),
      show_theme_toggle: homeiiEditorI18n$1("ui.show_theme_toggle"),
      cache_ttl: homeiiEditorI18n$1("ui.cache_ttl"),
      music_assistant_timeout_ms: "Music Assistant timeout",
      allow_local_likes: homeiiEditorI18n$1("ui.allow_local_likes"),
      use_mass_queue_send_command: homeiiEditorI18n$1("ui.mass_queue_send_command_fallback")
    },
    helpers: {
      settings_source: homeiiEditorI18n$1("ui.choose_whether_settings_are_controlled_from_the_in_card_ui_or_from_the_c"),
      layout_mode: homeiiEditorI18n$1("ui.auto_chooses_mobile_or_tablet_based_on_actual_width"),
      height: homeiiEditorI18n$1("ui.card_height_in_pixels"),
      theme_mode: homeiiEditorI18n$1("ui.includes_the_custom_theme_mode_from_the_in_card_settings_screen"),
      night_mode: homeiiEditorI18n$1("ui.off_disables_it_on_keeps_it_active_and_auto_follows_the_configured_time"),
      night_mode_auto_start: homeiiEditorI18n$1("ui.recommended_format_hh_mm_such_as_22_00"),
      night_mode_auto_end: homeiiEditorI18n$1("ui.recommended_format_hh_mm_such_as_06_00_crossing_midnight_is_supported"),
      night_mode_days: homeiiEditorI18n$1("ui.choose_which_days_the_auto_night_mode_window_applies_to"),
      mobile_show_up_next: homeiiEditorI18n$1("ui.show_or_hide_the_inline_next_track_row_in_now_playing"),
      hotel_mode: homeiiEditorI18n$1("ui.hotel_mode_helper", {}, "Minimal hotel-safe UI with only player controls, volume, search, artwork browsing, and player selection."),
      mobile_dynamic_theme_mode: homeiiEditorI18n$1("ui.extract_colors_from_the_current_artwork_and_apply_them_to_the_interface"),
      mobile_background_motion_mode: homeiiEditorI18n$1("ui.control_whether_the_card_background_moves_gently_and_how_strong_the_moti"),
      performance_profile: homeiiEditorI18n$1("ui.performance_profile_helper"),
      performance_mode: homeiiEditorI18n$1("ui.turns_off_heavy_visuals_so_the_card_runs_smoother_on_nest_hub_older_tabl"),
      mobile_font_scale: homeiiEditorI18n$1("ui.global_scale_for_every_interface_font_1_is_the_default_size"),
      mobile_icon_scale: homeiiEditorI18n$1("ui.scales_interface_icons_without_changing_button_sizes"),
      mobile_compact_mode: homeiiEditorI18n$1("ui.shows_a_standalone_compact_player_tile_with_artwork_basic_controls_volum"),
      mobile_compact_widget_mode: homeiiEditorI18n$1("ui.choose_when_compact_mode_uses_the_two_row_mobile_widget", {}, "Choose when compact mode uses the smaller two-row mobile widget."),
      mobile_compact_edge_to_edge: homeiiEditorI18n$1("ui.compact_edge_to_edge_helper", {}, "When disabled, compact expand opens as a floating window so Home Assistant navigation remains visible."),
      mobile_layout_mode: homeiiEditorI18n$1("ui.mobile_layout_mode_helper", {}, "Auto uses the available card space. Full keeps the phone player inline and requests a tall Sections slot. Compact always uses the compact tile."),
      mobile_swipe_mode: homeiiEditorI18n$1("ui.choose_whether_artwork_swipe_changes_track_or_browses_covers"),
      mobile_footer_search_enabled: homeiiEditorI18n$1("ui.enable_or_disable_the_footer_search_button"),
      mobile_mic_mode: homeiiEditorI18n$1("ui.matches_the_microphone_setting_from_the_in_card_settings_screen"),
      mobile_footer_mode: homeiiEditorI18n$1("ui.choose_icons_text_or_both_for_the_footer"),
      mobile_studio_shortcut: homeiiEditorI18n$1("ui.enable_or_disable_the_studio_button_in_the_footer_bar"),
      mobile_home_shortcut: homeiiEditorI18n$1("ui.show_home_inside_quick_actions"),
      mobile_home_shortcut_path: homeiiEditorI18n$1("ui.for_example_lovelace_home_or_any_other_home_assistant_path"),
      mobile_volume_mode: homeiiEditorI18n$1("ui.mainly_relevant_on_larger_layouts"),
      mobile_volume_step_buttons: homeiiEditorI18n$1("ui.show_plus_minus_buttons_next_to_the_volume_slider"),
      mobile_volume_step_percent: homeiiEditorI18n$1("ui.volume_step_between_1_and_10_percent_default_5"),
      voice_assistant_enabled: homeiiEditorI18n$1("ui.show_a_push_to_talk_button_for_music_and_assist_commands"),
      voice_assistant_mode: homeiiEditorI18n$1("ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist"),
      voice_assistant_agent_id: homeiiEditorI18n$1("ui.optional_assist_agent_leave_empty_for_home_assistant_default"),
      voice_assistant_speak_feedback: homeiiEditorI18n$1("ui.speak_voice_assistant_responses_out_loud"),
      flow_assistant_response_timeout_ms: "Maximum milliseconds to wait while Flow Assistant processes a captured command.",
      flow_assistant_listen_timeout_ms: "Maximum milliseconds Flow Assistant can wait for speech before showing a clear timeout.",
      flow_assistant_auto_close_ms: "Milliseconds to keep the Flow Assistant result open. Use 0 to keep it open.",
      mobile_liked_mode: homeiiEditorI18n$1("ui.local_mode_is_only_meaningful_when_local_likes_are_enabled"),
      mobile_radio_browser_country: homeiiEditorI18n$1("ui.uses_the_same_base_country_list_shown_in_the_in_card_settings_screen"),
      mobile_main_bar_items: homeiiEditorI18n$1("ui.choose_which_actions_appear_in_the_main_bar"),
      mobile_quick_actions: homeiiEditorI18n$1("ui.choose_which_icons_appear_in_the_quick_action_row"),
      ...quickActionSlotHelpers,
      mobile_library_default_layout: homeiiEditorI18n$1("ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually", {}, "Choose how library pages open. You can still switch Grid/List inside the library."),
      mobile_library_tabs: homeiiEditorI18n$1("ui.choose_which_tabs_are_available_in_the_library_screen"),
      mobile_announcement_presets: homeiiEditorI18n$1("ui.configure_ready_made_announcement_phrases"),
      mobile_announcement_volume: homeiiEditorI18n$1("ui.adds_to_the_current_volume_only_during_announcements_then_restores_the_p"),
      mobile_custom_color: homeiiEditorI18n$1("ui.choose_the_accent_color_for_the_mobile_layout"),
      active_player_helper_entity: homeiiEditorI18n$1("ui.optional_input_text_helper_updated_with_the_active_player_entity_id_for_2"),
      music_assistant_external_url: "Optional HTTPS Music Assistant URL used when Home Assistant is loaded over HTTPS.",
      favorite_button_entity: homeiiEditorI18n$1("ui.optional_entity_used_for_an_external_favorite_button"),
      announcement_tts_entity: homeiiEditorI18n$1("ui.tts_entity_used_by_the_announcement_screen"),
      announcement_tts_language: homeiiEditorI18n$1("ui.auto_leaves_home_assistant_cloud_voice_defaults_untouched_manual_choices"),
      ambient_light_enabled: homeiiEditorI18n$1("ui.sync_selected_lights_to_the_current_artwork_color"),
      ambient_light_entities: homeiiEditorI18n$1("ui.choose_only_the_lights_that_should_follow_the_music"),
      ambient_light_player_map: homeiiEditorI18n$1("ui.ambient_light_player_map_helper"),
      ambient_light_brightness: homeiiEditorI18n$1("ui.maximum_brightness_for_music_lighting"),
      ambient_light_transition: homeiiEditorI18n$1("ui.soft_transition_time_for_music_lighting"),
      ambient_light_cooldown: homeiiEditorI18n$1("ui.minimum_seconds_between_light_updates"),
      screensaver_enabled: homeiiEditorI18n$1("ui.show_a_calm_clock_artwork_display_after_idle_time"),
      screensaver_controls_enabled: homeiiEditorI18n$1("ui.show_previous_next_controls_on_the_screensaver"),
      screensaver_control_buttons: homeiiEditorI18n$1("ui.choose_which_buttons_appear_on_the_screensaver"),
      screensaver_message: homeiiEditorI18n$1("ui.short_optional_message_shown_on_the_screensaver"),
      screensaver_clock_size: homeiiEditorI18n$1("ui.screensaver_clock_size_helper"),
      screensaver_clock_x: homeiiEditorI18n$1("ui.screensaver_clock_x_helper"),
      screensaver_clock_y: homeiiEditorI18n$1("ui.screensaver_clock_y_helper"),
      power_button_enabled: homeiiEditorI18n$1("ui.show_power_button_in_the_player_controls"),
      power_button_name: homeiiEditorI18n$1("ui.optional_name_for_the_auxiliary_button"),
      power_button_icon: homeiiEditorI18n$1("ui.choose_the_auxiliary_button_icon"),
      power_button_entity: homeiiEditorI18n$1("ui.optional_entity_for_the_power_button_leave_empty_to_stop_the_active_player"),
      ...auxiliaryHelpers,
      discovery_mode_enabled: homeiiEditorI18n$1("ui.show_the_fullscreen_discovery_mode_in_actions"),
      pinned_player_entities: homeiiEditorI18n$1("ui.choose_music_assistant_players_only_the_in_card_pinning_list_shows_only"),
      excluded_player_entities: homeiiEditorI18n$1("ui.choose_music_assistant_players_to_hide_from_the_card"),
      player_sort_mode: homeiiEditorI18n$1("ui.choose_how_players_are_sorted"),
      player_order_entities: homeiiEditorI18n$1("ui.set_custom_player_order"),
      ...playerOrderSlotHelpers,
      allow_local_likes: homeiiEditorI18n$1("ui.enables_switching_to_local_liked_mode"),
      music_assistant_timeout_ms: "Maximum milliseconds to wait for Music Assistant service responses before showing an error.",
      use_mass_queue_send_command: homeiiEditorI18n$1("ui.fallback_for_setups_that_need_an_alternate_queue_service_path")
    },
    options: {
      settings_source: [
        { value: "ui", label: homeiiEditorI18n$1("ui.in_card_ui") },
        { value: "card", label: homeiiEditorI18n$1("ui.card_configuration") }
      ],
      layout_mode: [
        { value: "auto", label: "Auto" },
        { value: "mobile", label: homeiiEditorI18n$1("ui.mobile") },
        { value: "tablet", label: homeiiEditorI18n$1("ui.tablet") }
      ],
      language: visibleLanguageOptions(),
      theme_mode: [
        { value: "auto", label: "Auto" },
        { value: "dark", label: homeiiEditorI18n$1("ui.dark") },
        { value: "light", label: homeiiEditorI18n$1("ui.light") },
        { value: "custom", label: homeiiEditorI18n$1("ui.custom") }
      ],
      night_mode: [
        { value: "off", label: homeiiEditorI18n$1("ui.off") },
        { value: "auto", label: "Auto" },
        { value: "on", label: homeiiEditorI18n$1("ui.on") }
      ],
      performance_profile: [
        { value: "full", label: "Full" },
        { value: "high", label: "High" },
        { value: "low", label: "Low" },
        { value: "ultra_lite", label: "Ultra Lite" }
      ],
      night_mode_days: [
        { value: 0, label: homeiiEditorI18n$1("ui.sun") },
        { value: 1, label: homeiiEditorI18n$1("ui.mon") },
        { value: 2, label: homeiiEditorI18n$1("ui.tue") },
        { value: 3, label: homeiiEditorI18n$1("ui.wed") },
        { value: 4, label: homeiiEditorI18n$1("ui.thu") },
        { value: 5, label: homeiiEditorI18n$1("ui.fri") },
        { value: 6, label: homeiiEditorI18n$1("ui.sat") }
      ],
      mobile_custom_text_tone: [
        { value: "light", label: homeiiEditorI18n$1("ui.light") },
        { value: "dark", label: homeiiEditorI18n$1("ui.dark") }
      ],
      mobile_dynamic_theme_mode: [
        { value: "off", label: homeiiEditorI18n$1("ui.off") },
        { value: "auto", label: "Auto" },
        { value: "strong", label: homeiiEditorI18n$1("ui.strong") }
      ],
      mobile_background_motion_mode: [
        { value: "off", label: homeiiEditorI18n$1("ui.off") },
        { value: "subtle", label: homeiiEditorI18n$1("ui.subtle") },
        { value: "strong", label: homeiiEditorI18n$1("ui.strong") },
        { value: "extreme", label: homeiiEditorI18n$1("ui.extreme") }
      ],
      mobile_compact_widget_mode: [
        { value: "auto", label: "Auto" },
        { value: "full", label: homeiiEditorI18n$1("ui.full", {}, "Full") },
        { value: "mini", label: homeiiEditorI18n$1("ui.mini_widget", {}, "Mini widget") }
      ],
      mobile_layout_mode: [
        { value: "auto", label: "Auto" },
        { value: "full", label: homeiiEditorI18n$1("ui.full", {}, "Full") },
        { value: "compact", label: homeiiEditorI18n$1("ui.compact", {}, "Compact") }
      ],
      mobile_swipe_mode: [
        { value: "play", label: homeiiEditorI18n$1("ui.change_track") },
        { value: "browse", label: homeiiEditorI18n$1("ui.browse_covers") }
      ],
      mobile_mic_mode: [
        { value: "on", label: homeiiEditorI18n$1("ui.on") },
        { value: "off", label: homeiiEditorI18n$1("ui.off") },
        { value: "smart", label: homeiiEditorI18n$1("ui.smart") }
      ],
      announcement_tts_language: [
        { value: "auto", label: homeiiEditorI18n$1("ui.auto_cloud_default") },
        { value: "en-US", label: "English (US)" },
        { value: "en-GB", label: "English (UK)" },
        { value: "he-IL", label: homeiiEditorI18n$1("ui.hebrew") },
        { value: "de-DE", label: "Deutsch" },
        { value: "fr-FR", label: "Français" },
        { value: "es-ES", label: "Español" },
        { value: "it-IT", label: "Italiano" }
      ],
      mobile_footer_mode: [
        { value: "icon", label: homeiiEditorI18n$1("ui.icon_only") },
        { value: "text", label: homeiiEditorI18n$1("ui.text_only") },
        { value: "both", label: homeiiEditorI18n$1("ui.icon_plus_text") }
      ],
      mobile_volume_mode: [
        { value: "button", label: homeiiEditorI18n$1("ui.button") },
        { value: "always", label: homeiiEditorI18n$1("ui.always_visible") }
      ],
      voice_assistant_mode: [
        { value: "hybrid", label: homeiiEditorI18n$1("ui.hybrid_music_plus_assist") },
        { value: "music", label: homeiiEditorI18n$1("ui.music_only") },
        { value: "assist", label: homeiiEditorI18n$1("ui.assist_only") }
      ],
      mobile_liked_mode: [
        { value: "ma", label: "Music Assistant" },
        { value: "local", label: homeiiEditorI18n$1("ui.local") }
      ],
      mobile_library_default_layout: [
        { value: "grid", label: homeiiEditorI18n$1("ui.grid") },
        { value: "list", label: homeiiEditorI18n$1("ui.list") }
      ],
      screensaver_clock_mode: [
        { value: "digital", label: homeiiEditorI18n$1("ui.digital") },
        { value: "analog", label: homeiiEditorI18n$1("ui.analog") }
      ],
      screensaver_control_buttons: [
        { value: "previous", label: homeiiEditorI18n$1("ui.previous") },
        { value: "play_pause", label: homeiiEditorI18n$1("ui.play_pause") },
        { value: "next", label: homeiiEditorI18n$1("ui.next") },
        { value: "mute", label: homeiiEditorI18n$1("ui.mute") },
        { value: "power", label: homeiiEditorI18n$1("ui.auxiliary_button") },
        { value: "like", label: homeiiEditorI18n$1("ui.like_2") },
        { value: "lyrics", label: homeiiEditorI18n$1("ui.lyrics") },
        { value: "voice", label: homeiiEditorI18n$1("ui.flow_assistant", {}, "FLOW ASSISTANT") }
      ],
      power_button_action: [
        { value: "stop_player", label: homeiiEditorI18n$1("ui.stop_player") },
        { value: "toggle", label: homeiiEditorI18n$1("ui.toggle") },
        { value: "turn_on", label: homeiiEditorI18n$1("ui.turn_on") },
        { value: "turn_off", label: homeiiEditorI18n$1("ui.turn_off") },
        { value: "scene", label: homeiiEditorI18n$1("ui.scene") },
        { value: "script", label: homeiiEditorI18n$1("ui.script") }
      ],
      auxiliary_button_icons: [
        { value: "power", label: homeiiEditorI18n$1("ui.power") },
        { value: "home", label: homeiiEditorI18n$1("ui.home") },
        { value: "speaker", label: homeiiEditorI18n$1("ui.players") },
        { value: "music_note", label: homeiiEditorI18n$1("ui.music") },
        { value: "wand", label: homeiiEditorI18n$1("ui.surprise_me") },
        { value: "grid", label: homeiiEditorI18n$1("ui.actions_2") },
        { value: "settings", label: homeiiEditorI18n$1("ui.settings") },
        { value: "heart_outline", label: homeiiEditorI18n$1("ui.like_2") },
        { value: "play", label: homeiiEditorI18n$1("ui.play") },
        { value: "stop", label: homeiiEditorI18n$1("ui.stop_all") },
        { value: "radio", label: homeiiEditorI18n$1("ui.radio") },
        { value: "timer", label: homeiiEditorI18n$1("ui.timer") },
        { value: "info", label: homeiiEditorI18n$1("ui.info") }
      ],
      player_sort_mode: [
        { value: "default", label: homeiiEditorI18n$1("ui.default_order") },
        { value: "alphabetical", label: homeiiEditorI18n$1("ui.alphabetical") },
        { value: "custom", label: homeiiEditorI18n$1("ui.custom_order") }
      ],
      ma_interface_target: [
        { value: "_self", label: "_self" },
        { value: "_blank", label: "_blank" }
      ],
      mobile_main_bar_items: [
        { value: "search", label: homeiiEditorI18n$1("ui.search") },
        { value: "library", label: homeiiEditorI18n$1("ui.library_2") },
        { value: "players", label: homeiiEditorI18n$1("ui.players") },
        { value: "actions", label: homeiiEditorI18n$1("ui.actions_2") },
        { value: "settings", label: homeiiEditorI18n$1("ui.settings") },
        { value: "theme", label: homeiiEditorI18n$1("ui.theme_toggle_2") }
      ],
      mobile_quick_actions: quickActionOptions,
      mobile_quick_action_slots: [
        { value: "", label: homeiiEditorI18n$1("ui.none") },
        ...quickActionOptions
      ],
      mobile_library_tabs: [
        { value: "library_search", label: homeiiEditorI18n$1("ui.search") },
        { value: "library_playlists", label: homeiiEditorI18n$1("ui.playlists") },
        { value: "library_artists", label: homeiiEditorI18n$1("ui.artists") },
        { value: "library_albums", label: homeiiEditorI18n$1("ui.albums") },
        { value: "library_tracks", label: homeiiEditorI18n$1("ui.tracks") },
        { value: "library_radio", label: homeiiEditorI18n$1("ui.radio") },
        { value: "library_podcasts", label: homeiiEditorI18n$1("ui.podcasts") },
        { value: "library_liked", label: homeiiEditorI18n$1("ui.liked") }
      ]
    }
  };
}
function getRadioBrowserCountrySelectorOptions$1(translateFn = homeiiEditorI18n$1, language = "") {
  const lang = language || (HomeiiEditorLocale$1.detectEditorHebrew() ? "he" : "en");
  return homeiiRadioBrowserCountrySelectorOptions(translateFn, lang);
}
function getMobileCardConfigForm$1() {
  const t = getMobileEditorTexts();
  return {
    schema: [
      {
        type: "expandable",
        name: "general_section",
        title: t.sections.general,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "general_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "settings_source", selector: { select: { mode: "dropdown", options: t.options.settings_source } } },
              { name: "layout_mode", selector: { select: { mode: "dropdown", options: t.options.layout_mode } } },
              { name: "height", selector: { number: { min: 280, max: 1800, step: 10, mode: "box" } } },
              { name: "language", selector: { select: { mode: "dropdown", options: t.options.language } } },
              { name: "mobile_show_up_next", selector: { boolean: {} } },
              { name: "rtl", selector: { boolean: {} } },
              { name: "hotel_mode", selector: { boolean: {} } },
              { name: "mobile_compact_mode", selector: { boolean: {} } },
              { name: "mobile_layout_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_layout_mode } } },
              { name: "mobile_compact_widget_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_compact_widget_mode } } },
              { name: "pinned_player_entities", selector: { entity: { multiple: true, filter: [{ integration: "music_assistant", domain: "media_player" }] } } },
              { name: "excluded_player_entities", selector: { entity: { multiple: true, filter: [{ integration: "music_assistant", domain: "media_player" }] } } },
              { name: "player_sort_mode", selector: { select: { mode: "dropdown", options: t.options.player_sort_mode } } }
            ]
          },
          {
            type: "grid",
            name: "player_order_grid",
            flatten: true,
            column_min_width: "220px",
            schema: []
          }
        ]
      },
      {
        type: "expandable",
        name: "appearance_section",
        title: t.sections.appearance,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "appearance_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "theme_mode", selector: { select: { mode: "dropdown", options: t.options.theme_mode } } },
              { name: "night_mode", selector: { select: { mode: "dropdown", options: t.options.night_mode } } },
              { name: "main_opacity", selector: { number: { min: 0.3, max: 1, step: 0.02, mode: "slider" } } },
              { name: "popup_opacity", selector: { number: { min: 0.4, max: 1, step: 0.02, mode: "slider" } } },
              { name: "mobile_custom_color", selector: { text: { type: "color" } } },
              { name: "performance_profile", selector: { select: { mode: "dropdown", options: t.options.performance_profile } } },
              { name: "mobile_dynamic_theme_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_dynamic_theme_mode } } },
              { name: "mobile_background_motion_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_background_motion_mode } } },
              { name: "mobile_custom_text_tone", selector: { select: { mode: "dropdown", options: t.options.mobile_custom_text_tone } } },
              { name: "mobile_font_scale", selector: { number: { min: 0.5, max: 1.5, step: 0.05, mode: "slider" } } },
              { name: "mobile_icon_scale", selector: { number: { min: 0.8, max: 1.25, step: 0.05, mode: "slider" } } },
              { name: "mobile_footer_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_footer_mode } } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "behavior_section",
        title: t.sections.behavior,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "behavior_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "mobile_swipe_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_swipe_mode } } },
              { name: "mobile_mic_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_mic_mode } } },
              { name: "mobile_home_shortcut", selector: { boolean: {} } },
              { name: "mobile_home_shortcut_path", selector: { text: {} } },
              { name: "mobile_volume_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_volume_mode } } },
              { name: "mobile_volume_step_buttons", selector: { boolean: {} } },
              { name: "mobile_volume_step_percent", selector: { number: { min: 1, max: 10, step: 1, mode: "slider" } } },
              { name: "mobile_liked_mode", selector: { select: { mode: "dropdown", options: t.options.mobile_liked_mode } } },
              { name: "mobile_radio_browser_country", selector: { select: { mode: "dropdown", options: getRadioBrowserCountrySelectorOptions$1() } } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "connection_section",
        title: t.sections.connection,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "connection_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "config_entry_id", selector: { text: {} } },
              { name: "ma_url", selector: { text: { type: "url" } } },
              { name: "music_assistant_external_url", selector: { text: { type: "url" } } },
              { name: "ma_token", selector: { text: {} } },
              { name: "active_player_helper_entity", selector: { entity: { multiple: false, filter: [{ domain: "input_text" }] } } },
              { name: "favorite_button_entity", selector: { entity: { multiple: false } } },
              { name: "ma_interface_url", selector: { text: {} } },
              { name: "ma_interface_target", selector: { select: { mode: "dropdown", options: t.options.ma_interface_target } } },
              { name: "show_ma_button", selector: { boolean: {} } },
              { name: "show_theme_toggle", selector: { boolean: {} } },
              { name: "cache_ttl", selector: { number: { min: 0, max: 36e5, step: 1e3, mode: "box" } } },
              { name: "music_assistant_timeout_ms", selector: { number: { min: 3e3, max: 6e4, step: 1e3, mode: "box" } } },
              { name: "allow_local_likes", selector: { boolean: {} } },
              { name: "use_mass_queue_send_command", selector: { boolean: {} } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "voice_assistant_section",
        title: t.sections.voice_assistant,
        helper: t.helpers.voice_assistant_enabled,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "voice_assistant_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "voice_assistant_enabled", label: t.labels.voice_assistant_enabled, helper: t.helpers.voice_assistant_enabled, selector: { boolean: {} } },
              { name: "voice_assistant_mode", label: t.labels.voice_assistant_mode, helper: t.helpers.voice_assistant_mode, selector: { select: { mode: "dropdown", options: t.options.voice_assistant_mode } } },
              { name: "voice_assistant_agent_id", label: t.labels.voice_assistant_agent_id, helper: t.helpers.voice_assistant_agent_id, selector: { entity: { multiple: false, filter: [{ domain: "conversation" }] } } },
              { name: "voice_assistant_speak_feedback", label: t.labels.voice_assistant_speak_feedback, helper: t.helpers.voice_assistant_speak_feedback, selector: { boolean: {} } },
              { name: "flow_assistant_response_timeout_ms", label: t.labels.flow_assistant_response_timeout_ms, helper: t.helpers.flow_assistant_response_timeout_ms, selector: { number: { min: 5e3, max: 6e4, step: 1e3, mode: "box" } } },
              { name: "flow_assistant_listen_timeout_ms", label: t.labels.flow_assistant_listen_timeout_ms, helper: t.helpers.flow_assistant_listen_timeout_ms, selector: { number: { min: 5e3, max: 3e4, step: 1e3, mode: "box" } } },
              { name: "flow_assistant_auto_close_ms", label: t.labels.flow_assistant_auto_close_ms, helper: t.helpers.flow_assistant_auto_close_ms, selector: { number: { min: 0, max: 3e4, step: 500, mode: "box" } } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "smart_home_section",
        title: t.sections.smart_home,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "smart_home_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "ambient_light_enabled", selector: { boolean: {} } },
              { name: "ambient_light_entities", selector: { entity: { multiple: true, filter: [{ domain: "light" }] } } },
              { name: "ambient_light_player_map", selector: { text: { multiple: true } } },
              { name: "ambient_light_brightness", selector: { number: { min: 1, max: 100, step: 1, mode: "slider" } } },
              { name: "ambient_light_transition", selector: { number: { min: 0, max: 120, step: 1, mode: "box" } } },
              { name: "ambient_light_cooldown", selector: { number: { min: 0, max: 120, step: 1, mode: "box" } } },
              { name: "screensaver_enabled", selector: { boolean: {} } },
              { name: "screensaver_controls_enabled", selector: { boolean: {} } },
              { name: "screensaver_control_buttons", selector: { select: { multiple: true, mode: "list", options: t.options.screensaver_control_buttons } } },
              { name: "screensaver_clock_mode", selector: { select: { mode: "dropdown", options: t.options.screensaver_clock_mode } } },
              { name: "screensaver_clock_size", selector: { number: { min: 0.75, max: 1.45, step: 0.05, mode: "slider" } } },
              { name: "screensaver_clock_x", selector: { number: { min: 8, max: 92, step: 1, mode: "slider" } } },
              { name: "screensaver_clock_y", selector: { number: { min: 8, max: 70, step: 1, mode: "slider" } } },
              { name: "screensaver_timeout_seconds", selector: { number: { min: 15, max: 3600, step: 5, mode: "box" } } },
              { name: "screensaver_message", selector: { text: {} } },
              { name: "power_button_enabled", selector: { boolean: {} } },
              { name: "power_button_name", selector: { text: {} } },
              { name: "power_button_icon", selector: { icon: {} } },
              { name: "power_button_action", selector: { select: { mode: "dropdown", options: t.options.power_button_action } } },
              { name: "power_button_entity", selector: { entity: { multiple: false } } },
              ...Array.from({ length: 3 }, (_, offset) => {
                const index = offset + 2;
                return [
                  { name: `aux_button_${index}_enabled`, selector: { boolean: {} } },
                  { name: `aux_button_${index}_name`, selector: { text: {} } },
                  { name: `aux_button_${index}_icon`, selector: { icon: {} } },
                  { name: `aux_button_${index}_action`, selector: { select: { mode: "dropdown", options: t.options.power_button_action } } },
                  { name: `aux_button_${index}_entity`, selector: { entity: { multiple: false } } }
                ];
              }).flat(),
              { name: "discovery_mode_enabled", selector: { boolean: {} } }
            ]
          }
        ]
      },
      {
        type: "expandable",
        name: "mainbar_section",
        title: t.sections.mainbar,
        flatten: true,
        schema: [
          {
            name: "mobile_main_bar_items",
            selector: {
              select: {
                multiple: true,
                mode: "list",
                options: t.options.mobile_main_bar_items
              }
            }
          },
          { name: "mobile_studio_shortcut", selector: { boolean: {} } }
        ]
      },
      {
        type: "expandable",
        name: "quickactions_section",
        title: t.sections.quickactions,
        flatten: true,
        schema: [
          {
            name: "mobile_quick_actions",
            selector: {
              select: {
                multiple: true,
                mode: "list",
                options: t.options.mobile_quick_actions
              }
            }
          },
          {
            type: "grid",
            name: "quickactions_order_grid",
            flatten: true,
            column_min_width: "180px",
            schema: Array.from({ length: 10 }, (_, index) => ({
              name: `mobile_quick_action_${index + 1}`,
              selector: {
                select: {
                  mode: "dropdown",
                  options: t.options.mobile_quick_action_slots
                }
              }
            }))
          }
        ]
      },
      {
        type: "expandable",
        name: "library_section",
        title: t.sections.library,
        flatten: true,
        schema: [
          { name: "mobile_library_default_layout", selector: { select: { mode: "dropdown", options: t.options.mobile_library_default_layout } } },
          {
            name: "mobile_library_tabs",
            selector: {
              select: {
                multiple: true,
                mode: "list",
                options: t.options.mobile_library_tabs
              }
            }
          }
        ]
      },
      {
        type: "expandable",
        name: "announcements_section",
        title: t.sections.announcements,
        flatten: true,
        schema: [
          {
            type: "grid",
            name: "announcements_grid",
            flatten: true,
            column_min_width: "220px",
            schema: [
              { name: "mobile_announcement_presets", selector: { text: { multiple: true } } },
              { name: "mobile_announcement_volume", selector: { number: { min: 20, max: 50, step: 1, mode: "slider" } } },
              { name: "announcement_tts_entity", selector: { entity: { multiple: false } } },
              { name: "announcement_tts_language", selector: { select: { mode: "dropdown", options: t.options.announcement_tts_language } } }
            ]
          }
        ]
      }
    ],
    computeLabel: (schema) => homeiiEditorLabelFor$1(schema, t.labels),
    computeHelper: (schema) => homeiiEditorHelperFor$1(schema, t.helpers),
    assertConfig: (config) => {
      if (!config || typeof config !== "object" || Array.isArray(config)) {
        throw new Error("Card config must be an object");
      }
    }
  };
}
function createHomeiiBaseMusicEditor(deps = {}) {
  const {
    HomeiiBaseMusicCard: HomeiiBaseMusicCard2,
    ensureHaEditorComponents: ensureHaEditorComponents2,
    homeiiIsRtlLanguage,
    homeiiDetectLanguage,
    HomeiiConfigValidators: HomeiiConfigValidators2,
    HomeiiPlayersFoundation: HomeiiPlayersFoundation2,
    HomeiiMobileSettingsFoundation: HomeiiMobileSettingsFoundation2,
    homeiiEditorI18n: homeiiEditorI18n2,
    homeiiEditorLabelFor: homeiiEditorLabelFor2,
    homeiiEditorHelperFor: homeiiEditorHelperFor2,
    HOMEII_CARD_VERSION: HOMEII_CARD_VERSION2,
    AMBIENT_LIGHT_PAIR_PLAYER_PREFIX: AMBIENT_LIGHT_PAIR_PLAYER_PREFIX2,
    AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX: AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX2
  } = deps;
  return class HomeiiBaseMusicEditor extends HTMLElement {
    constructor() {
      super();
      this._config = HomeiiBaseMusicCard2.getStubConfig();
      this._hass = null;
      this._editorRoot = null;
      this._editorForm = null;
      this._editorUsePathBtn = null;
      this._editorPathHint = null;
      this._editorSponsorLink = null;
      this._editorAmbientLightDraft = { player: "", lights: [] };
      this._editorBound = false;
      this._editorLastConfigKey = "";
      this._editorLastSchemaKey = "";
    }
    connectedCallback() {
      this.style.display = "block";
      ensureHaEditorComponents2();
      this._ensureEditorShell();
      this._render();
    }
    set hass(hass) {
      const previousLang = String(this._hass?.locale?.language || this._hass?.language || "");
      const previousPinnedOptionsKey = this._editorPinnedPlayerOptionsKey();
      this._hass = hass;
      this._ensureEditorShell();
      const nextLang = String(this._hass?.locale?.language || this._hass?.language || "");
      const nextPinnedOptionsKey = this._editorPinnedPlayerOptionsKey();
      if (previousLang !== nextLang || previousPinnedOptionsKey !== nextPinnedOptionsKey || !this._editorRoot || !this._editorForm) {
        this._render();
        return;
      }
      this._syncEditorLiveContext();
    }
    setConfig(config) {
      const nextConfig = {
        ...this._getCardCtor().getStubConfig(),
        ...config
      };
      const validator = this._getConfigValidator?.();
      if (typeof validator === "function") {
        validator(nextConfig);
      }
      this._config = nextConfig;
      this._ensureEditorShell();
      this._render();
    }
    _getCardCtor() {
      return HomeiiBaseMusicCard2;
    }
    _isHebrew() {
      return homeiiIsRtlLanguage(homeiiDetectLanguage({
        configLanguage: this._config?.language || "en",
        hass: this._hass
      }));
    }
    _esc(value) {
      return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
    }
    _getConfigValidator() {
      return HomeiiConfigValidators2.validateBaseCardEditorConfig;
    }
    _dispatchConfig() {
      this.dispatchEvent(new CustomEvent("config-changed", {
        detail: { config: this._config },
        bubbles: true,
        composed: true
      }));
    }
    _currentUiPath() {
      try {
        const pathname = window.location?.pathname || "/";
        const search = window.location?.search || "";
        const hash = window.location?.hash || "";
        return `${pathname}${search}${hash}` || "/";
      } catch (_) {
        return "/";
      }
    }
    _editorSponsorTitle() {
      return this._isHebrew() ? "תמיכה ב-homeii-music-flow" : "Sponsor homeii-music-flow";
    }
    _editorSponsorConfirmMessage() {
      return this._isHebrew() ? "לפתוח את עמוד התמיכה ב-GitHub Sponsors?" : "Open the GitHub Sponsors page?";
    }
    _syncEditorSponsorLabels() {
      if (!this._editorSponsorLink) return;
      const label = this._editorSponsorTitle();
      this._editorSponsorLink.setAttribute("title", label);
      this._editorSponsorLink.setAttribute("aria-label", label);
    }
    _ensureEditorShell() {
      if (this._editorRoot) return;
      const root = this.attachShadow({ mode: "open" });
      root.innerHTML = `
      <style>
        :host {
          display:block;
          direction:${this._isHebrew() ? "rtl" : "ltr"};
        }
        .editor-shell {
          display:grid;
          gap:14px;
          padding:16px;
          border-radius:20px;
          border:1px solid rgba(146,161,183,.18);
          background:rgba(18,24,34,.04);
          contain:layout style paint;
        }
        .editor-header {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          min-height:40px;
          padding:0 2px 2px;
        }
        .editor-title {
          min-width:0;
          font-size:16px;
          line-height:1.2;
          font-weight:800;
          color:var(--primary-text-color, #1f2633);
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .editor-actions {
          flex:0 0 auto;
          display:flex;
          align-items:center;
          gap:8px;
        }
        .editor-sponsor {
          width:30px;
          height:30px;
          display:inline-grid;
          place-items:center;
          border-radius:999px;
          color:var(--secondary-text-color, rgba(31,38,51,.62));
          background:rgba(146,161,183,.08);
          border:1px solid rgba(146,161,183,.14);
          text-decoration:none;
          transition:background .16s ease, border-color .16s ease, color .16s ease, transform .16s ease;
        }
        .editor-sponsor:hover {
          color:#d14d72;
          background:rgba(209,77,114,.09);
          border-color:rgba(209,77,114,.22);
          transform:translateY(-1px);
        }
        .editor-sponsor:focus-visible {
          outline:2px solid color-mix(in srgb, var(--primary-color, #d14d72) 56%, transparent);
          outline-offset:2px;
        }
        .editor-sponsor svg {
          width:16px;
          height:16px;
          display:block;
        }
        .editor-version {
          flex:0 0 auto;
          border-radius:999px;
          padding:5px 10px;
          font-size:12px;
          line-height:1;
          font-weight:800;
          color:var(--secondary-text-color, rgba(31,38,51,.68));
          background:rgba(146,161,183,.12);
          border:1px solid rgba(146,161,183,.18);
        }
        .editor-shell.tablet-stable {
          background:rgba(18,24,34,.08);
          border-color:rgba(146,161,183,.12);
        }
        ha-form {
          display:block;
        }
      </style>
      <div class="editor-shell">
        <div class="editor-header">
          <div class="editor-title">homeii-music-flow</div>
          <div class="editor-actions">
            <a
              class="editor-sponsor"
              href="https://github.com/sponsors/r11a"
              target="_blank"
              rel="noopener noreferrer"
              title="${this._esc(this._editorSponsorTitle())}"
              aria-label="${this._esc(this._editorSponsorTitle())}"
            >
              <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
                <path
                  d="M20.8 4.6c-1.8-1.7-4.7-1.6-6.4.2L12 7.3 9.6 4.8C7.9 3 5 2.9 3.2 4.6 1.2 6.5 1.1 9.6 3 11.6l8.2 8.5c.4.4 1.1.4 1.5 0l8.2-8.5c2-2 1.9-5.1-.1-7Z"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                ></path>
              </svg>
            </a>
            <div class="editor-version">v${HOMEII_CARD_VERSION2}</div>
          </div>
        </div>
        <ha-form id="editorForm"></ha-form>
      </div>
    `;
      this._editorRoot = root;
      this._editorForm = root.querySelector("#editorForm");
      this._editorUsePathBtn = root.querySelector("#editorUseCurrentPath");
      this._editorPathHint = root.querySelector("#editorPathHint");
      this._editorSponsorLink = root.querySelector(".editor-sponsor");
      this._syncEditorSponsorLabels();
      this._refreshEditorShellClasses();
      if (!this._editorBound) {
        this._editorBound = true;
        this._editorSponsorLink?.addEventListener("click", (event) => {
          let confirmed = true;
          try {
            if (typeof window.confirm === "function") {
              confirmed = window.confirm(this._editorSponsorConfirmMessage());
            }
          } catch (_) {
          }
          if (!confirmed) event.preventDefault?.();
        });
        this._editorUsePathBtn?.addEventListener("click", () => {
          const currentPath = this._currentUiPath();
          this._config = {
            ...this._getCardCtor().getStubConfig(),
            ...this._config,
            mobile_home_shortcut_path: currentPath
          };
          this._render();
          this._dispatchConfig();
        });
        this._editorForm?.addEventListener("value-changed", (event) => {
          const nextValue = event.detail?.value;
          if (!nextValue || typeof nextValue !== "object") return;
          const nextConfigValue = this._normalizeEditorFormValue(nextValue);
          const previousSchemaKey = this._editorLastSchemaKey;
          this._config = {
            ...this._getCardCtor().getStubConfig(),
            ...this._config,
            ...nextConfigValue
          };
          this._dispatchConfig();
          const nextSchemaKey = JSON.stringify(this._withDynamicEditorSchema(this._currentBaseEditorSchema()));
          if (previousSchemaKey !== nextSchemaKey) queueMicrotask(() => this._render());
        });
      }
    }
    _isTabletStabilityMode() {
      try {
        const ua = String(window.navigator?.userAgent || "");
        const width = Math.max(
          Number(window.innerWidth || 0),
          Number(this.getBoundingClientRect?.().width || 0),
          Number(this.offsetWidth || 0)
        );
        const touchPoints = Number(window.navigator?.maxTouchPoints || 0);
        return /Android/i.test(ua) && width >= 980 && touchPoints > 0;
      } catch (_) {
        return false;
      }
    }
    _refreshEditorShellClasses() {
      const shell = this._editorRoot?.querySelector(".editor-shell");
      if (!shell) return;
      shell.classList.toggle("tablet-stable", this._isTabletStabilityMode());
    }
    _syncEditorLiveContext() {
      this._ensureEditorShell();
      this.style.direction = this._isHebrew() ? "rtl" : "ltr";
      this._refreshEditorShellClasses();
      this._syncEditorSponsorLabels();
      if (this._editorUsePathBtn) {
        this._editorUsePathBtn.textContent = homeiiEditorI18n2("ui.use_current_view_for_home_button");
      }
      if (this._editorPathHint) {
        this._editorPathHint.textContent = `${homeiiEditorI18n2("ui.current_path")}: ${this._currentUiPath()}`;
      }
      if (this._editorForm) {
        this._editorForm.hass = this._hass;
      }
    }
    _editorPinnedPlayerOptions() {
      const states = this._hass?.states || {};
      const entities = this._hass?.entities || {};
      const mediaPlayers = Object.values(states).filter((entity) => entity?.entity_id?.startsWith("media_player."));
      const musicAssistantPlayers = mediaPlayers.filter((entity) => {
        const registry = entities?.[entity.entity_id] || {};
        const registryText = [
          registry.platform,
          registry.integration,
          registry.device_class
        ].filter(Boolean).join(" ").toLowerCase();
        return HomeiiPlayersFoundation2.isMusicAssistantPlayer(entity, registry) || registryText.includes("music_assistant") || registryText.includes("music assistant");
      });
      const hasMusicAssistantBackend = !!this._hass?.services?.music_assistant?.play_media || !!String(this._config?.ma_url || this._config?.config_entry_id || "").trim();
      const options = (musicAssistantPlayers.length || !hasMusicAssistantBackend ? musicAssistantPlayers : mediaPlayers).filter((entity) => !HomeiiPlayersFoundation2.isLikelyBrowserPlayer(entity)).map((entity) => ({
        value: entity.entity_id,
        label: entity.attributes?.friendly_name || entity.entity_id
      })).sort((left, right) => String(left.label).localeCompare(String(right.label), void 0, { sensitivity: "base" }));
      return options;
    }
    _editorPinnedPlayerOptionsKey() {
      return JSON.stringify({
        players: this._editorPinnedPlayerOptions(),
        lights: this._editorColorLightOptions(),
        mappings: HomeiiMobileSettingsFoundation2.normalizeStringArray(this._config?.ambient_light_player_map),
        draft: this._editorAmbientLightDraft,
        excludedPlayers: HomeiiMobileSettingsFoundation2.normalizePinnedPlayerEntityList(this._config?.excluded_player_entities)
      });
    }
    _editorPlayerOrderOptions() {
      const excluded = new Set(HomeiiMobileSettingsFoundation2.normalizePinnedPlayerEntityList(this._config?.excluded_player_entities));
      return this._editorPinnedPlayerOptions().filter((option) => !excluded.has(option.value));
    }
    _playerOrderEntitySelector({ entities = [] } = {}) {
      const selector = {
        entity: {
          multiple: false,
          filter: [{ integration: "music_assistant", domain: "media_player" }]
        }
      };
      if (entities.length) selector.entity.include_entities = entities.map((option) => option.value);
      return selector;
    }
    _editorColorLightOptions() {
      const states = this._hass?.states || {};
      return Object.values(states).filter((entity) => entity?.entity_id?.startsWith("light.")).filter((entity) => HomeiiMobileSettingsFoundation2.isColorCapableLightEntity(entity)).map((entity) => ({
        value: entity.entity_id,
        label: entity.attributes?.friendly_name || entity.entity_id
      })).sort((left, right) => String(left.label).localeCompare(String(right.label), void 0, { sensitivity: "base" }));
    }
    _ambientLightPairPlayerKey(index) {
      return `${AMBIENT_LIGHT_PAIR_PLAYER_PREFIX2}${index}`;
    }
    _ambientLightPairLightsKey(index) {
      return `${AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX2}${index}`;
    }
    _ambientLightPairDisplayKey(type = "player", index = 0) {
      const pairIndex = Math.max(0, Number(index) || 0);
      const internalName = type === "lights" ? this._ambientLightPairLightsKey(pairIndex) : this._ambientLightPairPlayerKey(pairIndex);
      const label = this._editorDynamicLabel({ name: internalName });
      if (label) return label;
      const number = pairIndex + 1;
      return type === "lights" ? `Color lights ${number}` : `Music Assistant player ${number}`;
    }
    _ambientLightPairFieldInfo(source = "") {
      const candidates = [];
      const addCandidate = (value) => {
        if (value == null) return;
        if (Array.isArray(value)) {
          value.forEach(addCandidate);
          candidates.push(value.join("."));
          return;
        }
        if (typeof value === "object") {
          addCandidate(value.name);
          addCandidate(value.key);
          addCandidate(value.path);
          addCandidate(value.schema?.name);
          return;
        }
        candidates.push(String(value));
      };
      addCandidate(source);
      const matchPairField = (value, prefix, type) => {
        const offset = String(value || "").indexOf(prefix);
        if (offset < 0) return null;
        const match = String(value || "").slice(offset + prefix.length).match(/^\d+/);
        if (!match) return null;
        const index = Number(match[0]);
        return Number.isInteger(index) ? { type, index } : null;
      };
      for (const value of candidates) {
        const playerInfo = matchPairField(value, AMBIENT_LIGHT_PAIR_PLAYER_PREFIX2, "player");
        if (playerInfo) return playerInfo;
        const lightsInfo = matchPairField(value, AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX2, "lights");
        if (lightsInfo) return lightsInfo;
        const displayValue = String(value || "").trim();
        const displayMatch = displayValue.match(/(\d+)\s*$/);
        if (displayMatch) {
          const index = Number(displayMatch[1]) - 1;
          if (Number.isInteger(index) && index >= 0) {
            const lower = displayValue.toLowerCase();
            if (lower.includes("player") || lower.includes("נגן")) return { type: "player", index };
            if (lower.includes("light") || lower.includes("licht") || lower.includes("תאור")) return { type: "lights", index };
          }
        }
      }
      return null;
    }
    _editorSchemaItem(item = {}) {
      if (typeof item === "string") return { name: item };
      if (Array.isArray(item)) return { name: item[item.length - 1] || item.join(".") };
      return item && typeof item === "object" ? item : {};
    }
    _ambientLightPlayerMapConfigFromGroups(groups = []) {
      return groups.map((group) => HomeiiMobileSettingsFoundation2.formatAmbientLightPlayerMapEntry(group.player, group.lights)).filter(Boolean);
    }
    _ambientLightPairSelectorData(config = this._config) {
      const data = {};
      const groups = HomeiiMobileSettingsFoundation2.parseAmbientLightPlayerMap(config?.ambient_light_player_map);
      groups.forEach((group, index) => {
        data[this._ambientLightPairDisplayKey("player", index)] = group.player;
        data[this._ambientLightPairDisplayKey("lights", index)] = group.lights;
      });
      const draftIndex = groups.length;
      data[this._ambientLightPairDisplayKey("player", draftIndex)] = this._editorAmbientLightDraft?.player || "";
      data[this._ambientLightPairDisplayKey("lights", draftIndex)] = this._editorAmbientLightDraft?.lights || [];
      return data;
    }
    _editorFormData(config = this._config) {
      return {
        ...config,
        ...this._ambientLightPairSelectorData(config)
      };
    }
    _normalizeEditorFormValue(value = {}) {
      const next = { ...value };
      this._normalizeEditorQuickActionsValue(next);
      this._normalizeEditorPlayerOrderValue(next);
      const pairIndices = /* @__PURE__ */ new Set();
      Object.keys(next).forEach((key) => {
        const info = this._ambientLightPairFieldInfo(key);
        if (info) pairIndices.add(info.index);
      });
      if (!pairIndices.size) return next;
      const currentPairCount = HomeiiMobileSettingsFoundation2.parseAmbientLightPlayerMap(this._config?.ambient_light_player_map).length;
      const groups = [];
      let draft = { player: "", lights: [] };
      Array.from(pairIndices).sort((left, right) => left - right).forEach((index) => {
        const playerKey = this._ambientLightPairPlayerKey(index);
        const lightsKey = this._ambientLightPairLightsKey(index);
        const playerDisplayKey = this._ambientLightPairDisplayKey("player", index);
        const lightsDisplayKey = this._ambientLightPairDisplayKey("lights", index);
        const player = String((next[playerDisplayKey] ?? next[playerKey]) || "").trim();
        const lights = HomeiiMobileSettingsFoundation2.normalizeEntityList(next[lightsDisplayKey] ?? next[lightsKey]);
        delete next[playerDisplayKey];
        delete next[lightsDisplayKey];
        delete next[playerKey];
        delete next[lightsKey];
        if (player && lights.length) {
          groups.push({ player, lights });
        } else if (index >= currentPairCount && (player || lights.length)) {
          draft = { player, lights };
        }
      });
      this._editorAmbientLightDraft = draft;
      next.ambient_light_player_map = this._ambientLightPlayerMapConfigFromGroups(groups);
      return next;
    }
    _normalizeEditorQuickActionsValue(next = {}) {
      if (!Object.prototype.hasOwnProperty.call(next, "mobile_quick_actions")) return next;
      const selected = HomeiiMobileSettingsFoundation2.normalizeMobileQuickActions(next.mobile_quick_actions, []);
      next.mobile_quick_actions = selected;
      const selectedSet = new Set(selected);
      for (let index = 1; index <= 10; index += 1) {
        const key = `mobile_quick_action_${index}`;
        if (!Object.prototype.hasOwnProperty.call(next, key)) continue;
        const value = String(next[key] || "").trim();
        if (value && !selectedSet.has(value)) next[key] = "";
      }
      return next;
    }
    _normalizeEditorPlayerOrderValue(next = {}) {
      const hasPlayerOrderFields = Object.keys(next).some((key) => /^player_order_entity_\d+$/.test(key));
      if (!hasPlayerOrderFields) return next;
      const rowCount = this._playerOrderSchema()?.schema?.length || 0;
      const ordered = [];
      Object.keys(next).forEach((key) => {
        const match = /^player_order_entity_(\d+)$/.exec(key);
        if (!match) return;
        const index = Number(match[1]) || 0;
        if (!rowCount || index > rowCount) {
          delete next[key];
          return;
        }
        const entityId = String(next[key] || "").trim();
        if (entityId && !ordered.includes(entityId)) ordered.push(entityId);
      });
      next.player_order_entities = ordered;
      return next;
    }
    _ambientLightEntitySelector({ multiple = false, entities = [], fallbackFilter = [] } = {}) {
      const selector = {
        entity: {
          multiple,
          filter: fallbackFilter
        }
      };
      selector.entity.include_entities = entities.map((option) => option.value);
      return selector;
    }
    _playerOrderSchema() {
      const options = this._editorPlayerOrderOptions();
      const configuredOrder = HomeiiMobileSettingsFoundation2.normalizePlayerOrderEntities(this._config || {});
      const rowCount = Math.max(options.length, Math.min(configuredOrder.length, options.length || configuredOrder.length));
      if (!rowCount) return null;
      return {
        type: "grid",
        name: "player_order_grid",
        flatten: true,
        column_min_width: "220px",
        schema: Array.from({ length: rowCount }, (_, index) => ({
          name: `player_order_entity_${index + 1}`,
          selector: this._playerOrderEntitySelector({ entities: options })
        }))
      };
    }
    _ambientLightPlayerPairSchema() {
      const groups = HomeiiMobileSettingsFoundation2.parseAmbientLightPlayerMap(this._config?.ambient_light_player_map);
      const pairCount = Math.max(1, groups.length + 1);
      const players = this._editorPinnedPlayerOptions();
      const lights = this._editorColorLightOptions();
      const schema = [];
      for (let index = 0; index < pairCount; index += 1) {
        const playerName = this._ambientLightPairDisplayKey("player", index);
        const lightsName = this._ambientLightPairDisplayKey("lights", index);
        schema.push({
          name: playerName,
          label: this._editorDynamicLabel({ name: playerName }),
          helper: this._editorDynamicHelper({ name: playerName }),
          selector: this._ambientLightEntitySelector({
            multiple: false,
            entities: players,
            fallbackFilter: [{ integration: "music_assistant", domain: "media_player" }]
          })
        });
        schema.push({
          name: lightsName,
          label: this._editorDynamicLabel({ name: lightsName }),
          helper: this._editorDynamicHelper({ name: lightsName }),
          selector: this._ambientLightEntitySelector({
            multiple: true,
            entities: lights,
            fallbackFilter: [{ domain: "light" }]
          })
        });
      }
      return {
        type: "grid",
        name: "ambient_light_player_pair_grid",
        flatten: true,
        column_min_width: "220px",
        schema
      };
    }
    _editorDynamicLabel(item = {}) {
      const info = this._ambientLightPairFieldInfo(item);
      if (!info) {
        const name = this._editorSchemaItem(item)?.name || "";
        const match = /^player_order_entity_(\d+)$/.exec(String(name || ""));
        return match ? `${homeiiEditorI18n2("ui.player_order")} ${match[1]}` : "";
      }
      const key = info.type === "player" ? "ui.player_light_pair_player" : "ui.player_light_pair_lights";
      return homeiiEditorI18n2(key, { number: info.index + 1 });
    }
    _editorDynamicHelper(item = {}) {
      const info = this._ambientLightPairFieldInfo(item);
      if (!info) {
        const name = this._editorSchemaItem(item)?.name || "";
        return /^player_order_entity_\d+$/.test(String(name || "")) ? homeiiEditorI18n2("ui.set_custom_player_order") : "";
      }
      const key = info.type === "player" ? "ui.player_light_pair_player_helper" : "ui.player_light_pair_lights_helper";
      return homeiiEditorI18n2(key);
    }
    _currentBaseEditorSchema() {
      const formDef = this._getCardCtor().getConfigForm?.() || {};
      return Array.isArray(formDef) ? formDef : Array.isArray(formDef.schema) ? formDef.schema : [];
    }
    _withDynamicEditorSchema(schema = []) {
      const pinnedOptions = this._editorPinnedPlayerOptions();
      const colorLightOptions = this._editorColorLightOptions();
      const cloneItem = (item) => {
        if (!item || typeof item !== "object") return item;
        if (item.name === "ambient_light_player_map") return this._ambientLightPlayerPairSchema();
        if (item.name === "player_order_grid") return this._playerOrderSchema();
        const next = { ...item };
        if (Array.isArray(item.schema)) next.schema = item.schema.map(cloneItem).filter(Boolean);
        if (item.name === "pinned_player_entities") {
          next.selector = {
            select: {
              multiple: true,
              mode: "dropdown",
              options: pinnedOptions
            }
          };
        }
        if (item.name === "ambient_light_entities") {
          next.selector = this._ambientLightEntitySelector({
            multiple: true,
            entities: colorLightOptions,
            fallbackFilter: [{ domain: "light" }]
          });
        }
        return next;
      };
      return (Array.isArray(schema) ? schema : []).map(cloneItem).filter(Boolean);
    }
    _render() {
      this._ensureEditorShell();
      const cardCtor = this._getCardCtor();
      const config = {
        ...cardCtor.getStubConfig(),
        ...this._config
      };
      const formDef = cardCtor.getConfigForm?.() || {};
      const baseSchema = this._currentBaseEditorSchema();
      const schema = this._withDynamicEditorSchema(baseSchema);
      const formData = this._editorFormData(config);
      const labels = formDef.labels || {};
      const helpers = formDef.helpers || {};
      const computeLabel = typeof formDef.computeLabel === "function" ? formDef.computeLabel : (item) => labels?.[item?.name];
      const computeHelper = typeof formDef.computeHelper === "function" ? formDef.computeHelper : (item) => helpers?.[item?.name];
      this._syncEditorLiveContext();
      if (this._editorForm) {
        const computeEditorLabel = (item) => {
          const schemaItem = this._editorSchemaItem(item);
          return this._editorDynamicLabel(item) || computeLabel(schemaItem) || homeiiEditorLabelFor2(schemaItem, labels);
        };
        const computeEditorHelper = (item) => {
          const schemaItem = this._editorSchemaItem(item);
          return this._editorDynamicHelper(item) || computeHelper(schemaItem) || homeiiEditorHelperFor2(schemaItem, helpers);
        };
        this._editorForm.computeLabel = computeEditorLabel;
        this._editorForm.computeHelper = computeEditorHelper;
        const schemaKey = JSON.stringify(schema);
        if (this._editorLastSchemaKey !== schemaKey) {
          this._editorForm.schema = schema;
          this._editorLastSchemaKey = schemaKey;
        }
        const configKey = JSON.stringify(formData);
        if (this._editorLastConfigKey !== configKey) {
          this._editorForm.data = formData;
          this._editorLastConfigKey = configKey;
        }
      }
    }
  };
}
function createHomeiiBaseMusicCard({
  HOMEII_CARD_VERSION: HOMEII_CARD_VERSION2,
  HOMEII_VISIBLE_LANGUAGE_OPTIONS: HOMEII_VISIBLE_LANGUAGE_OPTIONS2,
  HomeiiStateFoundation: HomeiiStateFoundation2,
  HomeiiConfigValidators: HomeiiConfigValidators2,
  HomeiiResponsiveFoundation: HomeiiResponsiveFoundation2,
  HomeiiMediaQueueFoundation: HomeiiMediaQueueFoundation2,
  HomeiiMediaPresentationFoundation: HomeiiMediaPresentationFoundation2,
  HomeiiMediaHistoryFoundation: HomeiiMediaHistoryFoundation2,
  HomeiiNowPlayingFoundation: HomeiiNowPlayingFoundation2,
  HomeiiFavoritesFoundation: HomeiiFavoritesFoundation2,
  HomeiiPlayersFoundation: HomeiiPlayersFoundation2,
  getBaseCardConfigForm: getBaseCardConfigForm2,
  getRadioBrowserCountrySelectorOptions: getRadioBrowserCountrySelectorOptions2,
  homeiiRadioBrowserCountryLabel,
  homeiiCountryFlagEmoji,
  homeiiDetectLanguage,
  homeiiIsRtlLanguage,
  homeiiTranslate,
  homeiiTranslateText
}) {
  return class HomeiiBaseMusicCard extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: "open" });
      this._hass = null;
      this._config = {};
      this._built = false;
      this._resolvedConfigEntryId = "";
      this._state = HomeiiStateFoundation2.createBaseBrowserState();
      this._pollTimer = null;
      this._progressTimer = null;
      this._searchTimer = null;
      this._nowPlayingSearchTimer = null;
      this._volumeTimer = null;
      this._bigVolumeTimer = null;
      this._controlRoomVolumeTimer = null;
      this._seekTimer = null;
      this._resizeTimer = null;
      this._resizeObserver = null;
      this._visualViewportResizeListening = false;
      this._ws = null;
      this._wsPending = /* @__PURE__ */ new Map();
      this._wsMsgId = 100;
      this._maReconnectTimer = null;
      this._imgObserver = null;
      this._imgObserverRoot = null;
      this._ctxMenu = null;
      this._ctxMenuOpenedAt = 0;
      this._lastVolumeByPlayer = /* @__PURE__ */ new Map();
      this._softMutedPlayers = /* @__PURE__ */ new Set();
      this._optimisticVolumeByPlayer = /* @__PURE__ */ new Map();
      this._optimisticMuteByPlayer = /* @__PURE__ */ new Map();
      this._optimisticPlaybackByPlayer = /* @__PURE__ */ new Map();
      this._localSendspinPlayer = null;
      this._localSendspinSocket = null;
      this._localSendspinModule = null;
      this._localSendspinConnecting = false;
      this._localSendspinConnected = false;
      this._localSendspinPlayerId = "";
      this._localSendspinState = null;
      this._localSendspinDiscoveryTimers = [];
      this._localSendspinDisconnectTimer = null;
      this._localSendspinReconnectTimer = null;
      this._localSendspinDesired = false;
      this._localSendspinLifecycleListening = false;
      this._localSendspinSuppressClose = false;
      this._toastHistory = /* @__PURE__ */ new Map();
      this._directMaPlayers = [];
      this._directMaPlayersRefreshPromise = null;
      this._cache = {
        library: /* @__PURE__ */ new Map(),
        lyrics: /* @__PURE__ */ new Map()
      };
      this._boundDocClick = this._handleDocumentClick.bind(this);
      this._boundContentClick = this._handleContentClick.bind(this);
      this._boundContentContext = this._handleContentContext.bind(this);
      this._boundQueuePanelClick = this._handleQueuePanelClick.bind(this);
      this._boundWindowResize = this._handleWindowResize.bind(this);
      this._boundBrandLogoError = this._handleBrandLogoError.bind(this);
      this._boundLocalSendspinLifecycle = this._handleLocalSendspinLifecycle.bind(this);
      this._boundManualFrontRouteChange = this._handleManualFrontRouteChange.bind(this);
      this.shadowRoot.addEventListener("error", this._boundBrandLogoError, true);
      this._imageBlobCache = /* @__PURE__ */ new Map();
      this._imageFailed = /* @__PURE__ */ new Set();
      this._resizeListening = false;
      this._lastViewportWidth = typeof window !== "undefined" ? window.innerWidth : 0;
      this._lastViewportHeight = typeof window !== "undefined" ? window.innerHeight : 0;
      this._lastCardWidth = 0;
      this._lastCardHeight = 0;
      this._renderedLayoutMode = "";
      this._layoutRecoveryTimer = null;
      this._layoutRecoveryFrame = null;
      this._layoutResizeHoldUntil = 0;
      this._screensaverVisibilityObserver = null;
      this._screensaverVisibilityKnown = false;
      this._screensaverVisible = true;
      this._screensaverPageEntryPending = false;
      this._screensaverPageEntryReason = "";
      this._screensaverExitTimer = null;
      this._compactTransitionTimer = null;
      this._mobileMenuRenderToken = 0;
      this._manualFrontPlayerTimer = null;
      this._manualFrontLocationKey = "";
      this._manualFrontRouteListening = false;
      this._updateNowPlayingInFlight = false;
      this._updateNowPlayingQueued = false;
      this._queueSnapshotToken = 0;
      this._cardIssueNoticeTimes = /* @__PURE__ */ new Map();
      this._voiceAssistantRecognitionTimer = null;
      this._lyricsRefreshPromise = null;
      this._lyricsRefreshQueued = false;
    }
    setConfig(config) {
      const nextConfig = {
        rtl: true,
        language: "en",
        cache_ttl: 3e5,
        music_assistant_timeout_ms: 12e3,
        active_player_helper_entity: "",
        music_assistant_external_url: "",
        show_ma_button: true,
        ma_interface_url: "/music-assistant",
        ma_interface_target: "_self",
        theme_mode: "auto",
        show_theme_toggle: true,
        hotel_mode: false,
        performance_profile: "full",
        performance_mode: false,
        main_opacity: 0.66,
        popup_opacity: 0.92,
        ...config
      };
      const validator = this._getConfigValidator?.();
      if (typeof validator === "function") {
        validator(nextConfig);
      }
      this._config = nextConfig;
      this._homeiiBrandLogoUrl = "";
      this._homeiiBrandLogoCandidates = null;
      try {
        const configuredLanguage = this._config.language || "en";
        const storedLanguage = localStorage.getItem("homeii_music_flow_lang");
        const configControlsLanguage = typeof this._usesVisualSettings === "function" && this._usesVisualSettings();
        const configuredLanguageBase = String(configuredLanguage || "").trim().toLowerCase().replace("_", "-").split("-")[0];
        const hasExplicitNonDefaultLanguage = configuredLanguageBase && configuredLanguageBase !== "en" && configuredLanguageBase !== "auto";
        this._state.lang = configControlsLanguage || hasExplicitNonDefaultLanguage ? configuredLanguage : storedLanguage || configuredLanguage;
      } catch (_) {
        this._state.lang = this._config.language || "en";
      }
      try {
        this._state.cardTheme = localStorage.getItem("homeii_music_flow_theme") || this._config.theme_mode || "auto";
      } catch (_) {
        this._state.cardTheme = this._config.theme_mode || "auto";
      }
      try {
        this._state.tracksLayout = localStorage.getItem("homeii_music_flow_tracks_layout") || "list";
      } catch (_) {
        this._state.tracksLayout = "list";
      }
      this._maUrl = this._normalizeMaConfigUrl(this._config.ma_url);
      this._maExternalUrl = this._normalizeMaConfigUrl(this._config.music_assistant_external_url || this._config.ma_external_url);
      this._maToken = this._config.ma_token || "";
      this._resolvedConfigEntryId = String(this._config.config_entry_id || "").trim();
      this._resetMeasuredLayoutState();
      this._closeTransientEditorLayoutState();
      this._scheduleLayoutRecovery("config");
    }
    _getConfigValidator() {
      return HomeiiConfigValidators2.validateBaseCardEditorConfig;
    }
    _getCardWidth(fallback = 0) {
      const card = this.shadowRoot?.querySelector(".card") || null;
      const widths = [
        this.getBoundingClientRect?.().width,
        this.offsetWidth,
        card?.getBoundingClientRect?.().width,
        card?.offsetWidth
      ].map((value) => Number(value || 0)).filter((value) => Number.isFinite(value) && value > 0);
      if (widths.length) return Math.max(...widths);
      const fallbackWidth = Number(fallback || 0);
      return Number.isFinite(fallbackWidth) && fallbackWidth > 0 ? fallbackWidth : 0;
    }
    _getCardHeight(fallback = 0) {
      const card = this.shadowRoot?.querySelector(".card") || null;
      const heights = [
        this.getBoundingClientRect?.().height,
        this.offsetHeight,
        card?.getBoundingClientRect?.().height,
        card?.offsetHeight
      ].map((value) => Number(value || 0)).filter((value) => Number.isFinite(value) && value > 0);
      if (heights.length) return Math.max(...heights);
      const fallbackHeight = Number(fallback || 0);
      return Number.isFinite(fallbackHeight) && fallbackHeight > 0 ? fallbackHeight : 0;
    }
    _getViewportHeight(fallback = 0) {
      const fallbackHeight = Number(fallback || 0);
      const visualHeight = typeof window !== "undefined" ? Number(window.visualViewport?.height || 0) : 0;
      const innerHeight = typeof window !== "undefined" ? Number(window.innerHeight || 0) : 0;
      const height = visualHeight > 0 ? visualHeight : innerHeight;
      if (Number.isFinite(height) && height > 0) return height;
      return Number.isFinite(fallbackHeight) && fallbackHeight > 0 ? fallbackHeight : 0;
    }
    _configuredCardHeightFallback(defaultHeight = 760) {
      const configuredHeight = Number(this._config?.height);
      if (Number.isFinite(configuredHeight) && configuredHeight > 0) {
        return Math.max(280, Math.min(1800, configuredHeight));
      }
      const fallbackHeight = Number(defaultHeight || 0);
      return Number.isFinite(fallbackHeight) && fallbackHeight > 0 ? fallbackHeight : 0;
    }
    _getAllocatedCardHeight(fallback = 0) {
      const fallbackHeight = Number(fallback || 0);
      const configuredHeight = Number(this._config?.height);
      const manualHeight = Number.isFinite(configuredHeight) && configuredHeight > 0 ? Math.round(Math.max(280, Math.min(1800, configuredHeight))) : 0;
      const viewportHeight = this._getViewportHeight(fallbackHeight);
      const hostRect = this.getBoundingClientRect?.() || null;
      const top = Number(hostRect?.top || 0);
      const parentHeights = [
        this.parentElement?.getBoundingClientRect?.().height,
        this.parentElement?.parentElement?.getBoundingClientRect?.().height
      ].map((value) => Number(value || 0)).filter((value) => Number.isFinite(value) && value > 240);
      const panelViewportFill = HomeiiResponsiveFoundation2.panelViewportFillEnabled?.({
        viewportHeight,
        hostTop: top,
        parentHeights
      }) === true;
      const shellTopOffset = !panelViewportFill && top > 0 && top <= 160 ? top : 0;
      const viewportSafety = panelViewportFill ? 0 : viewportHeight >= 900 ? 28 : viewportHeight >= 700 ? 20 : 12;
      const viewportAvailable = viewportHeight > 0 ? Math.max(0, viewportHeight - shellTopOffset - Math.max(0, -top) - viewportSafety) : 0;
      const viewportLimit = viewportAvailable > 240 ? viewportAvailable : viewportHeight;
      const boundedParentHeights = parentHeights.filter((value) => !viewportLimit || value <= viewportLimit + 4);
      const parentHeight = boundedParentHeights.length ? Math.max(...boundedParentHeights) : 0;
      if (viewportLimit > 0) {
        if (manualHeight > 0) {
          return Math.round(Math.max(Math.min(280, viewportLimit), Math.min(manualHeight, viewportLimit)));
        }
        const shouldFillViewport = parentHeight <= 0 || viewportLimit > parentHeight + 96;
        const targetHeight = shouldFillViewport ? viewportLimit : Math.min(parentHeight, viewportLimit);
        const resolvedHeight = Math.round(Math.max(Math.min(280, viewportLimit), targetHeight));
        return resolvedHeight;
      }
      if (parentHeight > 0) return Math.round(Math.max(280, parentHeight));
      if (manualHeight > 0) return manualHeight;
      return Number.isFinite(fallbackHeight) && fallbackHeight > 0 ? Math.round(fallbackHeight) : 0;
    }
    _startResizeTracking() {
      if (typeof window !== "undefined" && !this._resizeListening) {
        window.addEventListener("resize", this._boundWindowResize);
        this._resizeListening = true;
      }
      if (typeof window !== "undefined" && window.visualViewport && !this._visualViewportResizeListening) {
        window.visualViewport.addEventListener("resize", this._boundWindowResize);
        this._visualViewportResizeListening = true;
      }
      if (this._resizeObserver || typeof ResizeObserver === "undefined") return;
      this._resizeObserver = new ResizeObserver(() => this._boundWindowResize());
      this._resizeObserver.observe(this);
      if (this.parentElement) this._resizeObserver.observe(this.parentElement);
      if (this.parentElement?.parentElement) this._resizeObserver.observe(this.parentElement.parentElement);
    }
    _stopResizeTracking() {
      if (typeof window !== "undefined" && this._resizeListening) {
        window.removeEventListener("resize", this._boundWindowResize);
        this._resizeListening = false;
      }
      if (typeof window !== "undefined" && window.visualViewport && this._visualViewportResizeListening) {
        window.visualViewport.removeEventListener("resize", this._boundWindowResize);
        this._visualViewportResizeListening = false;
      }
      if (this._resizeObserver) {
        this._resizeObserver.disconnect();
        this._resizeObserver = null;
      }
    }
    _resetMeasuredLayoutState() {
      this._lastCardWidth = 0;
      this._lastCardHeight = 0;
      this._renderedLayoutMode = "";
    }
    _closeTransientEditorLayoutState() {
      if (this._state?.screensaverOpen) this._state.screensaverOpen = false;
      const overlay = this.shadowRoot?.querySelector?.("#screensaverBackdrop");
      overlay?.classList?.remove("open", "closing");
      overlay?.setAttribute?.("aria-hidden", "true");
      this.shadowRoot?.querySelector?.(".card")?.classList?.remove("screensaver-active");
      this.classList.remove("screensaver-page-open");
      clearTimeout(this._screensaverExitTimer);
      this._screensaverExitTimer = null;
      clearInterval(this._screensaverClockTimer);
      this._screensaverClockTimer = null;
    }
    _scheduleLayoutRecovery(reason = "layout") {
      clearTimeout(this._layoutRecoveryTimer);
      if (this._layoutRecoveryFrame && typeof cancelAnimationFrame === "function") {
        cancelAnimationFrame(this._layoutRecoveryFrame);
        this._layoutRecoveryFrame = null;
      }
      const run = (attempt = 0) => {
        this._layoutRecoveryFrame = null;
        if (!this.isConnected || !this._built) return;
        this._recoverLayoutAfterHostChange(reason, attempt);
        if (attempt < 3) {
          const delay = attempt === 0 ? 80 : attempt === 1 ? 220 : 520;
          this._layoutRecoveryTimer = setTimeout(() => run(attempt + 1), delay);
        }
      };
      if (typeof requestAnimationFrame === "function") {
        this._layoutRecoveryFrame = requestAnimationFrame(() => run(0));
      } else {
        this._layoutRecoveryTimer = setTimeout(() => run(0), 0);
      }
    }
    _recoverLayoutAfterHostChange(reason = "layout", attempt = 0) {
      const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
      const viewportHeight = this._getViewportHeight(760);
      const compactExpanded = !!(this._state?.mobileCompactExpanded && this._mobileCompactModeEnabled?.());
      const compactEdgeToEdge = compactExpanded && this._mobileCompactEdgeToEdgeEnabled?.() && this._compactEdgeToEdgeAllowed?.();
      const compactWindow = compactExpanded && !compactEdgeToEdge;
      const compactWindowChrome = viewportWidth > 0 && viewportWidth < 760 ? 156 : 112;
      const width = compactWindow ? Math.max(320, Math.min(720, Number(viewportWidth || 0) - 32)) : this._getCardWidth(viewportWidth);
      const height = compactEdgeToEdge ? viewportHeight : compactWindow ? Math.max(340, Math.min(860, Number(viewportHeight || 0) - compactWindowChrome || viewportHeight)) : this._getAllocatedCardHeight(viewportHeight);
      if (width <= 0 || height <= 0) return;
      const layoutMode = this._layoutModeConfig({ width, compactPopup: compactExpanded });
      const profile = this._layoutProfileConfig(layoutMode, { width, height, compactPopup: compactExpanded });
      const card = this.shadowRoot?.querySelector?.(".card");
      const renderedLayoutMode = this._renderedLayoutMode || (card?.classList?.contains("layout-tablet") ? "tablet" : card?.classList?.contains("layout-mobile") ? "mobile" : "");
      const expectedClasses = [`layout-${layoutMode}`, ...Array.isArray(profile?.classes) ? profile.classes : []];
      const profileMismatch = !!card && expectedClasses.some((className) => !card.classList.contains(className));
      const widthDelta = Math.abs(width - Number(this._lastCardWidth || 0));
      const heightDelta = Math.abs(height - Number(this._lastCardHeight || 0));
      const layoutStale = !!renderedLayoutMode && renderedLayoutMode !== layoutMode;
      const screensaverStale = !!this._state?.screensaverOpen || !!this.shadowRoot?.querySelector?.("#screensaverBackdrop.open");
      const shouldRebuild = attempt === 0 || layoutStale || profileMismatch || widthDelta > 80 || heightDelta > 80 || screensaverStale;
      this._lastCardWidth = Math.round(width);
      this._lastCardHeight = Math.round(height);
      this._lastViewportWidth = viewportWidth;
      this._lastViewportHeight = viewportHeight;
      this._renderedLayoutMode = layoutMode;
      if (!shouldRebuild) return;
      this._closeTransientEditorLayoutState();
      const reopenPage = this._state?.menuOpen ? this._state.menuPage || "settings" : "";
      const reopenStudio = !!this._state?.controlRoomOpen;
      if (typeof this._rebuildMobileUi === "function") {
        this._rebuildMobileUi({ force: true, reopenPage, reopenStudio });
        return;
      }
      this._build();
      if (typeof this._init === "function") this._init();
    }
    _layoutProfileConfig(layoutMode = this._layoutModeConfig?.() || "mobile", options = {}) {
      const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
      const viewportHeight = this._getViewportHeight(this._configuredCardHeightFallback(0));
      const optionWidth = Number(options?.width || 0);
      const optionHeight = Number(options?.height || 0);
      const compactPopup = options?.compactPopup === true || !!(this._state?.mobileCompactExpanded && this._mobileCompactModeEnabled?.());
      const width = optionWidth > 0 ? optionWidth : compactPopup ? this._getCardWidth(viewportWidth) : this._getCardWidth(this._lastCardWidth || viewportWidth);
      const height = optionHeight > 0 ? optionHeight : compactPopup ? viewportHeight : this._getAllocatedCardHeight(this._lastCardHeight || this._configuredCardHeightFallback(viewportHeight) || viewportHeight);
      return HomeiiResponsiveFoundation2.resolveLayoutProfile({
        width,
        height,
        layoutMode
      });
    }
    _layoutProfileClassNames(profile = null) {
      return (Array.isArray(profile?.classes) ? profile.classes : []).map((className) => String(className || "").trim()).filter(Boolean).join(" ");
    }
    _layoutProfileStyleVars(profile = null) {
      const width = Math.max(0, Math.round(Number(profile?.width || 0)));
      const height = Math.max(0, Math.round(Number(profile?.height || 0)));
      const aspect = String(profile?.aspect || "");
      const mobileArtBudget = Math.max(120, Math.round(height * (aspect === "wide" ? 0.34 : 0.36)));
      const mobileShortArtBudget = Math.max(112, Math.round(height * 0.3));
      const mobileTightArtBudget = Math.max(104, Math.round(height * 0.25));
      const tabletArtBudget = Math.max(220, Math.round(height * 0.5));
      const tabletDenseArtBudget = Math.max(180, Math.round(height * 0.42));
      return [
        `--flow-measured-width:${width}px`,
        `--flow-measured-height:${height}px`,
        `--flow-available-height:${height}px`,
        `--flow-mobile-art-budget:${mobileArtBudget}px`,
        `--flow-mobile-short-art-budget:${mobileShortArtBudget}px`,
        `--flow-mobile-tight-art-budget:${mobileTightArtBudget}px`,
        `--flow-tablet-art-budget:${tabletArtBudget}px`,
        `--flow-tablet-dense-art-budget:${tabletDenseArtBudget}px`
      ].join(";") + ";";
    }
    set hass(hass) {
      this._hass = hass;
      if (typeof this._onHassReady === "function") this._onHassReady();
      this._handleManualFrontRouteChange();
      if (!this._built) {
        this._built = true;
        this._build();
        this._init();
        this._scheduleLayoutRecovery("initial-build");
        return;
      }
      this._loadPlayers();
      const selectedPlayer = this._getSelectedPlayer();
      const selectedPlaybackSignature = selectedPlayer ? [
        selectedPlayer.entity_id,
        selectedPlayer.state,
        selectedPlayer.attributes?.active_queue,
        selectedPlayer.attributes?.media_content_id,
        selectedPlayer.attributes?.media_content_type,
        selectedPlayer.attributes?.media_title,
        selectedPlayer.attributes?.media_artist,
        selectedPlayer.attributes?.media_album_name,
        selectedPlayer.attributes?.entity_picture_local,
        selectedPlayer.attributes?.entity_picture,
        selectedPlayer.attributes?.media_image_url,
        selectedPlayer.attributes?.media_image,
        selectedPlayer.attributes?.media_position,
        selectedPlayer.attributes?.media_position_updated_at
      ].map((value) => String(value ?? "")).join("||") : "";
      const selectedPlaybackChanged = !!(selectedPlaybackSignature && this._lastSelectedPlaybackSignature && selectedPlaybackSignature !== this._lastSelectedPlaybackSignature);
      if (selectedPlaybackChanged) {
        clearTimeout(this._externalPlaybackSyncTimer);
        this._externalPlaybackSyncTimer = setTimeout(() => {
          this._ensureQueueSnapshot(true).catch(() => {
          });
        }, 120);
      }
      this._lastSelectedPlaybackSignature = selectedPlaybackSignature;
      this._syncActivePlayerHelper(selectedPlayer);
      if (selectedPlaybackChanged) this._syncAmbientLightForCurrentMedia("playback-change");
      if (this._state?.screensaverOpen) {
        this._syncScreensaverDynamicArtwork();
        this._syncScreensaverUi();
        this._syncAmbientLightForCurrentMedia("screensaver");
        return;
      }
      this._renderPlayerSummary();
      this._syncBrandPlayingState();
      this._syncNowPlayingUI();
      if (this._state?.voiceAssistantDialogOpen) this._syncVoiceAssistantDialog();
    }
    getCardSize() {
      const fallbackHeight = this._configuredCardHeightFallback(this._getViewportHeight(760) || 760);
      const preferredFullHeight = Number(this._fullMobileInlineTargetHeight?.() || 0);
      const height = this._isCompactTileMode() ? this._compactTileReservedHeight() : this._preferFullMobileGridRows?.() && preferredFullHeight > 0 ? Math.max(preferredFullHeight, this._getAllocatedCardHeight(fallbackHeight) || fallbackHeight) : this._getAllocatedCardHeight(fallbackHeight) || fallbackHeight;
      return Math.max(4, Math.ceil(height / 50));
    }
    getGridOptions() {
      const fallbackHeight = this._configuredCardHeightFallback(this._getViewportHeight(760) || 760);
      const preferFullRows = this._preferFullMobileGridRows?.() === true;
      const compactTileMode = !preferFullRows && this._isCompactTileMode();
      const compactWidth = this._getCardWidth(this._lastCardWidth || (typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0) || 390);
      const compactMiniWidget = compactTileMode && this._compactMiniWidgetMode({ width: compactWidth });
      const preferredFullHeight = Number(this._fullMobileInlineTargetHeight?.() || 0);
      const height = compactTileMode ? this._compactTileReservedHeight() : preferFullRows && preferredFullHeight > 0 ? Math.max(preferredFullHeight, this._getAllocatedCardHeight(fallbackHeight) || fallbackHeight) : this._getAllocatedCardHeight(fallbackHeight) || fallbackHeight;
      const compactMiniRows = 5;
      const fullRows = preferFullRows ? Math.max(6, this._sectionGridRowsForHeight(preferredFullHeight || 760)) : 0;
      const options = {
        columns: compactTileMode ? compactMiniWidget ? 6 : 8 : 12,
        rows: Math.max(compactMiniWidget ? compactMiniRows : 6, fullRows, this._sectionGridRowsForHeight(height)),
        min_columns: compactTileMode ? compactMiniWidget ? 3 : 4 : 4,
        min_rows: compactMiniWidget ? compactMiniRows : preferFullRows ? fullRows : 4,
        max_columns: 12
      };
      if (compactMiniWidget) options.max_rows = compactMiniRows;
      return options;
    }
    _sectionGridRowsForHeight(height) {
      const target = Math.max(56, Number(height) || 56);
      return Math.max(1, Math.ceil((target + 8) / 64));
    }
    _sectionGridHeightForRows(rows) {
      const safeRows = Math.max(1, Math.round(Number(rows) || 1));
      return safeRows * 56 + (safeRows - 1) * 8;
    }
    static getStubConfig() {
      return {
        config_entry_id: "",
        ma_url: "",
        music_assistant_external_url: "",
        ma_token: "",
        active_player_helper_entity: "",
        rtl: true,
        language: "en",
        show_ma_button: true,
        ma_interface_url: "/music-assistant",
        ma_interface_target: "_self",
        music_assistant_timeout_ms: 12e3,
        theme_mode: "auto",
        show_theme_toggle: true,
        hotel_mode: false,
        performance_profile: "full",
        performance_mode: false,
        main_opacity: 0.66,
        popup_opacity: 0.92
      };
    }
    static getConfigForm() {
      return getBaseCardConfigForm2();
    }
    static assertConfig(config) {
      if (!config || typeof config !== "object" || Array.isArray(config)) {
        throw new Error("Card config must be an object");
      }
    }
    _language() {
      return homeiiDetectLanguage({
        configLanguage: this._state?.lang || this._config?.language || "en",
        hass: this._hass
      });
    }
    _isHebrew() {
      return homeiiIsRtlLanguage(this._language());
    }
    _i18n(key, params = {}, fallback = "") {
      return homeiiTranslate(this._language(), key, params, fallback);
    }
    _m(en, he, params = {}) {
      return homeiiTranslateText(
        this._language(),
        en,
        params,
        this._isHebrew() ? he : en
      );
    }
    _effectiveTheme() {
      if (this._state.cardTheme === "dark") return "dark";
      if (this._state.cardTheme === "light") return "light";
      return this._hass?.themes?.darkMode ? "dark" : "light";
    }
    _hasDirectMAConnection() {
      return !!this._maBrowserUrl();
    }
    _hasRealtimeDirectMA() {
      return !!(this._maBrowserUrl() && this._maToken);
    }
    _normalizeMaConfigUrl(value = "") {
      return String(value || "").trim().replace(/\/$/, "");
    }
    _maBrowserUrl() {
      const internalUrl = this._normalizeMaConfigUrl(this._maUrl);
      const externalUrl = this._normalizeMaConfigUrl(this._maExternalUrl);
      const pageIsHttps = typeof window !== "undefined" && window.location?.protocol === "https:";
      if (pageIsHttps && externalUrl) return externalUrl;
      return internalUrl || externalUrl;
    }
    _maArtworkBaseUrl() {
      return this._maBrowserUrl() || this._maUrl;
    }
    _maMixedContentMessage() {
      return this._localText(
        "The dashboard is HTTPS but Music Assistant is configured with HTTP only. Add music_assistant_external_url with an HTTPS Music Assistant URL, or open Home Assistant over HTTP on the local network.",
        "הדשבורד פתוח ב-HTTPS אבל Music Assistant מוגדר רק ב-HTTP. הוסף music_assistant_external_url עם כתובת HTTPS של Music Assistant, או פתח את Home Assistant ב-HTTP ברשת המקומית."
      );
    }
    _assertMaBrowserUrlSecure(url = this._maBrowserUrl()) {
      if (!url) return;
      if (typeof window === "undefined" || window.location?.protocol !== "https:") return;
      const base = new URL(url, window.location.href);
      if (base.protocol !== "https:") throw new Error(this._maMixedContentMessage());
    }
    _debugLog(level = "info", ...args) {
      if (!this._config?.debug) return;
      try {
        const fn = console?.[level] || console?.log;
        if (typeof fn === "function") fn.call(console, ...args);
      } catch (_) {
      }
    }
    _clampedConfigNumber(key, fallback, { min = 0, max = 6e4 } = {}) {
      const value = Number(this._config?.[key]);
      const safe = Number.isFinite(value) ? value : fallback;
      return Math.max(min, Math.min(max, safe));
    }
    _musicAssistantTimeoutMs() {
      return this._clampedConfigNumber("music_assistant_timeout_ms", 12e3, { min: 3e3, max: 6e4 });
    }
    _flowAssistantResponseTimeoutMs() {
      return this._clampedConfigNumber("flow_assistant_response_timeout_ms", 18e3, { min: 5e3, max: 6e4 });
    }
    _flowAssistantListenTimeoutMs() {
      return this._clampedConfigNumber("flow_assistant_listen_timeout_ms", 12e3, { min: 5e3, max: 3e4 });
    }
    _flowAssistantAutoCloseMs(status = "success") {
      const fallback = String(status || "").toLowerCase() === "error" ? 7e3 : 4200;
      return this._clampedConfigNumber("flow_assistant_auto_close_ms", fallback, { min: 0, max: 3e4 });
    }
    _timeoutMessage(label = "Request") {
      return this._m(
        `${label} did not respond in time. Check Music Assistant and try again.`,
        `${label} לא החזיר תשובה בזמן. בדוק את Music Assistant ונסה שוב.`
      );
    }
    _withTimeout(promise, timeoutMs = 0, message = "") {
      const ms = Number(timeoutMs || 0);
      if (!Number.isFinite(ms) || ms <= 0) return Promise.resolve(promise);
      return new Promise((resolve, reject) => {
        const timer = setTimeout(() => {
          const error = new Error(message || this._timeoutMessage());
          error.code = "HOMEII_TIMEOUT";
          reject(error);
        }, ms);
        Promise.resolve(promise).then((value) => {
          clearTimeout(timer);
          resolve(value);
        }).catch((error) => {
          clearTimeout(timer);
          reject(error);
        });
      });
    }
    _mediaRefsEquivalent(uriA = "", uriB = "", fallbackType = "track") {
      return HomeiiMediaQueueFoundation2.mediaRefsEquivalent(uriA, uriB, fallbackType);
    }
    _currentWindowOrigin() {
      try {
        return typeof window !== "undefined" ? window.location?.origin || "" : "";
      } catch (_) {
        return "";
      }
    }
    _currentWindowHostname() {
      try {
        return typeof window !== "undefined" ? window.location?.hostname || "" : "";
      } catch (_) {
        return "";
      }
    }
    _isPrivateNetworkHost(hostname = "") {
      const host = String(hostname || "").trim().toLowerCase().replace(/^\[|\]$/g, "");
      if (!host) return false;
      if (host === "localhost" || host.endsWith(".local") || host.endsWith(".lan")) return true;
      if (host === "::1" || host.startsWith("fe80:") || host.startsWith("fc") || host.startsWith("fd")) return true;
      if (/^127\./.test(host) || /^10\./.test(host) || /^192\.168\./.test(host) || /^169\.254\./.test(host)) return true;
      const match = host.match(/^172\.(\d{1,2})\./);
      return !!(match && Number(match[1]) >= 16 && Number(match[1]) <= 31);
    }
    _isRemoteUnsafeArtworkUrl(url = "") {
      const raw = String(url || "").trim();
      if (!/^https?:\/\//i.test(raw)) return false;
      try {
        const currentHost = this._currentWindowHostname();
        const currentProtocol = typeof window !== "undefined" ? window.location?.protocol : "";
        const target = new URL(raw);
        if (target.hostname === currentHost) return false;
        if (!this._isPrivateNetworkHost(currentHost) && this._isPrivateNetworkHost(target.hostname)) return true;
        return currentProtocol === "https:" && target.protocol === "http:" && this._isPrivateNetworkHost(target.hostname);
      } catch (_) {
        return false;
      }
    }
    _artworkCacheToken(cacheKey = "") {
      const key = String(cacheKey || "").trim();
      if (!key) return "";
      let hash = 2166136261;
      for (let index = 0; index < key.length; index += 1) {
        hash ^= key.charCodeAt(index);
        hash = Math.imul(hash, 16777619) >>> 0;
      }
      return `${hash.toString(36)}-${key.length.toString(36)}`;
    }
    _cacheBustedArtworkUrl(url = "", cacheKey = "") {
      const raw = String(url || "").trim();
      const token = this._artworkCacheToken(cacheKey);
      if (!raw || !token || /^(data:|blob:)/i.test(raw)) return raw;
      if (this._isOpaqueMaImageProxyUrl(raw)) return raw;
      try {
        const parsed = new URL(raw, this._currentWindowOrigin() || void 0);
        parsed.searchParams.set("_homeii_art", token);
        return parsed.toString();
      } catch {
        const separator = raw.includes("?") ? "&" : "?";
        return `${raw}${separator}_homeii_art=${encodeURIComponent(token)}`;
      }
    }
    _isOpaqueMaImageProxyUrl(url = "") {
      const raw = String(url || "").trim();
      if (!raw || raw.includes("/imageproxy?")) return false;
      try {
        const parsed = new URL(raw, this._maArtworkBaseUrl() || this._currentWindowOrigin() || void 0);
        return /\/imageproxy\/[^/]+/.test(parsed.pathname);
      } catch {
        return /^\/?imageproxy\/[^/]+/.test(raw);
      }
    }
    _normalizeArtworkUrl(value = "", { size = 300, cacheKey = "" } = {}) {
      const raw = String(value || "").trim();
      if (!raw) return "";
      if (/^(data:|blob:)/i.test(raw)) return raw;
      let resolved = raw;
      const maArtworkBaseUrl = this._maArtworkBaseUrl();
      if (!/^https?:\/\//i.test(resolved)) {
        if (resolved.startsWith("//")) {
          const protocol = typeof window !== "undefined" ? window.location?.protocol || "https:" : "https:";
          resolved = `${protocol}${resolved}`;
        } else if (resolved.startsWith("/api/") || resolved.startsWith("/local/") || resolved.startsWith("/hacsfiles/")) {
          const origin = this._currentWindowOrigin();
          resolved = origin ? new URL(resolved, origin).toString() : resolved;
        } else if (resolved.startsWith("/imageproxy")) {
          resolved = maArtworkBaseUrl ? new URL(resolved, maArtworkBaseUrl).toString() : resolved;
        } else if (resolved.startsWith("imageproxy?") || resolved.startsWith("imageproxy/")) {
          resolved = maArtworkBaseUrl ? new URL(`/${resolved}`, maArtworkBaseUrl).toString() : `/${resolved}`;
        } else if (resolved.startsWith("/")) {
          const origin = this._currentWindowOrigin();
          resolved = origin ? new URL(resolved, origin).toString() : resolved;
        } else {
          resolved = HomeiiMediaPresentationFoundation2.imageProxyUrl(resolved, "", size, maArtworkBaseUrl) || "";
        }
      }
      if (resolved.includes("/imageproxy?") || resolved.includes("/imageproxy/")) {
        resolved = HomeiiMediaPresentationFoundation2.normalizeImageProxyUrl(resolved, size, maArtworkBaseUrl);
      }
      return this._cacheBustedArtworkUrl(resolved, cacheKey);
    }
    _bestArtworkUrl(candidates = [], options = {}) {
      const resolved = (Array.isArray(candidates) ? candidates : [candidates]).map((candidate) => this._normalizeArtworkUrl(candidate, options)).filter(Boolean).filter((url, index, list) => list.indexOf(url) === index);
      if (!resolved.length) return "";
      return resolved.find((url) => !this._isRemoteUnsafeArtworkUrl(url)) || resolved[0];
    }
    _currentArtworkCacheKey(player = null, queueItem = null) {
      const media = queueItem?.media_item || {};
      return [
        player?.entity_id,
        player?.attributes?.media_content_id,
        player?.attributes?.media_title,
        player?.attributes?.media_artist,
        player?.attributes?.media_album_name,
        player?.attributes?.current_media?.image_url,
        player?.attributes?.currentMedia?.image_url,
        player?.attributes?.entity_picture_local,
        player?.attributes?.entity_picture,
        player?.attributes?.media_image_url,
        player?.attributes?.media_image,
        this._getQueueItemStableId?.(queueItem),
        this._getQueueItemUri?.(queueItem),
        queueItem?.streamdetails?.stream_metadata?.image_url,
        media?.name,
        media?.image_url,
        media?.image,
        media?.album?.name,
        media?.album?.image_url,
        media?.album?.image
      ].map((value) => String(value || "").trim()).filter(Boolean).join("|");
    }
    _currentArtworkUrl(player = null, queueItem = null, size = 420, options = {}) {
      const attrs = player?.attributes || {};
      const media = queueItem?.media_item || {};
      const cacheKey = this._currentArtworkCacheKey(player, queueItem);
      const playerArtwork = [
        attrs.current_media?.image_url,
        attrs.currentMedia?.image_url,
        attrs.media_image_url,
        attrs.media_image,
        attrs.entity_picture_local,
        attrs.entity_picture,
        attrs.thumbnail
      ];
      const queueArtwork = [
        this._queueItemImageUrl?.(queueItem, size),
        queueItem?.streamdetails?.stream_metadata?.image_url,
        queueItem?.media_image,
        queueItem?.image_url,
        queueItem?.image,
        media?.image_url,
        media?.image,
        media?.album?.image_url,
        media?.album?.image
      ];
      const queueMatchesPlayer = !queueItem || typeof this._queueItemMatchesPlayer !== "function" || this._queueItemMatchesPlayer(queueItem, player);
      const explicitQueueArtwork = options?.preferPlayerArtwork === false;
      const preferPlayerArtwork = options?.preferPlayerArtwork === true || !explicitQueueArtwork && !queueMatchesPlayer && playerArtwork.some(Boolean);
      return this._bestArtworkUrl(preferPlayerArtwork ? [...playerArtwork, ...queueArtwork] : [...queueArtwork, ...playerArtwork], { size, cacheKey });
    }
    _artPlaceholderHtml(icon = "music_note") {
      return `
        <span class="homeii-art-fallback" aria-hidden="true">
          <span class="homeii-art-fallback-disc">${this._iconSvg(icon || "music_note")}</span>
        </span>
      `;
    }
    _t(text, params = {}) {
      return homeiiTranslateText(this._language(), text, params, text);
    }
    _iconSvg(name) {
      const requestedIcon = String(name || "").trim();
      const icons = {
        play: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M8 5.5v13l10-6.5z"/></svg>`,
        pause: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="6.5" y="5" width="4" height="14" rx="1.2" fill="currentColor"></rect><rect x="13.5" y="5" width="4" height="14" rx="1.2" fill="currentColor"></rect></svg>`,
        previous: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M7 6h2v12H7zM18 6.5v11L10.5 12z"/></svg>`,
        next: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M15 6h2v12h-2zM6 6.5v11L13.5 12z"/></svg>`,
        shuffle: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M16 5h3v3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M4 7h5l7 10h3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M16 19h3v-3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M4 17h5l2-3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M13 10l3-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        repeat: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 7h10l-2-2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M17 17H7l2 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M17 7l2 2-2 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M7 17l-2-2 2-2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        repeat_one: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 7h10l-2-2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M17 17H7l2 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M17 7l2 2-2 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M7 17l-2-2 2-2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path fill="currentColor" d="M11.2 9.8h1.5v4.9h-1.5z"></path><path fill="currentColor" d="M10.4 10.7l1.8-1.3v1.7z"></path></svg>`,
        pin: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="m14 4 6 6-3.2 1.2-3.1 5.6-2.4-2.4L6 20l3.6-6.6-2.4-2.4 5.6-3.1z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"></path><path d="m4 20 5-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        speaker: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2M10 4.5A1.5 1.5 0 1 1 10 7.5 1.5 1.5 0 0 1 10 4.5M10 19a4 4 0 1 1 0-8 4 4 0 0 1 0 8M10 13a2 2 0 1 0 0 4 2 2 0 0 0 0-4Z"></path></svg>`,
        speaker_group: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="9" y="3.5" width="6" height="17" rx="1.8" fill="none" stroke="currentColor" stroke-width="2"></rect><circle cx="12" cy="8" r="1.1" fill="currentColor"></circle><circle cx="12" cy="15.5" r="2.5" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M5.5 7.5v9M18.5 7.5v9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M4 10.5v3M20 10.5v3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        volume_mute: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M14 4.5 9.5 8H6.8A1.8 1.8 0 0 0 5 9.8v4.4A1.8 1.8 0 0 0 6.8 16h2.7l4.5 3.5c.6.4 1.4 0 1.4-.8V5.3c0-.8-.8-1.2-1.4-.8Z"></path><path d="m18 9 4 4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m22 9-4 4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        volume_low: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M14 4.5 9.5 8H6.8A1.8 1.8 0 0 0 5 9.8v4.4A1.8 1.8 0 0 0 6.8 16h2.7l4.5 3.5c.6.4 1.4 0 1.4-.8V5.3c0-.8-.8-1.2-1.4-.8Z"></path><path d="M18 10.2a2.6 2.6 0 0 1 0 3.6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        volume_high: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path fill="currentColor" d="M14 4.5 9.5 8H6.8A1.8 1.8 0 0 0 5 9.8v4.4A1.8 1.8 0 0 0 6.8 16h2.7l4.5 3.5c.6.4 1.4 0 1.4-.8V5.3c0-.8-.8-1.2-1.4-.8Z"></path><path d="M17.5 9.2a4 4 0 0 1 0 5.6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M19.6 6.8a7 7 0 0 1 0 10.4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        library_music: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7h7.5M5 12h6.2M5 17h4.6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M16.8 5.4v9.3a2.5 2.5 0 1 1-1.45-2.27V7.05l4.35-1v2.4l-4.35 1" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        media: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="4" y="4" width="7" height="7" rx="2" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="13" y="4" width="7" height="7" rx="2" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="4" y="13" width="7" height="7" rx="2" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="13" y="13" width="7" height="7" rx="2" fill="none" stroke="currentColor" stroke-width="2"></rect></svg>`,
        menu: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M6 7h12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M6 12h12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M6 17h12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        drawer_handle: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 5.5 13.4 12 7 18.5" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"></path><path d="M13 5.5 19.4 12 13 18.5" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" opacity=".55"></path></svg>`,
        language: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="8.2" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M4.8 12h14.4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 3.8c2.1 2.2 3.2 4.9 3.2 8.2s-1.1 6-3.2 8.2" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"></path><path d="M12 3.8C9.9 6 8.8 8.7 8.8 12s1.1 6 3.2 8.2" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"></path></svg>`,
        settings: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 8.2A3.8 3.8 0 1 0 12 15.8 3.8 3.8 0 0 0 12 8.2Z" fill="none" stroke="currentColor" stroke-width="2"></path><path d="M4 13.4v-2.8l2.1-.5a6.2 6.2 0 0 1 .8-1.8L5.8 6.6l2-2 1.7 1.1c.6-.3 1.2-.6 1.8-.8L11.8 3h2.8l.5 2.1c.6.2 1.2.5 1.8.8l1.7-1.1 2 2-1.1 1.7c.3.6.6 1.2.8 1.8l2.1.5v2.8l-2.1.5a6.2 6.2 0 0 1-.8 1.8l1.1 1.7-2 2-1.7-1.1c-.6.3-1.2.6-1.8.8l-.5 2.1h-2.8l-.5-2.1a6.2 6.2 0 0 1-1.8-.8l-1.7 1.1-2-2 1.1-1.7a6.2 6.2 0 0 1-.8-1.8Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round"></path></svg>`,
        home: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M4.8 11.1 12 5l7.2 6.1" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M7.2 10.2v8.3h9.6v-8.3" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"></path><path d="M10.2 18.5v-4.7h3.6v4.7" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"></path></svg>`,
        stats: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 18.5V13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M10 18.5V9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M15 18.5V5.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M20 18.5V11" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        sort_asc: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 18V6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m4.5 8.5 2.5-2.5 2.5 2.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M12 8h7" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 13h5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 18h3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        sort_desc: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M7 6v12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m4.5 15.5 2.5 2.5 2.5-2.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M12 8h3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 13h5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 18h7" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        search: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="6.5" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="m16 16 4 4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        album: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="4" y="4" width="16" height="16" rx="3" fill="none" stroke="currentColor" stroke-width="2"></rect><circle cx="12" cy="12" r="3.1" fill="none" stroke="currentColor" stroke-width="2"></circle><circle cx="12" cy="12" r="1" fill="currentColor"></circle></svg>`,
        playlist: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7h10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 12h10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 17h6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M18 15.4a2.2 2.2 0 1 1-1.2-2V8.2l3.2-.9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        artist: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="8" r="3.2" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M6 18.5a6 6 0 0 1 12 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        podcast: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="11" r="2.2" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M12 14.2v3.8" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M8.3 15.2a5 5 0 1 1 7.4 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5.8 17.2a8.2 8.2 0 1 1 12.4 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        queue: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7h9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 12h9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 17h6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M17 9.5 20 12l-3 2.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        history: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7h7" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 12h5.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 17h4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M18.5 8.5a5.5 5.5 0 1 0 1.6 3.9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M18.5 5.8v3.2h-3.2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M16.5 12v2.6l2 1.1" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        lyrics: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7.5h9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 11.5h9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M5 15.5h6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M18 6v8.5a2.5 2.5 0 1 1-1.4-2.25V7.4l3.4-.9" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        heart_outline: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20.5 5.7 14.6A4.7 4.7 0 0 1 12 7.8a4.7 4.7 0 0 1 6.3-.1 4.8 4.8 0 0 1 0 6.9Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"></path></svg>`,
        heart_filled: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20.5 5.7 14.6A4.7 4.7 0 0 1 12 7.8a4.7 4.7 0 0 1 6.3-.1 4.8 4.8 0 0 1 0 6.9Z" fill="currentColor" stroke="currentColor" stroke-width="1.2" stroke-linejoin="round"></path></svg>`,
        music_note: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M14 5v9.1a3.3 3.3 0 1 1-1.9-3V7.1l6-1.6v6.6a3.3 3.3 0 1 1-1.9-3V4z" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        moon: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M18.2 14.6A7.8 7.8 0 0 1 9.4 5.8a7.8 7.8 0 1 0 8.8 8.8Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path><path d="M16.7 4.8v2.1M21 9.1h-2.1M18.9 7l-1.5-1.5" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"></path></svg>`,
        timer: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="13" r="7" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M12 13V9.6M12 13l2.3 1.5M9 3.8h6M15.4 5.4l1.4-1.4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        wand: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="m5 19 10-10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m14.2 5.8 1.3-1.3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m18 9.5 1.5-1.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m15.2 3.5.3 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m18.9 7.2.3 2" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m16.9 8.8 2 .3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m7.2 16.8 2 .3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        radio: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="4" y="7" width="16" height="11" rx="3" fill="none" stroke="currentColor" stroke-width="2"></rect><path d="M8 12h5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M8 15h3" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><circle cx="16.5" cy="13" r="2" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M9 7 16.5 4.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        tracks: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M8 6.5v10.2a2.6 2.6 0 1 1-1.5-2.35V8.2l9-2.2v8.7a2.6 2.6 0 1 1-1.5-2.35V7.2Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        grid: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="4.5" y="4.5" width="6" height="6" rx="1.5" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="13.5" y="4.5" width="6" height="6" rx="1.5" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="4.5" y="13.5" width="6" height="6" rx="1.5" fill="none" stroke="currentColor" stroke-width="2"></rect><rect x="13.5" y="13.5" width="6" height="6" rx="1.5" fill="none" stroke="currentColor" stroke-width="2"></rect></svg>`,
        list: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="6.5" cy="7" r="1.2" fill="currentColor"></circle><circle cx="6.5" cy="12" r="1.2" fill="currentColor"></circle><circle cx="6.5" cy="17" r="1.2" fill="currentColor"></circle><path d="M10 7h8" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M10 12h8" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M10 17h8" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        info: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="8.2" fill="none" stroke="currentColor" stroke-width="2"></circle><path d="M12 10.8v5.6" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path><circle cx="12" cy="7.6" r="1.25" fill="currentColor"></circle></svg>`,
        more: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><circle cx="6.5" cy="12" r="1.6" fill="currentColor"></circle><circle cx="12" cy="12" r="1.6" fill="currentColor"></circle><circle cx="17.5" cy="12" r="1.6" fill="currentColor"></circle></svg>`,
        mic: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="9" y="4" width="6" height="10" rx="3" fill="none" stroke="currentColor" stroke-width="2"></rect><path d="M6 11.5a6 6 0 0 0 12 0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M12 17.5V21" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M9 21h6" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        announcement: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 10v4a2 2 0 0 0 2 2h2l5 3V5L9 8H7a2 2 0 0 0-2 2Z" fill="none" stroke="currentColor" stroke-width="2" stroke-linejoin="round"></path><path d="M17 9.5a3.5 3.5 0 0 1 0 5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M19.5 7a7 7 0 0 1 0 10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        stop: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><rect x="7" y="7" width="10" height="10" rx="1.8" fill="currentColor"></rect></svg>`,
        power: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3.8v8.1" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round"></path><path d="M7.1 7.5a7 7 0 1 0 9.8 0" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path></svg>`,
        up: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 18V7" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m7.5 11.5 4.5-4.5 4.5 4.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        down: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 6v11" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m7.5 12.5 4.5 4.5 4.5-4.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        trash: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5 7h14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M9 7V5.8A1.8 1.8 0 0 1 10.8 4h2.4A1.8 1.8 0 0 1 15 5.8V7" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M8 7.5v9.2A2.3 2.3 0 0 0 10.3 19h3.4A2.3 2.3 0 0 0 16 16.7V7.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M10.5 10.5v5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="M13.5 10.5v5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        plus: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 5.5v13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path><path d="M5.5 12h13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path></svg>`,
        minus: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M5.5 12h13" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"></path></svg>`,
        check: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="m6 12.5 4 4L18 8.5" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"></path></svg>`,
        close: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="m7 7 10 10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path><path d="m17 7-10 10" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"></path></svg>`,
        fullscreen: `<svg class="ui-ic" viewBox="0 0 24 24" aria-hidden="true"><path d="M8.5 4.8H5.2v3.3" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round"></path><path d="M5.4 5.4 10 10" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round"></path><path d="M15.5 4.8h3.3v3.3" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round"></path><path d="M18.6 5.4 14 10" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round"></path><path d="M8.5 19.2H5.2v-3.3" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round"></path><path d="M5.4 18.6 10 14" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round"></path><path d="M15.5 19.2h3.3v-3.3" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round"></path><path d="M18.6 18.6 14 14" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round"></path></svg>`
      };
      if (icons[requestedIcon]) return icons[requestedIcon];
      if (/^[a-z0-9_-]+:[a-z0-9_-]+$/i.test(requestedIcon)) {
        return `<ha-icon class="ui-ic ha-ui-ic" icon="${this._esc(requestedIcon)}"></ha-icon>`;
      }
      return "";
    }
    _versionedAssetUrl(url) {
      const value = String(url || "").trim();
      if (!value || /^data:/i.test(value) || /[?&]v=/.test(value)) return value;
      const version = HOMEII_CARD_VERSION2;
      return `${value}${value.includes("?") ? "&" : "?"}v=${encodeURIComponent(version)}`;
    }
    _brandLogoCandidates() {
      if (Array.isArray(this._homeiiBrandLogoCandidates)) return this._homeiiBrandLogoCandidates;
      const urls = [];
      const push = (url, versioned = false) => {
        const value = String(url || "").trim();
        if (!value) return;
        const next = versioned ? this._versionedAssetUrl(value) : value;
        if (!urls.includes(next)) urls.push(next);
      };
      const configured = String(this._config?.brand_logo_url || this._config?.logo_url || "").trim();
      push(configured);
      try {
        push(new URL("data:image/svg+xml;base64,", import.meta.url).href, true);
      } catch (_) {
      }
      push("/local/community/homeii-music-flow/homeii-flow-logo.svg", true);
      push("/hacsfiles/homeii-music-flow/homeii-flow-logo.svg", true);
      push("/local/homeii-flow-logo.svg", true);
      this._homeiiBrandLogoCandidates = urls.length ? urls : ["/local/community/homeii-music-flow/homeii-flow-logo.svg"];
      return this._homeiiBrandLogoCandidates;
    }
    _brandLogoUrl() {
      if (typeof this._homeiiBrandLogoUrl === "string" && this._homeiiBrandLogoUrl) return this._homeiiBrandLogoUrl;
      this._homeiiBrandLogoUrl = this._brandLogoCandidates()[0] || "/local/community/homeii-music-flow/homeii-flow-logo.svg";
      return this._homeiiBrandLogoUrl;
    }
    _brandLogoImgHtml(className = "homeii-logo-fallback") {
      const candidates = this._brandLogoCandidates();
      const primary = candidates[0] || this._brandLogoUrl();
      const fallbacks = candidates.slice(1).join("|");
      return `<img class="${this._esc(className)}" data-homeii-brand-logo="1" data-homeii-logo-fallbacks="${this._esc(fallbacks)}" src="${this._esc(primary)}" alt="HOMEii Flow" loading="lazy" decoding="async">`;
    }
    _tabletBrandSignatureHtml(className = "tablet-brand-logo") {
      return `
        <svg class="brand-signature-logo ${this._esc(className)}" viewBox="0 0 360 86" role="img" aria-label="HOMEii Flow">
          <path class="tablet-brand-wave" d="M8 50 C42 34 72 34 106 50 C142 67 173 65 206 45 C241 24 274 28 352 48" fill="none" pathLength="1"/>
          <text class="tablet-brand-home" x="180" y="42" text-anchor="middle">HOMEii</text>
          <text class="tablet-brand-flow" x="180" y="70" text-anchor="middle">FLOW</text>
          <line class="tablet-brand-rule" x1="110" y1="52" x2="250" y2="52" pathLength="1"/>
        </svg>
      `;
    }
    _handleBrandLogoError(event) {
      const img = event?.target;
      if (!img?.dataset) return;
      if (img.dataset.homeiiArtFallback === "1") {
        const current2 = img.getAttribute("src") || "";
        if (current2) this._imageFailed?.add?.(current2);
        const fallback = img.dataset.homeiiArtFallbackIcon || "music_note";
        const host = img.closest?.(".art-stack-card");
        if (host) {
          host.classList.add("placeholder");
          host.innerHTML = this._mobileArtFallbackHtml(fallback);
        }
        return;
      }
      if (img.dataset.homeiiBrandLogo !== "1") return;
      const current = img.getAttribute("src") || "";
      const fallbacks = String(img.dataset.homeiiLogoFallbacks || "").split("|").map((url) => url.trim()).filter(Boolean);
      while (fallbacks.length) {
        const next = fallbacks.shift();
        if (!next || next === current) continue;
        img.dataset.homeiiLogoFallbacks = fallbacks.join("|");
        img.setAttribute("src", next);
        return;
      }
      img.classList.add("logo-load-failed");
    }
    _setButtonIcon(el, name) {
      if (!el) return;
      el.innerHTML = this._iconSvg(name);
    }
    _syncQueueLikeActionButton(el, liked = false) {
      if (!el) return;
      const nextLiked = !!liked;
      el.classList.toggle("active", nextLiked);
      el.dataset.queueLiked = nextLiked ? "true" : "false";
      el.setAttribute("aria-pressed", nextLiked ? "true" : "false");
      this._setButtonIcon(el, nextLiked ? "heart_filled" : "heart_outline");
    }
    _volumeIconName(player) {
      const vol = Math.round((player?.attributes?.volume_level || 0) * 100);
      if (this._isMuted(player) || vol === 0) return "volume_mute";
      if (vol < 40) return "volume_low";
      return "volume_high";
    }
    _playPauseIconName(player) {
      return player?.state === "playing" ? "pause" : "play";
    }
    _build() {
      if (this._imgObserver) {
        this._imgObserver.disconnect();
        this._imgObserver = null;
        this._imgObserverRoot = null;
      }
      this._cache.library.clear();
      this._imageBlobCache.clear();
      const rtl = this._isHebrew();
      const theme = this._effectiveTheme();
      const visualTheme = theme;
      const configuredHeight = Math.max(420, this._configuredCardHeightFallback(760));
      const viewportHeight = this._getViewportHeight(configuredHeight);
      const allocatedHeight = this._getAllocatedCardHeight(configuredHeight);
      const layoutProfile = this._layoutProfileConfig("browser", { height: allocatedHeight });
      const layoutProfileClass = this._layoutProfileClassNames(layoutProfile);
      const layoutProfileStyle = this._layoutProfileStyleVars(layoutProfile);
      const effectiveHeight = Math.max(360, Math.min(allocatedHeight || configuredHeight, viewportHeight - 24));
      const uiScale = Math.max(0.72, Math.min(1.02, effectiveHeight / 820));
      const mainOpacity = Math.max(0.3, Math.min(0.98, Number(this._config.main_opacity ?? 0.66)));
      const popupOpacity = Math.max(0.4, Math.min(0.98, Number(this._config.popup_opacity ?? 0.92)));
      const darkBgAlpha = Math.max(0.34, Math.min(0.9, mainOpacity * 0.92));
      const darkSidebarAlpha = Math.max(0.4, Math.min(0.95, mainOpacity * 0.98));
      const darkPanelAlpha = Math.max(0.42, Math.min(0.97, mainOpacity + 0.02));
      const lightBgAlpha = Math.max(0.46, Math.min(0.96, mainOpacity));
      const lightPanelAlpha = Math.max(0.56, Math.min(0.98, mainOpacity + 0.06));
      const modalOverlayAlpha = theme === "dark" ? Math.max(0.32, Math.min(0.7, popupOpacity * 0.62)) : Math.max(0.18, Math.min(0.42, popupOpacity * 0.28));
      this.shadowRoot.innerHTML = `
        <style>
          @import url('https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;700;800;900&family=Rubik:wght@400;500;700;800;900&display=swap');
          :host {
            display:block;
            container-type:inline-size;
            container-name:homeii-browser-host;
            margin:0 !important;
            padding:0 !important;
            background:transparent !important;
            border:none !important;
            box-shadow:none !important;
            --ma-accent: var(--accent-color, #e0a11b);
            --ma-radius-xl: 22px;
            overflow:hidden !important;
            border-radius:var(--ma-radius-xl);
            --ma-card-height: ${Math.round(allocatedHeight || effectiveHeight)}px;
            --ma-effective-height: ${effectiveHeight}px;
            --ma-ui-scale: ${uiScale.toFixed(3)};
            --ma-main-opacity: ${mainOpacity.toFixed(2)};
            --ma-popup-opacity: ${popupOpacity.toFixed(2)};
            --ma-shell-pad: calc(16px * var(--ma-ui-scale));
            --ma-shell-gap: calc(14px * var(--ma-ui-scale));
            --ma-control-size: calc(42px * var(--ma-ui-scale));
            --ma-chip-height: calc(44px * var(--ma-ui-scale));
            --ma-np-art-size: calc(96px * var(--ma-ui-scale));
            --ma-now-button-size: calc(58px * var(--ma-ui-scale));
            --ma-now-main-button-size: calc(82px * var(--ma-ui-scale));
            --ma-track-title-size: calc(24px * var(--ma-ui-scale));
            --ma-blur: blur(18px);
            font-family:'Outfit','Segoe UI',system-ui,sans-serif;
          }
          ha-card {
            background:transparent !important;
            border:none !important;
            box-shadow:none !important;
            overflow:hidden !important;
            border-radius:var(--ma-radius-xl);
          }
          .card.rtl,
          .card.rtl button,
          .card.rtl input,
          .card.rtl textarea,
          .card.rtl select {
            font-family:'Heebo','Outfit','Segoe UI',system-ui,sans-serif;
          }
          .theme-dark {
            --ma-bg: rgba(18,20,26,${darkBgAlpha.toFixed(2)});
            --ma-sidebar: rgba(14,16,22,${darkSidebarAlpha.toFixed(2)});
            --ma-topbar: rgba(20,22,29,${Math.max(0.36, Math.min(0.94, mainOpacity * 0.92)).toFixed(2)});
            --ma-panel: rgba(24,26,33,${darkPanelAlpha.toFixed(2)});
            --ma-soft: rgba(255,255,255,0.07);
            --ma-soft-2: rgba(255,255,255,0.10);
            --ma-border: rgba(255,255,255,0.10);
            --ma-text-1: var(--primary-text-color, #f1f3f8);
            --ma-text-2: var(--secondary-text-color, #b7bccb);
            --ma-text-3: rgba(200,205,220,0.65);
            --ma-shadow: 0 18px 44px rgba(0,0,0,0.34);
            --ma-modal-bg: rgba(18,22,30,${Math.max(0.68, popupOpacity).toFixed(2)});
            --ma-modal-soft: rgba(255,255,255,0.06);
            --ma-modal-border: rgba(255,255,255,0.14);
          }
          .theme-light {
            --ma-bg: rgba(248,250,253,${Math.max(0.84, lightBgAlpha + 0.16).toFixed(2)});
            --ma-sidebar: rgba(236,241,247,${Math.max(0.88, mainOpacity + 0.12).toFixed(2)});
            --ma-topbar: rgba(242,246,251,${Math.max(0.9, mainOpacity + 0.16).toFixed(2)});
            --ma-panel: rgba(255,255,255,${Math.max(0.9, lightPanelAlpha + 0.18).toFixed(2)});
            --ma-soft: rgba(18,25,36,0.055);
            --ma-soft-2: rgba(18,25,36,0.12);
            --ma-border: rgba(18,25,36,0.14);
            --ma-text-1: #16202d;
            --ma-text-2: #445166;
            --ma-text-3: rgba(22,32,45,0.72);
            --ma-shadow: 0 18px 44px rgba(28,35,45,0.10);
            --ma-modal-bg: rgba(255,255,255,${Math.max(0.82, popupOpacity).toFixed(2)});
            --ma-modal-soft: rgba(20,24,32,0.04);
            --ma-modal-border: rgba(20,24,32,0.12);
          }
          * { box-sizing:border-box; }
          *::before,*::after { box-sizing:border-box; }
          .card {
            display:grid;
            grid-template-columns:220px minmax(0,1fr);
            container-type:inline-size;
            container-name:homeii-browser-card;
            position:relative;
            width:100%;
            height:min(var(--ma-card-height), calc(100dvh - 24px));
            min-height:min(520px, var(--ma-effective-height));
            max-height:calc(100dvh - 24px);
            overflow:hidden;
            border-radius:var(--ma-radius-xl);
            background:linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01)), var(--ma-bg);
            color:var(--ma-text-1);
            border:1px solid var(--ma-border);
            box-shadow:var(--ma-shadow);
            backdrop-filter: var(--ma-blur);
            -webkit-backdrop-filter: var(--ma-blur);
          }
          .card.rtl { direction:rtl; }
          .sidebar {
            min-width:0;
            display:flex;
            flex-direction:column;
            background:var(--ma-sidebar);
            border-inline-end:1px solid var(--ma-border);
            backdrop-filter:blur(16px);
            -webkit-backdrop-filter:blur(16px);
          }
          .brand {
            display:flex;
            align-items:center;
            gap:12px;
            padding:18px 16px 16px;
            border-bottom:1px solid var(--ma-border);
          }
          .brand-icon {
            width:38px;
            height:38px;
            border-radius:12px;
            display:grid;
            place-items:center;
            background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 74%, white 26%));
            color:#111;
            font-weight:700;
            flex-shrink:0;
            box-shadow:0 10px 24px rgba(224,161,27,0.22);
            cursor:pointer;
            transition:transform 180ms ease, box-shadow 180ms ease, opacity 180ms ease;
          }
          .brand-icon:hover {
            transform:translateY(-1px) scale(1.02);
            box-shadow:0 14px 30px rgba(224,161,27,0.28);
          }
          .brand-icon.playing {
            animation:brandPulseFade 2.8s ease-in-out infinite;
          }
          @keyframes brandPulseFade {
            0% { opacity:1; transform:scale(1); }
            50% { opacity:.72; transform:scale(1.045); }
            100% { opacity:1; transform:scale(1); }
          }
          .brand-title { font-size:15px; font-weight:700; }
          .brand-sub { font-size:11px; color:var(--ma-text-3); }
          .nav { flex:1; overflow-y:auto; padding:12px 10px; }
          .nav-label { font-size:10px; text-transform:uppercase; letter-spacing:.12em; color:var(--ma-text-3); padding:10px 10px 6px; }
          .nav-btn {
            width:100%;
            display:flex;
            align-items:center;
            gap:10px;
            border:1px solid transparent;
            background:transparent;
            color:var(--ma-text-2);
            padding:10px 12px;
            border-radius:13px;
            cursor:pointer;
            margin-bottom:4px;
            text-align:start;
            font:inherit;
            transition:180ms ease;
          }
          .nav-btn:hover { background:var(--ma-soft); color:var(--ma-text-1); }
          .nav-btn.active {
            color:var(--ma-accent);
            background:color-mix(in srgb, var(--ma-accent) 14%, transparent);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
          }
          .nav-ico { width:18px; text-align:center; flex-shrink:0; }
          .player-panel {
            padding:16px 14px 14px;
            border-top:1px solid var(--ma-border);
            background:color-mix(in srgb, var(--ma-sidebar) 88%, black 12%);
            display:flex;
            flex-direction:column;
            gap:14px;
          }
          .np-row {
            display:flex;
            flex-direction:column;
            align-items:center;
            gap:12px;
            margin-bottom:0;
            min-width:0;
            cursor:pointer;
            text-align:center;
          }
          .np-art,.track-art,.queue-thumb,.queue-art,.mini-queue-thumb { background:var(--ma-soft); display:grid; place-items:center; overflow:hidden; }
          .np-art {
            width:104px;
            height:104px;
            border-radius:28px;
            flex-shrink:0;
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            box-shadow:0 16px 30px rgba(0,0,0,0.14);
          }
          .np-art img,.track-art img,.queue-thumb img,.queue-art img,.mini-queue-thumb img,.media-art img,.now-art img {
            width:100%;
            height:100%;
            object-fit:cover;
            display:block;
          }
          .np-art img,.media-art img,.now-art img {
            object-fit:contain;
            object-position:center;
          }
          .np-meta {
            width:100%;
            min-width:0;
          }
          .np-title,.media-title,.track-name,.queue-name,.mini-queue-name { white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
          .np-title {
            font-size:15px;
            font-weight:800;
            line-height:1.15;
          }
          .np-sub,.media-sub,.track-sub,.queue-artist,.mini-queue-artist { font-size:11px; color:var(--ma-text-3); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
          .controls,.now-controls-main { display:flex; align-items:center; justify-content:center; gap:8px; }
          .card.rtl .controls,.card.rtl .now-controls-main { direction:ltr; }
          .card.rtl .volume-row,.card.rtl .now-volume { direction:ltr; }
          .controls {
            margin-bottom:0;
            gap:14px;
          }
          .icon-btn,.play-btn,.lang-btn,.close-btn,.big-round-btn,.big-main-btn,.theme-btn {
            border:none;
            cursor:pointer;
            font:inherit;
            transition:180ms ease;
            display:grid;
            place-items:center;
          }
          .ui-ic {
            width:60%;
            height:60%;
            display:block;
            flex-shrink:0;
            pointer-events:none;
            overflow:visible;
            shape-rendering:geometricPrecision;
          }
          .ui-ic * {
            vector-effect:non-scaling-stroke;
          }
          ha-icon.ui-ic {
            --mdc-icon-size:100%;
            width:60%;
            height:60%;
            display:inline-flex;
            align-items:center;
            justify-content:center;
          }
          .play-btn .ui-ic,
          .big-main-btn .ui-ic {
            width:58%;
            height:58%;
          }
          .immersive-btn.small .ui-ic {
            width:48%;
            height:48%;
          }
          .immersive-btn.primary .ui-ic {
            width:54%;
            height:54%;
          }
          .icon-btn,.lang-btn,.close-btn,.theme-btn {
            width:42px;
            height:42px;
            border-radius:14px;
            background:color-mix(in srgb, var(--ma-soft) 92%, transparent);
            color:var(--ma-text-2);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            box-shadow:0 6px 16px rgba(0,0,0,0.06);
          }
          .theme-light .icon-btn,
          .theme-light .lang-btn,
          .theme-light .close-btn,
          .theme-light .theme-btn {
            background:rgba(255,255,255,0.44);
          }
          .icon-btn:hover,.lang-btn:hover,.close-btn:hover,.theme-btn:hover { background:var(--ma-soft); color:var(--ma-text-1); }
          .icon-btn.active,.big-round-btn.active,.theme-btn.active {
            color:var(--ma-accent);
            background:color-mix(in srgb, var(--ma-accent) 14%, transparent);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
          }
          .play-btn,.big-main-btn {
            width:94px;
            height:94px;
            border-radius:28px;
            font-size:32px;
            box-shadow:0 18px 34px rgba(224,161,27,0.24);
          }
          .play-btn {
            width:64px;
            height:64px;
            border-radius:22px;
            box-shadow:0 14px 30px rgba(224,161,27,0.24);
            font-size:22px;
          }
          .progress,.now-progress { position:relative; height:8px; border-radius:999px; background:var(--ma-soft-2); overflow:hidden; cursor:pointer; touch-action:none; }
          .progress::before,.now-progress::before,.immersive-progress::before {
            content:"";
            position:absolute;
            inset:-12px 0;
          }
          .progress { display:none; }
          .progress-fill,.now-progress-fill {
            height:100%;
            width:0%;
            background:linear-gradient(90deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          }
          .player-label { display:none; }
          .player-select { display:none; }
          .volume-row,.now-volume { display:flex; align-items:center; gap:12px; }
          .volume-range,.now-volume input {
            width:100%;
            appearance:none;
            height:8px;
            border-radius:999px;
            outline:none;
            background:linear-gradient(to right, var(--ma-accent) 0%, var(--ma-accent) var(--vol-pct, 50%), var(--ma-soft-2) var(--vol-pct, 50%), var(--ma-soft-2) 100%);
          }
          .volume-range::-webkit-slider-thumb,.now-volume input::-webkit-slider-thumb {
            appearance:none;
            width:18px;
            height:18px;
            border-radius:50%;
            background:var(--ma-accent);
            border:none;
          }
          .volume-range::-moz-range-thumb,.now-volume input::-moz-range-thumb {
            width:18px;
            height:18px;
            border-radius:50%;
            background:var(--ma-accent);
            border:none;
          }
          .main {
            min-width:0;
            min-height:0;
            display:flex;
            flex-direction:column;
            overflow:hidden;
            position:relative;
            backdrop-filter:blur(14px);
            -webkit-backdrop-filter:blur(14px);
            box-shadow:0 18px 44px rgba(0,0,0,0.24);
          }
          .topbar {
            display:flex;
            flex-direction:column;
            gap:10px;
            padding:14px 18px 14px;
            border-bottom:1px solid var(--ma-border);
            background:linear-gradient(180deg, color-mix(in srgb, var(--ma-topbar) 94%, transparent), color-mix(in srgb, var(--ma-topbar) 80%, transparent));
            backdrop-filter:blur(16px);
            -webkit-backdrop-filter:blur(16px);
            overflow-x:auto;
            overflow-y:hidden;
          }
          .topbar-row,.player-summary-row {
            display:flex;
            align-items:center;
            gap:10px;
            flex-wrap:nowrap !important;
            width:max-content;
            min-width:100%;
          }
          .player-summary-row {
            display:none;
          }
          .search {
            flex:1 1 320px;
            min-width:180px;
            display:flex;
            align-items:center;
            gap:10px;
            min-height:48px;
            padding:0 16px;
            border-radius:18px;
            background:color-mix(in srgb, var(--ma-soft) 92%, transparent);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            box-shadow:inset 0 1px 0 rgba(255,255,255,0.04);
          }
          .theme-light .search {
            background:rgba(255,255,255,0.92);
            box-shadow:inset 0 1px 0 rgba(255,255,255,0.75);
          }
          .search input {
            flex:1;
            min-width:0;
            border:none;
            background:transparent;
            color:var(--ma-text-1);
            outline:none;
            font:inherit;
          }
          .search input::placeholder { color:var(--ma-text-3); }
          .topbar-actions,.summary-actions {
            display:flex;
            gap:10px;
            align-items:center;
            flex-wrap:nowrap;
            flex:0 0 auto;
            margin-inline-start:auto;
          }
          .status-pill {
            display:inline-flex;
            align-items:center;
            gap:10px;
            min-height:46px;
            padding:0 16px;
            border-radius:999px;
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            background:color-mix(in srgb, var(--ma-soft) 90%, transparent);
            color:var(--ma-text-2);
            font-size:13px;
            font-weight:600;
            white-space:nowrap;
            box-shadow:0 6px 16px rgba(0,0,0,0.06);
          }
          .theme-light .status-pill {
            background:rgba(255,255,255,0.9);
          }
          .status-dot { width:8px; height:8px; border-radius:50%; background:#46c16f; box-shadow:0 0 10px rgba(70,193,111,0.4); }
          .status-pill.offline .status-dot { background:#d66; box-shadow:0 0 10px rgba(214,102,102,0.35); }
          .selected-player-box {
            display:flex;
            align-items:center;
            gap:12px;
            min-width:220px;
            max-width:340px;
            flex:0 0 clamp(220px, 24cqi, 340px);
            min-height:48px;
            padding:0 16px;
            border-radius:18px;
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            background:color-mix(in srgb, var(--ma-soft) 94%, transparent);
            box-shadow:0 8px 20px rgba(0,0,0,0.06);
          }
          .theme-light .selected-player-box {
            background:rgba(255,255,255,0.92);
          }
          .selected-player-meta { min-width:0; flex:1; }
          .selected-player-title {
            font-size:12px;
            font-weight:800;
            color:var(--ma-text-1);
            letter-spacing:-0.01em;
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .selected-player-sub {
            font-size:11px;
            color:var(--ma-text-3);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
            margin-top:1px;
          }
          .chip-dot {
            width:10px;
            height:10px;
            border-radius:50%;
            background:rgba(255,255,255,0.32);
            flex-shrink:0;
            box-shadow:0 0 0 5px rgba(255,255,255,0.04);
          }
          .theme-light .chip-dot { background:rgba(20,24,32,0.22); }
          .content { flex:1; min-height:0; overflow-y:auto; padding:16px; }
          .content.now-playing-mode { overflow:hidden; }
          .brand {
            padding:calc(16px * var(--ma-ui-scale));
            gap:calc(12px * var(--ma-ui-scale));
          }
          .brand-icon {
            width:calc(38px * var(--ma-ui-scale));
            height:calc(38px * var(--ma-ui-scale));
            border-radius:calc(12px * var(--ma-ui-scale));
          }
          .brand-title { font-size:calc(15px * var(--ma-ui-scale)); }
          .brand-sub { font-size:calc(11px * var(--ma-ui-scale)); }
          .player-panel { padding:var(--ma-shell-pad); gap:calc(14px * var(--ma-ui-scale)); }
          .np-art {
            width:var(--ma-np-art-size);
            height:var(--ma-np-art-size);
            border-radius:calc(26px * var(--ma-ui-scale));
            cursor:pointer;
            transition:transform 180ms ease, box-shadow 180ms ease;
          }
          .np-art:hover {
            transform:translateY(-1px) scale(1.015);
            box-shadow:0 18px 34px rgba(0,0,0,0.16);
          }
          .np-title { font-size:calc(15px * var(--ma-ui-scale)); }
          .topbar { padding:calc(14px * var(--ma-ui-scale)); }
          .topbar-row,.player-summary-row,.topbar-actions,.summary-actions { gap:calc(10px * var(--ma-ui-scale)); }
          .search {
            min-height:calc(46px * var(--ma-ui-scale));
            padding:0 calc(14px * var(--ma-ui-scale));
            border-radius:calc(18px * var(--ma-ui-scale));
          }
          .content { padding:var(--ma-shell-pad); }
          .chip-btn {
            min-height:var(--ma-chip-height);
            padding:0 calc(14px * var(--ma-ui-scale));
            border-radius:calc(16px * var(--ma-ui-scale));
          }
          .icon-btn,.lang-btn,.close-btn,.theme-btn {
            width:var(--ma-control-size);
            height:var(--ma-control-size);
            border-radius:calc(14px * var(--ma-ui-scale));
          }
          .selected-player-box,.status-pill {
            min-height:calc(46px * var(--ma-ui-scale));
            padding:0 calc(15px * var(--ma-ui-scale));
            border-radius:calc(18px * var(--ma-ui-scale));
          }
          .section { margin-bottom:calc(24px * var(--ma-ui-scale)); }
          .section { margin-bottom:28px; }
          .section-header { display:flex; align-items:center; gap:10px; flex-wrap:wrap; margin-bottom:12px; }
          .section-title { font-size:15px; font-weight:700; }
          .section-badge,.now-queue-count {
            padding:3px 9px;
            border-radius:999px;
            background:var(--ma-soft);
            border:1px solid var(--ma-border);
            color:var(--ma-text-3);
            font-size:11px;
          }
          .section-actions { margin-inline-start:auto; display:flex; gap:6px; flex-wrap:wrap; }
          .chip-btn {
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            background:color-mix(in srgb, var(--ma-soft) 92%, transparent);
            color:var(--ma-text-2);
            border-radius:16px;
            min-height:44px;
            padding:0 14px;
            font:inherit;
            font-size:12px;
            font-weight:700;
            cursor:pointer;
            display:inline-flex;
            align-items:center;
            justify-content:center;
            gap:8px;
            backdrop-filter:blur(10px);
            -webkit-backdrop-filter:blur(10px);
            transition:180ms ease;
            box-shadow:0 6px 16px rgba(0,0,0,0.06);
          }
          .theme-light .chip-btn {
            background:rgba(255,255,255,0.92);
          }
          .chip-btn:hover {
            color:var(--ma-accent);
            background:color-mix(in srgb, var(--ma-accent) 14%, transparent);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
          }
          .chip-btn.warn { color:#ffcf7a; border-color:rgba(255,207,122,0.18); }
          .chip-btn.active {
            color:var(--ma-accent);
            border-color:color-mix(in srgb, var(--ma-accent) 30%, transparent);
            background:color-mix(in srgb, var(--ma-accent) 12%, transparent);
          }
          .grid { display:grid; grid-template-columns:repeat(auto-fill, minmax(126px, 1fr)); gap:14px; }
          .media-card { cursor:pointer; min-width:0; transition:transform 180ms ease; }
          .media-card:hover { transform:translateY(-3px); }
          .media-card.playing .media-art {
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
            box-shadow:0 0 0 1px color-mix(in srgb, var(--ma-accent) 40%, transparent);
          }
          .media-art {
            position:relative;
            aspect-ratio:1/1;
            border-radius:18px;
            border:1px solid var(--ma-border);
            margin-bottom:8px;
            background:var(--ma-soft);
            overflow:hidden;
          }
          .media-placeholder {
            position:absolute;
            inset:0;
            display:grid;
            place-items:center;
            font-size:28px;
            color:var(--ma-text-3);
          }
          .homeii-art-fallback {
            width:100%;
            height:100%;
            display:grid;
            place-items:center;
            border-radius:inherit;
            background:
              radial-gradient(circle at 50% 36%, color-mix(in srgb, var(--ma-accent) 28%, transparent), transparent 52%),
              linear-gradient(145deg, rgba(255,255,255,.09), rgba(255,255,255,.025));
            color:rgba(255,255,255,.78);
            box-shadow:inset 0 0 0 1px rgba(255,255,255,.08);
          }
          .homeii-art-fallback-disc {
            width:46%;
            height:46%;
            min-width:34px;
            min-height:34px;
            display:grid;
            place-items:center;
            border-radius:50%;
            background:rgba(0,0,0,.2);
            border:1px solid rgba(255,255,255,.12);
            box-shadow:0 12px 28px rgba(0,0,0,.18);
          }
          .homeii-art-fallback svg {
            width:48%;
            height:48%;
            color:currentColor;
            opacity:.92;
          }
          .theme-light .homeii-art-fallback {
            color:rgba(48,55,68,.7);
            background:
              radial-gradient(circle at 50% 36%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 54%),
              linear-gradient(145deg, rgba(255,255,255,.86), rgba(238,242,247,.56));
            box-shadow:inset 0 0 0 1px rgba(90,105,125,.12);
          }
          .theme-light .homeii-art-fallback-disc {
            background:rgba(255,255,255,.48);
            border-color:rgba(82,96,118,.12);
          }
          .media-overlay {
            position:absolute;
            inset:0;
            display:grid;
            place-items:center;
            background:linear-gradient(180deg, transparent, rgba(0,0,0,0.18), rgba(0,0,0,0.54));
            opacity:0;
            transition:opacity 180ms ease;
          }
          .theme-light .media-overlay { background:linear-gradient(180deg, transparent, rgba(255,255,255,0.04), rgba(0,0,0,0.16)); }
          .media-card:hover .media-overlay { opacity:1; }
          .play-bubble {
            width:42px;
            height:42px;
            border-radius:50%;
            display:grid;
            place-items:center;
            background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
            color:#111;
          }
          .playing-badge {
            position:absolute;
            bottom:6px;
            left:6px;
            padding:3px 6px;
            border-radius:8px;
            font-size:10px;
            font-weight:700;
            color:#111;
            background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
            display:none;
          }
          .media-card.playing .playing-badge { display:inline-flex; }
          .media-title { font-size:12.5px; font-weight:600; }
          .track-list { display:flex; flex-direction:column; gap:6px; }
          .track-row {
            display:flex;
            align-items:center;
            gap:12px;
            min-width:0;
            padding:10px 12px;
            border-radius:14px;
            background:transparent;
            border:1px solid transparent;
            cursor:pointer;
          }
          .track-row:hover { background:var(--ma-soft); }
          .track-row.playing {
            background:color-mix(in srgb, var(--ma-accent) 14%, transparent);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
          }
          .track-num { width:24px; text-align:center; color:var(--ma-text-3); font-size:11px; flex-shrink:0; }
          .track-row.playing .track-num { color:var(--ma-accent); }
          .track-art { width:42px; height:42px; border-radius:12px; flex-shrink:0; }
          .track-meta { flex:1; min-width:0; }
          .track-name { font-size:12.5px; font-weight:600; }
          .track-row.playing .track-name { color:var(--ma-accent); }
          .track-dur { font-size:11px; color:var(--ma-text-3); flex-shrink:0; }
          .state-box {
            min-height:240px;
            display:grid;
            place-items:center;
            text-align:center;
            padding:24px;
            color:var(--ma-text-3);
          }
          .spinner {
            width:24px;
            height:24px;
            border:2px solid color-mix(in srgb, var(--ma-text-3) 25%, transparent);
            border-top-color:var(--ma-accent);
            border-radius:50%;
            animation:spin .8s linear infinite;
            margin:0 auto 10px;
          }
          @keyframes spin { to { transform:rotate(360deg); } }
          .homeii-loading-state {
            min-height:240px;
            display:grid;
            place-items:center;
            text-align:center;
            padding:24px;
            color:var(--ma-text-2);
          }
          .homeii-loading-content {
            display:grid;
            justify-items:center;
            gap:12px;
          }
          .homeii-loading-mark {
            position:relative;
            width:70px;
            height:70px;
            display:grid;
            place-items:center;
          }
          .homeii-loading-ring {
            position:absolute;
            inset:8px;
            border-radius:999px;
            border:2px solid color-mix(in srgb, var(--ma-text-3) 16%, transparent);
            border-top-color:color-mix(in srgb, var(--ma-accent) 88%, #fff 8%);
            animation:homeiiLoadingSpin 1.4s linear infinite;
          }
          .homeii-loading-ring.secondary {
            inset:17px;
            opacity:.58;
            animation-duration:2.1s;
            animation-direction:reverse;
          }
          .homeii-loading-core {
            width:15px;
            height:15px;
            border-radius:999px;
            background:var(--ma-accent);
            box-shadow:0 0 24px color-mix(in srgb, var(--ma-accent) 56%, transparent);
            animation:homeiiLoadingPulse 1.2s ease-in-out infinite;
          }
          .homeii-loading-text {
            color:var(--ma-text-2);
            font-size:13px;
            font-weight:850;
          }
          @keyframes homeiiLoadingSpin { to { transform:rotate(360deg); } }
          @keyframes homeiiLoadingPulse {
            0%,100% { transform:scale(.86); opacity:.62; }
            50% { transform:scale(1.08); opacity:1; }
          }
          @media (prefers-reduced-motion: reduce) {
            .homeii-loading-ring,
            .homeii-loading-core {
              animation:none;
            }
          }
          .queue-panel {
            position:absolute;
            inset:0;
            z-index:210;
            display:flex;
            flex-direction:column;
            background:rgba(8,12,18,0.42);
            backdrop-filter:blur(18px);
            -webkit-backdrop-filter:blur(18px);
          }
          .theme-light .queue-panel {
            background:rgba(236,241,247,0.58);
            backdrop-filter:blur(20px);
            -webkit-backdrop-filter:blur(20px);
          }
          .queue-shell {
            width:min(1100px, calc(100% - 20px));
            height:min(calc(100% - 20px), var(--ma-effective-height));
            margin:auto;
            display:flex;
            flex-direction:column;
            border-radius:26px;
            border:1px solid var(--ma-modal-border);
            background:
              linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 6%, transparent), transparent 20%),
              linear-gradient(180deg, color-mix(in srgb, var(--ma-modal-bg) 96%, transparent), color-mix(in srgb, var(--ma-modal-bg) 90%, black 10%));
            box-shadow:0 24px 60px rgba(0,0,0,0.22);
            overflow:hidden;
          }
          .theme-light .queue-shell {
            background:linear-gradient(180deg, rgba(255,255,255,0.94), rgba(247,250,255,0.92));
            box-shadow:0 22px 50px rgba(31,41,55,0.14);
          }
          .queue-header {
            display:flex;
            align-items:center;
            gap:14px;
            padding:18px 20px;
            border-bottom:1px solid var(--ma-border);
            background:color-mix(in srgb, var(--ma-modal-soft) 92%, transparent);
            backdrop-filter:blur(12px);
            -webkit-backdrop-filter:blur(12px);
          }
          .queue-art {
            width:58px;
            height:58px;
            border-radius:16px;
            flex-shrink:0;
            border:1px solid var(--ma-border);
            overflow:hidden;
            box-shadow:0 8px 18px rgba(0,0,0,0.12);
          }
          .queue-meta { min-width:0; flex:1; }
          .queue-title {
            font-size:18px;
            font-weight:800;
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .queue-sub {
            margin-top:3px;
            font-size:12px;
            color:var(--ma-text-3);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .queue-scroll {
            flex:1;
            overflow-y:auto;
            padding:16px;
            background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent 18%);
          }
          .queue-item {
            display:flex;
            align-items:center;
            gap:12px;
            padding:12px 14px;
            border-radius:16px;
            cursor:pointer;
            border:1px solid transparent;
            background:transparent;
            transition:180ms ease;
            margin-bottom:8px;
          }
          .queue-item:hover {
            background:color-mix(in srgb, var(--ma-soft) 82%, transparent);
            border-color:color-mix(in srgb, var(--ma-border) 78%, transparent);
          }
          .queue-item.active {
            background:linear-gradient(90deg, color-mix(in srgb, var(--ma-accent) 16%, transparent), transparent 72%), color-mix(in srgb, var(--ma-soft) 88%, transparent);
            border-color:color-mix(in srgb, var(--ma-accent) 26%, transparent);
            box-shadow:0 8px 20px rgba(0,0,0,0.08);
          }
          .queue-item.past { opacity:.44; }
          .queue-num {
            width:28px;
            text-align:center;
            font-size:12px;
            font-weight:700;
            color:var(--ma-text-3);
            flex-shrink:0;
          }
          .queue-item.active .queue-num,.queue-item.active .queue-name { color:var(--ma-accent); }
          .queue-thumb {
            width:46px;
            height:46px;
            border-radius:13px;
            flex-shrink:0;
            border:1px solid var(--ma-border);
            overflow:hidden;
          }
          .queue-item-meta { flex:1; min-width:0; }
          .queue-name { font-size:13px; font-weight:700; }
          .queue-artist { font-size:11.5px; color:var(--ma-text-3); margin-top:2px; }
          .queue-dur { font-size:11px; color:var(--ma-text-3); flex-shrink:0; }
          .queue-actions,.mini-queue-actions {
            display:flex;
            gap:10px;
            align-items:center;
            flex-wrap:wrap;
            direction:ltr;
          }
          .mini-queue-actions .chip-btn,.queue-actions .chip-btn {
            padding:10px 14px;
            min-width:46px;
            min-height:42px;
            font-size:14px;
            font-weight:800;
            border-radius:12px;
            background:color-mix(in srgb, var(--ma-panel) 82%, transparent);
            box-shadow:0 6px 18px rgba(0,0,0,0.12);
          }
          .mini-queue-actions .chip-btn:disabled,.queue-actions .chip-btn:disabled {
            opacity:.52;
            cursor:progress;
          }
          .queue-item-meta {
            flex:1;
            min-width:0;
          }
          .queue-actions {
            margin-inline-start:auto;
          }
          .theme-light .queue-item,
          .theme-light .queue-header {
            box-shadow:none;
          }
          .immersive-backdrop {
            position:absolute;
            inset:0;
            z-index:120;
            display:none;
            overflow:hidden;
            border-radius:inherit;
            background:rgba(8,12,18,0.38);
          }
          .immersive-backdrop.open { display:block; }
          .immersive-shell {
            position:relative;
            width:100%;
            height:100%;
            overflow:hidden;
            display:grid;
            grid-template-rows:auto minmax(0,1fr) auto;
            gap:clamp(14px, 2.2vw, 24px);
            padding:clamp(16px, 2.2vw, 28px);
            color:#f7f8fc;
            direction:ltr;
          }
          .immersive-shell.rtl { direction:rtl; }
          .immersive-bg,
          .immersive-cover-glow,
          .immersive-frost,
          .immersive-vignette {
            position:absolute;
            inset:0;
            pointer-events:none;
          }
          .immersive-bg,
          .immersive-cover-glow {
            background-position:center;
            background-size:cover;
            transform:scale(1.12);
            filter:blur(34px) saturate(1.08);
            opacity:0.9;
          }
          .immersive-cover-glow::after {
            content:"";
            position:absolute;
            inset:0;
            background:
              radial-gradient(circle at center, transparent 18%, rgba(8,12,18,0.34) 62%, rgba(8,12,18,0.72) 100%),
              linear-gradient(180deg, rgba(8,12,18,0.22), rgba(8,12,18,0.52));
          }
          .immersive-frost {
            background:
              radial-gradient(circle at top, rgba(224,161,27,0.14), transparent 34%),
              linear-gradient(180deg, rgba(16,18,26,0.26), rgba(12,14,20,0.58));
            backdrop-filter:blur(18px);
            -webkit-backdrop-filter:blur(18px);
          }
          .immersive-vignette {
            background:
              radial-gradient(circle at center, transparent 18%, rgba(8,12,18,0.34) 62%, rgba(8,12,18,0.72) 100%),
              linear-gradient(180deg, rgba(8,12,18,0.22), rgba(8,12,18,0.52));
          }
          .immersive-topbar,
          .immersive-stage,
          .immersive-footer,
          .immersive-header,
          .immersive-body,
          .immersive-panel {
            position:relative;
            z-index:1;
          }
          .immersive-topbar {
            display:flex;
            align-items:flex-start;
            justify-content:space-between;
            gap:16px;
          }
          .immersive-shell.rtl .immersive-topbar {
            flex-direction:row-reverse;
          }
          .immersive-header {
            display:flex;
            align-items:flex-start;
            justify-content:space-between;
            gap:16px;
          }
          .immersive-shell.rtl .immersive-header {
            flex-direction:row-reverse;
          }
          .immersive-backdrop .close-btn {
            width:58px;
            height:58px;
            border-radius:999px;
            background:rgba(255,255,255,0.18);
            color:#121212;
            border-color:rgba(255,255,255,0.22);
            box-shadow:0 12px 24px rgba(0,0,0,0.18);
            backdrop-filter:blur(10px);
            -webkit-backdrop-filter:blur(10px);
            flex:0 0 auto;
          }
          .immersive-meta {
            display:grid;
            gap:8px;
            min-width:0;
            text-align:start;
            max-width:min(44vw, 520px);
          }
          .immersive-shell.rtl .immersive-meta { text-align:right; }
          .immersive-kicker {
            font-size:12px;
            letter-spacing:.16em;
            text-transform:uppercase;
            color:rgba(255,255,255,0.72);
          }
          .immersive-title {
            font-size:clamp(24px, 3.4vw, 40px);
            line-height:1.04;
            font-weight:900;
            letter-spacing:-0.03em;
            text-shadow:0 10px 24px rgba(0,0,0,0.28);
            word-break:break-word;
          }
          .immersive-subtitle {
            font-size:clamp(14px, 1.35vw, 18px);
            color:rgba(255,255,255,0.84);
            text-shadow:0 8px 22px rgba(0,0,0,0.24);
            word-break:break-word;
          }
          .immersive-player-pill {
            display:inline-flex;
            align-items:center;
            gap:8px;
            width:fit-content;
            max-width:100%;
            min-height:38px;
            padding:0 14px;
            border-radius:999px;
            background:rgba(18,20,28,0.34);
            border:1px solid rgba(255,255,255,0.14);
            color:rgba(255,255,255,0.88);
            font-size:12px;
            font-weight:700;
            backdrop-filter:blur(12px);
            -webkit-backdrop-filter:blur(12px);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .immersive-stage,
          .immersive-body {
            min-height:0;
            display:grid;
            place-items:center;
            align-content:center;
            gap:clamp(20px, 3vw, 36px);
          }
          .immersive-art-wrap {
            width:min(100%, 620px);
            display:grid;
            place-items:center;
          }
          .immersive-art {
            width:min(100%, min(54vh, 620px));
            max-width:min(86vw, 620px);
            max-height:min(54vh, 620px);
            aspect-ratio:1/1;
            border-radius:32px;
            overflow:hidden;
            border:1px solid rgba(255,255,255,0.18);
            background:rgba(255,255,255,0.08);
            box-shadow:0 28px 80px rgba(0,0,0,0.34);
            display:grid;
            place-items:center;
            font-size:72px;
            color:rgba(255,255,255,0.5);
          }
          .immersive-art img {
            width:100%;
            height:100%;
            object-fit:contain;
            object-position:center;
            display:block;
          }
          .immersive-body {
            grid-template-rows:minmax(0,1fr) auto;
            gap:clamp(28px, 3.6vw, 44px);
          }
          .immersive-body > .immersive-art-wrap {
            align-self:center;
          }
          .immersive-track-pill {
            display:inline-flex;
            align-items:center;
            justify-content:center;
            gap:10px;
            max-width:min(92%, 720px);
            padding:12px 18px;
            border-radius:18px;
            background:rgba(18,20,28,0.36);
            border:1px solid rgba(255,255,255,0.14);
            backdrop-filter:blur(14px);
            -webkit-backdrop-filter:blur(14px);
            box-shadow:0 12px 28px rgba(0,0,0,0.18);
            text-align:center;
          }
          .immersive-track-pill .immersive-title,
          .immersive-track-pill .immersive-subtitle {
            font-size:inherit;
            line-height:1.2;
            text-shadow:none;
          }
          .immersive-track-pill-text {
            display:grid;
            gap:4px;
            min-width:0;
          }
          .immersive-track-pill-title {
            font-size:clamp(16px, 1.5vw, 22px);
            font-weight:800;
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .immersive-track-pill-sub {
            font-size:clamp(12px, 1vw, 15px);
            color:rgba(255,255,255,0.78);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .immersive-footer,
          .immersive-panel {
            display:grid;
            gap:clamp(12px, 1.8vw, 18px);
            align-self:end;
          }
          .immersive-panel {
            width:min(100%, 1180px);
            margin:0 auto;
            padding-top:clamp(10px, 2vh, 24px);
            background:transparent;
            border:none;
            box-shadow:none;
            grid-template-columns:minmax(210px, 320px) minmax(0, 1fr);
            align-items:end;
          }
          .immersive-panel > .immersive-time-row,
          .immersive-panel > .immersive-progress,
          .immersive-panel > .immersive-controls {
            grid-column:1 / -1;
          }
          .immersive-time-row {
            display:flex;
            align-items:center;
            justify-content:space-between;
            gap:12px;
            color:rgba(255,255,255,0.82);
            font-size:14px;
            padding:0 6px;
            direction:ltr;
          }
          .immersive-progress {
            position:relative;
            width:min(100%, 1180px);
            margin:0 auto;
            height:14px;
            border-radius:999px;
            background:rgba(255,255,255,0.16);
            overflow:hidden;
            cursor:pointer;
            touch-action:none;
            box-shadow:inset 0 1px 0 rgba(255,255,255,0.1);
          }
          .immersive-progress-fill {
            height:100%;
            width:0%;
            border-radius:inherit;
            background:linear-gradient(90deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 76%, white 24%));
          }
          .immersive-controls {
            display:flex;
            align-items:center;
            justify-content:center;
            gap:clamp(12px, 1.8vw, 20px);
            flex-wrap:nowrap;
            direction:ltr;
          }
          .immersive-btn {
            width:86px;
            height:86px;
            border-radius:999px;
            border:1px solid rgba(255,255,255,0.15);
            background:rgba(255,255,255,0.12);
            color:#fff;
            display:grid;
            place-items:center;
            font-size:32px;
            box-shadow:0 16px 32px rgba(0,0,0,0.22);
            backdrop-filter:blur(12px);
            -webkit-backdrop-filter:blur(12px);
            display:grid;
            place-items:center;
          }
          .immersive-btn.primary {
            width:126px;
            height:126px;
            font-size:48px;
            background:rgba(255,255,255,0.18);
          }
          .immersive-btn.small {
            width:72px;
            height:72px;
            font-size:26px;
            background:rgba(255,255,255,0.1);
          }
          .immersive-btn.active {
            border-color:rgba(255,220,140,0.74);
            box-shadow:0 0 0 2px rgba(255,220,140,0.24), 0 16px 32px rgba(0,0,0,0.22);
          }
          .immersive-bottom-row {
            width:min(100%, 1180px);
            margin:0 auto;
            display:grid;
            grid-template-columns:minmax(0, 1fr) minmax(220px, 360px);
            gap:18px;
            align-items:center;
          }
          .immersive-actions {
            display:flex;
            align-items:center;
            justify-content:flex-end;
            gap:10px;
            flex-wrap:wrap;
          }
          .immersive-actions .chip-btn {
            background:rgba(18,20,28,0.34);
            color:#fff;
            border-color:rgba(255,255,255,0.16);
            backdrop-filter:blur(10px);
            -webkit-backdrop-filter:blur(10px);
            min-height:40px;
            padding:0 12px;
          }
          .immersive-player-picker-btn,
          .now-player-picker-btn {
            width:calc(46px * var(--ma-ui-scale));
            min-width:calc(46px * var(--ma-ui-scale));
            min-height:calc(46px * var(--ma-ui-scale));
            padding:0;
            border-radius:calc(16px * var(--ma-ui-scale));
            flex:0 0 auto;
          }
          .immersive-player-picker-btn .ui-ic,
          .now-player-picker-btn .ui-ic {
            width:18px;
            height:18px;
          }
          .immersive-volume {
            display:grid;
            grid-template-columns:auto minmax(0,1fr);
            align-items:center;
            gap:12px;
            direction:ltr;
            max-width:300px;
            padding:6px 12px;
            border-radius:999px;
            background:rgba(18,20,28,0.34);
            border:1px solid rgba(255,255,255,0.14);
            backdrop-filter:blur(12px);
            -webkit-backdrop-filter:blur(12px);
          }
          .immersive-volume input {
            width:100%;
            appearance:none;
            height:8px;
            border-radius:999px;
            outline:none;
            background:linear-gradient(to right, var(--ma-accent) 0%, var(--ma-accent) var(--vol-pct, 50%), rgba(255,255,255,0.28) var(--vol-pct, 50%), rgba(255,255,255,0.28) 100%);
          }
          .immersive-volume input::-webkit-slider-thumb {
            appearance:none;
            width:18px;
            height:18px;
            border-radius:50%;
            background:var(--ma-accent);
            border:none;
          }
          .immersive-volume input::-moz-range-thumb {
            width:18px;
            height:18px;
            border-radius:50%;
            background:var(--ma-accent);
            border:none;
          }
          .immersive-panel .immersive-actions,
          .immersive-panel .immersive-volume {
            margin:0;
          }
          .immersive-panel > .immersive-volume {
            grid-column:1;
            width:100%;
            justify-self:stretch;
          }
          .immersive-panel .immersive-actions {
            grid-column:2;
            width:auto;
            justify-self:end;
          }
          @media (max-width:1024px) {
            .immersive-art {
              width:min(100%, min(50vh, 560px));
              max-width:min(86vw, 560px);
              max-height:min(50vh, 560px);
            }
            .immersive-btn {
              width:78px;
              height:78px;
              font-size:29px;
            }
            .immersive-btn.primary {
              width:112px;
              height:112px;
              font-size:42px;
            }
          }
          @media (max-width:760px) {
            .immersive-shell {
              padding:14px;
              grid-template-rows:auto minmax(0,1fr) auto;
            }
            .immersive-art {
              width:min(100%, min(42vh, 420px));
              max-width:min(88vw, 420px);
              max-height:min(42vh, 420px);
              border-radius:24px;
            }
            .immersive-body {
              gap:12px;
            }
            .immersive-topbar {
              gap:12px;
            }
            .immersive-header {
              gap:12px;
            }
            .immersive-bottom-row {
              grid-template-columns:1fr;
              gap:12px;
            }
            .immersive-panel {
              grid-template-columns:1fr;
            }
            .immersive-actions {
              order:2;
              justify-content:center;
            }
            .immersive-volume {
              order:1;
              max-width:none;
            }
            .immersive-panel .immersive-actions,
            .immersive-panel .immersive-volume {
              width:100%;
            }
            .immersive-panel > .immersive-volume,
            .immersive-panel .immersive-actions {
              grid-column:1;
              justify-self:stretch;
            }
            .immersive-btn {
              width:64px;
              height:64px;
              font-size:24px;
            }
            .immersive-btn.small {
              width:56px;
              height:56px;
              font-size:21px;
            }
            .immersive-btn.primary {
              width:86px;
              height:86px;
              font-size:34px;
            }
          }
          .ctx-menu {
            position:absolute;
            z-index:80;
            min-width:214px;
            max-width:min(268px, calc(100% - 24px));
            max-height:min(56vh, 360px);
            overflow:auto;
            padding:8px;
            border-radius:18px;
            border:1px solid var(--ma-border);
            background:var(--ma-panel);
            backdrop-filter:blur(14px);
            -webkit-backdrop-filter:blur(14px);
            box-shadow:0 18px 44px rgba(0,0,0,0.24);
            display:grid;
            gap:6px;
          }
          .ctx-item { display:flex; align-items:center; gap:10px; min-height:42px; padding:10px 12px; border-radius:14px; color:var(--ma-text-1); cursor:pointer; font-size:13px; font-weight:800; }
          .ctx-item:hover { background:var(--ma-soft); }
          .ctx-ico { width:18px; height:18px; text-align:center; color:var(--ma-text-2); flex-shrink:0; display:grid; place-items:center; }
          .ctx-ico .ui-ic { width:15px; height:15px; }
          .queue-ctx-menu {
            min-width:186px;
            max-width:min(220px, calc(100% - 24px));
            padding:6px;
            gap:4px;
            border-radius:16px;
          }
          .queue-ctx-menu .ctx-item {
            min-height:38px;
            padding:8px 10px;
            border-radius:12px;
            font-size:12px;
            font-weight:800;
          }
          .queue-ctx-menu .ctx-ico {
            width:16px;
            height:16px;
          }
          .queue-ctx-menu .ctx-ico .ui-ic {
            width:14px;
            height:14px;
          }
          .ctx-sep { height:1px; margin:6px 4px; background:rgba(255,255,255,.12); }
          .ctx-caption { padding:4px 8px 2px; font-size:11px; font-weight:800; letter-spacing:.08em; text-transform:uppercase; color:var(--ma-text-3); }
          .toast-wrap {
            position:absolute;
            inset-inline-end:16px;
            bottom:16px;
            z-index:60;
            display:flex;
            flex-direction:column;
            gap:8px;
            pointer-events:none;
          }
          .toast-wrap.center-toast {
            inset:0;
            bottom:auto;
            align-items:center;
            justify-content:center;
            padding:18px;
          }
          .toast {
            padding:10px 14px;
            border-radius:12px;
            border:1px solid var(--ma-border);
            background:var(--ma-panel);
            color:var(--ma-text-1);
            font-size:12px;
            backdrop-filter:blur(12px);
            -webkit-backdrop-filter:blur(12px);
          }
          .toast.centered {
            max-width:min(420px, calc(100% - 48px));
            padding:14px 18px;
            border-radius:18px;
            font-size:14px;
            font-weight:850;
            text-align:center;
            box-shadow:0 22px 54px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.12);
          }
          .modal-backdrop {
            position:absolute;
            inset:0;
            z-index:220;
            display:none;
            align-items:center;
            justify-content:center;
            background:rgba(10,14,22,${modalOverlayAlpha.toFixed(2)});
            backdrop-filter:blur(10px);
            -webkit-backdrop-filter:blur(10px);
            padding:20px;
          }
          .modal-backdrop.open { display:flex; }
          .modal {
            width:min(760px,100%);
            max-height:min(84vh,760px);
            overflow:auto;
            border-radius:26px;
            border:1px solid var(--ma-modal-border);
            background:
              linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 7%, transparent), transparent 22%),
              linear-gradient(180deg, color-mix(in srgb, var(--ma-modal-bg) 96%, transparent), color-mix(in srgb, var(--ma-modal-bg) 88%, black 12%));
            backdrop-filter:blur(22px);
            -webkit-backdrop-filter:blur(22px);
            box-shadow:0 28px 70px rgba(0,0,0,0.26);
            overflow-x:hidden;
            color:var(--ma-text-1);
          }
          .theme-light .modal {
            background:
              linear-gradient(180deg, rgba(255,255,255,0.92), rgba(247,250,255,0.88));
            box-shadow:0 24px 60px rgba(31,41,55,0.16);
          }
          .theme-light .modal-header,
          .theme-light .queue-header {
            background:rgba(255,255,255,0.9);
          }
          .theme-light .modal-title,
          .theme-light .modal-section-title,
          .theme-light .player-card-title,
          .theme-light .group-name,
          .theme-light .queue-title,
          .theme-light .media-title,
          .theme-light .track-name,
          .theme-light .selected-player-title,
          .theme-light .now-track-title {
            color:var(--ma-text-1);
          }
          .theme-light .modal-subtitle,
          .theme-light .player-card-sub,
          .theme-light .player-card-track,
          .theme-light .queue-sub,
          .theme-light .selected-player-sub,
          .theme-light .now-track-subtitle,
          .theme-light .track-sub,
          .theme-light .media-sub {
            color:var(--ma-text-2);
          }
          .modal-header {
            display:flex;
            align-items:center;
            gap:12px;
            padding:18px 20px;
            border-bottom:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            background:linear-gradient(180deg, color-mix(in srgb, var(--ma-modal-soft) 96%, transparent), transparent);
            position:sticky;
            top:0;
            z-index:2;
            backdrop-filter:blur(16px);
            -webkit-backdrop-filter:blur(16px);
          }
          .modal-header-icon {
            width:42px;
            height:42px;
            border-radius:14px;
            display:grid;
            place-items:center;
            background:color-mix(in srgb, var(--ma-accent) 16%, transparent);
            border:1px solid color-mix(in srgb, var(--ma-accent) 26%, transparent);
            color:var(--ma-accent);
            font-size:18px;
            flex-shrink:0;
          }
          .modal-header-meta { min-width:0; flex:1; }
          .modal-title { font-size:16px; font-weight:800; letter-spacing:-0.01em; }
          .modal-subtitle {
            margin-top:2px;
            font-size:11.5px;
            color:var(--ma-text-3);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .modal-body { padding:18px 20px 20px; }
          .modal-section {
            border:1px solid color-mix(in srgb, var(--ma-modal-border) 92%, transparent);
            background:color-mix(in srgb, var(--ma-modal-soft) 72%, transparent);
            border-radius:20px;
            padding:14px;
            margin-bottom:14px;
            box-shadow:0 10px 24px rgba(0,0,0,0.06);
          }
          .theme-light .modal-section {
            background:rgba(255,255,255,0.72);
            box-shadow:0 10px 24px rgba(31,41,55,0.08);
          }
          .modal-section:last-child { margin-bottom:0; }
          .modal-section-top {
            display:flex;
            align-items:center;
            gap:10px;
            flex-wrap:wrap;
            margin-bottom:12px;
          }
          .modal-section-title { font-size:12px; font-weight:800; color:var(--ma-text-1); }
          .modal-section-badge {
            padding:4px 9px;
            border-radius:999px;
            background:color-mix(in srgb, var(--ma-soft) 94%, transparent);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            color:var(--ma-text-3);
            font-size:11px;
            font-weight:700;
          }
          .group-list { display:grid; grid-template-columns:repeat(auto-fit, minmax(220px, 1fr)); gap:10px; margin-bottom:14px; }
          .group-item,.player-card {
            display:flex;
            align-items:center;
            gap:12px;
            padding:12px 12px;
            border-radius:16px;
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            background:color-mix(in srgb, var(--ma-panel) 78%, transparent);
            cursor:pointer;
            min-width:0;
            transition:180ms ease;
            box-shadow:0 8px 18px rgba(0,0,0,0.08);
          }
          .theme-light .group-item,
          .theme-light .player-card {
            background:rgba(255,255,255,0.88);
            box-shadow:0 8px 18px rgba(31,41,55,0.07);
          }
          .group-item:hover,.player-card:hover {
            transform:translateY(-1px);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
            background:color-mix(in srgb, var(--ma-accent) 8%, transparent);
          }
          .group-item.checked,.player-card.active {
            border-color:color-mix(in srgb, var(--ma-accent) 32%, transparent);
            background:linear-gradient(90deg, color-mix(in srgb, var(--ma-accent) 12%, transparent), transparent 68%), color-mix(in srgb, var(--ma-accent) 12%, transparent);
            box-shadow:0 10px 22px rgba(224,161,27,0.10);
          }
          .group-item input { margin:0; accent-color:var(--ma-accent); flex-shrink:0; }
          .group-icon,
          .player-card-icon {
            width:42px;
            height:42px;
            border-radius:14px;
            display:grid;
            place-items:center;
            background:color-mix(in srgb, var(--ma-soft) 92%, transparent);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            font-size:18px;
            flex-shrink:0;
          }
          .group-meta { min-width:0; flex:1; }
          .group-name { min-width:0; font-size:13px; font-weight:700; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
          .group-sub { min-width:0; font-size:11px; color:var(--ma-text-3); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; margin-top:2px; }
          .group-actions { display:flex; gap:8px; flex-wrap:wrap; }
          .player-modal-grid { display:grid; grid-template-columns:1fr; gap:14px; }
          .player-group-title { font-size:12px; font-weight:800; color:var(--ma-text-2); margin-bottom:10px; }
          .player-list { display:grid; grid-template-columns:repeat(auto-fit, minmax(240px, 1fr)); gap:10px; }
          .player-card { align-items:flex-start; text-align:start; width:100%; }
          .player-card-dot { width:10px; height:10px; border-radius:50%; background:var(--ma-text-3); margin-top:6px; flex-shrink:0; box-shadow:0 0 0 5px rgba(255,255,255,0.03); }
          .player-card.playing .player-card-dot { background:#46c16f; box-shadow:0 0 10px rgba(70,193,111,0.35); }
          .player-card.paused .player-card-dot { background:#d9a441; }
          .player-card-meta { min-width:0; flex:1; }
          .player-card-top {
            display:flex;
            align-items:center;
            gap:8px;
            justify-content:space-between;
            margin-bottom:4px;
          }
          .player-card-title { font-size:13px; font-weight:800; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
          .player-card-badge {
            padding:3px 8px;
            border-radius:999px;
            background:color-mix(in srgb, var(--ma-soft) 94%, transparent);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            color:var(--ma-text-3);
            font-size:10px;
            font-weight:800;
            flex-shrink:0;
          }
          .player-card.active .player-card-badge {
            color:var(--ma-accent);
            border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
            background:color-mix(in srgb, var(--ma-accent) 10%, transparent);
          }
          .player-card-sub,.player-card-track { font-size:11px; color:var(--ma-text-3); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
          .player-card-art,
          .group-art {
            width:52px;
            height:52px;
            border-radius:16px;
            overflow:hidden;
            border:1px solid color-mix(in srgb, var(--ma-border) 94%, transparent);
            background:color-mix(in srgb, var(--ma-soft) 92%, transparent);
            display:grid;
            place-items:center;
            flex-shrink:0;
            font-size:22px;
            color:var(--ma-text-3);
            box-shadow:0 8px 18px rgba(0,0,0,0.10);
          }
          .player-card-art img,
          .group-art img {
            width:100%;
            height:100%;
            object-fit:cover;
            display:block;
          }
          .group-item {
            justify-content:flex-start;
            align-items:center;
            gap:12px;
          }
          .group-item input {
            order:2;
            width:24px;
            height:24px;
            margin-inline-start:auto;
          }
          .group-item .group-meta {
            order:1;
            min-width:0;
            flex:1;
          }
          .group-item .group-name {
            font-size:16px;
            font-weight:800;
            color:var(--ma-text-1);
            white-space:nowrap;
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .group-item .group-sub {
            display:none;
          }
          .now-layout {
            height:100%;
            display:grid;
            grid-template-columns:minmax(320px,42%) minmax(0,58%);
            gap:calc(16px * var(--ma-ui-scale));
            min-height:0;
          }
          .now-left,.now-right { min-height:0; display:flex; flex-direction:column; gap:calc(14px * var(--ma-ui-scale)); overflow:hidden; }
          .now-right > .now-card { flex:1; }
          .now-card {
            border:1px solid var(--ma-border);
            background:var(--ma-panel);
            border-radius:calc(24px * var(--ma-ui-scale));
            backdrop-filter:blur(16px);
            -webkit-backdrop-filter:blur(16px);
            box-shadow:0 10px 28px rgba(0,0,0,0.16);
          }
          .now-art-card {
            padding:calc(18px * var(--ma-ui-scale));
            min-height:0;
            display:flex;
            flex-direction:column;
            justify-content:center;
            box-shadow:0 16px 34px rgba(0,0,0,0.12);
            flex:1 1 auto;
          }
          .now-art {
            width:min(100%, clamp(200px, 34vh, 420px));
            aspect-ratio:1/1;
            max-width:100%;
            border-radius:24px;
            overflow:hidden;
            background:var(--ma-soft);
            display:grid;
            place-items:center;
            font-size:54px;
            color:var(--ma-text-3);
            border:1px solid var(--ma-border);
            box-shadow:0 16px 34px rgba(0,0,0,0.14);
            margin-inline:auto;
          }
          .now-track-meta { padding-top:12px; min-width:0; }
          .now-track-title {
            font-size:var(--ma-track-title-size);
            font-weight:900;
            line-height:1.06;
            margin-bottom:6px;
            letter-spacing:-0.02em;
            word-break:break-word;
          }
          .now-track-subtitle {
            font-size:14px;
            color:var(--ma-text-2);
            word-break:break-word;
            line-height:1.45;
          }
          .now-controls-card {
            padding:calc(18px * var(--ma-ui-scale));
            box-shadow:0 16px 34px rgba(0,0,0,0.10);
            display:grid;
            gap:calc(14px * var(--ma-ui-scale));
            align-content:start;
            overflow:hidden;
            flex:0 0 auto;
          }
          .now-time-row { display:flex; justify-content:space-between; gap:10px; font-size:12px; color:var(--ma-text-3); }
          .now-progress {
            height:12px;
            border-radius:999px;
            box-shadow:inset 0 1px 2px rgba(0,0,0,0.08);
          }
          .now-controls-main { gap:calc(10px * var(--ma-ui-scale)); flex-wrap:nowrap; justify-content:center; }
          .big-round-btn {
            width:var(--ma-now-button-size);
            height:var(--ma-now-button-size);
            border-radius:calc(20px * var(--ma-ui-scale));
            background:color-mix(in srgb, var(--ma-soft) 96%, transparent);
            color:var(--ma-text-1);
            border:1px solid color-mix(in srgb, var(--ma-border) 96%, transparent);
            box-shadow:0 12px 24px rgba(0,0,0,0.10);
          }
          .big-main-btn {
            width:var(--ma-now-main-button-size);
            height:var(--ma-now-main-button-size);
            border-radius:50%;
            font-size:calc(28px * var(--ma-ui-scale));
            box-shadow:0 14px 32px rgba(224,161,27,0.22);
          }
          .now-controls-bottom { display:grid; grid-template-columns:minmax(0,1fr) auto; gap:calc(12px * var(--ma-ui-scale)); align-items:center; }
          .now-volume {
            width:100%;
            display:grid;
            grid-template-columns:auto minmax(0,1fr);
            align-items:center;
            gap:calc(12px * var(--ma-ui-scale));
          }
          .now-volume input {
            width:100%;
            flex:unset;
            min-width:0;
          }
          .now-actions {
            display:flex;
            gap:8px;
            flex-wrap:wrap;
            justify-content:flex-end;
            align-items:center;
          }
          .now-player-picker-btn { font-size:12px; }
          .now-queue-card { padding:calc(16px * var(--ma-ui-scale)); min-height:0; display:flex; flex-direction:column; overflow:hidden; }
          .now-queue-toolbar { display:grid; gap:calc(12px * var(--ma-ui-scale)); margin-bottom:calc(12px * var(--ma-ui-scale)); }
          .now-queue-toolbar .now-queue-header { margin-bottom:0; }
          .now-queue-header { display:flex; align-items:center; gap:10px; margin-bottom:12px; flex-wrap:wrap; }
          .now-queue-title { font-size:15px; font-weight:700; }
          .now-queue-body { flex:1; min-height:0; overflow:hidden; display:flex; flex-direction:column; }
          .now-side-scroll {
            min-height:0;
            flex:1;
            overflow:auto;
            padding-inline-end:4px;
          }
          .now-queue-list {
            min-height:0;
            flex:1;
            overflow-y:visible;
            overflow-x:hidden;
            display:flex;
            flex-direction:column;
            gap:8px;
          }
          .now-queue-search {
            width:100%;
            min-width:0;
            flex:unset;
          }
          .side-search-summary {
            display:flex;
            align-items:center;
            justify-content:space-between;
            gap:10px;
            flex-wrap:wrap;
            margin-bottom:12px;
          }
          .side-search-summary-text {
            font-size:12px;
            color:var(--ma-text-3);
          }
          .mini-queue-item {
            display:flex;
            align-items:center;
            gap:10px;
            min-width:0;
            padding:10px 10px;
            border-radius:14px;
            background:transparent;
            border:1px solid transparent;
            cursor:pointer;
            position:relative;
          }
          .mini-queue-item:hover { background:var(--ma-soft); }
          .mini-queue-item.active {
            background:linear-gradient(90deg, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 70%), var(--ma-soft);
            border-color:color-mix(in srgb, var(--ma-accent) 34%, transparent);
          }
          .mini-queue-item.active::before {
            content:'';
            position:absolute;
            inset-inline-start:0;
            top:8px;
            bottom:8px;
            width:3px;
            border-radius:999px;
            background:linear-gradient(180deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 70%, white 30%));
          }
          .mini-queue-thumb { width:40px; height:40px; border-radius:11px; flex-shrink:0; }
          .mini-queue-meta { flex:1; min-width:0; }
          .mini-queue-name { font-size:12px; font-weight:600; }
          .mini-queue-item.active .mini-queue-name { color:var(--ma-text-1); font-weight:700; }
          .mini-queue-item.active .mini-queue-artist,.mini-queue-item.active .mini-queue-index { color:color-mix(in srgb, var(--ma-accent) 68%, white 32%); }
          .mini-queue-index { width:22px; text-align:center; font-size:11px; color:var(--ma-text-3); flex-shrink:0; }
          .group-inline { display:flex; align-items:center; gap:8px; margin-inline-start:auto; flex-wrap:wrap; }
          @media (max-width:1280px) {
            .now-controls-bottom { grid-template-columns:minmax(0,1fr) auto; }
            .now-actions { justify-content:flex-start; }
            .now-volume { width:100%; }
            .now-volume input { flex:1; min-width:0; }
            .now-controls-main { justify-content:center; }
          }
          @media (max-width:1100px) { .now-layout { grid-template-columns:1fr; grid-template-rows:auto minmax(0,1fr); } }
          @media (max-width:920px) {
            .card { grid-template-columns:1fr; height:min(var(--ma-card-height), calc(100dvh - 24px)); min-height:min(620px, var(--ma-effective-height)); }
            .sidebar {
              border-inline-end:none;
              border-bottom:1px solid var(--ma-border);
              display:grid;
              grid-template-columns:minmax(0,1fr);
            }
            .brand {
              padding:12px;
              border-bottom:1px solid color-mix(in srgb, var(--ma-border) 92%, transparent);
            }
            .nav { display:flex; gap:8px; overflow-x:auto; overflow-y:hidden; flex:unset; padding:10px; }
            .nav-label { display:none; }
            .nav-btn { width:auto; margin-bottom:0; white-space:nowrap; }
            .player-panel {
              display:grid;
              grid-template-columns:minmax(0,1fr) auto;
              gap:10px 12px;
              padding:12px;
              border-top:1px solid color-mix(in srgb, var(--ma-border) 92%, transparent);
            }
            .np-row {
              flex-direction:row;
              align-items:center;
              text-align:start;
              gap:10px;
            }
            .np-art {
              width:72px;
              height:72px;
              border-radius:20px;
            }
            .np-meta { flex:1; }
            .controls {
              margin-inline-start:auto;
              gap:10px;
            }
            .volume-row {
              grid-column:1 / -1;
            }
            .topbar {
              overflow-x:visible;
              overflow-y:visible;
            }
            .topbar-row,.player-summary-row {
              width:100%;
              min-width:0;
              display:grid;
              grid-template-columns:minmax(0,1fr) auto;
              grid-template-areas:
                "player actions"
                "search search";
              align-items:center;
              gap:10px;
            }
            .search {
              grid-area:search;
              width:100%;
              min-width:0;
            }
            .selected-player-box {
              grid-area:player;
              width:100%;
              min-width:0;
              max-width:none;
              flex:none;
            }
            .topbar-actions {
              grid-area:actions;
              margin-inline-start:0;
              justify-self:end;
              max-width:100%;
              overflow-x:auto;
              overflow-y:hidden;
              padding-bottom:2px;
            }
            .status-pill { width:auto; justify-content:center; }
            .now-controls-bottom { grid-template-columns:1fr; }
            .now-actions { justify-content:flex-start; }
            .group-list,.player-list { grid-template-columns:1fr; }
            .modal-backdrop {
              padding:
                max(12px, env(safe-area-inset-top))
                max(12px, env(safe-area-inset-right))
                max(12px, env(safe-area-inset-bottom))
                max(12px, env(safe-area-inset-left));
            }
          }
          @media (max-width:720px) {
            :host { --ma-radius-xl: 18px; }
            .card {
              height:min(var(--ma-card-height), calc(100dvh - 12px));
              max-height:calc(100dvh - 12px);
              border-radius:20px;
            }
            .brand {
              padding:10px 12px;
              gap:10px;
            }
            .brand-sub { display:none; }
            .player-panel {
              grid-template-columns:minmax(0,1fr);
              padding:10px 12px 12px;
            }
            .np-row {
              gap:10px;
            }
            .np-art {
              width:64px;
              height:64px;
              border-radius:18px;
            }
            .np-title { font-size:14px; }
            .controls {
              margin-inline-start:0;
              justify-content:flex-start;
            }
            .play-btn {
              width:58px;
              height:58px;
              border-radius:20px;
            }
            .topbar { padding:12px; }
            .content { padding:12px; }
            .topbar-row,.player-summary-row {
              grid-template-columns:1fr;
              grid-template-areas:
                "actions"
                "player"
                "search";
            }
            .topbar-actions {
              justify-self:stretch;
              width:100%;
              justify-content:flex-start;
              flex-wrap:nowrap;
            }
            .search,
            .selected-player-box {
              width:100%;
              min-width:0;
            }
            .selected-player-box {
              min-height:44px;
            }
            .icon-btn,.lang-btn,.close-btn,.theme-btn {
              width:40px;
              height:40px;
            }
            .status-pill {
              min-height:40px;
              padding:0 12px;
            }
            .now-layout {
              height:auto;
              min-height:0;
              gap:12px;
            }
            .now-left,.now-right {
              gap:12px;
            }
            .now-art-card {
              padding:16px;
            }
            .now-art {
              width:min(100%, 280px);
              border-radius:20px;
            }
            .now-track-meta {
              padding-top:10px;
              text-align:center;
            }
            .now-track-title { font-size:22px; }
            .now-track-subtitle { font-size:13px; }
            .now-controls-card { padding:16px; gap:12px; }
            .now-time-row { font-size:11px; }
            .now-progress { height:10px; }
            .big-round-btn { width:54px; height:54px; border-radius:18px; }
            .big-main-btn { width:76px; height:76px; font-size:26px; }
            .now-controls-main {
              flex-wrap:nowrap;
              gap:8px;
            }
            .now-controls-bottom {
              grid-template-columns:1fr;
              gap:10px;
            }
            .now-volume {
              grid-template-columns:auto minmax(0,1fr);
              gap:10px;
            }
            .now-actions {
              width:100%;
              justify-content:flex-start;
            }
            .now-actions .chip-btn {
              width:100%;
            }
            .now-queue-card {
              padding:14px;
            }
            .now-queue-header {
              gap:8px;
            }
            .group-inline {
              width:100%;
              margin-inline-start:0;
              gap:8px;
            }
            .group-inline .chip-btn {
              flex:1 1 calc(50% - 4px);
            }
            .mini-queue-item {
              padding:10px 8px;
              gap:8px;
            }
            .mini-queue-actions .chip-btn,.queue-actions .chip-btn {
              min-width:40px;
              min-height:38px;
              padding:8px 10px;
              font-size:13px;
            }
            .modal {
              width:min(100%, calc(100cqi - 16px));
              max-height:calc(100dvh - 16px);
              border-radius:20px;
            }
            .immersive-shell {
              padding:
                max(12px, env(safe-area-inset-top))
                max(12px, env(safe-area-inset-right))
                max(12px, env(safe-area-inset-bottom))
                max(12px, env(safe-area-inset-left));
            }
          }
          @media (max-width:480px) {
            .card {
              min-height:min(560px, var(--ma-card-height));
              border-radius:18px;
            }
            .brand-title { font-size:14px; }
            .brand-sub { font-size:10px; }
            .nav { padding:8px; gap:6px; }
            .nav-btn { width:auto; min-height:40px; padding:0 12px; }
            .topbar-row { gap:8px; width:100%; min-width:0; }
            .topbar-actions { gap:8px; }
            .status-pill,.selected-player-box { width:auto; }
            .now-layout { gap:12px; }
            .now-art-card,.now-controls-card,.now-queue-card { border-radius:20px; }
            .now-track-title { font-size:20px; }
            .now-track-subtitle { font-size:13px; }
            .big-round-btn { width:48px; height:48px; border-radius:16px; }
            .big-main-btn { width:68px; height:68px; font-size:24px; }
            .play-btn {
              width:54px;
              height:54px;
              border-radius:18px;
            }
            .np-art {
              width:58px;
              height:58px;
              border-radius:16px;
            }
            .selected-player-title {
              font-size:11px;
            }
            .selected-player-sub {
              font-size:10px;
            }
            .group-inline .chip-btn {
              flex:1 1 100%;
            }
            .now-controls-main {
              gap:6px;
            }
            .mini-queue-actions .chip-btn,.queue-actions .chip-btn {
              min-width:36px;
              min-height:36px;
              padding:8px;
            }
            .modal-body { padding:14px; }
          }
        .mobile-volume-inline .volume-btn .ui-ic{width:20px;height:20px;}
  .card:not(.layout-tablet) .mobile-volume-inline{grid-template-columns:auto minmax(0,1fr) auto;gap:10px;align-items:center;}
  .card:not(.layout-tablet) .mobile-volume-inline .volume-value{order:1;min-width:46px;text-align:center;}
  .card:not(.layout-tablet) .mobile-volume-inline .tablet-volume-track{order:2;}
  .card:not(.layout-tablet) .mobile-volume-inline .volume-btn{order:3;width:38px;height:38px;border-radius:999px;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons{grid-template-columns:auto auto minmax(0,1fr) auto auto;gap:8px;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-value{order:1;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-step-minus{order:2;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .tablet-volume-track{order:3;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-step-plus{order:4;}
  .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-btn{order:5;}
  .card:not(.layout-tablet) .mobile-volume-inline .volume-btn.active{background:rgba(170,38,38,.28)!important;border-color:rgba(255,98,98,.36)!important;color:#fff!important;box-shadow:0 10px 24px rgba(120,22,22,.22)!important;}
  .card:not(.layout-tablet) .queue-action-item{min-height:58px;margin-bottom:10px;}
  .mobile-art-actions{left:50%!important;right:auto!important;transform:translateX(-50%)!important;inset-inline:auto!important;inset-block-end:16px!important;display:flex!important;align-items:center!important;justify-content:center!important;gap:10px!important;padding:0!important;border-radius:0!important;background:transparent!important;border:none!important;box-shadow:none!important;backdrop-filter:none!important;}
  .card.layout-tablet .mobile-art-actions{left:50%!important;right:auto!important;transform:translateX(-50%)!important;inset-inline:auto!important;inset-block-end:18px!important;}
  .theme-light .mobile-art-actions{background:transparent!important;border-color:transparent!important;box-shadow:none!important;}
  .mobile-art-fab{width:46px;min-width:46px;height:46px;border-radius:999px;}
  @media (max-width: 600px){.mobile-art-actions{left:50%!important;right:auto!important;transform:translateX(-50%)!important;inset-inline:auto!important;inset-block-end:12px!important;gap:8px!important;}.mobile-art-fab{width:42px;min-width:42px;height:42px;}}
  .card.layout-tablet .menu-backdrop{justify-content:center!important;align-items:stretch!important;padding:var(--flow-sheet-pad-block) var(--flow-sheet-pad-inline)!important;}
  .card.layout-tablet .menu-sheet{width:min(calc(100% - var(--flow-sheet-gutter)), 920px)!important;max-width:min(calc(100% - var(--flow-sheet-gutter)), 920px)!important;max-height:calc(100% - 26px)!important;height:calc(100% - 26px)!important;margin-inline:auto!important;}
  .card.layout-tablet .menu-sheet.sheet-library,.card.layout-tablet .menu-sheet.sheet-search{width:min(calc(100% - var(--flow-sheet-gutter)), 1120px)!important;max-width:min(calc(100% - var(--flow-sheet-gutter)), 1120px)!important;}
  .card.layout-tablet .menu-sheet.sheet-queue{width:min(calc(100% - var(--flow-sheet-queue-gutter)), 980px)!important;max-width:min(calc(100% - var(--flow-sheet-queue-gutter)), 980px)!important;}
  .card.layout-tablet .menu-sheet.sheet-actions,.card.layout-tablet .menu-sheet.sheet-simple,.card.layout-tablet .menu-sheet.sheet-schedules,.card.layout-tablet .menu-sheet.sheet-players,.card.layout-tablet .menu-sheet.sheet-groupplayers,.card.layout-tablet .menu-sheet.sheet-settings{width:min(calc(100% - var(--flow-sheet-narrow-gutter)), 860px)!important;max-width:min(calc(100% - var(--flow-sheet-narrow-gutter)), 860px)!important;}
  .card.layout-tablet .menu-sheet.sheet-schedules{width:min(calc(100% - var(--flow-sheet-gutter)), 1080px)!important;max-width:min(calc(100% - var(--flow-sheet-gutter)), 1080px)!important;}
  .card.layout-tablet .menu-body.sheet-schedules{justify-items:stretch!important;align-content:start!important;overflow:auto!important;}
  .card.layout-tablet .menu-body.sheet-schedules .settings-shell{height:auto!important;min-height:100%!important;grid-template-rows:auto auto!important;overflow:visible!important;}
  .card.layout-tablet .wake-schedule-layout{height:auto!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;align-items:stretch!important;overflow:visible!important;}
  .card.layout-tablet .wake-schedule-list-card{max-height:none!important;overflow:visible!important;}
  .card.layout-tablet .wake-schedule-editor-card{overflow:visible!important;}
  .card.layout-tablet .scheduled-start-grid{grid-template-columns:minmax(0,1fr)!important;}
  .card.layout-tablet .sleep-timer-corner{inset-block-end:22px!important;inset-block-start:auto!important;transform:none!important;justify-items:end!important;z-index:8!important;}
  .card.layout-tablet.rtl .sleep-timer-corner{inset-inline-end:76px!important;inset-inline-start:auto!important;}
  .card.layout-tablet:not(.rtl) .sleep-timer-corner{inset-inline-start:76px!important;inset-inline-end:auto!important;}
  .card.layout-tablet .queue-list{max-width:920px;margin-inline:auto;}
  .card.layout-tablet .queue-row{min-height:88px!important;}
  .card.layout-tablet .active-player-chip .bars,.card.layout-tablet .active-player-card .bars{display:none!important;}
  .menu-backdrop{overflow:hidden;isolation:isolate;}
  .menu-backdrop::before{content:"";position:absolute;inset:-42px;background:var(--menu-dynamic-art, var(--dynamic-art-url, none)) center/cover no-repeat;filter:blur(42px) saturate(1.12) brightness(.86);transform:scale(1.12);opacity:0;pointer-events:none;z-index:0;transition:opacity .22s ease;}
  .menu-backdrop.has-menu-art::before{opacity:.58;}
  .menu-backdrop::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(5,7,12,.46),rgba(8,10,16,.72));pointer-events:none;z-index:0;}
  .theme-light .menu-backdrop::after{background:linear-gradient(180deg,rgba(238,243,250,.34),rgba(222,229,240,.68));}
  .menu-backdrop>.menu-sheet{position:relative;z-index:1;isolation:isolate;}
  .menu-sheet::before{content:"";position:absolute;inset:-24px;background:var(--menu-dynamic-art, var(--dynamic-art-url, none)) center/cover no-repeat;filter:blur(30px) saturate(1.08);transform:scale(1.08);opacity:0;pointer-events:none;z-index:0;}
  .menu-backdrop.has-menu-art .menu-sheet::before{opacity:.22;}
  .menu-sheet::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(20,22,31,.82),rgba(12,14,22,.94));pointer-events:none;z-index:0;}
  .theme-light .menu-sheet::after{background:linear-gradient(180deg,rgba(255,255,255,.86),rgba(245,248,253,.94));}
  .menu-sheet>*,.menu-head,.menu-body{position:relative;z-index:1;}
  .card.dynamic-theme .modal{position:relative;overflow:hidden;isolation:isolate;}
  .card.dynamic-theme .modal::before{content:"";position:absolute;inset:-24px;background:var(--dynamic-art-url, none) center/cover no-repeat;filter:blur(34px) saturate(1.08);transform:scale(1.08);opacity:.22;pointer-events:none;z-index:0;}
  .card.dynamic-theme .modal::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(20,22,31,.8),rgba(12,14,22,.92));pointer-events:none;z-index:0;}
  .theme-light.card.dynamic-theme .modal::after{background:linear-gradient(180deg,rgba(255,255,255,.84),rgba(245,248,253,.94));}
  .card.dynamic-theme .modal>*{position:relative;z-index:1;}
  .group-player-card{position:relative;}
  .group-player-card.checked,.group-player-row.checked{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .42)!important;background:linear-gradient(180deg,rgba(var(--dynamic-accent-rgb,245 166 35) / .13),rgba(255,255,255,.06))!important;}
  .group-player-card .player-premium-head{position:relative;padding:10px 50px 0 0!important;}
  .group-player-title-row{gap:8px;}
  .group-player-toggle{position:absolute;top:6px;right:7px;left:auto;bottom:auto;width:29px;height:29px;border-radius:0;display:inline-grid;place-items:center;flex:0 0 auto;border:0;background:transparent;color:rgba(210,216,226,.62);box-shadow:none;opacity:.9;z-index:2;pointer-events:none;transition:transform .15s ease,opacity .15s ease,color .15s ease,filter .15s ease;}
  .group-player-toggle.checked{color:rgb(var(--dynamic-accent-rgb,245 166 35));filter:drop-shadow(0 0 8px rgba(var(--dynamic-accent-rgb,245 166 35) / .34));opacity:1;}
  .group-item-toggle{width:28px;height:28px;border-radius:999px;display:inline-grid;place-items:center;flex:0 0 auto;border:1px solid rgba(255,255,255,.18);background:rgba(255,255,255,.08);color:var(--ma-text-1);box-shadow:inset 0 1px 0 rgba(255,255,255,.12);}
  .group-item-toggle.checked{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .42);background:rgba(var(--dynamic-accent-rgb,245 166 35) / .18);color:var(--ma-accent);}
  .theme-light .group-player-toggle{color:rgba(74,84,100,.62);}
  .theme-light .group-player-toggle.checked{color:rgb(var(--dynamic-accent-rgb,245 166 35));}
  .theme-light .group-item-toggle{border-color:rgba(26,39,61,.14);background:rgba(255,255,255,.74);color:#263247;}
  .group-player-toggle .ui-ic{width:18px;height:18px;}
  .group-item-toggle .ui-ic{width:15px;height:15px;}
  .group-player-check,.group-player-card .group-player-check,.group-item input[type="checkbox"]{position:absolute!important;width:1px!important;height:1px!important;opacity:0!important;pointer-events:none!important;margin:0!important;}
  .group-item .group-name{display:flex;align-items:center;gap:10px;min-width:0;}
  .group-item .group-name .group-item-toggle{margin-inline-start:auto;}
  .action-btn.busy{position:relative;display:inline-flex!important;align-items:center;justify-content:center;gap:8px;cursor:progress!important;pointer-events:none;color:var(--ma-accent);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);background:linear-gradient(180deg,rgba(var(--dynamic-accent-rgb,245 166 35) / .2),rgba(255,255,255,.07));}
  .action-btn.busy::before{content:"";width:14px;height:14px;border-radius:999px;border:2px solid currentColor;border-inline-end-color:transparent;animation:spin .72s linear infinite;}
  .action-btn.busy::after{content:"";position:absolute;inset:-4px;border-radius:inherit;border:1px solid rgba(var(--dynamic-accent-rgb,245 166 35) / .28);animation:voiceAssistantListenPulse 1s ease-out infinite;pointer-events:none;}

  </style>

        <div class="card ${rtl ? "rtl" : ""} theme-${visualTheme}${theme === "custom" ? " theme-custom" : ""}${this._isHotelMode() ? " hotel-mode" : ""}${layoutProfileClass ? ` ${layoutProfileClass}` : ""}" style="${layoutProfileStyle}--v2-custom-text:${this._state.mobileCustomTextTone === "dark" ? "#1f2633" : "#ffffff"};">
          <aside class="sidebar">
            <div class="brand">
              <button class="brand-icon" id="brandPlayersBtn" title="${this._i18n("ui.open_music_assistant")}">▶</button>
              <div>
        <div class="brand-title">homeii-music-flow</div>
                <div class="brand-sub">Music Assistant</div>
              </div>
            </div>

            <nav class="nav">
              <button class="nav-btn active" data-view="home"><span class="nav-ico">⌂</span><span>${this._i18n("ui.home")}</span></button>
              <button class="nav-btn" data-view="now_playing"><span class="nav-ico">▶</span><span>${this._i18n("ui.now_playing")}</span></button>
              <button class="nav-btn" data-view="radio"><span class="nav-ico">📻</span><span>${this._i18n("ui.radio")}</span></button>
              <button class="nav-btn" data-view="podcasts"><span class="nav-ico">🎙</span><span>${this._i18n("ui.podcasts")}</span></button>
              <button class="nav-btn" data-view="albums"><span class="nav-ico">◉</span><span>${this._i18n("ui.albums")}</span></button>
              <button class="nav-btn" data-view="artists"><span class="nav-ico">♪</span><span>${this._i18n("ui.artists")}</span></button>
              <button class="nav-btn" data-view="tracks"><span class="nav-ico">♫</span><span>${this._i18n("ui.tracks")}</span></button>
              <button class="nav-btn" data-view="playlists"><span class="nav-ico">☰</span><span>${this._i18n("ui.playlists")}</span></button>
            </nav>

            <div class="player-panel">
              <div class="np-row" id="npRow" title="${this._i18n("ui.now_playing")}">
                <div class="np-art" id="npArt">${this._artPlaceholderHtml("music_note")}</div>
                <div class="np-meta">
                  <div class="np-title" id="npTitle">${this._i18n("ui.nothing_playing")}</div>
                  <div class="np-sub" id="npSub">—</div>
                </div>
              </div>

              <div class="controls">
                <button class="icon-btn" id="btnPrev" title="${this._i18n("ui.previous")}">${this._iconSvg("previous")}</button>
                <button class="play-btn" id="btnPlay" title="${this._i18n("ui.play_pause")}">${this._iconSvg("play")}</button>
                <button class="icon-btn" id="btnNext" title="${this._i18n("ui.next")}">${this._iconSvg("next")}</button>
              </div>

              <div class="volume-row">
                <button class="icon-btn" id="btnMute" title="${this._i18n("ui.mute")}">${this._iconSvg("volume_high")}</button>
                <input class="volume-range" id="volSlider" type="range" min="0" max="100" value="50" style="--vol-pct:50%">
              </div>

              <select class="player-select" id="playerSel" aria-hidden="true" tabindex="-1">
                <option value="">${this._i18n("ui.loading_players")}</option>
              </select>
            </div>
          </aside>

          <section class="main">
            <div class="topbar">
              <div class="topbar-row">
                <div class="search" id="searchWrap">
                  <span>🔍</span>
                  <input id="searchInp" type="text" placeholder="${this._i18n("ui.search_everything")}">
                  <button class="icon-btn" id="searchClear" style="display:none;" title="${this._i18n("ui.clear")}">✕</button>
                </div>

                <div class="selected-player-box" id="selectedPlayerBox">
                  <div class="chip-dot"></div>
                  <div class="selected-player-meta">
                    <div class="selected-player-title" id="selectedPlayerTitle">${this._i18n("ui.selected_player")}</div>
                    <div class="selected-player-sub" id="selectedPlayerSub">—</div>
                  </div>
                </div>

                <div class="topbar-actions">
                  <button class="chip-btn" id="choosePlayerBtn">${this._i18n("ui.choose_player")}</button>
                  <button class="theme-btn" id="themeToggleBtn" style="display:${this._config.show_theme_toggle && !this._isHotelMode() ? "" : "none"};">${theme === "dark" ? "☀" : "🌙"}</button>
                  <button class="chip-btn" id="maOpenBtn" style="display:none;">MA</button>
                  <button class="lang-btn" id="langBtn" title="${this._i18n("ui.language")}">${this._languageShortcutLabel()}</button>
                  <div class="status-pill offline" id="statusPill"><span class="status-dot"></span><span id="statusText">${this._i18n("ui.connecting")}</span></div>
                </div>
              </div>
            </div>

            <div class="content" id="content">
              <div class="state-box"><div><div class="spinner"></div><div>${this._i18n("ui.connecting_2")}</div></div></div>
            </div>

            <div class="modal-backdrop" id="groupModal">
              <div class="modal">
                <div class="modal-header">
                  <div class="modal-header-icon">🔗</div>
                  <div class="modal-header-meta">
                    <div class="modal-title">${this._i18n("ui.group_speakers")}</div>
                    <div class="modal-subtitle" id="groupModalSubtitle">${this._i18n("ui.choose_target_player")}</div>
                  </div>
                  <button class="close-btn" id="groupModalClose">✕</button>
                </div>
                <div class="modal-body">
                  <div class="modal-section">
                    <div class="modal-section-top">
                      <div class="modal-section-title">${this._i18n("ui.group_speakers")}</div>
                      <div class="modal-section-badge" id="groupCountBadge">0</div>
                    </div>
                    <div class="group-list" id="groupList"></div>
                  </div>
                  <div class="group-actions">
                    <button class="chip-btn" id="applyGroupBtn">${this._i18n("ui.apply_group")}</button>
                    <button class="chip-btn warn" id="unGroupBtn">${this._i18n("ui.ungroup")}</button>
                  </div>
                </div>
              </div>
            </div>

            <div class="modal-backdrop" id="playerModal">
              <div class="modal">
                <div class="modal-header">
                  <div class="modal-header-icon" id="playerModalIcon">🎵</div>
                  <div class="modal-header-meta">
                    <div class="modal-title" id="playerModalTitle">${this._i18n("ui.choose_player")}</div>
                    <div class="modal-subtitle" id="playerModalSubtitle">—</div>
                  </div>
                  <button class="close-btn" id="playerModalClose">✕</button>
                </div>
                <div class="modal-body" id="playerModalBody"></div>
              </div>
            </div>

            <div class="toast-wrap" id="toastWrap"></div>
          </section>

          <div class="immersive-backdrop" id="immersiveNowPlaying"></div>
        </div>
      `;
      this.shadowRoot.querySelectorAll(".nav-btn").forEach((btn) => btn.addEventListener("click", () => this._setView(btn.dataset.view, btn)));
      this._startResizeTracking();
      this.$("playerSel")?.addEventListener("change", (e) => this._selectPlayer(e.target.value || null, true));
      this.$("btnPlay").addEventListener("click", () => this._togglePlay());
      this.$("btnPrev").addEventListener("click", () => this._playerCmd("previous"));
      this.$("btnNext").addEventListener("click", () => this._playerCmd("next"));
      this.$("btnMute").addEventListener("click", () => this._toggleMute());
      this.$("npRow").addEventListener("click", () => {
        if (!this._isHotelMode()) this._toggleQueue();
      });
      this.$("npArt").addEventListener("click", (e) => {
        e.stopPropagation();
        this._openNowPlayingView();
      });
      this.$("langBtn").addEventListener("click", () => this._toggleLanguage());
      this.$("choosePlayerBtn").addEventListener("click", () => this._openPlayerModal());
      this.$("brandPlayersBtn").addEventListener("click", () => this._openMusicAssistant());
      this.$("selectedPlayerBox").addEventListener("click", () => this._openPlayerModal());
      this.$("themeToggleBtn").addEventListener("click", () => this._toggleCardTheme());
      this.$("maOpenBtn").addEventListener("click", () => this._openMusicAssistant());
      this.$("volSlider").addEventListener("input", (e) => {
        const pct = Number(e.target.value || 0);
        e.target.style.setProperty("--vol-pct", `${pct}%`);
        this._setButtonIcon(this.$("btnMute"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
        clearTimeout(this._volumeTimer);
        this._volumeTimer = setTimeout(() => this._setVolume(pct / 100), 120);
        this._syncBigVolumeMirror(pct);
      });
      this._bindProgressSeekBar(this.$("progressBar"));
      const searchInput = this.$("searchInp");
      const searchClear = this.$("searchClear");
      searchInput.addEventListener("input", () => {
        const q = searchInput.value.trim();
        this._state.query = q;
        searchClear.style.display = q ? "" : "none";
        clearTimeout(this._searchTimer);
        this._searchTimer = setTimeout(() => q ? this._renderGlobalSearch(q) : this._renderCurrentView(), 300);
      });
      searchInput.addEventListener("keydown", (e) => e.stopPropagation());
      searchClear.addEventListener("click", () => {
        searchInput.value = "";
        this._state.query = "";
        searchClear.style.display = "none";
        this._renderCurrentView();
      });
      this.$("content").addEventListener("click", this._boundContentClick);
      this.$("content").addEventListener("change", (e) => this._handleQueueMoveAutoChange(e));
      this.$("content").addEventListener("contextmenu", this._boundContentContext);
      this.$("groupList").addEventListener("change", (e) => this._handleGroupChange(e));
      this.$("applyGroupBtn").addEventListener("click", (event) => this._runMenuButtonLoading(event.currentTarget, this._i18n("ui.connecting_2"), () => this._applySpeakerGroup(), { kind: "connect" }));
      this.$("unGroupBtn").addEventListener("click", (event) => this._runMenuButtonLoading(event.currentTarget, this._i18n("ui.disconnecting"), () => this._clearSpeakerGroup(), { kind: "disconnect" }));
      this.$("groupModalClose").addEventListener("click", () => this._closeGroupModal());
      this.$("groupModal").addEventListener("click", (e) => {
        if (e.target === this.$("groupModal")) this._closeGroupModal();
      });
      this.$("playerModalClose").addEventListener("click", () => this._closePlayerModal());
      this.$("playerModal").addEventListener("click", (e) => {
        if (e.target === this.$("playerModal")) this._closePlayerModal();
      });
      document.addEventListener("click", this._boundDocClick);
    }
    async _init() {
      try {
        await this._ensureConfigEntryId();
        this._loadPlayers();
        this._connectMA();
        this._refreshGroupingState();
        this._renderPlayerSummary();
        this._syncMaButtonVisibility();
        if (this._state.view === "now_playing") await this._renderNowPlayingPage();
        else if (this._state.query) await this._renderGlobalSearch(this._state.query);
        else await this._renderCurrentView();
        this._startLoops();
        setTimeout(() => {
          if (this._state.view === "home" && !this._state.query) this._renderHome();
        }, 2500);
      } catch (e) {
        this._renderError(e);
      }
    }
    $(id) {
      return this.shadowRoot.getElementById(id);
    }
    _languageBaseCode(value = "") {
      return String(value || "").trim().toLowerCase().replace("_", "-").split("-")[0];
    }
    _languageOptions(includeAuto = false) {
      return HOMEII_VISIBLE_LANGUAGE_OPTIONS2.filter((option) => includeAuto || option.value !== "auto");
    }
    _languageShortLabel(value = "") {
      const code = this._languageBaseCode(value);
      if (code === "he") return "עב";
      return code ? code.slice(0, 2).toUpperCase() : "EN";
    }
    _nextLanguageCode() {
      const options = this._languageOptions(false).map((option) => this._languageBaseCode(option.value)).filter(Boolean);
      if (!options.length) return "en";
      const current = this._languageBaseCode(this._language() || this._state.lang || this._config?.language || "en");
      const index = options.indexOf(current);
      return options[(index + 1) % options.length] || options[0] || "en";
    }
    _languageShortcutLabel() {
      return this._languageShortLabel(this._nextLanguageCode());
    }
    _settingsLanguageSelectHtml() {
      const current = this._languageBaseCode(this._state.lang || this._config?.language || this._language() || "en");
      return `
        <label class="settings-select-card language-select-card">
          <span class="settings-select-card-icon">${this._iconSvg("language")}</span>
          <select class="media-sort-select settings-select language-select" id="mobileLanguageSelect" aria-label="${this._esc(this._i18n("ui.language"))}">
            ${this._languageOptions(true).map((option) => {
        const value = this._languageBaseCode(option.value);
        return `<option value="${this._esc(option.value)}" ${value === current ? "selected" : ""}>${this._esc(option.label || option.value)}</option>`;
      }).join("")}
          </select>
        </label>
      `;
    }
    _toggleLanguage() {
      this._state.lang = this._nextLanguageCode();
      try {
        localStorage.setItem("homeii_music_flow_lang", this._state.lang);
      } catch (_) {
      }
      const currentTheme = this._state.cardTheme;
      const currentPlayer = this._state.selectedPlayer;
      const hadAutoSelected = this._state.hasAutoSelectedPlayer;
      const currentView = this._state.view;
      const currentQuery = this._state.query;
      const currentNowPlayingQuery = this._state.nowPlayingQuery;
      const currentImmersive = this._state.immersiveNowPlayingOpen;
      if (this._imgObserver) {
        this._imgObserver.disconnect();
        this._imgObserver = null;
        this._imgObserverRoot = null;
      }
      if (this._ws) {
        try {
          this._ws.close();
        } catch (_) {
        }
        this._ws = null;
      }
      clearInterval(this._pollTimer);
      clearInterval(this._progressTimer);
      clearTimeout(this._searchTimer);
      clearTimeout(this._nowPlayingSearchTimer);
      clearTimeout(this._volumeTimer);
      clearTimeout(this._bigVolumeTimer);
      clearTimeout(this._seekTimer);
      clearTimeout(this._systemMobileStatePersistTimer);
      clearTimeout(this._resizeTimer);
      this._built = false;
      this._state.view = currentView;
      this._state.query = currentQuery;
      this._state.nowPlayingQuery = currentNowPlayingQuery;
      this._state.queueVisible = false;
      this._state.playerModalOpen = false;
      this._state.maQueueState = null;
      this._state.queueItems = [];
      this._state.queueMutationPendingUntil = 0;
      this._state.queueMutationExpectedSignature = "";
      this._state.nowPlayingUri = "";
      this._state.selectedPlayer = currentPlayer;
      this._state.hasAutoSelectedPlayer = hadAutoSelected;
      this._state.cardTheme = currentTheme;
      this._state.immersiveNowPlayingOpen = false;
      this._imageBlobCache.clear();
      this._build();
      this._init().then(() => {
        if (currentImmersive && this._state.view === "now_playing") this._openImmersiveNowPlaying();
      });
    }
    _toggleCardTheme() {
      const effective = this._effectiveTheme();
      this._state.cardTheme = effective === "dark" ? "light" : "dark";
      try {
        localStorage.setItem("homeii_music_flow_theme", this._state.cardTheme);
      } catch (_) {
      }
      const card = this.shadowRoot.querySelector(".card");
      if (card) {
        card.classList.remove("theme-dark", "theme-light");
        card.classList.add(`theme-${this._effectiveTheme()}`);
      }
      this._updateThemeButton();
    }
    _playerStateLabel(player) {
      if (!player) return this._i18n("ui.idle");
      if (player.state === "playing") return this._i18n("ui.playing");
      if (player.state === "paused") return this._i18n("ui.paused");
      return this._i18n("ui.idle");
    }
    _isPlayerActive(player) {
      if (!player) return false;
      const attrs = player.attributes || {};
      return player.state === "playing" || player.state === "paused" || !!attrs.media_title || !!attrs.active_queue;
    }
    _thisDeviceStorageKey() {
      return `homeii-this-device-player::${this._maBrowserUrl() || this._config?.ma_url || location.origin || "default"}`;
    }
    _getRememberedThisDevicePlayerId() {
      try {
        return localStorage.getItem(this._thisDeviceStorageKey()) || "";
      } catch (_) {
        return "";
      }
    }
    _rememberThisDevicePlayer(entityId) {
      try {
        if (!entityId) localStorage.removeItem(this._thisDeviceStorageKey());
        else localStorage.setItem(this._thisDeviceStorageKey(), entityId);
      } catch (_) {
      }
    }
    _localText(en, he) {
      return typeof this._m === "function" ? this._m(en, he) : this._isHebrew() ? he : en;
    }
    _localSendspinIdStorageKey() {
      return "homeii_sendspin_webplayer_id";
    }
    _legacySendspinIdStorageKey() {
      return "sendspin_webplayer_id";
    }
    _localSendspinDesiredStorageKey() {
      return "homeii_local_sendspin_desired";
    }
    _isLocalSendspinDesired() {
      const session = this._localSendspinGlobalSession(false);
      if (session?.desired) return true;
      if (this._localSendspinDesired) return true;
      try {
        return sessionStorage.getItem(this._localSendspinDesiredStorageKey()) === "1";
      } catch (_) {
        return false;
      }
    }
    _setLocalSendspinDesired(enabled) {
      this._localSendspinDesired = !!enabled;
      const session = this._localSendspinGlobalSession();
      if (session) session.desired = !!enabled;
      try {
        if (enabled) sessionStorage.setItem(this._localSendspinDesiredStorageKey(), "1");
        else sessionStorage.removeItem(this._localSendspinDesiredStorageKey());
      } catch (_) {
      }
    }
    _clearLocalSendspinReconnectTimer() {
      if (this._localSendspinReconnectTimer) {
        clearTimeout(this._localSendspinReconnectTimer);
        this._localSendspinReconnectTimer = null;
      }
    }
    _localSendspinGlobalSession(create = true) {
      if (typeof window === "undefined") return null;
      const key = "__homeiiLocalSendspinSessionV1";
      if (!window[key] && create) {
        window[key] = {
          player: null,
          socket: null,
          audioElement: null,
          connected: false,
          connecting: false,
          playerId: "",
          state: null,
          desired: false,
          controller: null,
          lifecycleListening: false,
          lifecycleHandler: null
        };
      }
      return window[key] || null;
    }
    _adoptLocalSendspinGlobalSession() {
      const session = this._localSendspinGlobalSession(false);
      if (!session) return null;
      this._localSendspinPlayer = session.player || null;
      this._localSendspinSocket = session.socket || null;
      this._localSendspinConnected = !!session.connected;
      this._localSendspinConnecting = !!session.connecting;
      this._localSendspinPlayerId = session.playerId || this._localSendspinPlayerId || "";
      this._localSendspinState = session.state || this._localSendspinState || null;
      this._localSendspinDesired = !!session.desired || this._localSendspinDesired;
      const desired = session.desired || this._isLocalSendspinDesired();
      if (desired) {
        this._state.localSendspinStatus = session.connected ? "connected" : session.connecting ? "connecting" : "reconnecting";
        if (session.connected) this._state.awaitingThisDevicePlayer = false;
      }
      return session;
    }
    _syncLocalSendspinGlobalSession() {
      const session = this._localSendspinGlobalSession();
      if (!session) return null;
      session.player = this._localSendspinPlayer || null;
      session.socket = this._localSendspinSocket || null;
      session.connected = !!this._localSendspinConnected;
      session.connecting = !!this._localSendspinConnecting;
      session.playerId = this._localSendspinPlayerId || session.playerId || "";
      session.state = this._localSendspinState || null;
      session.desired = !!this._isLocalSendspinDesired();
      if (!session.controller || session.controller === this || this.isConnected) session.controller = this;
      return session;
    }
    _clearLocalSendspinGlobalRuntime() {
      const session = this._localSendspinGlobalSession(false);
      if (!session) return;
      session.player = null;
      session.socket = null;
      session.connected = false;
      session.connecting = false;
      session.state = null;
      if (!session.desired) session.playerId = "";
      if (session.controller === this) session.controller = null;
    }
    _sanitizeLocalSendspinPlayerId(value) {
      return String(value || "").trim().replace(/[^\w.-]/g, "_");
    }
    _isHomeiiSendspinPlayerId(value) {
      const playerId = this._sanitizeLocalSendspinPlayerId(value);
      return /^ma_homeii_[\w.-]+$/i.test(playerId) || /^homeii_[\w.-]+$/i.test(playerId);
    }
    _peekLocalSendspinPlayerId() {
      if (this._localSendspinPlayerId) {
        const playerId = this._sanitizeLocalSendspinPlayerId(this._localSendspinPlayerId);
        return this._isHomeiiSendspinPlayerId(playerId) ? playerId : "";
      }
      try {
        const playerId = this._sanitizeLocalSendspinPlayerId(localStorage.getItem(this._localSendspinIdStorageKey()) || "");
        return this._isHomeiiSendspinPlayerId(playerId) ? playerId : "";
      } catch (_) {
        return "";
      }
    }
    _getLocalSendspinPlayerId() {
      if (this._localSendspinPlayerId) return this._localSendspinPlayerId;
      let playerId = "";
      try {
        playerId = this._sanitizeLocalSendspinPlayerId(localStorage.getItem(this._localSendspinIdStorageKey()) || "");
      } catch (_) {
      }
      if (playerId && !this._isHomeiiSendspinPlayerId(playerId)) playerId = "";
      if (!playerId) {
        const randomPart = Math.random().toString(36).slice(2, 12);
        playerId = `ma_homeii_${randomPart}`;
      }
      try {
        localStorage.setItem(this._localSendspinIdStorageKey(), playerId);
      } catch (_) {
      }
      this._localSendspinPlayerId = playerId;
      return playerId;
    }
    _localSendspinModuleUrl() {
      return new URL(
        /* @vite-ignore */
        "./sendspin-js/index.js",
        import.meta.url
      ).href;
    }
    async _loadLocalSendspinModule() {
      if (!this._localSendspinModule) {
        this._localSendspinModule = import(this._localSendspinModuleUrl()).catch((error) => {
          this._localSendspinModule = null;
          const detail = error?.message ? `: ${error.message}` : "";
          throw new Error(`${this._localText("Could not load the local Sendspin player files", "לא הצלחתי לטעון את קבצי נגן Sendspin המקומי")}${detail}`);
        });
      }
      return this._localSendspinModule;
    }
    _localSendspinSyncDelayStorageKey() {
      return "homeii_local_sendspin_sync_delay_ms";
    }
    _localSendspinWsUrl() {
      const baseUrl = this._maBrowserUrl();
      const base = new URL(baseUrl, typeof window !== "undefined" ? window.location.href : void 0);
      const protocol = base.protocol === "https:" ? "wss:" : "ws:";
      const path = base.pathname.replace(/\/$/, "");
      return `${protocol}//${base.host}${path}/sendspin`;
    }
    _assertLocalSendspinConfig() {
      const baseUrl = this._maBrowserUrl();
      if (!baseUrl || !this._maToken) {
        throw new Error(this._localText(
          "Set a direct Music Assistant URL and token in the card settings before connecting this device.",
          "כדי לחבר את המכשיר מתוך הכרטיס צריך להגדיר בכרטיס כתובת Music Assistant ישירה וגם token."
        ));
      }
      if (typeof WebSocket === "undefined") {
        throw new Error(this._localText("This browser does not support WebSocket.", "הדפדפן הזה לא תומך ב-WebSocket."));
      }
      this._assertMaBrowserUrlSecure(baseUrl);
    }
    _openAuthenticatedSendspinSocket(playerId) {
      const wsUrl = this._localSendspinWsUrl();
      return new Promise((resolve, reject) => {
        let settled = false;
        let ws = null;
        let timeout = null;
        const WebSocketCtor = window.__homeiiSendspinInterceptorV1?.original || window.WebSocket;
        const cleanup = () => {
          if (timeout) clearTimeout(timeout);
          if (ws) {
            ws.onopen = null;
            ws.onmessage = null;
            ws.onerror = null;
            ws.onclose = null;
          }
        };
        const fail = (error) => {
          if (settled) return;
          settled = true;
          cleanup();
          try {
            ws?.close?.();
          } catch (_) {
          }
          reject(error instanceof Error ? error : new Error(String(error || "Sendspin connection failed")));
        };
        try {
          this._debugLog("info", "[Homeii Sendspin] opening proxy socket", wsUrl);
          ws = new WebSocketCtor(wsUrl);
          ws.binaryType = "arraybuffer";
        } catch (error) {
          fail(error);
          return;
        }
        timeout = setTimeout(() => fail(new Error(this._localText("Timed out connecting to Sendspin.", "החיבור ל-Sendspin לקח יותר מדי זמן."))), 1e4);
        ws.onopen = () => {
          try {
            this._debugLog("info", "[Homeii Sendspin] sending auth to proxy");
            ws.send(JSON.stringify({ type: "auth", token: this._maToken, client_id: playerId }));
          } catch (error) {
            fail(error);
          }
        };
        ws.onmessage = (event) => {
          if (settled) return;
          if (typeof event?.data === "string") {
            try {
              const message = JSON.parse(event.data);
              this._debugLog("info", "[Homeii Sendspin] proxy auth response", message?.type || message);
              if (message?.type === "auth_invalid" || message?.type === "auth_failed" || message?.error) {
                fail(new Error(this._localText("Music Assistant rejected the Sendspin token.", "Music Assistant דחה את ה-token של Sendspin.")));
                return;
              }
            } catch (_) {
              this._debugLog("info", "[Homeii Sendspin] proxy auth response", event.data);
            }
          } else {
            this._debugLog("info", "[Homeii Sendspin] proxy auth response", "binary");
          }
          settled = true;
          cleanup();
          resolve(ws);
        };
        ws.onerror = () => fail(new Error(this._localText("Could not open the Sendspin WebSocket.", "לא הצלחתי לפתוח חיבור WebSocket ל-Sendspin.")));
        ws.onclose = () => fail(new Error(this._localText("Sendspin closed the connection before the player was ready.", "Sendspin סגר את החיבור לפני שהנגן היה מוכן.")));
      });
    }
    _createLocalSendspinBridge(ws) {
      const listeners = { open: /* @__PURE__ */ new Set(), message: /* @__PURE__ */ new Set(), error: /* @__PURE__ */ new Set(), close: /* @__PURE__ */ new Set() };
      const handlers = { open: null, message: null, error: null, close: null };
      const dispatch = (type, event) => {
        const handler = handlers[type];
        if (typeof handler === "function") handler.call(bridge, event);
        listeners[type]?.forEach((listener) => {
          if (typeof listener === "function") listener.call(bridge, event);
          else if (listener && typeof listener.handleEvent === "function") listener.handleEvent(event);
        });
      };
      const emitLateOpenIfNeeded = (handler) => {
        if (handler && bridge._isOpen) {
          setTimeout(() => handler.call(bridge, new Event("open")), 0);
        }
      };
      const bridge = {
        CONNECTING: 0,
        OPEN: 1,
        CLOSING: 2,
        CLOSED: 3,
        _isOpen: ws.readyState === WebSocket.OPEN,
        get onopen() {
          return handlers.open;
        },
        set onopen(handler) {
          handlers.open = handler;
          emitLateOpenIfNeeded(handler);
        },
        get onmessage() {
          return handlers.message;
        },
        set onmessage(handler) {
          handlers.message = handler;
        },
        get onerror() {
          return handlers.error;
        },
        set onerror(handler) {
          handlers.error = handler;
        },
        get onclose() {
          return handlers.close;
        },
        set onclose(handler) {
          handlers.close = handler;
        },
        get readyState() {
          return ws.readyState;
        },
        get binaryType() {
          return ws.binaryType || "arraybuffer";
        },
        set binaryType(value) {
          try {
            ws.binaryType = value;
          } catch (_) {
          }
        },
        get bufferedAmount() {
          return ws.bufferedAmount || 0;
        },
        get extensions() {
          return ws.extensions || "";
        },
        get protocol() {
          return ws.protocol || "";
        },
        get url() {
          return ws.url || "";
        },
        send: (data) => {
          if (ws.readyState !== WebSocket.OPEN) {
            this._debugLog("warn", "[Homeii Sendspin] bridge send ignored, socket not open");
            return;
          }
          if (data instanceof Blob) {
            data.arrayBuffer().then((buffer) => ws.send(buffer));
          } else {
            if (typeof data === "string") {
              try {
                const message = JSON.parse(data);
                this._debugLog("info", "[Homeii Sendspin] client message", message?.type || message);
              } catch (_) {
              }
            }
            ws.send(data);
          }
        },
        close: (code, reason) => ws.close(code, reason),
        addEventListener: (type, listener) => {
          if (listeners[type]) listeners[type].add(listener);
          if (type === "open" && bridge._isOpen && typeof listener === "function") {
            setTimeout(() => listener.call(bridge, new Event("open")), 0);
          }
        },
        removeEventListener: (type, listener) => {
          if (listeners[type]) listeners[type].delete(listener);
        },
        dispatchEvent: () => false
      };
      ws.onopen = (event) => {
        bridge._isOpen = true;
        dispatch("open", event || new Event("open"));
      };
      ws.onmessage = (event) => {
        if (typeof event?.data === "string") {
          try {
            const message = JSON.parse(event.data);
            this._debugLog("info", "[Homeii Sendspin] server message", message?.type || message);
          } catch (_) {
          }
        }
        dispatch("message", event);
      };
      ws.onerror = (event) => {
        this._debugLog("error", "[Homeii Sendspin] bridge socket error", event);
        dispatch("error", event || new Event("error"));
      };
      ws.onclose = (event) => {
        bridge._isOpen = false;
        this._debugLog("warn", "[Homeii Sendspin] bridge socket closed", event?.code, event?.reason);
        dispatch("close", event || new CloseEvent("close"));
        this._handleLocalSendspinSocketClosed(event || new Event("close"));
      };
      return bridge;
    }
    _installLocalSendspinInterceptor() {
      if (typeof window === "undefined" || !window.WebSocket) return null;
      if (window.__homeiiSendspinInterceptorV1?.installed) {
        return window.__homeiiSendspinInterceptorV1.original;
      }
      const OriginalWebSocket = window.WebSocket;
      const WrappedWebSocket = function(url, protocols) {
        const urlText = String(url || "");
        const pendingBridge = window.__homeiiSendspinPendingBridgeV1;
        if (urlText.includes("/sendspin") && pendingBridge) {
          this._debugLog("info", "[Homeii Sendspin] using prepared bridge for", urlText);
          window.__homeiiSendspinPendingBridgeV1 = null;
          return pendingBridge;
        }
        return protocols === void 0 ? new OriginalWebSocket(url) : new OriginalWebSocket(url, protocols);
      };
      WrappedWebSocket.CONNECTING = OriginalWebSocket.CONNECTING;
      WrappedWebSocket.OPEN = OriginalWebSocket.OPEN;
      WrappedWebSocket.CLOSING = OriginalWebSocket.CLOSING;
      WrappedWebSocket.CLOSED = OriginalWebSocket.CLOSED;
      WrappedWebSocket.prototype = OriginalWebSocket.prototype;
      window.WebSocket = WrappedWebSocket;
      window.__homeiiSendspinInterceptorV1 = { installed: true, original: OriginalWebSocket };
      this._debugLog("info", "[Homeii Sendspin] WebSocket interceptor installed");
      return OriginalWebSocket;
    }
    _restoreLocalSendspinInterceptor() {
      if (typeof window === "undefined") return;
      const installed = window.__homeiiSendspinInterceptorV1;
      if (installed?.installed && installed.original) {
        try {
          window.WebSocket = installed.original;
        } catch (_) {
        }
      }
      try {
        window.__homeiiSendspinPendingBridgeV1 = null;
      } catch (_) {
      }
      try {
        delete window.__homeiiSendspinInterceptorV1;
      } catch (_) {
        window.__homeiiSendspinInterceptorV1 = null;
      }
    }
    _prepareLocalSendspinSession(ws) {
      const bridge = this._createLocalSendspinBridge(ws);
      this._installLocalSendspinInterceptor();
      window.__homeiiSendspinPendingBridgeV1 = bridge;
      return bridge;
    }
    _ensureLocalSendspinAudioElement() {
      const session = this._localSendspinGlobalSession();
      let audio = session?.audioElement || null;
      if (audio && !audio.isConnected) audio = null;
      if (!audio && typeof document !== "undefined") audio = document.getElementById("homeiiLocalAudio");
      if (!audio) {
        audio = document.createElement("audio");
        audio.id = "homeiiLocalAudio";
        audio.className = "homeii-local-audio";
        audio.setAttribute("playsinline", "");
        audio.setAttribute("webkit-playsinline", "");
        audio.setAttribute("aria-hidden", "true");
        document.body?.appendChild(audio);
      }
      audio.controls = false;
      audio.preload = "auto";
      audio.autoplay = true;
      if (session) session.audioElement = audio;
      return audio;
    }
    _supportsMediaSession() {
      return typeof navigator !== "undefined" && !!navigator.mediaSession;
    }
    _resetLocalSendspinMediaSession() {
      if (!this._supportsMediaSession()) return;
      const mediaSession = navigator.mediaSession;
      try {
        mediaSession.metadata = null;
      } catch (_) {
      }
      try {
        mediaSession.playbackState = "none";
      } catch (_) {
      }
      ["play", "pause", "stop", "previoustrack", "nexttrack", "seekbackward", "seekforward", "seekto"].forEach((action) => {
        try {
          mediaSession.setActionHandler(action, null);
        } catch (_) {
        }
      });
    }
    _syncLocalSendspinMediaSession(player = this._getSelectedPlayer(), queueItem = this._state.maQueueState?.current_item || null) {
      if (!this._supportsMediaSession()) return;
      const mediaSession = navigator.mediaSession;
      if (!player || !this._isLocalSendspinPlayer(player)) {
        this._resetLocalSendspinMediaSession();
        return;
      }
      const media = queueItem?.media_item || {};
      const title = media?.name || queueItem?.name || player.attributes?.media_title || this._i18n("ui.nothing_playing");
      const artist = Array.isArray(media?.artists) ? media.artists.map((entry) => entry?.name).filter(Boolean).join(", ") : queueItem?.media_artist || player.attributes?.media_artist || "";
      const album = media?.album?.name || queueItem?.album || player.attributes?.media_album_name || "";
      const art = this._currentArtworkUrl(player, queueItem, 512) || "";
      try {
        if (typeof window.MediaMetadata === "function") {
          mediaSession.metadata = new window.MediaMetadata({
            title,
            artist,
            album,
            artwork: art ? [
              { src: art, sizes: "96x96", type: "image/png" },
              { src: art, sizes: "192x192", type: "image/png" },
              { src: art, sizes: "256x256", type: "image/png" },
              { src: art, sizes: "512x512", type: "image/png" }
            ] : []
          });
        }
      } catch (_) {
      }
      try {
        mediaSession.playbackState = player.state === "playing" ? "playing" : player.state === "paused" ? "paused" : "none";
      } catch (_) {
      }
      const bind = (action, handler) => {
        try {
          mediaSession.setActionHandler(action, handler);
        } catch (_) {
        }
      };
      bind("play", () => this._handleLocalSendspinMediaAction("play"));
      bind("pause", () => this._handleLocalSendspinMediaAction("pause"));
      bind("stop", () => this._handleLocalSendspinMediaAction("stop"));
      bind("previoustrack", () => this._handleLocalSendspinMediaAction("previous"));
      bind("nexttrack", () => this._handleLocalSendspinMediaAction("next"));
      bind("seekbackward", null);
      bind("seekforward", null);
      bind("seekto", null);
    }
    async _handleLocalSendspinMediaAction(action = "") {
      const player = this._getSelectedPlayer();
      if (!player || !this._isLocalSendspinPlayer(player)) return;
      const command = String(action || "").toLowerCase();
      try {
        if (command === "next" || command === "previous") {
          await this._playerCmdFor(player.entity_id, command);
        } else if (command === "stop") {
          await this._stopPlayer(player.entity_id);
        } else if (command === "play" || command === "pause") {
          const directCommand = command === "play" ? "players/cmd/play" : "players/cmd/pause";
          if (this._isDirectMaPlayer(player)) {
            await this._callDirectMaPlayerCommand(player, directCommand);
          } else {
            await this._hass?.callService?.("media_player", command === "play" ? "media_play" : "media_pause", { entity_id: player.entity_id });
          }
        }
        await this._refreshDirectMaPlayers().catch(() => {
        });
        setTimeout(() => this._ensureQueueSnapshot(true), 450);
      } catch (error) {
        this._debugLog("warn", "[Homeii Sendspin] media session action failed", command, error);
      }
    }
    _clearLocalSendspinDiscoveryTimers() {
      (this._localSendspinDiscoveryTimers || []).forEach((timer) => clearTimeout(timer));
      this._localSendspinDiscoveryTimers = [];
    }
    _scheduleThisDevicePlayerDiscovery() {
      this._clearLocalSendspinDiscoveryTimers();
      const delays = [500, 1500, 3e3, 6e3, 1e4];
      this._localSendspinDiscoveryTimers = delays.map((delay) => setTimeout(() => {
        this._loadPlayers();
        this._refreshDirectMaPlayers({ renderMenu: true }).catch(() => {
        });
        if (this._state.menuOpen && typeof this._renderMobileMenu === "function") {
          this._renderMobileMenu().catch(() => {
          });
        }
      }, delay));
    }
    _scheduleLocalSendspinReconnect(reason = "lifecycle", delayMs = 900) {
      this._adoptLocalSendspinGlobalSession();
      if (!this._isLocalSendspinDesired()) return;
      if (!this._maBrowserUrl() || !this._maToken) return;
      if (this._localSendspinConnecting || this._localSendspinConnected) return;
      if (typeof document !== "undefined" && document.visibilityState === "hidden") return;
      this._clearLocalSendspinReconnectTimer();
      this._localSendspinReconnectTimer = setTimeout(() => {
        this._localSendspinReconnectTimer = null;
        this._adoptLocalSendspinGlobalSession();
        if (!this._isLocalSendspinDesired() || this._localSendspinConnecting || this._localSendspinConnected) return;
        if (typeof document !== "undefined" && document.visibilityState === "hidden") return;
        this._debugLog("info", "[Homeii Sendspin] reconnecting local player", reason);
        this._startLocalSendspinPlayer({ automatic: true }).catch((error) => {
          this._debugLog("warn", "[Homeii Sendspin] automatic reconnect failed", error);
        });
      }, Math.max(0, Number(delayMs) || 0));
    }
    _handleLocalSendspinLifecycle(event = null) {
      if (!this._isLocalSendspinDesired()) return;
      this._cancelLocalSendspinDisconnect();
      const type = event?.type || "lifecycle";
      if (typeof document !== "undefined" && document.visibilityState === "hidden") {
        this._state.localSendspinStatus = this._localSendspinConnected ? "connected" : "suspended";
        return;
      }
      this._scheduleLocalSendspinReconnect(type, type === "pageshow" ? 350 : 800);
    }
    _attachLocalSendspinLifecycleListeners() {
      if (typeof window === "undefined" || typeof document === "undefined") return;
      const session = this._localSendspinGlobalSession();
      if (!session) return;
      session.controller = this;
      if (session.lifecycleListening) {
        this._localSendspinLifecycleListening = true;
        return;
      }
      session.lifecycleHandler = (event) => {
        const activeSession = this._localSendspinGlobalSession(false);
        const controller = activeSession?.controller || this;
        if (controller && typeof controller._handleLocalSendspinLifecycle === "function") {
          controller._handleLocalSendspinLifecycle(event);
        }
      };
      document.addEventListener("visibilitychange", session.lifecycleHandler);
      window.addEventListener("pageshow", session.lifecycleHandler);
      window.addEventListener("focus", session.lifecycleHandler);
      window.addEventListener("online", session.lifecycleHandler);
      session.lifecycleListening = true;
      this._localSendspinLifecycleListening = true;
    }
    _detachLocalSendspinLifecycleListeners() {
      if (typeof window === "undefined" || typeof document === "undefined") return;
      const session = this._localSendspinGlobalSession(false);
      if (!session?.lifecycleListening || !session.lifecycleHandler) {
        this._localSendspinLifecycleListening = false;
        return;
      }
      if (this._isLocalSendspinDesired() || session.desired) {
        this._localSendspinLifecycleListening = false;
        return;
      }
      document.removeEventListener("visibilitychange", session.lifecycleHandler);
      window.removeEventListener("pageshow", session.lifecycleHandler);
      window.removeEventListener("focus", session.lifecycleHandler);
      window.removeEventListener("online", session.lifecycleHandler);
      session.lifecycleListening = false;
      session.lifecycleHandler = null;
      if (session.controller === this) session.controller = null;
      this._localSendspinLifecycleListening = false;
    }
    _handleLocalSendspinSocketClosed(event = null) {
      if (this._localSendspinSuppressClose) return;
      if (this._localSendspinConnecting) return;
      if (!this._localSendspinConnected && !this._localSendspinPlayer && !this._localSendspinSocket) return;
      const desired = this._isLocalSendspinDesired();
      this._localSendspinConnected = false;
      this._localSendspinSocket = null;
      this._state.localSendspinStatus = desired ? "reconnecting" : "idle";
      const session = this._syncLocalSendspinGlobalSession();
      const controller = session?.controller && typeof session.controller._scheduleLocalSendspinReconnect === "function" ? session.controller : this;
      if (desired) {
        controller._scheduleLocalSendspinReconnect(event?.type || "socket_close", 1200);
      }
    }
    _cancelLocalSendspinDisconnect() {
      if (this._localSendspinDisconnectTimer) {
        clearTimeout(this._localSendspinDisconnectTimer);
        this._localSendspinDisconnectTimer = null;
      }
    }
    _scheduleLocalSendspinStop(reason = "shutdown", delayMs = 3e4) {
      this._cancelLocalSendspinDisconnect();
      if (!this._localSendspinPlayer && !this._localSendspinSocket) return;
      this._localSendspinDisconnectTimer = setTimeout(() => {
        this._localSendspinDisconnectTimer = null;
        if (this.isConnected) return;
        this._stopLocalSendspinPlayer(reason);
      }, Math.max(0, Number(delayMs) || 0));
    }
    _stopLocalSendspinPlayer(reason = "shutdown") {
      this._cancelLocalSendspinDisconnect();
      if (reason === "user_request") {
        this._setLocalSendspinDesired(false);
        this._clearLocalSendspinReconnectTimer();
      }
      this._clearLocalSendspinDiscoveryTimers();
      if (typeof window !== "undefined") window.__homeiiSendspinPendingBridgeV1 = null;
      const suppressClose = ["another_server", "shutdown", "restart", "user_request"].includes(reason);
      if (suppressClose) this._localSendspinSuppressClose = true;
      if (this._localSendspinPlayer) {
        const safeReason = ["another_server", "shutdown", "restart", "user_request"].includes(reason) ? reason : "shutdown";
        try {
          this._localSendspinPlayer.disconnect?.(safeReason);
        } catch (_) {
        }
      }
      if (this._localSendspinSocket) {
        try {
          if (this._localSendspinSocket.readyState === WebSocket.OPEN || this._localSendspinSocket.readyState === WebSocket.CONNECTING) {
            this._localSendspinSocket.close();
          }
        } catch (_) {
        }
      }
      this._localSendspinPlayer = null;
      this._localSendspinSocket = null;
      this._localSendspinConnected = false;
      this._localSendspinState = null;
      this._state.localSendspinStatus = this._isLocalSendspinDesired() ? "reconnecting" : "idle";
      this._syncLocalSendspinGlobalSession();
      if (reason === "user_request") {
        this._clearLocalSendspinGlobalRuntime();
        this._detachLocalSendspinLifecycleListeners();
      }
      if (suppressClose) setTimeout(() => {
        this._localSendspinSuppressClose = false;
      }, 1e3);
    }
    async _startLocalSendspinPlayer(options = {}) {
      const automatic = !!options.automatic;
      this._adoptLocalSendspinGlobalSession();
      if (!automatic) this._setLocalSendspinDesired(true);
      if (this._localSendspinConnecting) {
        if (!automatic) this._toast(this._localText("Local player is already connecting.", "הנגן המקומי כבר בתהליך התחברות."));
        return;
      }
      if (this._localSendspinPlayer && this._localSendspinConnected) {
        this._state.awaitingThisDevicePlayer = true;
        this._scheduleThisDevicePlayerDiscovery();
        if (!automatic) this._toastSuccess(this._localText("Local Sendspin player is connected.", "נגן Sendspin המקומי מחובר."));
        return;
      }
      this._clearLocalSendspinReconnectTimer();
      this._localSendspinConnecting = true;
      this._state.localSendspinStatus = "connecting";
      this._syncLocalSendspinGlobalSession();
      if (this._state.menuOpen && typeof this._renderMobileMenu === "function") this._renderMobileMenu().catch(() => {
      });
      try {
        this._assertLocalSendspinConfig();
        const knownBrowserPlayerIds = Array.isArray(this._state.knownBrowserPlayerIds) && this._state.knownBrowserPlayerIds.length ? [...this._state.knownBrowserPlayerIds] : this._getBrowserPlayers().map((p) => p.entity_id);
        const playerId = this._getLocalSendspinPlayerId();
        const module = await this._loadLocalSendspinModule();
        const SendspinPlayer = module?.SendspinPlayer;
        if (typeof SendspinPlayer !== "function") {
          throw new Error(this._localText("The local Sendspin module is missing SendspinPlayer.", "מודול Sendspin המקומי לא כולל את SendspinPlayer."));
        }
        this._stopLocalSendspinPlayer("restart");
        this._restoreLocalSendspinInterceptor();
        const webSocket = await this._openAuthenticatedSendspinSocket(playerId);
        this._localSendspinSocket = webSocket;
        const audioElement = this._ensureLocalSendspinAudioElement();
        let syncDelay = 0;
        try {
          syncDelay = Number(localStorage.getItem(this._localSendspinSyncDelayStorageKey()) || 0) || 0;
        } catch (_) {
        }
        this._localSendspinPlayer = new SendspinPlayer({
          playerId,
          webSocket,
          audioElement,
          clientName: "HOMEii Flow",
          codecs: ["flac", "pcm"],
          syncDelay: syncDelay || void 0,
          correctionMode: "quality-local",
          onStateChange: (state) => {
            this._localSendspinState = state;
            this._syncLocalSendspinGlobalSession();
          },
          onDelayCommand: (delayMs) => {
            try {
              localStorage.setItem(this._localSendspinSyncDelayStorageKey(), String(delayMs));
            } catch (_) {
            }
          }
        });
        await Promise.race([
          this._localSendspinPlayer.connect(),
          new Promise((_, reject) => setTimeout(() => reject(new Error(this._localText(
            "Timed out waiting for the local Sendspin player to start.",
            "ההפעלה של נגן Sendspin המקומי לקחה יותר מדי זמן."
          ))), 15e3))
        ]);
        this._localSendspinConnected = true;
        this._state.localSendspinStatus = "connected";
        this._state.localSendspinDisconnecting = false;
        this._state.awaitingThisDevicePlayer = true;
        this._state.knownBrowserPlayerIds = knownBrowserPlayerIds;
        this._syncLocalSendspinGlobalSession();
        if (!automatic) this._toastSuccess(this._localText("Local Sendspin player connected from this card.", "נגן Sendspin המקומי חובר מתוך הכרטיס."));
        this._scheduleThisDevicePlayerDiscovery();
        this._refreshDirectMaPlayers({ renderMenu: true }).catch(() => {
        });
      } catch (error) {
        this._stopLocalSendspinPlayer("shutdown");
        this._state.localSendspinDisconnecting = false;
        this._state.awaitingThisDevicePlayer = false;
        this._state.controlRoomRevealThisDevicePending = false;
        this._state.localSendspinStatus = "error";
        if (automatic) this._debugLog("warn", "[Homeii Sendspin] automatic local reconnect failed", error);
        else this._toastError(error?.message || this._localText("Local Sendspin connection failed.", "חיבור Sendspin המקומי נכשל."));
      } finally {
        this._localSendspinConnecting = false;
        this._syncLocalSendspinGlobalSession();
        if (this._state.menuOpen && typeof this._renderMobileMenu === "function") this._renderMobileMenu().catch(() => {
        });
      }
    }
    _isLikelyBrowserPlayer(player) {
      return HomeiiPlayersFoundation2.isLikelyBrowserPlayer(player);
    }
    _isMusicAssistantPlayer(player = null) {
      const entityId = String(player?.entity_id || "").trim();
      const knownPlayablePlayer = !!entityId && (this._state.players || []).some((entry) => entry?.entity_id === entityId);
      return !!(player && (this._isDirectMaPlayer(player) || knownPlayablePlayer || HomeiiPlayersFoundation2.isMusicAssistantPlayer(player, this._hass?.entities?.[player.entity_id])));
    }
    _getBrowserPlayers(players = this._state.players || []) {
      return HomeiiPlayersFoundation2.getBrowserPlayers(players);
    }
    _isLocalSendspinPlayer(player = null) {
      if (!player) return false;
      const attrs = player.attributes || {};
      const raw = player.__homeiiRawPlayer || {};
      const localPlayerId = this._peekLocalSendspinPlayerId();
      if (!localPlayerId) return false;
      const localId = localPlayerId.toLowerCase();
      const identityValues = [
        player.entity_id,
        attrs.mass_player_id,
        attrs.player_id,
        attrs.player_id_short,
        attrs.id
      ].filter(Boolean).map((value) => String(value || "").trim().toLowerCase());
      const displayHaystack = [
        attrs.friendly_name,
        attrs.name,
        attrs.display_name,
        attrs.client_name,
        raw.name,
        raw.display_name,
        raw.friendly_name
      ].filter(Boolean).join(" ").toLowerCase();
      const isHomeiiSendspin = displayHaystack.includes("homeii sendspin") || displayHaystack.includes("homeii") && displayHaystack.includes("sendspin");
      const isHomeiiFlow = displayHaystack.includes("homeii flow") || displayHaystack.includes("homeii") && displayHaystack.includes("flow");
      return identityValues.some((value) => value === localId || value.endsWith(`_${localId}`) || value.endsWith(`.${localId}`)) && (isHomeiiSendspin || isHomeiiFlow);
    }
    _isAvailableThisDevicePlayer(player = null) {
      if (!player) return false;
      if (this._isLocalSendspinPlayer(player)) {
        this._adoptLocalSendspinGlobalSession();
        if (!this._isLocalSendspinDesired() && !this._localSendspinConnected && !this._localSendspinConnecting) return false;
      }
      const state = String(player.state || "").toLowerCase();
      const attrs = player.attributes || {};
      const raw = player.__homeiiRawPlayer || {};
      if (state === "unavailable" || state === "off") return false;
      if (attrs.available === false || raw.available === false || raw.powered === false) return false;
      return true;
    }
    _isRememberableThisDevicePlayer(player = null) {
      if (!this._isAvailableThisDevicePlayer(player)) return false;
      return this._isLocalSendspinPlayer(player);
    }
    _isExternalBrowserPlayer(player = null) {
      return !!(player && this._isLikelyBrowserPlayer(player) && !this._isLocalSendspinPlayer(player));
    }
    _getThisDevicePlayer(players = this._state.players || []) {
      const sourcePlayers = Array.isArray(players) ? players : [];
      const localPlayer = sourcePlayers.find((player) => this._isLocalSendspinPlayer(player) && this._isAvailableThisDevicePlayer(player));
      if (localPlayer) return localPlayer;
      const rememberedPlayer = HomeiiPlayersFoundation2.getThisDevicePlayer(sourcePlayers, this._getRememberedThisDevicePlayerId());
      if (this._isRememberableThisDevicePlayer(rememberedPlayer)) return rememberedPlayer;
      if (rememberedPlayer) this._rememberThisDevicePlayer("");
      return null;
    }
    _directMaEntityId(playerId = "") {
      const slug = String(playerId || "").trim().toLowerCase().replace(/[^a-z0-9_]+/g, "_").replace(/^_+|_+$/g, "") || "player";
      return `media_player.homeii_direct_${slug}`;
    }
    _isDirectMaEntityId(entityId = "") {
      return String(entityId || "").startsWith("media_player.homeii_direct_");
    }
    _directMaPlayerId(playerOrEntityId = null) {
      if (!playerOrEntityId) return "";
      if (typeof playerOrEntityId === "string") {
        const directPlayer = (this._directMaPlayers || []).find((player) => player?.entity_id === playerOrEntityId) || (this._state.players || []).find((player) => player?.entity_id === playerOrEntityId && this._isDirectMaPlayer(player));
        return String(directPlayer?.attributes?.mass_player_id || directPlayer?.attributes?.player_id || directPlayer?.__homeiiDirectMaPlayerId || "").trim();
      }
      const attrs = playerOrEntityId.attributes || {};
      return String(attrs.mass_player_id || attrs.player_id || attrs.id || playerOrEntityId.__homeiiDirectMaPlayerId || "").trim();
    }
    _directMaQueueId(playerOrEntityId = null) {
      const player = typeof playerOrEntityId === "string" ? this._playerByEntityId(playerOrEntityId) : playerOrEntityId;
      const attrs = player?.attributes || {};
      return String(attrs.active_queue || attrs.queue_id || attrs.mass_player_id || attrs.player_id || this._directMaPlayerId(playerOrEntityId)).trim();
    }
    _isDirectMaPlayer(playerOrEntityId = null) {
      if (!playerOrEntityId) return false;
      if (typeof playerOrEntityId === "string") {
        if (this._isDirectMaEntityId(playerOrEntityId)) return true;
        return this._isDirectMaPlayer(this._playerByEntityId(playerOrEntityId));
      }
      return !!playerOrEntityId.__homeiiDirectMa || !!playerOrEntityId.attributes?.homeii_direct_ma || this._isDirectMaEntityId(playerOrEntityId.entity_id);
    }
    _normalizeDirectMaPlaybackState(state = "", available = true, powered = true) {
      if (available === false) return "unavailable";
      if (powered === false) return "off";
      const value = String(state || "").trim().toLowerCase();
      if (value === "playing" || value === "paused" || value === "idle" || value === "off") return value;
      return "idle";
    }
    _normalizeDirectMaPlayer(rawPlayer = null) {
      const raw = rawPlayer?.state && rawPlayer.state.player_id ? rawPlayer.state : rawPlayer;
      if (!raw || typeof raw !== "object") return null;
      const playerId = String(raw.player_id || raw.id || raw.playerId || "").trim();
      if (!playerId) return null;
      const currentMedia = raw.current_media || raw.media || {};
      const deviceInfo = raw.device_info || {};
      const volumeLevel = Number(raw.volume_level ?? raw.group_volume);
      const normalizedVolume = Number.isFinite(volumeLevel) ? volumeLevel > 1 ? volumeLevel / 100 : volumeLevel : 0;
      const updatedAt = Number(raw.elapsed_time_last_updated || currentMedia.elapsed_time_last_updated || 0);
      const mediaUpdatedAt = updatedAt > 0 ? new Date(updatedAt * 1e3).toISOString() : void 0;
      const activeQueue = String(raw.active_queue || raw.queue_id || currentMedia.source_id || playerId).trim();
      const name = raw.name || raw.display_name || raw.friendly_name || currentMedia.title || playerId;
      const attrs = {
        ...raw.extra_attributes || {},
        friendly_name: name,
        app_id: "music_assistant",
        source: raw.provider || "music_assistant",
        provider: raw.provider || "",
        provider_name: raw.provider || "",
        mass_player_id: playerId,
        player_id: playerId,
        active_queue: activeQueue,
        queue_id: activeQueue,
        mass_player_type: raw.type || "",
        player_type: raw.type || "",
        model: deviceInfo.model || "",
        manufacturer: deviceInfo.manufacturer || "",
        volume_level: Math.max(0, Math.min(1, normalizedVolume || 0)),
        is_volume_muted: !!(raw.volume_muted || raw.group_volume_muted),
        shuffle: !!raw.shuffle_enabled,
        repeat: raw.repeat_mode || raw.repeat || "off",
        media_content_id: currentMedia.uri || "",
        media_content_type: currentMedia.media_type || "track",
        media_title: currentMedia.title || currentMedia.name || "",
        media_artist: currentMedia.artist || "",
        media_album_name: currentMedia.album || "",
        media_duration: currentMedia.duration || 0,
        media_position: raw.elapsed_time ?? currentMedia.elapsed_time ?? 0,
        media_position_updated_at: mediaUpdatedAt,
        entity_picture: currentMedia.image_url || "",
        entity_picture_local: currentMedia.image_url || "",
        media_image_url: currentMedia.image_url || "",
        media_image: currentMedia.image_url || "",
        media_palette: currentMedia.palette || null,
        current_media_palette: currentMedia.palette || null,
        current_media: currentMedia,
        supported_features: raw.supported_features || [],
        available: raw.available !== false,
        powered: raw.powered !== false,
        homeii_direct_ma: true
      };
      return {
        entity_id: this._directMaEntityId(playerId),
        state: this._normalizeDirectMaPlaybackState(raw.playback_state || raw.state, raw.available, raw.powered),
        attributes: attrs,
        __homeiiDirectMa: true,
        __homeiiDirectMaPlayerId: playerId,
        __homeiiRawPlayer: raw
      };
    }
    _haHasDirectMaPlayer(player = null, haEntities = []) {
      const playerId = this._directMaPlayerId(player);
      if (!playerId) return false;
      return (Array.isArray(haEntities) ? haEntities : []).some((entity) => {
        const attrs = entity?.attributes || {};
        return [attrs.mass_player_id, attrs.player_id, attrs.id].map((value) => String(value || "").trim()).some((value) => value === playerId);
      });
    }
    _mergeDirectMaPlayers(haEntities = []) {
      const source = Array.isArray(this._directMaPlayers) ? this._directMaPlayers : [];
      return source.filter((player) => {
        if (!player?.entity_id) return false;
        if (haEntities.some((entity) => entity?.entity_id === player.entity_id)) return false;
        return !this._haHasDirectMaPlayer(player, haEntities);
      });
    }
    _directMaQueueOption(enqueue = "play") {
      const value = String(enqueue || "play").toLowerCase();
      if (value === "add") return "add";
      if (value === "next") return "next";
      if (value === "replace_next") return "replace_next";
      return "replace";
    }
    async _refreshDirectMaPlayers(options = {}) {
      if (this._directMaPlayersRefreshPromise) return this._directMaPlayersRefreshPromise;
      this._directMaPlayersRefreshPromise = (async () => {
        try {
          const rawPlayers = await this._callDirectMaCommand("players/all", {
            return_unavailable: true,
            return_disabled: false,
            return_protocol_players: true
          });
          const normalized = (Array.isArray(rawPlayers) ? rawPlayers : []).map((player) => this._normalizeDirectMaPlayer(player)).filter(Boolean).filter((player) => this._isLocalSendspinPlayer(player) && this._isAvailableThisDevicePlayer(player));
          this._directMaPlayers = normalized;
          this._loadPlayers();
          const thisDevicePlayer = this._getThisDevicePlayer(this._state.players || []);
          if (this._state.controlRoomRevealThisDevicePending && thisDevicePlayer?.entity_id) {
            this._revealControlRoomThisDevicePlayer(thisDevicePlayer.entity_id, { sync: false });
          }
          if (options.renderMenu && this._state.menuOpen && typeof this._renderMobileMenu === "function") {
            this._renderMobileMenu().catch(() => {
            });
          }
          if (this._state.controlRoomOpen) this._syncControlRoomUi({ force: true });
          this._renderPlayerSummary();
          this._syncBrandPlayingState();
          this._syncNowPlayingUI();
          return normalized;
        } catch (error) {
          this._debugLog("warn", "[Homeii Sendspin] direct MA player refresh failed", error);
          return this._directMaPlayers || [];
        } finally {
          this._directMaPlayersRefreshPromise = null;
        }
      })();
      return this._directMaPlayersRefreshPromise;
    }
    async _callDirectMaPlayerCommand(playerOrEntityId, command, args = {}) {
      const playerId = this._directMaPlayerId(playerOrEntityId);
      if (!playerId) throw new Error("Direct Music Assistant player was not found");
      return this._callDirectMaCommand(command, { player_id: playerId, ...args });
    }
    async _playMediaOnDirectMaPlayer(entityId, uri, mediaType = "album", enqueue = "play", options = {}) {
      const player = this._playerByEntityId(entityId);
      const queueId = this._directMaQueueId(player || entityId);
      if (!queueId) throw new Error("Direct Music Assistant player is not ready");
      if (enqueue === "shuffle") {
        await this._callDirectMaCommand("player_queues/shuffle", { queue_id: queueId, shuffle_enabled: true });
      }
      await this._callDirectMaCommand("player_queues/play_media", {
        queue_id: queueId,
        media: uri,
        option: this._directMaQueueOption(enqueue),
        radio_mode: !!options.radioMode
      });
      if (!options.silent) {
        const label = this._mediaFeedbackLabel(uri, options.label || "");
        this._toastMediaQueued(label, player?.attributes?.friendly_name || entityId);
      }
      if (entityId === this._state.selectedPlayer) {
        this._refreshDirectMaPlayers().catch(() => {
        });
        setTimeout(() => this._ensureQueueSnapshot(true), 600);
        setTimeout(() => this._ensureQueueSnapshot(true), 1600);
      }
      return true;
    }
    _connectThisDevicePlayer() {
      this._state.localSendspinDisconnecting = false;
      this._state.localSendspinStatus = "connecting";
      if (this._state.menuOpen && typeof this._renderMobileMenu === "function") this._renderMobileMenu().catch(() => {
      });
      this._attachLocalSendspinLifecycleListeners();
      const selectedPlayer = this._getSelectedPlayer();
      if (this._isExternalBrowserPlayer(selectedPlayer)) {
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
      }
      this._rememberThisDevicePlayer("");
      this._state.awaitingThisDevicePlayer = true;
      this._state.controlRoomRevealThisDevicePending = true;
      this._state.knownBrowserPlayerIds = this._getBrowserPlayers().map((p) => p.entity_id);
      this._startLocalSendspinPlayer().catch((error) => {
        this._state.localSendspinDisconnecting = false;
        this._state.awaitingThisDevicePlayer = false;
        this._state.controlRoomRevealThisDevicePending = false;
        this._toastError(error?.message || this._localText("Local Sendspin connection failed.", "חיבור Sendspin המקומי נכשל."));
      });
    }
    _disconnectThisDevicePlayer() {
      this._adoptLocalSendspinGlobalSession();
      this._state.localSendspinDisconnecting = true;
      this._state.localSendspinStatus = "disconnecting";
      if (this._state.menuOpen && typeof this._renderMobileMenu === "function") this._renderMobileMenu().catch(() => {
      });
      const selectedPlayer = this._getSelectedPlayer();
      if (this._isLocalSendspinPlayer(selectedPlayer)) {
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
      }
      this._rememberThisDevicePlayer("");
      this._state.awaitingThisDevicePlayer = false;
      this._state.controlRoomRevealThisDevicePending = false;
      this._state.knownBrowserPlayerIds = [];
      this._directMaPlayers = [];
      this._stopLocalSendspinPlayer("user_request");
      this._state.localSendspinDisconnecting = false;
      this._state.localSendspinStatus = "disconnected";
      this._refreshDirectMaPlayers({ renderMenu: true }).catch(() => {
      });
      this._loadPlayers();
      if (this._state.controlRoomOpen) this._syncControlRoomUi({ force: true });
      if (this._state.menuOpen && typeof this._renderMobileMenu === "function") this._renderMobileMenu().catch(() => {
      });
      this._toastSuccess(this._i18n("ui.this_device_player_disconnected"));
    }
    _selectPlayer(entityId, manual = false) {
      const pinnedEntities = typeof this._resolvedPinnedPlayerEntities === "function" ? this._resolvedPinnedPlayerEntities() : [];
      const requestedEntityId = String(entityId || "").trim();
      const nextEntityId = manual ? requestedEntityId : pinnedEntities.length ? pinnedEntities.includes(requestedEntityId) ? requestedEntityId : pinnedEntities[0] : requestedEntityId;
      if (!nextEntityId) return;
      const selectedPlayer = this._playerByEntityId(nextEntityId);
      if (!selectedPlayer || !this._isMusicAssistantPlayer(selectedPlayer)) {
        this._handleMusicAssistantIssue(this._musicAssistantRequiredMessage());
        if (manual) this._toastError(this._musicAssistantRequiredTitle());
        return;
      }
      this._state.selectedPlayer = nextEntityId;
      if (manual) {
        this._setManualFrontPlayer(nextEntityId, this._manualFrontHoldMsForSelection());
      }
      this._syncActivePlayerHelper(selectedPlayer);
      if (manual && this._isRememberableThisDevicePlayer(selectedPlayer)) {
        this._rememberThisDevicePlayer(nextEntityId);
      } else if (manual && selectedPlayer && this._isDirectMaPlayer(selectedPlayer) && !this._isLocalSendspinPlayer(selectedPlayer)) {
        this._rememberThisDevicePlayer("");
      }
      if (manual) this._state.hasAutoSelectedPlayer = true;
      const sel = this.$("playerSel");
      if (sel) sel.value = nextEntityId;
      this._state.maQueueState = null;
      this._state.queueItems = [];
      this._syncNowPlayingUI();
      this._renderPlayerSummary();
      this._syncBrandPlayingState();
      if (this._state.view === "now_playing") this._renderNowPlayingPage();
    }
    _renderPlayerSummary() {
      const selected = this._getSelectedPlayer();
      const title = this.$("selectedPlayerTitle");
      const sub = this.$("selectedPlayerSub");
      if (!selected) {
        if (title) title.textContent = this._musicAssistantRequiredTitle();
        if (sub) sub.textContent = this._state.musicAssistantIssueMessage || this._musicAssistantRequiredMessage();
        this._updateThemeButton();
        return;
      }
      if (title) title.textContent = selected?.attributes?.friendly_name || this._i18n("ui.selected_player");
      if (sub) {
        const label = this._playerStateLabel(selected);
        const track = selected?.attributes?.media_title || "";
        sub.textContent = track ? `${label} · ${track}` : label;
      }
      this._updateThemeButton();
    }
    _updateThemeButton() {
      const themeBtn = this.$("themeToggleBtn");
      if (!themeBtn) return;
      const effective = this._effectiveTheme();
      themeBtn.textContent = effective === "dark" ? "☀" : "🌙";
      themeBtn.title = `${this._i18n("ui.theme")}: ${this._t(effective === "dark" ? "Dark" : "Light")}`;
    }
    _syncBrandPlayingState() {
      const btn = this.$("brandPlayersBtn");
      const player = this._getSelectedPlayer();
      if (!btn) return;
      const isPlaying = player?.state === "playing";
      btn.classList.toggle("playing", !!isPlaying);
      btn.title = this._i18n("ui.open_music_assistant");
    }
    _setPlayerModalHeader(mode = "players") {
      const icon = this.$("playerModalIcon");
      const subtitle = this.$("playerModalSubtitle");
      const selected = this._getSelectedPlayer();
      if (icon) icon.textContent = mode === "transfer" ? "⇆" : "🎵";
      if (!subtitle) return;
      if (mode === "transfer") {
        subtitle.textContent = selected?.attributes?.friendly_name || this._i18n("ui.choose_target_player");
        return;
      }
      const count = (this._state.players || []).length;
      subtitle.textContent = `${count} ${this._i18n("ui.items")}`;
    }
    _renderPlayerModal() {
      this._state.modalMode = "players";
      this.$("playerModalTitle").textContent = this._i18n("ui.choose_player");
      this._setPlayerModalHeader("players");
      const body = this.$("playerModalBody");
      if (!body) return;
      const players = this._state.players || [];
      const activePlayers = players.filter((p) => this._isPlayerActive(p));
      const others = players.filter((p) => !this._isPlayerActive(p));
      const renderGroup = (title, list) => {
        if (!list.length) return "";
        return `
          <div class="modal-section">
            <div class="modal-section-top">
              <div class="modal-section-title">${this._esc(title)}</div>
              <div class="modal-section-badge">${list.length}</div>
            </div>
            <div class="player-list">
              ${list.map((p) => {
          const selected = p.entity_id === this._state.selectedPlayer;
          const stateCls = p.state === "playing" ? "playing" : p.state === "paused" ? "paused" : "idle";
          const name = p.attributes?.friendly_name || p.entity_id;
          const track = p.attributes?.media_title || "";
          const art = this._bestArtworkUrl([p.attributes?.entity_picture_local, p.attributes?.entity_picture], {
            size: 120,
            cacheKey: this._currentArtworkCacheKey(p)
          });
          return `
                  <button class="player-card ${stateCls} ${selected ? "active" : ""}" data-modal-player="${this._esc(p.entity_id)}">
                    <span class="player-card-dot"></span>
                    <span class="player-card-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("speaker")}</span>
                    <span class="player-card-meta">
                      <span class="player-card-top">
                        <span class="player-card-title">${this._esc(name)}</span>
                        <span class="player-card-badge">${selected ? this._esc(this._i18n("ui.selected_player")) : this._esc(this._playerStateLabel(p))}</span>
                      </span>
                      <span class="player-card-sub">${this._esc(this._playerStateLabel(p))}</span>
                      <span class="player-card-track">${this._esc(track || "—")}</span>
                    </span>
                  </button>
                `;
        }).join("")}
            </div>
          </div>
        `;
      };
      body.innerHTML = `<div class="player-modal-grid">${renderGroup(this._i18n("ui.playing"), activePlayers)}${renderGroup(this._i18n("ui.other_players"), others)}</div>`;
      body.querySelectorAll("[data-modal-player]").forEach((btn) => btn.addEventListener("click", () => {
        this._selectPlayer(btn.dataset.modalPlayer, true);
        this._closePlayerModal();
      }));
    }
    _openPlayerModal() {
      this.shadowRoot.querySelector(".card")?.appendChild(this.$("playerModal"));
      this._renderPlayerModal();
      this.$("playerModal").classList.add("open");
      this._state.playerModalOpen = true;
    }
    _closePlayerModal() {
      this.$("playerModal").classList.remove("open");
      this._state.playerModalOpen = false;
      this._state.modalMode = "players";
    }
    _syncMaButtonVisibility() {
      this.$("maOpenBtn").style.display = this._config.show_ma_button ? "" : "none";
    }
    _openMusicAssistant() {
      this.$("maConfirmModal")?.classList.add("open");
    }
    _closeMaConfirm() {
      this.$("maConfirmModal")?.classList.remove("open");
    }
    _confirmMusicAssistantOpen() {
      this._closeMaConfirm();
      this._launchMusicAssistant();
    }
    _flowAssistantLabel() {
      return this._i18n("ui.flow_assistant", {}, "FLOW ASSISTANT") || "FLOW ASSISTANT";
    }
    _cleanAllLabel() {
      return this._i18n("ui.clean_all", {}, this._m("Clean all", "נקה הכל")) || this._m("Clean all", "נקה הכל");
    }
    _cleanAllConfirmTitle() {
      return this._i18n("ui.clean_all_confirm_title", {}, this._m("Clean all players?", "לנקות את כל הנגנים?"));
    }
    _cleanAllConfirmCopy() {
      return this._i18n(
        "ui.clean_all_confirm_copy",
        {},
        this._m(
          "This will disconnect all active players, stop playback and clear their queues. Continue?",
          "הפעולה תנתק את כל הנגנים הפעילים, תעצור נגינה ותנקה את התורים שלהם. להמשיך?"
        )
      );
    }
    _openCleanAllConfirm() {
      this.$("cleanAllConfirmModal")?.classList.add("open");
    }
    _closeCleanAllConfirm() {
      this.$("cleanAllConfirmModal")?.classList.remove("open");
    }
    async _confirmCleanAllPlayers() {
      this._closeCleanAllConfirm();
      this._closeMobileMenu();
      await this._stopAllPlayers();
      setTimeout(() => this._updateNowPlayingState(), 350);
    }
    _launchMusicAssistant() {
      window.open(this._config.ma_interface_url || "/music-assistant", this._config.ma_interface_target || "_self");
    }
    _openNowPlayingView() {
      const btn = this.shadowRoot.querySelector('.nav-btn[data-view="now_playing"]');
      this._setView("now_playing", btn || null);
    }
    _openImmersiveNowPlaying() {
      const backdrop = this.$("immersiveNowPlaying");
      if (!backdrop) return;
      this.shadowRoot.querySelector(".card")?.appendChild(backdrop);
      this._state.immersiveNowPlayingOpen = true;
      this._renderImmersiveNowPlaying();
      backdrop.classList.add("open");
    }
    _closeImmersiveNowPlaying() {
      const backdrop = this.$("immersiveNowPlaying");
      if (!backdrop) return;
      backdrop.classList.remove("open");
      backdrop.classList.remove("has-lyrics-art");
      backdrop.style.removeProperty("--lyrics-dynamic-art");
      backdrop.onclick = null;
      backdrop.innerHTML = "";
      this._state.immersiveNowPlayingOpen = false;
    }
    _currentTrackInfo() {
      return HomeiiMediaPresentationFoundation2.buildCurrentTrackInfo({
        player: this._getSelectedPlayer(),
        queueItem: this._state.maQueueState?.current_item || null
      });
    }
    _currentLyricsTrackKey() {
      const info = this._currentTrackInfo();
      const queueItem = this._state.maQueueState?.current_item || null;
      const player = this._getSelectedPlayer();
      return [
        this._getQueueItemStableId?.(queueItem),
        this._getQueueItemUri?.(queueItem),
        player?.attributes?.media_content_id,
        info.key,
        Math.round(Number(info.duration || 0) || 0)
      ].map((value) => String(value || "").trim().toLowerCase()).filter(Boolean).join("|");
    }
    _saveMobileRecentHistory() {
      try {
        localStorage.setItem("homeii_music_flow_mobile_recent_history", JSON.stringify((this._state.mobileRecentHistory || []).slice(0, 10)));
      } catch (_) {
      }
    }
    _sourceProviderMeta(value = "") {
      return HomeiiMediaPresentationFoundation2.sourceProviderMeta(value, {
        libraryLabel: this._i18n("ui.library"),
        radioLabel: this._i18n("ui.radio")
      });
    }
    _qualityBadgeLabel(values = []) {
      return HomeiiMediaPresentationFoundation2.qualityBadgeLabel(values);
    }
    _currentSourceBadgeMeta(player = this._getSelectedPlayer(), queueItem = this._state.maQueueState?.current_item || null) {
      return HomeiiMediaHistoryFoundation2.buildCurrentSourceBadgeMeta(
        { player, queueItem: queueItem || this._state.maQueueState?.current_item || null },
        {
          parseMediaReferenceFn: (uri, mediaType) => this._parseMediaReference(uri, mediaType),
          sourceProviderMetaFn: (value, labels) => HomeiiMediaPresentationFoundation2.sourceProviderMeta(value, labels),
          qualityBadgeLabelFn: (values) => HomeiiMediaPresentationFoundation2.qualityBadgeLabel(values),
          libraryLabel: this._i18n("ui.library"),
          radioLabel: this._i18n("ui.radio")
        }
      );
    }
    _currentHistorySnapshot(player = this._getSelectedPlayer(), queueItem = this._state.maQueueState?.current_item || null) {
      return HomeiiMediaHistoryFoundation2.buildCurrentHistorySnapshot(
        { player, queueItem: queueItem || this._state.maQueueState?.current_item || null },
        {
          getQueueItemUriFn: (item) => this._getQueueItemUri(item),
          queueItemImageUrlFn: (item, size) => this._queueItemImageUrl(item, size),
          artUrlFn: (item) => this._artUrl(item),
          buildCurrentSourceBadgeMetaFn: ({ player: historyPlayer, queueItem: historyQueueItem }) => this._currentSourceBadgeMeta(historyPlayer, historyQueueItem)
        }
      );
    }
    _rememberRecentPlayback(player = this._getSelectedPlayer(), queueItem = this._state.maQueueState?.current_item || null) {
      const snapshot = this._currentHistorySnapshot(player, queueItem);
      const resolved = HomeiiMediaHistoryFoundation2.applyRecentPlaybackSnapshot(
        snapshot,
        this._state.mobileCurrentHistoryEntry || null,
        this._state.mobileRecentHistory || [],
        10
      );
      this._state.mobileCurrentHistoryEntry = resolved.currentEntry;
      this._state.mobileRecentHistory = resolved.recentHistory;
      if (resolved.historyChanged) {
        this._saveMobileRecentHistory();
      }
    }
    _visibleRecentHistoryItems() {
      return HomeiiMediaHistoryFoundation2.visibleRecentHistoryItems(
        this._state.mobileCurrentHistoryEntry || null,
        this._state.mobileRecentHistory || [],
        10
      );
    }
    _historyRecommendationPlaylistItems(limit = 5) {
      const playlists = Array.isArray(this._state.mobileRecommendationPlaylists) ? this._state.mobileRecommendationPlaylists : [];
      const seen = /* @__PURE__ */ new Set();
      return playlists.filter((item) => {
        const uri = String(item?.uri || "").trim();
        if (!uri || seen.has(uri)) return false;
        seen.add(uri);
        return String(item?.media_type || "playlist").toLowerCase() === "playlist";
      }).slice(0, limit).map((item) => ({
        uri: String(item.uri || "").trim(),
        media_type: "playlist",
        title: item.name || item.title || this._i18n("ui.recommended_playlist"),
        artist: this._i18n("ui.playlist"),
        album: item.provider_label || item.provider || "",
        image: this._artUrl(item) || item.image || "",
        provider_label: item.provider_label || item.provider || ""
      }));
    }
    async _loadHistoryRecommendationPlaylists(force = false) {
      const now = Date.now();
      const cached = Array.isArray(this._state.mobileRecommendationPlaylists) ? this._state.mobileRecommendationPlaylists : [];
      const fresh = cached.length && !force && now - Number(this._state.mobileRecommendationPlaylistsFetchedAt || 0) < 10 * 60 * 1e3;
      if (fresh || this._state.mobileRecommendationPlaylistsLoading) return cached;
      this._state.mobileRecommendationPlaylistsLoading = true;
      try {
        const playlists = await this._loadScheduledStartPlaylists(force);
        this._state.mobileRecommendationPlaylists = Array.isArray(playlists) ? playlists.slice(0, 24) : [];
        this._state.mobileRecommendationPlaylistsFetchedAt = Date.now();
        this._state.mobileHistoryRenderedHtml = "";
        if (this._state.mobileHistoryDrawerTab === "recommendations") this._syncRecentHistoryUi();
        return this._state.mobileRecommendationPlaylists;
      } catch (_) {
        return cached;
      } finally {
        this._state.mobileRecommendationPlaylistsLoading = false;
      }
    }
    _historyRecommendationItems(limit = 10) {
      const currentIndex = this._state.maQueueState?.current_index ?? -1;
      const seen = /* @__PURE__ */ new Set();
      const pushUnique = (items = [], output = []) => {
        (Array.isArray(items) ? items : []).forEach((item) => {
          const uri = String(item?.uri || "").trim();
          if (!uri || seen.has(uri) || output.length >= limit) return;
          seen.add(uri);
          output.push(item);
        });
        return output;
      };
      const queueItems = this._getNowPlayingQueueItems().filter((item) => (item?.sort_index ?? -1) !== currentIndex).map((item) => {
        const media = item.media_item || {};
        return {
          uri: String(media.uri || "").trim(),
          media_type: media.media_type || item.media_type || "track",
          title: media.name || item.name || this._i18n("ui.recommended_track"),
          artist: media.artists?.map((artist) => artist.name).join(", ") || media.album?.name || "",
          album: media.album?.name || "",
          image: this._queueItemImageUrl(item, 120) || this._artUrl(media) || ""
        };
      }).filter((item) => item.uri).slice(0, 4);
      const recentItems = (this._state.mobileRecentHistory || []).filter((item) => item?.uri).map((item) => ({
        ...item,
        title: item.title || item.name || this._i18n("ui.recommended_track")
      }));
      const recommendations = [];
      pushUnique(this._state.quickMixRecommendationItems || [], recommendations);
      pushUnique(queueItems, recommendations);
      pushUnique(this._historyRecommendationPlaylistItems(5), recommendations);
      pushUnique(recentItems, recommendations);
      return recommendations.slice(0, limit);
    }
    _historyPlayableItems(items = []) {
      return (Array.isArray(items) ? items : []).map((item) => ({
        uri: String(item?.uri || "").trim(),
        media_type: item?.media_type || "track",
        name: item?.name || item?.title || ""
      })).filter((item) => item.uri);
    }
    _setHistoryDrawerOpen(open = false) {
      this._state.mobileHistoryDrawerOpen = !!open;
      const drawer = this.$("historyDrawer");
      const button = this.$("historyToggleFab");
      drawer?.classList.toggle("open", this._state.mobileHistoryDrawerOpen);
      button?.classList.toggle("active", this._state.mobileHistoryDrawerOpen);
      if (button) button.setAttribute("aria-expanded", this._state.mobileHistoryDrawerOpen ? "true" : "false");
    }
    _toggleHistoryDrawer(force) {
      const next = typeof force === "boolean" ? force : !this._state.mobileHistoryDrawerOpen;
      if (next) {
        this._rememberRecentPlayback();
        this._state.mobileHistoryDrawerTab = "recent";
        this._state.mobileHistoryRenderedHtml = "";
      }
      this._setHistoryDrawerOpen(next);
      if (next) this._syncRecentHistoryUi(true);
    }
    _syncRecentHistoryUi(force = false) {
      const host = this.$("historyDrawerBody");
      const drawer = this.$("historyDrawer");
      const button = this.$("historyToggleFab");
      if (!host) return;
      if (!this._getSelectedPlayer()) {
        if (force || this._state.mobileHistoryRenderedHtml !== "") {
          host.innerHTML = "";
          this._state.mobileHistoryRenderedHtml = "";
        }
        drawer?.setAttribute("hidden", "");
        button?.setAttribute("hidden", "");
        this._setHistoryDrawerOpen(false);
        return;
      }
      drawer?.removeAttribute("hidden");
      button?.removeAttribute("hidden");
      const tab = this._state.mobileHistoryDrawerTab === "recommendations" ? "recommendations" : "recent";
      this.shadowRoot?.querySelectorAll("[data-history-tab]")?.forEach((btn) => {
        const active = btn.dataset.historyTab === tab;
        btn.classList.toggle("active", active);
        btn.setAttribute("aria-selected", active ? "true" : "false");
      });
      if (tab === "recommendations") this._loadHistoryRecommendationPlaylists().catch(() => {
      });
      const items = tab === "recommendations" ? this._historyRecommendationItems() : this._visibleRecentHistoryItems();
      if (!items.length) {
        const emptyText = tab === "recommendations" ? this._i18n("ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l") : this._i18n("ui.play_a_few_tracks_and_they_will_appear_here");
        const emptyHtml = `<div class="history-empty">${this._esc(emptyText)}</div>`;
        if (force || this._state.mobileHistoryRenderedHtml !== emptyHtml) {
          host.innerHTML = emptyHtml;
          this._state.mobileHistoryRenderedHtml = emptyHtml;
        }
        return;
      }
      const nextHtml = `
        <div class="history-actions">
          <button class="chip-btn history-play-all-btn" data-history-play-all title="${this._esc(this._i18n("ui.play_all"))}" aria-label="${this._esc(this._i18n("ui.play_all"))}">
            ${this._iconSvg("play")}
            <span>${this._esc(this._i18n("ui.play_all"))}</span>
          </button>
        </div>
        ${items.map((item, index) => `
          <button class="history-chip" data-history-index="${this._esc(String(index))}" title="${this._esc(item.title || "")}">
            <span class="history-chip-art">${item.image ? `<img src="${this._esc(item.image)}" alt="">` : this._iconSvg("music_note")}</span>
            <span class="history-chip-copy">
              <span class="history-chip-title">${this._esc(item.title || this._i18n("ui.recent_track"))}</span>
              <span class="history-chip-sub">${this._esc(item.artist || item.album || item.provider_label || "—")}</span>
            </span>
          </button>
        `).join("")}
      `;
      const changed = force || this._state.mobileHistoryRenderedHtml !== nextHtml;
      if (changed) {
        host.innerHTML = nextHtml;
        this._state.mobileHistoryRenderedHtml = nextHtml;
      }
      if (changed) {
        host.querySelectorAll("[data-history-index]").forEach((btn) => btn.addEventListener("click", async (e) => {
          const trigger = e.currentTarget;
          const index = Number(trigger.dataset.historyIndex);
          const currentTab = this._state.mobileHistoryDrawerTab === "recommendations" ? "recommendations" : "recent";
          const item = (currentTab === "recommendations" ? this._historyRecommendationItems() : this._visibleRecentHistoryItems())[index];
          if (!item?.uri) return;
          this._pressUiButton(trigger);
          await this._playMedia(item.uri, item.media_type || "track", "play", {
            label: item.title || "",
            sourceEl: trigger
          });
          this._setHistoryDrawerOpen(false);
        }));
      }
    }
    _controlRoomEnabled() {
      return this._layoutModeConfig() === "tablet" && !this._isCompactTileMode();
    }
    _controlRoomLabel() {
      return this._i18n("ui.studio");
    }
    _controlRoomPlayerName(entityOrPlayer = "") {
      const player = entityOrPlayer && typeof entityOrPlayer === "object" ? entityOrPlayer : this._playerByEntityId(String(entityOrPlayer || ""));
      return player?.attributes?.friendly_name || String(entityOrPlayer || "") || this._i18n("ui.player");
    }
    _controlRoomPlayerCountLabel(count = 0) {
      const amount = Math.max(0, Number(count) || 0);
      if (amount === 1) return this._i18n("ui.player_count_one");
      return this._i18n("ui.player_count_many", { count: amount });
    }
    _controlRoomPanelLabel(panel = "") {
      const labels = {
        selection: this._i18n("ui.connected_players"),
        visible: this._i18n("ui.visible_tiles"),
        music: this._i18n("ui.music_hub"),
        actions: this._i18n("ui.actions"),
        library: this._i18n("ui.studio_search"),
        transfer: this._i18n("ui.queue_cockpit"),
        mix: this._i18n("ui.smart_mix"),
        recent: this._i18n("ui.recent_listening"),
        favorites: this._i18n("ui.favorite_center"),
        scenes: this._i18n("ui.scene_presets"),
        announce: this._i18n("ui.announcement_studio"),
        pro: this._i18n("ui.studio_pro")
      };
      return labels[String(panel || "")] || this._controlRoomLabel();
    }
    _controlRoomActionTargetIds() {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      if (selectedIds.length) return selectedIds;
      const primaryId = this._controlRoomPrimaryPlayerId();
      return primaryId ? [primaryId] : [];
    }
    _controlRoomFocusTarget() {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const selectedPlayers = selectedIds.map((entityId) => this._playerByEntityId(entityId)).filter(Boolean);
      const primary = this._controlRoomPrimaryPlayer();
      const players = selectedPlayers.length ? selectedPlayers : primary ? [primary] : [];
      const first = players[0] || null;
      const art = this._bestArtworkUrl([first?.attributes?.entity_picture_local, first?.attributes?.entity_picture], {
        size: 160,
        cacheKey: this._currentArtworkCacheKey(first)
      });
      if (players.length > 1) {
        const names = players.map((player) => player.attributes?.friendly_name || player.entity_id).filter(Boolean);
        return {
          art,
          count: players.length,
          kicker: this._i18n("ui.controlling"),
          name: this._controlRoomPlayerCountLabel(players.length),
          track: names.slice(0, 3).join(" · ") + (names.length > 3 ? "..." : "")
        };
      }
      const name = first?.attributes?.friendly_name || this._i18n("ui.selected_player_2");
      return {
        art,
        count: first ? 1 : 0,
        kicker: selectedIds.length ? this._i18n("ui.controlling") : this._i18n("ui.primary_target"),
        name,
        track: first?.attributes?.media_title || first?.attributes?.media_artist || this._playerStateLabel(first) || this._i18n("ui.idle_2")
      };
    }
    _controlRoomContextChipHtml() {
      const target = this._controlRoomFocusTarget();
      return `
        <div class="control-room-context-chip">
          <span class="control-room-context-art">${target.art ? `<img src="${this._esc(target.art)}" alt="">` : this._iconSvg("speaker")}</span>
          <span class="control-room-context-copy">
            <span class="control-room-context-kicker">${this._esc(target.kicker)}</span>
            <span class="control-room-context-name">${this._esc(target.name)}</span>
          </span>
        </div>
      `;
    }
    _tabletStabilityModeEnabled() {
      try {
        const ua = String(window.navigator?.userAgent || "");
        const width = this._getCardWidth(Number(window.innerWidth || 0));
        const touchPoints = Number(window.navigator?.maxTouchPoints || 0);
        return HomeiiResponsiveFoundation2.tabletStabilityModeEnabled({
          layoutMode: this._layoutModeConfig(),
          userAgent: ua,
          width,
          touchPoints
        });
      } catch (_) {
        return false;
      }
    }
    _controlRoomAllPlayers() {
      this._loadPlayers();
      const players = Array.isArray(this._state.players) ? this._state.players : [];
      const visible = players.filter((player) => !this._isLikelyBrowserPlayer(player) || this._isLocalSendspinPlayer(player));
      return visible.length ? visible : players;
    }
    _controlRoomVisiblePlayerIds() {
      const players = this._controlRoomAllPlayers();
      const validIds = new Set(players.map((player) => player.entity_id));
      let visibleIds = (Array.isArray(this._state.controlRoomVisiblePlayers) ? this._state.controlRoomVisiblePlayers : []).filter((entityId) => validIds.has(entityId));
      const thisDevicePlayer = players.find((player) => this._isLocalSendspinPlayer(player) && this._isAvailableThisDevicePlayer(player));
      if (this._state.controlRoomRevealThisDevicePending && thisDevicePlayer?.entity_id) {
        if (!visibleIds.length && this._state.controlRoomVisiblePlayers?.length) visibleIds = [thisDevicePlayer.entity_id];
        else if (!visibleIds.includes(thisDevicePlayer.entity_id)) visibleIds.push(thisDevicePlayer.entity_id);
        this._state.controlRoomRevealThisDevicePending = false;
      }
      if (!visibleIds.length) visibleIds = players.map((player) => player.entity_id);
      if (!visibleIds.length && players[0]?.entity_id) visibleIds = [players[0].entity_id];
      this._state.controlRoomVisiblePlayers = visibleIds;
      return visibleIds;
    }
    _revealControlRoomThisDevicePlayer(entityId = "", options = {}) {
      const id = String(entityId || "").trim();
      if (!id) return false;
      const visible = Array.isArray(this._state.controlRoomVisiblePlayers) ? this._state.controlRoomVisiblePlayers.filter(Boolean) : [];
      if (visible.length && !visible.includes(id)) this._state.controlRoomVisiblePlayers = [...visible, id];
      const selected = Array.isArray(this._state.controlRoomSelectedPlayers) ? this._state.controlRoomSelectedPlayers.filter(Boolean) : [];
      this._state.controlRoomSelectedPlayers = [id, ...selected.filter((value) => value !== id)];
      this._state.controlRoomRevealThisDevicePending = false;
      if (options.sync !== false && this._state.controlRoomOpen) {
        this._syncControlRoomTransferDefaults();
        this._syncControlRoomUi({ force: true });
      }
      return true;
    }
    _controlRoomPlayers() {
      const players = this._controlRoomAllPlayers();
      const visibleIds = new Set(this._controlRoomVisiblePlayerIds());
      const filtered = players.filter((player) => visibleIds.has(player.entity_id));
      return filtered.length ? filtered : players;
    }
    _playerByEntityId(entityId = "") {
      const target = String(entityId || "").trim();
      if (!target) return null;
      if (typeof this._isPlayerExcluded === "function" && this._isPlayerExcluded(target)) return null;
      const knownPlayer = HomeiiPlayersFoundation2.playerByEntityId(
        target,
        [...this._state.players || [], ...this._directMaPlayers || []],
        {}
      );
      if (knownPlayer) return knownPlayer;
      const hassPlayer = this._hass?.states?.[target] || null;
      return this._isMusicAssistantPlayer(hassPlayer) ? hassPlayer : null;
    }
    _controlRoomGroupKey(player = null) {
      const attrs = player?.attributes || {};
      const candidates = [
        attrs.group_id,
        attrs.group,
        attrs.group_leader,
        attrs.group_parent,
        attrs.group_master,
        attrs.group_entity_id,
        attrs.sync_group,
        attrs.active_group,
        attrs.synced_to
      ];
      const key = candidates.map((value) => String(value || "").trim()).find((value) => value && !/^(false|true|none|null|unknown|unavailable)$/i.test(value));
      return key || "";
    }
    _controlRoomGroupInfo(player = null) {
      if (!player?.entity_id || HomeiiPlayersFoundation2.isLikelyBrowserPlayer(player)) return { ids: [], count: 0, label: "" };
      const allPlayers = this._controlRoomAllPlayers();
      const byId = new Map(allPlayers.map((entry) => [entry?.entity_id, entry]).filter(([entityId]) => !!entityId));
      let ids = this._playerGroupMemberIds(player);
      if (ids.length <= 1) {
        const owner = allPlayers.find((candidate) => {
          const members = this._playerGroupMemberIds(candidate);
          return members.length > 1 && members.includes(player.entity_id);
        });
        if (owner) ids = this._playerGroupMemberIds(owner);
      }
      if (ids.length <= 1) {
        const key = this._controlRoomGroupKey(player);
        if (key) {
          ids = allPlayers.filter((candidate) => this._controlRoomGroupKey(candidate) === key).map((candidate) => candidate.entity_id);
        }
      }
      ids = [...new Set(ids)].filter((entityId) => entityId && byId.has(entityId)).filter((entityId) => !HomeiiPlayersFoundation2.isLikelyBrowserPlayer(byId.get(entityId)));
      const names = ids.map((entityId) => byId.get(entityId)?.attributes?.friendly_name || entityId).filter(Boolean);
      return {
        ids,
        count: ids.length > 1 ? ids.length : 0,
        label: names.length > 1 ? names.join(" · ") : ""
      };
    }
    _controlRoomGroupSummaries(players = []) {
      const visible = Array.isArray(players) ? players : [];
      const byKey = /* @__PURE__ */ new Map();
      visible.forEach((player) => {
        const info = this._controlRoomGroupInfo(player);
        if (!info.count) return;
        const ids = [...info.ids].sort();
        const key = ids.join("|");
        if (!key || byKey.has(key)) return;
        const names = ids.map((entityId) => this._controlRoomPlayerName(entityId)).filter(Boolean);
        const primaryId = ids[0] || player.entity_id;
        const primary = this._playerByEntityId(primaryId) || player;
        byKey.set(key, {
          ids,
          count: ids.length,
          label: names.join(" · "),
          art: this._bestArtworkUrl([primary?.attributes?.entity_picture_local, primary?.attributes?.entity_picture], {
            size: 120,
            cacheKey: this._currentArtworkCacheKey(primary)
          })
        });
      });
      return [...byKey.values()];
    }
    _controlRoomGroupSummaryHtml(players = []) {
      const groups = this._controlRoomGroupSummaries(players);
      if (!groups.length) return "";
      return `
        <div class="control-room-group-summary" data-control-room-scroll="groups">
          ${groups.map((group) => `
            <div class="control-room-group-chip" title="${this._esc(group.label)}">
              <span class="control-room-group-art">${group.art ? `<img src="${this._esc(group.art)}" alt="">` : this._iconSvg("speaker")}</span>
              <span class="control-room-group-copy">
                <span class="control-room-group-title">${this._esc(this._m(`${group.count} grouped players`, `${group.count} נגנים בקבוצה`))}</span>
                <span class="control-room-group-members">${this._esc(group.label)}</span>
              </span>
            </div>
          `).join("")}
        </div>
      `;
    }
    _controlRoomSelectedPlayerIds() {
      const players = this._controlRoomPlayers();
      const validIds = new Set(players.map((player) => player.entity_id));
      let selected = (Array.isArray(this._state.controlRoomSelectedPlayers) ? this._state.controlRoomSelectedPlayers : []).filter((entityId) => validIds.has(entityId));
      if (!selected.length) {
        const preferred = this._state.selectedPlayer;
        if (preferred && validIds.has(preferred)) selected = [preferred];
        else if (players[0]?.entity_id) selected = [players[0].entity_id];
      }
      this._state.controlRoomSelectedPlayers = selected;
      return selected;
    }
    _controlRoomPrimaryPlayerId() {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      if (selectedIds[0]) return selectedIds[0];
      const players = this._controlRoomPlayers();
      const validIds = new Set(players.map((player) => player.entity_id));
      const preferred = this._state.selectedPlayer;
      if (preferred && validIds.has(preferred)) return preferred;
      return players[0]?.entity_id || "";
    }
    _controlRoomPrimaryPlayer() {
      return this._playerByEntityId(this._controlRoomPrimaryPlayerId());
    }
    _setControlRoomSelection(entityIds = []) {
      const players = this._controlRoomPlayers();
      const validIds = new Set(players.map((player) => player.entity_id));
      const next = [];
      (Array.isArray(entityIds) ? entityIds : []).forEach((entityId) => {
        if (entityId && validIds.has(entityId) && !next.includes(entityId)) next.push(entityId);
      });
      if (!next.length) {
        const preferred = this._state.selectedPlayer;
        if (preferred && validIds.has(preferred)) next.push(preferred);
        else if (players[0]?.entity_id) next.push(players[0].entity_id);
      }
      this._state.controlRoomSelectedPlayers = next;
      this._syncControlRoomTransferDefaults();
      this._syncControlRoomUi();
    }
    _toggleControlRoomPlayerSelection(entityId) {
      if (!entityId) return "kept";
      const current = this._controlRoomSelectedPlayerIds();
      const isSelected = current.includes(entityId);
      if (isSelected && current.length <= 1) {
        this._setControlRoomSelection(current);
        return "kept";
      }
      const next = isSelected ? current.filter((id) => id !== entityId) : [...current, entityId];
      this._setControlRoomSelection(next);
      return isSelected ? "removed" : "added";
    }
    _setControlRoomPrimary(entityId, options = {}) {
      if (!entityId) return;
      const current = this._controlRoomSelectedPlayerIds().filter((id) => id !== entityId);
      const exclusive = !!options.exclusive;
      this._state.controlRoomSelectedPlayers = [entityId, ...exclusive ? [] : current];
      this._syncControlRoomTransferDefaults();
      if (options.selectPlayer !== false) this._selectPlayer(entityId, true);
      else this._syncControlRoomUi();
    }
    _setControlRoomVisiblePlayers(entityIds = []) {
      const players = this._controlRoomAllPlayers();
      const validIds = new Set(players.map((player) => player.entity_id));
      const next = [];
      (Array.isArray(entityIds) ? entityIds : []).forEach((entityId) => {
        if (entityId && validIds.has(entityId) && !next.includes(entityId)) next.push(entityId);
      });
      if (!next.length && players[0]?.entity_id) next.push(players[0].entity_id);
      this._state.controlRoomVisiblePlayers = next;
      this._state.controlRoomSelectedPlayers = this._controlRoomSelectedPlayerIds().filter((entityId) => next.includes(entityId));
      const primaryId = this._controlRoomPrimaryPlayerId();
      if (!next.includes(primaryId) && next[0]) this._setControlRoomPrimary(next[0], { exclusive: false, selectPlayer: true });
      else {
        this._syncControlRoomTransferDefaults();
        this._syncControlRoomUi();
      }
    }
    _toggleControlRoomVisiblePlayer(entityId) {
      if (!entityId) return;
      const current = this._controlRoomVisiblePlayerIds();
      const next = current.includes(entityId) ? current.filter((id) => id !== entityId) : [...current, entityId];
      this._setControlRoomVisiblePlayers(next);
    }
    _controlRoomPlayerChoiceRows(kind = "selection") {
      const allPlayers = kind === "visible" ? this._controlRoomAllPlayers() : this._controlRoomPlayers();
      const activeIds = new Set(
        kind === "visible" ? this._controlRoomVisiblePlayerIds() : this._controlRoomSelectedPlayerIds()
      );
      return `
        <div class="control-room-picker-list" data-control-room-scroll="${this._esc(kind)}">
          ${allPlayers.map((player) => {
        const entityId = player.entity_id;
        const active = activeIds.has(entityId);
        const art = this._bestArtworkUrl([player.attributes?.entity_picture_local, player.attributes?.entity_picture], {
          size: 120,
          cacheKey: this._currentArtworkCacheKey(player)
        });
        const name = player.attributes?.friendly_name || entityId;
        const subtitle = player.attributes?.media_title || this._playerStateLabel(player);
        const attr = kind === "visible" ? "data-room-visible-toggle" : "data-room-selection-toggle";
        return `
              <button class="control-room-picker-row ${active ? "active" : ""}" ${attr}="${this._esc(entityId)}">
                <span class="control-room-picker-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("speaker")}</span>
                <span class="control-room-picker-copy">
                  <span class="control-room-picker-title">${this._esc(name)}</span>
                  <span class="control-room-picker-sub">${this._esc(subtitle || "")}</span>
                </span>
                <span class="control-room-picker-check">${this._iconSvg(active ? "check" : "plus")}</span>
              </button>
            `;
      }).join("")}
        </div>
      `;
    }
    _syncControlRoomTransferDefaults() {
      const players = this._controlRoomPlayers();
      const ids = players.map((player) => player.entity_id);
      const primaryId = this._controlRoomPrimaryPlayerId();
      if (!ids.includes(this._state.controlRoomTransferSource)) {
        this._state.controlRoomTransferSource = this._state.selectedPlayer && ids.includes(this._state.selectedPlayer) ? this._state.selectedPlayer : ids[0] || "";
      }
      if (!ids.includes(this._state.controlRoomTransferTarget) || this._state.controlRoomTransferTarget === this._state.controlRoomTransferSource) {
        this._state.controlRoomTransferTarget = primaryId && primaryId !== this._state.controlRoomTransferSource ? primaryId : ids.find((id) => id !== this._state.controlRoomTransferSource) || primaryId || "";
      }
    }
    _syncControlRoomChrome() {
      const open = !!this._state.controlRoomOpen && this._controlRoomEnabled();
      this.$("controlRoomBackdrop")?.classList.toggle("open", open);
      this.shadowRoot?.querySelector(".card")?.classList.toggle("control-room-open", open);
    }
    _openControlRoom() {
      if (!this._controlRoomEnabled()) return;
      this._state.controlRoomOpen = true;
      this._state.controlRoomPanel = "";
      this._controlRoomSelectedPlayerIds();
      this._syncControlRoomTransferDefaults();
      this._syncControlRoomChrome();
      this._syncControlRoomUi({ force: true });
      this._loadControlRoomQueues(this._controlRoomPlayers().map((player) => player.entity_id)).catch(() => {
      });
      this._toastSuccess(this._i18n("ui.studio_opened"));
    }
    _closeControlRoom(options = {}) {
      this._suppressHomeShortcutNavigation();
      this._state.controlRoomOpen = false;
      this._state.controlRoomPanel = "";
      this._state.controlRoomRestoreAfterMenu = false;
      this._syncControlRoomChrome();
      if (!options.silent) this._toast(this._i18n("ui.studio_closed"));
    }
    _isScheduleFormControl(target) {
      const el = target?.closest?.("input, select, textarea");
      if (!el) return false;
      const id = el.id || "";
      if ([
        "scheduledStartTimeInput",
        "scheduledStartPlayerSelect",
        "scheduledStartPlaylistSelect",
        "scheduledStartAfterRunSelect",
        "scheduledStartVolumeInput",
        "mobileNightStartInput",
        "mobileNightEndInput"
      ].includes(id)) return true;
      return el.dataset?.startTimerDay !== void 0 || el.dataset?.settingNightDay !== void 0;
    }
    _markScheduleFormControlActive(target = null) {
      if (!this._isScheduleFormControl(target)) return false;
      this._state.mobileScheduleControlActiveUntil = Date.now() + 2500;
      return true;
    }
    _isScheduleFormEditing() {
      if (!this._state.menuOpen || this._state.menuPage !== "sleep_timer") return false;
      const active = this.shadowRoot?.activeElement;
      return this._isScheduleFormControl(active) || Date.now() < Number(this._state.mobileScheduleControlActiveUntil || 0);
    }
    _rebuildMobileUi(options = {}) {
      const reopenPage = typeof options.reopenPage === "string" ? options.reopenPage : this._state.menuOpen ? this._state.menuPage || "main" : "";
      const reopenStudio = typeof options.reopenStudio === "boolean" ? options.reopenStudio : !!this._state.controlRoomOpen;
      if (!options.force && reopenPage === "sleep_timer" && this._isScheduleFormEditing()) return;
      const previousMenuPage = this._state.menuPage || "main";
      const previousMenuScrollTop = reopenPage && reopenPage === previousMenuPage ? this.$("mobileMenuBody")?.scrollTop || 0 : null;
      this._build();
      this._init();
      if (reopenPage) this._openMobileMenu(reopenPage, { scrollTop: previousMenuScrollTop });
      if (reopenStudio && this._controlRoomEnabled()) {
        this._state.controlRoomOpen = true;
        this._syncControlRoomChrome();
        this._syncControlRoomUi({ force: true });
      }
    }
    _openControlRoomLibrary(page = "library_playlists") {
      this._state.controlRoomRestoreAfterMenu = true;
      this._state.controlRoomOpen = true;
      this._openMobileMenu(page);
    }
    _toggleControlRoomPanel(panel = "") {
      const next = String(panel || "");
      this._state.controlRoomPanel = this._state.controlRoomPanel === next ? "" : next;
      this._syncControlRoomUi();
      this._primeControlRoomPanelData(this._state.controlRoomPanel);
    }
    _primeControlRoomPanelData(panel = "") {
      const activePanel = String(panel || "");
      if (!activePanel) return;
      if (activePanel === "transfer") {
        const ids = [
          this._state.controlRoomTransferSource,
          this._state.controlRoomTransferTarget,
          ...this._controlRoomSelectedPlayerIds()
        ].filter(Boolean);
        this._loadControlRoomQueues(ids).catch(() => {
        });
        return;
      }
      if (activePanel === "recent") {
        this._loadControlRoomRecent().catch(() => {
        });
        return;
      }
      if (activePanel === "favorites") {
        this._loadControlRoomFavorites().catch(() => {
        });
      }
    }
    _controlRoomMediaTypeIcon(mediaType = "") {
      const type = String(mediaType || "").toLowerCase();
      if (type === "playlist") return "playlist";
      if (type === "artist") return "artist";
      if (type === "track") return "tracks";
      if (type === "radio") return "radio";
      if (type === "podcast") return "podcast";
      return "album";
    }
    _controlRoomMediaTypeLabel(mediaType = "") {
      const type = String(mediaType || "").toLowerCase();
      const labels = {
        track: this._i18n("ui.track"),
        album: this._i18n("ui.album"),
        artist: this._i18n("ui.artist"),
        playlist: this._i18n("ui.playlist"),
        radio: this._i18n("ui.radio"),
        podcast: this._i18n("ui.podcast")
      };
      return labels[type] || this._i18n("ui.media");
    }
    _controlRoomNormalizeMediaEntry(item = {}, fallbackType = "album", options = {}) {
      const mediaType = String(item?.media_type || item?.type || item?.media_item?.media_type || fallbackType || "album").toLowerCase();
      const artists = Array.isArray(item?.artists) ? item.artists.map((artist) => artist?.name).filter(Boolean).join(", ") : "";
      const uri = item?.uri || item?.media_item?.uri || item?.media_content_id || "";
      return {
        uri,
        media_type: mediaType,
        name: item?.name || item?.title || item?.media_item?.name || uri || this._controlRoomMediaTypeLabel(mediaType),
        subtitle: options.subtitle || artists || item?.artist || item?.album?.name || item?.metadata?.description || item?.provider_label || this._controlRoomMediaTypeLabel(mediaType),
        artist: artists || item?.artist || "",
        album: item?.album?.name || item?.album || "",
        image: this._artUrl(item) || item?.image || item?.image_url || item?.media_item?.image || item?.media_image || "",
        favorite: !!item?.favorite
      };
    }
    _controlRoomEntryDataAttrs(entry = {}) {
      return [
        `data-room-library-uri="${this._esc(entry.uri || "")}"`,
        `data-room-library-type="${this._esc(entry.media_type || "album")}"`,
        `data-room-library-name="${this._esc(entry.name || "")}"`,
        `data-room-library-subtitle="${this._esc(entry.subtitle || "")}"`,
        `data-room-library-image="${this._esc(entry.image || "")}"`
      ].join(" ");
    }
    _controlRoomProtocolLabel(player = null) {
      const attrs = player?.attributes || {};
      return String(
        attrs.mass_player_type || attrs.player_type || attrs.provider || attrs.provider_name || attrs.source || attrs.app_name || "MA"
      ).replace(/_/g, " ").trim();
    }
    _controlRoomQueueCount(player = null, snapshot = null) {
      const attrs = player?.attributes || {};
      const candidates = [
        snapshot?.state?.items,
        attrs.queue_items,
        attrs.queue_size,
        attrs.queue_length,
        attrs.media_playlist_length,
        attrs.items_in_queue,
        attrs.active_queue_items
      ];
      const value = candidates.map((item) => Number(item)).find((item) => Number.isFinite(item) && item >= 0);
      return Number.isFinite(value) ? Math.round(value) : 0;
    }
    _controlRoomQueueCache(entityId = "") {
      const cache = this._state.controlRoomQueueSnapshots || {};
      const entry = cache[String(entityId || "")];
      return entry?.snapshot || null;
    }
    async _fetchDirectControlRoomQueueSnapshot(player = null) {
      const queueId = this._directMaQueueId(player);
      if (!queueId || !this._hasDirectMAConnection()) return null;
      let queueState = null;
      let items = [];
      try {
        queueState = await this._callDirectMaCommand("player_queues/get", { queue_id: queueId });
      } catch (_) {
      }
      try {
        const fullSnapshot = await this._callDirectMaCommand("player_queues/items", { queue_id: queueId, limit: 120, offset: 0 });
        items = Array.isArray(fullSnapshot?.items) ? fullSnapshot.items : Array.isArray(fullSnapshot) ? fullSnapshot : [];
      } catch (_) {
      }
      if (!queueState && !items.length) return null;
      return this._normalizeQueueSnapshot({ queue_state: queueState || {}, items }, player?.entity_id || "");
    }
    async _fetchControlRoomQueueSnapshot(entityId = "") {
      const player = this._playerByEntityId(entityId);
      if (!player) return null;
      if (entityId === this._state.selectedPlayer) {
        await this._ensureQueueSnapshot(true);
        const items = Array.isArray(this._state.queueItems) ? this._state.queueItems : [];
        if (items.length || this._state.maQueueState) {
          return {
            state: this._state.maQueueState || { items: items.length, current_index: 0 },
            items
          };
        }
      }
      let snapshot = null;
      try {
        snapshot = await this._fetchMassQueueItemsSnapshot(player);
      } catch (_) {
        snapshot = null;
      }
      if (!snapshot) {
        try {
          snapshot = await this._fetchDirectControlRoomQueueSnapshot(player);
        } catch (_) {
          snapshot = null;
        }
      }
      return snapshot;
    }
    async _loadControlRoomQueues(entityIds = []) {
      const ids = [...new Set((Array.isArray(entityIds) ? entityIds : []).filter(Boolean))];
      if (!ids.length) return;
      this._state.controlRoomQueueLoading = true;
      this._syncControlRoomUi();
      const nextCache = { ...this._state.controlRoomQueueSnapshots || {} };
      const results = await Promise.allSettled(ids.map(async (entityId) => {
        const snapshot = await this._fetchControlRoomQueueSnapshot(entityId);
        return { entityId, snapshot };
      }));
      results.forEach((result) => {
        if (result.status !== "fulfilled") return;
        nextCache[result.value.entityId] = {
          ts: Date.now(),
          snapshot: result.value.snapshot
        };
      });
      this._state.controlRoomQueueSnapshots = nextCache;
      this._state.controlRoomQueueLoading = false;
      this._syncControlRoomUi({ force: true });
    }
    _controlRoomQueuePreviewHtml(entityId = "") {
      const player = this._playerByEntityId(entityId);
      const snapshot = this._controlRoomQueueCache(entityId);
      const items = HomeiiMediaQueueFoundation2.sortQueueItems(snapshot?.items || []);
      const currentIndex = Number(snapshot?.state?.current_index);
      const currentItem = Number.isFinite(currentIndex) ? items.find((item) => Number(item?.sort_index) === currentIndex) || items[0] : items[0];
      const queueCount = this._controlRoomQueueCount(player, snapshot);
      const previewItems = (currentItem ? [currentItem, ...items.filter((item) => item !== currentItem)] : items).slice(0, 4);
      const title = player?.attributes?.friendly_name || entityId || this._i18n("ui.player_2");
      return `
        <div class="control-room-queue-preview" data-control-room-scroll="queue-${this._esc(entityId)}">
          <div class="control-room-queue-preview-head">
            <span class="control-room-queue-player">${this._esc(title)}</span>
            <span class="control-room-queue-count">${this._esc(queueCount ? `${queueCount}` : this._i18n("ui.no_queue"))}</span>
          </div>
          ${previewItems.length ? previewItems.map((item, index) => {
        const media = item.media_item || {};
        const art = this._queueItemImageUrl(item, 96) || this._artUrl(media) || "";
        const itemTitle = media.name || item.name || item.media_title || this._i18n("ui.queue_item");
        const artist = item.media_artist || (media.artists || []).map((artistEntry) => artistEntry?.name).filter(Boolean).join(", ") || media.album?.name || "";
        return `
              <div class="control-room-queue-row ${index === 0 ? "current" : ""}">
                <span class="control-room-queue-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("music_note")}</span>
                <span class="control-room-queue-copy">
                  <span class="control-room-queue-title">${this._esc(itemTitle)}</span>
                  <span class="control-room-queue-sub">${this._esc(index === 0 ? this._i18n("ui.now_playing_2") : artist || this._i18n("ui.up_next_2"))}</span>
                </span>
              </div>
            `;
      }).join("") : `<div class="control-room-empty subtle">${this._esc(this._state.controlRoomQueueLoading ? this._i18n("ui.loading_queue") : this._i18n("ui.queue_is_unavailable_for_this_player"))}</div>`}
        </div>
      `;
    }
    _controlRoomMixPresets() {
      return [
        { id: "calm", icon: "moon", label: this._i18n("ui.calm"), subtitle: this._i18n("ui.soft_relaxed_music"), queries: ["relax chill playlist", "calm music", "acoustic chill"] },
        { id: "party", icon: "radio", label: this._i18n("ui.party"), subtitle: this._i18n("ui.energy_and_rhythm"), queries: ["party hits playlist", "dance playlist", "upbeat music"] },
        { id: "morning", icon: "music_note", label: this._i18n("ui.morning"), subtitle: this._i18n("ui.fresh_start"), queries: ["morning playlist", "coffee music", "feel good morning"] },
        { id: "night", icon: "moon", label: this._i18n("ui.night"), subtitle: this._i18n("ui.lower_volume_mood"), queries: ["night chill playlist", "sleep music", "quiet jazz"] },
        { id: "kids", icon: "speaker", label: this._i18n("ui.kids"), subtitle: this._i18n("ui.family_friendly"), queries: ["kids music playlist", "children songs", "family music"] },
        { id: "israeli", icon: "music_note", label: this._i18n("ui.israeli"), subtitle: this._i18n("ui.local_favorites"), queries: ["ישראלי עברית פלייליסט", "israeli music hebrew"] },
        { id: "favorites", icon: "heart_filled", label: this._i18n("ui.liked"), subtitle: this._i18n("ui.shuffle_favorites"), favorite: true },
        { id: "random", icon: "shuffle", label: this._i18n("ui.random"), subtitle: this._i18n("ui.library_surprise"), random: true }
      ];
    }
    async _controlRoomFindMixEntries(presetId = "", customQuery = "") {
      const preset = this._controlRoomMixPresets().find((item) => item.id === presetId) || null;
      const query = String(customQuery || "").trim();
      if (preset?.favorite) {
        const favorites = this._useMaLikedMode() ? await this._loadMaLikedEntries(true) : this._likedEntries();
        return favorites.filter((item) => item?.uri).slice(0, 24);
      }
      if (preset?.random) {
        const tracks = await this._fetchLibrary("track", "random", 24, false);
        return tracks.map((item) => this._controlRoomNormalizeMediaEntry(item, "track")).filter((item) => item.uri);
      }
      const queries = query ? [query] : preset?.queries || ["music playlist"];
      const entries = [];
      for (const currentQuery of queries) {
        try {
          const results = await this._search(currentQuery);
          entries.push(...this._controlRoomSearchEntries(results));
        } catch (_) {
        }
        if (entries.length >= 12) break;
      }
      return this._controlRoomUniqueEntries(entries).slice(0, 12);
    }
    _controlRoomUniqueEntries(entries = []) {
      const seen = /* @__PURE__ */ new Set();
      return (Array.isArray(entries) ? entries : []).filter((entry) => {
        const key = String(entry?.uri || entry?.name || "").trim().toLowerCase();
        if (!key || seen.has(key)) return false;
        seen.add(key);
        return true;
      });
    }
    async _loadControlRoomRecent() {
      this._state.controlRoomRecentLoading = true;
      this._syncControlRoomUi();
      const items = [];
      try {
        items.push(...(await this._fetchRecentlyPlayed(18)).map((item) => this._controlRoomNormalizeMediaEntry(item, item.media_type || "album")));
      } catch (_) {
      }
      if (this._hasDirectMAConnection()) {
        try {
          const inProgress = await this._callDirectMaCommand("music/in_progress_items", { limit: 12 });
          const rawItems = Array.isArray(inProgress?.items) ? inProgress.items : Array.isArray(inProgress) ? inProgress : [];
          items.push(...rawItems.map((item) => this._controlRoomNormalizeMediaEntry(item, item.media_type || "podcast", {
            subtitle: this._i18n("ui.continue_listening")
          })));
        } catch (_) {
        }
      }
      this._state.controlRoomRecentItems = this._controlRoomUniqueEntries(items).slice(0, 24);
      this._state.controlRoomRecentLoading = false;
      this._syncControlRoomUi({ force: true });
    }
    async _loadControlRoomFavorites() {
      this._state.controlRoomFavoritesLoading = true;
      this._syncControlRoomUi();
      let items = [];
      try {
        items = this._useMaLikedMode() ? await this._loadMaLikedEntries(true) : this._likedEntries();
      } catch (_) {
        items = this._likedEntries();
      }
      this._state.controlRoomFavoritesItems = this._controlRoomUniqueEntries(
        items.map((item) => this._controlRoomNormalizeMediaEntry(item, item.media_type || "track", {
          subtitle: this._i18n("ui.favorite")
        }))
      ).slice(0, 36);
      this._state.controlRoomFavoritesLoading = false;
      this._syncControlRoomUi({ force: true });
    }
    _controlRoomMediaGridHtml(entries = [], options = {}) {
      const list = Array.isArray(entries) ? entries.filter((entry) => entry?.uri) : [];
      const empty = options.empty || this._i18n("ui.no_media_found");
      if (!list.length) return `<div class="control-room-empty subtle">${this._esc(empty)}</div>`;
      return `
        <div class="control-room-media-grid ${options.large ? "large" : ""}">
          ${list.map((entry) => {
        const liked = this._isEntryLiked(entry) || !!entry.favorite;
        const radioSupported = this._supportsMusicAssistantRadioMode(entry.media_type);
        const attrs = this._controlRoomEntryDataAttrs(entry);
        return `
              <article class="control-room-media-card ${liked ? "liked" : ""}">
                <button class="control-room-media-main" data-room-library-action="play" ${attrs} title="${this._esc(this._i18n("ui.play_now"))}">
                  <span class="control-room-media-art">${entry.image ? `<img src="${this._esc(entry.image)}" alt="">` : this._iconSvg(this._controlRoomMediaTypeIcon(entry.media_type))}</span>
                  <span class="control-room-media-copy">
                    <span class="control-room-media-kicker">${this._esc(this._controlRoomMediaTypeLabel(entry.media_type))}</span>
                    <span class="control-room-media-title">${this._esc(entry.name || this._i18n("ui.media"))}</span>
                    <span class="control-room-media-sub">${this._esc(entry.subtitle || entry.media_type || "")}</span>
                  </span>
                </button>
                <span class="control-room-media-actions">
                  <button type="button" class="control-room-media-action primary" data-room-library-action="play" ${attrs}>${this._esc(this._i18n("ui.play"))}</button>
                  <button type="button" class="control-room-media-action" data-room-library-action="next" ${attrs}>${this._esc(this._i18n("ui.next"))}</button>
                  <button type="button" class="control-room-media-action" data-room-library-action="add" ${attrs}>${this._esc(this._i18n("ui.add"))}</button>
                  ${radioSupported ? `<button type="button" class="control-room-media-action" data-room-library-action="radio_mode" ${attrs}>${this._esc(this._i18n("ui.radio"))}</button>` : ``}
                  <button type="button" class="control-room-media-action icon ${liked ? "active" : ""}" data-room-library-action="like" ${attrs} title="${this._esc(this._i18n("ui.like_2"))}">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}</button>
                </span>
              </article>
            `;
      }).join("")}
        </div>
      `;
    }
    async _prepareControlRoomPlaybackTargets() {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const primaryId = selectedIds[0] || this._controlRoomPrimaryPlayerId();
      if (!primaryId) return "";
      const groupMembers = selectedIds.slice(1);
      if (groupMembers.length) {
        await this._applySpeakerGroupFor(primaryId, groupMembers);
      }
      return primaryId;
    }
    async _playControlRoomEntries(entries = [], options = {}) {
      const playable = (Array.isArray(entries) ? entries : []).filter((entry) => entry?.uri);
      const primaryId = await this._prepareControlRoomPlaybackTargets();
      if (!primaryId || !playable.length) return false;
      const first = playable[0];
      const firstOk = await this._playMediaOnPlayer(primaryId, first.uri, first.media_type || "track", options.shuffle ? "shuffle" : "play", {
        label: first.name || "",
        silent: true,
        radioMode: !!options.radioMode
      });
      if (!firstOk) return false;
      for (const entry of playable.slice(1, 40)) {
        await this._playMediaOnPlayer(primaryId, entry.uri, entry.media_type || "track", "add", {
          label: entry.name || "",
          silent: true
        });
      }
      if (!options.silent) {
        this._toastSuccess(this._m(
          `Started ${playable.length} items in Studio`,
          `${playable.length} פריטים הופעלו בסטודיו`
        ));
      }
      setTimeout(() => this._updateNowPlayingState(), 500);
      return true;
    }
    async _startControlRoomMix(presetId = "", sourceEl = null) {
      const customInput = this.$("controlRoomSmartQueryInput");
      const customQuery = customInput?.value || this._state.controlRoomSmartQuery || "";
      if (sourceEl) this._pressUiButton(sourceEl);
      this._toast(this._i18n("ui.building_studio_mix"));
      try {
        const entries = await this._controlRoomFindMixEntries(presetId, customQuery);
        if (!entries.length) {
          this._toastError(this._i18n("ui.no_mix_content_found"));
          return false;
        }
        const ok = await this._playControlRoomEntries(entries, { shuffle: presetId === "favorites" || presetId === "random" });
        if (ok) {
          this._state.controlRoomPanel = "";
          this._toastSuccess(this._i18n("ui.studio_mix_started"));
        }
        return ok;
      } catch (error) {
        this._toastError(error?.message || this._i18n("ui.could_not_build_studio_mix"));
        return false;
      }
    }
    async _cloneQueueBetween(sourcePlayerEntityId, targetPlayerEntityId, options = {}) {
      const sourcePlayer = this._playerByEntityId(sourcePlayerEntityId);
      if (!sourcePlayer || !targetPlayerEntityId || sourcePlayer.entity_id === targetPlayerEntityId) return false;
      try {
        const snapshot = await this._fetchControlRoomQueueSnapshot(sourcePlayer.entity_id);
        const items = HomeiiMediaQueueFoundation2.sortQueueItems(snapshot?.items || []);
        if (!items.length) throw new Error(this._i18n("ui.no_queue_to_clone"));
        const currentPos = sourcePlayer.entity_id === this._state.selectedPlayer ? this._getCurrentPosition() : 0;
        await this._rebuildQueue(targetPlayerEntityId, items, currentPos);
        if (options.selectTarget !== false) this._selectPlayer(targetPlayerEntityId, true);
        if (!options.silent) this._toastSuccess(this._i18n("ui.queue_cloned"));
        this._loadControlRoomQueues([sourcePlayer.entity_id, targetPlayerEntityId]).catch(() => {
        });
        return true;
      } catch (error) {
        if (!options.silent) this._toastError(error?.message || this._i18n("ui.could_not_clone_the_queue"));
        return false;
      }
    }
    async _sendControlRoomAnnouncement(sourceEl = null) {
      const input = this.$("controlRoomAnnouncementText");
      const volumeInput = this.$("controlRoomAnnouncementVolumeInput");
      const message = String(input?.value || this._state.controlRoomAnnouncementText || "").trim();
      if (!message) {
        this._toastError(this._i18n("ui.enter_an_announcement_first"));
        return false;
      }
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const targets = selectedIds.length ? selectedIds : [this._controlRoomPrimaryPlayerId()].filter(Boolean);
      if (!targets.length) {
        this._toastError(this._i18n("ui.select_at_least_one_studio_player"));
        return false;
      }
      if (sourceEl) this._pressUiButton(sourceEl);
      const previousText = this._state.mobileAnnouncementText;
      const previousTarget = this._state.mobileAnnouncementTarget;
      const previousVolume = this._state.mobileAnnouncementVolume;
      this._state.mobileAnnouncementText = message;
      this._state.mobileAnnouncementTarget = targets.length === this._announcementEligiblePlayers().length ? "all" : targets[0];
      this._state.mobileAnnouncementVolume = Math.max(20, Math.min(50, Number(volumeInput?.value || this._state.controlRoomAnnouncementVolume || 20) || 20));
      try {
        if (targets.length === 1) {
          await this._sendMobileAnnouncement();
        } else {
          const eligibleMap = new Map(this._announcementEligiblePlayers().map((player) => [player.entity_id, player]));
          const volumeSnapshots = this._prepareAnnouncementVolumes(targets.map((entityId) => eligibleMap.get(entityId)).filter(Boolean));
          for (const entityId of targets) {
            this._state.mobileAnnouncementTarget = entityId;
            await this._sendMobileAnnouncement();
          }
          this._scheduleAnnouncementVolumeRestore(volumeSnapshots, this._announcementRestoreDelayMs(message));
        }
        return true;
      } finally {
        this._state.mobileAnnouncementText = previousText;
        this._state.mobileAnnouncementTarget = previousTarget;
        this._state.mobileAnnouncementVolume = previousVolume;
      }
    }
    _controlRoomScenesStorageKey() {
      return "homeii_music_flow_control_room_scenes_v1";
    }
    _normalizeControlRoomScene(scene = {}, index = 0) {
      const rawId = String(scene?.id || `custom:${Date.now()}_${index}`).trim();
      const id = rawId.startsWith("custom:") ? rawId : `custom:${rawId}`;
      const playerIds = Array.isArray(scene?.playerIds) ? scene.playerIds.map((entityId) => String(entityId || "").trim()).filter(Boolean) : [];
      const visibleIds = Array.isArray(scene?.visibleIds) ? scene.visibleIds.map((entityId) => String(entityId || "").trim()).filter(Boolean) : [];
      const volumes = {};
      if (scene?.volumes && typeof scene.volumes === "object") {
        Object.entries(scene.volumes).forEach(([entityId, value]) => {
          const pct = Math.max(0, Math.min(1, Number(value)));
          if (entityId && Number.isFinite(pct)) volumes[String(entityId)] = pct;
        });
      }
      const media = scene?.media && typeof scene.media === "object" ? {
        uri: String(scene.media.uri || "").trim(),
        media_type: String(scene.media.media_type || scene.media.type || "track").trim() || "track",
        name: String(scene.media.name || "").trim()
      } : { uri: "", media_type: "track", name: "" };
      return {
        id,
        name: String(scene?.name || "").trim() || this._i18n("ui.studio_scene"),
        primaryId: String(scene?.primaryId || playerIds[0] || "").trim(),
        playerIds: [...new Set(playerIds)],
        visibleIds: [...new Set(visibleIds)],
        volumes,
        group: scene?.group !== false,
        media,
        createdAt: Number(scene?.createdAt || Date.now()) || Date.now()
      };
    }
    _loadControlRoomScenesFromStorage() {
      try {
        const raw = JSON.parse(localStorage.getItem(this._controlRoomScenesStorageKey()) || "[]");
        this._state.controlRoomCustomScenes = Array.isArray(raw) ? raw.map((scene, index) => this._normalizeControlRoomScene(scene, index)).filter((scene) => scene.playerIds.length).slice(0, 12) : [];
      } catch (_) {
        this._state.controlRoomCustomScenes = [];
      }
    }
    _persistControlRoomScenes() {
      try {
        localStorage.setItem(this._controlRoomScenesStorageKey(), JSON.stringify(this._controlRoomCustomScenes()));
      } catch (_) {
      }
    }
    _controlRoomCustomScenes() {
      return (Array.isArray(this._state.controlRoomCustomScenes) ? this._state.controlRoomCustomScenes : []).map((scene, index) => this._normalizeControlRoomScene(scene, index)).filter((scene) => scene.playerIds.length).slice(0, 12);
    }
    _captureControlRoomScene(name = "") {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const primaryId = this._controlRoomPrimaryPlayerId();
      const targets = selectedIds.length ? selectedIds : [primaryId].filter(Boolean);
      if (!targets.length) return null;
      const primary = this._playerByEntityId(primaryId || targets[0]);
      const attrs = primary?.attributes || {};
      const volumes = {};
      targets.forEach((entityId) => {
        const player = this._playerByEntityId(entityId);
        const volume = Number(player?.attributes?.volume_level);
        if (Number.isFinite(volume)) volumes[entityId] = Math.max(0, Math.min(1, volume));
      });
      const mediaUri = String(attrs.media_content_id || attrs.media_uri || attrs.uri || "").trim();
      const mediaType = String(attrs.media_content_type || attrs.media_type || "track").trim() || "track";
      return this._normalizeControlRoomScene({
        id: `custom:${Date.now().toString(36)}`,
        name: String(name || "").trim() || this._i18n("ui.my_studio_scene"),
        primaryId: primaryId || targets[0],
        playerIds: targets,
        visibleIds: this._controlRoomVisiblePlayerIds(),
        volumes,
        group: targets.length > 1,
        media: {
          uri: mediaUri,
          media_type: mediaType,
          name: attrs.media_title || ""
        },
        createdAt: Date.now()
      });
    }
    _saveControlRoomSceneFromStudio(sourceEl = null) {
      if (sourceEl) this._pressUiButton(sourceEl);
      const input = this.$("controlRoomSceneNameInput");
      const scene = this._captureControlRoomScene(input?.value || this._state.controlRoomSceneName || "");
      if (!scene) {
        this._toastError(this._i18n("ui.select_at_least_one_studio_player"));
        return false;
      }
      this._state.controlRoomCustomScenes = [scene, ...this._controlRoomCustomScenes()].slice(0, 12);
      this._state.controlRoomSceneName = "";
      if (input) input.value = "";
      this._persistControlRoomScenes();
      this._syncControlRoomUi({ force: true });
      this._toastSuccess(this._i18n("ui.studio_scene_saved"));
      return true;
    }
    _deleteControlRoomScene(sceneId = "", sourceEl = null) {
      if (sourceEl) this._pressUiButton(sourceEl);
      const id = String(sceneId || "").trim();
      if (!id) return false;
      this._state.controlRoomCustomScenes = this._controlRoomCustomScenes().filter((scene) => scene.id !== id);
      this._persistControlRoomScenes();
      this._syncControlRoomUi({ force: true });
      this._toastSuccess(this._i18n("ui.studio_scene_deleted"));
      return true;
    }
    async _applySavedControlRoomScene(scene = null, sourceEl = null) {
      const saved = scene ? this._normalizeControlRoomScene(scene) : null;
      if (!saved) return false;
      const allPlayers = this._controlRoomAllPlayers();
      const validIds = new Set(allPlayers.map((player) => player.entity_id));
      const selected = saved.playerIds.filter((entityId) => validIds.has(entityId));
      if (!selected.length) {
        this._toastError(this._i18n("ui.scene_players_are_not_available"));
        return false;
      }
      const primaryId = selected.includes(saved.primaryId) ? saved.primaryId : selected[0];
      const ordered = [primaryId, ...selected.filter((entityId) => entityId !== primaryId)];
      const visible = [.../* @__PURE__ */ new Set([
        ...this._controlRoomVisiblePlayerIds(),
        ...ordered,
        ...saved.visibleIds.filter((entityId) => validIds.has(entityId))
      ])];
      this._state.controlRoomVisiblePlayers = visible;
      this._state.controlRoomSelectedPlayers = ordered;
      this._syncControlRoomTransferDefaults();
      if (sourceEl) this._pressUiButton(sourceEl);
      if (ordered.length > 1 && saved.group) {
        await this._applySpeakerGroupFor(primaryId, ordered.slice(1));
      }
      await Promise.allSettled(ordered.map((entityId) => {
        const volume = saved.volumes?.[entityId];
        return Number.isFinite(volume) ? this._setPlayerVolumeFor(entityId, volume) : Promise.resolve();
      }));
      if (saved.media?.uri) {
        await this._playMediaOnPlayer(primaryId, saved.media.uri, saved.media.media_type || "track", "play", {
          label: saved.media.name || saved.name,
          silent: true
        });
      }
      this._syncControlRoomUi({ force: true });
      this._toastSuccess(this._m(`Scene "${saved.name}" applied`, `הסצנה "${saved.name}" הופעלה`));
      setTimeout(() => this._updateNowPlayingState(), 350);
      return true;
    }
    async _applyControlRoomScene(sceneId = "", sourceEl = null) {
      if (sourceEl) this._pressUiButton(sourceEl);
      const scene = String(sceneId || "home");
      if (scene.startsWith("custom:")) {
        const saved = this._controlRoomCustomScenes().find((item) => item.id === scene);
        if (!saved) {
          this._toastError(this._i18n("ui.studio_scene_was_not_found"));
          return false;
        }
        return this._applySavedControlRoomScene(saved, sourceEl);
      }
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const primaryId = selectedIds[0] || this._controlRoomPrimaryPlayerId();
      if (!primaryId) {
        this._toastError(this._i18n("ui.select_at_least_one_studio_player"));
        return false;
      }
      const targets = selectedIds.length ? selectedIds : [primaryId];
      if (targets.length > 1) await this._applySpeakerGroupFor(primaryId, targets.slice(1));
      const volume = scene === "night" ? 0.18 : scene === "party" ? 0.55 : 0.35;
      await Promise.allSettled(targets.map((entityId) => this._setPlayerVolumeFor(entityId, volume)));
      if (scene === "home") {
        this._toastSuccess(this._i18n("ui.home_scene_prepared"));
        return true;
      }
      const mixId = scene === "party" ? "party" : "night";
      return this._startControlRoomMix(mixId, sourceEl);
    }
    _controlRoomSearchEntries(results = {}) {
      const groups = [
        ["playlists", "playlist"],
        ["albums", "album"],
        ["tracks", "track"],
        ["artists", "artist"],
        ["radio", "radio"],
        ["podcasts", "podcast"]
      ];
      const entries = [];
      groups.forEach(([bucket, mediaType]) => {
        (Array.isArray(results?.[bucket]) ? results[bucket] : []).slice(0, 4).forEach((item) => {
          entries.push(this._controlRoomNormalizeMediaEntry(item, mediaType));
        });
      });
      return entries.filter((entry) => entry.uri).slice(0, 14);
    }
    async _searchControlRoomLibrary(query = "") {
      const rawQuery = String(query || "");
      const normalizedQuery = rawQuery.trim();
      this._state.controlRoomLibraryQuery = rawQuery;
      if (!normalizedQuery) {
        this._state.controlRoomLibraryLoading = false;
        this._state.controlRoomLibraryResults = [];
        this._syncControlRoomLibraryResultsUi();
        return;
      }
      const token = Date.now();
      this._state.controlRoomLibraryToken = token;
      this._state.controlRoomLibraryLoading = true;
      this._syncControlRoomLibraryResultsUi();
      try {
        const results = await this._search(normalizedQuery);
        if (this._state.controlRoomLibraryToken !== token) return;
        this._state.controlRoomLibraryResults = this._controlRoomSearchEntries(results);
      } catch (_) {
        if (this._state.controlRoomLibraryToken !== token) return;
        this._state.controlRoomLibraryResults = [];
      }
      if (this._state.controlRoomLibraryToken !== token) return;
      this._state.controlRoomLibraryLoading = false;
      this._syncControlRoomLibraryResultsUi();
    }
    _syncControlRoomLibraryResultsUi() {
      const host = this.$("controlRoomLibraryResults");
      if (host && this._state.controlRoomPanel === "library") {
        host.innerHTML = this._controlRoomLibraryResultsHtml();
        return;
      }
      this._syncControlRoomUi();
    }
    async _startControlRoomLibraryVoice() {
      const SpeechRecognition = this._speechRecognitionCtor();
      if (!SpeechRecognition) {
        this._toastError(this._i18n("ui.voice_input_is_not_supported_on_this_device"));
        return;
      }
      try {
        this._voiceRecognition?.abort?.();
      } catch {
      }
      const recognition = new SpeechRecognition();
      this._voiceRecognition = recognition;
      recognition.lang = this._isHebrew() ? "he-IL" : "en-US";
      recognition.interimResults = true;
      recognition.continuous = false;
      recognition.maxAlternatives = 1;
      this._toast(this._i18n("ui.listening"));
      recognition.onresult = (event) => {
        const transcript = Array.from(event.results || []).map((result) => result?.[0]?.transcript || "").join(" ").trim();
        if (!transcript) return;
        this._state.controlRoomLibraryQuery = transcript;
        this._state.controlRoomPanel = "library";
        this._syncControlRoomUi();
        const input = this.$("controlRoomLibraryInput");
        if (input) {
          input.value = transcript;
          input.focus({ preventScroll: true });
          input.setSelectionRange(transcript.length, transcript.length);
        }
        clearTimeout(this._searchTimer);
        this._searchTimer = setTimeout(() => this._searchControlRoomLibrary(transcript), 120);
      };
      recognition.onerror = () => this._toastError(this._i18n("ui.voice_input_failed"));
      recognition.onend = () => {
        if (this._voiceRecognition === recognition) this._voiceRecognition = null;
      };
      try {
        recognition.start();
      } catch (_) {
        this._toastError(this._i18n("ui.voice_input_failed"));
      }
    }
    async _playControlRoomLibraryEntry(entry, mode = "play") {
      const action = String(mode || "play");
      if (!entry?.uri) return false;
      if (action === "like") {
        await this._toggleLikeEntry(entry);
        return true;
      }
      const primaryId = await this._prepareControlRoomPlaybackTargets();
      if (!primaryId) return false;
      const mediaType = entry.media_type || "album";
      if (action === "radio_mode" && !this._supportsMusicAssistantRadioMode(mediaType)) {
        this._toastError(this._i18n("ui.radio_mode_is_not_available_for_this_media_type"));
        return false;
      }
      const enqueue = action === "next" ? "next" : action === "add" ? "add" : action === "shuffle" ? "shuffle" : "play";
      return this._playMediaOnPlayer(primaryId, entry.uri, mediaType, enqueue, {
        label: entry.name || "",
        silent: action !== "play",
        radioMode: action === "radio_mode"
      });
    }
    _controlRoomPlayerTileHtml(player) {
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const primaryId = this._controlRoomPrimaryPlayerId();
      const isSelected = selectedIds.includes(player.entity_id);
      const isPrimary = primaryId === player.entity_id;
      const playing = player.state === "playing";
      const art = this._playerArtworkUrl(player, 320);
      const name = player.attributes?.friendly_name || player.entity_id;
      const track = player.attributes?.media_title || this._i18n("ui.idle_2");
      const volume = Math.round((player.attributes?.volume_level || 0) * 100);
      const groupInfo = this._controlRoomGroupInfo(player);
      const groupCount = groupInfo.count;
      const stateLabel = this._playerStateLabel(player);
      const snapshot = this._controlRoomQueueCache(player.entity_id);
      const queueCount = this._controlRoomQueueCount(player, snapshot);
      const protocolLabel = this._controlRoomProtocolLabel(player);
      const muted = this._isMuted(player);
      const tileStyle = art ? `style="--control-room-tile-art:url('${this._esc(art)}')"` : "";
      return `
        <article class="control-room-tile ${art ? "has-art" : "no-art"} ${isSelected ? "selected" : ""} ${isPrimary ? "primary" : ""} ${playing ? "is-playing" : ""} ${groupCount ? "grouped" : ""}" data-room-tile="${this._esc(player.entity_id)}" ${tileStyle}>
          <div class="control-room-tile-bg"></div>
          <div class="control-room-tile-shade"></div>
          <button class="control-room-select-fab ${isSelected ? "active" : ""} ${isSelected && selectedIds.length > 1 ? "removable" : ""}" data-room-select="${this._esc(player.entity_id)}" title="${this._esc(isSelected && selectedIds.length > 1 ? this._i18n("ui.remove_from_selection") : isSelected ? this._i18n("ui.selected_player_2") : this._i18n("ui.add_to_selection"))}">
            ${this._iconSvg(isSelected && selectedIds.length > 1 ? "close" : isSelected ? "check" : "grid")}
            <span class="control-room-select-label">${this._esc(isSelected && selectedIds.length > 1 ? this._i18n("ui.remove") : isSelected ? this._i18n("ui.selected") : this._i18n("ui.select"))}</span>
          </button>
          <button class="control-room-tile-main" data-room-primary="${this._esc(player.entity_id)}" title="${this._esc(name)}">
            <span class="control-room-tile-copy">
              <span class="control-room-tile-pills">
                ${isPrimary ? `<span class="control-room-primary-pill">${this._esc(this._i18n("ui.primary"))}</span>` : ``}
                ${groupCount ? `<span class="control-room-float-pill grouped" title="${this._esc(groupInfo.label || this._i18n("ui.grouped_players"))}">${this._iconSvg("speaker")}${this._esc(this._m(`${groupCount} grouped`, `${groupCount} בקבוצה`))}</span>` : ``}
                ${queueCount ? `<span class="control-room-float-pill">${this._iconSvg("queue")}${this._esc(`${queueCount}`)}</span>` : ``}
                ${protocolLabel ? `<span class="control-room-float-pill protocol">${this._esc(protocolLabel)}</span>` : ``}
                ${playing ? `<span class="control-room-float-pill live">${this._esc(this._i18n("ui.playing"))}</span>` : ``}
              </span>
              <span class="control-room-tile-track">${this._esc(track)}</span>
              <span class="control-room-tile-name">${this._esc(name)}</span>
              <span class="control-room-tile-state">${this._esc(stateLabel)}</span>
            </span>
          </button>
          <div class="control-room-tile-actions">
            <button type="button" data-room-toggle-play="${this._esc(player.entity_id)}" title="${this._esc(this._i18n("ui.play_pause"))}">${this._iconSvg(playing ? "pause" : "play")}</button>
            <button type="button" data-room-next="${this._esc(player.entity_id)}" title="${this._esc(this._i18n("ui.next"))}">${this._iconSvg("next")}</button>
            <button type="button" class="${muted ? "active" : ""}" data-room-mute="${this._esc(player.entity_id)}" title="${this._esc(this._i18n("ui.mute"))}">${this._iconSvg(muted ? "volume_mute" : this._volumeIconName(player))}</button>
          </div>
          <label class="control-room-volume-row">
            <input class="control-room-volume" data-room-volume="${this._esc(player.entity_id)}" type="range" min="0" max="100" value="${volume}" style="--vol-pct:${volume}%">
            <span class="control-room-volume-value" data-room-volume-value="${this._esc(player.entity_id)}">${this._esc(String(volume))}%</span>
          </label>
        </article>
      `;
    }
    _controlRoomLibraryResultsHtml() {
      const loading = !!this._state.controlRoomLibraryLoading;
      const results = Array.isArray(this._state.controlRoomLibraryResults) ? this._state.controlRoomLibraryResults : [];
      const query = String(this._state.controlRoomLibraryQuery || "").trim();
      if (loading) return `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.searching_library"))}</div>`;
      if (!query) return `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.search_and_choose_play_next_add_radio_or_like"))}</div>`;
      return this._controlRoomMediaGridHtml(results, {
        empty: this._i18n("ui.no_media_found_for_this_search"),
        large: true
      });
    }
    _controlRoomPanelHtml(players = []) {
      const panel = String(this._state.controlRoomPanel || "");
      if (!panel) return ``;
      const context = this._controlRoomContextChipHtml();
      if (panel === "selection") {
        return `
          <div class="control-room-tray open compact">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.connected_players_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.choose_which_players_stay_in_the_current_control_selection"))}</div>
            </div>
            ${this._controlRoomPlayerChoiceRows("selection")}
          </div>
        `;
      }
      if (panel === "visible") {
        return `
          <div class="control-room-tray open compact">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.visible_tiles_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.choose_which_players_appear_as_tiles_in_the_room"))}</div>
            </div>
            ${this._controlRoomPlayerChoiceRows("visible")}
          </div>
        `;
      }
      if (panel === "music") {
        return `
          <div class="control-room-tray open wide control-room-hub-panel">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.music_hub_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.choose_the_source_first_playback_will_use_the_current_target"))}</div>
            </div>
            ${context}
            <div class="control-room-library-shortcuts">
              <button data-room-selection-action="browse_library">${this._iconSvg("playlist")}<span>${this._esc(this._i18n("ui.playlists"))}</span></button>
              <button data-room-selection-action="browse_artists">${this._iconSvg("artist")}<span>${this._esc(this._i18n("ui.artists"))}</span></button>
              <button data-room-selection-action="browse_albums">${this._iconSvg("album")}<span>${this._esc(this._i18n("ui.albums"))}</span></button>
              <button data-room-selection-action="browse_tracks">${this._iconSvg("tracks")}<span>${this._esc(this._i18n("ui.tracks"))}</span></button>
              <button data-room-selection-action="browse_radio">${this._iconSvg("radio")}<span>${this._esc(this._i18n("ui.radio"))}</span></button>
            </div>
            <div class="control-room-hub-grid">
              <button class="control-room-hub-card primary" data-room-selection-action="browse_library">${this._iconSvg("library_music")}<span>${this._esc(this._i18n("ui.library"))}</span><small>${this._esc(this._i18n("ui.browse_playlists_artists_albums_and_radio"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="library">${this._iconSvg("search")}<span>${this._esc(this._i18n("ui.search"))}</span><small>${this._esc(this._i18n("ui.search_tracks_albums_artists_and_playlists"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="mix">${this._iconSvg("wand")}<span>${this._esc(this._i18n("ui.flow_mix"))}</span><small>${this._esc(this._i18n("ui.mood_style_or_free_text"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="recent">${this._iconSvg("history")}<span>${this._esc(this._i18n("ui.recent"))}</span><small>${this._esc(this._i18n("ui.continue_what_was_played_recently"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="favorites">${this._iconSvg("heart_filled")}<span>${this._esc(this._i18n("ui.liked"))}</span><small>${this._esc(this._i18n("ui.favorites_and_liked_music"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="scenes">${this._iconSvg("home")}<span>${this._esc(this._i18n("ui.scenes"))}</span><small>${this._esc(this._i18n("ui.home_party_night_presets"))}</small></button>
            </div>
          </div>
        `;
      }
      if (panel === "actions") {
        const targetIds = this._controlRoomActionTargetIds();
        const targetCount = targetIds.length;
        const actionTarget = this._controlRoomFocusTarget();
        const actionArt = actionTarget.art || "";
        const primary = this._controlRoomPrimaryPlayer();
        const primaryPlaying = primary?.state === "playing";
        const primaryMuted = primary ? this._isMuted(primary) : false;
        return `
          <div class="control-room-tray open wide control-room-hub-panel">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.actions_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.only_actions_for_the_current_target_are_shown_here"))}</div>
            </div>
            <div class="control-room-action-console">
              <div class="control-room-action-now">
                <span class="control-room-action-art">${actionArt ? `<img src="${this._esc(actionArt)}" alt="">` : this._iconSvg("speaker")}</span>
                <span class="control-room-action-copy">
                  <span class="control-room-action-kicker">${this._esc(actionTarget.kicker)}</span>
                  <span class="control-room-action-name">${this._esc(actionTarget.name)}</span>
                  <span class="control-room-action-track">${this._esc(actionTarget.track)}</span>
                </span>
              </div>
              <div class="control-room-media-controls">
                <button class="control-room-media-control primary" data-room-selection-action="playpause" ${targetCount ? "" : "disabled"}>${this._iconSvg(primaryPlaying ? "pause" : "play")}<span>${this._esc(primaryPlaying ? this._i18n("ui.pause") : this._i18n("ui.play"))}</span></button>
                <button class="control-room-media-control" data-room-selection-action="next" ${targetCount ? "" : "disabled"}>${this._iconSvg("next")}<span>${this._esc(this._i18n("ui.next"))}</span></button>
                <button class="control-room-media-control ${primaryMuted ? "active" : ""}" data-room-selection-action="mute" ${targetCount ? "" : "disabled"}>${this._iconSvg(primaryMuted ? "volume_mute" : primary ? this._volumeIconName(primary) : "speaker")}<span>${this._esc(this._i18n("ui.mute"))}</span></button>
                <button class="control-room-media-control danger" data-room-selection-action="clear" ${targetCount ? "" : "disabled"}>${this._iconSvg("trash")}<span>${this._esc(this._i18n("ui.clear_queue"))}</span></button>
              </div>
            </div>
            <div class="control-room-action-grid management">
              <button class="control-room-hub-card" data-room-selection-action="selection">${this._iconSvg("grid")}<span>${this._esc(this._i18n("ui.target_players"))}</span><small>${this._esc(this._i18n("ui.choose_who_is_controlled"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="visible">${this._iconSvg("grid")}<span>${this._esc(this._i18n("ui.visible_tiles_2"))}</span><small>${this._esc(this._i18n("ui.clean_the_studio_wall"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="group" ${targetCount > 1 ? "" : "disabled"}>${this._iconSvg("speaker")}<span>${this._esc(this._i18n("ui.group"))}</span><small>${this._esc(this._i18n("ui.join_selected_players"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="ungroup" ${targetCount ? "" : "disabled"}>${this._iconSvg("close")}<span>${this._esc(this._i18n("ui.ungroup_2"))}</span><small>${this._esc(this._i18n("ui.disconnect_groups"))}</small></button>
              <button class="control-room-hub-card danger" data-room-selection-action="stop_all">${this._iconSvg("stop")}<span>${this._esc(this._i18n("ui.stop_all"))}</span><small>${this._esc(this._i18n("ui.stop_playback_clear_queues_and_disconnect_groups"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="announce">${this._iconSvg("announcement")}<span>${this._esc(this._i18n("ui.announcement"))}</span><small>${this._esc(this._i18n("ui.speak_to_target_players"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="timers">${this._iconSvg("timer")}<span>${this._esc(this._i18n("ui.timers"))}</span><small>${this._esc(this._i18n("ui.sleep_timer_and_scheduled_playback"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="open_ma">${this._iconSvg("library_music")}<span>${this._esc(this._i18n("ui.open_ma"))}</span><small>${this._esc(this._i18n("ui.open_the_full_music_assistant_interface_2"))}</small></button>
              <button class="control-room-hub-card" data-room-selection-action="pro">${this._iconSvg("settings")}<span>${this._esc(this._i18n("ui.pro_tools"))}</span><small>${this._esc(this._i18n("ui.sendspin_and_diagnostics"))}</small></button>
            </div>
          </div>
        `;
      }
      if (panel === "transfer") {
        const source = this._state.controlRoomTransferSource || "";
        const target = this._state.controlRoomTransferTarget || "";
        const sourcePlayer = this._playerByEntityId(source);
        const targetPlayer = this._playerByEntityId(target);
        const sourceName = sourcePlayer?.attributes?.friendly_name || source || this._i18n("ui.choose_source");
        const targetName = targetPlayer?.attributes?.friendly_name || target || this._i18n("ui.choose_target");
        const targetPlayers = players.filter((player) => player.entity_id !== source);
        const transferChoiceRows = (role, options, selectedId) => `
          <div class="control-room-transfer-list" data-control-room-scroll="transfer-${this._esc(role)}">
            ${options.length ? options.map((player) => {
          const art = this._playerArtworkUrl(player, 120);
          const isActive = player.entity_id === selectedId;
          return `
                <button class="control-room-transfer-choice ${isActive ? "active" : ""}" data-room-transfer-${role}="${this._esc(player.entity_id)}">
                  <span class="control-room-transfer-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("speaker")}</span>
                  <span class="control-room-transfer-copy">
                    <span class="control-room-transfer-title">${this._esc(player.attributes?.friendly_name || player.entity_id)}</span>
                    <span class="control-room-transfer-sub">${this._esc(player.attributes?.media_title || this._playerStateLabel(player))}</span>
                  </span>
                  <span class="control-room-transfer-check">${isActive ? this._iconSvg("check") : ""}</span>
                </button>
              `;
        }).join("") : `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.no_available_players"))}</div>`}
          </div>
        `;
        return `
          <div class="control-room-tray open transfer-panel">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.queue_cockpit_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.transfer_clone_inspect_or_clear_queues_without_hiding_the_studio"))}</div>
            </div>
            <div class="control-room-queue-layout">
              <div class="control-room-queue-lanes">
                <section class="control-room-queue-lane source">
                  <div class="control-room-queue-lane-head">
                    <span>${this._esc(this._i18n("ui.from"))}</span>
                    <strong>${this._esc(sourceName)}</strong>
                  </div>
                  ${transferChoiceRows("source", players, source)}
                  <div class="control-room-transfer-label">${this._esc(this._i18n("ui.source_queue"))}</div>
                  ${source ? this._controlRoomQueuePreviewHtml(source) : `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.choose_a_source_player"))}</div>`}
                </section>
                <section class="control-room-queue-lane target">
                  <div class="control-room-queue-lane-head">
                    <span>${this._esc(this._i18n("ui.to"))}</span>
                    <strong>${this._esc(targetName)}</strong>
                  </div>
                  ${transferChoiceRows("target", targetPlayers, target)}
                  <div class="control-room-transfer-label">${this._esc(this._i18n("ui.target_queue"))}</div>
                  ${target ? this._controlRoomQueuePreviewHtml(target) : `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.choose_a_target_player"))}</div>`}
                </section>
              </div>
              <div class="control-room-queue-actions">
                <button class="control-room-panel-action primary" data-room-transfer ${source && target ? "" : "disabled"}>${this._iconSvg("queue")}<span>${this._esc(this._i18n("ui.transfer_queue_2"))}</span></button>
                <button class="control-room-panel-action" data-room-clone ${source && target ? "" : "disabled"}>${this._iconSvg("repeat")}<span>${this._esc(this._i18n("ui.clone_queue"))}</span></button>
                <button class="control-room-panel-action" data-room-refresh-queues>${this._iconSvg("sync")}<span>${this._esc(this._i18n("ui.refresh"))}</span></button>
                <button class="control-room-panel-action danger" data-room-clear-queue="${this._esc(target || source || "")}" ${source || target ? "" : "disabled"}>${this._iconSvg("trash")}<span>${this._esc(this._i18n("ui.clear_queue"))}</span></button>
              </div>
            </div>
          </div>
        `;
      }
      if (panel === "library") {
        return `
          <div class="control-room-tray open wide">
            <label class="control-room-search">
              ${this._iconSvg("search")}
              <input id="controlRoomLibraryInput" type="search" placeholder="${this._esc(this._i18n("ui.search_the_library"))}" value="${this._esc(this._state.controlRoomLibraryQuery || "")}" autocomplete="off" spellcheck="false">
              <button type="button" class="control-room-search-mic" data-room-library-mic title="${this._esc(this._i18n("ui.voice_search"))}">
                ${this._iconSvg("mic")}
              </button>
            </label>
            <div class="control-room-library-results" id="controlRoomLibraryResults" data-control-room-scroll="library">${this._controlRoomLibraryResultsHtml()}</div>
          </div>
        `;
      }
      if (panel === "mix") {
        return `
          <div class="control-room-tray open wide">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.smart_mix_builder"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.pick_a_mood_or_type_your_own_style_studio_will_search_music_assistant_an"))}</div>
            </div>
            <div class="control-room-mix-panel">
              <label class="control-room-search">
                ${this._iconSvg("wand")}
                <input id="controlRoomSmartQueryInput" type="search" placeholder="${this._esc(this._i18n("ui.free_style_quiet_jazz_greek_music_workout"))}" value="${this._esc(this._state.controlRoomSmartQuery || "")}" autocomplete="off" spellcheck="false">
                <button type="button" class="control-room-search-mic" data-room-smart-custom title="${this._esc(this._i18n("ui.build_custom_mix"))}">${this._iconSvg("play")}</button>
              </label>
              <div class="control-room-mix-grid">
                ${this._controlRoomMixPresets().map((preset) => `
                  <button class="control-room-mix-card" data-room-smart-mix="${this._esc(preset.id)}">
                    <span class="control-room-mix-icon">${this._iconSvg(preset.icon || "music_note")}</span>
                    <span class="control-room-mix-title">${this._esc(preset.label)}</span>
                    <span class="control-room-mix-sub">${this._esc(preset.subtitle || "")}</span>
                  </button>
                `).join("")}
              </div>
            </div>
          </div>
        `;
      }
      if (panel === "recent") {
        const loading = !!this._state.controlRoomRecentLoading;
        const items = Array.isArray(this._state.controlRoomRecentItems) ? this._state.controlRoomRecentItems : [];
        return `
          <div class="control-room-tray open wide">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.recent_continue"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.continue_from_recent_music_assistant_activity"))}</div>
            </div>
            <div class="control-room-library-results" data-control-room-scroll="recent">
              ${loading ? `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.loading_recent_items"))}</div>` : this._controlRoomMediaGridHtml(items, { empty: this._i18n("ui.no_recent_listening_yet"), large: true })}
            </div>
          </div>
        `;
      }
      if (panel === "favorites") {
        const loading = !!this._state.controlRoomFavoritesLoading;
        const items = Array.isArray(this._state.controlRoomFavoritesItems) ? this._state.controlRoomFavoritesItems : [];
        return `
          <div class="control-room-tray open wide">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.favorite_center_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.play_queue_radio_or_remove_favorites_directly_from_studio"))}</div>
            </div>
            <div class="control-room-library-results" data-control-room-scroll="favorites">
              ${loading ? `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.loading_favorites"))}</div>` : this._controlRoomMediaGridHtml(items, { empty: this._i18n("ui.no_favorites_found"), large: true })}
            </div>
          </div>
        `;
      }
      if (panel === "scenes") {
        const customScenes = this._controlRoomCustomScenes();
        return `
          <div class="control-room-tray open wide">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.scene_presets_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.one_tap_prepares_players_grouping_volume_and_content_save_your_own_curre"))}</div>
            </div>
            <div class="control-room-scenes-grid">
              <button class="control-room-scene-card" data-room-scene="home">${this._iconSvg("home")}<span>${this._esc(this._i18n("ui.home"))}</span><small>${this._esc(this._i18n("ui.selected_players_at_comfortable_volume"))}</small></button>
              <button class="control-room-scene-card" data-room-scene="party">${this._iconSvg("radio")}<span>${this._esc(this._i18n("ui.party"))}</span><small>${this._esc(this._i18n("ui.group_volume_up_energetic_mix"))}</small></button>
              <button class="control-room-scene-card" data-room-scene="night">${this._iconSvg("moon")}<span>${this._esc(this._i18n("ui.night"))}</span><small>${this._esc(this._i18n("ui.low_volume_and_quiet_mix"))}</small></button>
            </div>
            <div class="control-room-scene-save">
              <label class="control-room-search">
                ${this._iconSvg("home")}
                <input id="controlRoomSceneNameInput" type="text" placeholder="${this._esc(this._i18n("ui.name_this_studio_scene"))}" value="${this._esc(this._state.controlRoomSceneName || "")}" autocomplete="off" spellcheck="false">
              </label>
              <button class="control-room-panel-action primary" data-room-save-scene>${this._iconSvg("plus")}<span>${this._esc(this._i18n("ui.save_current_target"))}</span></button>
            </div>
            <div class="control-room-saved-scenes" data-control-room-scroll="saved-scenes">
              ${customScenes.length ? customScenes.map((scene) => {
          const count = scene.playerIds.length;
          const mediaName = scene.media?.name || this._i18n("ui.volume_and_player_target");
          return `
                  <article class="control-room-saved-scene-card">
                    <button class="control-room-saved-scene-main" data-room-scene="${this._esc(scene.id)}">
                      ${this._iconSvg(count > 1 ? "speaker" : "home")}
                      <span>
                        <strong>${this._esc(scene.name)}</strong>
                        <small>${this._esc(`${this._controlRoomPlayerCountLabel(count)} · ${mediaName}`)}</small>
                      </span>
                    </button>
                    <button class="control-room-saved-scene-delete" data-room-delete-scene="${this._esc(scene.id)}" title="${this._esc(this._i18n("ui.delete_scene"))}">${this._iconSvg("trash")}</button>
                  </article>
                `;
        }).join("") : `<div class="control-room-empty subtle">${this._esc(this._i18n("ui.no_saved_studio_scenes_yet"))}</div>`}
            </div>
          </div>
        `;
      }
      if (panel === "announce") {
        const volume = Math.max(20, Math.min(50, Number(this._state.controlRoomAnnouncementVolume || 20) || 20));
        return `
          <div class="control-room-tray open compact control-room-announcement-tray">
            <div class="control-room-announce-hero">
              <span class="control-room-announce-icon">${this._iconSvg("announcement")}</span>
              <span class="control-room-announce-copy">
                <span class="control-room-tray-title">${this._esc(this._i18n("ui.announcement_studio_2"))}</span>
                <span class="control-room-tray-sub">${this._esc(this._i18n("ui.send_a_short_voice_message_or_announcement_url"))}</span>
              </span>
            </div>
            ${context}
            <div class="control-room-announce-panel">
              <label class="control-room-announce-compose">
                <span>${this._esc(this._i18n("ui.message"))}</span>
                <textarea id="controlRoomAnnouncementText" class="announcement-textarea" rows="3" placeholder="${this._esc(this._i18n("ui.type_what_should_be_announced"))}">${this._esc(this._state.controlRoomAnnouncementText || "")}</textarea>
              </label>
              <div class="control-room-announce-controls">
                <div class="control-room-announce-volume-card announcement-volume-field">
                  <div class="control-room-announce-volume-head">
                    <span>${this._esc(this._i18n("ui.volume_boost"))}</span>
                    <strong class="settings-value">+${this._esc(String(volume))}%</strong>
                  </div>
                  <input id="controlRoomAnnouncementVolumeInput" type="range" min="20" max="50" step="1" value="${this._esc(String(volume))}">
                </div>
                <button class="control-room-panel-action primary wide control-room-announce-send" data-room-announce-send>
                  ${this._iconSvg("announcement")}
                  <span>${this._esc(this._i18n("ui.send_announcement"))}</span>
                </button>
              </div>
            </div>
          </div>
        `;
      }
      if (panel === "pro") {
        const directReady = this._hasDirectMAConnection();
        const realtimeReady = this._hasRealtimeDirectMA();
        const sendspinState = this._localSendspinConnected ? this._i18n("ui.connected") : this._localSendspinConnecting ? this._i18n("ui.connecting") : this._i18n("ui.idle_2");
        const primary = this._controlRoomPrimaryPlayer();
        const protocol = primary ? this._controlRoomProtocolLabel(primary) : "";
        return `
          <div class="control-room-tray open compact">
            <div class="control-room-tray-head">
              <div class="control-room-tray-title">${this._esc(this._i18n("ui.studio_pro_2"))}</div>
              <div class="control-room-tray-sub">${this._esc(this._i18n("ui.feature_detection_player_context_and_this_device_playback_tools"))}</div>
            </div>
            <div class="control-room-diagnostics">
              <div class="control-room-diagnostic-row"><span>Target player</span><strong>${this._esc(primary?.attributes?.friendly_name || this._i18n("ui.none"))}</strong></div>
              <div class="control-room-diagnostic-row"><span>Protocol</span><strong>${this._esc(protocol || this._i18n("ui.unknown"))}</strong></div>
              <div class="control-room-diagnostic-row"><span>Direct API</span><strong>${this._esc(directReady ? this._i18n("ui.available") : this._i18n("ui.missing_url"))}</strong></div>
              <div class="control-room-diagnostic-row"><span>Realtime token</span><strong>${this._esc(realtimeReady ? this._i18n("ui.ready") : this._i18n("ui.optional"))}</strong></div>
              <div class="control-room-diagnostic-row"><span>Sendspin</span><strong>${this._esc(sendspinState)}</strong></div>
              <div class="control-room-diagnostic-row"><span>Players</span><strong>${this._esc(String(players.length))}</strong></div>
              <div class="control-room-pro-actions">
                <button class="control-room-panel-action" data-room-this-device="connect">${this._iconSvg("speaker")}<span>${this._esc(this._i18n("ui.connect_this_device_2"))}</span></button>
                <button class="control-room-panel-action danger" data-room-this-device="disconnect">${this._iconSvg("close")}<span>${this._esc(this._i18n("ui.disconnect"))}</span></button>
                <button class="control-room-panel-action" data-room-selection-action="open_ma">${this._iconSvg("library_music")}<span>${this._esc(this._i18n("ui.open_music_assistant"))}</span></button>
              </div>
              <div class="control-room-empty subtle">${this._esc(this._i18n("ui.player_settings_and_dsp_presets_stay_read_only_until_music_assistant_exp"))}</div>
            </div>
          </div>
        `;
      }
      return ``;
    }
    _controlRoomViewportSize() {
      const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
      const viewportHeight = this._getViewportHeight(this._config?.height || 0);
      return {
        width: Math.max(320, Math.round(this._getCardWidth(this._lastCardWidth || viewportWidth || 1600))),
        height: Math.max(280, Math.round(this._getAllocatedCardHeight(this._lastCardHeight || this._config?.height || viewportHeight || 760)))
      };
    }
    _controlRoomGridStyle(playerCount = 0) {
      const count = Math.max(1, Number(playerCount) || 0);
      const { width, height } = this._controlRoomViewportSize();
      const compactViewport = width <= 1280;
      const narrowViewport = width <= 980;
      const shortViewport = height <= 640;
      const gap = narrowViewport ? 10 : compactViewport ? 12 : 16;
      const minTileWidth = narrowViewport ? 78 : compactViewport ? 86 : 96;
      const preferredMaxCols = width >= 1560 ? 6 : width >= 1060 ? 5 : width >= 720 ? 5 : 3;
      const dockReserve = shortViewport ? 82 : narrowViewport ? 92 : compactViewport ? 104 : 116;
      const headReserve = shortViewport ? 54 : narrowViewport ? 64 : 74;
      const verticalReserve = dockReserve + headReserve + (shortViewport ? 10 : 18);
      const availableWidth = Math.max(220, width - (narrowViewport ? 20 : compactViewport ? 34 : 56));
      const availableHeight = Math.max(180, height - verticalReserve);
      const maxColsByWidth = Math.max(1, Math.floor((availableWidth + gap) / (minTileWidth + gap)));
      const maxCols = Math.max(1, Math.min(count, Math.max(preferredMaxCols, maxColsByWidth)));
      let best = { cols: 1, tileWidth: Math.min(availableWidth, availableHeight * 16 / 9), rows: count, score: 0 };
      for (let cols = 1; cols <= maxCols; cols += 1) {
        const rows = Math.ceil(count / cols);
        const widthLimited = (availableWidth - gap * (cols - 1)) / cols;
        const heightLimited = (availableHeight - gap * (rows - 1)) / rows * 16 / 9;
        const tileWidth = Math.max(minTileWidth, Math.min(widthLimited, heightLimited));
        const score = tileWidth * tileWidth * cols - rows * 22 + cols * 6;
        if (score > best.score) best = { cols, tileWidth, rows, score };
      }
      const gridMaxWidth = Math.max(240, Math.floor(best.tileWidth * best.cols + gap * (best.cols - 1)));
      const gridMaxHeight = Math.max(160, Math.floor(best.tileWidth * 9 / 16 * best.rows + gap * (best.rows - 1)));
      const tileScale = Math.max(0.72, Math.min(1, best.tileWidth / 300));
      return [
        `--control-room-cols:${best.cols}`,
        `--control-room-rows:${best.rows}`,
        `--control-room-gap:${gap}px`,
        `--control-room-player-count:${count}`,
        `--control-room-grid-max-width:${gridMaxWidth}px`,
        `--control-room-grid-max-height:${gridMaxHeight}px`,
        `--control-room-grid-available-height:${availableHeight}px`,
        `--control-room-dock-reserve:${dockReserve}px`,
        `--control-room-head-reserve:${headReserve}px`,
        `--control-room-viewport-width:${width}px`,
        `--control-room-viewport-height:${height}px`,
        `--control-room-tile-scale:${tileScale.toFixed(3)}`
      ].join(";");
    }
    _controlRoomRenderSignature() {
      const players = this._controlRoomPlayers().map((player) => ({
        id: player.entity_id,
        groupCount: this._controlRoomGroupInfo(player).count
      }));
      const results = Array.isArray(this._state.controlRoomLibraryResults) ? this._state.controlRoomLibraryResults.slice(0, 10).map((entry) => ({
        name: entry?.name || "",
        subtitle: entry?.subtitle || "",
        type: entry?.media_type || "",
        image: entry?.image || ""
      })) : [];
      const queueCache = this._state.controlRoomQueueSnapshots || {};
      const queues = Object.fromEntries(Object.entries(queueCache).map(([entityId, entry]) => [
        entityId,
        {
          count: Number(entry?.snapshot?.state?.items || 0) || (Array.isArray(entry?.snapshot?.items) ? entry.snapshot.items.length : 0),
          current: entry?.snapshot?.state?.current_index ?? ""
        }
      ]));
      const viewport = this._controlRoomViewportSize();
      return JSON.stringify({
        open: !!this._state.controlRoomOpen,
        panel: this._state.controlRoomPanel || "",
        viewport,
        selected: this._controlRoomSelectedPlayerIds(),
        visible: this._controlRoomVisiblePlayerIds(),
        query: this._state.controlRoomLibraryQuery || "",
        loading: !!this._state.controlRoomLibraryLoading,
        source: this._state.controlRoomTransferSource || "",
        target: this._state.controlRoomTransferTarget || "",
        results,
        queues,
        queueLoading: !!this._state.controlRoomQueueLoading,
        recentLoading: !!this._state.controlRoomRecentLoading,
        recent: (this._state.controlRoomRecentItems || []).slice(0, 12).map((entry) => entry?.uri || entry?.name || ""),
        favoritesLoading: !!this._state.controlRoomFavoritesLoading,
        favorites: (this._state.controlRoomFavoritesItems || []).slice(0, 12).map((entry) => entry?.uri || entry?.name || ""),
        smartQuery: this._state.controlRoomSmartQuery || "",
        announcementText: this._state.controlRoomAnnouncementText || "",
        announcementVolume: this._state.controlRoomAnnouncementVolume || 20,
        sceneName: this._state.controlRoomSceneName || "",
        customScenes: this._controlRoomCustomScenes().map((scene) => ({
          id: scene.id,
          name: scene.name,
          players: scene.playerIds,
          media: scene.media?.uri || ""
        })),
        players
      });
    }
    _controlRoomHtml() {
      if (!this._controlRoomEnabled()) return "";
      const players = this._controlRoomPlayers();
      const primary = this._controlRoomPrimaryPlayer();
      const primaryArt = this._playerArtworkUrl(primary, 320);
      const roomStyleVars = this._controlRoomGridStyle(players.length);
      const sceneStyle = `style="${primaryArt ? `--control-room-scene-art:url('${this._esc(primaryArt)}');` : ""}${roomStyleVars}"`;
      const focusTarget = this._controlRoomFocusTarget();
      const focusArt = focusTarget.art || primaryArt;
      const targetIds = this._controlRoomActionTargetIds();
      const primaryPlaying = primary?.state === "playing";
      const primaryMuted = primary ? this._isMuted(primary) : false;
      const panelOpen = !!this._state.controlRoomPanel;
      const musicPanelActive = ["music", "mix", "library", "recent", "favorites", "scenes"].includes(this._state.controlRoomPanel);
      const actionsPanelActive = ["actions", "selection", "visible", "announce", "pro"].includes(this._state.controlRoomPanel);
      return `
        <div class="control-room-scene ${primaryArt ? "has-art" : ""} ${panelOpen ? "panel-open" : ""}" ${sceneStyle}>
          <div class="control-room-scene-bg"></div>
          <div class="control-room-scene-glow"></div>
          <div class="control-room-layout">
            <div class="control-room-grid-wrap">
              ${this._controlRoomGroupSummaryHtml(players)}
              <div class="control-room-grid">
                ${players.map((player) => this._controlRoomPlayerTileHtml(player)).join("")}
              </div>
            </div>
            ${this._controlRoomPanelHtml(players)}
            <div class="control-room-dock focus-mode">
              <div class="control-room-player-console">
                <div class="control-room-now-pill focus-target">
                  <span class="control-room-now-art">${focusArt ? `<img src="${this._esc(focusArt)}" alt="">` : this._iconSvg("speaker")}</span>
                  <span class="control-room-now-copy">
                    <span class="control-room-now-kicker">${this._esc(focusTarget.kicker)}</span>
                    <span class="control-room-now-name">${this._esc(focusTarget.name)}</span>
                    <span class="control-room-now-track">${this._esc(focusTarget.track)}</span>
                  </span>
                </div>
                <div class="control-room-dock-section player primary-actions">
                  <button class="control-room-dock-btn" data-room-selection-action="player_playpause" title="${this._esc(this._i18n("ui.play_pause"))}">
                    ${this._iconSvg(primaryPlaying ? "pause" : "play")}
                    <span class="control-room-dock-label">${this._esc(primaryPlaying ? this._i18n("ui.pause") : this._i18n("ui.play"))}</span>
                  </button>
                  <button class="control-room-dock-btn" data-room-selection-action="player_next" title="${this._esc(this._i18n("ui.next"))}">
                    ${this._iconSvg("next")}
                    <span class="control-room-dock-label">${this._esc(this._i18n("ui.next"))}</span>
                  </button>
                  <button class="control-room-dock-btn ${primaryMuted ? "active" : ""}" data-room-selection-action="player_mute" title="${this._esc(this._i18n("ui.mute"))}">
                    ${this._iconSvg(primary ? this._volumeIconName(primary) : "speaker")}
                    <span class="control-room-dock-label">${this._esc(this._i18n("ui.mute"))}</span>
                  </button>
                </div>
              </div>
              <span class="control-room-dock-divider" aria-hidden="true"></span>
              <div class="control-room-dock-section room focus-nav">
                <button class="control-room-selection-pill ${this._state.controlRoomPanel === "selection" ? "active" : ""}" data-room-selection-action="selection" title="${this._esc(this._i18n("ui.connected_players_2"))}">
                  <span class="control-room-selection-count">${this._esc(String(targetIds.length))}</span>
                  <span class="control-room-dock-label">${this._esc(this._i18n("ui.players"))}</span>
                </button>
                <button class="control-room-dock-btn ${musicPanelActive ? "active" : ""}" data-room-selection-action="music" title="${this._esc(this._i18n("ui.music_hub_2"))}">
                  ${this._iconSvg("wand")}
                  <span class="control-room-dock-label">${this._esc(this._i18n("ui.music"))}</span>
                </button>
                <button class="control-room-dock-btn ${this._state.controlRoomPanel === "transfer" ? "active" : ""}" data-room-selection-action="transfer" title="${this._esc(this._i18n("ui.transfer_queue_2"))}">
                  ${this._iconSvg("queue")}
                  <span class="control-room-dock-label">${this._esc(this._i18n("ui.queue_2"))}</span>
                </button>
                <button class="control-room-dock-btn ${actionsPanelActive ? "active" : ""}" data-room-selection-action="actions" title="${this._esc(this._i18n("ui.actions_2"))}">
                  ${this._iconSvg("settings")}
                  <span class="control-room-dock-label">${this._esc(this._i18n("ui.actions_2"))}</span>
                </button>
              </div>
            </div>
          </div>
        </div>
      `;
    }
    _syncControlRoomLiveFields() {
      if (!this._state.controlRoomOpen || !this.shadowRoot) return;
      const host = this.$("controlRoomBody");
      if (!host) return;
      const players = this._controlRoomPlayers();
      const playerMap = new Map(players.map((player) => [player.entity_id, player]));
      const selectedIds = this._controlRoomSelectedPlayerIds();
      const primaryId = this._controlRoomPrimaryPlayerId();
      const primary = this._controlRoomPrimaryPlayer();
      const setText = (el, text) => {
        if (el && el.textContent !== String(text ?? "")) el.textContent = String(text ?? "");
      };
      const setHtml = (el, html) => {
        if (el && el.dataset.liveHtml !== html) {
          el.innerHTML = html;
          el.dataset.liveHtml = html;
        }
      };
      const cssUrl = (url) => `url("${String(url || "").replace(/\\/g, "\\\\").replace(/"/g, '\\"')}")`;
      host.querySelectorAll("[data-room-tile]").forEach((tile) => {
        const entityId = tile.dataset.roomTile || "";
        const player = playerMap.get(entityId);
        if (!player) return;
        const art = this._playerArtworkUrl(player, 320);
        const playing = player.state === "playing";
        const isSelected = selectedIds.includes(entityId);
        const isPrimary = primaryId === entityId;
        const volume = Math.round((player.attributes?.volume_level || 0) * 100);
        const groupInfo = this._controlRoomGroupInfo(player);
        tile.classList.toggle("has-art", !!art);
        tile.classList.toggle("no-art", !art);
        tile.classList.toggle("is-playing", playing);
        tile.classList.toggle("selected", isSelected);
        tile.classList.toggle("primary", isPrimary);
        tile.classList.toggle("grouped", !!groupInfo.count);
        const selectFab = tile.querySelector("[data-room-select]");
        if (selectFab) {
          const removable = isSelected && selectedIds.length > 1;
          selectFab.classList.toggle("active", isSelected);
          selectFab.classList.toggle("removable", removable);
          selectFab.title = removable ? this._i18n("ui.remove_from_selection") : isSelected ? this._i18n("ui.selected_player_2") : this._i18n("ui.add_to_selection");
          setHtml(selectFab, `${this._iconSvg(removable ? "close" : isSelected ? "check" : "grid")}<span class="control-room-select-label">${this._esc(removable ? this._i18n("ui.remove") : isSelected ? this._i18n("ui.selected") : this._i18n("ui.select"))}</span>`);
        }
        if (art) tile.style.setProperty("--control-room-tile-art", cssUrl(art));
        else tile.style.removeProperty("--control-room-tile-art");
        const pills = tile.querySelector(".control-room-tile-pills");
        const groupCount = groupInfo.count;
        const snapshot = this._controlRoomQueueCache(entityId);
        const queueCount = this._controlRoomQueueCount(player, snapshot);
        const protocolLabel = this._controlRoomProtocolLabel(player);
        const pillsHtml = [
          isPrimary ? `<span class="control-room-primary-pill">${this._esc(this._i18n("ui.primary"))}</span>` : ``,
          groupCount ? `<span class="control-room-float-pill grouped" title="${this._esc(groupInfo.label || this._i18n("ui.grouped_players"))}">${this._iconSvg("speaker")}${this._esc(this._m(`${groupCount} grouped`, `${groupCount} בקבוצה`))}</span>` : ``,
          queueCount ? `<span class="control-room-float-pill">${this._iconSvg("queue")}${this._esc(String(queueCount))}</span>` : ``,
          protocolLabel ? `<span class="control-room-float-pill protocol">${this._esc(protocolLabel)}</span>` : ``,
          playing ? `<span class="control-room-float-pill live">${this._esc(this._i18n("ui.playing"))}</span>` : ``
        ].filter(Boolean).join("");
        setHtml(pills, pillsHtml);
        setText(tile.querySelector(".control-room-tile-track"), player.attributes?.media_title || this._i18n("ui.idle_2"));
        setText(tile.querySelector(".control-room-tile-name"), player.attributes?.friendly_name || player.entity_id);
        setText(tile.querySelector(".control-room-tile-state"), this._playerStateLabel(player));
        setHtml(tile.querySelector("[data-room-toggle-play]"), this._iconSvg(playing ? "pause" : "play"));
        const tileMute = tile.querySelector("[data-room-mute]");
        if (tileMute) {
          const muted = this._isMuted(player);
          tileMute.classList.toggle("active", muted);
          setHtml(tileMute, this._iconSvg(muted ? "volume_mute" : this._volumeIconName(player)));
        }
        const input = tile.querySelector(".control-room-volume");
        if (input && this.shadowRoot.activeElement !== input && String(input.value) !== String(volume)) {
          input.value = String(volume);
          input.style.setProperty("--vol-pct", `${volume}%`);
        }
        setText(tile.querySelector("[data-room-volume-value]"), `${volume}%`);
      });
      const primaryArt = this._playerArtworkUrl(primary, 320);
      const scene = host.querySelector(".control-room-scene");
      if (scene) {
        scene.classList.toggle("has-art", !!primaryArt);
        if (primaryArt) scene.style.setProperty("--control-room-scene-art", cssUrl(primaryArt));
        else scene.style.removeProperty("--control-room-scene-art");
      }
      const focusTarget = this._controlRoomFocusTarget();
      const focusArt = focusTarget.art || primaryArt;
      setHtml(host.querySelector(".control-room-now-art"), focusArt ? `<img src="${this._esc(focusArt)}" alt="">` : this._iconSvg("speaker"));
      setText(host.querySelector(".control-room-now-kicker"), focusTarget.kicker);
      setText(host.querySelector(".control-room-now-name"), focusTarget.name);
      setText(host.querySelector(".control-room-now-track"), focusTarget.track);
      const playPauseBtn = host.querySelector('[data-room-selection-action="player_playpause"]');
      if (playPauseBtn) {
        const primaryPlaying = primary?.state === "playing";
        setHtml(playPauseBtn, `${this._iconSvg(primaryPlaying ? "pause" : "play")}<span class="control-room-dock-label">${this._esc(primaryPlaying ? this._i18n("ui.pause") : this._i18n("ui.play"))}</span>`);
      }
      const actionPlayPauseBtn = host.querySelector('.control-room-action-console [data-room-selection-action="playpause"]');
      if (actionPlayPauseBtn) {
        const primaryPlaying = primary?.state === "playing";
        setHtml(actionPlayPauseBtn, `${this._iconSvg(primaryPlaying ? "pause" : "play")}<span>${this._esc(primaryPlaying ? this._i18n("ui.pause") : this._i18n("ui.play"))}</span>`);
      }
      const muteBtn = host.querySelector('[data-room-selection-action="player_mute"]');
      if (muteBtn) {
        const muted = primary ? this._isMuted(primary) : false;
        muteBtn.classList.toggle("active", muted);
        setHtml(muteBtn, `${this._iconSvg(primary ? this._volumeIconName(primary) : "speaker")}<span class="control-room-dock-label">${this._esc(this._i18n("ui.mute"))}</span>`);
      }
      const actionMuteBtn = host.querySelector('.control-room-action-console [data-room-selection-action="mute"]');
      if (actionMuteBtn) {
        const muted = primary ? this._isMuted(primary) : false;
        actionMuteBtn.classList.toggle("active", muted);
        setHtml(actionMuteBtn, `${this._iconSvg(muted ? "volume_mute" : primary ? this._volumeIconName(primary) : "speaker")}<span>${this._esc(this._i18n("ui.mute"))}</span>`);
      }
    }
    _syncControlRoomUi(options = {}) {
      this._syncControlRoomChrome();
      if (!this._controlRoomEnabled()) return;
      const host = this.$("controlRoomBody");
      if (!host) return;
      const force = !!options.force;
      if (!this._state.controlRoomOpen && !force) return;
      const activeEl = this.shadowRoot?.activeElement;
      const restorableInputIds = /* @__PURE__ */ new Set(["controlRoomLibraryInput", "controlRoomSmartQueryInput", "controlRoomAnnouncementText", "controlRoomSceneNameInput"]);
      const activeControlRoomInputId = restorableInputIds.has(activeEl?.id) ? activeEl.id : "";
      const selectionStart = activeControlRoomInputId ? activeEl.selectionStart : null;
      const selectionEnd = activeControlRoomInputId ? activeEl.selectionEnd : null;
      const nextSignature = this._controlRoomRenderSignature();
      const needsRender = force || this._state.controlRoomRenderSignature !== nextSignature || !host.firstElementChild;
      if (needsRender) {
        const nextHtml = this._controlRoomHtml();
        const scrollSnapshot = {};
        host.querySelectorAll?.("[data-control-room-scroll]")?.forEach((el) => {
          const key = el.dataset.controlRoomScroll || "";
          if (key) scrollSnapshot[key] = { top: el.scrollTop || 0, left: el.scrollLeft || 0 };
        });
        const restoreScroll = () => {
          host.querySelectorAll?.("[data-control-room-scroll]")?.forEach((el) => {
            const key = el.dataset.controlRoomScroll || "";
            const pos = scrollSnapshot[key];
            if (!pos) return;
            el.scrollTop = pos.top;
            el.scrollLeft = pos.left;
          });
        };
        if (force || this._state.controlRoomRenderedHtml !== nextHtml || !host.firstElementChild) {
          host.innerHTML = nextHtml;
          this._state.controlRoomRenderedHtml = nextHtml;
          restoreScroll();
          requestAnimationFrame(() => restoreScroll());
        }
        this._state.controlRoomRenderSignature = nextSignature;
      }
      this._syncControlRoomLiveFields();
      const input = this.$("controlRoomLibraryInput");
      if (input) {
        input.value = this._state.controlRoomLibraryQuery || "";
      }
      const smartInput = this.$("controlRoomSmartQueryInput");
      if (smartInput) smartInput.value = this._state.controlRoomSmartQuery || "";
      const announceInput = this.$("controlRoomAnnouncementText");
      if (announceInput) announceInput.value = this._state.controlRoomAnnouncementText || "";
      const sceneNameInput = this.$("controlRoomSceneNameInput");
      if (sceneNameInput) sceneNameInput.value = this._state.controlRoomSceneName || "";
      if (activeControlRoomInputId) {
        const targetInput = this.$(activeControlRoomInputId);
        targetInput?.focus?.({ preventScroll: true });
        if (targetInput && typeof selectionStart === "number" && typeof selectionEnd === "number") {
          try {
            targetInput.setSelectionRange(selectionStart, selectionEnd);
          } catch (_) {
          }
        }
      }
    }
    _syncSourceBadgesUi(player = this._getSelectedPlayer(), queueItem = this._state.maQueueState?.current_item || null) {
      if (this._isHotelMode()) {
        this.shadowRoot?.querySelectorAll("[data-art-source-badges]")?.forEach((host) => {
          host.innerHTML = "";
          host.dataset.renderedBadgesHtml = "";
          host.hidden = true;
        });
        return;
      }
      const meta = player ? this._currentSourceBadgeMeta(player, queueItem) : null;
      this.shadowRoot?.querySelectorAll("[data-art-source-badges]")?.forEach((host) => {
        if (!meta?.providerLabel && !meta?.qualityLabel) {
          if (host.dataset.renderedBadgesHtml !== "") {
            host.innerHTML = "";
            host.dataset.renderedBadgesHtml = "";
          }
          host.hidden = true;
          return;
        }
        const nextHtml = [
          meta.providerLabel ? `<span class="source-badge provider ${this._esc(`provider-${meta.providerKey || "source"}`)}">${this._esc(meta.providerLabel)}</span>` : ``,
          meta.qualityLabel ? `<span class="source-badge quality">${this._esc(meta.qualityLabel)}</span>` : ``
        ].filter(Boolean).join("");
        if (host.dataset.renderedBadgesHtml !== nextHtml) {
          host.innerHTML = nextHtml;
          host.dataset.renderedBadgesHtml = nextHtml;
        }
        host.hidden = false;
      });
    }
    _stripLyricsTimestamps(text = "") {
      return HomeiiMediaPresentationFoundation2.stripLyricsTimestamps(text);
    }
    _coerceLyricsRawText(value) {
      return HomeiiMediaPresentationFoundation2.coerceLyricsRawText(value);
    }
    _coerceLyricsText(value) {
      return HomeiiMediaPresentationFoundation2.coerceLyricsText(value);
    }
    _parseLrcLyrics(text = "") {
      return HomeiiMediaPresentationFoundation2.parseLrcLyrics(text);
    }
    _extractCurrentLyricsRawText() {
      return HomeiiMediaPresentationFoundation2.extractCurrentLyricsRawText(this._state.maQueueState?.current_item || {});
    }
    _extractCurrentLyricsText() {
      return HomeiiMediaPresentationFoundation2.extractCurrentLyricsText(this._state.maQueueState?.current_item || {});
    }
    async _fetchLyricsForCurrentTrack() {
      const info = this._currentTrackInfo();
      if (!info.title) return { text: "", source: "" };
      const cacheKey = info.key || info.title;
      const cached = this._cache.lyrics.get(cacheKey);
      if (cached) return cached;
      const embeddedRaw = this._extractCurrentLyricsRawText();
      if (embeddedRaw) {
        const payload2 = {
          text: this._stripLyricsTimestamps(embeddedRaw),
          rawText: embeddedRaw,
          lrc: this._parseLrcLyrics(embeddedRaw),
          source: "metadata"
        };
        this._cache.lyrics.set(cacheKey, payload2);
        return payload2;
      }
      const params = new URLSearchParams();
      params.set("track_name", info.title);
      if (info.artist) params.set("artist_name", info.artist);
      if (info.album) params.set("album_name", info.album);
      if (info.duration) params.set("duration", String(Math.round(info.duration)));
      const parseLyrics = async (url) => {
        const response = await fetch(url, { headers: { Accept: "application/json" } });
        if (!response.ok) return "";
        const data = await response.json();
        if (Array.isArray(data)) {
          return data.map((item) => this._coerceLyricsRawText(item)).find(Boolean) || "";
        }
        return this._coerceLyricsRawText(data);
      };
      const rawText = await parseLyrics(`https://lrclib.net/api/get?${params.toString()}`) || await parseLyrics(`https://lrclib.net/api/search?${params.toString()}`);
      const payload = {
        text: rawText ? this._stripLyricsTimestamps(rawText) : "",
        rawText: rawText || "",
        lrc: this._parseLrcLyrics(rawText),
        source: rawText ? "lrclib" : ""
      };
      this._cache.lyrics.set(cacheKey, payload);
      return payload;
    }
    _lyricsSessionActive() {
      return !!(this._state.lyricsOpen || this._state.screensaverLyricsOpen);
    }
    _clearLyricsState() {
      this._state.lyricsTrackKey = "";
      this._state.lyricsText = "";
      this._state.lyricsLoading = false;
      this._state.lyricsLines = [];
      this._state.lyricsActiveIndex = -1;
      this._lyricsRequestToken = "";
      this._lyricsRefreshQueued = false;
    }
    _closeLyricsModal(options = {}) {
      const backdrop = this.$("lyricsBackdrop");
      const card = this.shadowRoot?.querySelector(".card");
      const preserveLyrics = options.preserveLyrics === true || this._state.screensaverLyricsOpen === true;
      if (backdrop) {
        backdrop.classList.remove("open");
        backdrop.onclick = null;
        backdrop.innerHTML = "";
      }
      card?.classList.remove("lyrics-modal-open");
      this._state.lyricsOpen = false;
      if (!preserveLyrics) this._clearLyricsState();
      if (options.sync === false) return;
      const restoreFrame = () => {
        const currentCard = this.shadowRoot?.querySelector(".card");
        [currentCard, this.shadowRoot?.querySelector(".stage"), this.shadowRoot?.querySelector(".tablet-shell"), this.shadowRoot?.querySelector(".tablet-main")].filter(Boolean).forEach((el) => {
          try {
            el.scrollTop = 0;
          } catch (_) {
          }
        });
        void currentCard?.offsetHeight;
      };
      requestAnimationFrame(() => {
        this._syncNowPlayingUI();
        this._syncTabletAutoFitUi();
        if (typeof this._refreshMobileArtStack === "function") this._refreshMobileArtStack(true);
        restoreFrame();
        requestAnimationFrame(restoreFrame);
      });
    }
    _renderLyricsModalShell(title, subtitle, bodyHtml) {
      const backdrop = this.$("lyricsBackdrop");
      if (!backdrop) return;
      const offsetLabel = this._lyricsSyncOffsetLabel();
      const lyricsArt = this._currentArtworkUrl(this._getSelectedPlayer(), this._state.maQueueState?.current_item || null, 920, { preferPlayerArtwork: true });
      if (lyricsArt) {
        backdrop.style.setProperty("--lyrics-dynamic-art", `url(${JSON.stringify(lyricsArt)})`);
        backdrop.classList.add("has-lyrics-art");
      } else {
        backdrop.style.removeProperty("--lyrics-dynamic-art");
        backdrop.classList.remove("has-lyrics-art");
      }
      backdrop.innerHTML = `
        <div class="lyrics-sheet" style="--lyrics-font-scale:${this._esc(this._lyricsFontScale().toFixed(2))}">
          <div class="lyrics-head">
            <div class="lyrics-title-wrap">
              <div class="lyrics-title-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("lyrics-title-logo")}</div>
              <div class="lyrics-title">${this._esc(title || this._i18n("ui.track_lyrics"))}</div>
              <div class="lyrics-sub">${this._esc(subtitle || "")}</div>
            </div>
            <div class="lyrics-head-actions">
              <div class="lyrics-font-controls" title="${this._esc(this._i18n("ui.lyrics_font_size"))}">
                <button class="lyrics-offset-btn" id="lyricsFontMinusBtn" title="${this._esc(this._i18n("ui.smaller_lyrics"))}">−</button>
                <button class="lyrics-offset-label" id="lyricsFontResetBtn" title="${this._esc(this._i18n("ui.reset_lyrics_font_size"))}">${this._esc(this._lyricsFontScaleLabel())}</button>
                <button class="lyrics-offset-btn" id="lyricsFontPlusBtn" title="${this._esc(this._i18n("ui.larger_lyrics"))}">+</button>
              </div>
              <div class="lyrics-offset-controls" title="${this._esc(this._i18n("ui.lyrics_timing"))}">
                <button class="lyrics-offset-btn" id="lyricsOffsetMinusBtn" title="${this._esc(this._i18n("ui.lyrics_earlier"))}">−</button>
                <button class="lyrics-offset-label" id="lyricsOffsetResetBtn" title="${this._esc(this._i18n("ui.reset_lyrics_timing"))}">${this._esc(offsetLabel)}</button>
                <button class="lyrics-offset-btn" id="lyricsOffsetPlusBtn" title="${this._esc(this._i18n("ui.lyrics_later"))}">+</button>
              </div>
              <button class="lyrics-sync-btn ${this._state.mobileLyricsSyncEnabled !== false ? "active" : ""}" id="lyricsSyncBtn" title="${this._esc(this._i18n("ui.sync_lyrics"))}">
                ${this._iconSvg("sync")}
                <span>${this._esc(this._i18n("ui.sync"))}</span>
              </button>
              <button class="close-btn" id="lyricsCloseBtn">✕</button>
            </div>
          </div>
          <div class="lyrics-body">${bodyHtml}</div>
        </div>`;
      backdrop.classList.add("open");
      this.shadowRoot?.querySelector(".card")?.classList.add("lyrics-modal-open");
      backdrop.onclick = (e) => {
        if (e.target === backdrop) this._closeLyricsModal();
      };
      backdrop.querySelector("#lyricsCloseBtn")?.addEventListener("click", () => this._closeLyricsModal());
      backdrop.querySelector("#lyricsSyncBtn")?.addEventListener("click", () => this._toggleLyricsSyncEnabled());
      backdrop.querySelector("#lyricsOffsetMinusBtn")?.addEventListener("click", () => this._nudgeLyricsSyncOffset(-500));
      backdrop.querySelector("#lyricsOffsetPlusBtn")?.addEventListener("click", () => this._nudgeLyricsSyncOffset(500));
      backdrop.querySelector("#lyricsOffsetResetBtn")?.addEventListener("click", () => this._setLyricsSyncOffset(0));
      backdrop.querySelector("#lyricsFontMinusBtn")?.addEventListener("click", () => this._nudgeLyricsFontScale(-0.08));
      backdrop.querySelector("#lyricsFontPlusBtn")?.addEventListener("click", () => this._nudgeLyricsFontScale(0.08));
      backdrop.querySelector("#lyricsFontResetBtn")?.addEventListener("click", () => this._setLyricsFontScale(1));
    }
    _lyricsTimelineHtml(lines = []) {
      return `
        <div class="lyrics-timeline" id="lyricsTimeline">
          ${lines.map((line, index) => `
            <div class="lyrics-line" data-lyrics-index="${index}" data-lyrics-time="${Number(line.time) || 0}">
              ${this._esc(line.text || "")}
            </div>
          `).join("")}
        </div>`;
    }
    _currentLyricsActiveIndex(lines = []) {
      const list = Array.isArray(lines) ? lines : [];
      if (!list.length) return -1;
      if (this._state.mobileLyricsSyncEnabled === false) return -1;
      const position = this._getCurrentPosition() + this._lyricsSyncOffsetMs() / 1e3;
      let activeIndex = 0;
      for (let i = 0; i < list.length; i += 1) {
        if (Number(list[i]?.time || 0) <= position + 0.15) activeIndex = i;
        else break;
      }
      return activeIndex;
    }
    _syncLyricsHighlight(force = false) {
      if (!this._state.lyricsOpen) return;
      const lines = Array.isArray(this._state.lyricsLines) ? this._state.lyricsLines : [];
      if (!lines.length) return;
      const timeline = this.shadowRoot?.querySelector("#lyricsTimeline");
      if (!timeline) return;
      if (this._state.mobileLyricsSyncEnabled === false) {
        this._state.lyricsActiveIndex = -1;
        timeline.querySelectorAll(".lyrics-line").forEach((row) => row.classList.remove("active"));
        return;
      }
      const activeIndex = this._currentLyricsActiveIndex(lines);
      if (activeIndex < 0) return;
      if (!force && activeIndex === this._state.lyricsActiveIndex) return;
      this._state.lyricsActiveIndex = activeIndex;
      this._syncScreensaverLyricsUi?.();
      timeline.querySelectorAll(".lyrics-line").forEach((row, index) => {
        row.classList.toggle("active", index === activeIndex);
      });
      const activeRow = timeline.querySelector(`.lyrics-line[data-lyrics-index="${activeIndex}"]`);
      const body = timeline.closest(".lyrics-body");
      if (activeRow && body) {
        const bodyRect = body.getBoundingClientRect();
        const rowRect = activeRow.getBoundingClientRect();
        const targetTop = body.scrollTop + rowRect.top - bodyRect.top - body.clientHeight / 2 + rowRect.height / 2;
        const maxTop = Math.max(0, body.scrollHeight - body.clientHeight);
        const nextTop = Math.max(0, Math.min(maxTop, targetTop));
        try {
          body.scrollTo({ top: nextTop, behavior: force ? "auto" : "smooth" });
        } catch (_) {
          body.scrollTop = nextTop;
        }
      }
    }
    _toggleLyricsSyncEnabled() {
      this._state.mobileLyricsSyncEnabled = this._state.mobileLyricsSyncEnabled === false;
      this._persistMobileAppearance();
      const syncBtn = this.shadowRoot?.querySelector("#lyricsSyncBtn");
      if (syncBtn) syncBtn.classList.toggle("active", this._state.mobileLyricsSyncEnabled !== false);
      this._syncLyricsHighlight(true);
    }
    _lyricsSyncOffsetMs() {
      return Math.max(-1e4, Math.min(1e4, Number(this._state.mobileLyricsSyncOffsetMs || 0) || 0));
    }
    _lyricsSyncOffsetLabel() {
      const seconds = this._lyricsSyncOffsetMs() / 1e3;
      return `${seconds > 0 ? "+" : ""}${seconds.toFixed(1)}s`;
    }
    _setLyricsSyncOffset(offsetMs = 0) {
      this._state.mobileLyricsSyncOffsetMs = Math.max(-1e4, Math.min(1e4, Number(offsetMs || 0) || 0));
      this._persistMobileAppearance();
      const label = this.shadowRoot?.querySelector("#lyricsOffsetResetBtn");
      if (label) label.textContent = this._lyricsSyncOffsetLabel();
      this._syncLyricsHighlight(true);
    }
    _nudgeLyricsSyncOffset(deltaMs = 0) {
      this._setLyricsSyncOffset(this._lyricsSyncOffsetMs() + (Number(deltaMs || 0) || 0));
    }
    _lyricsFontScale() {
      return Math.max(0.75, Math.min(1.4, Number(this._state.mobileLyricsFontScale || 1) || 1));
    }
    _lyricsFontScaleLabel() {
      return `${Math.round(this._lyricsFontScale() * 100)}%`;
    }
    _setLyricsFontScale(value = 1) {
      this._state.mobileLyricsFontScale = Math.max(0.75, Math.min(1.4, Number(value || 1) || 1));
      this._persistMobileAppearance();
      const sheet = this.shadowRoot?.querySelector(".lyrics-sheet");
      if (sheet) sheet.style.setProperty("--lyrics-font-scale", this._lyricsFontScale().toFixed(2));
      const label = this.shadowRoot?.querySelector("#lyricsFontResetBtn");
      if (label) label.textContent = this._lyricsFontScaleLabel();
      this._syncLyricsHighlight(true);
    }
    _nudgeLyricsFontScale(delta = 0) {
      this._setLyricsFontScale(this._lyricsFontScale() + (Number(delta || 0) || 0));
    }
    async _renderLyricsModalForCurrentTrack({ force = false } = {}) {
      if (!this._lyricsSessionActive()) return;
      const info = this._currentTrackInfo();
      const trackKey = this._currentLyricsTrackKey() || info.key || info.title || "";
      if (!force && trackKey && this._state.lyricsTrackKey === trackKey) {
        this._syncLyricsHighlight();
        this._syncScreensaverLyricsUi?.();
        return;
      }
      const subtitle = [info.artist, info.album].filter(Boolean).join(" Â· ");
      this._state.lyricsTrackKey = trackKey;
      const lyricsSubtitle = subtitle.replace(/\u00c3\u201a\u00c2\u00b7|\u00c2\u00b7/g, "·");
      this._state.lyricsText = "";
      this._state.lyricsLines = [];
      this._state.lyricsActiveIndex = -1;
      this._state.lyricsLoading = true;
      const token = `${trackKey || Date.now()}-${Math.random()}`;
      this._lyricsRequestToken = token;
      if (this._state.lyricsOpen) {
        this._renderLyricsModalShell(
          info.title || this._i18n("ui.track_lyrics"),
          lyricsSubtitle,
          `<div class="lyrics-state">${this._esc(this._i18n("ui.loading_lyrics"))}</div>`
        );
      }
      this._syncScreensaverLyricsUi?.();
      try {
        const payload = await this._fetchLyricsForCurrentTrack();
        if (!this._lyricsSessionActive() || this._lyricsRequestToken !== token) return;
        const text = payload?.text || "";
        const lines = Array.isArray(payload?.lrc) ? payload.lrc : [];
        this._state.lyricsText = text;
        this._state.lyricsLoading = false;
        this._state.lyricsLines = lines;
        this._state.lyricsActiveIndex = -1;
        if (this._state.lyricsOpen) {
          this._renderLyricsModalShell(
            info.title || this._i18n("ui.track_lyrics"),
            lyricsSubtitle,
            lines.length ? this._lyricsTimelineHtml(lines) : text ? `<pre class="lyrics-pre">${this._esc(text)}</pre>` : `<div class="lyrics-state">${this._esc(this._i18n("ui.no_lyrics_found"))}</div>`
          );
        }
        this._syncScreensaverLyricsUi?.();
        if (lines.length) requestAnimationFrame(() => this._syncLyricsHighlight(true));
      } catch (_) {
        if (!this._lyricsSessionActive() || this._lyricsRequestToken !== token) return;
        this._state.lyricsText = "";
        this._state.lyricsLoading = false;
        this._state.lyricsLines = [];
        this._state.lyricsActiveIndex = -1;
        if (this._state.lyricsOpen) {
          this._renderLyricsModalShell(
            info.title || this._i18n("ui.track_lyrics"),
            lyricsSubtitle,
            `<div class="lyrics-state">${this._esc(this._i18n("ui.lyrics_unavailable_right_now"))}</div>`
          );
        }
        this._syncScreensaverLyricsUi?.();
      }
    }
    async _openLyricsModal() {
      const backdrop = this.$("lyricsBackdrop");
      if (!backdrop) return;
      this.shadowRoot.querySelector(".card")?.appendChild(backdrop);
      const info = this._currentTrackInfo();
      const trackKey = this._currentLyricsTrackKey() || info.key || info.title || "";
      const subtitle = [info.artist, info.album].filter(Boolean).join(" · ");
      this._state.lyricsOpen = true;
      this._state.lyricsTrackKey = trackKey;
      const lyricsSubtitle = subtitle.replace(/\u00c3\u201a\u00c2\u00b7|\u00c2\u00b7/g, "·");
      this._state.lyricsText = "";
      this._state.lyricsLines = [];
      this._state.lyricsActiveIndex = -1;
      this._state.lyricsLoading = true;
      const token = `${trackKey || Date.now()}-${Math.random()}`;
      this._lyricsRequestToken = token;
      this._renderLyricsModalShell(
        info.title || this._i18n("ui.track_lyrics"),
        lyricsSubtitle,
        `<div class="lyrics-state">${this._esc(this._i18n("ui.loading_lyrics"))}</div>`
      );
      this._syncScreensaverLyricsUi?.();
      try {
        const payload = await this._fetchLyricsForCurrentTrack();
        if (!this._lyricsSessionActive() || this._lyricsRequestToken !== token) return;
        const text = payload?.text || "";
        const lines = Array.isArray(payload?.lrc) ? payload.lrc : [];
        this._state.lyricsText = text;
        this._state.lyricsLoading = false;
        this._state.lyricsLines = lines;
        this._state.lyricsActiveIndex = -1;
        if (this._state.lyricsOpen) {
          this._renderLyricsModalShell(
            info.title || this._i18n("ui.track_lyrics"),
            lyricsSubtitle,
            lines.length ? this._lyricsTimelineHtml(lines) : text ? `<pre class="lyrics-pre">${this._esc(text)}</pre>` : `<div class="lyrics-state">${this._esc(this._i18n("ui.no_lyrics_found"))}</div>`
          );
        }
        this._syncScreensaverLyricsUi?.();
        if (lines.length) requestAnimationFrame(() => this._syncLyricsHighlight(true));
      } catch (_) {
        if (!this._lyricsSessionActive() || this._lyricsRequestToken !== token) return;
        this._state.lyricsText = "";
        this._state.lyricsLoading = false;
        this._state.lyricsLines = [];
        this._state.lyricsActiveIndex = -1;
        if (this._state.lyricsOpen) {
          this._renderLyricsModalShell(
            info.title || this._i18n("ui.track_lyrics"),
            lyricsSubtitle,
            `<div class="lyrics-state">${this._esc(this._i18n("ui.lyrics_unavailable_right_now"))}</div>`
          );
        }
        this._syncScreensaverLyricsUi?.();
      }
    }
    _syncLyricsForCurrentTrack({ force = false } = {}) {
      if (!this._lyricsSessionActive()) return;
      const trackKey = this._currentLyricsTrackKey();
      if (!force && trackKey && this._state.lyricsTrackKey === trackKey) {
        this._syncLyricsHighlight();
        this._syncScreensaverLyricsUi?.();
        return;
      }
      if (this._lyricsRefreshPromise) {
        this._lyricsRefreshQueued = true;
        return;
      }
      this._lyricsRefreshPromise = this._renderLyricsModalForCurrentTrack({ force: true }).catch(() => {
      }).finally(() => {
        this._lyricsRefreshPromise = null;
        if (this._lyricsRefreshQueued && this._lyricsSessionActive()) {
          this._lyricsRefreshQueued = false;
          this._syncLyricsForCurrentTrack({ force: true });
        }
      });
    }
    _likedStorageKey() {
      return "homeii_music_flow_likes_v2";
    }
    _likedMetaStorageKey() {
      return "homeii_music_flow_like_meta_v2";
    }
    _loadLikedUris() {
      if (!this._likedUris) {
        const liked = /* @__PURE__ */ new Set();
        try {
          const raw = JSON.parse(localStorage.getItem(this._likedStorageKey()) || "[]");
          if (Array.isArray(raw)) raw.filter(Boolean).forEach((uri) => liked.add(String(uri)));
        } catch (_) {
        }
        ["homeii_music_flow_likes", "homeii_music_flow_mobile_likes"].forEach((key) => {
          try {
            const raw = JSON.parse(localStorage.getItem(key) || "[]");
            if (Array.isArray(raw)) raw.filter(Boolean).forEach((uri) => liked.add(String(uri)));
          } catch (_) {
          }
        });
        this._likedUris = liked;
        this._saveLikedUris();
      }
      return this._likedUris;
    }
    _loadLikedMetaMap() {
      if (!this._likedMeta) {
        try {
          const raw = JSON.parse(localStorage.getItem(this._likedMetaStorageKey()) || "{}");
          this._likedMeta = raw && typeof raw === "object" && !Array.isArray(raw) ? raw : {};
        } catch (_) {
          this._likedMeta = {};
        }
      }
      return this._likedMeta;
    }
    _saveLikedUris() {
      try {
        localStorage.setItem(this._likedStorageKey(), JSON.stringify(Array.from(this._loadLikedUris())));
      } catch (_) {
      }
    }
    _saveLikedMetaMap() {
      try {
        localStorage.setItem(this._likedMetaStorageKey(), JSON.stringify(this._loadLikedMetaMap()));
      } catch (_) {
      }
    }
    _removeLikedUri(uri) {
      const normalized = String(uri || "").trim();
      if (!normalized) return;
      const likedUris = this._loadLikedUris();
      const likedMeta = this._loadLikedMetaMap();
      likedUris.delete(normalized);
      delete likedMeta[normalized];
      this._saveLikedUris();
      this._saveLikedMetaMap();
      this._syncLikeButtons();
    }
    _getCurrentMediaUri() {
      const queueItem = this._state.maQueueState?.current_item || {};
      const playerUri = String(this._getSelectedPlayer()?.attributes?.media_content_id || "").trim();
      const queueUri = String(queueItem?.media_item?.uri || queueItem?.uri || "").trim();
      return playerUri || this._state.nowPlayingUri || queueUri || "";
    }
    _currentMediaLikeMeta() {
      return HomeiiFavoritesFoundation2.buildCurrentMediaLikeMeta({
        player: this._getSelectedPlayer(),
        queueItem: this._state.maQueueState?.current_item || {},
        resolvedUri: this._getCurrentMediaUri(),
        queueItemImage: this._queueItemImageUrl(this._state.maQueueState?.current_item || {}, 240),
        fallbackName: this._i18n("ui.unknown")
      }, (uri, fallbackType) => this._parseMediaReference(uri, fallbackType));
    }
    _setCurrentMediaFavoriteOverride(liked, entry = null) {
      const uri = String(entry?.uri || this._getCurrentMediaUri() || "").trim();
      if (!uri) return;
      this._state.currentMediaFavoriteOverride = {
        uri,
        liked: !!liked,
        ts: Date.now()
      };
      const currentItem = this._state.maQueueState?.current_item;
      if (currentItem) {
        currentItem.favorite = !!liked;
        if (currentItem.media_item && typeof currentItem.media_item === "object") {
          currentItem.media_item.favorite = !!liked;
        }
      }
    }
    _clearCurrentMediaFavoriteOverride() {
      this._state.currentMediaFavoriteOverride = null;
    }
    _likedEntries() {
      if (this._useMaLikedMode()) {
        const cache = this._cache.library.get("liked:ma");
        return Array.isArray(cache?.items) ? cache.items : [];
      }
      const likedUris = Array.from(this._loadLikedUris());
      const meta = this._loadLikedMetaMap();
      return likedUris.map((uri) => {
        const item = meta?.[uri] || {};
        return {
          uri,
          media_type: item.media_type || "track",
          name: item.name || uri,
          artist: item.artist || "",
          album: item.album || "",
          image: item.image || ""
        };
      });
    }
    async _loadMaLikedEntries(force = false) {
      if (!this._useMaLikedMode()) return [];
      if (!force) {
        const cached = this._cache.library.get("liked:ma");
        if (Array.isArray(cached?.items)) return cached.items;
      }
      const types = [
        ["track", 160],
        ["album", 80],
        ["playlist", 80],
        ["radio", 80],
        ["podcast", 80],
        ["artist", 80]
      ];
      const results = await Promise.allSettled(
        types.map(([type, limit]) => this._fetchLibrary(type, "sort_name", limit, true))
      );
      const items = [];
      results.forEach((result, idx) => {
        if (result.status !== "fulfilled" || !Array.isArray(result.value)) return;
        const mediaType = types[idx][0];
        result.value.forEach((item) => {
          items.push({
            uri: item.uri || "",
            media_type: item.media_type || mediaType,
            item_id: item.item_id || item.id || "",
            provider: item.provider || item.provider_domain || item.provider_instance || "",
            library_item_id: item.library_item_id || "",
            name: item.name || "",
            artist: this._artistName(item) || "",
            album: item.album?.name || "",
            image: this._artUrl(item) || "",
            favorite: true
          });
        });
      });
      this._cache.library.set("liked:ma", { items });
      return items;
    }
    _currentMediaFavoriteState() {
      const cache = this._cache.library.get("liked:ma");
      return HomeiiFavoritesFoundation2.resolveCurrentMediaFavoriteState({
        currentUri: this._getCurrentMediaUri(),
        override: this._state.currentMediaFavoriteOverride || null,
        queueItem: this._state.maQueueState?.current_item || {},
        currentEntry: this._currentMediaLikeMeta(),
        useMaLikedMode: this._useMaLikedMode(),
        likedItems: Array.isArray(cache?.items) ? cache.items : null,
        localLikedUris: this._loadLikedUris(),
        now: Date.now()
      }, {
        compareMediaRefsFn: (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType),
        matchFavoriteLibraryItemFn: (entry, likedItems, fallbackType) => this._matchFavoriteLibraryItem(entry, likedItems, fallbackType)
      });
    }
    _syncLikeButtons() {
      const liked = this._currentMediaFavoriteState();
      ["mobileLikeBtn"].forEach((id) => {
        const btn = this.$(id);
        if (!btn) return;
        btn.classList.toggle("active", liked);
        this._setButtonIcon(btn, liked ? "heart_filled" : "heart_outline");
        if (liked) btn.style.color = "#f5a623";
        else btn.style.removeProperty("color");
      });
    }
    _resolveFavoriteButtonEntity(player = this._getSelectedPlayer()) {
      if (!this._hass?.states || !player) return "";
      const configured = String(this._config.favorite_button_entity || "").trim();
      if (configured && this._hass.states[configured]) return configured;
      const fallback = "button.bathroom_favorite_current_song_2";
      const playerEntity = String(player.entity_id || "").toLowerCase();
      const playerName = String(player.attributes?.friendly_name || "").toLowerCase();
      const playerTokens = [
        ...playerEntity.split(".").pop().split(/[_\s-]+/),
        ...playerName.split(/[_\s-]+/)
      ].filter(Boolean);
      const buttons = Object.keys(this._hass.states).filter(
        (entityId) => entityId.startsWith("button.") && /(favorite|אהב|אהבתי)/i.test(entityId)
      );
      const matched = buttons.find((entityId) => {
        const lower = entityId.toLowerCase();
        return playerTokens.some((token) => token && lower.includes(token));
      });
      if (matched) return matched;
      return this._hass.states[fallback] ? fallback : "";
    }
    async _toggleLikeViaFavoriteButton(sourceEl = null) {
      const entityId = this._resolveFavoriteButtonEntity();
      if (!entityId) return false;
      await this._hass.callService("button", "press", {}, { entity_id: entityId });
      if (sourceEl) this._flashInteraction(sourceEl);
      setTimeout(() => {
        this._cache.library.delete("liked:ma");
        this._syncLikeButtons();
        this._renderMobileMenu();
      }, 500);
      return true;
    }
    async _toggleLikeViaMassQueue() {
      const player = this._getSelectedPlayer();
      const entityId = player?.entity_id || this._config?.entity || this.config?.entity;
      if (this._config?.use_mass_queue_send_command !== true) return false;
      if (!entityId || !this._hass?.services?.mass_queue?.send_command) return false;
      const configEntryId = await this._ensureConfigEntryId();
      const queueId = String(player?.attributes?.active_queue || this._state?.maQueueState?.queue_id || "").trim();
      const currentUri = String(this._getCurrentMediaUri() || "").trim();
      const parsed = this._parseMediaReference(currentUri, this._state?.maQueueState?.current_item?.media_type || "track");
      const attempts = [
        { command: "favorite_current", payload: { entity_id: entityId, queue_id: queueId || void 0 } },
        { command: "favorite_current", payload: { player_id: entityId, queue_id: queueId || void 0 } },
        { command: "toggle_favorite", payload: { entity_id: entityId, uri: currentUri || void 0, media_type: parsed.media_type || void 0 } },
        { command: "toggle_favorite", payload: { player_id: entityId, uri: currentUri || void 0, media_type: parsed.media_type || void 0 } },
        { command: "add_to_library", payload: { entity_id: entityId, uri: currentUri || void 0, media_type: parsed.media_type || void 0, item_id: parsed.item_id || void 0, provider: parsed.provider || void 0 } },
        { command: "add_to_library", payload: { player_id: entityId, uri: currentUri || void 0, media_type: parsed.media_type || void 0, item_id: parsed.item_id || void 0, provider: parsed.provider || void 0 } }
      ];
      for (const attempt of attempts) {
        const payload = Object.fromEntries(Object.entries(attempt.payload).filter(([, value]) => value !== void 0 && value !== ""));
        const variants = [
          { command: attempt.command, ...payload },
          { command: attempt.command, data: payload }
        ].map((entry) => configEntryId ? { config_entry_id: configEntryId, ...entry } : entry);
        for (const data of variants) {
          try {
            await this._hass.callService("mass_queue", "send_command", data);
            return true;
          } catch (_) {
          }
        }
      }
      return false;
    }
    async _toggleLikeCurrentMedia(sourceEl = null) {
      const uri = this._getCurrentMediaUri();
      if (!uri) return;
      if (this._useMaLikedMode()) {
        const entry = this._currentMediaLikeMeta();
        await this._toggleMaLikeEntry(entry, sourceEl);
        return;
      }
      const likedUris = this._loadLikedUris();
      const likedMeta = this._loadLikedMetaMap();
      if (likedUris.has(uri)) {
        likedUris.delete(uri);
        delete likedMeta[uri];
      } else {
        likedUris.add(uri);
        likedMeta[uri] = this._currentMediaLikeMeta();
      }
      this._saveLikedUris();
      this._saveLikedMetaMap();
      if (sourceEl) this._flashInteraction(sourceEl);
      this._syncLikeButtons();
      if (this._state.menuOpen && this._state.menuPage === "library_liked") this._renderMobileMenu();
    }
    async _toggleLikeEntry(entry = {}, sourceEl = null) {
      const uri = String(entry?.uri || "").trim();
      if (!uri) return;
      if (this._useMaLikedMode()) {
        await this._toggleMaLikeEntry(entry, sourceEl);
        return;
      }
      const likedUris = this._loadLikedUris();
      const likedMeta = this._loadLikedMetaMap();
      if (likedUris.has(uri)) {
        likedUris.delete(uri);
        delete likedMeta[uri];
      } else {
        likedUris.add(uri);
        likedMeta[uri] = {
          uri,
          media_type: entry.media_type || "track",
          name: entry.name || uri,
          artist: entry.artist || "",
          album: entry.album || "",
          image: entry.image || ""
        };
      }
      this._saveLikedUris();
      this._saveLikedMetaMap();
      if (sourceEl) this._flashInteraction(sourceEl);
      this._syncLikeButtons();
    }
    async _toggleMaLikeEntry(entry = {}, sourceEl = null) {
      const targetsCurrentMedia = this._entryTargetsCurrentMedia(entry);
      const likedNow = targetsCurrentMedia ? this._currentMediaFavoriteState() : this._isEntryLiked(entry);
      if (targetsCurrentMedia) {
        const player = this._getSelectedPlayer();
        const playerEntityId = String(player?.entity_id || this._state.selectedPlayer || "").trim();
        const fallbackFavoriteEntity = this._favoriteButtonEntityForPlayer(player);
        const uri2 = String(entry?.uri || this._getCurrentMediaUri() || "").trim();
        const mediaType2 = entry.media_type || entry.type || this._parseMediaReference(uri2, entry.media_type || entry.type || "track").media_type || "track";
        const nextLiked = !likedNow;
        this._setCurrentMediaFavoriteOverride(nextLiked, entry);
        this._applyMaFavoriteOptimisticState(entry, nextLiked);
        if (sourceEl) this._flashInteraction(sourceEl);
        entry.favorite = nextLiked;
        if (entry.media_item) entry.media_item.favorite = nextLiked;
        try {
          let actionOk = false;
          if (likedNow) {
            actionOk = await this._unfavoriteCurrentViaMassQueue();
            if (!actionOk) {
              try {
                actionOk = await this._toggleMaLikeEntryDirect(entry, true, mediaType2);
              } catch (_) {
              }
            }
            if (!actionOk) throw new Error("Music Assistant favorite action was not available");
            this._toast(this._i18n("ui.removed_from_music_assistant_liked"));
          } else {
            try {
              actionOk = await this._toggleMaLikeEntryDirect(entry, false, mediaType2);
            } catch (_) {
            }
            if (!actionOk) {
              try {
                actionOk = await this._toggleLikeViaMassQueue();
              } catch (_) {
              }
            }
            if (!actionOk && fallbackFavoriteEntity) {
              await this._pressFavoriteButtonEntity(fallbackFavoriteEntity);
              actionOk = true;
            }
            if (!actionOk && playerEntityId && !this._isDirectMaPlayer(playerEntityId)) {
              await this._pressFavoriteButtonEntity(playerEntityId);
              actionOk = true;
            }
            if (!actionOk) throw new Error("Music Assistant favorite action was not available");
            this._toast(this._i18n("ui.added_to_music_assistant_liked"));
          }
          [140, 520, 1400, 3e3].forEach((delay) => setTimeout(() => this._refreshFavoriteState(true).catch(() => {
          }), delay));
          return true;
        } catch (error) {
          this._clearCurrentMediaFavoriteOverride();
          this._applyMaFavoriteOptimisticState(entry, likedNow);
          entry.favorite = likedNow;
          if (entry.media_item) entry.media_item.favorite = likedNow;
          this._toastError(this._i18n("ui.ma_favorite_action_failed_with_error", {
            error: error?.message ? `: ${error.message}` : ""
          }));
          return false;
        }
      }
      const canonicalLikedEntry = likedNow ? await this._resolveCanonicalMaLikedEntry(entry, true) : null;
      const effectiveEntry = canonicalLikedEntry || entry;
      const uri = String(entry?.uri || "").trim();
      if (!uri) return false;
      const mediaType = entry.media_type || entry.type || this._parseMediaReference(uri, entry.media_type || entry.type || "track").media_type || "track";
      try {
        let verified = false;
        const usedHaFavoriteService = await this._toggleMaLikeViaHaService(effectiveEntry, likedNow, mediaType);
        if (usedHaFavoriteService) {
          this._applyMaFavoriteOptimisticState(entry, !likedNow);
          verified = await this._waitForFavoriteState(effectiveEntry, !likedNow, [400, 1100, 2200, 4200, 5800]);
          if (!verified) this._applyMaFavoriteOptimisticState(entry, !likedNow);
          this._toast(likedNow ? this._i18n("ui.removed_from_music_assistant_liked") : this._i18n("ui.added_to_music_assistant_liked"));
          if (sourceEl) this._flashInteraction(sourceEl);
          entry.favorite = !likedNow;
          if (entry.media_item) entry.media_item.favorite = !likedNow;
          [500, 1500, 3200, 5200].forEach((delay) => setTimeout(() => this._refreshFavoriteState(true).catch(() => {
          }), delay));
          return true;
        }
        const usedDirectApi = await this._toggleMaLikeEntryDirect(effectiveEntry, likedNow, mediaType);
        if (!usedDirectApi && targetsCurrentMedia) {
          const massQueueUsed = await this._toggleLikeViaMassQueue();
          if (massQueueUsed) {
            this._applyMaFavoriteOptimisticState(entry, !likedNow);
            verified = await this._waitForFavoriteState(effectiveEntry, !likedNow, [450, 1200, 2600, 4200, 5800]);
            if (!verified) this._applyMaFavoriteOptimisticState(entry, !likedNow);
            this._toast(likedNow ? this._i18n("ui.removed_from_music_assistant_liked") : this._i18n("ui.added_to_music_assistant_liked"));
            if (sourceEl) this._flashInteraction(sourceEl);
            entry.favorite = !likedNow;
            if (entry.media_item) entry.media_item.favorite = !likedNow;
            [500, 1500, 3200, 5200].forEach((delay) => setTimeout(() => this._refreshFavoriteState(true).catch(() => {
            }), delay));
            return true;
          }
        }
        if (!usedDirectApi) {
          throw new Error("Music Assistant Direct API is not available");
        }
        this._applyMaFavoriteOptimisticState(entry, !likedNow);
        verified = await this._waitForFavoriteState(effectiveEntry, !likedNow, [350, 900, 1800, 3200, 5200]);
        if (!verified) this._applyMaFavoriteOptimisticState(entry, !likedNow);
        this._toast(likedNow ? this._i18n("ui.removed_from_music_assistant_liked") : this._i18n("ui.added_to_music_assistant_liked"));
        if (sourceEl) this._flashInteraction(sourceEl);
        [300, 900, 1800].forEach((delay) => setTimeout(() => this._refreshFavoriteState(true).catch(() => {
        }), delay));
        entry.favorite = !likedNow;
        if (entry.media_item) entry.media_item.favorite = !likedNow;
        return true;
      } catch (error) {
        this._toastError(this._i18n("ui.ma_favorite_action_failed_with_error", {
          error: error?.message ? `: ${error.message}` : ""
        }));
        return false;
      }
    }
    async _toggleMaLikeEntryDirect(entry = {}, likedNow = false, mediaType = "track") {
      if (!this._hasDirectMAConnection()) return false;
      const uri = String(entry?.uri || "").trim();
      if (!uri) return false;
      if (!likedNow) {
        const playerUri = this._entryTargetsCurrentMedia(entry) ? String(this._getSelectedPlayer()?.attributes?.media_content_id || "").trim() : "";
        const bestUri = playerUri || uri;
        const bestRef = this._parseMediaReference(bestUri, mediaType);
        let lastError2 = null;
        const attempts2 = [
          { item: bestUri },
          { item: bestRef.item_id || bestUri },
          { item: { uri: bestUri, media_type: bestRef.media_type || mediaType, item_id: bestRef.item_id || "", provider: bestRef.provider || "" } },
          { uri: bestUri, media_type: bestRef.media_type || mediaType, item_id: bestRef.item_id || "", provider: bestRef.provider || "" }
        ];
        for (const args of attempts2) {
          try {
            await this._callDirectMaCommand("music/favorites/add_item", args);
            return true;
          } catch (error) {
            lastError2 = error;
          }
        }
        if (lastError2) throw lastError2;
        return true;
      }
      const removeArgs = await this._resolveMaFavoriteRemoveArgs(entry, mediaType);
      if (!removeArgs) return false;
      const attempts = [
        removeArgs,
        { media_type: removeArgs.media_type || mediaType, library_item_id: removeArgs.library_item_id }
      ].filter((candidate) => candidate?.library_item_id);
      let lastError = null;
      for (const args of attempts) {
        try {
          await this._callDirectMaCommand("music/favorites/remove_item", args);
          return true;
        } catch (error) {
          lastError = error;
        }
      }
      if (lastError) throw lastError;
      return false;
    }
    async _toggleMaLikeViaHaService(entry = {}, likedNow = false, mediaType = "track") {
      const uri = String(entry?.uri || "").trim();
      if (!uri) return false;
      const parsed = this._parseMediaReference(uri, mediaType);
      const removeArgs = likedNow ? await this._resolveMaFavoriteRemoveArgs(entry, mediaType) : null;
      const base = {
        entity_id: this._state.selectedPlayer || this._getSelectedPlayer()?.entity_id || "",
        media_id: uri,
        uri,
        media_type: parsed.media_type || mediaType || "track",
        item_id: parsed.item_id || "",
        provider: parsed.provider || "",
        favorite: !likedNow
      };
      const attempts = likedNow ? [
        { service: "remove_from_library", data: { entity_id: base.entity_id, media_type: removeArgs?.media_type || base.media_type, library_item_id: removeArgs?.library_item_id || "" } },
        { service: "remove_favorite", data: { entity_id: base.entity_id, media_type: removeArgs?.media_type || base.media_type, library_item_id: removeArgs?.library_item_id || "" } },
        { service: "favorite_item", data: { entity_id: base.entity_id, media_type: removeArgs?.media_type || base.media_type, library_item_id: removeArgs?.library_item_id || "", favorite: false } },
        { service: "set_favorite", data: { entity_id: base.entity_id, media_type: removeArgs?.media_type || base.media_type, library_item_id: removeArgs?.library_item_id || "", favorite: false } },
        { service: "remove_from_library", data: { ...base, ...removeArgs || {} } }
      ] : [
        { service: "add_to_library", data: base },
        { service: "add_favorite", data: base },
        { service: "favorite_item", data: { ...base, favorite: true } },
        { service: "set_favorite", data: { ...base, favorite: true } }
      ];
      for (const attempt of attempts) {
        if (!this._hasService("music_assistant", attempt.service)) continue;
        try {
          await this._callService(attempt.service, Object.fromEntries(Object.entries(attempt.data).filter(([, value]) => value !== "" && value !== void 0 && value !== null)));
          return true;
        } catch (_) {
        }
      }
      return false;
    }
    async _resolveCanonicalMaLikedEntry(entry = {}, force = true) {
      if (!this._useMaLikedMode()) return null;
      let likedItems = Array.isArray(this._cache.library.get("liked:ma")?.items) ? this._cache.library.get("liked:ma").items : [];
      if (force || !likedItems.length) {
        try {
          likedItems = await this._loadMaLikedEntries(true);
        } catch (_) {
          likedItems = Array.isArray(likedItems) ? likedItems : [];
        }
      }
      return this._findMaLikedEntryMatch(entry, likedItems);
    }
    _applyMaFavoriteOptimisticState(entry = {}, liked = false) {
      const nextEntry = HomeiiFavoritesFoundation2.buildOptimisticFavoriteEntry(entry, liked);
      const cache = this._cache.library.get("liked:ma");
      const items = HomeiiFavoritesFoundation2.applyOptimisticFavoriteCache(
        Array.isArray(cache?.items) ? cache.items : [],
        nextEntry,
        liked,
        {
          compareMediaRefsFn: (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType),
          matchFavoriteLibraryItemFn: (candidateEntry, likedEntries, fallbackType) => this._matchFavoriteLibraryItem(candidateEntry, likedEntries, fallbackType)
        }
      );
      this._cache.library.set("liked:ma", { items });
      entry.favorite = !!liked;
      if (entry.media_item) entry.media_item.favorite = !!liked;
      this._syncLikeButtons();
      if (this._state.menuOpen && this._state.menuPage === "library_liked") {
        this._renderMobileMenu().catch(() => {
        });
      }
    }
    async _resolveMaFavoriteRemoveArgs(entry = {}, mediaType = "track") {
      const uri = String(entry?.uri || "").trim();
      let likedItems = Array.isArray(this._cache.library.get("liked:ma")?.items) ? this._cache.library.get("liked:ma").items : [];
      if (!likedItems.length) {
        try {
          likedItems = await this._loadMaLikedEntries(true);
        } catch (_) {
          likedItems = [];
        }
      }
      const currentEntryMatches = this._entryTargetsCurrentMedia(entry);
      const currentEntry = currentEntryMatches ? this._currentMediaLikeMeta() : null;
      const resolvedFromKnownState = HomeiiFavoritesFoundation2.resolveCachedFavoriteRemoveArgs({
        entry,
        mediaType,
        likedItems,
        currentEntry,
        currentEntryMatches
      }, {
        parseMediaReferenceFn: (targetUri, fallbackType) => this._parseMediaReference(targetUri, fallbackType),
        favoriteRemoveArgsFromItemFn: (item, fallbackType) => this._favoriteRemoveArgsFromItem(item, fallbackType),
        findFavoriteEntryMatchFn: (targetEntry, entries) => this._findMaLikedEntryMatch(targetEntry, entries),
        matchFavoriteLibraryItemFn: (targetEntry, entries, fallbackType) => this._matchFavoriteLibraryItem(targetEntry, entries, fallbackType)
      });
      if (resolvedFromKnownState) return resolvedFromKnownState;
      try {
        const remote = await this._callDirectMaCommand("music/item_by_uri", { uri });
        const remoteArgs = this._favoriteRemoveArgsFromItem(remote, mediaType);
        if (remoteArgs) return remoteArgs;
      } catch (_) {
      }
      return null;
    }
    _findMaLikedEntryMatch(entry = {}, likedItems = []) {
      return HomeiiFavoritesFoundation2.findFavoriteEntryMatch(entry, likedItems, {
        parseMediaReferenceFn: (uri, fallbackType) => this._parseMediaReference(uri, fallbackType)
      });
    }
    _matchFavoriteLibraryItem(entry = {}, likedItems = [], fallbackType = "") {
      return HomeiiFavoritesFoundation2.matchFavoriteLibraryItem(
        entry,
        likedItems,
        fallbackType || entry?.media_type || entry?.type || "track",
        {
          parseMediaReferenceFn: (uri, mediaType) => this._parseMediaReference(uri, mediaType),
          favoriteRemoveArgsFromItemFn: (item, mediaType) => this._favoriteRemoveArgsFromItem(item, mediaType),
          findFavoriteEntryMatchFn: (targetEntry, entries) => this._findMaLikedEntryMatch(targetEntry, entries)
        }
      );
    }
    _favoriteRemoveArgsFromItem(item = {}, fallbackType = "track") {
      return HomeiiFavoritesFoundation2.favoriteRemoveArgsFromItem(
        item,
        fallbackType,
        (uri, mediaType) => this._parseMediaReference(uri, mediaType)
      );
    }
    _isEntryLiked(entry = {}) {
      const cache = this._cache.library.get("liked:ma");
      return HomeiiFavoritesFoundation2.isEntryLiked(
        entry,
        {
          useMaLikedMode: this._useMaLikedMode(),
          likedItems: Array.isArray(cache?.items) ? cache.items : null,
          localLikedUris: this._loadLikedUris()
        },
        {
          compareMediaRefsFn: (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType),
          matchFavoriteLibraryItemFn: (targetEntry, likedItems, fallbackType) => this._matchFavoriteLibraryItem(targetEntry, likedItems, fallbackType)
        }
      );
    }
    _parseMediaReference(uri = "", fallbackType = "track") {
      return HomeiiMediaQueueFoundation2.parseMediaReference(uri, fallbackType);
    }
    _showQueueItemMenu(clientX, clientY, entry = {}) {
      this._dismissCtx();
      const mount = this.$("mobileMenu")?.classList.contains("open") ? this.$("mobileMenu")?.querySelector(".menu-body") || this.$("mobileMenu")?.querySelector(".menu-sheet") || this.$("mobileMenu") : this.shadowRoot.querySelector(".card");
      if (!mount) return;
      const liked = this._isEntryLiked(entry);
      const queueCount = Math.max(1, this._getNowPlayingQueueItems().length || (this._state.queueItems || []).length || Number(this._state.maQueueState?.items || 1));
      const currentPosition = Math.max(1, Math.min(queueCount, this._queueDisplayPositionForEntry(entry, Math.round(Number(entry.sort_index || 0)) + 1 || 1)));
      const menu = document.createElement("div");
      menu.className = "ctx-menu queue-ctx-menu";
      menu.innerHTML = `
        <div class="queue-move-control ctx-move-control">
          <label>
            <span>${this._esc(this._i18n("ui.move_to_position"))}</span>
            ${this._queueMoveSelectHtml(queueCount, currentPosition, entry)}
          </label>
        </div>
        <div class="ctx-item" data-queue-popup="next"><span class="ctx-ico">${this._iconSvg("next")}</span><span>${this._esc(this._i18n("ui.move_to_next"))}</span></div>
        <div class="ctx-item" data-queue-popup="like"><span class="ctx-ico">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}</span><span>${this._esc(this._i18n("ui.like_2"))}</span></div>
        <div class="ctx-item" data-queue-popup="remove"><span class="ctx-ico">${this._iconSvg("menu")}</span><span>${this._esc(this._i18n("ui.remove"))}</span></div>
        <div class="ctx-item" data-queue-popup="close"><span class="ctx-ico">×</span><span>${this._esc(this._i18n("ui.close"))}</span></div>`;
      menu.addEventListener("click", (e) => e.stopPropagation());
      menu.querySelectorAll("[data-queue-popup]").forEach((item) => item.addEventListener("click", async (e) => {
        e.stopPropagation();
        const action = item.dataset.queuePopup;
        if (action === "close") {
          this._dismissCtx();
          return;
        }
        if (action === "like") {
          this._toggleLikeEntry(entry, item);
          if (this._state.menuOpen && this._state.menuPage === "queue") await this._renderMobileMenu();
        } else {
          const targetPosition = action === "move_to" ? this._queueMoveTargetFromElement(item) : null;
          if (action === "move_to" && !targetPosition) return;
          await this._handleQueueAction(action, entry.queue_item_id, entry.uri || "", entry.sort_index || "", targetPosition);
        }
        this._dismissCtx();
      }));
      menu.addEventListener("change", async (e) => {
        if (await this._handleQueueMoveAutoChange(e)) this._dismissCtx();
      });
      mount.appendChild(menu);
      this._ctxMenu = menu;
      this._ctxMenuOpenedAt = Date.now();
      const cardRect = mount.getBoundingClientRect();
      const fallbackRect = entry.anchorRect || null;
      const pointerX = Number.isFinite(clientX) && clientX > 0 ? clientX : null;
      const pointerY = Number.isFinite(clientY) && clientY > 0 ? clientY : null;
      let left = (pointerX ?? fallbackRect?.left ?? cardRect.left + 20) - cardRect.left;
      let top = (pointerY ?? fallbackRect?.bottom ?? cardRect.top + 20) - cardRect.top;
      menu.style.left = `${left}px`;
      menu.style.top = `${top}px`;
      requestAnimationFrame(() => {
        const mr = menu.getBoundingClientRect();
        const cr = mount.getBoundingClientRect();
        if (mr.right > cr.right) left = Math.max(12, left - (mr.right - cr.right) - 12);
        if (mr.bottom > cr.bottom) top = Math.max(12, top - mr.height - (fallbackRect?.height || 0) - 8);
        menu.style.left = `${Math.max(12, Math.min(left, cr.width - mr.width - 12))}px`;
        menu.style.top = `${Math.max(12, Math.min(top, cr.height - mr.height - 12))}px`;
      });
    }
    _renderImmersiveNowPlaying() {
      const backdrop = this.$("immersiveNowPlaying");
      const player = this._getSelectedPlayer();
      if (!backdrop || !player) return;
      const title = player.attributes.media_title || this._i18n("ui.no_active_media");
      const artist = player.attributes.media_artist || this._i18n("ui.unknown");
      const album = player.attributes.media_album_name || "";
      const playerName = player.attributes.friendly_name || player.entity_id;
      const art = this._currentArtworkUrl(player, this._state.maQueueState?.current_item || null, 180);
      const duration = this._getCurrentDuration();
      const position = this._getCurrentPosition();
      const progressPct = duration ? Math.min(100, position / duration * 100) : 0;
      const volumePct = Math.round((player.attributes.volume_level || 0) * 100);
      const shuffle = !!player.attributes.shuffle;
      const repeat = player.attributes.repeat || "off";
      const rtl = this._isHebrew();
      backdrop.innerHTML = `
        <div class="immersive-shell ${rtl ? "rtl" : ""}">
          <div class="immersive-bg" ${art ? `style="background-image:url('${this._esc(art)}')"` : ""}></div>
          <div class="immersive-cover-glow" ${art ? `style="background-image:url('${this._esc(art)}')"` : ""}></div>
          <div class="immersive-frost"></div>
          <div class="immersive-vignette"></div>
          <div class="immersive-header">
            <button class="close-btn" id="immersiveCloseBtn">✕</button>
            <div class="immersive-meta">
        <div class="immersive-kicker">homeii-music-flow</div>
              <div class="immersive-title">${this._esc(title)}</div>
              <div class="immersive-subtitle">${this._esc([artist, album].filter(Boolean).join(" · "))}</div>
              <div class="immersive-player-pill" id="immersivePlayerName">${this._esc(`${this._i18n("ui.playing_on")}: ${playerName}`)}</div>
            </div>
          </div>
          <div class="immersive-body">
            <div class="immersive-art-wrap">
              <div class="immersive-art" id="immersiveArt">${art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("music_note")}</div>
            </div>
            <div class="immersive-panel">
              <div class="immersive-time-row"><span id="immersiveCurTime">${this._esc(this._fmtDur(position))}</span><span id="immersiveTotalTime">${this._esc(this._fmtDur(duration))}</span></div>
              <div class="immersive-progress" id="immersiveProgressBar"><div class="immersive-progress-fill" id="immersiveProgressFill" style="width:${progressPct}%"></div></div>
              <div class="immersive-controls">
                <button class="immersive-btn ${shuffle ? "active" : ""}" id="immersiveShuffleBtn">${this._iconSvg("shuffle")}</button>
                <button class="immersive-btn" id="immersivePrevBtn">${this._iconSvg("previous")}</button>
                <button class="immersive-btn primary" id="immersivePlayBtn">${this._iconSvg(this._playPauseIconName(player))}</button>
                <button class="immersive-btn" id="immersiveNextBtn">${this._iconSvg("next")}</button>
                <button class="immersive-btn ${repeat !== "off" ? "active" : ""}" id="immersiveRepeatBtn">${this._iconSvg(repeat === "one" ? "repeat_one" : "repeat")}</button>
              </div>
              <div class="immersive-volume">
                <button class="immersive-btn small" id="immersiveMuteBtn">${this._iconSvg(this._volumeIconName(player))}</button>
                <input id="immersiveVolumeSlider" type="range" min="0" max="100" value="${volumePct}" style="--vol-pct:${volumePct}%">
              </div>
              <div class="immersive-actions">
                <button class="chip-btn immersive-player-picker-btn" id="immersiveChoosePlayerBtn" title="${this._i18n("ui.open_player_picker")}">${this._iconSvg("speaker")}</button>
                <button class="chip-btn" id="immersiveQueueBtn">${this._i18n("ui.open_full_queue")}</button>
                <button class="chip-btn" id="immersiveTransferBtn">${this._i18n("ui.transfer_queue")}</button>
              </div>
            </div>
          </div>
        </div>`;
      backdrop.querySelector("#immersiveCloseBtn")?.addEventListener("click", () => this._closeImmersiveNowPlaying());
      backdrop.onclick = (e) => {
        if (e.target === backdrop) this._closeImmersiveNowPlaying();
      };
      this._bindProgressSeekBar(backdrop.querySelector("#immersiveProgressBar"));
      backdrop.querySelector("#immersivePlayBtn")?.addEventListener("click", () => this._togglePlay());
      backdrop.querySelector("#immersivePrevBtn")?.addEventListener("click", () => this._playerCmd("previous"));
      backdrop.querySelector("#immersiveNextBtn")?.addEventListener("click", () => this._playerCmd("next"));
      backdrop.querySelector("#immersiveShuffleBtn")?.addEventListener("click", () => this._toggleShuffle());
      backdrop.querySelector("#immersiveRepeatBtn")?.addEventListener("click", () => this._toggleRepeat());
      backdrop.querySelector("#immersiveMuteBtn")?.addEventListener("click", () => this._toggleMute());
      backdrop.querySelector("#immersiveChoosePlayerBtn")?.addEventListener("click", () => this._openPlayerModal());
      backdrop.querySelector("#immersiveQueueBtn")?.addEventListener("click", () => this._showQueue());
      backdrop.querySelector("#immersiveTransferBtn")?.addEventListener("click", () => this._openTransferQueuePicker());
      backdrop.querySelector("#immersiveVolumeSlider")?.addEventListener("input", (e) => {
        const pct = Number(e.target.value || 0);
        e.target.style.setProperty("--vol-pct", `${pct}%`);
        this._syncBigVolumeMirror(pct);
        const small = this.$("volSlider");
        if (small) {
          small.value = pct;
          small.style.setProperty("--vol-pct", `${pct}%`);
        }
        this._setButtonIcon(this.$("btnMute"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
        this._setButtonIcon(this.$("bigMuteBtn"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
        this._setButtonIcon(backdrop.querySelector("#immersiveMuteBtn"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
        clearTimeout(this._bigVolumeTimer);
        this._bigVolumeTimer = setTimeout(() => this._setVolume(pct / 100), 120);
      });
    }
    _syncImmersiveNowPlaying() {
      if (!this._state.immersiveNowPlayingOpen) return;
      const player = this._getSelectedPlayer();
      const backdrop = this.$("immersiveNowPlaying");
      if (!player || !backdrop?.classList.contains("open")) return;
      const duration = this._getCurrentDuration();
      const position = this._getCurrentPosition();
      const pct = duration ? Math.min(100, position / duration * 100) : 0;
      const vol = Math.round((player.attributes.volume_level || 0) * 100);
      const art = this._currentArtworkUrl(player, this._state.maQueueState?.current_item || null, 180);
      const repeat = player.attributes.repeat || "off";
      const title = player.attributes.media_title || this._i18n("ui.no_active_media");
      const artist = player.attributes.media_artist || this._i18n("ui.unknown");
      const album = player.attributes.media_album_name || "";
      const playerName = player.attributes.friendly_name || player.entity_id;
      backdrop.querySelector(".immersive-title")?.replaceChildren(document.createTextNode(title));
      backdrop.querySelector(".immersive-subtitle")?.replaceChildren(document.createTextNode([artist, album].filter(Boolean).join(" · ")));
      backdrop.querySelector("#immersivePlayerName")?.replaceChildren(document.createTextNode(`${this._i18n("ui.playing_on")}: ${playerName}`));
      const play = backdrop.querySelector("#immersivePlayBtn");
      this._setButtonIcon(play, this._playPauseIconName(player));
      const shuffle = backdrop.querySelector("#immersiveShuffleBtn");
      if (shuffle) shuffle.classList.toggle("active", !!player.attributes.shuffle);
      const repeatBtn = backdrop.querySelector("#immersiveRepeatBtn");
      if (repeatBtn) {
        repeatBtn.classList.toggle("active", repeat !== "off");
        repeatBtn.dataset.repeatMode = repeat;
        repeatBtn.title = this._repeatModeLabel(repeat);
        repeatBtn.setAttribute("aria-label", this._repeatModeLabel(repeat));
      }
      const mute = backdrop.querySelector("#immersiveMuteBtn");
      this._setButtonIcon(repeatBtn, repeat === "one" ? "repeat_one" : "repeat");
      this._setButtonIcon(mute, this._volumeIconName(player));
      const slider = backdrop.querySelector("#immersiveVolumeSlider");
      if (slider) {
        slider.value = vol;
        slider.style.setProperty("--vol-pct", `${vol}%`);
      }
      const progress = backdrop.querySelector("#immersiveProgressFill");
      if (progress) progress.style.width = `${pct}%`;
      const cur = backdrop.querySelector("#immersiveCurTime");
      if (cur) cur.textContent = this._fmtDur(position);
      const total = backdrop.querySelector("#immersiveTotalTime");
      if (total) total.textContent = this._fmtDur(duration);
      const artBox = backdrop.querySelector("#immersiveArt");
      if (artBox) artBox.innerHTML = art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("music_note");
      const bg = backdrop.querySelector(".immersive-bg");
      if (bg) bg.style.backgroundImage = art ? `url("${art}")` : "";
      const glow = backdrop.querySelector(".immersive-cover-glow");
      if (glow) glow.style.backgroundImage = art ? `url("${art}")` : "";
    }
    _handleWindowResize() {
      clearTimeout(this._resizeTimer);
      this._resizeTimer = setTimeout(() => {
        if (!this.isConnected || !this._built) return;
        if (Date.now() < Number(this._layoutResizeHoldUntil || 0)) return;
        const reopenImmersive = this._state.immersiveNowPlayingOpen;
        const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
        const viewportHeight = typeof window !== "undefined" ? Number(window.innerHeight || 0) : 0;
        const currentWidth = this._getCardWidth(viewportWidth);
        const currentHeight = this._getAllocatedCardHeight(viewportHeight);
        const previousWidth = this._lastCardWidth || this._lastViewportWidth || 0;
        const previousHeight = this._lastCardHeight || this._lastViewportHeight || 0;
        const currentLayoutMode = this._layoutModeConfig({ width: currentWidth });
        const card = this.shadowRoot?.querySelector(".card") || null;
        const renderedLayoutMode = this._renderedLayoutMode || (card?.classList.contains("layout-tablet") ? "tablet" : card?.classList.contains("layout-mobile") ? "mobile" : "");
        const layoutModeStale = !!(renderedLayoutMode && currentLayoutMode && renderedLayoutMode !== currentLayoutMode);
        const active = this.shadowRoot?.activeElement || document.activeElement;
        const activeTag = active?.tagName?.toLowerCase?.() || "";
        const editingText = active && (activeTag === "input" || activeTag === "textarea" || active?.isContentEditable);
        const tabletStabilityMode = this._tabletStabilityModeEnabled();
        const resizeStrategy = HomeiiResponsiveFoundation2.resolveResizeStrategy({
          previousWidth,
          currentWidth,
          previousHeight,
          currentHeight,
          editingText,
          tabletStabilityMode
        });
        this._lastCardWidth = currentWidth;
        this._lastCardHeight = currentHeight;
        this._lastViewportWidth = viewportWidth;
        this._lastViewportHeight = viewportHeight;
        if (resizeStrategy.keyboardLikeResize && !layoutModeStale) return;
        if (resizeStrategy.softSync && !layoutModeStale) {
          this._syncTabletAutoFitUi();
          this._syncSleepTimerChip();
          this._syncSourceBadgesUi();
          this._syncRecentHistoryUi();
          this._syncControlRoomUi();
          return;
        }
        this._build();
        this._loadPlayers();
        this._renderPlayerSummary();
        this._syncMaButtonVisibility();
        this._syncBrandPlayingState();
        this._updateThemeButton();
        if (this._state.view === "now_playing") this._renderNowPlayingPage();
        else if (this._state.query) this._renderGlobalSearch(this._state.query);
        else this._renderCurrentView();
        this._syncNowPlayingUI();
        if (reopenImmersive && this._state.view === "now_playing") this._openImmersiveNowPlaying();
      }, 120);
    }
    _setView(view, btn = null) {
      if (view !== "now_playing" && this._state.immersiveNowPlayingOpen) this._closeImmersiveNowPlaying();
      this._state.view = view;
      this.shadowRoot.querySelectorAll(".nav-btn").forEach((b) => b.classList.toggle("active", b === btn || b.dataset.view === view));
      const isNowPlaying = view === "now_playing";
      this.$("searchWrap").style.display = isNowPlaying ? "none" : "";
      if (isNowPlaying) {
        this.$("searchInp").value = "";
        this._state.query = "";
        this.$("searchClear").style.display = "none";
      }
      if (this._state.queueVisible) this._hideQueue();
      this._renderCurrentView();
    }
    async _renderCurrentView() {
      switch (this._state.view) {
        case "now_playing":
          return this._renderNowPlayingPage();
        case "radio":
          return this._renderRadio();
        case "podcasts":
          return this._renderPodcasts();
        case "albums":
          return this._renderAlbums();
        case "artists":
          return this._renderArtists();
        case "tracks":
          return this._renderTracks();
        case "playlists":
          return this._renderPlaylists();
        default:
          return this._renderHome();
      }
    }
    _nextRenderToken() {
      this._state.renderToken += 1;
      return this._state.renderToken;
    }
    _isValidRender(token) {
      return token === this._state.renderToken;
    }
    _loadingStateHtml(text = this._i18n("ui.loading"), { notice = false, compact = false } = {}) {
      const className = notice ? "notice open homeii-loading-notice" : `homeii-loading-state${compact ? " compact" : ""}`;
      return `
        <div class="${className}" role="status" aria-live="polite">
          <div class="homeii-loading-content">
            <span class="homeii-loading-mark" aria-hidden="true">
              <span class="homeii-loading-ring"></span>
              <span class="homeii-loading-ring secondary"></span>
              <span class="homeii-loading-core"></span>
            </span>
            <span class="homeii-loading-text">${this._esc(text)}</span>
          </div>
        </div>
      `;
    }
    _renderLoading(text = this._i18n("ui.loading")) {
      this.$("content").classList.remove("now-playing-mode");
      this.$("content").innerHTML = this._loadingStateHtml(text);
    }
    _renderEmpty(text = this._i18n("ui.no_content_found")) {
      this.$("content").classList.remove("now-playing-mode");
      this.$("content").innerHTML = `<div class="state-box">${this._esc(text)}</div>`;
    }
    _renderError(error, retryFn = null) {
      const msg = error?.message || String(error || "Unknown error");
      this.$("content").classList.remove("now-playing-mode");
      this.$("content").innerHTML = `
        <div class="state-box">
          <div>
            <div style="font-size:20px;margin-bottom:8px;">⚠</div>
            <div>${this._esc(msg)}</div>
            ${retryFn ? `<div style="margin-top:12px;"><button class="chip-btn" id="retryBtn">${this._i18n("ui.try_again")}</button></div>` : ""}
          </div>
        </div>`;
      if (retryFn) this.$("retryBtn")?.addEventListener("click", retryFn, { once: true });
    }
    async _renderHome() {
      const token = this._nextRenderToken();
      this._renderLoading(this._i18n("ui.loading_library"));
      try {
        const results = await Promise.allSettled([
          this._getLibrary("radio", "sort_name", 24, true),
          this._fetchRecentlyPlayed(18),
          this._fetchLibrary("album", "last_modified", 18, false),
          this._fetchLibrary("album", "random", 18, false)
        ]);
        if (!this._isValidRender(token)) return;
        const [radio, recentlyPlayed, recentAlbums, randomAlbums] = results.map((r) => r.value ?? []);
        let html = "";
        if (radio.length) html += this._sectionHtml(this._i18n("ui.favorite_radio"), radio, "radio", true);
        if (recentlyPlayed.length) html += this._sectionHtml(this._i18n("ui.recently_played"), recentlyPlayed, "album", true);
        if (recentAlbums.length) html += this._sectionHtml(this._i18n("ui.recently_added"), recentAlbums, "album", true);
        if (randomAlbums.length) html += this._sectionHtml(this._i18n("ui.discover"), randomAlbums, "album", true);
        const availabilityError = results.find((result) => result.status === "rejected" && this._isMusicAssistantAvailabilityError(result.reason));
        const issueMessage = this._state.musicAssistantIssueMessage || (availabilityError ? this._handleMusicAssistantIssue(availabilityError.reason) : "");
        this.$("content").classList.remove("now-playing-mode");
        this.$("content").innerHTML = html || `<div class="state-box">${this._esc(issueMessage || this._i18n("ui.no_content_found"))}</div>`;
        this._hydrateImages();
        this._highlightNowPlaying();
      } catch (e) {
        if (!this._isValidRender(token)) return;
        this._renderError(e, () => this._renderHome());
      }
    }
    async _renderRadio() {
      return this._renderGridCollection("radio", this._i18n("ui.radio_stations"), 5e3, false);
    }
    async _renderPodcasts() {
      return this._renderGridCollection("podcast", this._i18n("ui.all_podcasts"), 500, false);
    }
    async _renderAlbums() {
      return this._renderGridCollection("album", this._i18n("ui.all_albums"), 500, false);
    }
    async _renderArtists() {
      return this._renderGridCollection("artist", this._i18n("ui.all_artists"), 500, false);
    }
    async _renderPlaylists() {
      return this._renderGridCollection("playlist", this._i18n("ui.playlists"), 500, false);
    }
    _setTracksLayout(layout) {
      this._state.tracksLayout = layout === "grid" ? "grid" : "list";
      try {
        localStorage.setItem("homeii_music_flow_tracks_layout", this._state.tracksLayout);
      } catch (_) {
      }
      if (this._state.view === "tracks") this._renderTracks();
    }
    async _renderTracks() {
      const token = this._nextRenderToken();
      this._renderLoading(this._i18n("ui.loading"));
      try {
        const items = await this._getLibrary("track", "sort_name", 500);
        if (!this._isValidRender(token)) return;
        const isGrid = this._state.tracksLayout === "grid";
        this.$("content").classList.remove("now-playing-mode");
        this.$("content").innerHTML = `
          <div class="section">
            <div class="section-header">
              <div class="section-title">${this._esc(this._i18n("ui.all_tracks"))}</div>
              <div class="section-badge">${items.length}</div>
              <div class="section-actions">
                <button class="chip-btn ${isGrid ? "active" : ""}" id="tracksGridBtn">${this._i18n("ui.grid")}</button>
                <button class="chip-btn ${!isGrid ? "active" : ""}" id="tracksListBtn">${this._i18n("ui.list")}</button>
                ${this._sectionActionButtons(items)}
              </div>
            </div>
            <div class="${isGrid ? "grid" : "track-list"}">${isGrid ? items.map((item) => this._mediaCardHtml(item, "track")).join("") : items.map((item, i) => this._trackRowHtml(item, i + 1)).join("")}</div>
          </div>`;
        this.$("tracksGridBtn")?.addEventListener("click", () => this._setTracksLayout("grid"));
        this.$("tracksListBtn")?.addEventListener("click", () => this._setTracksLayout("list"));
        this._hydrateImages();
        this._highlightNowPlaying();
      } catch (e) {
        if (!this._isValidRender(token)) return;
        this._renderError(e, () => this._renderTracks());
      }
    }
    async _renderGridCollection(mediaType, title, limit = 500, favoritesOnly = false) {
      const token = this._nextRenderToken();
      this._renderLoading(this._i18n("ui.loading"));
      try {
        const items = await this._getLibrary(mediaType, "sort_name", limit, favoritesOnly);
        if (!this._isValidRender(token)) return;
        this.$("content").classList.remove("now-playing-mode");
        this.$("content").innerHTML = this._sectionHtml(title, items, mediaType, true);
        this._hydrateImages();
        this._highlightNowPlaying();
      } catch (e) {
        if (!this._isValidRender(token)) return;
        this._renderError(e, () => this._renderGridCollection(mediaType, title, limit, favoritesOnly));
      }
    }
    async _renderGlobalSearch(query) {
      const token = this._nextRenderToken();
      this._renderLoading(`${this._i18n("ui.search")}: ${query}`);
      try {
        const res = await this._search(query);
        if (!this._isValidRender(token)) return;
        const { radio = [], podcasts = [], albums = [], artists = [], tracks = [], playlists = [] } = res;
        let html = `<div class="section"><div class="section-header"><div class="section-title">${this._esc(this._i18n("ui.search"))}: ${this._esc(query)}</div></div></div>`;
        if (radio.length) html += this._sectionHtml(this._i18n("ui.radio"), radio, "radio", false);
        if (podcasts.length) html += this._sectionHtml(this._i18n("ui.podcasts"), podcasts, "podcast", false);
        if (albums.length) html += this._sectionHtml(this._i18n("ui.albums"), albums, "album", false);
        if (artists.length) html += this._sectionHtml(this._i18n("ui.artists"), artists, "artist", false);
        if (tracks.length) {
          html += `<div class="section"><div class="section-header"><div class="section-title">${this._esc(this._i18n("ui.tracks"))}</div><div class="section-badge">${tracks.length}</div><div class="section-actions">${this._sectionActionButtons(tracks)}</div></div><div class="track-list">${tracks.map((item, i) => this._trackRowHtml(item, i + 1)).join("")}</div></div>`;
        }
        if (playlists.length) html += this._sectionHtml(this._i18n("ui.playlists"), playlists, "playlist", false);
        if (!radio.length && !podcasts.length && !albums.length && !artists.length && !tracks.length && !playlists.length) html = `<div class="state-box">${this._esc(this._i18n("ui.no_results"))}: "${this._esc(query)}"</div>`;
        this.$("content").classList.remove("now-playing-mode");
        this.$("content").innerHTML = html;
        this._hydrateImages();
        this._highlightNowPlaying();
      } catch (e) {
        if (!this._isValidRender(token)) return;
        this._renderError(e, () => this._renderGlobalSearch(query));
      }
    }
    async _renderNowPlayingPage() {
      const token = this._nextRenderToken();
      this._renderLoading(this._i18n("ui.loading"));
      try {
        await this._ensureQueueSnapshot();
        this._refreshGroupingState();
        if (!this._isValidRender(token)) return;
        const player = this._getSelectedPlayer();
        if (!player) return this._renderEmpty(this._i18n("ui.no_active_media"));
        const title = player.attributes.media_title || this._i18n("ui.no_active_media");
        const artist = player.attributes.media_artist || this._i18n("ui.unknown");
        const album = player.attributes.media_album_name || "";
        const state = player.state || "idle";
        const queueItem = this._state.maQueueState?.current_item || null;
        const art = this._currentArtworkUrl(player, queueItem, 520);
        const duration = this._getCurrentDuration();
        const position = this._getCurrentPosition();
        const progressPct = duration ? Math.min(100, position / duration * 100) : 0;
        const volumePct = Math.round((player.attributes.volume_level || 0) * 100);
        const shuffle = !!player.attributes.shuffle;
        const repeat = player.attributes.repeat || "off";
        const queueItems = this._getNowPlayingQueueItems();
        const nowPlayingQuery = this._state.nowPlayingQuery || "";
        this.$("content").classList.add("now-playing-mode");
        this.$("content").innerHTML = `
          <div class="now-layout">
            <div class="now-left">
              <div class="now-card now-art-card">
                <div class="now-art" id="nowHeroArt">${art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("music_note")}</div>
                <div class="now-track-meta">
                  <div class="now-track-title">${this._esc(title)}</div>
                  <div class="now-track-subtitle">${this._esc([artist, album].filter(Boolean).join(" · "))}</div>
                </div>
              </div>

              <div class="now-card now-controls-card">
                <div class="now-time-row"><span id="bigCurTime">${this._esc(this._fmtDur(position))}</span><span id="bigTotalTime">${this._esc(this._fmtDur(duration))}</span></div>
                <div class="now-progress" id="bigProgressBar"><div class="now-progress-fill" id="bigProgressFill" style="width:${progressPct}%"></div></div>
                <div class="now-controls-main">
                  <button class="big-round-btn ${shuffle ? "active" : ""}" id="bigShuffleBtn">${this._iconSvg("shuffle")}</button>
                  <button class="big-round-btn" id="bigPrevBtn">${this._iconSvg("previous")}</button>
                  <button class="big-main-btn" id="bigPlayBtn">${this._iconSvg(state === "playing" ? "pause" : "play")}</button>
                  <button class="big-round-btn" id="bigNextBtn">${this._iconSvg("next")}</button>
                  <button class="big-round-btn ${repeat !== "off" ? "active" : ""}" id="bigRepeatBtn">${this._iconSvg(repeat === "one" ? "repeat_one" : "repeat")}</button>
                </div>
                <div class="now-controls-bottom">
                  <div class="now-volume">
                    <button class="big-round-btn" id="bigMuteBtn">${this._iconSvg(this._volumeIconName(player))}</button>
                    <input id="bigVolumeSlider" type="range" min="0" max="100" value="${volumePct}" style="--vol-pct:${volumePct}%">
                  </div>
                  <div class="now-actions">
                    <button class="chip-btn now-player-picker-btn" id="choosePlayerInlineBtn" title="${this._i18n("ui.open_player_picker")}">${this._iconSvg("speaker")}</button>
                  </div>
                </div>
              </div>
            </div>

            <div class="now-right">
              <div class="now-card now-queue-card">
                <div class="now-queue-toolbar">
                  <div class="now-queue-header">
                    <div class="now-queue-title" id="nowQueuePanelTitle">${this._i18n("ui.up_next")}</div>
                    <div class="now-queue-count" id="nowQueuePanelCount">${queueItems.length}</div>
                    <div class="group-inline">
                      <button class="chip-btn" id="openQueueBtn">${this._i18n("ui.open_full_queue")}</button>
                      <button class="chip-btn" id="transferQueueBtn">${this._i18n("ui.transfer_queue")}</button>
                      <button class="chip-btn" id="groupBtn">${this._i18n("ui.group_speakers")}</button>
                    </div>
                  </div>
                  <div class="search now-queue-search">
                    <span>🔍</span>
                    <input id="nowQueueSearchInput" type="text" value="${this._esc(nowPlayingQuery)}" placeholder="${this._i18n("ui.search_queue_and_library")}">
                    <button class="icon-btn" id="nowQueueSearchClear" style="display:${nowPlayingQuery.trim() ? "" : "none"};" title="${this._i18n("ui.clear_search")}">✕</button>
                  </div>
                </div>
                <div class="now-queue-body" id="nowQueuePanelBody">${this._queuePanelHtml(queueItems)}</div>
              </div>
            </div>
          </div>`;
        this._bindNowPlayingPage();
        this._updateNowPlayingSidePanel();
        this._highlightNowPlaying();
      } catch (e) {
        if (!this._isValidRender(token)) return;
        this._renderError(e, () => this._renderNowPlayingPage());
      }
    }
    _getNowPlayingQueueItems() {
      const currentIndex = this._state.maQueueState?.current_index ?? -1;
      return (this._state.queueItems || []).filter((item) => (item.sort_index ?? 0) >= currentIndex).slice(0, 100);
    }
    _queuePanelHtml(queueItems = []) {
      if (!queueItems.length) {
        return `<div class="now-side-scroll"><div class="state-box" style="min-height:120px;">${this._esc(this._i18n("ui.queue_is_empty"))}</div></div>`;
      }
      return `<div class="now-side-scroll"><div class="now-queue-list">${queueItems.map((item) => this._miniQueueItemHtml(item)).join("")}</div></div>`;
    }
    _nowPlayingSearchResultsHtml(query, results) {
      const { radio = [], podcasts = [], albums = [], artists = [], tracks = [], playlists = [] } = results || {};
      const total = radio.length + podcasts.length + albums.length + artists.length + tracks.length + playlists.length;
      if (!total) {
        return `<div class="now-side-scroll"><div class="state-box">${this._esc(this._i18n("ui.no_results"))}: "${this._esc(query)}"</div></div>`;
      }
      let html = `
        <div class="now-side-scroll">
          <div class="side-search-summary">
            <div class="side-search-summary-text">${this._esc(this._i18n("ui.queue_results"))}: "${this._esc(query)}"</div>
            <button class="chip-btn" id="backToQueueBtn">${this._esc(this._i18n("ui.back_to_queue"))}</button>
          </div>`;
      if (radio.length) html += this._sectionHtml(this._i18n("ui.radio"), radio, "radio", false);
      if (podcasts.length) html += this._sectionHtml(this._i18n("ui.podcasts"), podcasts, "podcast", false);
      if (albums.length) html += this._sectionHtml(this._i18n("ui.albums"), albums, "album", false);
      if (artists.length) html += this._sectionHtml(this._i18n("ui.artists"), artists, "artist", false);
      if (tracks.length) {
        html += `<div class="section"><div class="section-header"><div class="section-title">${this._esc(this._i18n("ui.tracks"))}</div><div class="section-badge">${tracks.length}</div></div><div class="track-list">${tracks.map((item, i) => this._trackRowHtml(item, i + 1)).join("")}</div></div>`;
      }
      if (playlists.length) html += this._sectionHtml(this._i18n("ui.playlists"), playlists, "playlist", false);
      html += `</div>`;
      return html;
    }
    async _updateNowPlayingSidePanel() {
      if (this._state.view !== "now_playing") return;
      const body = this.$("nowQueuePanelBody");
      const title = this.$("nowQueuePanelTitle");
      const count = this.$("nowQueuePanelCount");
      const clear = this.$("nowQueueSearchClear");
      if (!body || !title || !count) return;
      const query = String(this._state.nowPlayingQuery || "").trim();
      if (clear) clear.style.display = query ? "" : "none";
      const token = ++this._state.sidePanelToken;
      if (!query) {
        const queueItems = this._getNowPlayingQueueItems();
        title.textContent = this._i18n("ui.up_next");
        count.textContent = String(queueItems.length);
        body.innerHTML = this._queuePanelHtml(queueItems);
        this._hydrateImages();
        this._highlightNowPlaying();
        return;
      }
      title.textContent = this._i18n("ui.search");
      count.textContent = "…";
      body.innerHTML = `<div class="now-side-scroll">${this._loadingStateHtml(this._i18n("ui.loading"), { compact: true })}</div>`;
      try {
        const results = await this._search(query);
        if (token !== this._state.sidePanelToken || !body.isConnected) return;
        const total = Object.values(results || {}).reduce((sum, arr) => sum + (Array.isArray(arr) ? arr.length : 0), 0);
        count.textContent = String(total);
        body.innerHTML = this._nowPlayingSearchResultsHtml(query, results);
        this.$("backToQueueBtn")?.addEventListener("click", () => {
          const input = this.$("nowQueueSearchInput");
          if (input) input.value = "";
          this._state.nowPlayingQuery = "";
          this._updateNowPlayingSidePanel();
        });
        this._hydrateImages();
        this._highlightNowPlaying();
      } catch (e) {
        if (token !== this._state.sidePanelToken || !body.isConnected) return;
        count.textContent = "!";
        body.innerHTML = `<div class="now-side-scroll"><div class="state-box">${this._esc(e?.message || this._i18n("ui.try_again"))}</div></div>`;
      }
    }
    _bindNowPlayingPage() {
      const bind = (id, fn) => {
        const el = this.$(id);
        if (el) el.addEventListener("click", fn);
      };
      bind("bigPlayBtn", () => this._togglePlay());
      bind("bigPrevBtn", () => this._playerCmd("previous"));
      bind("bigNextBtn", () => this._playerCmd("next"));
      bind("bigShuffleBtn", () => this._toggleShuffle());
      bind("bigRepeatBtn", () => this._toggleRepeat());
      bind("bigMuteBtn", () => this._toggleMute());
      bind("openQueueBtn", () => this._showQueue());
      bind("transferQueueBtn", () => this._openTransferQueuePicker());
      bind("groupBtn", () => this._openGroupModal());
      bind("choosePlayerInlineBtn", () => this._openPlayerModal());
      bind("nowHeroArt", () => this._openImmersiveNowPlaying());
      this._bindProgressSeekBar(this.$("bigProgressBar"));
      const queueSearch = this.$("nowQueueSearchInput");
      if (queueSearch) {
        queueSearch.addEventListener("input", () => {
          this._state.nowPlayingQuery = queueSearch.value || "";
          clearTimeout(this._nowPlayingSearchTimer);
          this._nowPlayingSearchTimer = setTimeout(() => this._updateNowPlayingSidePanel(), 220);
        });
        queueSearch.addEventListener("keydown", (e) => e.stopPropagation());
      }
      bind("nowQueueSearchClear", () => {
        const input = this.$("nowQueueSearchInput");
        if (input) input.value = "";
        this._state.nowPlayingQuery = "";
        this._updateNowPlayingSidePanel();
      });
      const bigVolume = this.$("bigVolumeSlider");
      if (bigVolume) {
        bigVolume.addEventListener("input", (e) => {
          const pct = Number(e.target.value || 0);
          e.target.style.setProperty("--vol-pct", `${pct}%`);
          const small = this.$("volSlider");
          if (small) {
            small.value = pct;
            small.style.setProperty("--vol-pct", `${pct}%`);
          }
          this._setButtonIcon(this.$("btnMute"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
          this._setButtonIcon(this.$("bigMuteBtn"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
          clearTimeout(this._bigVolumeTimer);
          this._bigVolumeTimer = setTimeout(() => this._setVolume(pct / 100), 120);
        });
      }
    }
    _sectionHtml(title, items, mediaType, withActions = true) {
      const count = items.length || 0;
      const actions = withActions ? `<div class="section-actions">${this._sectionActionButtons(items)}</div>` : "";
      return `<div class="section"><div class="section-header"><div class="section-title">${this._esc(title)}</div><div class="section-badge">${count}</div>${actions}</div><div class="grid">${items.map((item) => this._mediaCardHtml(item, mediaType)).join("")}</div></div>`;
    }
    _sectionActionButtons(items) {
      if (!items?.length) return "";
      const encoded = this._esc(JSON.stringify(items.map((i) => ({ uri: i.uri, media_type: i.media_type || "album" }))));
      return `<button class="chip-btn" data-action="play-all" data-items="${encoded}">${this._esc(this._i18n("ui.play_all"))}</button><button class="chip-btn" data-action="shuffle-all" data-items="${encoded}">${this._esc(this._i18n("ui.shuffle_all"))}</button>`;
    }
    _mediaCardHtml(item, forcedType = null) {
      const mediaType = forcedType || item.media_type || "album";
      const uri = item.uri || "";
      const name = item.name || "";
      const artUrl2 = this._artUrl(item);
      const artist = mediaType === "artist" ? this._i18n("ui.artist") : mediaType === "radio" ? item.metadata?.description || "" : this._artistName(item) || item.album?.name || "";
      const placeholder = mediaType === "radio" ? "radio" : mediaType === "artist" ? "artist" : mediaType === "podcast" ? "podcast" : "music_note";
      const imgAttrs = artUrl2 ? `data-img="${this._esc(artUrl2)}" data-placeholder="${placeholder}"` : "";
      return `<div class="media-card" data-uri="${this._esc(uri)}" data-type="${this._esc(mediaType)}"><div class="media-art" ${imgAttrs}><div class="media-placeholder">${this._artPlaceholderHtml(placeholder)}</div><div class="media-overlay"><div class="play-bubble">▶</div></div><div class="playing-badge">▶</div></div><div class="media-title">${this._esc(name)}</div><div class="media-sub">${this._esc(artist)}</div></div>`;
    }
    _trackRowHtml(item, index = 1) {
      const artUrl2 = this._artUrl(item);
      const name = item.name || "";
      const artist = this._artistName(item);
      const sub = [artist, item.album?.name].filter(Boolean).join(" · ");
      const imgAttrs = artUrl2 ? `data-img="${this._esc(artUrl2)}" data-placeholder="music_note"` : "";
      return `<div class="track-row" data-uri="${this._esc(item.uri || "")}" data-type="track"><div class="track-num">${index}</div><div class="track-art" ${imgAttrs}>${this._artPlaceholderHtml("music_note")}</div><div class="track-meta"><div class="track-name">${this._esc(name)}</div><div class="track-sub">${this._esc(sub)}</div></div><div class="track-dur">${this._fmtDur(item.duration)}</div></div>`;
    }
    _getQueueItemKey(item) {
      return HomeiiMediaQueueFoundation2.getQueueItemKey(item);
    }
    _getQueueItemStableId(item) {
      return HomeiiMediaQueueFoundation2.getQueueItemStableId(item);
    }
    _getQueueItemPlaybackId(item) {
      return HomeiiMediaQueueFoundation2.getQueueItemPlaybackId(item);
    }
    _getQueueItemUri(item) {
      return HomeiiMediaQueueFoundation2.getQueueItemUri(item);
    }
    _queueItemOrderIdentity(item = {}, index = 0) {
      const stableId = this._getQueueItemStableId(item);
      const uri = this._getQueueItemUri(item);
      const title = this._queueItemPrimaryTitle(item);
      const artist = this._queueItemPrimaryArtist(item);
      const sortIndex = Number.isFinite(Number(item?.sort_index)) ? `sort:${item.sort_index}` : "";
      return stableId || uri || [title, artist].filter(Boolean).join("::") || sortIndex || `idx:${index}`;
    }
    _queueOrderSignature(items = []) {
      return (Array.isArray(items) ? items : []).map((item, index) => this._queueItemOrderIdentity(item, index)).join("|");
    }
    _queueRenderSignature() {
      return [
        this._queueOrderSignature(this._state.queueItems || []),
        this._state.maQueueState?.current_index ?? "",
        this._getQueueItemStableId(this._state.maQueueState?.current_item || {}) || this._getQueueItemUri(this._state.maQueueState?.current_item || "")
      ].join("::");
    }
    _queueItemsWithSequentialSortIndexes(items = []) {
      return (Array.isArray(items) ? items : []).filter(Boolean).map((item, index) => ({
        ...item,
        sort_index: index
      }));
    }
    _queueVisibleItemsForMove(items = []) {
      const allItems = Array.isArray(items) ? items.filter(Boolean) : [];
      const currentIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(this._state.maQueueState?.current_index);
      if (!Number.isFinite(currentIndex)) return allItems;
      const visibleItems = allItems.filter((item) => Number(item?.sort_index ?? -1) >= currentIndex);
      return visibleItems.length ? visibleItems : allItems;
    }
    _queueFirstMovableIndex(items = []) {
      const allItems = Array.isArray(items) ? items : [];
      const currentIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(this._state.maQueueState?.current_index);
      if (!Number.isFinite(currentIndex)) return 0;
      const currentArrayIndex = allItems.findIndex((item) => Number(item?.sort_index) === currentIndex);
      return Math.max(0, currentArrayIndex + 1);
    }
    _queueMoveGlobalIndexForDisplayPosition(items = [], targetPosition = 1) {
      const allItems = Array.isArray(items) ? items.filter(Boolean) : [];
      if (!allItems.length) return -1;
      const visibleItems = this._queueVisibleItemsForMove(allItems);
      const displayIndex = Math.max(0, Math.min(visibleItems.length - 1, Math.round(Number(targetPosition)) - 1 || 0));
      const targetItem = visibleItems[displayIndex] || null;
      if (!targetItem) return -1;
      const targetKey = this._getQueueItemKey(targetItem);
      const targetStableId = this._getQueueItemStableId(targetItem);
      const targetUri = this._getQueueItemUri(targetItem);
      const targetSortIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(targetItem?.sort_index);
      const targetGlobalIndex = allItems.findIndex(
        (item) => item === targetItem || targetKey && this._getQueueItemKey(item) === targetKey || targetStableId && this._getQueueItemStableId(item) === targetStableId || targetUri && this._mediaRefsEquivalent(this._getQueueItemUri(item), targetUri, item?.media_item?.media_type || item?.media_type || "track") || Number.isFinite(targetSortIndex) && Number(item?.sort_index) === targetSortIndex
      );
      if (targetGlobalIndex < 0) return -1;
      return Math.max(this._queueFirstMovableIndex(allItems), targetGlobalIndex);
    }
    _queueDisplayPositionForEntry(entry = {}, fallbackPosition = 1) {
      const visibleItems = this._getNowPlayingQueueItems();
      const entryKey = this._getQueueItemKey(entry) || String(entry?.queue_item_id || entry?.queueItemId || entry?.key || "").trim();
      const entryStableId = this._getQueueItemStableId(entry);
      const entryUri = this._getQueueItemUri(entry) || String(entry?.uri || entry?.media_item?.uri || "").trim();
      const entrySortIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(entry?.sort_index);
      const index = visibleItems.findIndex(
        (item) => entryKey && this._getQueueItemKey(item) === entryKey || entryStableId && this._getQueueItemStableId(item) === entryStableId || entryUri && this._mediaRefsEquivalent(this._getQueueItemUri(item), entryUri, item?.media_item?.media_type || item?.media_type || "track") || Number.isFinite(entrySortIndex) && Number(item?.sort_index) === entrySortIndex
      );
      if (index >= 0) return index + 1;
      const fallback = Math.round(Number(fallbackPosition || entry?.position));
      return Math.max(1, fallback || 1);
    }
    _resolveQueueActionTarget(items = [], queueItemId = "", fallbackUri = "", sortIndex = "") {
      const allItems = Array.isArray(items) ? items : [];
      const rawKey = String(queueItemId || "").trim();
      const uri = String(fallbackUri || "").trim();
      const numericSortIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(sortIndex);
      return (rawKey ? allItems.find(
        (item) => this._getQueueItemKey(item) === rawKey || this._getQueueItemStableId(item) === rawKey || this._getQueueItemUri(item) === rawKey
      ) : null) || (uri ? allItems.find(
        (item) => this._mediaRefsEquivalent(
          this._getQueueItemUri(item),
          uri,
          item?.media_item?.media_type || item?.media_type || "track"
        )
      ) : null) || allItems.find((item) => Number.isFinite(numericSortIndex) && Number(item?.sort_index) === numericSortIndex) || null;
    }
    _queueItemPrimaryArtist(item = {}) {
      return HomeiiMediaQueueFoundation2.queueItemPrimaryArtist(item);
    }
    _queueItemPrimaryTitle(item = {}) {
      return HomeiiMediaQueueFoundation2.queueItemPrimaryTitle(item);
    }
    _isQueueItemCurrent(item = {}) {
      if (!item) return false;
      const currentItem = this._state.maQueueState?.current_item || null;
      const currentIndex = Number(this._state.maQueueState?.current_index);
      const itemIndex = Number(item?.sort_index);
      if (Number.isFinite(currentIndex) && Number.isFinite(itemIndex) && currentIndex === itemIndex) return true;
      const currentKey = this._getQueueItemKey(currentItem);
      const itemKey = this._getQueueItemKey(item);
      if (currentKey && itemKey && currentKey === itemKey) return true;
      const currentUri = this._getQueueItemUri(currentItem);
      const itemUri = this._getQueueItemUri(item);
      if (currentUri && itemUri && this._mediaRefsEquivalent(itemUri, currentUri, item?.media_item?.media_type || item?.media_type || "track")) return true;
      return this._queueItemMatchesPlayer(item);
    }
    _mobileUpNextItem() {
      return HomeiiMediaQueueFoundation2.resolveMobileUpNextItem(
        this._state.maQueueState || {},
        this._state.queueItems || []
      );
    }
    _syncMobileUpNextUi(item = null) {
      const buttons = Array.from(this.shadowRoot?.querySelectorAll?.("[data-up-next-inline]") || []);
      const enabled = this._mobileShowUpNextEnabled();
      this._syncTabletAutoFitUi(!!(enabled && item));
      buttons.forEach((button) => {
        const artEl = button.querySelector(".up-next-art");
        const prefixEl = button.querySelector(".up-next-prefix");
        const titleEl = button.querySelector(".up-next-title");
        if (!enabled || !item) {
          button.hidden = true;
          button.dataset.queueItemId = "";
          button.dataset.uri = "";
          button.dataset.type = "";
          button.dataset.sortIndex = "";
          button.title = "";
          if (artEl) artEl.innerHTML = "";
          if (prefixEl) prefixEl.textContent = "";
          if (titleEl) titleEl.textContent = "";
          return;
        }
        const title = this._queueItemPrimaryTitle(item) || this._i18n("ui.up_next_2");
        const art = this._queueItemImageUrl(item, 72);
        button.hidden = false;
        button.dataset.queueItemId = this._getQueueItemKey(item);
        button.dataset.uri = this._getQueueItemUri(item);
        button.dataset.type = item?.media_item?.media_type || item?.media_type || "track";
        button.dataset.sortIndex = Number.isFinite(Number(item?.sort_index)) ? String(item.sort_index) : "";
        button.title = this._i18n("ui.up_next_title", { title });
        if (artEl) {
          const artKey = art || "";
          if (artEl.dataset.artSrc !== artKey) {
            artEl.dataset.artSrc = artKey;
            artEl.innerHTML = art ? this._imgHtml(art, "", { loading: "eager", fetchpriority: "high" }) : `<span class="up-next-art-fallback">${this._iconSvg("tracks")}</span>`;
          }
        }
        if (prefixEl) prefixEl.textContent = this._i18n("ui.up_next_2");
        if (titleEl) titleEl.textContent = title;
      });
    }
    async _playMobileUpNext() {
      const item = this._mobileUpNextItem();
      if (!item) {
        this._toast(this._i18n("ui.no_next_track_in_queue"));
        return;
      }
      const queueItemId = this._getQueueItemKey(item);
      const uri = this._getQueueItemUri(item);
      const mediaType = item?.media_item?.media_type || item?.media_type || "track";
      const sortIndex = Number.isFinite(Number(item?.sort_index)) ? Number(item.sort_index) : "";
      const played = await this._playQueueItem(queueItemId, uri, mediaType, sortIndex);
      if (played) this._toastSuccess(this._i18n("ui.skipped_to_up_next"));
    }
    _miniQueueItemHtml(item) {
      const img = this._queueItemImageUrl(item, 120);
      const artist = item.media_item?.artists?.map((a) => a.name).join(", ") || "";
      const isActive = this._isQueueItemCurrent(item);
      const key = this._getQueueItemKey(item);
      const priority = isActive ? "high" : "low";
      return `
        <div class="mini-queue-item ${isActive ? "active" : ""}" data-uri="${this._esc(item.media_item?.uri || "")}" data-type="track" data-sort-index="${this._esc(item.sort_index ?? "")}" data-queue-item-id="${this._esc(key)}">
          <div class="mini-queue-index">${isActive ? "▶" : item.sort_index ?? ""}</div>
          <div class="mini-queue-thumb">${img ? this._imgHtml(img, "", { loading: isActive ? "eager" : "lazy", fetchpriority: priority }) : "♫"}</div>
          <div class="mini-queue-meta">
            <div class="mini-queue-name">${this._esc(item.media_item?.name || item.name || "")}</div>
            <div class="mini-queue-artist">${this._esc(artist)}</div>
          </div>
          <div class="mini-queue-actions">
            <button class="chip-btn" data-queue-action="up" data-queue-item-id="${this._esc(key)}">↑</button>
            <button class="chip-btn" data-queue-action="next" data-queue-item-id="${this._esc(key)}">⏭</button>
            <button class="chip-btn" data-queue-action="down" data-queue-item-id="${this._esc(key)}">↓</button>
            <button class="chip-btn warn" data-queue-action="remove" data-queue-item-id="${this._esc(key)}">✕</button>
          </div>
        </div>`;
    }
    async _handleContentClick(e) {
      this._dismissCtx();
      const queueActionBtn = e.target.closest("[data-queue-action]");
      if (queueActionBtn) {
        e.stopPropagation();
        const queueRow = queueActionBtn.closest("[data-queue-item-id]");
        const action = queueActionBtn.dataset.queueAction;
        const targetPosition = action === "move_to" ? this._queueMoveTargetFromElement(queueActionBtn) : null;
        if (action === "move_to" && !targetPosition) return;
        await this._handleQueueAction(
          action,
          queueActionBtn.dataset.queueItemId || queueRow?.dataset.queueItemId || "",
          queueRow?.dataset.uri || "",
          queueRow?.dataset.sortIndex || "",
          targetPosition
        );
        return;
      }
      const secBtn = e.target.closest('[data-action="play-all"], [data-action="shuffle-all"]');
      if (secBtn) {
        let items = [];
        try {
          items = JSON.parse(secBtn.dataset.items || "[]");
        } catch (_) {
          items = [];
        }
        if (!Array.isArray(items) || !items.length) {
          this._toastError(this._i18n("ui.no_content_found"));
          return;
        }
        const shuffle = secBtn.dataset.action === "shuffle-all";
        await this._playAll(items, shuffle);
        return;
      }
      const mediaCard = e.target.closest(".media-card");
      if (mediaCard?.dataset.uri) {
        await this._playMedia(mediaCard.dataset.uri, mediaCard.dataset.type || "album");
        return;
      }
      const trackRow = e.target.closest(".track-row");
      if (trackRow?.dataset.uri) {
        await this._playMedia(trackRow.dataset.uri, "track");
        return;
      }
      const miniQueueItem = e.target.closest(".mini-queue-item");
      if (miniQueueItem?.dataset.queueItemId || miniQueueItem?.dataset.uri) {
        await this._playQueueItem(
          miniQueueItem.dataset.queueItemId,
          miniQueueItem.dataset.uri,
          miniQueueItem.dataset.type || "track",
          miniQueueItem.dataset.sortIndex || ""
        );
      }
    }
    async _handleQueuePanelClick(e) {
      const queueActionBtn = e.target.closest("[data-queue-action]");
      if (queueActionBtn) {
        e.preventDefault();
        e.stopPropagation();
        const queueRow = queueActionBtn.closest("[data-queue-item-id]");
        const action = queueActionBtn.dataset.queueAction;
        const targetPosition = action === "move_to" ? this._queueMoveTargetFromElement(queueActionBtn) : null;
        if (action === "move_to" && !targetPosition) return;
        await this._handleQueueAction(
          action,
          queueActionBtn.dataset.queueItemId || queueRow?.dataset.queueItemId || "",
          queueRow?.dataset.uri || "",
          queueRow?.dataset.sortIndex || "",
          targetPosition
        );
        return;
      }
      const queueItem = e.target.closest(".queue-item");
      if (queueItem?.dataset.queueItemId || queueItem?.dataset.uri) {
        await this._playQueueItem(
          queueItem.dataset.queueItemId,
          queueItem.dataset.uri,
          queueItem.dataset.type || "track",
          queueItem.dataset.sortIndex || ""
        );
      }
    }
    _handleContentContext(e) {
      if (this._isHotelMode()) return;
      const mediaCard = e.target.closest(".media-card");
      const trackRow = e.target.closest(".track-row");
      const el = mediaCard || trackRow;
      if (!el || !el.dataset.uri) return;
      e.preventDefault();
      this._showCtxMenu(e.clientX, e.clientY, el.dataset.uri, el.dataset.type || "album");
    }
    _handleDocumentClick(event) {
      if (!this._ctxMenu) return;
      if (Date.now() - (this._ctxMenuOpenedAt || 0) < 180) return;
      const path = event.composedPath ? event.composedPath() : [];
      if (!path.includes(this._ctxMenu)) this._dismissCtx();
    }
    _showCtxMenu(clientX, clientY, uri, type) {
      if (this._isHotelMode()) return;
      this._dismissCtx();
      const card = this.shadowRoot.querySelector(".card");
      const menu = document.createElement("div");
      menu.className = "ctx-menu";
      menu.innerHTML = `
        <div class="ctx-item" data-enqueue="play"><span class="ctx-ico">▶</span><span>${this._esc(this._i18n("ui.play_now"))}</span></div>
        <div class="ctx-item" data-enqueue="shuffle"><span class="ctx-ico">⇄</span><span>${this._esc(this._i18n("ui.shuffle_play"))}</span></div>
        <div class="ctx-item" data-enqueue="next"><span class="ctx-ico">⏭</span><span>${this._esc(this._i18n("ui.play_next"))}</span></div>
        <div class="ctx-item" data-enqueue="add"><span class="ctx-ico">＋</span><span>${this._esc(this._i18n("ui.add_to_queue"))}</span></div>`;
      menu.querySelectorAll(".ctx-item").forEach((item) => item.addEventListener("click", async (e) => {
        e.stopPropagation();
        await this._playMedia(uri, type, item.dataset.enqueue);
        this._dismissCtx();
      }));
      card.appendChild(menu);
      this._ctxMenu = menu;
      const cardRect = card.getBoundingClientRect();
      let left = clientX - cardRect.left;
      let top = clientY - cardRect.top;
      menu.style.left = `${left}px`;
      menu.style.top = `${top}px`;
      requestAnimationFrame(() => {
        const mr = menu.getBoundingClientRect();
        const cr = card.getBoundingClientRect();
        if (mr.right > cr.right) left -= mr.width;
        if (mr.bottom > cr.bottom) top -= mr.height;
        menu.style.left = `${Math.max(8, left)}px`;
        menu.style.top = `${Math.max(8, top)}px`;
      });
    }
    _dismissCtx() {
      if (this._ctxMenu) {
        this._ctxMenu.remove();
        this._ctxMenu = null;
      }
    }
    _hasService(domain, service) {
      return !!this._hass?.services?.[domain]?.[service];
    }
    _hasMusicAssistantBackend() {
      return this._hasService("music_assistant", "play_media") || this._hasDirectMAConnection() || !!String(this._resolvedConfigEntryId || this._config?.config_entry_id || "").trim();
    }
    _hasMassQueueService(service) {
      return this._hasService("mass_queue", service);
    }
    _setQueueBusy(isBusy) {
      const busy = !!isBusy;
      this._state.queueActionPending = busy;
      this.shadowRoot.querySelectorAll("[data-queue-action]").forEach((btn) => {
        btn.disabled = busy;
      });
    }
    _isQueueUiVisible() {
      return !!(this._state.queueVisible || this._state.view === "now_playing" || this._state.menuOpen && this._state.menuPage === "queue");
    }
    async _refreshQueueAfterMutation(delay = 160) {
      if (delay > 0) {
        await new Promise((resolve) => window.setTimeout(resolve, delay));
      }
      if (!this.isConnected) return;
      const previousQueueSignature = this._queueRenderSignature();
      await this._ensureQueueSnapshot(true);
      if (!this.isConnected) return;
      const nextQueueSignature = this._queueRenderSignature();
      if (this._state.view === "now_playing") this._renderNowPlayingPage();
      if (this._state.queueVisible) await this._renderQueueItems();
      if (this._state.menuOpen && this._state.menuPage === "queue" && nextQueueSignature !== previousQueueSignature) {
        await this._renderMobileMenu();
      }
    }
    _queueItemsContainCurrent(items = [], queueState = this._state.maQueueState) {
      return HomeiiMediaQueueFoundation2.queueItemsContainCurrent(items, queueState);
    }
    _queueItemMatchesPlayer(item, player = this._getSelectedPlayer()) {
      return HomeiiMediaQueueFoundation2.queueItemMatchesPlayer(
        item,
        player,
        (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
      );
    }
    _applyQueueSnapshot(queueState, items = [], force = false) {
      if (!queueState) return;
      const normalizedState = { ...queueState };
      const pendingState = HomeiiNowPlayingFoundation2.pendingQueuePlayState(this._state, Date.now());
      const { hasPendingPlay, pendingKey, pendingUri, pendingIndex } = pendingState;
      const shouldConsiderReplace = force || !this._state.queueItems.length || this._isQueueUiVisible();
      const nextItems = this._queueItemsWithSequentialSortIndexes(Array.isArray(items) ? items : []);
      let freezeQueueItemsForPendingPlay = false;
      if (nextItems.length) {
        const currentIndex = Number(normalizedState.current_index);
        const currentKey = this._getQueueItemKey(normalizedState.current_item);
        const currentUri = this._getQueueItemUri(normalizedState.current_item);
        const currentFromItems = nextItems.find(
          (item) => Number.isFinite(currentIndex) && Number(item?.sort_index) === currentIndex || currentKey && this._getQueueItemKey(item) === currentKey || currentUri && this._mediaRefsEquivalent(this._getQueueItemUri(item), currentUri, item?.media_item?.media_type || item?.media_type || "track")
        );
        if (currentFromItems) normalizedState.current_item = currentFromItems;
        const normalizedIndex = Number(normalizedState.current_index);
        if (Number.isFinite(normalizedIndex)) {
          const nextFromItems = nextItems.find((item) => Number(item?.sort_index) === normalizedIndex + 1);
          if (nextFromItems) normalizedState.next_item = nextFromItems;
        }
      }
      if (nextItems.length) {
        const player = this._getSelectedPlayer();
        const playerUri = String(player?.attributes?.media_content_id || "").trim();
        const playerTitle = String(player?.attributes?.media_title || "").trim().toLowerCase();
        const playerArtist = String(player?.attributes?.media_artist || "").trim().toLowerCase();
        let playerMatch = null;
        if (playerUri) {
          playerMatch = nextItems.find((item) => this._mediaRefsEquivalent(this._getQueueItemUri(item), playerUri, item?.media_item?.media_type || item?.media_type || "track"));
        }
        if (!playerMatch && playerTitle) {
          playerMatch = nextItems.find((item) => {
            const title = String(item?.media_item?.name || item?.media_title || item?.name || "").trim().toLowerCase();
            const artists = String(item?.media_artist || (item?.media_item?.artists || []).map((artist) => artist?.name).filter(Boolean).join(", ") || "").trim().toLowerCase();
            return title === playerTitle && (!playerArtist || artists.includes(playerArtist) || playerArtist.includes(artists));
          });
        }
        if (playerMatch) {
          const incomingTitle = String(normalizedState.current_item?.media_item?.name || normalizedState.current_item?.media_title || normalizedState.current_item?.name || "").trim().toLowerCase();
          const incomingUri = String(this._getQueueItemUri(normalizedState.current_item) || "").trim();
          const playerAligned = playerUri ? this._mediaRefsEquivalent(incomingUri, playerUri, normalizedState.current_item?.media_item?.media_type || normalizedState.current_item?.media_type || "track") : incomingTitle === playerTitle;
          if (!playerAligned) {
            const matchedIndex = Number(playerMatch.sort_index);
            normalizedState.current_item = playerMatch;
            if (Number.isFinite(matchedIndex)) {
              normalizedState.current_index = matchedIndex;
              normalizedState.next_item = nextItems.find((item) => Number(item?.sort_index) === matchedIndex + 1) || normalizedState.next_item || null;
            }
          }
        }
      }
      if (hasPendingPlay) {
        const incomingKey = this._getQueueItemKey(normalizedState.current_item);
        const incomingUri = this._getQueueItemUri(normalizedState.current_item);
        const incomingIndex = Number(normalizedState.current_index);
        const hasPendingPosition = !!pendingKey || Number.isFinite(pendingIndex);
        const matchesPending = pendingKey && incomingKey === pendingKey || Number.isFinite(pendingIndex) && Number.isFinite(incomingIndex) && pendingIndex === incomingIndex || !hasPendingPosition && pendingUri && this._mediaRefsEquivalent(incomingUri, pendingUri, normalizedState.current_item?.media_item?.media_type || normalizedState.current_item?.media_type || "track");
        if (!matchesPending && this._state.maQueueState?.current_item) {
          normalizedState.current_index = this._state.maQueueState.current_index;
          normalizedState.current_item = this._state.maQueueState.current_item;
          normalizedState.next_item = this._state.maQueueState.next_item;
          freezeQueueItemsForPendingPlay = true;
        } else if (matchesPending) {
          const playerCaughtUp = this._queueItemMatchesPlayer(normalizedState.current_item);
          if (playerCaughtUp) {
            Object.assign(this._state, HomeiiNowPlayingFoundation2.clearPendingQueuePlayPatch());
          }
        }
      }
      this._state.maQueueState = normalizedState;
      if (!shouldConsiderReplace || !nextItems.length) return;
      if (freezeQueueItemsForPendingPlay) return;
      const existingItems = Array.isArray(this._state.queueItems) ? this._state.queueItems : [];
      const pendingMutationUntil = Number(this._state.queueMutationPendingUntil || 0);
      const pendingMutationSignature = String(this._state.queueMutationExpectedSignature || "");
      if (pendingMutationUntil > Date.now() && pendingMutationSignature) {
        const incomingSignature = this._queueOrderSignature(nextItems);
        if (incomingSignature !== pendingMutationSignature) return;
        this._state.queueMutationPendingUntil = 0;
        this._state.queueMutationExpectedSignature = "";
      }
      const totalItems = Number(queueState?.items);
      const looksPartial = Number.isFinite(totalItems) && totalItems > 0 && nextItems.length < Math.min(totalItems, 25);
      const existingLooksBetter = existingItems.length > nextItems.length && this._queueItemsContainCurrent(existingItems, normalizedState);
      if (looksPartial && existingLooksBetter) return;
      this._state.queueItems = nextItems;
      if (typeof this._prefetchQueueArtworkWindow === "function") {
        this._prefetchQueueArtworkWindow(nextItems, { immediate: false, before: 2, after: 14 });
      }
    }
    async _callMassQueueService(service, queueItemId) {
      const player = this._getSelectedPlayer();
      if (!player || !queueItemId || !this._hasMassQueueService(service)) return false;
      const itemId = String(queueItemId);
      const playerId = this._directMaPlayerId(player) || player.attributes?.mass_player_id || player.attributes?.player_id || player.entity_id;
      const queueId = player.attributes?.active_queue || player.attributes?.queue_id || this._state.maQueueState?.queue_id || this._directMaQueueId(player);
      const attempts = [
        { entity: player.entity_id, queue_item_id: itemId },
        { entity_id: player.entity_id, queue_item_id: itemId },
        { player_id: playerId, queue_item_id: itemId },
        { queue_id: queueId, queue_item_id: itemId },
        { entity: player.entity_id, item_id: itemId },
        { entity_id: player.entity_id, item_id: itemId },
        { player_id: playerId, item_id: itemId },
        { queue_id: queueId, item_id: itemId }
      ].map((payload) => Object.fromEntries(Object.entries(payload).filter(([, value]) => value !== void 0 && value !== null && value !== ""))).filter((payload, index, all) => all.findIndex((entry) => JSON.stringify(entry) === JSON.stringify(payload)) === index);
      for (const payload of attempts) {
        try {
          await this._hass.callService("mass_queue", service, payload);
          return true;
        } catch (_) {
        }
      }
      return false;
    }
    async _callMassQueueCommand(command, data = {}) {
      if (String(command || "").startsWith("player_queues/")) return false;
      if (this._config?.use_mass_queue_send_command !== true) return false;
      if (!command || !this._hasMassQueueService("send_command")) return false;
      const configEntryId = await this._ensureConfigEntryId();
      if (!configEntryId) return false;
      try {
        await this._hass.callService("mass_queue", "send_command", {
          config_entry_id: configEntryId,
          command,
          data
        });
        return true;
      } catch (_) {
        return false;
      }
    }
    _queueIdForPlayer(player) {
      return String(
        player?.attributes?.active_queue || player?.attributes?.queue_id || this._state.maQueueState?.queue_id || this._directMaQueueId(player) || ""
      ).trim();
    }
    async _callDirectMaQueueAction(action, queueId, queueItemIdOrIndex, explicitPosShift = null) {
      if (!queueId || queueItemIdOrIndex === "" || queueItemIdOrIndex === null || queueItemIdOrIndex === void 0 || !this._hasDirectMAConnection()) return false;
      if (action === "remove") {
        await this._callDirectMaCommand("player_queues/delete_item", {
          queue_id: queueId,
          item_id_or_index: queueItemIdOrIndex
        });
        return true;
      }
      const posShiftByAction = {
        up: -1,
        down: 1,
        next: 0
      };
      if (action === "move_to") {
        const posShift = Math.round(Number(explicitPosShift));
        if (!Number.isFinite(posShift)) return false;
        if (posShift === 0) return true;
        if (typeof queueItemIdOrIndex !== "string" || !queueItemIdOrIndex.trim()) return false;
        await this._callDirectMaCommand("player_queues/move_item", {
          queue_id: queueId,
          queue_item_id: queueItemIdOrIndex,
          pos_shift: posShift
        });
        return true;
      }
      if (!(action in posShiftByAction) || typeof queueItemIdOrIndex !== "string" || !queueItemIdOrIndex.trim()) return false;
      await this._callDirectMaCommand("player_queues/move_item", {
        queue_id: queueId,
        queue_item_id: queueItemIdOrIndex,
        pos_shift: posShiftByAction[action]
      });
      return true;
    }
    _markMobileQueuePlayPending(item, playIndex) {
      const patch = HomeiiNowPlayingFoundation2.buildPendingQueuePlayPatch({
        item,
        playIndex,
        selectedPlayerId: this._state.selectedPlayer || this._getSelectedPlayer()?.entity_id || "",
        accessors: {
          getQueueItemPlaybackId: (entry) => this._getQueueItemPlaybackId(entry),
          getQueueItemStableId: (entry) => this._getQueueItemStableId(entry),
          getQueueItemKey: (entry) => this._getQueueItemKey(entry),
          getQueueItemUri: (entry) => this._getQueueItemUri(entry)
        }
      });
      if (patch) Object.assign(this._state, patch);
    }
    _clearMobileQueuePlayPending() {
      Object.assign(this._state, HomeiiNowPlayingFoundation2.clearPendingQueuePlayPatch());
    }
    _resolveQueuePlayIndex(queueItemId = "", fallbackUri = "", explicitSortIndex = "") {
      return HomeiiMediaQueueFoundation2.resolveQueuePlayIndex(
        this._state.queueItems || [],
        { queueItemId, fallbackUri, explicitSortIndex },
        (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
      );
    }
    _getQueueItemByIndexOrKey(sortIndex = "", queueItemId = "", fallbackUri = "") {
      return HomeiiMediaQueueFoundation2.getQueueItemByIndexOrKey(
        this._state.queueItems || [],
        { sortIndex, queueItemId, fallbackUri },
        (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
      );
    }
    async _playQueueItem(queueItemId, fallbackUri = "", mediaType = "track", sortIndex = "") {
      if (!queueItemId && !fallbackUri || this._state.queueActionPending) return false;
      this._setQueueBusy(true);
      try {
        const player = this._getSelectedPlayer();
        const playIndex = this._resolveQueuePlayIndex(queueItemId, fallbackUri, sortIndex);
        const currentContextBeforePlay = this._mobileArtStackContext();
        const currentSortIndexBeforePlay = Number(currentContextBeforePlay.queueItems?.[currentContextBeforePlay.baseIndex]?.sort_index);
        const currentForStepBeforePlay = Number.isFinite(currentSortIndexBeforePlay) ? currentSortIndexBeforePlay : currentContextBeforePlay.baseIndex;
        const optimisticItem = this._getQueueItemByIndexOrKey(playIndex, queueItemId, fallbackUri);
        if (optimisticItem && Number.isFinite(playIndex)) {
          const optimisticUntil = Date.now() + 8500;
          this._markMobileQueuePlayPending(optimisticItem, playIndex);
          this._state.maQueueState = {
            ...this._state.maQueueState || {},
            current_index: playIndex,
            current_item: optimisticItem,
            next_item: (this._state.queueItems || []).find((item) => Number(item?.sort_index) === playIndex + 1) || null,
            elapsed_time: 0,
            elapsed_time_last_updated: (/* @__PURE__ */ new Date()).toISOString()
          };
          this._state.mobileArtBrowseOffset = 0;
          if (typeof this._preloadPendingMobileQueueItemArtwork === "function") this._preloadPendingMobileQueueItemArtwork(optimisticItem);
          if (player?.entity_id) this._setPlayerPlaybackOptimistic(player.entity_id, { state: "playing", until: optimisticUntil });
          else this._syncNowPlayingUI();
        }
        const queueId = player?.attributes?.active_queue || player?.attributes?.queue_id || this._state.maQueueState?.queue_id || this._directMaQueueId(player);
        const queueItemPlaybackId = optimisticItem ? this._getQueueItemPlaybackId(optimisticItem) : "";
        const directPlayTarget = queueItemPlaybackId || (Number.isFinite(playIndex) ? playIndex : null);
        this._debugLog("info", "[Homeii Queue] play item", {
          queueId,
          queueItemId,
          fallbackUri,
          sortIndex,
          playIndex,
          directPlayTarget,
          title: optimisticItem ? this._queueItemPrimaryTitle(optimisticItem) : ""
        });
        if (queueId && directPlayTarget !== null && this._hasDirectMAConnection()) {
          try {
            await this._callDirectMaCommand("player_queues/play_index", { queue_id: queueId, index: directPlayTarget });
            this._refreshQueueAfterMutation(700).catch(() => {
            });
            this._refreshQueueAfterMutation(1450).catch(() => {
            });
            this._refreshQueueAfterMutation(2850).catch(() => {
            });
            return true;
          } catch (_) {
          }
        }
        if (queueId && directPlayTarget !== null) {
          const usedMassCommand = await this._callMassQueueCommand("player_queues/play_index", { queue_id: queueId, index: directPlayTarget });
          if (usedMassCommand) {
            this._refreshQueueAfterMutation(700).catch(() => {
            });
            this._refreshQueueAfterMutation(1450).catch(() => {
            });
            this._refreshQueueAfterMutation(2850).catch(() => {
            });
            return true;
          }
        }
        const queueItemServiceId = optimisticItem ? this._getQueueItemPlaybackId(optimisticItem) || this._getQueueItemStableId(optimisticItem) : "";
        const usedMassQueue = queueItemServiceId ? await this._callMassQueueService("play_queue_item", queueItemServiceId) : false;
        if (usedMassQueue) {
          this._refreshQueueAfterMutation(700).catch(() => {
          });
          this._refreshQueueAfterMutation(1450).catch(() => {
          });
          this._refreshQueueAfterMutation(2850).catch(() => {
          });
          return true;
        }
        const stepDelta = Number.isFinite(playIndex) && Number.isFinite(currentForStepBeforePlay) ? playIndex - currentForStepBeforePlay : NaN;
        if (Number.isFinite(stepDelta) && stepDelta !== 0) {
          const stepped = await this._stepQueueByDelta(stepDelta);
          if (stepped) {
            this._refreshQueueAfterMutation(700).catch(() => {
            });
            this._refreshQueueAfterMutation(1450).catch(() => {
            });
            this._refreshQueueAfterMutation(2850).catch(() => {
            });
            return true;
          }
        }
        if (fallbackUri && !queueItemId && !Number.isFinite(playIndex)) {
          if (!player?.entity_id && !this._state.selectedPlayer) return false;
          await this._hass.callService("media_player", "play_media", {
            entity_id: player?.entity_id || this._state.selectedPlayer,
            media_content_id: fallbackUri,
            media_content_type: mediaType
          });
          this._refreshQueueAfterMutation(900).catch(() => {
          });
          this._refreshQueueAfterMutation(2850).catch(() => {
          });
          return true;
        }
        if (optimisticItem) {
          this._clearMobileQueuePlayPending();
          this._ensureQueueSnapshot(true).catch(() => {
          });
        }
      } catch (e) {
        this._clearMobileQueuePlayPending();
        this._toast(e?.message || this._i18n("ui.queue_action_failed"));
        return false;
      } finally {
        this._setQueueBusy(false);
      }
      return false;
    }
    async _callService(service, data, options = {}) {
      const includeConfigEntryId = options.includeConfigEntryId !== false;
      let serviceData = { ...data };
      if (includeConfigEntryId) {
        const configEntryId = await this._ensureConfigEntryId();
        if (!configEntryId) {
          const error = new Error(this._musicAssistantSetupMessage());
          error.code = "HOMEII_MA_NOT_READY";
          throw error;
        }
        serviceData = { config_entry_id: configEntryId, ...serviceData };
      }
      const request = this._hass.connection.sendMessagePromise({
        type: "call_service",
        domain: "music_assistant",
        service,
        service_data: serviceData,
        return_response: true
      });
      return this._withTimeout(request, this._musicAssistantTimeoutMs(), this._timeoutMessage(`Music Assistant ${service}`));
    }
    async _ensureConfigEntryId(force = false) {
      if (!this._hass) return "";
      const explicit = String(this._config?.config_entry_id || "").trim();
      if (explicit && !force) {
        this._resolvedConfigEntryId = explicit;
        return explicit;
      }
      if (this._resolvedConfigEntryId && !force) return this._resolvedConfigEntryId;
      try {
        const entries = await this._withTimeout(this._hass.connection.sendMessagePromise({
          type: "config_entries/get",
          domain: "music_assistant"
        }), this._musicAssistantTimeoutMs(), this._timeoutMessage("Music Assistant config lookup"));
        const list = Array.isArray(entries) ? entries : [];
        const preferred = list.find((entry) => entry?.state === "loaded") || list.find((entry) => entry?.state === "setup_retry") || list.find((entry) => entry?.state === "not_loaded") || list[0];
        this._resolvedConfigEntryId = preferred?.entry_id || "";
        if (!this._resolvedConfigEntryId || preferred?.state && preferred.state !== "loaded") {
          this._handleMusicAssistantIssue(preferred?.state ? `Music Assistant entry ${preferred.state}` : this._musicAssistantSetupMessage());
        }
        return this._resolvedConfigEntryId;
      } catch (error) {
        this._resolvedConfigEntryId = explicit || "";
        if (!explicit) this._handleMusicAssistantIssue(error);
        return this._resolvedConfigEntryId;
      }
    }
    _normalizeQueueItem(item, fallbackIndex = 0) {
      if (!item || typeof item !== "object") return null;
      const mediaItem = item.media_item || item.media || item.item || item;
      const flatArtist = String(item.media_artist || item.artist || "").trim();
      const artists = Array.isArray(mediaItem?.artists) ? mediaItem.artists : Array.isArray(item.artists) ? item.artists : flatArtist ? flatArtist.split(",").map((name) => ({ name: String(name).trim() })).filter((artist) => artist.name) : [];
      const flatAlbumName = String(item.media_album_name || item.album_name || "").trim();
      const album = mediaItem?.album || item.album || (flatAlbumName ? { name: flatAlbumName } : null);
      const flatImage = item.media_image || item.media_image_url || item.image_url || item.image || item.thumbnail || item.thumb || item.local_image_url || item.local_image || item.local_image_encoded || "";
      const resolvedImage = this._artUrl(item, { size: 420 }) || this._artUrl(mediaItem, { size: 420 }) || this._artUrl(album, { size: 420 }) || this._imageUrl(flatImage, 420) || "";
      const sortIndex = Number(item.sort_index);
      const positionIndex = Number(item.position);
      const mediaUri = mediaItem?.uri || item.uri || item.media_content_id || item.media_id || "";
      const rawQueueItemId = String(item.queue_item_id || item.queueItemId || item.queue_item?.queue_item_id || item.queueItem?.queue_item_id || "").trim();
      const rawItemId = String(item.item_id || item.id || "").trim();
      const parsedMediaRef = HomeiiMediaQueueFoundation2.parseMediaReference(mediaUri, mediaItem?.media_type || item.media_type || "track");
      const rawItemLooksLikeMediaId = !!(rawItemId && (rawItemId === mediaUri || rawItemId === parsedMediaRef.item_id || mediaUri.endsWith(`/${rawItemId}`) || mediaUri.endsWith(`:${rawItemId}`)));
      const queueServiceId = rawQueueItemId || (rawItemId && !rawItemLooksLikeMediaId ? rawItemId : "");
      return {
        ...item,
        media_item: {
          ...mediaItem,
          uri: mediaUri || (rawItemLooksLikeMediaId ? rawItemId : ""),
          name: mediaItem?.name || item.name || item.media_title || "",
          artists,
          album
        },
        image: resolvedImage || item.image || flatImage || mediaItem?.image || album?.image || null,
        image_url: resolvedImage || item.image_url || flatImage || mediaItem?.image_url || mediaItem?.image || album?.image_url || album?.image || null,
        sort_index: Number.isFinite(sortIndex) ? sortIndex : Number.isFinite(positionIndex) ? positionIndex : fallbackIndex,
        queue_item_id: queueServiceId || rawItemId || mediaUri || String(fallbackIndex),
        queue_item_id_trusted: !!queueServiceId,
        queue_service_id: queueServiceId
      };
    }
    _normalizeQueueSnapshot(raw, entityId = "") {
      let src = raw?.response ?? raw;
      if (!src || typeof src !== "object") return null;
      if (entityId && src[entityId] && typeof src[entityId] === "object") {
        src = src[entityId];
      } else if (!src.queue_state && !src.queue && !Array.isArray(src.items) && !src.current_item) {
        const firstEntry = Object.values(src).find((value) => value && typeof value === "object");
        if (firstEntry) src = firstEntry;
      }
      if (!src || typeof src !== "object") return null;
      const queueState = src.queue_state || src.queue || src;
      const previousItems = Array.isArray(src.previous_items) ? src.previous_items : Array.isArray(queueState?.previous_items) ? queueState.previous_items : [];
      const currentItem = src.current_item || queueState?.current_item || null;
      const singleNextItem = src.next_item || queueState?.next_item || null;
      const nextItems = Array.isArray(src.next_items) ? src.next_items : Array.isArray(queueState?.next_items) ? queueState.next_items : [];
      const combinedNextItems = [...nextItems, ...singleNextItem ? [singleNextItem] : []];
      let items = [];
      if (Array.isArray(src.items)) items = src.items;
      else if (Array.isArray(queueState?.items)) items = queueState.items;
      else if (Array.isArray(src.queue_items)) items = src.queue_items;
      else items = [...previousItems, ...currentItem ? [currentItem] : [], ...combinedNextItems];
      const guessedCurrentIndex = Number.isFinite(queueState?.current_index) ? queueState.current_index : Number.isFinite(src.current_index) ? src.current_index : previousItems.length;
      const baseSortIndex = Math.max(0, guessedCurrentIndex - previousItems.length);
      const normalizedItems = items.map((item, index) => this._normalizeQueueItem(item, baseSortIndex + index)).filter(Boolean).map((item, index) => ({
        ...item,
        sort_index: Number.isFinite(item.sort_index) ? item.sort_index : baseSortIndex + index
      }));
      const totalItems = Number.isFinite(queueState?.items) ? queueState.items : Number.isFinite(src.items_count) ? src.items_count : normalizedItems.length;
      return {
        state: {
          ...queueState,
          current_index: guessedCurrentIndex,
          items: totalItems
        },
        items: normalizedItems
      };
    }
    _guessCurrentQueueIndexFromItems(items = [], player = null) {
      if (!Array.isArray(items) || !items.length) return 0;
      const currentTitle = String(player?.attributes?.media_title || "").trim().toLowerCase();
      const currentArtist = String(player?.attributes?.media_artist || "").trim().toLowerCase();
      const idx = items.findIndex((item) => {
        const title = String(item?.media_item?.name || item?.name || "").trim().toLowerCase();
        const artist = String(item?.media_item?.artists?.map((a) => a.name).join(", ") || "").trim().toLowerCase();
        return !!currentTitle && title === currentTitle && (!currentArtist || !artist || artist.includes(currentArtist));
      });
      return idx >= 0 ? items[idx]?.sort_index ?? idx : items[0]?.sort_index ?? 0;
    }
    async _fetchMassQueueItemsSnapshot(player) {
      if (!player || !this._hasMassQueueService("get_queue_items")) return null;
      try {
        const res = await this._hass.connection.sendMessagePromise({
          type: "call_service",
          domain: "mass_queue",
          service: "get_queue_items",
          service_data: {
            entity: player.entity_id,
            limit_before: 20,
            limit_after: 120
          },
          return_response: true
        });
        const raw = res?.response ?? res;
        let items = raw?.items ?? raw?.queue_items ?? raw;
        if (raw?.[player.entity_id]) {
          const scoped = raw[player.entity_id];
          items = scoped?.items ?? scoped?.queue_items ?? scoped;
        }
        if (!Array.isArray(items)) return null;
        const queueState = raw?.queue_state || raw?.queue || raw;
        const rawCurrentIndex = Number(queueState?.current_index ?? raw?.current_index);
        const currentTitle = String(player?.attributes?.media_title || "").trim().toLowerCase();
        const currentArtist = String(player?.attributes?.media_artist || "").trim().toLowerCase();
        const currentOffset = items.findIndex((item) => {
          const mediaItem = item?.media_item || item?.media || item?.item || item;
          const title = String(mediaItem?.name || item?.media_title || item?.name || "").trim().toLowerCase();
          const artist = String(item?.media_artist || (mediaItem?.artists || []).map((a) => a?.name).filter(Boolean).join(", ") || "").trim().toLowerCase();
          return !!currentTitle && title === currentTitle && (!currentArtist || !artist || artist.includes(currentArtist) || currentArtist.includes(artist));
        });
        const baseSortIndex = Number.isFinite(rawCurrentIndex) && currentOffset >= 0 ? Math.max(0, rawCurrentIndex - currentOffset) : 0;
        const normalizedItems = items.map((item, index) => this._normalizeQueueItem(item, baseSortIndex + index)).filter(Boolean).map((item, index) => ({
          ...item,
          sort_index: Number.isFinite(item.sort_index) ? item.sort_index : baseSortIndex + index
        }));
        if (!normalizedItems.length) return null;
        const guessedCurrentIndex = Number.isFinite(rawCurrentIndex) ? rawCurrentIndex : this._guessCurrentQueueIndexFromItems(normalizedItems, player);
        return {
          state: {
            ...queueState,
            current_index: guessedCurrentIndex,
            items: Number.isFinite(Number(queueState?.items)) ? Number(queueState.items) : normalizedItems.length
          },
          items: normalizedItems
        };
      } catch (_) {
        return null;
      }
    }
    async _fetchLibrary(mediaType, orderBy = "sort_name", limit = 500, favoritesOnly = false, search = "") {
      const data = { media_type: mediaType, order_by: orderBy, limit };
      if (favoritesOnly) data.favorite = true;
      if (search) data.search = search;
      try {
        const res = await this._callService("get_library", data);
        const raw = res?.response ?? res;
        this._state.musicAssistantIssueMessage = "";
        return raw?.items ?? (Array.isArray(raw) ? raw : []);
      } catch (error) {
        if (this._isMusicAssistantAvailabilityError(error)) {
          this._handleMusicAssistantIssue(error);
          return [];
        }
        throw error;
      }
    }
    async _getLibrary(mediaType, orderBy = "sort_name", limit = 500, favoritesOnly = false) {
      const key = `${mediaType}:${orderBy}:${limit}:${favoritesOnly}`;
      const ttl = Number(this._config.cache_ttl || 3e5);
      const cached = this._cache.library.get(key);
      if (cached && Date.now() - cached.ts < ttl) return cached.items;
      let items;
      try {
        items = await this._fetchLibrary(mediaType, orderBy, limit, favoritesOnly);
      } catch (error) {
        if (this._isMusicAssistantAvailabilityError(error)) {
          this._handleMusicAssistantIssue(error);
          items = [];
        } else if (orderBy !== "sort_name") items = await this._fetchLibrary(mediaType, "sort_name", limit, favoritesOnly);
        else throw error;
      }
      this._cache.library.set(key, { ts: Date.now(), items });
      return items;
    }
    _mediaTypeCanOpenDetails(mediaType = "") {
      return ["album", "artist", "playlist"].includes(String(mediaType || "").toLowerCase());
    }
    _libraryDetailParentPageForType(mediaType = "") {
      const type = String(mediaType || "").toLowerCase();
      if (type === "album") return "library_albums";
      if (type === "artist") return "library_artists";
      if (type === "playlist") return "library_playlists";
      return "library_playlists";
    }
    _libraryDetailMediaType(detail = this._state?.mobileLibraryDetail || {}) {
      return String(detail?.media_type || detail?.type || "").toLowerCase();
    }
    _isArtistLibraryDetailPage(page = this._state?.menuPage || "") {
      return String(page || "") === "media_detail" && this._libraryDetailMediaType() === "artist";
    }
    _captureCurrentLibraryDetailForStack() {
      const current = this._state?.mobileLibraryDetail;
      if (!current || typeof current !== "object") return null;
      const scrollSnapshot = this._captureMobileMenuScroll?.("media_detail") || current._homeiiScrollSnapshot || null;
      return {
        ...current,
        _homeiiParentPage: this._state.mobileLibraryDetailParentPage || "",
        _homeiiScrollSnapshot: scrollSnapshot
      };
    }
    _pushCurrentLibraryDetailStack() {
      const current = this._captureCurrentLibraryDetailForStack();
      if (!current) return;
      const stack = Array.isArray(this._state.mobileLibraryDetailStack) ? this._state.mobileLibraryDetailStack.slice() : [];
      stack.push(current);
      this._state.mobileLibraryDetailStack = stack.slice(-8);
    }
    _clearLibraryDetailStack() {
      this._state.mobileLibraryDetailStack = [];
    }
    _findAlbumBrowseIndex(albums = [], entry = {}) {
      const entryUri = String(entry?.uri || entry?.media_item?.uri || "").trim().toLowerCase();
      const entryName = HomeiiMediaQueueFoundation2.normalizeComparableText(entry?.name || entry?.title || entry?.media_item?.name || "");
      const entryYear = this._mediaYearValue(entry) || "";
      const uriIndex = entryUri ? albums.findIndex((album) => String(album?.uri || album?.media_item?.uri || "").trim().toLowerCase() === entryUri) : -1;
      if (uriIndex >= 0) return uriIndex;
      return albums.findIndex((album) => {
        const albumName = HomeiiMediaQueueFoundation2.normalizeComparableText(album?.name || album?.title || album?.media_item?.name || "");
        const albumYear = this._mediaYearValue(album) || "";
        return albumName && albumName === entryName && (!entryYear || !albumYear || entryYear === albumYear);
      });
    }
    _albumBrowseContextFromDetail(detail = {}, entry = {}) {
      if (this._libraryDetailMediaType(detail) !== "artist") return null;
      const albums = Array.isArray(detail?.albums) ? detail.albums.filter(Boolean) : [];
      if (!albums.length) return null;
      const index = this._findAlbumBrowseIndex(albums, entry);
      const artistInfo = detail.artistInfo || detail;
      return {
        source: "artist",
        artistName: artistInfo?.name || detail?.name || "",
        artistUri: artistInfo?.uri || detail?.uri || "",
        albums,
        index: index >= 0 ? index : 0
      };
    }
    _albumBrowseState(detail = {}) {
      const context = detail?.albumBrowseContext || null;
      const albums = Array.isArray(context?.albums) ? context.albums.filter(Boolean) : [];
      if (!albums.length) return null;
      let index = Number(context?.index);
      if (!Number.isInteger(index) || index < 0 || index >= albums.length) {
        index = this._findAlbumBrowseIndex(albums, detail);
      }
      if (!Number.isInteger(index) || index < 0) index = 0;
      return {
        ...context,
        albums,
        index,
        total: albums.length,
        canPrev: index > 0,
        canNext: index < albums.length - 1
      };
    }
    _libraryMediaDetailCommandArgs(entry = {}, mediaType = "album") {
      return this._libraryMediaDetailCommandArgsList(entry, mediaType)[0] || null;
    }
    _libraryMediaDetailCommandArgsList(entry = {}, mediaType = "album") {
      const type = String(mediaType || entry?.media_type || entry?.type || "album").toLowerCase();
      const mediaItem = entry?.media_item || {};
      const mapping = this._searchItemProviderMapping(entry);
      const uri = String(entry?.uri || mediaItem?.uri || "").trim();
      const parsed = this._parseMediaReference(uri, type);
      const mappings = [].concat(Array.isArray(entry?.provider_mappings) ? entry.provider_mappings : []).concat(Array.isArray(mediaItem?.provider_mappings) ? mediaItem.provider_mappings : []).concat(mapping && typeof mapping === "object" ? [mapping] : []);
      const candidates = [];
      const add = (itemIdValue, providerValue) => {
        const itemId = String(itemIdValue || "").trim();
        const provider = String(providerValue || "").trim();
        if (!itemId || !provider) return;
        const key = `${provider}::${itemId}`.toLowerCase();
        if (candidates.some((candidate) => candidate.key === key)) return;
        candidates.push({ key, item_id: itemId, provider });
      };
      add(entry?.item_id || entry?.id || mediaItem?.item_id || mediaItem?.id, entry?.provider_instance || mediaItem?.provider_instance || entry?.provider_domain || mediaItem?.provider_domain || entry?.provider || mediaItem?.provider);
      add(entry?.item_id || entry?.id || mediaItem?.item_id || mediaItem?.id, "library");
      add(parsed?.item_id, parsed?.provider);
      add(parsed?.item_id, "library");
      add(mapping?.item_id || mapping?.provider_item_id, mapping?.provider_instance || mapping?.provider_domain || mapping?.provider || mapping?.provider_id);
      mappings.forEach((mapping2) => {
        const itemId = mapping2?.item_id || mapping2?.provider_item_id || mapping2?.media_item?.item_id || "";
        const mappedRef = this._parseMediaReference(mapping2?.uri || mapping2?.media_item?.uri || "", type);
        add(itemId, mapping2?.provider_instance);
        add(itemId, mapping2?.provider_domain);
        add(itemId, mapping2?.provider);
        add(itemId, mapping2?.provider_id);
        add(mappedRef?.item_id, mappedRef?.provider);
        add(mappedRef?.item_id, mapping2?.provider_instance || mapping2?.provider_domain || mapping2?.provider || mapping2?.provider_id);
      });
      return candidates.map(({ item_id, provider }) => ({ item_id, provider }));
    }
    _normalizeLibraryDetailTrack(raw = {}, forceTrack = false) {
      if (!raw || typeof raw !== "object") return null;
      const mediaItem = raw.media_item || raw.item || raw;
      if (!mediaItem || typeof mediaItem !== "object") return null;
      const rawType = String(
        mediaItem.media_type || mediaItem.type || mediaItem.media_content_type || mediaItem.media_class || raw.media_type || raw.type || raw.media_content_type || raw.media_class || ""
      ).toLowerCase();
      if (rawType && !["track", "song", "music", "album_track", "albumtrack"].includes(rawType)) return null;
      if (!forceTrack && !rawType && !mediaItem.uri && !mediaItem.media_content_id && !mediaItem.item_id) return null;
      const normalized = this._normalizeSearchItem(mediaItem, "track");
      const uri = String(normalized?.uri || mediaItem.uri || mediaItem.media_content_id || raw.uri || raw.media_content_id || "").trim();
      const name = normalized?.name || mediaItem.name || mediaItem.title || raw.name || raw.title || raw.media_title || "";
      if (!uri && !name) return null;
      const artists = Array.isArray(normalized?.artists) ? normalized.artists : Array.isArray(mediaItem.artists) ? mediaItem.artists : Array.isArray(raw.artists) ? raw.artists : [];
      const artistStr = normalized?.artist_str || mediaItem.artist_str || mediaItem.artist || raw.artist_str || raw.artist || raw.media_artist || artists.map((artist) => artist?.name || artist).filter(Boolean).join(", ");
      const albumValue = normalized?.album || mediaItem.album || raw.album || {};
      const album = typeof albumValue === "string" ? { name: albumValue } : albumValue;
      const image = this._artUrl(normalized) || this._artUrl(mediaItem) || this._artUrl(raw) || "";
      return {
        ...normalized,
        media_type: "track",
        uri,
        name,
        artists,
        artist_str: artistStr,
        album,
        image: normalized?.image || normalized?.image_url || mediaItem.image || mediaItem.image_url || mediaItem.thumbnail || raw.image || raw.image_url || raw.thumbnail || image,
        image_url: normalized?.image_url || normalized?.image || mediaItem.image_url || mediaItem.image || mediaItem.thumbnail || raw.image_url || raw.image || raw.thumbnail || image
      };
    }
    _libraryMediaDetailTracksFromPayload(payload, options = {}) {
      const tracks = [];
      const seen = /* @__PURE__ */ new Set();
      const addTrack = (raw, forceTrack = false) => {
        const track = this._normalizeLibraryDetailTrack(raw, forceTrack);
        if (!track) return false;
        const key = String(track.uri || `${track.name}:${track.artist_str || ""}`).trim().toLowerCase();
        if (!key || seen.has(key)) return true;
        seen.add(key);
        tracks.push(track);
        return true;
      };
      const collect = (value, forceTrack = false, depth = 0) => {
        if (value === null || value === void 0 || depth > 5) return;
        if (Array.isArray(value)) {
          value.forEach((item) => {
            if (!addTrack(item, forceTrack)) collect(item, forceTrack, depth + 1);
          });
          return;
        }
        if (typeof value !== "object") return;
        if (addTrack(value, forceTrack)) return;
        ["tracks", "playlist_tracks", "album_tracks", "items", "media_items", "children", "contents", "result", "response"].forEach((key) => {
          if (!(key in value)) return;
          collect(value[key], forceTrack || key.includes("track"), depth + 1);
        });
      };
      collect(payload?.result ?? payload?.response ?? payload, !!options.forceTrackItems, 0);
      return tracks;
    }
    _sortLibraryDetailTracks(tracks = []) {
      return (Array.isArray(tracks) ? [...tracks] : []).sort((left, right) => {
        const leftDisc = Number(left?.disc_number ?? left?.discNumber ?? left?.media_item?.disc_number ?? 0);
        const rightDisc = Number(right?.disc_number ?? right?.discNumber ?? right?.media_item?.disc_number ?? 0);
        if (Number.isFinite(leftDisc) && Number.isFinite(rightDisc) && leftDisc !== rightDisc) return leftDisc - rightDisc;
        const leftTrack = Number(left?.track_number ?? left?.trackNumber ?? left?.media_item?.track_number ?? left?.sort_index ?? 0);
        const rightTrack = Number(right?.track_number ?? right?.trackNumber ?? right?.media_item?.track_number ?? right?.sort_index ?? 0);
        if (Number.isFinite(leftTrack) && Number.isFinite(rightTrack) && leftTrack !== rightTrack) return leftTrack - rightTrack;
        return String(left?.name || "").localeCompare(String(right?.name || ""), this._isHebrew() ? "he" : "en", { sensitivity: "base", numeric: true });
      });
    }
    _libraryTrackMatchesAlbum(track = {}, entry = {}) {
      const albumName = HomeiiMediaQueueFoundation2.normalizeComparableText(entry?.name || entry?.title || "");
      if (!albumName) return true;
      const albumValue = track?.album || track?.media_item?.album || {};
      const trackAlbumName = HomeiiMediaQueueFoundation2.normalizeComparableText(
        typeof albumValue === "string" ? albumValue : albumValue?.name || track?.album_name || track?.media_album_name || ""
      );
      if (!trackAlbumName) return false;
      if (trackAlbumName === albumName) return true;
      return albumName.length >= 5 && (trackAlbumName.includes(albumName) || albumName.includes(trackAlbumName));
    }
    _libraryTrackMatchesArtist(track = {}, entry = {}) {
      const artistName2 = HomeiiMediaQueueFoundation2.normalizeComparableText(entry?.artist || entry?.artist_str || this._artistName(entry) || "");
      if (!artistName2) return true;
      const artists = [
        this._artistName(track),
        track?.artist,
        track?.artist_str,
        track?.media_artist,
        ...Array.isArray(track?.artists) ? track.artists.map((artist) => artist?.name || artist) : [],
        ...Array.isArray(track?.media_item?.artists) ? track.media_item.artists.map((artist) => artist?.name || artist) : []
      ];
      return artists.some((artist) => {
        const normalized = HomeiiMediaQueueFoundation2.normalizeComparableText(artist);
        return normalized && (normalized === artistName2 || normalized.includes(artistName2) || artistName2.includes(normalized));
      });
    }
    async _loadLibraryMediaDetailTracksViaLibrarySearch(entry = {}) {
      const queries = [
        entry?.name,
        [entry?.artist || entry?.artist_str || this._artistName(entry), entry?.name].filter(Boolean).join(" ")
      ].map((query) => String(query || "").trim()).filter(Boolean);
      const uniqueQueries = [...new Set(queries)];
      const collected = [];
      for (const query of uniqueQueries) {
        try {
          const rawTracks = await this._fetchLibrary("track", "sort_name", 1e3, false, query);
          collected.push(...Array.isArray(rawTracks) ? rawTracks : []);
        } catch (_) {
        }
      }
      const tracks = collected.map((item) => this._normalizeLibraryDetailTrack(item, true)).filter((track) => track?.uri && this._libraryTrackMatchesAlbum(track, entry) && this._libraryTrackMatchesArtist(track, entry));
      return this._sortLibraryDetailTracks(tracks);
    }
    _libraryMediaItemsFromPayload(payload, fallbackType = "album") {
      const mediaType = String(fallbackType || "album").toLowerCase();
      const items = [];
      const seen = /* @__PURE__ */ new Set();
      const addItem = (raw) => {
        if (!raw || typeof raw !== "object") return false;
        const mediaItem = raw.media_item || raw.item || raw;
        if (!mediaItem || typeof mediaItem !== "object") return false;
        const rawType = String(
          mediaItem.media_type || mediaItem.type || mediaItem.media_content_type || mediaItem.media_class || raw.media_type || raw.type || raw.media_content_type || raw.media_class || ""
        ).toLowerCase();
        if (rawType && rawType !== mediaType) return false;
        const normalized = this._normalizeSearchItem(mediaItem, mediaType);
        const resolvedType = String(normalized?.media_type || mediaType).toLowerCase();
        if (resolvedType !== mediaType) return false;
        const uri = String(normalized?.uri || mediaItem.uri || mediaItem.media_content_id || raw.uri || raw.media_content_id || "").trim();
        const name = String(normalized?.name || normalized?.title || mediaItem.name || mediaItem.title || raw.name || raw.title || "").trim();
        if (!uri && !name) return false;
        const key = String(uri || `${name}:${normalized?.provider || mediaItem.provider || ""}`).toLowerCase();
        if (seen.has(key)) return true;
        seen.add(key);
        items.push({
          ...normalized,
          media_type: mediaType,
          uri,
          name,
          image: normalized?.image || normalized?.image_url || mediaItem.image || mediaItem.image_url || raw.image || raw.image_url || "",
          image_url: normalized?.image_url || normalized?.image || mediaItem.image_url || mediaItem.image || raw.image_url || raw.image || ""
        });
        return true;
      };
      const collect = (value, forceItems = false, depth = 0) => {
        if (value === null || value === void 0 || depth > 5) return;
        if (Array.isArray(value)) {
          value.forEach((item) => {
            if (!addItem(item)) collect(item, forceItems, depth + 1);
          });
          return;
        }
        if (typeof value !== "object") return;
        if (forceItems && addItem(value)) return;
        const keys = mediaType === "album" ? ["albums", "album", "items", "media_items", "children", "contents", "result", "response"] : ["playlists", "playlist", "items", "media_items", "children", "contents", "result", "response"];
        keys.forEach((key) => {
          if (key in value) collect(value[key], ["albums", "album", "playlists", "playlist", "items", "media_items"].includes(key), depth + 1);
        });
      };
      collect(payload?.result ?? payload?.response ?? payload, false, 0);
      return items;
    }
    _mediaYearValue(item = {}) {
      const metadata = item?.metadata || item?.media_item?.metadata || {};
      const album = item?.album || item?.media_item?.album || {};
      const candidates = [
        item.year,
        item.release_year,
        item.releaseYear,
        item.date,
        item.release_date,
        item.original_date,
        album.year,
        album.release_year,
        album.release_date,
        metadata.year,
        metadata.release_year,
        metadata.releaseYear,
        metadata.release_date,
        metadata.original_date
      ];
      for (const candidate of candidates) {
        const text = String(candidate ?? "").trim();
        const match = text.match(/\b(19|20)\d{2}\b/);
        const year = match ? Number(match[0]) : Number(candidate);
        if (Number.isInteger(year) && year >= 1900 && year <= 2100) return year;
      }
      return 0;
    }
    _mediaDateLabel(item = {}) {
      const metadata = item?.metadata || item?.media_item?.metadata || {};
      const album = item?.album || item?.media_item?.album || {};
      const candidates = [
        item.release_date,
        item.original_date,
        item.date,
        item.year,
        item.release_year,
        album.release_date,
        album.original_date,
        album.date,
        album.year,
        metadata.release_date,
        metadata.original_date,
        metadata.date,
        metadata.year
      ];
      for (const candidate of candidates) {
        const text = String(candidate ?? "").trim();
        if (!text) continue;
        const dateMatch = text.match(/\b(19|20)\d{2}[-/.]\d{1,2}[-/.]\d{1,2}\b/);
        if (dateMatch) return dateMatch[0].replace(/[/.]/g, "-");
        const yearMatch = text.match(/\b(19|20)\d{2}\b/);
        if (yearMatch) return yearMatch[0];
      }
      return "";
    }
    _albumKindLabel(item = {}) {
      const metadata = item?.metadata || item?.media_item?.metadata || {};
      const name = HomeiiMediaQueueFoundation2.normalizeComparableText(item?.name || item?.title || item?.media_item?.name || "");
      const rawValues = [
        item.album_type,
        item.albumType,
        item.release_type,
        item.releaseType,
        item.type,
        metadata.album_type,
        metadata.release_type,
        metadata.type,
        metadata.musicbrainz_albumtype,
        ...Array.isArray(metadata.release_group_types) ? metadata.release_group_types : []
      ].map((value) => HomeiiMediaQueueFoundation2.normalizeComparableText(value)).filter(Boolean);
      const haystack = `${rawValues.join(" ")} ${name}`;
      if (/(^| )(single|סינגל)( |$)/.test(haystack)) return this._m("Single", "סינגל");
      if (/(^| )(live|concert|הופעה|חיה)( |$)/.test(haystack)) return this._m("Live", "הופעה חיה");
      if (/(^| )(ep|mini album|מיני)( |$)/.test(haystack)) return "EP";
      return this._m("Studio", "אולפן");
    }
    _albumSelectLabel(album = {}, index = 0) {
      const date = this._mediaDateLabel(album);
      const kind = this._albumKindLabel(album);
      const name = String(album?.name || album?.title || album?.media_item?.name || this._i18n("ui.album")).trim();
      return [date, name, kind].filter(Boolean).join(" · ") || `${index + 1}. ${this._i18n("ui.album")}`;
    }
    _artistDescriptionText(item = {}) {
      const metadata = item?.metadata || item?.media_item?.metadata || {};
      const readText = (value) => {
        if (!value) return "";
        if (typeof value === "string") return value.trim();
        if (typeof value !== "object") return "";
        return String(value.he || value.en || value.text || value.description || value.summary || "").trim();
      };
      const candidates = [
        item.description,
        item.biography,
        item.bio,
        item.summary,
        metadata.description,
        metadata.biography,
        metadata.bio,
        metadata.summary,
        metadata.review,
        metadata.wikipedia,
        metadata.wiki
      ];
      for (const candidate of candidates) {
        const text = readText(candidate);
        if (text) return text;
      }
      return "";
    }
    _artistAlbumMatches(album = {}, artistName2 = "") {
      const target = HomeiiMediaQueueFoundation2.normalizeComparableText(artistName2);
      if (!target) return true;
      const artistValues = [
        this._artistName(album),
        album.artist,
        album.artist_str,
        album.album_artist,
        album.albumArtist,
        ...Array.isArray(album.artists) ? album.artists.map((artist) => artist?.name || artist) : [],
        ...Array.isArray(album.album_artists) ? album.album_artists.map((artist) => artist?.name || artist) : [],
        ...Array.isArray(album.media_item?.artists) ? album.media_item.artists.map((artist) => artist?.name || artist) : [],
        ...Array.isArray(album.media_item?.album_artists) ? album.media_item.album_artists.map((artist) => artist?.name || artist) : []
      ];
      return artistValues.some((value) => {
        const normalized = HomeiiMediaQueueFoundation2.normalizeComparableText(value);
        return normalized && (normalized === target || normalized.includes(target) || target.includes(normalized));
      });
    }
    _normalizeArtistAlbumCandidate(item = {}, artistName2 = "") {
      if (!item) return null;
      const mediaItem = item?.media_item || item?.item || item;
      const albumSource = mediaItem?.album && typeof mediaItem.album === "object" ? mediaItem.album : item?.album && typeof item.album === "object" ? item.album : mediaItem;
      const albumValue = typeof mediaItem?.album === "string" ? mediaItem.album : typeof item?.album === "string" ? item.album : "";
      const normalized = this._normalizeSearchItem(albumValue ? { name: albumValue, media_type: "album" } : albumSource, "album");
      if (!normalized || typeof normalized !== "object") return null;
      const name = String(normalized.name || normalized.title || albumSource?.name || albumSource?.title || albumValue || "").trim();
      const uri = String(normalized.uri || albumSource?.uri || item?.uri || "").trim();
      if (!uri && !name) return null;
      const image = this._artUrl(normalized, { size: 300 }) || this._artUrl(albumSource, { size: 300 }) || this._artUrl(item, { size: 300 }) || normalized.image || normalized.image_url || albumSource?.image || albumSource?.image_url || "";
      return {
        ...normalized,
        media_type: "album",
        uri,
        name,
        artist: normalized.artist || normalized.artist_str || this._artistName(item) || item?.artist || item?.artist_str || artistName2 || "",
        artist_str: normalized.artist_str || normalized.artist || this._artistName(item) || item?.artist_str || item?.artist || artistName2 || "",
        image: normalized.image || normalized.image_url || image,
        image_url: normalized.image_url || normalized.image || image
      };
    }
    _dedupeArtistAlbums(albums = []) {
      const byUri = /* @__PURE__ */ new Set();
      const byIdentity = /* @__PURE__ */ new Map();
      const titleKeyForAlbum = (album = {}) => {
        const name = HomeiiMediaQueueFoundation2.normalizeComparableText(album?.name || album?.title || album?.media_item?.name || "");
        if (!name) return "";
        const year = this._mediaYearValue(album) || "";
        return year ? `${name}::${year}` : name;
      };
      const identityForAlbum = (album = {}) => {
        const titleKey = titleKeyForAlbum(album);
        const name = titleKey.split("::")[0] || "";
        if (!name) return "";
        const year = this._mediaYearValue(album) || "";
        const artist = HomeiiMediaQueueFoundation2.normalizeComparableText(this._artistName(album) || album?.artist_str || album?.artist || "");
        return [name, year, artist].filter((part) => part !== "").join("::");
      };
      const scoreAlbum = (album = {}) => {
        let score = 0;
        if (album.uri) score += 4;
        if (this._mediaYearValue(album)) score += 2;
        if (this._artUrl(album) || album.image || album.image_url) score += 2;
        if (album.provider_instance === "library" || album.provider === "library") score += 3;
        if (Array.isArray(album.provider_mappings) && album.provider_mappings.length) score += album.provider_mappings.length;
        return score;
      };
      const deduped = [];
      (Array.isArray(albums) ? albums : []).forEach((album) => {
        const uri = String(album?.uri || album?.media_item?.uri || "").trim().toLowerCase();
        const identity = identityForAlbum(album);
        const titleKey = titleKeyForAlbum(album);
        if (uri && byUri.has(uri)) return;
        const existingIndex = identity && byIdentity.has(identity) ? byIdentity.get(identity) : titleKey && byIdentity.has(`title:${titleKey}`) ? byIdentity.get(`title:${titleKey}`) : -1;
        if (existingIndex >= 0) {
          if (scoreAlbum(album) > scoreAlbum(deduped[existingIndex])) {
            deduped[existingIndex] = album;
            if (uri) byUri.add(uri);
          }
          return;
        }
        if (!uri && !identity && !titleKey) return;
        const index = deduped.length;
        if (uri) byUri.add(uri);
        if (identity) byIdentity.set(identity, index);
        if (titleKey) byIdentity.set(`title:${titleKey}`, index);
        deduped.push(album);
      });
      return deduped;
    }
    _artistAlbumsFromTracksPayload(payload, artistName2 = "") {
      const tracks = this._libraryMediaDetailTracksFromPayload(payload, { forceTrackItems: true });
      const albums = tracks.map((track) => {
        const album = track?.album;
        return this._normalizeArtistAlbumCandidate(album && typeof album === "object" ? { ...album, media_type: "album" } : track, artistName2);
      }).filter(Boolean);
      return this._dedupeArtistAlbums(albums);
    }
    async _loadArtistPlaylistRecommendations(artistName2 = "") {
      const name = String(artistName2 || "").trim();
      if (!name) return [];
      const normalizedArtist = HomeiiMediaQueueFoundation2.normalizeComparableText(name);
      const seen = /* @__PURE__ */ new Set();
      const playlists = [];
      const addPlaylist = (item = {}) => {
        const normalized = this._normalizeSearchItem(item, "playlist");
        const uri = String(normalized?.uri || "").trim();
        const title = String(normalized?.name || normalized?.title || "").trim();
        if (!uri && !title) return;
        const key = String(uri || title).toLowerCase();
        if (seen.has(key)) return;
        const haystack = HomeiiMediaQueueFoundation2.normalizeComparableText([
          title,
          normalized?.description,
          normalized?.metadata?.description,
          normalized?.provider_label,
          normalized?.provider
        ].filter(Boolean).join(" "));
        seen.add(key);
        playlists.push({
          ...normalized,
          media_type: "playlist",
          _homeiiArtistPlaylistScore: haystack.includes(normalizedArtist) ? 4 : 1
        });
      };
      for (const query of [`${name} playlist`, `${name} playlists`, name]) {
        try {
          const results = await this._search(query);
          (results.playlists || []).forEach(addPlaylist);
        } catch (_) {
        }
        if (playlists.length >= 12) break;
      }
      return playlists.sort((a, b) => Number(b._homeiiArtistPlaylistScore || 0) - Number(a._homeiiArtistPlaylistScore || 0)).slice(0, 12).map(({ _homeiiArtistPlaylistScore, ...item }) => item);
    }
    async _loadLibraryMediaDetailTracksViaHaBrowse(entry = {}) {
      const player = this._getSelectedPlayer();
      const entityId = this._selectedPlayerMoreInfoEntityId?.(player) || player?.entity_id || this._state.selectedPlayer || "";
      const uri = String(entry?.uri || "").trim();
      if (!this._hass?.connection || !entityId || !uri || this._isDirectMaEntityId?.(entityId)) return [];
      const raw = await this._hass.connection.sendMessagePromise({
        type: "media_player/browse_media",
        entity_id: entityId,
        media_content_id: uri,
        media_content_type: entry.media_type || entry.type || "album"
      });
      return this._libraryMediaDetailTracksFromPayload(raw, { forceTrackItems: false });
    }
    async _loadLibraryMediaDetailTracks(entry = {}) {
      const type = String(entry?.media_type || entry?.type || "album").toLowerCase();
      const uri = String(entry?.uri || "").trim();
      const cacheKey = `media-detail:${type}:${uri}`;
      const ttl = Number(this._config.cache_ttl || 3e5);
      const cached = this._cache.library.get(cacheKey);
      if (cached && Date.now() - cached.ts < ttl) return cached.items;
      const attempts = [];
      const commandArgsList = this._libraryMediaDetailCommandArgsList(entry, type);
      if (commandArgsList.length) {
        if (type === "album") {
          commandArgsList.forEach((commandArgs) => {
            attempts.push({
              command: "music/albums/album_tracks",
              args: { item_id: commandArgs.item_id, provider_instance_id_or_domain: commandArgs.provider, in_library_only: false },
              forceTrackItems: true
            });
            attempts.push({
              command: "music/albums/album_tracks",
              args: { item_id: commandArgs.item_id, provider_instance_or_domain: commandArgs.provider, in_library_only: false },
              forceTrackItems: true
            });
          });
        } else if (type === "playlist") {
          commandArgsList.forEach((commandArgs) => {
            attempts.push({
              command: "music/playlists/playlist_tracks",
              args: { item_id: commandArgs.item_id, provider_instance_or_domain: commandArgs.provider, force_refresh: false },
              forceTrackItems: true
            });
            attempts.push({
              command: "music/playlists/playlist_tracks",
              args: { item_id: commandArgs.item_id, provider_instance_id_or_domain: commandArgs.provider, force_refresh: false },
              forceTrackItems: true
            });
          });
        }
      }
      if (uri) attempts.push({ command: "music/item_by_uri", args: { uri }, forceTrackItems: false });
      let lastError = null;
      let bestTracks = [];
      for (const attempt of attempts) {
        try {
          const raw = await this._callDirectMaCommand(attempt.command, attempt.args);
          const tracks = this._sortLibraryDetailTracks(this._libraryMediaDetailTracksFromPayload(raw, { forceTrackItems: attempt.forceTrackItems }));
          if (tracks.length > bestTracks.length) bestTracks = tracks;
          if (tracks.length > 1 && attempt.command.includes("album_tracks")) break;
        } catch (error) {
          lastError = error;
        }
      }
      if (bestTracks.length > 1 || bestTracks.length && type !== "album") {
        this._cache.library.set(cacheKey, { ts: Date.now(), items: bestTracks });
        return bestTracks;
      }
      try {
        const tracks = await this._loadLibraryMediaDetailTracksViaHaBrowse(entry);
        const sortedTracks = this._sortLibraryDetailTracks(tracks);
        if (sortedTracks.length > bestTracks.length) bestTracks = sortedTracks;
      } catch (_) {
      }
      if (type === "album" && entry?.name) {
        try {
          const tracks = await this._loadLibraryMediaDetailTracksViaLibrarySearch(entry);
          if (tracks.length > bestTracks.length) bestTracks = tracks;
        } catch (_) {
        }
      }
      if (bestTracks.length) {
        this._cache.library.set(cacheKey, { ts: Date.now(), items: bestTracks });
        return bestTracks;
      }
      if (lastError && !attempts.length) throw lastError;
      this._cache.library.set(cacheKey, { ts: Date.now(), items: [] });
      return [];
    }
    async _loadLibraryArtistDetail(entry = {}) {
      const uri = String(entry?.uri || "").trim();
      const artistName2 = String(entry?.name || this._artistName(entry) || "").trim();
      const cacheKey = `artist-detail:${uri || artistName2.toLowerCase()}`;
      const ttl = Number(this._config.cache_ttl || 3e5);
      const cached = this._cache.library.get(cacheKey);
      if (cached && Date.now() - cached.ts < ttl && cached.detail) return cached.detail;
      let artistInfo = { ...entry, media_type: "artist", uri };
      let rawArtistPayload = null;
      if (uri) {
        try {
          const rawArtist = await this._callDirectMaCommand("music/item_by_uri", { uri });
          rawArtistPayload = rawArtist;
          const rawItem = rawArtist?.media_item || rawArtist?.item || rawArtist;
          if (rawItem && typeof rawItem === "object") {
            artistInfo = {
              ...artistInfo,
              ...this._normalizeSearchItem(rawItem, "artist"),
              media_type: "artist",
              uri: String(rawItem.uri || uri).trim() || uri
            };
          }
        } catch (_) {
        }
      }
      const name = String(artistInfo?.name || artistName2 || "").trim();
      const commandArgsList = [];
      const addCommandArgs = (args) => {
        if (!args?.item_id || !args?.provider) return;
        const key = `${args.provider}::${args.item_id}`.toLowerCase();
        if (commandArgsList.some((candidate) => candidate.key === key)) return;
        commandArgsList.push({ key, item_id: args.item_id, provider: args.provider });
      };
      this._libraryMediaDetailCommandArgsList(artistInfo, "artist").forEach(addCommandArgs);
      this._libraryMediaDetailCommandArgsList(entry, "artist").forEach(addCommandArgs);
      const attempts = [];
      commandArgsList.forEach((commandArgs) => {
        attempts.push({
          command: "music/artists/artist_albums",
          args: { item_id: commandArgs.item_id, provider_instance_id_or_domain: commandArgs.provider, in_library_only: false },
          type: "albums"
        });
        attempts.push({
          command: "music/artists/artist_albums",
          args: { item_id: commandArgs.item_id, provider_instance_or_domain: commandArgs.provider, in_library_only: false },
          type: "albums"
        });
        attempts.push({
          command: "music/artists/artist_tracks",
          args: { item_id: commandArgs.item_id, provider_instance_id_or_domain: commandArgs.provider, in_library_only: false },
          type: "tracks"
        });
        attempts.push({
          command: "music/artists/artist_tracks",
          args: { item_id: commandArgs.item_id, provider_instance_or_domain: commandArgs.provider, in_library_only: false },
          type: "tracks"
        });
      });
      let albums = [];
      const addAlbums = (items = [], { trusted = false } = {}) => {
        const normalized = (Array.isArray(items) ? items : []).map((item) => this._normalizeArtistAlbumCandidate(item, name)).filter((album) => album?.uri || album?.name).filter((album) => trusted || this._artistAlbumMatches(album, name));
        if (!normalized.length) return;
        albums = this._dedupeArtistAlbums([...albums, ...normalized]);
      };
      if (rawArtistPayload) {
        addAlbums(this._libraryMediaItemsFromPayload(rawArtistPayload, "album"), { trusted: true });
      }
      for (const attempt of attempts) {
        try {
          const raw = await this._callDirectMaCommand(attempt.command, attempt.args);
          const attemptAlbums = attempt.type === "tracks" ? this._artistAlbumsFromTracksPayload(raw, name) : this._libraryMediaItemsFromPayload(raw, "album");
          addAlbums(attemptAlbums, { trusted: true });
        } catch (_) {
        }
      }
      if (name) {
        try {
          const searchResults = await this._search(name);
          const searchAlbums = (Array.isArray(searchResults?.albums) ? searchResults.albums : []).map((item) => this._normalizeArtistAlbumCandidate(item, name)).filter(Boolean);
          const artistMatches = searchAlbums.filter((album) => this._artistAlbumMatches(album, name));
          addAlbums(artistMatches.length ? artistMatches : searchAlbums.slice(0, 60), { trusted: !artistMatches.length });
        } catch (_) {
        }
      }
      if (name) {
        const libraryAlbumQueries = [
          ["artist_name", 2e3, ""],
          ["sort_name", 1e3, name],
          ["artist_name", 1e3, name]
        ];
        for (const [orderBy, limit, search] of libraryAlbumQueries) {
          try {
            const rawAlbums = await this._fetchLibrary("album", orderBy, limit, false, search);
            addAlbums(rawAlbums);
          } catch (_) {
          }
        }
      }
      albums = this._dedupeArtistAlbums(albums).map((album) => ({ ...album, media_type: "album" })).sort((left, right) => {
        const leftYear = this._mediaYearValue(left);
        const rightYear = this._mediaYearValue(right);
        if (leftYear !== rightYear) return rightYear - leftYear;
        return String(left?.name || "").localeCompare(String(right?.name || ""), this._isHebrew() ? "he" : "en", { sensitivity: "base", numeric: true });
      });
      const playlists = await this._loadArtistPlaylistRecommendations(name);
      const detail = { artistInfo, albums, playlists };
      this._cache.library.set(cacheKey, { ts: Date.now(), detail });
      return detail;
    }
    _openLibraryMediaDetail(entry = {}, sourceEl = null, options = {}) {
      const mediaType = String(entry?.media_type || entry?.type || "album").toLowerCase();
      const uri = String(entry?.uri || "").trim();
      if (!uri || !this._mediaTypeCanOpenDetails(mediaType)) return false;
      if (sourceEl) this._flashInteraction(sourceEl);
      const token = `${mediaType}:${uri}:${Date.now()}`;
      const previousDetail = this._state.menuPage === "media_detail" ? this._state.mobileLibraryDetail : null;
      const replaceCurrentDetail = options?.replaceCurrentDetail === true;
      const shouldStackCurrent = !!(previousDetail && !replaceCurrentDetail && previousDetail.token && String(previousDetail.uri || "") !== uri);
      if (shouldStackCurrent) this._pushCurrentLibraryDetailStack();
      else if (!previousDetail) this._clearLibraryDetailStack();
      const albumBrowseContext = options?.albumBrowseContext || (mediaType === "album" ? this._albumBrowseContextFromDetail(previousDetail, entry) : null);
      const parentPage = String(this._state.menuPage || "").startsWith("library_") ? this._state.menuPage : this._libraryDetailParentPageForType(mediaType);
      this._state.mobileLibraryDetailParentPage = parentPage;
      this._state.mobileLibraryDetail = {
        ...entry,
        uri,
        media_type: mediaType,
        name: entry.name || "",
        artist: entry.artist || "",
        album: entry.album || "",
        image: entry.image || entry.image_url || "",
        tracks: [],
        albums: [],
        playlists: [],
        artistInfo: null,
        artistSearchQuery: "",
        artistDescriptionExpanded: !!entry.artistDescriptionExpanded,
        albumBrowseContext: albumBrowseContext || null,
        loading: true,
        error: "",
        token
      };
      if (this._state.menuPage === "media_detail") this._renderMobileMenu().catch(() => {
      });
      else this._pushMobileMenu("media_detail");
      if (mediaType === "artist") {
        this._loadLibraryArtistDetail(this._state.mobileLibraryDetail).then((artistDetail) => {
          const current = this._state.mobileLibraryDetail;
          if (!current || current.token !== token) return;
          current.artistInfo = artistDetail?.artistInfo || null;
          current.albums = Array.isArray(artistDetail?.albums) ? artistDetail.albums : [];
          current.playlists = Array.isArray(artistDetail?.playlists) ? artistDetail.playlists : [];
          current.loading = false;
          current.error = current.albums.length ? "" : this._m("No albums were returned for this artist.", "לא נטענו אלבומים לאמן הזה.");
          if (this._state.menuOpen && this._state.menuPage === "media_detail") this._renderMobileMenu().catch(() => {
          });
        }).catch((error) => {
          const current = this._state.mobileLibraryDetail;
          if (!current || current.token !== token) return;
          current.albums = [];
          current.playlists = [];
          current.loading = false;
          current.error = error?.message || this._m("Could not load this artist.", "לא הצלחתי לטעון את האמן.");
          if (this._state.menuOpen && this._state.menuPage === "media_detail") this._renderMobileMenu().catch(() => {
          });
        });
        return true;
      }
      this._loadLibraryMediaDetailTracks(this._state.mobileLibraryDetail).then((tracks) => {
        const current = this._state.mobileLibraryDetail;
        if (!current || current.token !== token) return;
        current.tracks = tracks;
        current.loading = false;
        current.error = tracks.length ? "" : this._m("No tracks were returned. You can still play it.", "לא נטענו שירים. עדיין אפשר לנגן.");
        if (this._state.menuOpen && this._state.menuPage === "media_detail") this._renderMobileMenu().catch(() => {
        });
      }).catch((error) => {
        const current = this._state.mobileLibraryDetail;
        if (!current || current.token !== token) return;
        current.tracks = [];
        current.loading = false;
        current.error = error?.message || this._m("Could not load tracks. You can still play it.", "לא הצלחתי לטעון שירים. עדיין אפשר לנגן.");
        if (this._state.menuOpen && this._state.menuPage === "media_detail") this._renderMobileMenu().catch(() => {
        });
      });
      return true;
    }
    async _openLibraryAdjacentAlbum(direction = 0, sourceEl = null) {
      const current = this._state.mobileLibraryDetail || {};
      if (this._libraryDetailMediaType(current) !== "album") return false;
      const browse = this._albumBrowseState(current);
      const step = Number(direction) < 0 ? -1 : 1;
      const nextIndex = Number(browse?.index ?? -1) + step;
      return this._openLibraryBrowseAlbumAtIndex(nextIndex, sourceEl);
    }
    async _openLibraryBrowseAlbumAtIndex(index = 0, sourceEl = null) {
      const current = this._state.mobileLibraryDetail || {};
      if (this._libraryDetailMediaType(current) !== "album") return false;
      const browse = this._albumBrowseState(current);
      const nextIndex = Number(index);
      if (!browse || nextIndex < 0 || nextIndex >= browse.total) return false;
      const nextAlbum = browse.albums[nextIndex];
      if (!nextAlbum) return false;
      const opened = this._openLibraryMediaDetail({ ...nextAlbum, media_type: "album" }, sourceEl, {
        replaceCurrentDetail: true,
        albumBrowseContext: {
          ...browse,
          index: nextIndex
        }
      });
      if (!opened) return false;
      await Promise.resolve();
      return true;
    }
    _radioBrowserCountryOptions() {
      const language = this._language?.() || this._state.lang || this._config?.language || "en";
      const base = getRadioBrowserCountrySelectorOptions2(
        (key, params = {}, fallback = "") => this._i18n(key, params, fallback),
        language
      ).map((option) => [option.value, option.label]);
      const current = this._mobileRadioBrowserCountry();
      if (current !== "all" && !base.some(([code]) => code === current)) {
        base.push([current, homeiiRadioBrowserCountryLabel(current, (key) => this._i18n(key), language)]);
      }
      return base;
    }
    _mobileRadioBrowserCountry() {
      const value = String(this._state.mobileRadioBrowserCountry || "all").trim().toUpperCase();
      return value && value !== "ALL" ? value : "all";
    }
    _radioBrowserCountryLabel(code = "") {
      const normalized = String(code || "").trim().toUpperCase();
      if (!normalized || normalized === "ALL") return this._i18n("ui.all_countries");
      const found = this._radioBrowserCountryOptions().find(([value]) => value === normalized);
      return found?.[1] || this._state.mobileRadioBrowseCountryName || normalized;
    }
    async _fetchRadioBrowserCountries(limit = 260) {
      const safeLimit = Math.max(20, Math.min(400, Number(limit) || 260));
      const key = "radio-browser:countries";
      const ttl = Number(this._config.cache_ttl || 3e5);
      const cached = this._cache.library.get(key);
      if (cached && Date.now() - cached.ts < ttl) return cached.items;
      const res = await fetch("https://de1.api.radio-browser.info/json/countries", {
        headers: { "Accept": "application/json" }
      });
      if (!res.ok) throw new Error(`Radio Browser countries ${res.status}`);
      const raw = await res.json();
      const countries = (Array.isArray(raw) ? raw : []).map((country) => ({
        code: String(country.iso_3166_1 || country.countrycode || "").trim().toUpperCase(),
        name: country.name || country.country || "",
        stationcount: Number(country.stationcount || 0) || 0
      })).filter((country) => country.code && country.name && country.stationcount > 0).sort((a, b) => a.name.localeCompare(b.name)).slice(0, safeLimit);
      this._cache.library.set(key, { ts: Date.now(), items: countries });
      return countries;
    }
    async _fetchRadioBrowserStations(query = "", limit = 40, options = {}) {
      const safeLimit = Math.max(8, Math.min(80, Number(limit) || 40));
      const q = String(query || "").trim();
      const countryCode = String(options.countryCode || "").trim().toUpperCase();
      const hasCountry = countryCode && countryCode !== "ALL";
      let endpoint;
      if (q) {
        const params = new URLSearchParams({
          hidebroken: "true",
          limit: String(safeLimit),
          name: q
        });
        if (hasCountry) params.set("countrycode", countryCode);
        endpoint = `https://de1.api.radio-browser.info/json/stations/search?${params.toString()}`;
      } else if (hasCountry) {
        endpoint = `https://de1.api.radio-browser.info/json/stations/bycountrycodeexact/${encodeURIComponent(countryCode)}?hidebroken=true&limit=${safeLimit}&order=votes&reverse=true`;
      } else {
        endpoint = `https://de1.api.radio-browser.info/json/stations/topvote/${safeLimit}`;
      }
      const res = await fetch(endpoint, {
        headers: {
          "Accept": "application/json"
        }
      });
      if (!res.ok) throw new Error(`Radio Browser ${res.status}`);
      const raw = await res.json();
      return (Array.isArray(raw) ? raw : []).map((station) => ({
        uri: station.url_resolved || station.url || "",
        media_type: "radio",
        name: station.name || station.stationuuid || "Radio Browser",
        image: station.favicon || "",
        image_url: station.favicon || "",
        metadata: {
          description: [station.country, station.tags].filter(Boolean).join(" · ")
        },
        artist_str: station.country || "Radio Browser",
        radio_browser_id: station.stationuuid || "",
        radio_browser: true,
        radio_browser_country: station.countrycode || station.country || ""
      })).filter((item) => item.uri);
    }
    _currentRadioIdentity() {
      const player = this._getSelectedPlayer();
      const queueItem = this._state.maQueueState?.current_item || {};
      const media = queueItem.media_item || queueItem || {};
      const uri = media.uri || queueItem.media_content_id || player?.attributes?.media_content_id || "";
      const name = media.name || queueItem.name || queueItem.media_title || player?.attributes?.media_title || "";
      const mediaType = String(media.media_type || queueItem.media_type || player?.attributes?.media_content_type || "").toLowerCase();
      return { uri: String(uri || ""), name: String(name || ""), mediaType };
    }
    _isLikelyRadioPlayback(player, queueItem = null, media = null) {
      const currentQueueItem = queueItem || this._state.maQueueState?.current_item || {};
      const currentMedia = media || currentQueueItem?.media_item || currentQueueItem || {};
      const mediaType = String(
        currentMedia?.media_type || currentQueueItem?.media_type || player?.attributes?.media_content_type || player?.attributes?.media_channel || ""
      ).toLowerCase();
      if (mediaType === "radio") return true;
      const haystack = [
        currentMedia?.uri,
        currentQueueItem?.uri,
        currentQueueItem?.media_content_id,
        player?.attributes?.media_content_id,
        player?.attributes?.media_channel,
        currentMedia?.provider,
        currentMedia?.provider_id,
        currentMedia?.provider_domain,
        currentQueueItem?.provider,
        currentQueueItem?.provider_id,
        currentMedia?.radio_browser_id,
        currentQueueItem?.radio_browser_id,
        currentMedia?.stationuuid,
        currentQueueItem?.stationuuid,
        currentMedia?.metadata?.radio_browser_id,
        currentQueueItem?.metadata?.radio_browser_id,
        currentMedia?.metadata?.stationuuid,
        currentQueueItem?.metadata?.stationuuid,
        currentMedia?.metadata?.stream_url,
        currentQueueItem?.metadata?.stream_url
      ].map((value) => String(value || "").toLowerCase()).filter(Boolean).join(" | ");
      if (/(radiobrowser|radio_browser|stationuuid|tunein|streamurl|stream url|icy|webradio)/.test(haystack)) return true;
      if (["track", "song", "album", "playlist", "artist"].includes(mediaType)) return false;
      return /(^|[/:._-])radio([/:._-]|$)/.test(haystack);
    }
    async _playAdjacentRadioStation(direction = "next") {
      const current = this._currentRadioIdentity();
      if (current.mediaType && current.mediaType !== "radio") return false;
      const country = this._mobileRadioBrowserCountry();
      let stations = [];
      try {
        const maStations = await this._getLibrary("radio", "sort_name", 250);
        const browserStations = await this._fetchRadioBrowserStations("", 80, { countryCode: country === "all" ? "all" : country });
        stations = [...maStations || [], ...browserStations || []].filter((station) => station?.uri).filter((station, index2, arr) => arr.findIndex((candidate) => candidate.uri === station.uri) === index2);
      } catch (_) {
        try {
          stations = await this._getLibrary("radio", "sort_name", 250);
        } catch (__) {
          stations = [];
        }
      }
      if (!stations.length) return false;
      const currentUri = current.uri.toLowerCase();
      const currentName = current.name.toLowerCase();
      let index = stations.findIndex((station) => String(station.uri || "").toLowerCase() === currentUri);
      if (index < 0 && currentName) {
        index = stations.findIndex((station) => String(station.name || "").toLowerCase() === currentName);
      }
      const fallbackIndex = direction === "next" ? -1 : 0;
      const base = index >= 0 ? index : fallbackIndex;
      const nextIndex = direction === "next" ? (base + 1) % stations.length : (base - 1 + stations.length) % stations.length;
      const next = stations[nextIndex];
      if (!next?.uri) return false;
      await this._playMedia(next.uri, "radio", "play", { label: next.name || "Radio", silent: true });
      this._toastSuccess(this._i18n("ui.radio_station_changed"));
      return true;
    }
    _searchItemProviderMapping(item = {}) {
      const mappings = [].concat(Array.isArray(item?.provider_mappings) ? item.provider_mappings : []).concat(Array.isArray(item?.media_item?.provider_mappings) ? item.media_item.provider_mappings : []);
      return mappings.find((mapping) => mapping?.uri || mapping?.item_id || mapping?.provider_instance || mapping?.provider_domain || mapping?.provider) || {};
    }
    _searchItemUri(item = {}, fallbackType = "track") {
      const directUri = String(item?.uri || item?.media_item?.uri || item?.media_id || "").trim();
      if (directUri) return directUri;
      const mapping = this._searchItemProviderMapping(item);
      const mappingUri = String(mapping?.uri || mapping?.media_item?.uri || "").trim();
      if (mappingUri) return mappingUri;
      const itemId = String(item?.item_id || item?.id || mapping?.item_id || mapping?.provider_item_id || "").trim();
      const provider = String(
        item?.provider || item?.provider_domain || item?.provider_instance || item?.provider_id || mapping?.provider_domain || mapping?.provider_instance || mapping?.provider || mapping?.provider_id || ""
      ).trim();
      const mediaType = String(item?.media_type || item?.type || item?.media_item?.media_type || fallbackType || "track").toLowerCase();
      return provider && itemId ? `${provider}://${mediaType}/${itemId}` : "";
    }
    _normalizeSearchItem(item = {}, fallbackType = "track") {
      if (!item || typeof item !== "object") return item;
      const mapping = this._searchItemProviderMapping(item);
      const mediaType = String(item.media_type || item.type || item.media_item?.media_type || fallbackType || "track").toLowerCase();
      const uri = this._searchItemUri(item, mediaType);
      const provider = String(
        item.provider || item.provider_instance || item.provider_domain || item.provider_id || mapping.provider_instance || mapping.provider_domain || mapping.provider || mapping.provider_id || ""
      ).trim();
      return {
        ...item,
        media_type: mediaType,
        uri: uri || item.uri || "",
        provider: item.provider || provider,
        provider_instance: item.provider_instance || mapping.provider_instance || "",
        provider_domain: item.provider_domain || mapping.provider_domain || "",
        provider_id: item.provider_id || mapping.provider_id || ""
      };
    }
    _normalizeSearchResponse(raw) {
      const out = { radio: [], podcasts: [], albums: [], artists: [], tracks: [], playlists: [], genres: [] };
      if (!raw) return out;
      const readGroup = (value) => Array.isArray(value) ? value : value?.items && Array.isArray(value.items) ? value.items : [];
      const groupForItem = (item = {}) => {
        const type = String(item?.media_type || item?.type || item?.item_type || item?.media_item?.media_type || "").toLowerCase();
        if (type === "radio") return "radio";
        if (type === "podcast") return "podcasts";
        if (type === "album") return "albums";
        if (type === "artist") return "artists";
        if (type === "track") return "tracks";
        if (type === "playlist") return "playlists";
        if (type === "genre") return "genres";
        return "";
      };
      const addFlatItems = (items = []) => {
        (Array.isArray(items) ? items : []).forEach((item) => {
          const group = groupForItem(item);
          if (group) out[group].push(this._normalizeSearchItem(item, group === "podcasts" ? "podcast" : group.replace(/s$/, "")));
        });
      };
      const addGroupedItems = (source = {}) => {
        out.radio.push(...readGroup(source.radio || source.radios).map((item) => this._normalizeSearchItem(item, "radio")));
        out.podcasts.push(...readGroup(source.podcast || source.podcasts).map((item) => this._normalizeSearchItem(item, "podcast")));
        out.albums.push(...readGroup(source.album || source.albums).map((item) => this._normalizeSearchItem(item, "album")));
        out.artists.push(...readGroup(source.artist || source.artists).map((item) => this._normalizeSearchItem(item, "artist")));
        out.tracks.push(...readGroup(source.track || source.tracks).map((item) => this._normalizeSearchItem(item, "track")));
        out.playlists.push(...readGroup(source.playlist || source.playlists).map((item) => this._normalizeSearchItem(item, "playlist")));
        out.genres.push(...readGroup(source.genre || source.genres).map((item) => this._normalizeSearchItem(item, "genre")));
      };
      const src = raw.response ?? raw.result ?? raw;
      const sources = [src];
      if (src?.response) sources.push(src.response);
      if (src?.result) sources.push(src.result);
      if (src && typeof src === "object" && !Array.isArray(src)) {
        Object.values(src).forEach((value) => {
          if (!value || typeof value !== "object" || Array.isArray(value)) return;
          const nested = value.response ?? value.result ?? value;
          if (nested && typeof nested === "object") sources.push(nested);
        });
      }
      sources.forEach((source) => {
        addGroupedItems(source);
        addFlatItems(Array.isArray(source) ? source : []);
        addFlatItems(source?.items);
        addFlatItems(source?.results);
      });
      return out;
    }
    _emptySearchResults() {
      return { radio: [], podcasts: [], albums: [], artists: [], tracks: [], playlists: [], genres: [] };
    }
    _hasSearchResults(results = {}) {
      return Object.entries(results || {}).some(([group, arr]) => group !== "genres" && Array.isArray(arr) && arr.length);
    }
    _mergeSearchResults(...sets) {
      const out = this._emptySearchResults();
      const groups = ["radio", "podcasts", "albums", "artists", "tracks", "playlists", "genres"];
      const seen = /* @__PURE__ */ new Set();
      sets.forEach((results) => {
        groups.forEach((group) => {
          const items = Array.isArray(results?.[group]) ? results[group] : [];
          items.forEach((item) => {
            const uri = String(item?.uri || item?.media_item?.uri || "").trim();
            const name = String(item?.name || item?.title || item?.media_item?.name || "").trim().toLowerCase();
            const provider = String(item?.provider || item?.provider_id || item?.provider_domain || item?.media_item?.provider || "").trim().toLowerCase();
            const key = `${group}:${uri || `${provider}:${name}`}`;
            if (!key || seen.has(key)) return;
            seen.add(key);
            out[group].push(item);
          });
        });
      });
      return out;
    }
    _cachedLibrarySearchResults(query = "", limit = 8) {
      const q = String(query || "").trim().toLowerCase();
      const out = this._emptySearchResults();
      if (!q || !this._cache?.library) return out;
      const typeMap = {
        radio: "radio",
        podcast: "podcasts",
        album: "albums",
        artist: "artists",
        track: "tracks",
        playlist: "playlists",
        genre: "genres"
      };
      const seen = /* @__PURE__ */ new Set();
      const scoreItem = (item, mediaType) => {
        const title = String(item?.name || item?.title || item?.media_item?.name || "").toLowerCase();
        const artist = String(this._artistName?.(item) || item?.artist || item?.artist_str || "").toLowerCase();
        const album = String(item?.album?.name || item?.media_item?.album?.name || "").toLowerCase();
        const haystack = [title, artist, album, item?.metadata?.description, item?.description].filter(Boolean).join(" ").toLowerCase();
        if (!haystack.includes(q)) return 0;
        let score = 1;
        if (title.startsWith(q)) score += 6;
        if (title === q) score += 4;
        if (artist.startsWith(q)) score += 3;
        if (mediaType === "track") score += 1;
        return score;
      };
      for (const [key, cached] of this._cache.library.entries()) {
        const mediaType = String(key || "").split(":")[0];
        const group = typeMap[mediaType];
        if (!group || !Array.isArray(cached?.items)) continue;
        cached.items.forEach((item) => {
          const uri = String(item?.uri || item?.media_item?.uri || "").trim();
          const stableKey = `${group}:${uri || item?.item_id || item?.name || ""}`;
          if (!stableKey || seen.has(stableKey)) return;
          const score = scoreItem(item, mediaType);
          if (!score) return;
          seen.add(stableKey);
          out[group].push({ ...item, media_type: item?.media_type || mediaType, _homeiiSearchScore: score });
        });
      }
      Object.keys(out).forEach((group) => {
        out[group] = out[group].sort((a, b) => Number(b._homeiiSearchScore || 0) - Number(a._homeiiSearchScore || 0)).slice(0, limit).map(({ _homeiiSearchScore, ...item }) => item);
      });
      return out;
    }
    async _searchPreviewResults(query = "", limit = 8) {
      const q = String(query || "").trim();
      const cached = this._cachedLibrarySearchResults(q, limit);
      if (this._hasSearchResults(cached)) return cached;
      if (!q) return this._emptySearchResults();
      const previewLimit = Math.max(4, Math.min(20, Number(limit || 8) || 8));
      const [radioRes, podcastRes, albumRes, artistRes, trackRes, playlistRes] = await Promise.allSettled([
        this._fetchLibrary("radio", "sort_name", previewLimit, false, q),
        this._fetchLibrary("podcast", "sort_name", previewLimit, false, q),
        this._fetchLibrary("album", "sort_name", previewLimit, false, q),
        this._fetchLibrary("artist", "sort_name", previewLimit, false, q),
        this._fetchLibrary("track", "sort_name", previewLimit, false, q),
        this._fetchLibrary("playlist", "sort_name", previewLimit, false, q)
      ]);
      return {
        radio: radioRes.status === "fulfilled" ? radioRes.value : [],
        podcasts: podcastRes.status === "fulfilled" ? podcastRes.value : [],
        albums: albumRes.status === "fulfilled" ? albumRes.value : [],
        artists: artistRes.status === "fulfilled" ? artistRes.value : [],
        tracks: trackRes.status === "fulfilled" ? trackRes.value : [],
        playlists: playlistRes.status === "fulfilled" ? playlistRes.value : []
      };
    }
    async _search(query) {
      const q = String(query || "").trim();
      if (!q) return this._emptySearchResults();
      let globalResults = this._emptySearchResults();
      try {
        const raw = await this._callService("search", { query: q, limit: 25 });
        globalResults = this._normalizeSearchResponse(raw);
      } catch (_) {
        try {
          const raw2 = await this._callService("search", { name: q, limit: 25, media_type: ["radio", "podcast", "album", "artist", "track", "playlist", "genre"] });
          globalResults = this._normalizeSearchResponse(raw2);
        } catch (_2) {
        }
      }
      if (!this._hasSearchResults(globalResults)) {
        try {
          const raw3 = await this._callService("search", { name: q, limit: 25 });
          globalResults = this._normalizeSearchResponse(raw3);
        } catch (_) {
        }
      }
      if (this._hasSearchResults(globalResults)) return globalResults;
      const [radioRes, podcastRes, albumRes, artistRes, trackRes, playlistRes] = await Promise.allSettled([
        this._fetchLibrary("radio", "sort_name", 50, false, q),
        this._fetchLibrary("podcast", "sort_name", 50, false, q),
        this._fetchLibrary("album", "sort_name", 50, false, q),
        this._fetchLibrary("artist", "sort_name", 50, false, q),
        this._fetchLibrary("track", "sort_name", 50, false, q),
        this._fetchLibrary("playlist", "sort_name", 50, false, q)
      ]);
      return {
        radio: radioRes.status === "fulfilled" ? radioRes.value : [],
        podcasts: podcastRes.status === "fulfilled" ? podcastRes.value : [],
        albums: albumRes.status === "fulfilled" ? albumRes.value : [],
        artists: artistRes.status === "fulfilled" ? artistRes.value : [],
        tracks: trackRes.status === "fulfilled" ? trackRes.value : [],
        playlists: playlistRes.status === "fulfilled" ? playlistRes.value : []
      };
    }
    _rejectWsPending(error = new Error("MA WS disconnected")) {
      this._wsPending.forEach((pending) => {
        if (pending?.timer) clearTimeout(pending.timer);
        try {
          pending.reject(error);
        } catch (_) {
        }
      });
      this._wsPending.clear();
    }
    _scheduleMaReconnect(delay = 8e3) {
      clearTimeout(this._maReconnectTimer);
      this._maReconnectTimer = setTimeout(() => {
        this._maReconnectTimer = null;
        if (this.isConnected && this._hasRealtimeDirectMA()) this._connectMA();
      }, Math.max(500, Number(delay) || 8e3));
    }
    _connectMA() {
      clearTimeout(this._maReconnectTimer);
      this._maReconnectTimer = null;
      if (!this._hasRealtimeDirectMA()) {
        this._rejectWsPending(new Error("MA WS disabled"));
        this._state.wsReady = false;
        this._syncStatus();
        return;
      }
      if (this._ws) {
        this._rejectWsPending(new Error("MA WS reconnecting"));
        try {
          this._ws.onclose = null;
          this._ws.close();
        } catch (_) {
        }
        this._ws = null;
      }
      const maUrl = this._maBrowserUrl();
      try {
        this._assertMaBrowserUrlSecure(maUrl);
      } catch (error) {
        this._state.wsReady = false;
        this._syncStatus();
        this._handleMusicAssistantIssue(error?.message || this._maMixedContentMessage());
        this._notifyCardIssue("ma-mixed-content", error?.message || this._maMixedContentMessage(), "error", 45e3);
        return;
      }
      if (!maUrl) {
        this._state.wsReady = false;
        this._syncStatus();
        return;
      }
      const wsUrl = maUrl.replace(/^http:\/\//, "ws://").replace(/^https:\/\//, "wss://") + "/ws";
      try {
        const ws = new WebSocket(wsUrl);
        this._ws = ws;
        this._state.wsReady = false;
        this._syncStatus();
        ws.onmessage = (e) => {
          let msg;
          try {
            msg = JSON.parse(e.data);
          } catch (_) {
            return;
          }
          if (msg.server_version && !msg.message_id) {
            try {
              ws.send(JSON.stringify({ message_id: "auth", command: "auth", args: { token: this._maToken } }));
            } catch (_) {
              try {
                ws.close();
              } catch (_2) {
              }
            }
            return;
          }
          const messageId = String(msg.message_id ?? "");
          if (messageId === "auth") {
            this._state.wsReady = !!msg.result?.authenticated;
            this._syncStatus();
            if (this._state.wsReady) {
              this._refreshDirectMaPlayers({ renderMenu: true }).catch(() => {
              });
            }
            return;
          }
          const pending = this._wsPending.get(messageId);
          if (pending) {
            this._wsPending.delete(messageId);
            if (pending.timer) clearTimeout(pending.timer);
            if (msg.error_code) pending.reject(new Error(msg.details || `MA error ${msg.error_code}`));
            else pending.resolve(msg.result);
          }
        };
        ws.onerror = () => {
          this._state.wsReady = false;
          this._syncStatus();
        };
        ws.onclose = () => {
          if (this._ws === ws) this._ws = null;
          this._rejectWsPending(new Error("MA WS closed"));
          this._state.wsReady = false;
          this._syncStatus();
          this._scheduleMaReconnect(8e3);
        };
      } catch (_) {
        this._rejectWsPending(new Error("MA WS connection failed"));
        this._state.wsReady = false;
        this._syncStatus();
        this._scheduleMaReconnect(8e3);
      }
    }
    _wsSend(command, args = {}) {
      return new Promise((resolve, reject) => {
        if (!this._ws || !this._state.wsReady) return reject(new Error("MA WS not ready"));
        const id = String(++this._wsMsgId);
        const timer = setTimeout(() => {
          if (this._wsPending.has(id)) {
            this._wsPending.delete(id);
            reject(new Error("MA WS timeout"));
          }
        }, 1e4);
        this._wsPending.set(id, { resolve, reject, timer });
        try {
          this._ws.send(JSON.stringify({ message_id: id, command, args }));
        } catch (error) {
          clearTimeout(timer);
          this._wsPending.delete(id);
          reject(error);
        }
      });
    }
    async _callDirectMaCommand(command, args = {}) {
      if (this._state.wsReady && this._ws) {
        return this._wsSend(command, args);
      }
      const maUrl = this._maBrowserUrl();
      if (!maUrl) {
        throw new Error("Direct Music Assistant API is not configured");
      }
      this._assertMaBrowserUrlSecure(maUrl);
      const headers = {
        "Content-Type": "application/json",
        "Accept": "application/json"
      };
      if (this._maToken) headers.Authorization = `Bearer ${this._maToken}`;
      const controller = typeof AbortController !== "undefined" ? new AbortController() : null;
      const timeout = controller ? setTimeout(() => controller.abort(), 12e3) : null;
      let response;
      try {
        response = await fetch(`${maUrl}/api`, {
          method: "POST",
          credentials: "include",
          mode: "cors",
          headers,
          signal: controller?.signal,
          body: JSON.stringify({
            message_id: `rest_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`,
            command,
            args
          })
        });
      } finally {
        if (timeout) clearTimeout(timeout);
      }
      const rawText = await response.text().catch(() => "");
      let raw = {};
      try {
        raw = rawText ? JSON.parse(rawText) : {};
      } catch (_) {
        raw = { error: rawText || `${command} failed` };
      }
      if (!response.ok || raw?.error_code) {
        throw new Error(raw?.details || raw?.error || `${command} failed`);
      }
      return raw?.result ?? raw;
    }
    async _callHaServiceRaw(domain, service, serviceData = {}, returnResponse = false) {
      const request = this._hass.connection.sendMessagePromise({
        type: "call_service",
        domain,
        service,
        service_data: { ...serviceData },
        return_response: !!returnResponse
      });
      return this._withTimeout(request, this._musicAssistantTimeoutMs(), this._timeoutMessage(`${domain}.${service}`));
    }
    async _callHaServiceTargeted(domain, service, serviceData = {}, target = {}, returnResponse = false) {
      const payload = {
        type: "call_service",
        domain,
        service,
        service_data: { ...serviceData },
        return_response: !!returnResponse
      };
      if (target && typeof target === "object" && Object.keys(target).length) {
        payload.target = { ...target };
      }
      return this._withTimeout(
        this._hass.connection.sendMessagePromise(payload),
        this._musicAssistantTimeoutMs(),
        this._timeoutMessage(`${domain}.${service}`)
      );
    }
    async _callHaMediaPlayerService(entityId, service, serviceData = {}) {
      const targetId = String(entityId || "").trim();
      if (!targetId) return null;
      const data = { ...serviceData || {} };
      try {
        return await this._callHaServiceTargeted("media_player", service, data, { entity_id: targetId });
      } catch (targetError) {
        try {
          return await this._callHaServiceRaw("media_player", service, { entity_id: targetId, ...data });
        } catch (rawError) {
          throw rawError || targetError;
        }
      }
    }
    async _callMusicAssistantTransferQueue(sourcePlayerEntityId, targetPlayerEntityId) {
      const sourceId = String(sourcePlayerEntityId || "").trim();
      const targetId = String(targetPlayerEntityId || "").trim();
      if (!sourceId || !targetId || sourceId === targetId) return false;
      const data = { source_player: sourceId, auto_play: true };
      try {
        await this._callHaServiceTargeted("music_assistant", "transfer_queue", data, { entity_id: targetId });
        return true;
      } catch (targetError) {
        try {
          await this._callHaServiceRaw("music_assistant", "transfer_queue", { entity_id: targetId, ...data });
          return true;
        } catch (rawError) {
          throw rawError || targetError;
        }
      }
    }
    _activePlayerHelperEntity() {
      return String(this._config?.active_player_helper_entity || "").trim();
    }
    _syncActivePlayerHelper(player = this._getSelectedPlayer()) {
      const helperEntity = this._activePlayerHelperEntity();
      if (!helperEntity || !this._hass?.callService) return;
      if (!helperEntity.startsWith("input_text.")) {
        this._notifyCardIssue(
          "active-player-helper-domain",
          this._i18n("ui.active_player_helper_must_be_an_input_text_entity"),
          "error",
          6e4
        );
        return;
      }
      const value = String(player?.entity_id || this._state.selectedPlayer || "").trim();
      if (this._state.activePlayerHelperLastValue === value) return;
      this._state.activePlayerHelperLastValue = value;
      this._hass.callService("input_text", "set_value", {
        entity_id: helperEntity,
        value
      }).catch((error) => {
        this._state.activePlayerHelperLastValue = void 0;
        this._notifyCardIssue(
          "active-player-helper-update",
          error?.message || this._i18n("ui.could_not_update_active_player_helper"),
          "error",
          6e4
        );
      });
    }
    _notifyCardIssue(key = "issue", message = "", variant = "error", cooldown = 3e4) {
      const text = String(message || "").trim();
      if (!text) return;
      if (!this._cardIssueNoticeTimes) this._cardIssueNoticeTimes = /* @__PURE__ */ new Map();
      const now = Date.now();
      const noticeKey = String(key || text);
      const last = this._cardIssueNoticeTimes.get(noticeKey) || 0;
      if (now - last < cooldown) return;
      this._cardIssueNoticeTimes.set(noticeKey, now);
      if (variant === "success") this._toastSuccess(text);
      else if (variant === "info") this._toast(text);
      else this._toastError(text);
    }
    _isMissingMusicAssistantConfigError(error = null) {
      const message = String(error?.message || error || "").toLowerCase();
      return message.includes("homeii_ma_not_ready") || message.includes("config entry") && message.includes("music assistant") || message.includes("entry not loaded") || message.includes("music assistant entry") || message.includes("music assistant is not ready");
    }
    _isMusicAssistantAvailabilityError(error = null) {
      const message = String(error?.message || error || "").toLowerCase();
      return this._isMissingMusicAssistantConfigError(error) || error?.code === "HOMEII_TIMEOUT" || error?.code === "HOMEII_MA_NOT_READY" || message.includes("music assistant") && message.includes("timeout") || message.includes("music_assistant") && (message.includes("not found") || message.includes("not loaded")) || message.includes("service") && message.includes("music_assistant") && message.includes("not found");
    }
    _handleMusicAssistantIssue(error = null) {
      const rawMessage = String(error?.message || error || "").trim();
      const message = this._isMusicAssistantAvailabilityError(error) ? this._musicAssistantSetupMessage(rawMessage) : rawMessage || this._musicAssistantSetupMessage();
      this._state.musicAssistantIssueMessage = message;
      this._notifyCardIssue("music-assistant-not-ready", message, "error", 45e3);
      return message;
    }
    _musicAssistantRequiredTitle() {
      return this._i18n(
        "ui.music_assistant_required",
        {},
        this._m("Music Assistant required", "נדרשת אינטגרציית Music Assistant")
      );
    }
    _musicAssistantRequiredMessage() {
      return this._i18n(
        "ui.no_music_assistant_players_were_found_check_music_assistant_and_media_pl",
        {},
        this._m(
          "Enable the Music Assistant integration in Home Assistant and expose at least one Music Assistant media_player for this card to work. Other Home Assistant media players are intentionally hidden.",
          "צריך להפעיל את אינטגרציית Music Assistant ב-Home Assistant ולחשוף לפחות נגן Music Assistant אחד כדי שהכרטיס יפעל. נגני Home Assistant רגילים מוסתרים בכוונה."
        )
      );
    }
    _musicAssistantSetupMessage(detail = "") {
      const suffix = String(detail || "").trim();
      const base = this._musicAssistantRequiredMessage();
      return suffix && !suffix.toLowerCase().includes(base.toLowerCase()) ? `${base} ${suffix}` : base;
    }
    async _fetchRecentlyPlayed(limit = 18) {
      if (!this._state.wsReady) {
        try {
          return this._normalizeMediaItems(await this._fetchLibrary("album", "last_played", limit, false));
        } catch (_) {
          return [];
        }
      }
      try {
        const items = await this._wsSend("music/recently_played_items", { limit, media_types: ["album"] });
        return this._normalizeMediaItems(Array.isArray(items) ? items : []);
      } catch (_) {
        return [];
      }
    }
    _loadPlayers() {
      const sel = this.$("playerSel");
      const hassStates = this._hass?.states || {};
      const hassEntities = this._hass?.entities || {};
      const musicAssistantEntities = Object.values(hassStates).filter((entity) => HomeiiPlayersFoundation2.isMusicAssistantPlayer(entity, hassEntities?.[entity.entity_id]));
      const genericMediaPlayerEntities = Object.values(hassStates).filter((entity) => entity?.entity_id?.startsWith("media_player."));
      let entities = musicAssistantEntities.length ? musicAssistantEntities : this._hasMusicAssistantBackend() ? genericMediaPlayerEntities : [];
      if (!entities.length) {
        const message = this._handleMusicAssistantIssue(this._musicAssistantRequiredMessage());
        this._state.players = [];
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
        this._state.maQueueState = null;
        this._state.queueItems = [];
        if (sel) sel.innerHTML = `<option value="">${this._esc(this._musicAssistantRequiredTitle())}</option>`;
        this._syncActivePlayerHelper(null);
        this._notifyCardIssue("no-music-assistant-players", message, "error", 45e3);
        return;
      }
      this._state.musicAssistantIssueMessage = "";
      const directPlayers = this._mergeDirectMaPlayers(entities);
      if (directPlayers.length) entities = [...entities, ...directPlayers];
      entities = entities.filter((entity) => !this._isLocalSendspinPlayer(entity) || this._isAvailableThisDevicePlayer(entity));
      this._state.configurableMusicAssistantPlayers = entities.slice();
      const excludedPrefs = typeof this._excludedPlayerPreferences === "function" ? this._excludedPlayerPreferences() : [];
      if (excludedPrefs.length) {
        const excludedSet = new Set(excludedPrefs);
        entities = entities.filter((entity) => !excludedSet.has(entity.entity_id));
      }
      const pinnedPrefs = typeof this._pinnedPlayerPreferences === "function" ? this._pinnedPlayerPreferences() : [];
      if (pinnedPrefs.length) {
        const pinnedSet = new Set(pinnedPrefs);
        const frontPinnedEntityId = String(this._state.frontPinnedPlayerEntity || "").trim();
        const manualFrontEntityId = String(this._state.manualFrontPlayerEntity || "").trim();
        const manualFrontActive = manualFrontEntityId && Number(this._state.manualFrontPlayerUntil || 0) > Date.now();
        entities = entities.filter((entity) => pinnedSet.has(entity.entity_id) || entity.entity_id === frontPinnedEntityId || manualFrontActive && entity.entity_id === manualFrontEntityId || entity.state === "playing" || this._isLocalSendspinPlayer(entity));
      }
      entities = entities.map((entity) => this._applyOptimisticPlayerVolumeState(entity));
      entities = typeof this._sortPlayerList === "function" ? this._sortPlayerList(entities) : entities;
      this._state.players = entities;
      if (!entities.length) {
        const message = excludedPrefs.length ? this._i18n("ui.no_visible_music_assistant_players_after_exclusions", {}, this._m(
          "All Music Assistant players are hidden by the excluded players setting.",
          "כל נגני Music Assistant מוסתרים בהגדרת החרגת נגנים."
        )) : this._handleMusicAssistantIssue(this._musicAssistantRequiredMessage());
        if (excludedPrefs.length) this._state.musicAssistantIssueMessage = message;
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
        this._state.maQueueState = null;
        this._state.queueItems = [];
        if (sel) sel.innerHTML = `<option value="">${this._esc(this._musicAssistantRequiredTitle())}</option>`;
        this._syncActivePlayerHelper(null);
        this._notifyCardIssue("no-music-assistant-players", message, excludedPrefs.length ? "warning" : "error", 45e3);
        return;
      }
      const rememberedThisDevice = this._getThisDevicePlayer(entities);
      const pinnedEntities = typeof this._resolvedPinnedPlayerEntities === "function" ? this._resolvedPinnedPlayerEntities(entities) : [];
      if (this._state.awaitingThisDevicePlayer) {
        const localThisDevice = this._getThisDevicePlayer(entities);
        const newcomer = localThisDevice;
        if (newcomer) {
          this._rememberThisDevicePlayer(newcomer.entity_id);
          this._state.awaitingThisDevicePlayer = false;
          this._state.knownBrowserPlayerIds = [];
          this._state.selectedPlayer = newcomer.entity_id;
          this._state.hasAutoSelectedPlayer = true;
          this._revealControlRoomThisDevicePlayer(newcomer.entity_id, { sync: false });
        }
      }
      const selectedPlayer = this._playerByEntityId(this._state.selectedPlayer);
      if (this._state.awaitingThisDevicePlayer && this._isExternalBrowserPlayer(selectedPlayer)) {
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
      }
      const pendingPlayerLock = HomeiiNowPlayingFoundation2.pendingPlayerLockState(this._state, entities, Date.now());
      if (pendingPlayerLock.lockedPlayerId && this._state.selectedPlayer !== pendingPlayerLock.lockedPlayerId) {
        this._state.selectedPlayer = pendingPlayerLock.lockedPlayerId;
      }
      const holdSelectedPlayerDuringTransition = pendingPlayerLock.shouldHoldSelectedPlayer;
      const preferredFrontPlayerEntity = holdSelectedPlayerDuringTransition ? "" : HomeiiPlayersFoundation2.resolvePreferredFrontPlayerEntity(entities, {
        currentEntityId: this._state.selectedPlayer,
        frontPinnedEntityId: this._frontPinnedPlayerEntity(entities),
        manualFrontEntityId: this._manualFrontPlayerEntity(entities),
        manualFrontUntil: this._state.manualFrontPlayerUntil,
        now: Date.now(),
        pinnedEntityIds: pinnedEntities,
        orderedEntityIds: typeof this._playerOrderPreferences === "function" && this._playerSortMode?.() === "custom" ? this._playerOrderPreferences() : [],
        isPlayerActiveFn: (player) => this._isPlayerActive(player),
        isExternalBrowserPlayerFn: (player) => this._isExternalBrowserPlayer(player)
      });
      if (preferredFrontPlayerEntity) {
        if (this._state.selectedPlayer !== preferredFrontPlayerEntity) this._state.selectedPlayer = preferredFrontPlayerEntity;
        this._state.hasAutoSelectedPlayer = true;
      }
      const currentStillExists = this._state.selectedPlayer && entities.some((p) => p.entity_id === this._state.selectedPlayer);
      if (!currentStillExists) this._state.selectedPlayer = null;
      if (!this._state.selectedPlayer) {
        const fallbackPlayer = rememberedThisDevice || entities.find((p) => !this._isExternalBrowserPlayer(p)) || entities[0];
        if (fallbackPlayer) this._state.selectedPlayer = fallbackPlayer.entity_id;
      }
      if (sel) {
        sel.innerHTML = entities.map((entity) => {
          const active = this._isPlayerActive(entity) ? "● " : "";
          const name = entity.attributes.friendly_name || entity.entity_id;
          return `<option value="${this._esc(entity.entity_id)}">${this._esc(active + name)}</option>`;
        }).join("");
        sel.value = this._state.selectedPlayer || "";
      }
      this._syncActivePlayerHelper();
    }
    _getSelectedPlayer() {
      if (!this._state.selectedPlayer) return null;
      return this._playerByEntityId(this._state.selectedPlayer);
    }
    _selectedPlayerMoreInfoEntityId(player = this._getSelectedPlayer()) {
      const entityId = String(player?.entity_id || "").trim();
      if (!entityId) return "";
      if (this._hass?.states?.[entityId]) return entityId;
      const attrs = player?.attributes || {};
      const directIds = [
        this._directMaPlayerId(player),
        attrs.mass_player_id,
        attrs.player_id,
        attrs.id,
        attrs.active_queue,
        attrs.queue_id
      ].map((value) => String(value || "").trim()).filter(Boolean);
      if (directIds.length && this._hass?.states) {
        const match = Object.values(this._hass.states).find((stateObj) => {
          if (!stateObj?.entity_id?.startsWith?.("media_player.")) return false;
          const stateAttrs = stateObj.attributes || {};
          return [
            stateAttrs.mass_player_id,
            stateAttrs.player_id,
            stateAttrs.id,
            stateAttrs.active_queue,
            stateAttrs.queue_id
          ].some((value) => directIds.includes(String(value || "").trim()));
        });
        if (match?.entity_id) return match.entity_id;
      }
      return this._isDirectMaEntityId(entityId) ? "" : entityId;
    }
    _openSelectedPlayerMoreInfo() {
      const player = this._getSelectedPlayer();
      const entityId = this._selectedPlayerMoreInfoEntityId(player);
      if (!entityId) {
        this._toast(this._i18n("ui.more_info_is_unavailable_for_this_player"));
        return;
      }
      this.dispatchEvent(new CustomEvent("hass-more-info", {
        detail: { entityId },
        bubbles: true,
        composed: true
      }));
    }
    async _playMediaOnPlayer(entityId, uri, mediaType = "album", enqueue = "play", options = {}) {
      if (!entityId) return false;
      const label = this._mediaFeedbackLabel(uri, options.label || "");
      try {
        if (this._isDirectMaPlayer(entityId)) {
          return await this._playMediaOnDirectMaPlayer(entityId, uri, mediaType, enqueue, options);
        }
        if (enqueue === "shuffle") {
          await this._callHaServiceRaw("media_player", "shuffle_set", { entity_id: entityId, shuffle: true });
        }
        const serviceData = {
          entity_id: entityId,
          media_id: uri,
          media_type: mediaType,
          enqueue: enqueue === "shuffle" ? "play" : enqueue
        };
        if (options.radioMode) serviceData.radio_mode = true;
        await this._callHaServiceRaw("music_assistant", "play_media", serviceData);
        if (!options.silent) {
          const targetPlayer = this._playerByEntityId(entityId);
          this._toastMediaQueued(label, targetPlayer?.attributes?.friendly_name || entityId);
        }
        if (entityId === this._state.selectedPlayer) setTimeout(() => this._ensureQueueSnapshot(true), 600);
        return true;
      } catch (error) {
        if (!options.silent) {
          if (this._isMusicAssistantAvailabilityError(error)) this._handleMusicAssistantIssue(error);
          else this._toastError(this._i18n("ui.could_not_play_label", { label }));
        }
        return false;
      }
    }
    async _playMediaOnPlayers(entityIds = [], uri, mediaType = "album", enqueue = "play", options = {}) {
      const targets = [...new Set((Array.isArray(entityIds) ? entityIds : []).filter(Boolean))];
      if (!targets.length) return false;
      const results = await Promise.allSettled(targets.map((entityId) => this._playMediaOnPlayer(entityId, uri, mediaType, enqueue, { ...options, silent: true })));
      const successCount = results.filter((result) => result.status === "fulfilled" && result.value).length;
      if (!options.silent) {
        const successMessage = successCount > 1 ? this._m(`Started on ${successCount} players`, `הופעל על ${successCount} נגנים`) : this._i18n("ui.started_on_selected_player");
        (successCount ? this._toastSuccess : this._toastError).call(this, successCount ? successMessage : this._i18n("ui.could_not_start_playback"));
      }
      return successCount > 0;
    }
    async _playMedia(uri, mediaType = "album", enqueue = "play", options = {}) {
      if (!this._state.selectedPlayer) {
        this._toastError(this._i18n("ui.select_a_player_first"));
        return false;
      }
      this._state.forceRadioHero = !!options.forceRadioHero || String(mediaType || "").toLowerCase() === "radio";
      if (options.sourceEl) this._flashInteraction(options.sourceEl);
      return this._playMediaOnPlayer(this._state.selectedPlayer, uri, mediaType, enqueue, options);
    }
    _supportsMusicAssistantRadioMode(mediaType = "") {
      return ["track", "album", "artist", "playlist"].includes(String(mediaType || "").toLowerCase());
    }
    async _playAll(items = [], shuffle = false) {
      if (!items.length) return;
      if (!this._state.selectedPlayer) return this._toast(this._i18n("ui.select_a_player_first"));
      if (this._isDirectMaPlayer(this._state.selectedPlayer)) {
        const playable = items.map((item) => item?.uri).filter(Boolean);
        if (!playable.length) return;
        await this._playMediaOnPlayer(this._state.selectedPlayer, playable, items[0]?.media_type || "track", shuffle ? "shuffle" : "play", {
          label: items[0]?.name || "",
          silent: true
        });
        setTimeout(() => this._ensureQueueSnapshot(true), 600);
        return;
      }
      if (shuffle) {
        await this._callHaServiceRaw("media_player", "shuffle_set", { entity_id: this._state.selectedPlayer, shuffle: true });
      }
      await this._callHaServiceRaw("music_assistant", "play_media", {
        entity_id: this._state.selectedPlayer,
        media_id: items[0].uri,
        media_type: items[0].media_type || "album",
        enqueue: "play"
      });
      for (let i = 1; i < items.length; i++) {
        await this._callHaServiceRaw("music_assistant", "play_media", {
          entity_id: this._state.selectedPlayer,
          media_id: items[i].uri,
          media_type: items[i].media_type || "album",
          enqueue: "add"
        });
      }
      setTimeout(() => this._ensureQueueSnapshot(true), 600);
    }
    _likedPlayableEntries(entries = [], selectedOnly = false) {
      const source = Array.isArray(entries) ? entries.filter((entry) => String(entry?.uri || "").trim()) : [];
      const selectedSet = new Set(Array.isArray(this._state.likedSelectedUris) ? this._state.likedSelectedUris : []);
      const scoped = selectedOnly ? source.filter((entry) => selectedSet.has(String(entry?.uri || "").trim())) : source;
      const tracks = scoped.filter((entry) => String(entry?.media_type || "").toLowerCase() === "track");
      return (tracks.length ? tracks : scoped).map((entry) => ({
        uri: String(entry.uri || "").trim(),
        media_type: entry.media_type || "track",
        name: entry.name || ""
      })).filter((entry) => entry.uri);
    }
    _togglePlay() {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const nextState = player.state === "playing" ? "paused" : "playing";
      this._setPlayerPlaybackOptimistic(player.entity_id, { state: nextState });
      if (this._isDirectMaPlayer(player)) {
        this._callDirectMaPlayerCommand(player, player.state === "playing" ? "players/cmd/pause" : "players/cmd/play").then(() => this._refreshDirectMaPlayers().catch(() => {
        })).catch((error) => {
          this._clearPlayerPlaybackOptimistic(player.entity_id);
          this._toastError(this._mediaControlFailureMessage(error));
          this._refreshDirectMaPlayers().catch(() => {
          });
        });
        return;
      }
      const service = player.state === "playing" ? "media_pause" : "media_play";
      this._callHaServiceRaw("media_player", service, { entity_id: player.entity_id }).catch((error) => {
        this._clearPlayerPlaybackOptimistic(player.entity_id);
        this._toastError(this._mediaControlFailureMessage(error));
        this._loadPlayers();
        this._syncNowPlayingUI();
      });
    }
    _playerCmd(cmd) {
      const player = this._getSelectedPlayer();
      if (!player) return;
      let queueItems = HomeiiMediaQueueFoundation2.sortQueueItems(this._state.queueItems || []);
      const currentQueueIndex = Number(this._state.maQueueState?.current_index);
      let baseIndex = Number.isFinite(currentQueueIndex) ? queueItems.findIndex((item) => Number(item?.sort_index) === currentQueueIndex) : -1;
      if (baseIndex < 0 && typeof this._mobileArtStackContext === "function") {
        const context = this._mobileArtStackContext();
        queueItems = Array.isArray(context.queueItems) ? context.queueItems : queueItems;
        baseIndex = Number(context.baseIndex);
      }
      const targetIndex = cmd === "previous" ? baseIndex - 1 : baseIndex + 1;
      const targetItem = Array.isArray(queueItems) ? queueItems[targetIndex] : null;
      if (targetItem) {
        this._setOptimisticMobileQueueItem(targetItem);
        this._setPlayerPlaybackOptimistic(player.entity_id, { state: "playing", until: Date.now() + 8500 });
        this._refreshMobileArtStack(true);
        this._syncNowPlayingUI();
      }
      if (this._isDirectMaPlayer(player)) {
        this._callDirectMaPlayerCommand(player, cmd === "previous" ? "players/cmd/previous" : "players/cmd/next").then(() => {
          setTimeout(() => this._ensureQueueSnapshot(true), 700);
          setTimeout(() => this._ensureQueueSnapshot(true), 1500);
          setTimeout(() => this._ensureQueueSnapshot(true), 2850);
        }).catch((error) => this._toastError(error?.message || this._i18n("ui.playback_command_failed")));
        return;
      }
      this._callHaServiceRaw("media_player", cmd === "previous" ? "media_previous_track" : "media_next_track", { entity_id: player.entity_id }).catch((error) => this._toastError(this._mediaControlFailureMessage(error)));
      setTimeout(() => this._ensureQueueSnapshot(true), 700);
      setTimeout(() => this._ensureQueueSnapshot(true), 1500);
      setTimeout(() => this._ensureQueueSnapshot(true), 2850);
    }
    async _playerCmdFor(entityId, cmd = "next") {
      const player = this._playerByEntityId(entityId);
      if (!player) return;
      if (this._isDirectMaPlayer(player)) {
        await this._callDirectMaPlayerCommand(player, cmd === "previous" ? "players/cmd/previous" : "players/cmd/next");
        if (entityId === this._state.selectedPlayer) {
          setTimeout(() => this._ensureQueueSnapshot(true), 700);
          setTimeout(() => this._ensureQueueSnapshot(true), 1500);
        }
        return;
      }
      const service = cmd === "previous" ? "media_previous_track" : "media_next_track";
      await this._callHaServiceRaw("media_player", service, { entity_id: player.entity_id });
      if (entityId === this._state.selectedPlayer) {
        setTimeout(() => this._ensureQueueSnapshot(true), 700);
        setTimeout(() => this._ensureQueueSnapshot(true), 1500);
      }
    }
    async _togglePlayFor(entityId) {
      if (!entityId) return;
      if (this._isDirectMaPlayer(entityId)) {
        await this._callDirectMaPlayerCommand(entityId, "players/cmd/play_pause");
        return;
      }
      const player = this._playerByEntityId(entityId);
      if (player) {
        this._setPlayerPlaybackOptimistic(entityId, { state: player.state === "playing" ? "paused" : "playing" });
      }
      await this._callHaServiceRaw("media_player", "media_play_pause", { entity_id: entityId });
    }
    async _stepQueueByDelta(delta) {
      const player = this._getSelectedPlayer();
      const steps = Math.abs(Math.trunc(Number(delta) || 0));
      if (!player || !steps) return !steps;
      const service = delta > 0 ? "media_next_track" : "media_previous_track";
      for (let i = 0; i < steps; i += 1) {
        await this._callHaServiceRaw("media_player", service, { entity_id: player.entity_id });
        if (i < steps - 1) {
          await new Promise((resolve) => window.setTimeout(resolve, 85));
        }
      }
      return true;
    }
    _toggleShuffle() {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const nextShuffle = !player.attributes.shuffle;
      this._setPlayerPlaybackOptimistic(player.entity_id, { shuffle: nextShuffle });
      if (this._isDirectMaPlayer(player)) {
        const queueId = this._directMaQueueId(player);
        if (!queueId) {
          this._clearPlayerPlaybackOptimistic(player.entity_id);
          this._syncNowPlayingUI();
          return;
        }
        this._callDirectMaCommand("player_queues/shuffle", { queue_id: queueId, shuffle_enabled: nextShuffle }).then(() => this._refreshDirectMaPlayers().catch(() => {
        })).catch((error) => {
          this._clearPlayerPlaybackOptimistic(player.entity_id);
          this._toastError(this._mediaControlFailureMessage(error));
          this._refreshDirectMaPlayers().catch(() => {
          });
          this._syncNowPlayingUI();
        });
        return;
      }
      this._callHaServiceRaw("media_player", "shuffle_set", { entity_id: player.entity_id, shuffle: nextShuffle }).catch((error) => {
        this._clearPlayerPlaybackOptimistic(player.entity_id);
        this._toastError(this._mediaControlFailureMessage(error));
        this._loadPlayers();
        this._syncNowPlayingUI();
      });
    }
    _repeatModeLabel(mode = "off") {
      const normalized = String(mode || "off").toLowerCase();
      if (normalized === "one") return this._i18n("ui.repeat_one");
      if (normalized === "all") return this._i18n("ui.repeat_all");
      return this._i18n("ui.repeat_off");
    }
    _toggleRepeat() {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const modes = ["off", "one", "all"];
      const current = player.attributes.repeat || "off";
      const next = modes[(modes.indexOf(current) + 1) % modes.length];
      this._setPlayerPlaybackOptimistic(player.entity_id, { repeat: next });
      if (this._isDirectMaPlayer(player)) {
        const queueId = this._directMaQueueId(player);
        if (!queueId) {
          this._clearPlayerPlaybackOptimistic(player.entity_id);
          this._syncNowPlayingUI();
          return;
        }
        this._callDirectMaCommand("player_queues/repeat", { queue_id: queueId, repeat_mode: next }).then(() => this._refreshDirectMaPlayers().catch(() => {
        })).catch((error) => {
          this._clearPlayerPlaybackOptimistic(player.entity_id);
          this._toastError(this._mediaControlFailureMessage(error));
          this._refreshDirectMaPlayers().catch(() => {
          });
          this._syncNowPlayingUI();
        });
        return;
      }
      this._callHaServiceRaw("media_player", "repeat_set", { entity_id: player.entity_id, repeat: next }).catch((error) => {
        this._clearPlayerPlaybackOptimistic(player.entity_id);
        this._toastError(this._mediaControlFailureMessage(error));
        this._loadPlayers();
        this._syncNowPlayingUI();
      });
    }
    _setVolume(level) {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const normalized = Math.max(0, Math.min(1, Number(level) || 0));
      this._setPlayerVolumeOptimistic(player.entity_id, normalized, normalized <= 0 ? true : false);
      if (this._isDirectMaPlayer(player)) {
        this._callDirectMaPlayerCommand(player, "players/cmd/volume_set", { volume_level: Math.round(normalized * 100) }).then(() => this._refreshDirectMaPlayers().catch(() => {
        })).catch((error) => this._toastError(this._mediaControlFailureMessage(error)));
        return;
      }
      this._callHaServiceRaw("media_player", "volume_set", { entity_id: player.entity_id, volume_level: normalized }).catch((error) => this._toastError(this._mediaControlFailureMessage(error)));
    }
    _stepSelectedVolume(direction = 0) {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const currentPct = Math.round(this._effectivePlayerVolumeLevel(player) * 100);
      const stepPct = this._mobileVolumeStepPercent();
      const nextPct = Math.max(0, Math.min(100, currentPct + (direction < 0 ? -stepPct : stepPct)));
      const slider = this.$("volSlider");
      if (slider) {
        slider.value = String(nextPct);
        slider.style.setProperty("--vol-pct", `${nextPct}%`);
      }
      const label = this.$("mobileVolPctLabel");
      if (label) label.textContent = `${nextPct}%`;
      this._setButtonIcon(this.$("btnMute"), nextPct === 0 ? "volume_mute" : nextPct < 40 ? "volume_low" : "volume_high");
      this.$("btnMute")?.classList.toggle("muted", nextPct === 0);
      this._setVolume(nextPct / 100);
      this._syncNowPlayingUI();
    }
    _applyOptimisticPlayerVolumeState(player = null) {
      const entityId = String(player?.entity_id || "").trim();
      if (!entityId) return player;
      const now = Date.now();
      const volumeState = this._optimisticVolumeByPlayer.get(entityId);
      const muteState = this._optimisticMuteByPlayer.get(entityId);
      const playbackState = this._optimisticPlaybackByPlayer.get(entityId);
      const patch = {};
      let statePatch = "";
      if (volumeState && now - Number(volumeState.ts || 0) < 5e3) {
        patch.volume_level = volumeState.level;
      } else if (volumeState) {
        this._optimisticVolumeByPlayer.delete(entityId);
      }
      if (muteState && now - Number(muteState.ts || 0) < 5e3) {
        patch.is_volume_muted = !!muteState.muted;
      } else if (muteState) {
        this._optimisticMuteByPlayer.delete(entityId);
      }
      const playbackStateStillFresh = playbackState && (Number(playbackState.until || 0) > now || now - Number(playbackState.ts || 0) < 5e3);
      if (playbackStateStillFresh) {
        if (playbackState.state) statePatch = playbackState.state;
        if (playbackState.shuffle !== void 0) patch.shuffle = !!playbackState.shuffle;
        if (playbackState.repeat !== void 0) patch.repeat = playbackState.repeat;
      } else if (playbackState) {
        this._optimisticPlaybackByPlayer.delete(entityId);
      }
      if (!Object.keys(patch).length && !statePatch) return player;
      return {
        ...player,
        ...statePatch ? { state: statePatch } : {},
        attributes: {
          ...player.attributes || {},
          ...patch
        }
      };
    }
    _setPlayerPlaybackOptimistic(entityId, patch = {}) {
      const playerId = String(entityId || "").trim();
      if (!playerId) return;
      const current = this._optimisticPlaybackByPlayer.get(playerId) || {};
      this._optimisticPlaybackByPlayer.set(playerId, {
        ...current,
        ...patch,
        ts: Date.now()
      });
      const updatePlayer = (player) => {
        if (!player || player.entity_id !== playerId) return player;
        return this._applyOptimisticPlayerVolumeState(player);
      };
      this._state.players = (this._state.players || []).map(updatePlayer);
      this._directMaPlayers = (this._directMaPlayers || []).map(updatePlayer);
      this._syncNowPlayingUI();
    }
    _clearPlayerPlaybackOptimistic(entityId) {
      const playerId = String(entityId || "").trim();
      if (!playerId) return;
      this._optimisticPlaybackByPlayer.delete(playerId);
    }
    _mediaControlFailureMessage(error = null) {
      const message = String(error?.message || error || "").trim();
      if (/does not support action|unsupported|not supported/i.test(message)) {
        return this._m(
          "This player does not support that media control through Home Assistant.",
          "הנגן הזה לא תומך בפקד המדיה הזה דרך Home Assistant."
        );
      }
      if (this._isMusicAssistantAvailabilityError(error)) return this._handleMusicAssistantIssue(error);
      return message || this._i18n("ui.playback_command_failed");
    }
    _setPlayerVolumeOptimistic(entityId, level, muted = null) {
      const normalized = Math.max(0, Math.min(1, Number(level) || 0));
      const playerId = String(entityId || "").trim();
      if (!playerId) return;
      const now = Date.now();
      this._optimisticVolumeByPlayer.set(playerId, { level: normalized, ts: now });
      if (muted !== null) this._optimisticMuteByPlayer.set(playerId, { muted: !!muted, ts: now });
      if (muted === true) this._softMutedPlayers.add(playerId);
      if (muted === false) this._softMutedPlayers.delete(playerId);
      const updatePlayer = (player) => {
        if (!player || player.entity_id !== playerId) return player;
        return this._applyOptimisticPlayerVolumeState(player);
      };
      this._state.players = (this._state.players || []).map(updatePlayer);
      this._directMaPlayers = (this._directMaPlayers || []).map(updatePlayer);
      this._syncPlayerVolumeControls(playerId, Math.round(normalized * 100), { muted });
    }
    _syncPlayerVolumeControls(entityId, pct, options = {}) {
      const playerId = String(entityId || "").trim();
      if (!playerId) return;
      const value = Math.max(0, Math.min(100, Math.round(Number(pct) || 0)));
      const muted = options.muted !== void 0 ? !!options.muted : value === 0;
      const icon = muted || value === 0 ? "volume_mute" : value < 40 ? "volume_low" : "volume_high";
      if (playerId === String(this._state.selectedPlayer || "")) {
        const slider = this.$("volSlider");
        if (slider) {
          if (String(slider.value) !== String(value)) slider.value = String(value);
          slider.style.setProperty("--vol-pct", `${value}%`);
        }
        const label = this.$("mobileVolPctLabel");
        if (label) label.textContent = `${value}%`;
        const muteBtn = this.$("btnMute");
        if (muteBtn) {
          muteBtn.classList.toggle("active", muted);
          muteBtn.classList.toggle("muted", muted);
          this._setButtonIcon(muteBtn, icon);
        }
        const controlVolumeBtn = this.$("controlVolumeBtn");
        if (controlVolumeBtn) {
          controlVolumeBtn.classList.toggle("muted", muted);
          this._setButtonIcon(controlVolumeBtn, icon);
        }
        const popupSlider = this.$("tabletPopupVolSlider");
        if (popupSlider) {
          if (String(popupSlider.value) !== String(value)) popupSlider.value = String(value);
          popupSlider.style.setProperty("--vol-pct", `${value}%`);
        }
        const popupValue = this.$("tabletPopupVolPct");
        if (popupValue) popupValue.textContent = `${value}%`;
        const popupMuteBtn = this.$("tabletPopupMuteBtn");
        if (popupMuteBtn) this._setButtonIcon(popupMuteBtn, icon);
      }
      this.shadowRoot?.querySelectorAll("[data-player-volume]")?.forEach((input) => {
        if (input.dataset.playerVolume !== playerId) return;
        if (String(input.value) !== String(value)) input.value = String(value);
        input.style.setProperty("--vol-pct", `${value}%`);
        const row = input.closest(".player-volume-row");
        const valueEl = row?.querySelector(".player-mini-value");
        if (valueEl) valueEl.textContent = `${value}%`;
        const muteBtn = row?.querySelector("[data-player-mute]");
        if (muteBtn) {
          muteBtn.classList.toggle("active", muted);
          this._setButtonIcon(muteBtn, icon);
        }
      });
      this.shadowRoot?.querySelectorAll("[data-player-mute]")?.forEach((button) => {
        if (button.dataset.playerMute !== playerId) return;
        button.classList.toggle("active", muted);
        this._setButtonIcon(button, icon);
      });
    }
    _isMuted(player) {
      if (!player) return false;
      return !!player.attributes?.is_volume_muted || this._softMutedPlayers.has(player.entity_id);
    }
    async _toggleMute() {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const entityId = player.entity_id;
      const currentlyMuted = this._isMuted(player);
      const currentVolume = Number(player.attributes.volume_level ?? 0);
      if (!currentlyMuted && currentVolume > 0) this._lastVolumeByPlayer.set(entityId, currentVolume);
      const restoreVolume = this._lastVolumeByPlayer.get(entityId) ?? 0.35;
      const targetVolume = currentlyMuted ? currentVolume === 0 ? restoreVolume : currentVolume : 0;
      if (this._isDirectMaPlayer(player)) {
        this._setPlayerVolumeOptimistic(entityId, currentVolume, !currentlyMuted);
        try {
          await this._callDirectMaPlayerCommand(player, "players/cmd/volume_mute", { muted: !currentlyMuted });
        } catch (error) {
          this._toastError(this._mediaControlFailureMessage(error));
        }
        this._refreshDirectMaPlayers().catch(() => {
        });
        setTimeout(() => this._syncNowPlayingUI(), 120);
        return;
      }
      this._setPlayerVolumeOptimistic(entityId, targetVolume, !currentlyMuted);
      try {
        await this._callHaServiceRaw("media_player", "volume_mute", { entity_id: entityId, is_volume_muted: !currentlyMuted });
      } catch (error) {
        this._toastError(this._mediaControlFailureMessage(error));
      }
      if (!currentlyMuted) {
        if (currentVolume > 0) {
          this._callHaServiceRaw("media_player", "volume_set", { entity_id: entityId, volume_level: 0 }).catch((error) => this._toastError(this._mediaControlFailureMessage(error)));
        }
      } else {
        if (currentVolume === 0) {
          this._callHaServiceRaw("media_player", "volume_set", { entity_id: entityId, volume_level: restoreVolume }).catch((error) => this._toastError(this._mediaControlFailureMessage(error)));
        }
        try {
          await this._callHaServiceRaw("media_player", "volume_mute", { entity_id: entityId, is_volume_muted: false });
        } catch (error) {
          this._toastError(this._mediaControlFailureMessage(error));
        }
      }
      setTimeout(() => this._syncNowPlayingUI(), 120);
    }
    _syncStatus() {
      const pill = this.$("statusPill");
      const text = this.$("statusText");
      if (!pill || !text) return;
      if (this._maToken && this._maBrowserUrl()) {
        if (this._state.wsReady) {
          pill.classList.remove("offline");
          text.textContent = this._i18n("ui.connected");
        } else {
          pill.classList.add("offline");
          text.textContent = this._i18n("ui.connecting");
        }
      } else {
        pill.classList.remove("offline");
        text.textContent = this._i18n("ui.connected");
      }
    }
    _getCurrentDuration() {
      const player = this._getSelectedPlayer();
      return HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(
        this._state.maQueueState?.current_item?.duration || this._state.maQueueState?.current_item?.media_item?.duration || player?.attributes?.media_duration || player?.attributes?.current_media?.duration || player?.attributes?.currentMedia?.duration || 0
      );
    }
    _getCurrentPosition() {
      const player = this._getSelectedPlayer();
      const hasQueuePosition = this._state.maQueueState?.elapsed_time !== void 0 && this._state.maQueueState?.elapsed_time !== null && this._state.maQueueState?.elapsed_time !== "";
      let position = hasQueuePosition ? HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(this._state.maQueueState?.elapsed_time) : HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(player?.attributes?.media_position || 0);
      if (this._state.maQueueState && player?.state === "playing" && this._state.maQueueState.elapsed_time_last_updated) {
        const updatedAt = HomeiiMediaPresentationFoundation2.parsePlaybackTimestampMs(this._state.maQueueState.elapsed_time_last_updated);
        if (updatedAt) position += Math.max(0, (Date.now() - updatedAt) / 1e3);
      } else if (player?.state === "playing" && player?.attributes?.media_position_updated_at) {
        const updatedAt = HomeiiMediaPresentationFoundation2.parsePlaybackTimestampMs(player.attributes.media_position_updated_at);
        if (updatedAt) position += Math.max(0, (Date.now() - updatedAt) / 1e3);
      }
      const duration = this._getCurrentDuration();
      if (duration && position > duration + 2) return duration;
      return Math.max(0, position || 0);
    }
    _applyProgressUi(position = this._getCurrentPosition(), duration = this._getCurrentDuration()) {
      const safeDuration = HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(duration);
      const safePosition = safeDuration ? Math.max(0, Math.min(safeDuration, HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(position))) : Math.max(0, HomeiiMediaPresentationFoundation2.coercePlaybackSeconds(position));
      const pct = safeDuration ? Math.min(100, safePosition / safeDuration * 100) : 0;
      ["progressFill", "bigProgressFill", "immersiveProgressFill"].forEach((id) => {
        const el = this.$(id) || this.shadowRoot?.querySelector?.(`#${id}`);
        if (el) el.style.width = `${pct}%`;
      });
      ["bigCurTime", "immersiveCurTime"].forEach((id) => {
        const el = this.$(id) || this.shadowRoot?.querySelector?.(`#${id}`);
        if (el) el.textContent = this._fmtDur(safePosition);
      });
      ["bigTotalTime", "immersiveTotalTime"].forEach((id) => {
        const el = this.$(id) || this.shadowRoot?.querySelector?.(`#${id}`);
        if (el) el.textContent = this._fmtDur(safeDuration);
      });
    }
    _bindProgressSeekBar(el) {
      if (!el || el.dataset.homeiiSeekBound === "1") return;
      el.dataset.homeiiSeekBound = "1";
      let dragging = false;
      const seek = (event, immediate = false) => this._seekFromProgress(event, { immediate });
      el.addEventListener("pointerdown", (event) => {
        if (event.button !== void 0 && event.button > 0) return;
        event.preventDefault();
        dragging = true;
        this._lastProgressPointerSeekAt = Date.now();
        try {
          el.setPointerCapture?.(event.pointerId);
        } catch {
        }
        seek(event, true);
      });
      el.addEventListener("pointermove", (event) => {
        if (!dragging) return;
        event.preventDefault();
        seek(event, false);
      });
      const finish = (event) => {
        if (!dragging) return;
        dragging = false;
        try {
          el.releasePointerCapture?.(event.pointerId);
        } catch {
        }
        seek(event, true);
      };
      el.addEventListener("pointerup", finish);
      el.addEventListener("pointercancel", finish);
      el.addEventListener("click", (event) => {
        if (Date.now() - Number(this._lastProgressPointerSeekAt || 0) < 240) return;
        seek(event, true);
      });
    }
    async _seekFromProgress(e, options = {}) {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const duration = this._getCurrentDuration();
      if (!duration) return;
      const rect = e.currentTarget.getBoundingClientRect();
      if (!rect.width) return;
      const pct = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
      const newPos = Math.floor(duration * pct);
      this._applyProgressUi(newPos, duration);
      if (this._state.maQueueState) {
        this._state.maQueueState.elapsed_time = newPos;
        this._state.maQueueState.elapsed_time_last_updated = Date.now() / 1e3;
      }
      clearTimeout(this._seekTimer);
      const runSeek = () => {
        if (this._isDirectMaPlayer(player)) {
          this._callDirectMaPlayerCommand(player, "players/cmd/seek", { position: newPos }).catch(() => {
          });
          return;
        }
        this._hass.callService("media_player", "media_seek", { entity_id: player.entity_id, seek_position: newPos });
      };
      if (options.immediate) runSeek();
      else this._seekTimer = setTimeout(runSeek, 20);
    }
    _setNowPlayingSubtitle(value, { scrollWhenOverflow = false } = {}) {
      const el = this.$("npSub");
      if (!el) return;
      const text = String(value || "—");
      const shouldScroll = this._nowPlayingSubtitleShouldScroll(scrollWhenOverflow);
      const existingInner = el.querySelector?.(".scrolling-text-inner");
      const unchanged = el.dataset.homeiiSubtitleText === text && el.dataset.homeiiSubtitleScroll === String(shouldScroll) && existingInner;
      el.title = text;
      el.classList.toggle("scroll-when-overflow", shouldScroll);
      if (unchanged) {
        if (shouldScroll) this._queueNowPlayingSubtitleOverflowSync(el);
        return;
      }
      el.dataset.homeiiSubtitleText = text;
      el.dataset.homeiiSubtitleScroll = String(shouldScroll);
      el.classList.remove("is-overflowing");
      el.style.removeProperty("--scroll-distance");
      el.style.removeProperty("--scroll-duration");
      el.textContent = "";
      const inner = document.createElement("span");
      inner.className = "scrolling-text-inner";
      const primary = document.createElement("span");
      primary.className = "scrolling-text-item";
      primary.textContent = text;
      inner.appendChild(primary);
      if (shouldScroll) {
        const duplicate = document.createElement("span");
        duplicate.className = "scrolling-text-item";
        duplicate.setAttribute("aria-hidden", "true");
        duplicate.textContent = text;
        inner.appendChild(duplicate);
      }
      el.appendChild(inner);
      if (shouldScroll) this._queueNowPlayingSubtitleOverflowSync(el);
    }
    _nowPlayingSubtitleShouldScroll(scrollWhenOverflow = false) {
      return !!scrollWhenOverflow && !this._isHebrew() && !this._performanceUltraLiteEnabled();
    }
    _queueNowPlayingSubtitleOverflowSync(el) {
      if (typeof requestAnimationFrame === "function") requestAnimationFrame(() => this._syncNowPlayingSubtitleOverflow(el));
      else this._syncNowPlayingSubtitleOverflow(el);
    }
    _syncNowPlayingSubtitleOverflow(el) {
      const inner = el?.querySelector?.(".scrolling-text-inner");
      const item = inner?.querySelector?.(".scrolling-text-item");
      if (!el || !inner || !item) return;
      const gap = Math.max(24, Math.round(el.clientWidth * 0.18));
      const itemWidth = item.scrollWidth;
      const distance = itemWidth + gap;
      const overflow = itemWidth > el.clientWidth + 1;
      el.classList.toggle("is-overflowing", overflow);
      el.style.setProperty("--scroll-gap", gap + "px");
      el.style.setProperty("--scroll-distance", distance + "px");
      el.style.setProperty("--scroll-duration", Math.max(9, Math.min(24, distance / 24)).toFixed(2) + "s");
    }
    _syncNowPlayingUI() {
      const player = this._getSelectedPlayer();
      if (!player) {
        const issue = this._state.musicAssistantIssueMessage || this._musicAssistantRequiredMessage();
        this._setButtonIcon(this.$("btnPlay"), "play");
        if (this.$("npTitle")) this.$("npTitle").textContent = this._musicAssistantRequiredTitle();
        this._setNowPlayingSubtitle(issue);
        if (this.$("npArt")) this.$("npArt").innerHTML = this._artPlaceholderHtml("speaker");
        if (this.$("progressFill")) this.$("progressFill").style.width = "0%";
        const slider2 = this.$("volSlider");
        if (slider2) {
          slider2.value = 0;
          slider2.style.setProperty("--vol-pct", "0%");
        }
        this._renderPlayerSummary();
        this._syncBrandPlayingState();
        this._syncStatus();
        return;
      }
      const queueItem = this._state.maQueueState?.current_item || null;
      const media = queueItem?.media_item || {};
      const queueTitle = media?.name || queueItem?.media_title || queueItem?.name || "";
      const queueArtist = Array.isArray(media?.artists) ? media.artists.map((artist) => artist?.name).filter(Boolean).join(", ") : queueItem?.media_artist || "";
      const queueArt = this._queueItemImageUrl(queueItem, 420) || queueItem?.media_image || queueItem?.image || queueItem?.image_url || media?.image || media?.image_url || media?.album?.image || media?.album?.image_url || "";
      this._setButtonIcon(this.$("btnPlay"), this._playPauseIconName(player));
      this.$("npTitle").textContent = queueTitle || player.attributes.media_title || this._i18n("ui.nothing_playing");
      this._setNowPlayingSubtitle(queueArtist || player.attributes.media_artist || "—");
      const art = this._currentArtworkUrl(player, queueItem, 420, { preferPlayerArtwork: true }) || queueArt || this._bestArtworkUrl([
        player.attributes.entity_picture_local,
        player.attributes.entity_picture
      ], { size: 420, cacheKey: this._currentArtworkCacheKey(player, queueItem) });
      const artBox = this.$("npArt");
      if (art && artBox?.querySelector("img")?.getAttribute("src") !== art) artBox.innerHTML = `<img src="${this._esc(art)}" alt="">`;
      else if (!art) artBox.innerHTML = this._artPlaceholderHtml("music_note");
      const vol = Math.round((player.attributes.volume_level || 0) * 100);
      const slider = this.$("volSlider");
      if (slider) {
        slider.value = vol;
        slider.style.setProperty("--vol-pct", `${vol}%`);
      }
      this._setButtonIcon(this.$("btnMute"), this._volumeIconName(player));
      const duration = this._getCurrentDuration();
      const position = this._getCurrentPosition();
      this.$("progressFill") && (this.$("progressFill").style.width = duration ? `${Math.min(100, position / duration * 100)}%` : "0%");
      const nowPlayingUri = String(player.attributes.media_content_id || "").trim() || String(this._getQueueItemUri(queueItem) || "").trim() || "";
      if (nowPlayingUri !== this._state.nowPlayingUri) {
        this._state.nowPlayingUri = nowPlayingUri;
        this._highlightNowPlaying();
      }
      this._renderPlayerSummary();
      this._syncBrandPlayingState();
      this._syncStatus();
      this._syncNowPlayingPageLive();
      if (this._state.screensaverOpen) this._syncScreensaverUi();
    }
    _syncNowPlayingPageLive() {
      if (this._state.view !== "now_playing") return;
      const player = this._getSelectedPlayer();
      if (!player) return;
      const title = player.attributes.media_title || this._i18n("ui.no_active_media");
      const artist = player.attributes.media_artist || this._i18n("ui.unknown");
      const album = player.attributes.media_album_name || "";
      const duration = this._getCurrentDuration();
      const position = this._getCurrentPosition();
      const pct = duration ? Math.min(100, position / duration * 100) : 0;
      const vol = Math.round((player.attributes.volume_level || 0) * 100);
      const queueItem = this._state.maQueueState?.current_item || null;
      const art = this._currentArtworkUrl(player, queueItem, 620, { preferPlayerArtwork: true });
      const repeat = player.attributes.repeat || "off";
      const bigPlay = this.$("bigPlayBtn");
      this._setButtonIcon(bigPlay, this._playPauseIconName(player));
      const bigShuffle = this.$("bigShuffleBtn");
      if (bigShuffle) bigShuffle.classList.toggle("active", !!player.attributes.shuffle);
      const bigRepeat = this.$("bigRepeatBtn");
      if (bigRepeat) {
        bigRepeat.classList.toggle("active", repeat !== "off");
        bigRepeat.dataset.repeatMode = repeat;
        bigRepeat.title = this._repeatModeLabel(repeat);
        bigRepeat.setAttribute("aria-label", this._repeatModeLabel(repeat));
      }
      this._setButtonIcon(bigRepeat, repeat === "one" ? "repeat_one" : "repeat");
      const bigMute = this.$("bigMuteBtn");
      this._setButtonIcon(bigMute, this._volumeIconName(player));
      const bigVolume = this.$("bigVolumeSlider");
      if (bigVolume) {
        bigVolume.value = vol;
        bigVolume.style.setProperty("--vol-pct", `${vol}%`);
      }
      const bigProgress = this.$("bigProgressFill");
      if (bigProgress) bigProgress.style.width = `${pct}%`;
      this.$("bigCurTime") && (this.$("bigCurTime").textContent = this._fmtDur(position));
      this.$("bigTotalTime") && (this.$("bigTotalTime").textContent = this._fmtDur(duration));
      const heroArt = this.$("nowHeroArt");
      if (heroArt) heroArt.innerHTML = art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("music_note");
      const trackTitle = this.shadowRoot.querySelector(".now-track-title");
      if (trackTitle) trackTitle.textContent = title;
      const trackSubtitle = this.shadowRoot.querySelector(".now-track-subtitle");
      if (trackSubtitle) trackSubtitle.textContent = [artist, album].filter(Boolean).join(" · ");
      this._syncImmersiveNowPlaying();
    }
    _syncBigVolumeMirror(pct) {
      const big = this.$("bigVolumeSlider");
      if (big) {
        big.value = pct;
        big.style.setProperty("--vol-pct", `${pct}%`);
      }
    }
    _highlightNowPlaying() {
      const nowUri = this._state.nowPlayingUri;
      const currentIndex = this._state.maQueueState?.current_index;
      this.shadowRoot.querySelectorAll(".media-card[data-uri], .track-row[data-uri], .mini-queue-item[data-uri]").forEach((el) => {
        const uriMatch = !!nowUri && el.dataset.uri === nowUri;
        el.classList.toggle("playing", uriMatch);
        if (el.classList.contains("mini-queue-item")) {
          const sortIndex = el.dataset.sortIndex !== void 0 ? Number(el.dataset.sortIndex) : null;
          const indexMatch = currentIndex != null && sortIndex === currentIndex;
          el.classList.toggle("active", uriMatch || indexMatch);
        }
      });
    }
    _startLoops() {
      this._syncNowPlayingUI();
      clearInterval(this._pollTimer);
      clearInterval(this._progressTimer);
      this._pollTimer = setInterval(() => this._updateNowPlayingState(), 2e3);
      this._progressTimer = setInterval(() => this._tickProgress(), 700);
    }
    _tickProgress() {
      this._syncSleepTimerState();
      this._syncScheduledStartState();
      this._syncNightModeUi();
      this._syncSleepTimerChip();
      if (this._lyricsSessionActive()) this._syncLyricsForCurrentTrack();
      if (this._state.screensaverOpen) {
        this._syncScreensaverUi();
        return;
      }
      const player = this._getSelectedPlayer();
      if (!player) return;
      const duration = this._getCurrentDuration();
      if (!duration) return;
      const position = this._getCurrentPosition();
      const pct = Math.min(100, position / duration * 100);
      this.$("progressFill") && (this.$("progressFill").style.width = `${pct}%`);
      this.$("bigProgressFill") && (this.$("bigProgressFill").style.width = `${pct}%`);
      this.$("bigCurTime") && (this.$("bigCurTime").textContent = this._fmtDur(position));
      this.$("bigTotalTime") && (this.$("bigTotalTime").textContent = this._fmtDur(duration));
    }
    async _updateNowPlayingState() {
      if (this._updateNowPlayingInFlight) {
        this._updateNowPlayingQueued = true;
        return;
      }
      this._updateNowPlayingInFlight = true;
      try {
        this._syncSleepTimerState();
        this._syncScheduledStartState();
        if (this._state.selectedPlayer && this._isDirectMaPlayer(this._state.selectedPlayer)) {
          await this._refreshDirectMaPlayers();
        }
        const player = this._getSelectedPlayer();
        if (!player) return;
        this._syncActivePlayerHelper(player);
        const previousQueueSignature = this._queueRenderSignature();
        await this._ensureQueueSnapshot();
        const nextQueueSignature = this._queueRenderSignature();
        this._refreshGroupingState();
        this._loadPlayers();
        this._syncNowPlayingUI();
        if (this._state.queueVisible) this._renderQueueItems();
        if (this._state.menuOpen && this._state.menuPage === "queue" && nextQueueSignature !== previousQueueSignature) this._renderMobileMenu();
        if (this._state.playerModalOpen) {
          if (this._state.modalMode === "transfer") this._openTransferQueuePicker(true);
          else this._renderPlayerModal();
        }
      } finally {
        this._updateNowPlayingInFlight = false;
        if (this._updateNowPlayingQueued && this.isConnected) {
          this._updateNowPlayingQueued = false;
          this._updateNowPlayingState().catch(() => {
          });
        }
      }
    }
    async _ensureQueueSnapshot(force = false) {
      const snapshotToken = ++this._queueSnapshotToken;
      const selectedEntityId = String(this._state.selectedPlayer || "");
      const isCurrentSnapshot = () => snapshotToken === this._queueSnapshotToken && String(this._state.selectedPlayer || "") === selectedEntityId;
      const player = this._getSelectedPlayer();
      if (!player) return;
      const queueId = this._queueIdForPlayer(player);
      if (queueId && this._hasDirectMAConnection()) {
        try {
          const queueState = await this._callDirectMaCommand("player_queues/get", { queue_id: queueId });
          if (!isCurrentSnapshot()) return;
          let queueItems = [];
          if (force || !this._state.queueItems.length || this._isQueueUiVisible()) {
            const totalItems = Math.max(1, Number(queueState.items) || 0);
            const limit = Math.min(1e3, Math.max(50, totalItems));
            const fullSnapshot = await this._callDirectMaCommand("player_queues/items", { queue_id: queueId, limit, offset: 0 });
            if (!isCurrentSnapshot()) return;
            const snapshotItems = Array.isArray(fullSnapshot?.items) ? fullSnapshot.items : Array.isArray(fullSnapshot?.queue_items) ? fullSnapshot.queue_items : Array.isArray(fullSnapshot) ? fullSnapshot : [];
            queueItems = snapshotItems.map((item, index) => this._normalizeQueueItem(item, index)).filter(Boolean);
          }
          if (!isCurrentSnapshot()) return;
          this._applyQueueSnapshot(queueState, queueItems, force);
          return;
        } catch (_) {
        }
      }
      try {
        const payload = { entity_id: player.entity_id, limit: 250 };
        if (queueId) payload.queue_id = queueId;
        const snapshot = await this._callService("get_queue", payload, { includeConfigEntryId: false });
        if (!isCurrentSnapshot()) return;
        const normalized = this._normalizeQueueSnapshot(snapshot, player.entity_id);
        const queueData = normalized?.items?.length ? normalized : await this._fetchMassQueueItemsSnapshot(player);
        if (!isCurrentSnapshot()) return;
        if (!queueData) return;
        this._applyQueueSnapshot(queueData.state, queueData.items, force);
      } catch (_) {
        const queueData = await this._fetchMassQueueItemsSnapshot(player);
        if (!isCurrentSnapshot()) return;
        if (!queueData) return;
        this._applyQueueSnapshot(queueData.state, queueData.items, force);
      }
    }
    _queueItemImageUrl(item, size = 120) {
      return this._imageUrl(item?.streamdetails?.stream_metadata?.image_url, size) || this._imageUrl(item?.local_image_encoded, size) || this._imageUrl(item?.local_image_url, size) || this._imageUrl(item?.local_image, size) || this._imageUrl(item?.media_image, size) || this._imageUrl(item?.media_image_url, size) || this._imageUrl(item?.image_url, size) || this._imageUrl(item?.image, size) || this._artUrl(item, { size }) || this._artUrl(item?.media_item || item, { size }) || this._imageUrl(item?.media_item?.image_url, size) || this._imageUrl(item?.media_item?.image, size) || this._imageUrl(item?.media_item?.metadata?.images, size) || this._imageUrl(item?.media_item?.album?.image_url, size) || this._imageUrl(item?.media_item?.album?.image, size) || this._imageUrl(item?.media_item?.album?.metadata?.images, size) || null;
    }
    _getAvailableGroupPlayers() {
      const current = this._state.selectedPlayer;
      return (this._state.players || []).filter((p) => p.entity_id !== current).filter((p) => !(typeof this._isLikelyBrowserPlayer === "function" && this._isLikelyBrowserPlayer(p))).filter((p) => !(typeof this._isStaticGroupPlayer === "function" && this._isStaticGroupPlayer(p)));
    }
    _refreshGroupingState() {
      const player = this._getSelectedPlayer();
      if (!player) return;
      const attrs = player.attributes || {};
      const children = Array.isArray(attrs.group_members) ? attrs.group_members.filter((id) => id !== player.entity_id) : [];
      if (!this._state.pendingGroupSelections.length) this._state.pendingGroupSelections = [...children];
    }
    _openGroupModal() {
      this.shadowRoot.querySelector(".card")?.appendChild(this.$("groupModal"));
      const players = this._getAvailableGroupPlayers();
      const list = this.$("groupList");
      if (!list) return;
      const selected = this._getSelectedPlayer();
      const subtitle = this.$("groupModalSubtitle");
      const badge = this.$("groupCountBadge");
      if (subtitle) subtitle.textContent = selected?.attributes?.friendly_name || this._i18n("ui.choose_player");
      if (badge) badge.textContent = String(players.length);
      list.innerHTML = players.length ? players.map((p) => {
        const checked = (this._state.pendingGroupSelections || []).includes(p.entity_id);
        return `<label class="group-item ${checked ? "checked" : ""}"><span class="group-meta"><span class="group-name">${this._esc(p.attributes.friendly_name || p.entity_id)}<span class="group-item-toggle ${checked ? "checked" : ""}" aria-hidden="true">${this._iconSvg(checked ? "check" : "plus")}</span></span><span class="group-sub"></span></span><input type="checkbox" data-group-player="${this._esc(p.entity_id)}" ${checked ? "checked" : ""}></label>`;
      }).join("") : `<div class="state-box" style="min-height:80px;padding:8px 0;">${this._esc(this._i18n("ui.no_extra_ma_players"))}</div>`;
      this.$("groupModal").classList.add("open");
    }
    _closeGroupModal() {
      this.$("groupModal").classList.remove("open");
    }
    _handleGroupChange(e) {
      const checkbox = e.target.closest("input[data-group-player]");
      if (!checkbox) return;
      const entityId = checkbox.dataset.groupPlayer;
      const next = new Set(this._state.pendingGroupSelections || []);
      if (checkbox.checked) next.add(entityId);
      else next.delete(entityId);
      this._state.pendingGroupSelections = Array.from(next);
      const groupItem = checkbox.closest(".group-item");
      groupItem?.classList.toggle("checked", checkbox.checked);
      const toggle = groupItem?.querySelector(".group-item-toggle");
      if (toggle) {
        toggle.classList.toggle("checked", checkbox.checked);
        toggle.innerHTML = this._iconSvg(checkbox.checked ? "check" : "plus");
      }
    }
    async _applySpeakerGroupFor(entityId, groupMembers = []) {
      const primaryId = String(entityId || "").trim();
      if (!primaryId) return false;
      const members = [...new Set((Array.isArray(groupMembers) ? groupMembers : []).map((id) => String(id || "").trim()).filter((id) => id && id !== primaryId))];
      if (!members.length) return false;
      await this._callHaMediaPlayerService(primaryId, "join", { group_members: members });
      setTimeout(() => {
        this._loadPlayers();
        this._refreshGroupingState();
        if (this._state.menuOpen) this._renderMobileMenu();
        if (this._state.controlRoomOpen) this._syncControlRoomUi({ force: true });
      }, 650);
      return true;
    }
    async _applySpeakerGroup() {
      const player = this._getSelectedPlayer();
      if (!player) return false;
      const groupMembers = [...this._state.pendingGroupSelections || []];
      let ok = false;
      try {
        ok = await this._applySpeakerGroupFor(player.entity_id, groupMembers);
      } catch (error) {
        this._toastError(error?.message || this._i18n("ui.queue_action_failed"));
        return false;
      }
      if (!ok) {
        this._toastError(this._i18n("ui.select_at_least_two_players_to_create_a_group"));
        return false;
      }
      this._toastSuccess(this._i18n("ui.group_updated"));
      this._closeGroupModal();
      setTimeout(() => {
        this._refreshGroupingState();
        if (this._state.view === "now_playing") this._renderNowPlayingPage();
      }, 500);
      return true;
    }
    _clearLocalGroupState(entityId) {
      const primaryId = String(entityId || "").trim();
      if (!primaryId) return;
      const sourcePlayers = Array.isArray(this._state.players) ? this._state.players : [];
      const selectedPlayer = sourcePlayers.find((player) => player?.entity_id === primaryId) || this._playerByEntityId(primaryId);
      const related = /* @__PURE__ */ new Set([primaryId, ...this._playerGroupMemberIds(selectedPlayer).filter(Boolean)]);
      for (const player of sourcePlayers) {
        const members = this._playerGroupMemberIds(player);
        if (members.includes(primaryId)) members.forEach((id) => related.add(id));
      }
      this._state.players = sourcePlayers.map((player) => {
        if (!player?.entity_id) return player;
        const attrs = player.attributes || {};
        const rawMembers = Array.isArray(attrs.group_members) ? attrs.group_members.filter(Boolean) : [];
        const touchesGroup = related.has(player.entity_id) || rawMembers.some((id) => related.has(id));
        if (!touchesGroup && !rawMembers.length) return player;
        const nextAttrs = { ...attrs };
        if (rawMembers.length) {
          const nextMembers = rawMembers.filter((id) => id && !related.has(id));
          if (nextMembers.length > 1) nextAttrs.group_members = nextMembers;
          else delete nextAttrs.group_members;
        }
        if (related.has(player.entity_id)) {
          delete nextAttrs.group_members;
          delete nextAttrs.group_childs;
          delete nextAttrs.group_children;
          delete nextAttrs.group_leader;
          delete nextAttrs.group_parent;
          delete nextAttrs.group_master;
        }
        return { ...player, attributes: nextAttrs };
      });
      this._state.pendingGroupSelections = [];
      this._refreshGroupingState();
      this._syncNowPlayingUI();
      if (this._state.view === "now_playing") this._renderNowPlayingPage();
      if (this._state.menuOpen && this._state.menuPage === "group") this._renderMobileMenu().catch(() => {
      });
    }
    async _clearSpeakerGroupFor(entityId) {
      const player = this._playerByEntityId(entityId);
      if (!player) return;
      if (typeof this._isStaticGroupPlayer === "function" && this._isStaticGroupPlayer(player)) {
        const targets = this._playerGroupMemberIds(player).filter((id) => id && id !== player.entity_id).filter((id) => {
          const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
          return target && !this._isStaticGroupPlayer(target);
        });
        if (targets.length) {
          await Promise.allSettled(targets.map((id) => this._callHaMediaPlayerService(id, "unjoin")));
        }
        this._clearLocalGroupState(player.entity_id);
        setTimeout(() => {
          this._loadPlayers();
          this._refreshGroupingState();
          if (this._state.menuOpen) this._renderMobileMenu();
          if (this._state.view === "now_playing") this._renderNowPlayingPage();
        }, 500);
        return true;
      }
      try {
        await this._callHaMediaPlayerService(player.entity_id, "unjoin");
      } catch (_) {
      }
      this._clearLocalGroupState(player.entity_id);
      setTimeout(() => {
        this._loadPlayers();
        this._refreshGroupingState();
        if (this._state.menuOpen) this._renderMobileMenu();
        if (this._state.view === "now_playing") this._renderNowPlayingPage();
      }, 500);
      return true;
    }
    async _clearSpeakerGroup() {
      const player = this._getSelectedPlayer();
      if (!player) return false;
      await this._clearSpeakerGroupFor(player.entity_id);
      this._toast(this._i18n("ui.group_cleared"));
      this._closeGroupModal();
      return true;
    }
    _toggleQueue() {
      if (this._isHotelMode()) return;
      if (this._state.queueVisible) this._hideQueue();
      else this._showQueue();
    }
    async _showQueue() {
      if (this._isHotelMode()) return;
      const player = this._getSelectedPlayer();
      if (!player) return;
      this._state.queueVisible = true;
      this._hideQueue();
      const panel = document.createElement("div");
      panel.className = "queue-panel";
      panel.id = "queuePanel";
      const art = this._currentArtworkUrl(player, this._state.maQueueState?.current_item || null, 180);
      panel.innerHTML = `
        <div class="queue-shell">
        <div class="queue-header">
          <div class="queue-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("music_note")}</div>
          <div class="queue-meta"><div class="queue-title">${this._esc(player.attributes.media_title || this._i18n("ui.queue"))}</div><div class="queue-sub" id="queueSub">${this._esc(player.attributes.media_artist || "")}</div></div>
          <button class="close-btn" id="queueClose">✕</button>
        </div>
        <div class="queue-scroll" id="queueScroll">${this._loadingStateHtml(this._i18n("ui.loading_queue"))}</div>
        </div>`;
      this.shadowRoot.querySelector(".card")?.appendChild(panel);
      panel.querySelector("#queueClose").addEventListener("click", () => this._hideQueue());
      panel.addEventListener("click", this._boundQueuePanelClick);
      panel.addEventListener("change", (e) => this._handleQueueMoveAutoChange(e));
      await this._renderQueueItems();
    }
    _renderQueueRowActions(item) {
      const key = this._getQueueItemKey(item);
      return `
        <div class="queue-actions">
          <button class="chip-btn" data-queue-action="up" data-queue-item-id="${this._esc(key)}">↑</button>
          <button class="chip-btn" data-queue-action="next" data-queue-item-id="${this._esc(key)}">⏭</button>
          <button class="chip-btn" data-queue-action="down" data-queue-item-id="${this._esc(key)}">↓</button>
          <button class="chip-btn warn" data-queue-action="remove" data-queue-item-id="${this._esc(key)}">✕</button>
        </div>`;
    }
    async _renderQueueItems() {
      const panel = this.shadowRoot.getElementById("queuePanel");
      if (!panel) return;
      const player = this._getSelectedPlayer();
      if (!player) return;
      const queueScroll = panel.querySelector("#queueScroll");
      const queueSub = panel.querySelector("#queueSub");
      try {
        await this._ensureQueueSnapshot(true);
        const queueState = this._state.maQueueState;
        if (!queueState) {
          queueScroll.innerHTML = `<div class="state-box">${this._esc(this._i18n("ui.queue_is_empty"))}</div>`;
          return;
        }
        const currentIndex = queueState.current_index ?? 0;
        const totalItems = queueState.items ?? 0;
        const queueItems = this._state.queueItems || [];
        queueSub.textContent = `${player.attributes.media_artist || ""}${player.attributes.media_artist ? " · " : ""}${totalItems} ${this._i18n("ui.items")}`;
        if (!Array.isArray(queueItems) || !queueItems.length) {
          queueScroll.innerHTML = `<div class="state-box">${this._esc(this._i18n("ui.queue_is_empty"))}</div>`;
          return;
        }
        queueScroll.innerHTML = queueItems.map((item, idx) => {
          const current = item.sort_index === currentIndex;
          const past = item.sort_index < currentIndex;
          const img = this._queueItemImageUrl(item, 120);
          const artist = item.media_item?.artists?.map((a) => a.name).join(", ") || "";
          const key = this._getQueueItemKey(item);
          const eager = current || idx < 24;
          return `
            <div class="queue-item ${current ? "active" : ""} ${past ? "past" : ""}" data-uri="${this._esc(item.media_item?.uri || "")}" data-type="track" data-queue-item-id="${this._esc(key)}" data-sort-index="${this._esc(item.sort_index ?? "")}">
              <div class="queue-num">${current ? "▶" : item.sort_index ?? idx + 1}</div>
              <div class="queue-thumb">${img ? this._imgHtml(img, "", { loading: eager ? "eager" : "lazy", fetchpriority: eager ? "high" : "low" }) : "♫"}</div>
              <div class="queue-item-meta">
                <div class="queue-name">${this._esc(item.media_item?.name || item.name || "")}</div>
                <div class="queue-artist">${this._esc(artist)}</div>
              </div>
              ${this._renderQueueRowActions(item)}
            </div>`;
        }).join("");
      } catch (e) {
        queueScroll.innerHTML = `<div class="state-box">${this._esc(e.message || String(e))}</div>`;
      }
    }
    _hideQueue() {
      this._state.queueVisible = false;
      this.shadowRoot.getElementById("queuePanel")?.remove();
    }
    _queueItemsAfterLocalAction(action, queueItemId, items = this._state.queueItems || [], targetPosition = null) {
      const allItems = [...Array.isArray(items) ? items : []];
      const targetKey = String(queueItemId || "").trim();
      const idx = allItems.findIndex(
        (i) => this._getQueueItemKey(i) === targetKey || this._getQueueItemStableId(i) === targetKey || this._getQueueItemUri(i) === targetKey
      );
      if (idx === -1) return null;
      const reordered = [...allItems];
      if (action === "remove") {
        reordered.splice(idx, 1);
        return this._queueItemsWithSequentialSortIndexes(reordered);
      }
      const firstMovableIdx = this._queueFirstMovableIndex(reordered);
      if (action === "up") {
        if (idx > firstMovableIdx) {
          [reordered[idx - 1], reordered[idx]] = [reordered[idx], reordered[idx - 1]];
          return this._queueItemsWithSequentialSortIndexes(reordered);
        }
        return null;
      }
      if (action === "down") {
        if (idx >= firstMovableIdx && idx < reordered.length - 1) {
          [reordered[idx + 1], reordered[idx]] = [reordered[idx], reordered[idx + 1]];
          return this._queueItemsWithSequentialSortIndexes(reordered);
        }
        return null;
      }
      if (action === "next") {
        const item = reordered[idx];
        const targetIdx = Math.max(0, firstMovableIdx);
        if (item && idx > targetIdx) {
          reordered.splice(idx, 1);
          reordered.splice(targetIdx, 0, item);
          return this._queueItemsWithSequentialSortIndexes(reordered);
        }
        return null;
      }
      if (action === "move_to") {
        const item = reordered[idx];
        const requestedPosition = Math.round(Number(targetPosition));
        if (!item || !Number.isFinite(requestedPosition)) return null;
        const targetIdx = this._queueMoveGlobalIndexForDisplayPosition(reordered, requestedPosition);
        if (targetIdx < 0) return null;
        if (idx === targetIdx) return reordered;
        reordered.splice(idx, 1);
        reordered.splice(Math.max(firstMovableIdx, Math.min(reordered.length, targetIdx)), 0, item);
        return this._queueItemsWithSequentialSortIndexes(reordered);
      }
      return null;
    }
    async _handleQueueAction(action, queueItemId, fallbackUri = "", sortIndex = "", targetPosition = null) {
      const player = this._getSelectedPlayer();
      if (!player || !queueItemId || this._state.queueActionPending) return;
      const allItems = [...this._state.queueItems || []];
      const targetItem = this._resolveQueueActionTarget(allItems, queueItemId, fallbackUri, sortIndex);
      const targetKey = targetItem ? this._getQueueItemKey(targetItem) : String(queueItemId || "").trim();
      const idx = targetItem ? allItems.indexOf(targetItem) : allItems.findIndex((i) => this._getQueueItemKey(i) === targetKey);
      if (idx === -1) return;
      try {
        this._setQueueBusy(true);
        const numericTargetPosition = Math.round(Number(targetPosition));
        const moveToIndex = action === "move_to" && Number.isFinite(numericTargetPosition) ? this._queueMoveGlobalIndexForDisplayPosition(allItems, numericTargetPosition) : null;
        const posShift = action === "move_to" && Number.isFinite(moveToIndex) && moveToIndex >= 0 ? moveToIndex - idx : null;
        const optimisticItems = this._queueItemsAfterLocalAction(action, targetKey, allItems, numericTargetPosition);
        const queueId = this._queueIdForPlayer(player);
        const massQueueServiceByAction = {
          up: "move_queue_item_up",
          down: "move_queue_item_down",
          next: "move_queue_item_next",
          remove: "remove_queue_item"
        };
        const serviceQueueItemId = targetItem ? String(targetItem.queue_service_id || (targetItem.queue_item_id_trusted !== false ? targetItem.queue_item_id : "") || this._getQueueItemPlaybackId(targetItem) || "").trim() : "";
        const numericSortIndex = HomeiiMediaQueueFoundation2.normalizeFiniteNumber(targetItem?.sort_index ?? sortIndex);
        const directTarget = serviceQueueItemId || (action === "remove" && Number.isFinite(numericSortIndex) ? numericSortIndex : null);
        this._debugLog("info", "[Homeii Queue] action", {
          action,
          queueId,
          queueItemId,
          serviceQueueItemId,
          sortIndex: numericSortIndex,
          targetPosition: Number.isFinite(numericTargetPosition) ? numericTargetPosition : "",
          title: targetItem ? this._queueItemPrimaryTitle(targetItem) : ""
        });
        let acted = false;
        try {
          acted = await this._callDirectMaQueueAction(action, queueId, directTarget, posShift);
        } catch (error) {
          this._debugLog("warn", "[Homeii Queue] direct action failed", error);
        }
        if (!acted && serviceQueueItemId && massQueueServiceByAction[action]) {
          acted = await this._callMassQueueService(massQueueServiceByAction[action], serviceQueueItemId);
        }
        if (!acted && optimisticItems && this._config?.queue_rebuild_fallback === true) {
          await this._rebuildQueue(player.entity_id, optimisticItems, this._getCurrentPosition());
          acted = true;
        }
        if (!acted) {
          await this._ensureQueueSnapshot(true).catch(() => {
          });
          const message = serviceQueueItemId ? this._i18n("ui.queue_action_is_not_available_for_this_player") : this._i18n("ui.queue_item_id_is_not_ready_yet_refresh_the_queue_and_try_again");
          this._toast(message);
          return;
        }
        if (optimisticItems) {
          this._state.queueMutationPendingUntil = Date.now() + 4e3;
          this._state.queueMutationExpectedSignature = this._queueOrderSignature(optimisticItems);
          this._state.queueItems = optimisticItems;
          if (this._state.menuOpen && this._state.menuPage === "queue") {
            await this._renderMobileMenu();
            this._setQueueBusy(true);
          } else this._syncNowPlayingUI();
        }
        await this._refreshQueueAfterMutation(220);
        this._refreshQueueAfterMutation(900).catch(() => {
        });
        this._refreshQueueAfterMutation(1800).catch(() => {
        });
      } catch (e) {
        this._toast(e?.message || this._i18n("ui.queue_action_failed"));
      } finally {
        this._setQueueBusy(false);
      }
    }
    async _clearQueueForPlayer(entityId) {
      if (this._isDirectMaPlayer(entityId)) {
        const queueId = this._directMaQueueId(entityId);
        if (queueId) {
          try {
            await this._callDirectMaCommand("player_queues/clear", { queue_id: queueId });
            this._refreshDirectMaPlayers().catch(() => {
            });
          } catch (_) {
          }
        }
        return;
      }
      try {
        await this._callHaMediaPlayerService(entityId, "clear_playlist");
      } catch (_) {
      }
      try {
        await this._callHaMediaPlayerService(entityId, "media_stop");
      } catch (_) {
      }
    }
    async _stopPlayer(entityId) {
      if (!entityId) return;
      if (this._isDirectMaPlayer(entityId)) {
        try {
          await this._callDirectMaPlayerCommand(entityId, "players/cmd/stop");
          this._refreshDirectMaPlayers().catch(() => {
          });
          return;
        } catch (_) {
          await this._clearQueueForPlayer(entityId);
          return;
        }
      }
      await this._callHaMediaPlayerService(entityId, "media_stop");
    }
    async _rebuildQueue(targetEntityId, orderedItems, seekPosition = 0) {
      const currentIndex = this._state.maQueueState?.current_index ?? 0;
      const currentItem = orderedItems.find((i) => (i.sort_index ?? -1) === currentIndex) || orderedItems[0];
      const currentArrayIndex = orderedItems.indexOf(currentItem);
      const itemsAfterCurrent = currentArrayIndex >= 0 ? orderedItems.slice(currentArrayIndex + 1) : orderedItems.filter((i) => Number(i?.sort_index ?? -1) > Number(currentIndex));
      const currentUri = this._getQueueItemUri(currentItem);
      if (!currentUri) throw new Error(this._i18n("ui.no_queue_item_to_rebuild"));
      const uriList = [currentUri, ...itemsAfterCurrent.map((i) => this._getQueueItemUri(i)).filter(Boolean)];
      if (this._isDirectMaPlayer(targetEntityId)) {
        const queueId = this._directMaQueueId(targetEntityId);
        if (!queueId) throw new Error("Direct Music Assistant player is not ready");
        await this._clearQueueForPlayer(targetEntityId);
        try {
          await this._callDirectMaCommand("player_queues/play_media", {
            queue_id: queueId,
            media: uriList,
            option: "replace"
          });
        } catch (error) {
          await this._callDirectMaCommand("player_queues/play_media", {
            queue_id: queueId,
            media: currentUri,
            option: "replace"
          });
          const rest = uriList.slice(1);
          if (rest.length) {
            await this._callDirectMaCommand("player_queues/play_media", {
              queue_id: queueId,
              media: rest,
              option: "add"
            }).catch(async () => {
              for (const uri of rest) {
                await this._callDirectMaCommand("player_queues/play_media", {
                  queue_id: queueId,
                  media: uri,
                  option: "add"
                });
              }
            });
          }
        }
        if (seekPosition > 0) {
          setTimeout(() => {
            this._callDirectMaPlayerCommand(targetEntityId, "players/cmd/seek", {
              position: Math.floor(seekPosition)
            }).catch(() => {
            });
          }, 900);
        }
        this._refreshDirectMaPlayers().catch(() => {
        });
        return;
      }
      await this._clearQueueForPlayer(targetEntityId);
      await this._hass.callService("music_assistant", "play_media", {
        entity_id: targetEntityId,
        media_id: uriList,
        enqueue: "play"
      });
      if (seekPosition > 0) {
        setTimeout(() => {
          this._hass.callService("media_player", "media_seek", {
            entity_id: targetEntityId,
            seek_position: Math.floor(seekPosition)
          });
        }, 900);
      }
    }
    async _openTransferQueuePicker(refreshOnly = false) {
      this.shadowRoot.querySelector(".card")?.appendChild(this.$("playerModal"));
      this._state.modalMode = "transfer";
      this.$("playerModalTitle").textContent = this._i18n("ui.transfer_queue");
      this._setPlayerModalHeader("transfer");
      const player = this._getSelectedPlayer();
      if (!player) return;
      const others = (this._state.players || []).filter((p) => p.entity_id !== player.entity_id);
      const body = this.$("playerModalBody");
      if (!body) return;
      if (!others.length) {
        body.innerHTML = `<div class="modal-section"><div class="state-box" style="min-height:120px;">${this._esc(this._i18n("ui.no_target_players_available"))}</div></div>`;
        if (!refreshOnly) {
          this.$("playerModal").classList.add("open");
          this._state.playerModalOpen = true;
        }
        return;
      }
      body.innerHTML = `
        <div class="player-modal-grid">
          <div class="modal-section">
            <div class="modal-section-top">
              <div class="modal-section-title">${this._i18n("ui.choose_target_player")}</div>
              <div class="modal-section-badge">${others.length}</div>
            </div>
            <div class="player-list">
              ${others.map((p) => {
        const art = this._playerArtworkUrl(p, 120);
        return `
                  <button class="player-card ${p.state === "playing" ? "playing" : p.state === "paused" ? "paused" : "idle"}" data-transfer-player="${this._esc(p.entity_id)}">
                    <span class="player-card-dot"></span>
                    <span class="player-card-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._artPlaceholderHtml("speaker")}</span>
                    <span class="player-card-meta">
                      <span class="player-card-top">
                        <span class="player-card-title">${this._esc(p.attributes?.friendly_name || p.entity_id)}</span>
                        <span class="player-card-badge">${this._esc(this._playerStateLabel(p))}</span>
                      </span>
                      <span class="player-card-sub">${this._esc(this._playerStateLabel(p))}</span>
                      <span class="player-card-track">${this._esc(p.attributes?.media_title || "—")}</span>
                    </span>
                  </button>`;
      }).join("")}
            </div>
          </div>
        </div>`;
      body.querySelectorAll("[data-transfer-player]").forEach((btn) => btn.addEventListener("click", async () => {
        await this._transferQueueTo(btn.dataset.transferPlayer);
        this._closePlayerModal();
      }));
      if (!refreshOnly) {
        this.$("playerModal").classList.add("open");
        this._state.playerModalOpen = true;
      }
    }
    async _transferQueueBetween(sourcePlayerEntityId, targetPlayerEntityId, options = {}) {
      const sourcePlayer = this._playerByEntityId(sourcePlayerEntityId);
      if (!sourcePlayer || !targetPlayerEntityId || sourcePlayer.entity_id === targetPlayerEntityId) return false;
      try {
        let items = [];
        let currentPos = 0;
        const usingSelectedSource = sourcePlayer.entity_id === this._state.selectedPlayer;
        if (usingSelectedSource) {
          await this._ensureQueueSnapshot(true);
          items = [...this._state.queueItems || []];
          currentPos = this._getCurrentPosition();
        }
        try {
          await this._callMusicAssistantTransferQueue(sourcePlayer.entity_id, targetPlayerEntityId);
        } catch (transferError) {
          if (!usingSelectedSource || !items.length) throw transferError;
          await this._rebuildQueue(targetPlayerEntityId, items, currentPos);
          try {
            await this._callHaMediaPlayerService(sourcePlayer.entity_id, "media_stop");
          } catch (_) {
          }
        }
        if (options.selectTarget !== false) this._selectPlayer(targetPlayerEntityId, true);
        if (!options.silent) this._toast(this._i18n("ui.queue_transferred"));
        setTimeout(() => this._ensureQueueSnapshot(true), 1200);
        return true;
      } catch (e) {
        if (!options.silent) this._toast(e?.message || this._i18n("ui.queue_action_failed"));
        return false;
      }
    }
    async _transferQueueTo(targetPlayerEntityId) {
      const sourcePlayer = this._getSelectedPlayer();
      if (!sourcePlayer || !targetPlayerEntityId) return;
      await this._transferQueueBetween(sourcePlayer.entity_id, targetPlayerEntityId);
    }
    _normalizeMediaItems(items = []) {
      return (Array.isArray(items) ? items : []).map((item) => this._normalizeMediaItem(item));
    }
    _normalizeMediaItem(item) {
      return HomeiiMediaPresentationFoundation2.normalizeMediaItem(item, this._maArtworkBaseUrl());
    }
    _imageProxyUrl(path, provider = "", size = 300) {
      return HomeiiMediaPresentationFoundation2.imageProxyUrl(path, provider, size, this._maArtworkBaseUrl());
    }
    _imageUrl(value, size = 300, seen = /* @__PURE__ */ new Set(), depth = 0) {
      const raw = HomeiiMediaPresentationFoundation2.imageUrl(value, size, {
        maUrl: this._maArtworkBaseUrl(),
        seen,
        depth
      });
      return this._normalizeArtworkUrl(raw, { size });
    }
    _artUrl(item, options = {}) {
      const size = Number(options?.size || 300) || 300;
      const raw = HomeiiMediaPresentationFoundation2.artUrl(item, this._maArtworkBaseUrl(), size);
      return this._normalizeArtworkUrl(raw, { size, cacheKey: options?.cacheKey || "" });
    }
    _artistName(item) {
      return HomeiiMediaPresentationFoundation2.artistName(item);
    }
    _fmtDur(sec) {
      return HomeiiMediaPresentationFoundation2.formatDuration(sec);
    }
    _esc(value) {
      return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
    }
    _imgHtml(src = "", alt = "", options = {}) {
      const url = String(src || "").trim();
      if (!url) return "";
      const loading = options.loading || "lazy";
      const decoding = options.decoding || "async";
      const className = options.className ? ` class="${this._esc(options.className)}"` : "";
      const fetchPriority = options.fetchpriority || options.fetchPriority || "low";
      const fetchPriorityAttr = ` fetchpriority="${this._esc(fetchPriority)}"`;
      return `<img${className} src="${this._esc(url)}" alt="${this._esc(alt)}" loading="${this._esc(loading)}" decoding="${this._esc(decoding)}"${fetchPriorityAttr}>`;
    }
    _toast(message, variant = "info", options = {}) {
      if (this._state?.controlRoomOpen && options?.allowStudio !== true) return;
      const wrap = this.$("toastWrap");
      if (!wrap) return;
      const text = String(message ?? "").trim();
      if (!text) return;
      const safeVariant = ["success", "error", "info"].includes(variant) ? variant : "info";
      const now = Date.now();
      const key = `${safeVariant}:${text}`;
      const lastShown = this._toastHistory?.get(key) || 0;
      if (now - lastShown < 2500) return;
      if (!this._toastHistory) this._toastHistory = /* @__PURE__ */ new Map();
      this._toastHistory.set(key, now);
      for (const [toastKey, at] of this._toastHistory.entries()) {
        if (now - at > 12e3) this._toastHistory.delete(toastKey);
      }
      const activeToasts = Array.from(wrap.querySelectorAll(".toast"));
      while (activeToasts.length >= 3) activeToasts.shift()?.remove();
      const centered = options?.position === "center";
      wrap.classList.toggle("studio-toast", !!this._state.controlRoomOpen && !centered);
      if (centered) wrap.classList.add("center-toast");
      const el = document.createElement("div");
      el.className = `toast ${safeVariant}${centered ? " centered" : ""}`;
      const icon = safeVariant === "success" ? "✓" : safeVariant === "error" ? "×" : "i";
      el.innerHTML = `<span class="toast-icon">${icon}</span><span class="toast-text">${this._esc(text)}</span>`;
      wrap.appendChild(el);
      setTimeout(() => {
        el.remove();
        if (centered && !wrap.querySelector(".toast.centered")) wrap.classList.remove("center-toast");
      }, Number(options?.duration || 3300));
    }
    _toastSuccess(message, options = {}) {
      this._toast(message, "success", options);
    }
    _toastError(message, options = {}) {
      this._toast(message, "error", options);
    }
    _showSurprisePopup(item = {}) {
      const host = this.$("surprisePopup");
      if (!host) return;
      const playerName = this._selectedPlayerName();
      const art = this._artUrl(item) || item?.image || item?.media_item?.image || item?.media_item?.album?.image || "";
      const title = item?.name || this._i18n("ui.random_playlist");
      host.innerHTML = `
        <div class="surprise-popup-card">
          <div class="surprise-popup-player">${this._esc(playerName)}</div>
          <div class="surprise-popup-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("wand")}</div>
          <div class="surprise-popup-title">${this._esc(title)}</div>
        </div>
      `;
      host.classList.add("open");
      clearTimeout(this._surprisePopupTimer);
      this._surprisePopupTimer = setTimeout(() => host.classList.remove("open"), 3200);
    }
    _hapticTap(pattern = 10) {
      try {
        if (typeof navigator === "undefined" || typeof navigator.vibrate !== "function") return;
        if (typeof window !== "undefined" && window.matchMedia && !window.matchMedia("(pointer: coarse)").matches) return;
        navigator.vibrate(pattern);
      } catch (_) {
      }
    }
    _flashInteraction(el) {
      if (!el?.classList) return;
      this._hapticTap(12);
      el.classList.add("tap-feedback");
      setTimeout(() => {
        if (el?.classList) el.classList.remove("tap-feedback");
      }, 260);
    }
    _selectedPlayerName() {
      const player = this._getSelectedPlayer();
      return player?.attributes?.friendly_name || player?.entity_id || this._i18n("ui.choose_player");
    }
    _mediaFeedbackLabel(uri = "", fallback = "") {
      return HomeiiMediaHistoryFoundation2.mediaFeedbackLabel(
        uri,
        fallback,
        this._state.queueItems || [],
        {
          getQueueItemUriFn: (item) => this._getQueueItemUri(item),
          defaultLabel: this._i18n("ui.media_2")
        }
      );
    }
    _toastMediaQueued(label, targetName) {
      const mediaLabel = this._mediaFeedbackLabel("", label);
      const target = String(targetName || "").trim() || this._selectedPlayerName();
      const message = this._i18n("ui.media_selected_target", {
        media: mediaLabel,
        target
      });
      this._toastSuccess(message);
    }
    _hydrateImages(rootOverride = null) {
      const root = rootOverride || this.$("content");
      if (!root) return;
      if (rootOverride) {
        root.querySelectorAll("[data-img]").forEach((el) => {
          const url = el.dataset.img;
          const placeholder = el.dataset.placeholder || "music_note";
          delete el.dataset.img;
          this._loadImgInto(url, el, placeholder);
        });
        return;
      }
      if (this._imgObserver && this._imgObserverRoot !== root) {
        this._imgObserver.disconnect();
        this._imgObserver = null;
        this._imgObserverRoot = null;
      }
      if (!this._imgObserver) {
        this._imgObserver = new IntersectionObserver((entries) => {
          for (const entry of entries) {
            if (!entry.isIntersecting) continue;
            const el = entry.target;
            this._imgObserver.unobserve(el);
            const url = el.dataset.img;
            const placeholder = el.dataset.placeholder || "💿";
            delete el.dataset.img;
            this._loadImgInto(url, el, placeholder);
          }
        }, { root, rootMargin: "120px" });
        this._imgObserverRoot = root;
      }
      root.querySelectorAll("[data-img]").forEach((el) => this._imgObserver.observe(el));
    }
    async _loadImgInto(url, el, fallback = "💿") {
      const supportedFallbacks = ["artist", "radio", "podcast", "music_note", "album", "playlist", "tracks", "speaker", "repeat"];
      const fallbackIcon = supportedFallbacks.includes(String(fallback || "")) ? fallback : fallback === "🎤" ? "artist" : fallback === "📻" ? "radio" : "music_note";
      if (!url || !el?.isConnected) {
        if (el?.isConnected) el.innerHTML = `<div class="media-placeholder">${this._artPlaceholderHtml(fallbackIcon)}</div>`;
        return;
      }
      if (this._imageFailed.has(url)) {
        if (el.isConnected) el.innerHTML = `<div class="media-placeholder">${this._artPlaceholderHtml(fallbackIcon)}</div>`;
        return;
      }
      const existing = this._imageBlobCache.get(url);
      if (existing) {
        el.innerHTML = `<img src="${existing}" alt="">`;
        return;
      }
      try {
        const response = await fetch(url, { cache: "force-cache", credentials: "same-origin" });
        if (!response.ok) throw new Error(`Image load failed: ${response.status}`);
        const blob = await response.blob();
        const objectUrl = URL.createObjectURL(blob);
        this._imageBlobCache.set(url, objectUrl);
        if (el.isConnected) el.innerHTML = `<img src="${objectUrl}" alt="">`;
      } catch (_) {
        this._imageFailed.add(url);
        if (el.isConnected) el.innerHTML = `<div class="media-placeholder">${this._artPlaceholderHtml(fallbackIcon)}</div>`;
      }
    }
    connectedCallback() {
      this._startResizeTracking();
      this._startScreensaverVisibilityTracking();
      this._markScreensaverPageEntry("connected");
      this._lastCardWidth = this._getCardWidth(this._lastCardWidth);
      this._lastCardHeight = this._getAllocatedCardHeight(this._lastCardHeight || this._configuredCardHeightFallback(0));
      this._scheduleLayoutRecovery("connected");
      this._adoptLocalSendspinGlobalSession();
      this._cancelLocalSendspinDisconnect();
      this._attachLocalSendspinLifecycleListeners();
      this._scheduleLocalSendspinReconnect("connected", 600);
    }
    disconnectedCallback() {
      clearInterval(this._pollTimer);
      clearInterval(this._progressTimer);
      clearTimeout(this._searchTimer);
      clearTimeout(this._nowPlayingSearchTimer);
      clearTimeout(this._volumeTimer);
      clearTimeout(this._bigVolumeTimer);
      clearTimeout(this._controlRoomVolumeTimer);
      clearTimeout(this._seekTimer);
      clearTimeout(this._resizeTimer);
      clearTimeout(this._layoutRecoveryTimer);
      if (this._layoutRecoveryFrame && typeof cancelAnimationFrame === "function") {
        cancelAnimationFrame(this._layoutRecoveryFrame);
        this._layoutRecoveryFrame = null;
      }
      clearTimeout(this._externalPlaybackSyncTimer);
      clearTimeout(this._systemMobileStatePersistTimer);
      clearTimeout(this._compactTransitionTimer);
      clearTimeout(this._mobileArtBrowseResetTimer);
      clearTimeout(this._surprisePopupTimer);
      clearTimeout(this._simpleWizardPopupTimer);
      clearTimeout(this._maReconnectTimer);
      clearTimeout(this._voiceAssistantDialogCloseTimer);
      clearTimeout(this._screensaverTimer);
      clearTimeout(this._screensaverExitTimer);
      clearInterval(this._screensaverClockTimer);
      this._screensaverTimer = null;
      this._screensaverExitTimer = null;
      this._screensaverClockTimer = null;
      this._state.screensaverOpen = false;
      this.classList.remove("compact-popup-open", "compact-window-popup-open", "compact-inline-popup-open", "compact-tile-open", "screensaver-page-open", "volume-preset-open");
      this._stopVoiceAssistantRecognition();
      this._state.voiceAssistantDialogOpen = false;
      this._state.voiceAssistantKeepScreensaver = false;
      this._clearManualFrontPlayer({ sync: false });
      try {
        this._voiceRecognition?.abort?.();
      } catch {
      }
      this._voiceRecognition = null;
      this._stopScreensaverVisibilityTracking();
      this._maReconnectTimer = null;
      clearInterval(this._mobileSmartVoiceTimer);
      this._mobileSmartVoiceTimer = null;
      if (this._imgObserver) {
        this._imgObserver.disconnect();
        this._imgObserver = null;
      }
      if (this._ws) {
        this._rejectWsPending(new Error("MA WS disconnected"));
        try {
          this._ws.onclose = null;
          this._ws.close();
        } catch (_) {
        }
        this._ws = null;
      } else {
        this._rejectWsPending(new Error("MA WS disconnected"));
      }
      if (this._ctxMenu) {
        this._ctxMenu.remove();
        this._ctxMenu = null;
      }
      const keepLocalSendspinAlive = this._isLocalSendspinDesired();
      this._syncLocalSendspinGlobalSession();
      if (keepLocalSendspinAlive) {
        const session = this._localSendspinGlobalSession();
        if (session && !session.controller) session.controller = this;
      } else {
        this._detachLocalSendspinLifecycleListeners();
        this._scheduleLocalSendspinStop("shutdown", 5 * 60 * 1e3);
      }
      document.removeEventListener("click", this._boundDocClick);
      this._stopResizeTracking();
      this._imageBlobCache.forEach((url) => {
        try {
          URL.revokeObjectURL(url);
        } catch (_) {
        }
      });
      this._imageBlobCache.clear();
      this._imageFailed.clear();
    }
  };
}
function createBaseBrowserState() {
  return {
    view: "home",
    query: "",
    nowPlayingQuery: "",
    selectedPlayer: null,
    frontPinnedPlayerEntity: "",
    manualFrontPlayerEntity: "",
    manualFrontPlayerUntil: 0,
    players: [],
    wsReady: false,
    queueVisible: false,
    queueItems: [],
    queueMutationPendingUntil: 0,
    queueMutationExpectedSignature: "",
    maQueueState: null,
    mobileQueuePlayPendingUntil: 0,
    mobileQueuePlayPendingKey: "",
    mobileQueuePlayPendingIndex: null,
    mobileQueuePlayPendingUri: "",
    mobileQueuePlayPendingPlayerId: "",
    lang: "en",
    pendingGroupSelections: [],
    nowPlayingUri: "",
    renderToken: 0,
    playerModalOpen: false,
    hasAutoSelectedPlayer: false,
    cardTheme: "auto",
    hotelMode: false,
    performanceProfile: "full",
    performanceMode: false,
    performanceModeLocalOverride: false,
    modalMode: "players",
    sidePanelToken: 0,
    immersiveNowPlayingOpen: false,
    lyricsOpen: false,
    screensaverLyricsOpen: false,
    lyricsTrackKey: "",
    lyricsText: "",
    lyricsLoading: false,
    lyricsLines: [],
    lyricsActiveIndex: -1,
    tracksLayout: "list",
    queueActionPending: false,
    emptyQuickShelfItems: [],
    emptyQuickShelfMode: "default",
    forceRadioHero: false,
    quickMixPendingUntil: 0,
    quickMixPendingEntry: null,
    quickMixRecommendationItems: [],
    likedSelectionMode: false,
    likedSelectedUris: [],
    libraryTabSearchQueries: {},
    libraryTabSearchDrafts: {},
    libraryTabSearchOpen: false,
    mobileLibraryDetail: null,
    mobileLibraryDetailParentPage: "",
    mobileLibraryDetailStack: [],
    controlRoomOpen: false,
    controlRoomSelectedPlayers: [],
    controlRoomLibraryQuery: "",
    controlRoomLibraryResults: [],
    controlRoomLibraryLoading: false,
    controlRoomTransferSource: "",
    controlRoomTransferTarget: "",
    controlRoomPanel: "",
    controlRoomVisiblePlayers: [],
    controlRoomCustomScenes: [],
    controlRoomSceneName: "",
    controlRoomRevealThisDevicePending: false,
    controlRoomRenderedHtml: "",
    controlRoomRenderSignature: "",
    activePlayerHelperLastValue: void 0
  };
}
function normalizeSettingsSource(value) {
  const normalized = String(value || "").trim().toLowerCase();
  if (["card", "visual", "config", "card_config", "editor"].includes(normalized)) return "card";
  if (["ui", "device", "local", "in_card"].includes(normalized)) return "ui";
  return "ui";
}
function usesVisualSettings(config) {
  return normalizeSettingsSource(config?.settings_source) === "card";
}
function mobileCompactModeEnabled(state) {
  return !!state?.mobileCompactMode;
}
function mobileCompactEdgeToEdgeEnabled(state) {
  return state?.mobileCompactEdgeToEdge !== false;
}
function mobileCompactWidgetMode(state) {
  const mode = String(state?.mobileCompactWidgetMode || "auto").trim().toLowerCase();
  return ["auto", "full", "mini"].includes(mode) ? mode : "auto";
}
function mobileShowUpNextEnabled(state) {
  return state?.mobileShowUpNext === true;
}
function performanceProfile(state) {
  const profile = String(state?.performanceProfile || "").trim().toLowerCase();
  if (["full", "high", "low", "ultra_lite"].includes(profile)) return profile;
  return state?.performanceMode === true ? "low" : "full";
}
function performanceModeEnabled(state) {
  return ["low", "ultra_lite"].includes(performanceProfile(state));
}
function performanceUltraLiteEnabled(state) {
  return performanceProfile(state) === "ultra_lite";
}
function mobileDynamicThemeMode(state) {
  const profile = performanceProfile(state);
  if (profile === "low" || profile === "ultra_lite") return "off";
  const mode = String(state?.mobileDynamicThemeMode || "auto").toLowerCase();
  const normalized = ["off", "auto", "strong"].includes(mode) ? mode : "auto";
  return profile === "high" && normalized === "strong" ? "auto" : normalized;
}
function mobileBackgroundMotionMode(state) {
  const profile = performanceProfile(state);
  if (profile === "low" || profile === "ultra_lite") return "off";
  const mode = String(state?.mobileBackgroundMotionMode || "subtle").toLowerCase();
  const normalized = ["off", "subtle", "strong", "extreme"].includes(mode) ? mode : "subtle";
  return profile === "high" && ["strong", "extreme"].includes(normalized) ? "subtle" : normalized;
}
function backgroundMotionEnabled(state) {
  return mobileBackgroundMotionMode(state) !== "off";
}
function backgroundMotionAmount(state) {
  const mode = mobileBackgroundMotionMode(state);
  if (mode === "extreme") return "1.85";
  if (mode === "strong") return "1.35";
  return "1";
}
function isCompactTileMode(state) {
  return mobileCompactModeEnabled(state) && !state?.mobileCompactExpanded;
}
const MOBILE_DYNAMIC_THEME_MODES = ["off", "auto", "strong"];
const MOBILE_BACKGROUND_MOTION_MODES = ["off", "subtle", "strong", "extreme"];
const PERFORMANCE_PROFILE_MODES = ["full", "high", "low", "ultra_lite"];
const MOBILE_NIGHT_MODES = ["off", "auto", "on"];
const MOBILE_FOOTER_MODES = ["icon", "text", "both"];
const MOBILE_VOLUME_MODES = ["always", "button"];
const MOBILE_COMPACT_WIDGET_MODES = ["auto", "full", "mini"];
const MOBILE_LAYOUT_MODES = ["auto", "full", "compact"];
const MOBILE_LIBRARY_LAYOUT_MODES = ["grid", "list"];
const MOBILE_MIC_MODES = ["on", "off", "smart"];
const MOBILE_LIKED_MODES = ["ma", "local"];
const MOBILE_SWIPE_MODES = ["play", "browse"];
const VOICE_ASSISTANT_MODES = ["hybrid", "music", "assist"];
const SCREENSAVER_CLOCK_MODES = ["digital", "analog"];
const SCREENSAVER_CONTROL_BUTTONS = ["previous", "play_pause", "next", "mute", "power", "like", "lyrics", "voice"];
const POWER_BUTTON_ACTIONS = ["stop_player", "toggle", "turn_on", "turn_off", "scene", "script"];
const AUXILIARY_BUTTON_ICONS = ["power", "home", "speaker", "music_note", "wand", "grid", "settings", "heart_outline", "play", "stop", "radio", "timer", "info"];
const PLAYER_SORT_MODES = ["default", "alphabetical", "custom"];
const MOBILE_MAIN_BAR_ITEMS = ["search", "library", "players", "actions", "settings", "theme"];
const MOBILE_QUICK_ACTIONS = ["home", "search", "timer", "like", "lyrics", "queue", "radio", "voice", "history", "info", "disconnect_all"];
const MOBILE_LIBRARY_TABS = ["library_playlists", "library_artists", "library_albums", "library_tracks", "library_radio", "library_podcasts", "library_liked", "library_search"];
const COLOR_LIGHT_MODES = ["hs", "xy", "rgb", "rgbw", "rgbww"];
function normalizeEnum(value, allowedValues, fallbackValue) {
  const normalized = String(value || "").trim().toLowerCase();
  return allowedValues.includes(normalized) ? normalized : fallbackValue;
}
function normalizePerformanceProfile(value, legacyPerformanceMode = false) {
  const normalized = normalizeEnum(value, PERFORMANCE_PROFILE_MODES, "");
  if (normalized) return normalized;
  return legacyPerformanceMode === true ? "low" : "full";
}
function normalizeStringArray(value) {
  return Array.isArray(value) ? value.map((entry) => String(entry || "").trim()).filter(Boolean) : [];
}
function normalizeEntityList(value) {
  const source = Array.isArray(value) ? value : String(value || "").split(/[\s,]+/);
  const next = [];
  source.map((entry) => String(entry || "").trim()).filter(Boolean).forEach((entityId) => {
    if (!next.includes(entityId)) next.push(entityId);
  });
  return next;
}
function parseAmbientLightPlayerMap(value) {
  const groups = [];
  const byPlayer = /* @__PURE__ */ new Map();
  normalizeStringArray(value).forEach((mapping) => {
    const parts = String(mapping || "").split(/\s*(?:=>|=|:)\s*/);
    const player = String(parts[0] || "").trim();
    const lights = normalizeEntityList(parts.slice(1).join(",")).filter((entityId) => entityId.startsWith("light."));
    if (!player.startsWith("media_player.") || !lights.length) return;
    if (!byPlayer.has(player)) {
      const group2 = { player, lights: [] };
      byPlayer.set(player, group2);
      groups.push(group2);
    }
    const group = byPlayer.get(player);
    lights.forEach((entityId) => {
      if (!group.lights.includes(entityId)) group.lights.push(entityId);
    });
  });
  return groups;
}
function formatAmbientLightPlayerMapEntry(player, lights = []) {
  const playerId = String(player || "").trim();
  const lightIds = normalizeEntityList(lights).filter((entityId) => entityId.startsWith("light."));
  return playerId && lightIds.length ? `${playerId} = ${lightIds.join(", ")}` : "";
}
function isColorCapableLightEntity(entity) {
  if (!entity?.entity_id?.startsWith?.("light.")) return false;
  const attributes = entity.attributes || {};
  const supportedModes = Array.isArray(attributes.supported_color_modes) ? attributes.supported_color_modes.map((mode) => String(mode || "").toLowerCase()) : [];
  if (supportedModes.some((mode) => COLOR_LIGHT_MODES.includes(mode))) return true;
  if (COLOR_LIGHT_MODES.includes(String(attributes.color_mode || "").toLowerCase())) return true;
  const supportedFeatures = Number(attributes.supported_features);
  return Number.isFinite(supportedFeatures) && (supportedFeatures & 16) === 16;
}
function clampPercent(value, fallback = 35, { min = 1, max = 100 } = {}) {
  const number = Number(value);
  const safe = Number.isFinite(number) ? number : fallback;
  return Math.max(min, Math.min(max, safe));
}
function clampSeconds(value, fallback = 3, { min = 0, max = 300 } = {}) {
  const number = Number(value);
  const safe = Number.isFinite(number) ? number : fallback;
  return Math.max(min, Math.min(max, safe));
}
function clampNumber(value, fallback = 1, { min = 0, max = 1 } = {}) {
  const number = Number(value);
  const safe = Number.isFinite(number) ? number : fallback;
  return Math.max(min, Math.min(max, safe));
}
function normalizeScreensaverClockMode(value) {
  return normalizeEnum(value, SCREENSAVER_CLOCK_MODES, "digital");
}
function normalizeScreensaverControlButtons(items, fallbackItems = []) {
  const allowed = new Set(SCREENSAVER_CONTROL_BUTTONS);
  const fallback = normalizeStringArray(fallbackItems).filter((item) => allowed.has(item));
  const hasExplicitItems = Array.isArray(items);
  const source = hasExplicitItems ? items : fallback;
  const cleaned = [];
  normalizeStringArray(source).forEach((item) => {
    if (allowed.has(item) && !cleaned.includes(item)) cleaned.push(item);
  });
  return cleaned.length ? cleaned : hasExplicitItems ? [] : fallback;
}
function normalizePowerButtonAction(value) {
  return normalizeEnum(value, POWER_BUTTON_ACTIONS, "stop_player");
}
function normalizeAuxiliaryButtonIcon(value) {
  const icon = String(value || "").trim();
  if (/^[a-z0-9_-]+:[a-z0-9_-]+$/i.test(icon)) return icon;
  return normalizeEnum(icon, AUXILIARY_BUTTON_ICONS, "power");
}
function normalizePlayerSortMode(value) {
  return normalizeEnum(value, PLAYER_SORT_MODES, "default");
}
function normalizePlayerOrderEntities(config = {}, limit = 50) {
  const ordered = [];
  const dynamicLimit = Object.keys(config || {}).reduce((max, key) => {
    const match = /^player_order_entity_(\d+)$/.exec(key);
    return match ? Math.max(max, Number(match[1]) || 0) : max;
  }, limit);
  for (let index = 1; index <= dynamicLimit; index += 1) {
    const entityId = String(config?.[`player_order_entity_${index}`] || "").trim();
    if (entityId && !ordered.includes(entityId)) ordered.push(entityId);
  }
  normalizeStringArray(config?.player_order_entities).forEach((entityId) => {
    if (!ordered.includes(entityId)) ordered.push(entityId);
  });
  return ordered;
}
function normalizeAuxiliaryButtons(config = {}, limit = 4) {
  const buttons = [{
    enabled: config.power_button_enabled === true,
    name: String(config.power_button_name || "").trim(),
    icon: normalizeAuxiliaryButtonIcon(config.power_button_icon || "power"),
    action: normalizePowerButtonAction(config.power_button_action),
    entity: String(config.power_button_entity || "").trim()
  }];
  for (let index = 2; index <= limit; index += 1) {
    buttons.push({
      enabled: config[`aux_button_${index}_enabled`] === true,
      name: String(config[`aux_button_${index}_name`] || "").trim(),
      icon: normalizeAuxiliaryButtonIcon(config[`aux_button_${index}_icon`] || "power"),
      action: normalizePowerButtonAction(config[`aux_button_${index}_action`]),
      entity: String(config[`aux_button_${index}_entity`] || "").trim()
    });
  }
  return buttons;
}
function clampMobileFontScale(value) {
  return Math.max(0.5, Math.min(1.5, Number(value || 1) || 1));
}
function clampMobileIconScale(value) {
  return Math.max(0.8, Math.min(1.25, Number(value || 1) || 1));
}
function clampMobileVolumeStepPercent(value) {
  const number = Number(value);
  return Math.round(Math.max(1, Math.min(10, Number.isFinite(number) ? number : 5)));
}
function normalizeHomeShortcutPath(value, { leadingSlash = false } = {}) {
  const normalized = String(value || "/").trim() || "/";
  if (!leadingSlash) return normalized;
  if (normalized.startsWith("/")) return normalized;
  return `/${normalized.replace(/^\/+/, "")}`;
}
function normalizeMobileFooterMode(value) {
  return normalizeEnum(value, MOBILE_FOOTER_MODES, "both");
}
function normalizeMobileMicMode(value) {
  return normalizeEnum(value, MOBILE_MIC_MODES, "smart");
}
function normalizeVoiceAssistantMode(value) {
  return normalizeEnum(value, VOICE_ASSISTANT_MODES, "hybrid");
}
function normalizeMobileVolumeMode(value) {
  return normalizeEnum(value, MOBILE_VOLUME_MODES, "button");
}
function normalizeMobileCompactWidgetMode(value) {
  return normalizeEnum(value, MOBILE_COMPACT_WIDGET_MODES, "auto");
}
function normalizeMobileLayoutMode(value) {
  return normalizeEnum(value, MOBILE_LAYOUT_MODES, "auto");
}
function normalizeMobileLibraryDefaultLayout(value, fallback = "list") {
  return normalizeEnum(value, MOBILE_LIBRARY_LAYOUT_MODES, normalizeEnum(fallback, MOBILE_LIBRARY_LAYOUT_MODES, "list"));
}
function normalizePinnedPlayerEntityList(value) {
  const next = [];
  normalizeStringArray(value).forEach((entityId) => {
    if (!next.includes(entityId)) next.push(entityId);
  });
  return next;
}
function normalizePinnedPlayerEntities(config = {}) {
  const explicitEntities = normalizeStringArray(config?.pinned_player_entities);
  if (explicitEntities.length) return normalizePinnedPlayerEntityList(explicitEntities);
  const singleEntity = String(config?.pinned_player_entity || "").trim();
  return normalizePinnedPlayerEntityList(singleEntity ? [singleEntity] : []);
}
function normalizeExcludedPlayerEntities(config = {}) {
  return normalizePinnedPlayerEntityList(config?.excluded_player_entities);
}
function normalizeMobileMainBarItems(items, {
  usesVisualSettings: usesVisualSettings2 = false,
  hidePlayers = false,
  fallbackItems = []
} = {}) {
  const allowed = new Set(MOBILE_MAIN_BAR_ITEMS);
  const baseFallback = normalizeStringArray(fallbackItems);
  const fallback = usesVisualSettings2 ? baseFallback.filter((item) => item !== "settings") : baseFallback;
  const source = Array.isArray(items) && items.length ? items : fallback;
  const cleaned = normalizeStringArray(source).filter((item) => allowed.has(item)).filter((item) => !(usesVisualSettings2 && item === "settings")).filter((item) => !(hidePlayers && item === "players"));
  const normalizedFallback = fallback.filter((item) => !(hidePlayers && item === "players"));
  const normalized = cleaned.length ? cleaned : normalizedFallback;
  if (!usesVisualSettings2 && !normalized.includes("settings")) normalized.push("settings");
  return normalized;
}
function normalizeMobileLibraryTabs(tabs, fallbackTabs = []) {
  const allowed = new Set(MOBILE_LIBRARY_TABS);
  const fallback = normalizeStringArray(fallbackTabs);
  const source = Array.isArray(tabs) && tabs.length ? tabs : fallback;
  const cleaned = normalizeStringArray(source).filter((tab) => allowed.has(tab));
  const normalized = cleaned.length ? cleaned : fallback;
  return normalized.includes("library_search") ? ["library_search", ...normalized.filter((tab) => tab !== "library_search")] : normalized;
}
function normalizeMobileQuickActions(items, fallbackItems = []) {
  const allowed = new Set(MOBILE_QUICK_ACTIONS);
  const fallback = normalizeStringArray(fallbackItems).filter((item) => allowed.has(item));
  const hasExplicitItems = Array.isArray(items);
  const source = hasExplicitItems ? items : fallback;
  const cleaned = [];
  normalizeStringArray(source).forEach((item) => {
    if (allowed.has(item) && !cleaned.includes(item)) cleaned.push(item);
  });
  return cleaned.length ? cleaned : hasExplicitItems ? [] : fallback;
}
function normalizeMobileQuickActionSlots(config = {}, selectedItems = []) {
  const allowed = new Set(MOBILE_QUICK_ACTIONS);
  const selected = normalizeStringArray(selectedItems).filter((item) => allowed.has(item));
  const ordered = [];
  for (let index = 1; index <= 10; index += 1) {
    const item = String(config?.[`mobile_quick_action_${index}`] || "").trim();
    if (!allowed.has(item) || !selected.includes(item) || ordered.includes(item)) continue;
    ordered.push(item);
  }
  selected.forEach((item) => {
    if (!ordered.includes(item)) ordered.push(item);
  });
  return ordered;
}
function normalizeVisualMobileState(config = {}, {
  normalizeClockTime: normalizeClockTime2 = (value, fallback) => String(value || fallback || ""),
  normalizeNightModeDays: normalizeNightModeDays2 = (value) => Array.isArray(value) ? value : [0, 1, 2, 3, 4, 5, 6],
  defaultLibraryTabs = [],
  defaultMainBarItems = [],
  defaultQuickActions = [],
  defaultAnnouncementPresets = []
} = {}) {
  const screensaverControlFallback = config.screensaver_controls_enabled === true ? ["previous", "next"] : [];
  const performanceProfile2 = normalizePerformanceProfile(config.performance_profile, config.performance_mode);
  return {
    lang: String(config.language || "en"),
    cardTheme: String(config.theme_mode || "auto"),
    performanceProfile: performanceProfile2,
    performanceMode: ["low", "ultra_lite"].includes(performanceProfile2),
    mobileCustomColor: String(config.mobile_custom_color || "#f5a623"),
    mobileDynamicThemeMode: normalizeEnum(config.mobile_dynamic_theme_mode, MOBILE_DYNAMIC_THEME_MODES, "auto"),
    mobileBackgroundMotionMode: normalizeEnum(config.mobile_background_motion_mode, MOBILE_BACKGROUND_MOTION_MODES, "subtle"),
    mobileCustomTextTone: String(config.mobile_custom_text_tone || "light") === "dark" ? "dark" : "light",
    hotelMode: config.hotel_mode === true,
    mobileFontScale: clampMobileFontScale(config.mobile_font_scale),
    mobileIconScale: clampMobileIconScale(config.mobile_icon_scale),
    mobileNightMode: normalizeEnum(config.night_mode, MOBILE_NIGHT_MODES, "off"),
    mobileNightModeStart: normalizeClockTime2(config.night_mode_auto_start || "22:00", "22:00"),
    mobileNightModeEnd: normalizeClockTime2(config.night_mode_auto_end || "06:00", "06:00"),
    mobileNightModeDays: normalizeNightModeDays2(config.night_mode_days),
    mobileCompactMode: !!config.mobile_compact_mode,
    mobileCompactWidgetMode: normalizeMobileCompactWidgetMode(config.mobile_compact_widget_mode),
    mobileCompactEdgeToEdge: config.mobile_compact_edge_to_edge !== false,
    mobileLayoutMode: normalizeMobileLayoutMode(config.mobile_layout_mode),
    mobileLibraryDefaultLayout: normalizeMobileLibraryDefaultLayout(config.mobile_library_default_layout, "list"),
    mobileShowUpNext: config.mobile_show_up_next === true,
    mobileFooterSearchEnabled: !!config.mobile_footer_search_enabled,
    mobileStudioShortcutEnabled: config.mobile_studio_shortcut !== false,
    mobileFooterMode: normalizeMobileFooterMode(config.mobile_footer_mode),
    mobileHomeShortcutEnabled: !!config.mobile_home_shortcut,
    mobileHomeShortcutPath: normalizeHomeShortcutPath(config.mobile_home_shortcut_path),
    mobileVolumeMode: normalizeMobileVolumeMode(config.mobile_volume_mode),
    mobileVolumeStepButtonsEnabled: config.mobile_volume_step_buttons === true,
    mobileVolumeStepPercent: clampMobileVolumeStepPercent(config.mobile_volume_step_percent),
    mobileMicMode: normalizeMobileMicMode(config.mobile_mic_mode),
    voiceAssistantEnabled: config.voice_assistant_enabled === true,
    voiceAssistantMode: normalizeVoiceAssistantMode(config.voice_assistant_mode),
    voiceAssistantAgentId: String(config.voice_assistant_agent_id || "").trim(),
    voiceAssistantSpeakFeedback: config.voice_assistant_speak_feedback === true,
    mobileLikedMode: normalizeEnum(config.mobile_liked_mode, MOBILE_LIKED_MODES, "ma"),
    mobileSwipeMode: normalizeEnum(config.mobile_swipe_mode, MOBILE_SWIPE_MODES, "browse"),
    mobileRadioBrowserCountry: String(config.mobile_radio_browser_country || "all"),
    mobileLibraryTabs: Array.isArray(config.mobile_library_tabs) && config.mobile_library_tabs.length ? config.mobile_library_tabs.slice() : normalizeStringArray(defaultLibraryTabs),
    mobileMainBarItems: Array.isArray(config.mobile_main_bar_items) && config.mobile_main_bar_items.length ? config.mobile_main_bar_items.slice() : normalizeStringArray(defaultMainBarItems),
    mobileQuickActions: normalizeMobileQuickActionSlots(config, normalizeMobileQuickActions(
      config.mobile_quick_actions,
      defaultQuickActions
    )),
    mobileAnnouncementPresets: Array.isArray(config.mobile_announcement_presets) && config.mobile_announcement_presets.length ? config.mobile_announcement_presets.slice(0, 3) : normalizeStringArray(defaultAnnouncementPresets).slice(0, 3),
    mobileAnnouncementVolume: Number.isFinite(Number(config.mobile_announcement_volume)) ? Math.max(20, Math.min(50, Number(config.mobile_announcement_volume))) : 20,
    mobileAnnouncementTtsEntity: String(config.announcement_tts_entity || ""),
    mobileAnnouncementTtsLanguage: String(config.announcement_tts_language || "auto"),
    pinnedPlayerEntities: normalizePinnedPlayerEntities(config),
    ambientLightEnabled: config.ambient_light_enabled === true,
    ambientLightEntities: normalizeEntityList(config.ambient_light_entities),
    ambientLightPlayerMap: normalizeStringArray(config.ambient_light_player_map),
    ambientLightBrightness: clampPercent(config.ambient_light_brightness, 35, { min: 1, max: 100 }),
    ambientLightTransition: clampSeconds(config.ambient_light_transition, 3, { min: 0, max: 120 }),
    ambientLightCooldown: clampSeconds(config.ambient_light_cooldown, 8, { min: 0, max: 120 }),
    screensaverEnabled: config.screensaver_enabled === true,
    screensaverControlsEnabled: config.screensaver_controls_enabled === true,
    screensaverControlButtons: normalizeScreensaverControlButtons(config.screensaver_control_buttons, screensaverControlFallback),
    screensaverClockMode: normalizeScreensaverClockMode(config.screensaver_clock_mode),
    screensaverTimeoutSeconds: clampSeconds(config.screensaver_timeout_seconds, 90, { min: 15, max: 3600 }),
    screensaverMessage: String(config.screensaver_message || ""),
    screensaverClockSize: clampNumber(config.screensaver_clock_size, 1, { min: 0.75, max: 1.45 }),
    screensaverClockX: clampNumber(config.screensaver_clock_x, 82, { min: 8, max: 92 }),
    screensaverClockY: clampNumber(config.screensaver_clock_y, 24, { min: 8, max: 70 }),
    powerButtonEnabled: config.power_button_enabled === true,
    powerButtonName: String(config.power_button_name || "").trim(),
    powerButtonIcon: normalizeAuxiliaryButtonIcon(config.power_button_icon || "power"),
    powerButtonAction: normalizePowerButtonAction(config.power_button_action),
    powerButtonEntity: String(config.power_button_entity || "").trim(),
    auxiliaryButtons: normalizeAuxiliaryButtons(config).slice(1),
    excludedPlayerEntities: normalizeExcludedPlayerEntities(config),
    playerSortMode: normalizePlayerSortMode(config.player_sort_mode),
    playerOrderEntities: normalizePlayerOrderEntities(config),
    discoveryModeEnabled: config.discovery_mode_enabled !== false
  };
}
const HomeiiMobileSettingsFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  clampMobileFontScale,
  clampMobileIconScale,
  clampMobileVolumeStepPercent,
  clampNumber,
  clampPercent,
  clampSeconds,
  formatAmbientLightPlayerMapEntry,
  isColorCapableLightEntity,
  normalizeAuxiliaryButtonIcon,
  normalizeAuxiliaryButtons,
  normalizeEntityList,
  normalizeEnum,
  normalizeExcludedPlayerEntities,
  normalizeHomeShortcutPath,
  normalizeMobileCompactWidgetMode,
  normalizeMobileFooterMode,
  normalizeMobileLayoutMode,
  normalizeMobileLibraryDefaultLayout,
  normalizeMobileLibraryTabs,
  normalizeMobileMainBarItems,
  normalizeMobileMicMode,
  normalizeMobileQuickActionSlots,
  normalizeMobileQuickActions,
  normalizeMobileVolumeMode,
  normalizePerformanceProfile,
  normalizePinnedPlayerEntities,
  normalizePinnedPlayerEntityList,
  normalizePlayerOrderEntities,
  normalizePlayerSortMode,
  normalizePowerButtonAction,
  normalizeScreensaverClockMode,
  normalizeScreensaverControlButtons,
  normalizeStringArray,
  normalizeVisualMobileState,
  normalizeVoiceAssistantMode,
  parseAmbientLightPlayerMap
}, Symbol.toStringTag, { value: "Module" }));
const TABLET_BREAKPOINT = 980;
function normalizeLayoutMode(value) {
  const normalized = String(value || "auto").trim().toLowerCase();
  return normalized === "mobile" || normalized === "tablet" ? normalized : "auto";
}
function resolveLayoutMode(layoutMode, {
  rectWidth = 0,
  hostWidth = 0,
  viewportWidth = 0
} = {}, tabletBreakpoint = TABLET_BREAKPOINT) {
  const normalized = normalizeLayoutMode(layoutMode);
  if (normalized === "mobile" || normalized === "tablet") return normalized;
  const cardWidth = Math.max(
    Number(rectWidth || 0),
    Number(hostWidth || 0)
  );
  const width = cardWidth > 0 ? cardWidth : Number(viewportWidth || 0);
  return width >= tabletBreakpoint ? "tablet" : "mobile";
}
function defaultMobileMediaLayout(layoutMode) {
  return layoutMode === "tablet" ? "grid" : "list";
}
function panelViewportFillEnabled({
  viewportHeight = 0,
  hostTop = 0,
  parentHeights = []
} = {}) {
  const viewport = Number(viewportHeight || 0);
  const top = Number(hostTop || 0);
  if (!Number.isFinite(viewport) || viewport <= 240) return false;
  if (!Number.isFinite(top) || Math.abs(top) > 8) return false;
  const heights = (Array.isArray(parentHeights) ? parentHeights : [parentHeights]).map((value) => Number(value || 0)).filter((value) => Number.isFinite(value) && value > 240);
  return heights.some((height) => Math.abs(height - viewport) <= 8);
}
function autoCompactRecommended({
  width = 0,
  height = 0
} = {}) {
  const measuredWidth = Number(width || 0);
  const measuredHeight = Number(height || 0);
  if (!Number.isFinite(measuredWidth) || measuredWidth <= 0) return false;
  if (measuredWidth <= 340) return true;
  if (!Number.isFinite(measuredHeight) || measuredHeight <= 0) return false;
  if (measuredWidth <= 560 && measuredHeight < 620) return true;
  return measuredWidth <= 660 && measuredHeight < 520;
}
function resolveLayoutProfile({
  width = 0,
  height = 0,
  layoutMode = "mobile"
} = {}) {
  const measuredWidth = Math.max(0, Number(width || 0));
  const measuredHeight = Math.max(0, Number(height || 0));
  const size = measuredWidth < 380 ? "xs" : measuredWidth < 520 ? "sm" : measuredWidth < 760 ? "md" : measuredWidth < 1100 ? "lg" : "xl";
  const heightSize = measuredHeight > 0 && measuredHeight < 620 ? "short" : measuredHeight >= 900 ? "tall" : "normal";
  const aspect = measuredHeight > 0 && measuredWidth / measuredHeight >= 1.08 ? "wide" : measuredWidth > 0 && measuredHeight / measuredWidth >= 1.18 ? "portrait" : "balanced";
  const tight = measuredHeight > 0 && measuredHeight < 560;
  const compact = size === "xs" || heightSize === "short";
  const roomy = size === "xl" && heightSize === "tall";
  return {
    width: measuredWidth,
    height: measuredHeight,
    layoutMode,
    size,
    heightSize,
    aspect,
    tight,
    compact,
    roomy,
    classes: [
      `size-${size}`,
      `height-${heightSize}`,
      `aspect-${aspect}`,
      tight ? "height-tight" : "",
      compact ? "profile-compact" : "profile-comfort",
      roomy ? "profile-roomy" : ""
    ].filter(Boolean)
  };
}
function tabletAutoFitEnabled(layoutMode) {
  return layoutMode === "tablet";
}
function tabletAutoFitDense(layoutMode, {
  showNightRow = false,
  showUpNext = false
} = {}) {
  return tabletAutoFitEnabled(layoutMode) && !!(showNightRow || showUpNext);
}
function resolveTabletAutoFitFlags(layoutMode, {
  showNightRow = false,
  showUpNext = false
} = {}) {
  const autoFit = tabletAutoFitEnabled(layoutMode);
  return {
    autoFit,
    showNight: autoFit && !!showNightRow,
    showUpNext: autoFit && !!showUpNext,
    dense: autoFit && !!(showNightRow || showUpNext)
  };
}
function tabletStabilityModeEnabled({
  layoutMode = "mobile",
  userAgent = "",
  width = 0,
  touchPoints = 0
} = {}, tabletBreakpoint = TABLET_BREAKPOINT) {
  return layoutMode === "tablet" && /Android/i.test(String(userAgent || "")) && Number(width || 0) >= tabletBreakpoint && Number(touchPoints || 0) > 0;
}
function detectKeyboardLikeResize({
  editingText = false,
  widthDelta = 0,
  heightDelta = 0
} = {}) {
  return !!editingText && (Number(widthDelta || 0) < 120 && Number(heightDelta || 0) > 18 || Number(widthDelta || 0) < 8 && Number(heightDelta || 0) > 0);
}
function resolveResizeStrategy({
  previousWidth = 0,
  currentWidth = 0,
  previousHeight = 0,
  currentHeight = 0,
  editingText = false,
  tabletStabilityMode = false
} = {}, tabletBreakpoint = TABLET_BREAKPOINT) {
  const widthDelta = Math.abs(Number(currentWidth || 0) - Number(previousWidth || 0));
  const heightDelta = Math.abs(Number(currentHeight || 0) - Number(previousHeight || 0));
  const keyboardLikeResize = detectKeyboardLikeResize({
    editingText,
    widthDelta,
    heightDelta
  });
  const previousTablet = Number(previousWidth || 0) >= tabletBreakpoint;
  const currentTablet = Number(currentWidth || 0) >= tabletBreakpoint;
  const widthThreshold = tabletStabilityMode ? 140 : 48;
  const heightThreshold = tabletStabilityMode ? 180 : 48;
  const softSync = !keyboardLikeResize && previousTablet === currentTablet && widthDelta < widthThreshold && heightDelta < heightThreshold;
  return {
    widthDelta,
    heightDelta,
    keyboardLikeResize,
    previousTablet,
    currentTablet,
    widthThreshold,
    heightThreshold,
    softSync
  };
}
const HomeiiResponsiveFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  autoCompactRecommended,
  defaultMobileMediaLayout,
  detectKeyboardLikeResize,
  panelViewportFillEnabled,
  resolveLayoutMode,
  resolveLayoutProfile,
  resolveResizeStrategy,
  resolveTabletAutoFitFlags,
  tabletAutoFitDense,
  tabletAutoFitEnabled,
  tabletStabilityModeEnabled
}, Symbol.toStringTag, { value: "Module" }));
const DEFAULT_ACCENT_TUPLE = [245, 166, 35];
const DEFAULT_ACCENT_HEX = "#f5a623";
function clampRgbByte(value) {
  return Math.max(0, Math.min(255, Math.round(Number(value) || 0)));
}
function normalizeRgbTuple(value, fallback = DEFAULT_ACCENT_TUPLE) {
  if (Array.isArray(value) && value.length >= 3) {
    return value.slice(0, 3).map((entry) => clampRgbByte(entry));
  }
  if (typeof value === "string") {
    const parts = value.trim().split(/[,\s]+/).map((entry) => Number(entry));
    if (parts.length >= 3 && parts.every((entry) => Number.isFinite(entry))) {
      return parts.slice(0, 3).map((entry) => clampRgbByte(entry));
    }
  }
  return fallback.map((entry) => clampRgbByte(entry));
}
function hexToRgbTuple(value, fallback = DEFAULT_ACCENT_TUPLE) {
  const hex = String(value || DEFAULT_ACCENT_HEX).replace("#", "").trim();
  const normalized = hex.length === 3 ? hex.split("").map((char) => char + char).join("") : hex.padEnd(6, "0").slice(0, 6);
  const tuple = [
    parseInt(normalized.slice(0, 2), 16),
    parseInt(normalized.slice(2, 4), 16),
    parseInt(normalized.slice(4, 6), 16)
  ];
  if (tuple.every((entry) => Number.isFinite(entry))) {
    return tuple.map((entry) => clampRgbByte(entry));
  }
  return normalizeRgbTuple(fallback);
}
function rgbTupleToString(tuple = DEFAULT_ACCENT_TUPLE) {
  const [r, g, b] = normalizeRgbTuple(tuple);
  return `${r} ${g} ${b}`;
}
function rgbTupleToHex(tuple = DEFAULT_ACCENT_TUPLE) {
  return `#${normalizeRgbTuple(tuple).map((entry) => clampRgbByte(entry).toString(16).padStart(2, "0")).join("")}`;
}
function mixRgb(left = DEFAULT_ACCENT_TUPLE, right = [255, 255, 255], ratio = 0.5) {
  const weight = Math.max(0, Math.min(1, Number(ratio) || 0));
  const from = normalizeRgbTuple(left);
  const to = normalizeRgbTuple(right);
  return from.map((entry, index) => clampRgbByte(entry + (to[index] - entry) * weight));
}
function rgbToHsl(tuple = DEFAULT_ACCENT_TUPLE) {
  let [r, g, b] = normalizeRgbTuple(tuple).map((entry) => entry / 255);
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const lightness = (max + min) / 2;
  if (max === min) return [0, 0, lightness];
  const delta = max - min;
  const saturation = lightness > 0.5 ? delta / (2 - max - min) : delta / (max + min);
  let hue = 0;
  switch (max) {
    case r:
      hue = (g - b) / delta + (g < b ? 6 : 0);
      break;
    case g:
      hue = (b - r) / delta + 2;
      break;
    default:
      hue = (r - g) / delta + 4;
      break;
  }
  return [hue / 6, saturation, lightness];
}
function hslToRgb(hue = 0, saturation = 0, lightness = 0.5) {
  const h = (Number(hue) % 1 + 1) % 1;
  const s = Math.max(0, Math.min(1, Number(saturation) || 0));
  const l = Math.max(0, Math.min(1, Number(lightness) || 0));
  if (s === 0) {
    const value = clampRgbByte(l * 255);
    return [value, value, value];
  }
  const hueToRgb = (p2, q2, t) => {
    let value = t;
    if (value < 0) value += 1;
    if (value > 1) value -= 1;
    if (value < 1 / 6) return p2 + (q2 - p2) * 6 * value;
    if (value < 1 / 2) return q2;
    if (value < 2 / 3) return p2 + (q2 - p2) * (2 / 3 - value) * 6;
    return p2;
  };
  const q = l < 0.5 ? l * (1 + s) : l + s - l * s;
  const p = 2 * l - q;
  return [
    clampRgbByte(hueToRgb(p, q, h + 1 / 3) * 255),
    clampRgbByte(hueToRgb(p, q, h) * 255),
    clampRgbByte(hueToRgb(p, q, h - 1 / 3) * 255)
  ];
}
function tunePaletteColor(tuple = DEFAULT_ACCENT_TUPLE, options = {}) {
  const [hue, saturation, lightness] = rgbToHsl(tuple);
  const minSat = Number(options.minSaturation ?? 0.42);
  const minLight = Number(options.minLightness ?? 0.42);
  const maxLight = Number(options.maxLightness ?? 0.6);
  const sat = Math.max(minSat, saturation);
  const light = Math.max(minLight, Math.min(maxLight, lightness));
  return hslToRgb(hue, sat, light);
}
function isRgbTupleDark(tuple = DEFAULT_ACCENT_TUPLE, threshold = 0.58) {
  const [r, g, b] = normalizeRgbTuple(tuple);
  const luminance = (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255;
  return luminance < Number(threshold || 0.58);
}
function dynamicThemeStrengthValue(mode = "auto") {
  return String(mode || "").toLowerCase() === "strong" ? "1.35" : "1";
}
function buildDynamicThemePalette({
  baseTuple = DEFAULT_ACCENT_TUPLE,
  vividTuple = DEFAULT_ACCENT_TUPLE,
  mode = "auto"
} = {}) {
  const normalizedMode = String(mode || "auto").toLowerCase();
  const strong = normalizedMode === "strong";
  const base = normalizeRgbTuple(baseTuple);
  const accent = tunePaletteColor(vividTuple, {
    minSaturation: strong ? 0.58 : 0.46,
    minLightness: 0.42,
    maxLightness: strong ? 0.56 : 0.6
  });
  const surface = mixRgb(base, accent, strong ? 0.32 : 0.2);
  const glow = mixRgb(accent, [255, 255, 255], strong ? 0.12 : 0.2);
  return {
    accent: rgbTupleToHex(accent),
    accent_rgb: rgbTupleToString(accent),
    surface_rgb: rgbTupleToString(surface),
    glow_rgb: rgbTupleToString(glow)
  };
}
function normalizeMaPalette(palette = null, { mode = "auto" } = {}) {
  if (!palette || typeof palette !== "object") return null;
  if (palette.accent && palette.accent_rgb && palette.surface_rgb && palette.glow_rgb) return palette;
  const accentSource = palette.accent || palette.primary || palette.background_light || palette.background_dark;
  const baseSource = palette.background_dark || palette.background_light || palette.primary || accentSource;
  if (!accentSource && !baseSource) return null;
  return buildDynamicThemePalette({
    baseTuple: normalizeRgbTuple(baseSource || accentSource),
    vividTuple: normalizeRgbTuple(accentSource || baseSource),
    mode
  });
}
function resolveActiveAccentColor(palette = null, customColor = DEFAULT_ACCENT_HEX) {
  return palette?.accent || String(customColor || DEFAULT_ACCENT_HEX);
}
function resolveActiveAccentRgb(palette = null, customColor = DEFAULT_ACCENT_HEX) {
  return palette?.accent_rgb || rgbTupleToString(hexToRgbTuple(customColor));
}
const HomeiiPaletteFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  buildDynamicThemePalette,
  clampRgbByte,
  dynamicThemeStrengthValue,
  hexToRgbTuple,
  hslToRgb,
  isRgbTupleDark,
  mixRgb,
  normalizeMaPalette,
  normalizeRgbTuple,
  resolveActiveAccentColor,
  resolveActiveAccentRgb,
  rgbToHsl,
  rgbTupleToHex,
  rgbTupleToString,
  tunePaletteColor
}, Symbol.toStringTag, { value: "Module" }));
const DEFAULT_NIGHT_MODE = "auto";
const DEFAULT_NIGHT_START = "22:00";
const DEFAULT_NIGHT_END = "06:00";
const DEFAULT_NIGHT_DAYS = Object.freeze([0, 1, 2, 3, 4, 5, 6]);
const DEFAULT_SLEEP_TIMER_STEPS = Object.freeze([15, 30, 45, 60, 0]);
function normalizeNightMode(value) {
  const mode = String(value || DEFAULT_NIGHT_MODE).toLowerCase();
  return ["off", "auto", "on"].includes(mode) ? mode : DEFAULT_NIGHT_MODE;
}
function normalizeClockTime(value, fallback = DEFAULT_NIGHT_START) {
  const match = String(value || "").trim().match(/^(\d{1,2})(?::(\d{1,2}))?$/);
  if (!match) return fallback;
  const hours = Math.max(0, Math.min(23, Number(match[1]) || 0));
  const minutes = Math.max(0, Math.min(59, Number(match[2] ?? 0) || 0));
  return `${String(hours).padStart(2, "0")}:${String(minutes).padStart(2, "0")}`;
}
function clockMinutesOfDay(value, fallback = DEFAULT_NIGHT_START) {
  const normalized = normalizeClockTime(value, fallback);
  const [hours, minutes] = normalized.split(":").map((part) => Number(part) || 0);
  return hours * 60 + minutes;
}
function defaultNightModeDays() {
  return [...DEFAULT_NIGHT_DAYS];
}
function normalizeNightModeDays(value, fallbackDays = DEFAULT_NIGHT_DAYS) {
  let source = value;
  if (typeof source === "string") {
    const raw = source.trim();
    if (!raw) return [...fallbackDays];
    try {
      const parsed = JSON.parse(raw);
      if (Array.isArray(parsed)) source = parsed;
    } catch (_) {
      source = raw.split(/[,\s]+/);
    }
  }
  const normalized = Array.isArray(source) ? source.map((entry) => Number(entry)).filter((entry) => Number.isInteger(entry) && entry >= 0 && entry <= 6) : [];
  const unique = Array.from(new Set(normalized)).sort((a, b) => a - b);
  return unique.length ? unique : [...fallbackDays];
}
function resolveNightModeWindow(start, end, defaults = {}) {
  const fallbackStart = defaults.start || DEFAULT_NIGHT_START;
  const fallbackEnd = defaults.end || DEFAULT_NIGHT_END;
  return {
    start: normalizeClockTime(start || fallbackStart, fallbackStart),
    end: normalizeClockTime(end || fallbackEnd, fallbackEnd)
  };
}
function isMinutesInsideWindow(minutes, startMinutes, endMinutes) {
  if (startMinutes === endMinutes) return true;
  if (startMinutes < endMinutes) {
    return minutes >= startMinutes && minutes < endMinutes;
  }
  return minutes >= startMinutes || minutes < endMinutes;
}
function isNightModeActive({
  mode = DEFAULT_NIGHT_MODE,
  start = DEFAULT_NIGHT_START,
  end = DEFAULT_NIGHT_END,
  days = DEFAULT_NIGHT_DAYS,
  date = /* @__PURE__ */ new Date()
} = {}) {
  const normalizedMode = normalizeNightMode(mode);
  if (normalizedMode === "off") return false;
  if (normalizedMode === "on") return true;
  const nowMinutes = date.getHours() * 60 + date.getMinutes();
  const startMinutes = clockMinutesOfDay(start, DEFAULT_NIGHT_START);
  const endMinutes = clockMinutesOfDay(end, DEFAULT_NIGHT_END);
  if (!isMinutesInsideWindow(nowMinutes, startMinutes, endMinutes)) return false;
  const enabledDays = new Set(normalizeNightModeDays(days));
  const currentDay = Number(date.getDay());
  if (startMinutes === endMinutes || startMinutes < endMinutes) {
    return enabledDays.has(currentDay);
  }
  const windowOwnerDay = nowMinutes >= startMinutes ? currentDay : (currentDay + 6) % 7;
  return enabledDays.has(windowOwnerDay);
}
function sleepTimerRemainingMs(targetAt, now = Date.now()) {
  const target = Number(targetAt || 0);
  if (!target || target <= now) return 0;
  return target - now;
}
function sleepTimerRemainingLabel(remainingMs) {
  const remaining = Number(remainingMs || 0);
  if (!remaining) return "";
  const totalMinutes = Math.max(1, Math.ceil(remaining / 6e4));
  if (totalMinutes < 60) return `${totalMinutes}m`;
  const hours = Math.floor(totalMinutes / 60);
  const minutes = totalMinutes % 60;
  return minutes ? `${hours}h ${minutes}m` : `${hours}h`;
}
function sleepTimerFooterLabel(remainingMs) {
  const remaining = Number(remainingMs || 0);
  if (!remaining) return "";
  const totalSeconds = Math.max(0, Math.ceil(remaining / 1e3));
  const hours = Math.floor(totalSeconds / 3600);
  const minutes = Math.floor(totalSeconds % 3600 / 60);
  const seconds = totalSeconds % 60;
  return hours > 0 ? `${hours}:${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}` : `${minutes}:${String(seconds).padStart(2, "0")}`;
}
function normalizeSleepTimerOrigin(source = "") {
  return String(source || "").toLowerCase() === "night" ? "night" : "";
}
function sleepTimerStartedFromNightMode(remainingMs, origin = "") {
  return Number(remainingMs || 0) > 0 && normalizeSleepTimerOrigin(origin) === "night";
}
function sleepTimerChipVisible(remainingMs, origin = "") {
  return Number(remainingMs || 0) > 0;
}
function createSleepTimerTargetAt(minutes, now = Date.now()) {
  const amount = Math.max(1, Number(minutes) || 0);
  return Number(now || 0) + amount * 6e4;
}
function extendSleepTimerTargetAt(existingTargetAt, minutes, now = Date.now()) {
  const amount = Math.max(1, Number(minutes) || 0);
  const base = sleepTimerRemainingMs(existingTargetAt, now) ? Number(existingTargetAt || 0) : Number(now || 0);
  return Math.max(base, Number(now || 0)) + amount * 6e4;
}
function nextSleepTimerStep(currentRemainingMs, steps = DEFAULT_SLEEP_TIMER_STEPS) {
  const remaining = Number(currentRemainingMs || 0);
  if (!remaining) return Number(steps[0] || 15);
  const currentMinutes = Math.max(1, Math.ceil(remaining / 6e4));
  return steps.find((value) => value > currentMinutes) ?? 0;
}
const HomeiiNightFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  clockMinutesOfDay,
  createSleepTimerTargetAt,
  defaultNightModeDays,
  extendSleepTimerTargetAt,
  isMinutesInsideWindow,
  isNightModeActive,
  nextSleepTimerStep,
  normalizeClockTime,
  normalizeNightMode,
  normalizeNightModeDays,
  normalizeSleepTimerOrigin,
  resolveNightModeWindow,
  sleepTimerChipVisible,
  sleepTimerFooterLabel,
  sleepTimerRemainingLabel,
  sleepTimerRemainingMs,
  sleepTimerStartedFromNightMode
}, Symbol.toStringTag, { value: "Module" }));
const GENERIC_PLAYER_TOKENS = /* @__PURE__ */ new Set([
  "media",
  "player",
  "speaker",
  "room",
  "homeii",
  "browser",
  "music",
  "assistant",
  "רמקולים",
  "נגן"
]);
const genericPlayerTokens = GENERIC_PLAYER_TOKENS;
function entityMatchTokens(value = "") {
  return String(value || "").toLowerCase().replace(/^media_player\./, "").replace(/^button\./, "").replace(/[_\-.]+/g, " ").split(/\s+/).map((token) => token.trim()).filter((token) => token && token.length > 1);
}
function isLikelyBrowserPlayer(player = null) {
  if (!player) return false;
  const attrs = player.attributes || {};
  const haystack = [
    player.entity_id,
    attrs.friendly_name,
    attrs.mass_player_type,
    attrs.mass_player_id,
    attrs.app_id,
    attrs.provider,
    attrs.provider_name,
    attrs.source,
    attrs.model
  ].filter(Boolean).join(" ").toLowerCase();
  return haystack.includes("sendspin") || haystack.includes("browser") || haystack.includes("web player") || haystack.includes("this device");
}
function isMusicAssistantPlayer(player = null, registryEntry = null) {
  if (!player?.entity_id?.startsWith("media_player.")) return false;
  const attrs = player.attributes || {};
  const registry = registryEntry || {};
  const identity = [
    attrs.app_id,
    attrs.integration,
    attrs.platform,
    attrs.provider,
    attrs.provider_name,
    registry.integration,
    registry.platform,
    registry.device_class
  ].filter(Boolean).join(" ").toLowerCase();
  return identity.includes("music_assistant") || identity.includes("music assistant") || !!attrs.mass_player_type || !!attrs.mass_player_id || !!attrs.active_queue;
}
function getBrowserPlayers(players = []) {
  return (Array.isArray(players) ? players : []).filter((player) => isLikelyBrowserPlayer(player));
}
function getThisDevicePlayer(players = [], rememberedId = "") {
  const target = String(rememberedId || "").trim();
  if (!target) return null;
  return (Array.isArray(players) ? players : []).find((player) => player?.entity_id === target) || null;
}
function resolvePinnedPlayerEntities(preferredIds = [], players = []) {
  const validIds = new Set((Array.isArray(players) ? players : []).map((player) => String(player?.entity_id || "").toLowerCase()));
  return (Array.isArray(preferredIds) ? preferredIds : []).filter((entityId) => validIds.has(String(entityId || "").toLowerCase()));
}
function resolvePreferredFrontPlayerEntity(players = [], {
  currentEntityId = "",
  frontPinnedEntityId = "",
  manualFrontEntityId = "",
  manualFrontUntil = 0,
  now = Date.now(),
  pinnedEntityIds = [],
  orderedEntityIds = [],
  isPlayerActiveFn = (player) => player?.state === "playing",
  isExternalBrowserPlayerFn = isLikelyBrowserPlayer
} = {}) {
  const sourcePlayers = Array.isArray(players) ? players : [];
  const byId = new Map(sourcePlayers.map((player) => [player?.entity_id, player]).filter(([entityId]) => !!entityId));
  const sourceIndex = new Map(sourcePlayers.map((player, index) => [player?.entity_id, index]).filter(([entityId]) => !!entityId));
  const orderIndex = new Map((Array.isArray(orderedEntityIds) ? orderedEntityIds : []).map((entityId, index) => [String(entityId || "").trim(), index]).filter(([entityId]) => !!entityId));
  const usable = (player) => !!player?.entity_id && !isExternalBrowserPlayerFn(player);
  const usableById = (entityId) => {
    const player = byId.get(String(entityId || "").trim());
    return usable(player) ? player : null;
  };
  const rank = (player) => orderIndex.has(player?.entity_id) ? orderIndex.get(player.entity_id) : 1e5 + (sourceIndex.get(player?.entity_id) ?? 1e5);
  const pickPreferred = (candidates = []) => candidates.filter(usable).sort((left, right) => rank(left) - rank(right))[0] || null;
  const manualFrontCandidate = usableById(manualFrontEntityId);
  const manualFront = manualFrontCandidate && (Number(manualFrontUntil || 0) > Number(now || 0) || manualFrontCandidate.state === "playing") ? manualFrontCandidate : null;
  if (manualFront) return manualFront.entity_id;
  const frontPinned = usableById(frontPinnedEntityId);
  if (frontPinned) return frontPinned.entity_id;
  const playing = pickPreferred(sourcePlayers.filter((player) => player?.state === "playing"));
  if (playing) return playing.entity_id;
  const active = pickPreferred(sourcePlayers.filter((player) => isPlayerActiveFn(player)));
  if (active) return active.entity_id;
  for (const entityId of Array.isArray(pinnedEntityIds) ? pinnedEntityIds : []) {
    const pinned = usableById(entityId);
    if (pinned) return pinned.entity_id;
  }
  for (const entityId of Array.isArray(orderedEntityIds) ? orderedEntityIds : []) {
    const ordered = usableById(entityId);
    if (ordered) return ordered.entity_id;
  }
  const current = usableById(currentEntityId);
  if (current) return current.entity_id;
  return (sourcePlayers.find(usable) || sourcePlayers[0])?.entity_id || "";
}
function playerByEntityId(entityId = "", players = [], hassStates = {}) {
  const target = String(entityId || "").trim();
  if (!target) return null;
  return (Array.isArray(players) ? players : []).find((player) => player?.entity_id === target) || hassStates?.[target] || null;
}
function favoriteButtonEntityForPlayer({
  player = null,
  hassStates = {},
  explicitEntity = "",
  fallbackEntity = "button.bathroom_favorite_current_song_2"
} = {}) {
  const configured = String(explicitEntity || "").trim();
  if (configured && hassStates?.[configured]) return configured;
  if (hassStates?.[fallbackEntity]) {
    const selectedName = `${player?.entity_id || ""} ${player?.attributes?.friendly_name || ""}`.toLowerCase();
    if (!selectedName || selectedName.includes("bathroom") || selectedName.includes("מקלחת")) {
      return fallbackEntity;
    }
  }
  const buttons = Object.values(hassStates || {}).filter((entity) => {
    if (!entity?.entity_id?.startsWith("button.")) return false;
    const search = `${entity.entity_id} ${entity.attributes?.friendly_name || ""}`.toLowerCase();
    return search.includes("favorite") || search.includes("אהוב") || search.includes("אהבתי");
  });
  if (!buttons.length) return "";
  if (buttons.length === 1) return buttons[0]?.entity_id || "";
  const tokens = [
    ...entityMatchTokens(player?.entity_id || ""),
    ...entityMatchTokens(player?.attributes?.friendly_name || "")
  ].filter((token) => !GENERIC_PLAYER_TOKENS.has(token));
  const scored = buttons.map((entity) => {
    const haystack = `${entity.entity_id} ${entity.attributes?.friendly_name || ""}`.toLowerCase();
    const score = tokens.reduce((sum, token) => sum + (haystack.includes(token) ? 1 : 0), 0);
    const currentSongBoost = /(current|song|track|playing|\u05e0\u05d5\u05db\u05d7\u05d9|\u05de\u05ea\u05e0\u05d2\u05df)/.test(haystack) ? 0.5 : 0;
    return { entity_id: entity.entity_id, score: score + currentSongBoost };
  }).sort((left, right) => right.score - left.score);
  if (scored[0]?.score > 0) return scored[0].entity_id;
  const genericCurrent = buttons.find((entity) => /(current|song|track|playing|\u05e0\u05d5\u05db\u05d7\u05d9|\u05de\u05ea\u05e0\u05d2\u05df)/.test(`${entity.entity_id} ${entity.attributes?.friendly_name || ""}`.toLowerCase()));
  if (genericCurrent?.entity_id) return genericCurrent.entity_id;
  return "";
}
function favoriteButtonDeviceId(entityId = "", hassEntities = {}) {
  const target = String(entityId || "").trim();
  if (!target) return "";
  return String(hassEntities?.[target]?.device_id || "").trim();
}
function announcementEligiblePlayers(players = []) {
  return (Array.isArray(players) ? players : []).filter((player) => player?.entity_id).filter((player) => !isLikelyBrowserPlayer(player));
}
function mobileNavigableActivePlayers(players = [], pinnedEntityIds = [], isPlayerActiveFn = () => false) {
  const sourcePlayers = Array.isArray(players) ? players : [];
  const preferred = Array.isArray(pinnedEntityIds) ? pinnedEntityIds.filter(Boolean) : [];
  if (preferred.length) {
    return preferred.map((entityId) => sourcePlayers.find((player) => player?.entity_id === entityId)).filter(Boolean);
  }
  return sourcePlayers.filter((player) => isPlayerActiveFn(player)).filter((player) => !isLikelyBrowserPlayer(player));
}
function playerGroupMemberIds(player = null) {
  const ids = Array.isArray(player?.attributes?.group_members) ? player.attributes.group_members.filter(Boolean) : [];
  if (player?.entity_id && ids.length && !ids.includes(player.entity_id)) ids.unshift(player.entity_id);
  return [...new Set(ids)];
}
function isStaticGroupPlayer(player = null) {
  if (!player) return false;
  const attrs = player.attributes || {};
  const typeText = [
    player.entity_id,
    attrs.friendly_name,
    attrs.mass_player_type,
    attrs.player_type,
    attrs.type,
    attrs.grouping_type,
    attrs.group_type,
    attrs.device_class
  ].map((value) => String(value || "").toLowerCase()).join(" ");
  const looksLikeGroup = !!(attrs.is_group || attrs.is_group_player || attrs.group_childs || attrs.group_children || attrs.group_members?.length > 1);
  return looksLikeGroup && /(sync|static|group|party|all speakers|whole home|everywhere)/.test(typeText);
}
function playerGroupCount(player = null) {
  if (isStaticGroupPlayer(player)) return 0;
  const count = playerGroupMemberIds(player).length;
  return count > 1 ? count : 0;
}
function playerGroupMemberNames(player = null, players = []) {
  const ids = playerGroupMemberIds(player);
  const byId = new Map((Array.isArray(players) ? players : []).map((entry) => [entry?.entity_id, entry]));
  return ids.map((entityId) => byId.get(entityId)?.attributes?.friendly_name || entityId).filter(Boolean);
}
function groupAverageVolume(player = null, players = []) {
  const ids = playerGroupMemberIds(player);
  const byId = new Map((Array.isArray(players) ? players : []).map((entry) => [entry?.entity_id, entry]));
  const volumes = ids.map((entityId) => byId.get(entityId)?.attributes?.volume_level).map((value) => Number(value)).filter((value) => Number.isFinite(value));
  if (!volumes.length) return Math.round((player?.attributes?.volume_level || 0) * 100);
  return Math.round(volumes.reduce((sum, value) => sum + value, 0) / volumes.length * 100);
}
function groupedPlayerIds(players = []) {
  const sourcePlayers = Array.isArray(players) ? players : [];
  const ids = /* @__PURE__ */ new Set();
  const byId = new Map(sourcePlayers.map((player) => [player?.entity_id, player]));
  for (const player of sourcePlayers) {
    if (isLikelyBrowserPlayer(player)) continue;
    if (isStaticGroupPlayer(player)) continue;
    const members = playerGroupMemberIds(player);
    if (members.length > 1) {
      members.filter((entityId) => entityId && entityId !== player.entity_id).filter((entityId) => !isLikelyBrowserPlayer(byId.get(entityId))).filter((entityId) => !isStaticGroupPlayer(byId.get(entityId))).forEach((entityId) => ids.add(entityId));
    }
  }
  return Array.from(ids);
}
const HomeiiPlayersFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  announcementEligiblePlayers,
  entityMatchTokens,
  favoriteButtonDeviceId,
  favoriteButtonEntityForPlayer,
  genericPlayerTokens,
  getBrowserPlayers,
  getThisDevicePlayer,
  groupAverageVolume,
  groupedPlayerIds,
  isLikelyBrowserPlayer,
  isMusicAssistantPlayer,
  isStaticGroupPlayer,
  mobileNavigableActivePlayers,
  playerByEntityId,
  playerGroupCount,
  playerGroupMemberIds,
  playerGroupMemberNames,
  resolvePinnedPlayerEntities,
  resolvePreferredFrontPlayerEntity
}, Symbol.toStringTag, { value: "Module" }));
function normalizeComparableText$1(value = "") {
  return String(value || "").trim().toLowerCase();
}
function normalizeFiniteNumber(value) {
  return value !== "" && value !== null && value !== void 0 ? Number(value) : NaN;
}
function queueItemMatchTitle(item = {}) {
  return normalizeComparableText$1(item?.media_item?.name || item?.media_title || item?.name || "");
}
function queueItemMatchArtist(item = {}) {
  return normalizeComparableText$1(
    item?.media_artist || (item?.media_item?.artists || []).map((artist) => artist?.name).filter(Boolean).join(", ") || ""
  );
}
function queueTitleArtistMatch(item, title = "", artist = "") {
  const normalizedTitle = normalizeComparableText$1(title);
  if (!item || !normalizedTitle) return false;
  const itemTitle = queueItemMatchTitle(item);
  const itemArtist = queueItemMatchArtist(item);
  const normalizedArtist = normalizeComparableText$1(artist);
  return itemTitle === normalizedTitle && (!normalizedArtist || itemArtist.includes(normalizedArtist) || normalizedArtist.includes(itemArtist));
}
function findQueueItemIndex(queueItems = [], {
  queueItemId = "",
  uri = "",
  sortIndex = "",
  fallbackType = "track"
} = {}, compareMediaRefs = mediaRefsEquivalent) {
  const items = Array.isArray(queueItems) ? queueItems : [];
  const normalizedIndex = normalizeFiniteNumber(sortIndex);
  const key = String(queueItemId || "").trim();
  const targetUri = String(uri || "").trim();
  const keyIndex = key ? items.findIndex(
    (item) => getQueueItemStableId(item) === key || getQueueItemKey(item) === key
  ) : -1;
  if (keyIndex >= 0) return keyIndex;
  const uriIndex = targetUri ? items.findIndex(
    (item) => compareMediaRefs(
      getQueueItemUri(item),
      targetUri,
      item?.media_item?.media_type || item?.media_type || fallbackType
    )
  ) : -1;
  if (uriIndex >= 0) return uriIndex;
  return items.findIndex((item) => Number.isFinite(normalizedIndex) && Number(item?.sort_index) === normalizedIndex);
}
function parseMediaReference(uri = "", fallbackType = "track") {
  const value = String(uri || "").trim();
  const fallback = {
    uri: value,
    media_type: String(fallbackType || "track").toLowerCase(),
    provider: "",
    item_id: "",
    media_id: value
  };
  if (!value.includes("://")) return fallback;
  const [providerRaw, restRaw] = value.split("://");
  const provider = String(providerRaw || "").trim();
  const rest = String(restRaw || "").replace(/^\/+/, "");
  const segments = rest.split("/").filter(Boolean);
  if (!segments.length) return { ...fallback, provider };
  const mediaType = String(segments[0] || fallback.media_type).toLowerCase();
  const itemId = segments.slice(1).join("/") || rest;
  return {
    uri: value,
    provider,
    media_type: mediaType || fallback.media_type,
    item_id: itemId,
    media_id: itemId || value
  };
}
function mediaRefsEquivalent(uriA = "", uriB = "", fallbackType = "track") {
  const left = String(uriA || "").trim();
  const right = String(uriB || "").trim();
  if (!left || !right) return false;
  if (left === right) return true;
  const leftRef = parseMediaReference(left, fallbackType);
  const rightRef = parseMediaReference(right, fallbackType);
  return !!(leftRef.provider && rightRef.provider && leftRef.provider === rightRef.provider && leftRef.media_type === rightRef.media_type && leftRef.item_id && leftRef.item_id === rightRef.item_id);
}
function getQueueItemKey(item) {
  const stableId = getQueueItemStableId(item);
  const sortIndex = normalizeFiniteNumber(item?.sort_index);
  return String(stableId || (Number.isFinite(sortIndex) ? sortIndex : "") || getQueueItemUri(item) || "");
}
function getQueueItemStableId(item) {
  const uri = String(getQueueItemUri(item) || "").trim();
  const parsed = parseMediaReference(uri, item?.media_item?.media_type || item?.media_type || "track");
  const trustedQueueId = [item?.queue_service_id].map((value) => String(value || "").trim()).find(Boolean);
  if (trustedQueueId) return trustedQueueId;
  const queueItemId = String(item?.queue_item_id || "").trim();
  if (queueItemId && item?.queue_item_id_trusted !== false) return queueItemId;
  const fallbackCandidates = [item?.item_id, item?.id].map((value) => String(value || "").trim()).filter(Boolean);
  return fallbackCandidates.find(
    (value) => value !== uri && value !== parsed.item_id && !uri.endsWith(`/${value}`) && !uri.endsWith(`:${value}`)
  ) || "";
}
function getQueueItemPlaybackId(item) {
  const value = getQueueItemStableId(item);
  if (!value || /^\d+$/.test(value)) return "";
  return value;
}
function getQueueItemUri(item) {
  return item?.media_item?.uri || item?.uri || item?.streamdetails?.uri || "";
}
function queueItemPrimaryArtist(item = {}) {
  return item?.media_artist || item?.artist || item?.media_item?.artist || (Array.isArray(item?.media_item?.artists) ? item.media_item.artists.map((artist) => artist?.name).filter(Boolean).join(", ") : "") || "";
}
function queueItemPrimaryTitle(item = {}) {
  return item?.media_item?.name || item?.media_title || item?.name || "";
}
function mobileCurrentQueueIndex(value) {
  const currentIndex = normalizeFiniteNumber(value);
  return Number.isFinite(currentIndex) ? currentIndex : -1;
}
function sortQueueItems(items = []) {
  return [...Array.isArray(items) ? items : []].filter(Boolean).sort((a, b) => Number(a?.sort_index ?? 0) - Number(b?.sort_index ?? 0));
}
function queueItemsContainCurrent(items = [], queueState = null) {
  if (!Array.isArray(items) || !items.length || !queueState) return false;
  const currentIndex = normalizeFiniteNumber(queueState.current_index);
  const currentKey = getQueueItemKey(queueState.current_item);
  return items.some(
    (item) => Number.isFinite(currentIndex) && Number(item?.sort_index) === currentIndex || currentKey && getQueueItemKey(item) === currentKey
  );
}
function queueItemMatchesPlayer(item, player, compareMediaRefs = mediaRefsEquivalent) {
  if (!item || !player) return false;
  const playerUri = String(player?.attributes?.media_content_id || "").trim();
  const itemUri = String(getQueueItemUri(item) || "").trim();
  if (playerUri && itemUri) {
    return compareMediaRefs(playerUri, itemUri, item?.media_item?.media_type || item?.media_type || "track");
  }
  const playerTitle = normalizeComparableText$1(player?.attributes?.media_title || "");
  if (!playerTitle) return false;
  const playerArtist = normalizeComparableText$1(player?.attributes?.media_artist || "");
  return queueTitleArtistMatch(item, playerTitle, playerArtist);
}
function resolveMobileUpNextItem(queueState = {}, queueItems = []) {
  if (queueState?.next_item) return queueState.next_item;
  const items = Array.isArray(queueItems) ? queueItems : [];
  if (!items.length) return null;
  const currentIndex = normalizeFiniteNumber(queueState.current_index);
  if (Number.isFinite(currentIndex)) {
    return items.find((item) => Number(item?.sort_index) === currentIndex + 1) || items.find((item) => Number(item?.sort_index) > currentIndex) || null;
  }
  const currentKey = getQueueItemKey(queueState.current_item);
  if (currentKey) {
    const index = items.findIndex((item) => getQueueItemKey(item) === currentKey);
    if (index >= 0 && index < items.length - 1) return items[index + 1];
  }
  return items[1] || null;
}
function entryTargetsCurrentMedia(entry = {}, current = {}, compareMediaRefs = mediaRefsEquivalent) {
  const currentUri = String(current?.uri || "").trim();
  const entryUri = String(entry?.uri || "").trim();
  if (currentUri && entryUri) {
    return compareMediaRefs(currentUri, entryUri, entry?.media_type || current?.media_type || "track");
  }
  const currentTitle = normalizeComparableText$1(current?.name || "");
  const entryTitle = normalizeComparableText$1(entry?.name || entry?.title || "");
  if (!currentTitle || !entryTitle || currentTitle !== entryTitle) return false;
  const currentArtist = normalizeComparableText$1(current?.artist || "");
  const entryArtist = normalizeComparableText$1(entry?.artist || "");
  return !currentArtist || !entryArtist || currentArtist === entryArtist || currentArtist.includes(entryArtist) || entryArtist.includes(currentArtist);
}
function resolveQueuePlayIndex(queueItems = [], {
  queueItemId = "",
  fallbackUri = "",
  explicitSortIndex = ""
} = {}, compareMediaRefs = mediaRefsEquivalent) {
  const normalizedExplicit = normalizeFiniteNumber(explicitSortIndex);
  const items = Array.isArray(queueItems) ? queueItems : [];
  const key = String(queueItemId || "").trim();
  const uri = String(fallbackUri || "").trim();
  const match = (key ? items.find(
    (item) => getQueueItemStableId(item) === key || getQueueItemKey(item) === key
  ) : null) || (uri ? items.find(
    (item) => compareMediaRefs(
      getQueueItemUri(item),
      uri,
      item?.media_item?.media_type || item?.media_type || "track"
    )
  ) : null);
  const resolved = Number(match?.sort_index);
  if (Number.isFinite(resolved)) return resolved;
  return Number.isFinite(normalizedExplicit) ? normalizedExplicit : null;
}
function getQueueItemByIndexOrKey(queueItems = [], {
  sortIndex = "",
  queueItemId = "",
  fallbackUri = ""
} = {}, compareMediaRefs = mediaRefsEquivalent) {
  const normalizedIndex = normalizeFiniteNumber(sortIndex);
  const key = String(queueItemId || "").trim();
  const uri = String(fallbackUri || "").trim();
  const items = Array.isArray(queueItems) ? queueItems : [];
  return (key ? items.find(
    (item) => getQueueItemStableId(item) === key || getQueueItemKey(item) === key
  ) : null) || (uri ? items.find(
    (item) => compareMediaRefs(
      getQueueItemUri(item),
      uri,
      item?.media_item?.media_type || item?.media_type || "track"
    )
  ) : null) || items.find((item) => Number.isFinite(normalizedIndex) && Number(item?.sort_index) === normalizedIndex) || null;
}
function resolveMobileArtStackContext({
  queueItems = [],
  currentIndexValue = "",
  player = null,
  currentItem = null,
  hasPendingPlay = false,
  pendingKey = "",
  pendingUri = "",
  pendingIndexValue = "",
  browseOffset = 0
} = {}, compareMediaRefs = mediaRefsEquivalent) {
  const sortedQueueItems = sortQueueItems(queueItems);
  const currentIndex = mobileCurrentQueueIndex(currentIndexValue);
  const playerUri = String(player?.attributes?.media_content_id || "").trim();
  const currentItemTitle = currentItem?.media_item?.name || currentItem?.media_title || currentItem?.name || "";
  const currentItemArtist = currentItem?.media_artist || (currentItem?.media_item?.artists || []).map((artist) => artist?.name).filter(Boolean).join(", ") || "";
  const playerTitle = normalizeComparableText$1(
    hasPendingPlay ? currentItemTitle || player?.attributes?.media_title || "" : player?.attributes?.media_title || currentItemTitle || ""
  );
  const playerArtist = normalizeComparableText$1(
    hasPendingPlay ? currentItemArtist || player?.attributes?.media_artist || "" : player?.attributes?.media_artist || currentItemArtist || ""
  );
  let baseIndex = -1;
  const titleMatches = (item) => queueTitleArtistMatch(item, playerTitle, playerArtist);
  let pendingResolved = false;
  if (hasPendingPlay) {
    const pendingMatch = findQueueItemIndex(sortedQueueItems, {
      queueItemId: pendingKey,
      uri: pendingUri,
      sortIndex: pendingIndexValue
    }, compareMediaRefs);
    if (pendingMatch >= 0) {
      baseIndex = pendingMatch;
      pendingResolved = true;
    }
  }
  if (currentItem && !pendingResolved) {
    const currentKey = getQueueItemKey(currentItem);
    const currentUri = getQueueItemUri(currentItem);
    const keyIndex = findQueueItemIndex(sortedQueueItems, {
      queueItemId: currentKey,
      uri: currentUri
    }, compareMediaRefs);
    if (keyIndex >= 0 && (hasPendingPlay || !playerTitle || titleMatches(sortedQueueItems[keyIndex]))) {
      baseIndex = keyIndex;
    }
  }
  if (baseIndex < 0 && playerUri) {
    baseIndex = findQueueItemIndex(sortedQueueItems, { uri: playerUri }, compareMediaRefs);
  }
  if (baseIndex < 0 && !playerUri && playerTitle) {
    baseIndex = sortedQueueItems.findIndex((item) => titleMatches(item));
  }
  if (baseIndex < 0 && Number.isFinite(currentIndex)) {
    baseIndex = sortedQueueItems.findIndex((item) => Number(item?.sort_index) === currentIndex);
  }
  if (baseIndex < 0 && Number.isFinite(currentIndex) && currentIndex >= 0 && currentIndex < sortedQueueItems.length) {
    baseIndex = currentIndex;
  }
  if (baseIndex < 0) baseIndex = 0;
  const minOffset = sortedQueueItems.length ? -baseIndex : 0;
  const maxOffset = sortedQueueItems.length ? sortedQueueItems.length - 1 - baseIndex : 0;
  const offset = Math.max(minOffset, Math.min(maxOffset, Number(browseOffset || 0)));
  const displayIndex = Math.max(0, Math.min(sortedQueueItems.length - 1, baseIndex + offset));
  return {
    queueItems: sortedQueueItems,
    baseIndex,
    displayIndex,
    offset
  };
}
const HomeiiMediaQueueFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  entryTargetsCurrentMedia,
  findQueueItemIndex,
  getQueueItemByIndexOrKey,
  getQueueItemKey,
  getQueueItemPlaybackId,
  getQueueItemStableId,
  getQueueItemUri,
  mediaRefsEquivalent,
  mobileCurrentQueueIndex,
  normalizeComparableText: normalizeComparableText$1,
  normalizeFiniteNumber,
  parseMediaReference,
  queueItemMatchArtist,
  queueItemMatchTitle,
  queueItemMatchesPlayer,
  queueItemPrimaryArtist,
  queueItemPrimaryTitle,
  queueItemsContainCurrent,
  queueTitleArtistMatch,
  resolveMobileArtStackContext,
  resolveMobileUpNextItem,
  resolveQueuePlayIndex,
  sortQueueItems
}, Symbol.toStringTag, { value: "Module" }));
const CURRENT_MEDIA_FAVORITE_OVERRIDE_WINDOW_MS = 8e3;
function normalizeComparableText(value = "") {
  return String(value || "").trim().toLowerCase();
}
function favoriteEntryArtist(entry = {}) {
  return String(
    entry?.artist || entry?.artists?.map?.((artist) => artist?.name).filter(Boolean).join(", ") || entry?.media_item?.artists?.map?.((artist) => artist?.name).filter(Boolean).join(", ") || entry?.media_artist || ""
  ).trim();
}
function favoriteEntryTitle(entry = {}) {
  return String(entry?.name || entry?.title || entry?.media_item?.name || "").trim();
}
function buildCurrentMediaLikeMeta({
  player = null,
  queueItem = {},
  resolvedUri = "",
  queueItemImage = "",
  fallbackName = "Unknown"
} = {}, parseMediaReferenceFn = parseMediaReference) {
  const media = queueItem?.media_item || {};
  const playerUri = String(player?.attributes?.media_content_id || "").trim();
  const uri = playerUri || String(resolvedUri || "").trim();
  const artist = Array.isArray(media?.artists) ? media.artists.map((artistEntry) => artistEntry?.name).filter(Boolean).join(", ") : player?.attributes?.media_artist || "";
  const parsed = parseMediaReferenceFn(
    uri,
    media?.media_type || queueItem?.media_type || player?.attributes?.media_content_type || "track"
  );
  return {
    uri,
    ...parsed,
    media_type: parsed.media_type || media?.media_type || queueItem?.media_type || "track",
    name: player?.attributes?.media_title || media?.name || uri || fallbackName,
    artist: artist || "",
    album: media?.album?.name || player?.attributes?.media_album_name || "",
    image: queueItemImage || media?.image || media?.album?.image || player?.attributes?.entity_picture_local || player?.attributes?.entity_picture || ""
  };
}
function favoriteRemoveArgsFromItem(item = {}, fallbackType = "track", parseMediaReferenceFn = parseMediaReference) {
  if (!item || typeof item !== "object") return null;
  const mediaType = String(item.media_type || item.type || item.media_item?.media_type || fallbackType || "track").toLowerCase();
  const parsed = parseMediaReferenceFn(item.uri || item.media_item?.uri || "", mediaType);
  const explicitLibraryId = item.library_item_id || (String(item.provider || item.provider_domain || item.provider_instance || "").toLowerCase() === "library" ? item.item_id || item.id || "" : "");
  const libraryItemId = String(parsed.provider === "library" ? parsed.item_id : explicitLibraryId || "").trim();
  if (!libraryItemId) return null;
  return { media_type: parsed.media_type || mediaType, library_item_id: libraryItemId };
}
function findFavoriteEntryMatch(entry = {}, likedItems = [], {
  parseMediaReferenceFn = parseMediaReference
} = {}) {
  if (!Array.isArray(likedItems) || !likedItems.length) return null;
  const targetUri = String(entry?.uri || "").trim();
  const targetType = String(entry?.media_type || entry?.type || "track").toLowerCase();
  const targetLibraryId = String(entry?.library_item_id || "").trim();
  const targetItemId = String(entry?.item_id || entry?.id || "").trim();
  const targetProvider = String(entry?.provider || entry?.provider_domain || entry?.provider_instance || "").trim().toLowerCase();
  const targetTitle = normalizeComparableText(favoriteEntryTitle(entry));
  const targetArtist = normalizeComparableText(favoriteEntryArtist(entry));
  for (const item of likedItems) {
    const candidateLibraryId = String(item?.library_item_id || "").trim();
    const candidateItemId = String(item?.item_id || item?.id || "").trim();
    const candidateProvider = String(item?.provider || item?.provider_domain || item?.provider_instance || "").trim().toLowerCase();
    const candidateUri = String(item?.uri || "").trim();
    const candidateType = String(item?.media_type || item?.type || "").toLowerCase();
    if (targetLibraryId && candidateLibraryId && targetLibraryId === candidateLibraryId) {
      return item;
    }
    if (targetItemId && candidateItemId && targetProvider && candidateProvider && targetItemId === candidateItemId && targetProvider === candidateProvider) {
      return item;
    }
    if (candidateUri && targetUri && candidateUri === targetUri) {
      return item;
    }
    const parsedTarget = parseMediaReferenceFn(targetUri, targetType || "track");
    const parsedCandidate = parseMediaReferenceFn(candidateUri, candidateType || targetType || "track");
    if (parsedTarget.provider && parsedCandidate.provider && parsedTarget.provider === parsedCandidate.provider && parsedTarget.item_id && parsedTarget.item_id === parsedCandidate.item_id) {
      return item;
    }
    const candidateTitle = normalizeComparableText(favoriteEntryTitle(item));
    const candidateArtist = normalizeComparableText(favoriteEntryArtist(item));
    if (targetTitle && candidateTitle === targetTitle && (!targetArtist || !candidateArtist || candidateArtist === targetArtist || candidateArtist.includes(targetArtist) || targetArtist.includes(candidateArtist))) {
      return item;
    }
  }
  return null;
}
function matchFavoriteLibraryItem(entry = {}, likedItems = [], fallbackType = "", {
  parseMediaReferenceFn = parseMediaReference,
  favoriteRemoveArgsFromItemFn = favoriteRemoveArgsFromItem,
  findFavoriteEntryMatchFn = findFavoriteEntryMatch
} = {}) {
  const effectiveFallbackType = fallbackType || entry?.media_type || entry?.type || "track";
  const match = findFavoriteEntryMatchFn(entry, likedItems, { parseMediaReferenceFn });
  return match ? favoriteRemoveArgsFromItemFn(match, effectiveFallbackType, parseMediaReferenceFn) : null;
}
function buildOptimisticFavoriteEntry(entry = {}, liked = false) {
  return {
    uri: String(entry?.uri || "").trim(),
    media_type: entry?.media_type || entry?.type || "track",
    item_id: entry?.item_id || "",
    provider: entry?.provider || "",
    library_item_id: entry?.library_item_id || "",
    name: entry?.name || entry?.title || entry?.media_item?.name || "",
    artist: favoriteEntryArtist(entry),
    album: entry?.album || entry?.media_item?.album?.name || "",
    image: entry?.image || entry?.media_item?.image || entry?.media_item?.album?.image || "",
    favorite: !!liked
  };
}
function applyOptimisticFavoriteCache(likedItems = [], nextEntry = {}, liked = false, {
  compareMediaRefsFn = mediaRefsEquivalent,
  matchFavoriteLibraryItemFn = matchFavoriteLibraryItem
} = {}) {
  const items = Array.isArray(likedItems) ? [...likedItems] : [];
  const idx = items.findIndex(
    (item) => compareMediaRefsFn(String(item?.uri || "").trim(), nextEntry.uri, item?.media_type || nextEntry.media_type || "track") || !!matchFavoriteLibraryItemFn(nextEntry, [item], nextEntry.media_type || "track")
  );
  if (liked) {
    if (idx >= 0) items[idx] = { ...items[idx], ...nextEntry, favorite: true };
    else items.unshift(nextEntry);
  } else if (idx >= 0) {
    items.splice(idx, 1);
  }
  return items;
}
function isEntryLiked(entry = {}, {
  useMaLikedMode = false,
  likedItems = null,
  localLikedUris = null
} = {}, {
  compareMediaRefsFn = mediaRefsEquivalent,
  matchFavoriteLibraryItemFn = matchFavoriteLibraryItem
} = {}) {
  const uri = String(entry?.uri || "").trim();
  if (useMaLikedMode) {
    if (Array.isArray(likedItems)) {
      return uri ? likedItems.some((item) => compareMediaRefsFn(String(item?.uri || "").trim(), uri, item?.media_type || entry?.media_type || "track")) || !!matchFavoriteLibraryItemFn(entry, likedItems, entry?.media_type || entry?.type || "track") : !!matchFavoriteLibraryItemFn(entry, likedItems, entry?.media_type || entry?.type || "track");
    }
    return !!entry?.favorite || !!entry?.media_item?.favorite;
  }
  if (entry?.favorite || entry?.media_item?.favorite) return true;
  if (!uri) return false;
  if (localLikedUris instanceof Set) return localLikedUris.has(uri);
  if (Array.isArray(localLikedUris)) return localLikedUris.includes(uri);
  return false;
}
function resolveCurrentMediaFavoriteState({
  currentUri = "",
  override = null,
  queueItem = {},
  currentEntry = null,
  useMaLikedMode = false,
  likedItems = null,
  localLikedUris = null,
  now = Date.now()
} = {}, {
  compareMediaRefsFn = mediaRefsEquivalent,
  matchFavoriteLibraryItemFn = matchFavoriteLibraryItem
} = {}) {
  const uri = String(currentUri || "").trim();
  if (!uri) return false;
  if (override?.uri === uri && Number(now || 0) - Number(override?.ts || 0) < CURRENT_MEDIA_FAVORITE_OVERRIDE_WINDOW_MS) {
    return !!override.liked;
  }
  const queueFavorite = queueItem?.media_item?.favorite;
  if (typeof queueFavorite === "boolean") return queueFavorite;
  if (typeof queueItem?.favorite === "boolean") return queueItem.favorite;
  if (useMaLikedMode) {
    if (Array.isArray(likedItems)) {
      return likedItems.some((item) => compareMediaRefsFn(String(item?.uri || "").trim(), uri, item?.media_type || "track")) || !!matchFavoriteLibraryItemFn(currentEntry || {}, likedItems, currentEntry?.media_type || "track");
    }
    return false;
  }
  if (localLikedUris instanceof Set) return localLikedUris.has(uri);
  if (Array.isArray(localLikedUris)) return localLikedUris.includes(uri);
  return false;
}
function resolveCachedFavoriteRemoveArgs({
  entry = {},
  mediaType = "track",
  likedItems = [],
  currentEntry = null,
  currentEntryMatches = false
} = {}, {
  parseMediaReferenceFn = parseMediaReference,
  favoriteRemoveArgsFromItemFn = favoriteRemoveArgsFromItem,
  findFavoriteEntryMatchFn = findFavoriteEntryMatch,
  matchFavoriteLibraryItemFn = matchFavoriteLibraryItem
} = {}) {
  const uri = String(entry?.uri || "").trim();
  const ref = parseMediaReferenceFn(uri, mediaType);
  if (ref.provider === "library" && ref.item_id) {
    return { media_type: ref.media_type || mediaType, library_item_id: ref.item_id };
  }
  const canonicalEntry = findFavoriteEntryMatchFn(entry, likedItems, { parseMediaReferenceFn });
  if (canonicalEntry) {
    const canonicalArgs = favoriteRemoveArgsFromItemFn(canonicalEntry, mediaType, parseMediaReferenceFn);
    if (canonicalArgs) return canonicalArgs;
  }
  const resolvedFromCache = matchFavoriteLibraryItemFn(entry, likedItems, mediaType, {
    parseMediaReferenceFn,
    favoriteRemoveArgsFromItemFn,
    findFavoriteEntryMatchFn
  });
  if (resolvedFromCache) return resolvedFromCache;
  if (currentEntryMatches && currentEntry?.uri && currentEntry.uri !== uri) {
    return matchFavoriteLibraryItemFn(currentEntry, likedItems, mediaType, {
      parseMediaReferenceFn,
      favoriteRemoveArgsFromItemFn,
      findFavoriteEntryMatchFn
    });
  }
  return null;
}
const HomeiiFavoritesFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  applyOptimisticFavoriteCache,
  buildCurrentMediaLikeMeta,
  buildOptimisticFavoriteEntry,
  favoriteEntryArtist,
  favoriteEntryTitle,
  favoriteRemoveArgsFromItem,
  findFavoriteEntryMatch,
  isEntryLiked,
  matchFavoriteLibraryItem,
  normalizeComparableText,
  resolveCachedFavoriteRemoveArgs,
  resolveCurrentMediaFavoriteState
}, Symbol.toStringTag, { value: "Module" }));
function queueItemArtworkCacheKey(item = null, accessors = {}) {
  const getQueueItemPlaybackId2 = accessors.getQueueItemPlaybackId || (() => "");
  const getQueueItemStableId2 = accessors.getQueueItemStableId || (() => "");
  const getQueueItemKey2 = accessors.getQueueItemKey || (() => "");
  const getQueueItemUri2 = accessors.getQueueItemUri || (() => "");
  return [
    getQueueItemPlaybackId2(item),
    getQueueItemStableId2(item),
    getQueueItemKey2(item),
    getQueueItemUri2(item),
    item?.media_item?.name,
    item?.media_item?.album?.name,
    item?.streamdetails?.stream_metadata?.image_url,
    item?.media_image,
    item?.media_image_url,
    item?.image,
    item?.image_url,
    item?.media_item?.image,
    item?.media_item?.image_url,
    item?.media_item?.album?.image,
    item?.media_item?.album?.image_url
  ].map((value) => String(value || "").trim()).filter(Boolean).join("|");
}
function queueArtworkPrefetchIndexes(items = [], options = {}) {
  const queueItems = Array.isArray(items) ? items.filter(Boolean) : [];
  if (!queueItems.length) return [];
  const currentIndex = Number(options.currentIndex);
  let centerIndex = Number.isFinite(currentIndex) ? queueItems.findIndex((item) => Number(item?.sort_index) === currentIndex) : -1;
  const isCurrentItemFn = typeof options.isCurrentItemFn === "function" ? options.isCurrentItemFn : null;
  if (centerIndex < 0 && isCurrentItemFn) centerIndex = queueItems.findIndex((item) => isCurrentItemFn(item));
  if (centerIndex < 0) centerIndex = 0;
  const before = Math.max(0, Math.round(Number(options.before ?? 2)));
  const after = Math.max(0, Math.round(Number(options.after ?? 14)));
  const visibleStartIndex = Math.max(0, Math.round(Number(options.visibleStartIndex || 0)));
  const visibleCount = Math.max(0, Math.round(Number(options.visibleCount || 0)));
  const indexes = [];
  const seenIndexes = /* @__PURE__ */ new Set();
  const addIndex = (index) => {
    if (index < 0 || index >= queueItems.length || seenIndexes.has(index)) return;
    seenIndexes.add(index);
    indexes.push(index);
  };
  addIndex(centerIndex);
  const maxDistance = Math.max(before, after);
  for (let distance = 1; distance <= maxDistance; distance += 1) {
    if (distance <= after) addIndex(centerIndex + distance);
    if (distance <= before) addIndex(centerIndex - distance);
  }
  for (let index = visibleStartIndex; index < Math.min(queueItems.length, visibleStartIndex + visibleCount); index += 1) {
    addIndex(index);
  }
  return indexes;
}
function queueArtworkPrefetchUrls(items = [], options = {}, artworkUrlFn = () => "") {
  const queueItems = Array.isArray(items) ? items.filter(Boolean) : [];
  const indexes = queueArtworkPrefetchIndexes(queueItems, options);
  const centerIndex = indexes[0] ?? 0;
  const urls = [];
  indexes.forEach((index) => {
    const item = queueItems[index];
    const distance = Math.abs(index - centerIndex);
    const thumb = artworkUrlFn(item, 160);
    if (thumb) urls.push(thumb);
    if (distance <= 3) {
      const large = artworkUrlFn(item, 420);
      if (large) urls.push(large);
    }
  });
  return urls.filter((url, index, list) => url && list.indexOf(url) === index);
}
const HomeiiArtworkFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  queueArtworkPrefetchIndexes,
  queueArtworkPrefetchUrls,
  queueItemArtworkCacheKey
}, Symbol.toStringTag, { value: "Module" }));
function queueItemIdentityAccessors(accessors = {}) {
  return {
    getQueueItemPlaybackId: accessors.getQueueItemPlaybackId || (() => ""),
    getQueueItemStableId: accessors.getQueueItemStableId || (() => ""),
    getQueueItemKey: accessors.getQueueItemKey || (() => ""),
    getQueueItemUri: accessors.getQueueItemUri || (() => "")
  };
}
function pendingQueuePlayState(state = {}, now = Date.now()) {
  const pendingUntil = Number(state?.mobileQueuePlayPendingUntil || 0);
  const pendingIndex = normalizeFiniteNumber(state?.mobileQueuePlayPendingIndex);
  return {
    hasPendingPlay: pendingUntil > Number(now || 0),
    pendingUntil,
    pendingKey: String(state?.mobileQueuePlayPendingKey || "").trim(),
    pendingUri: String(state?.mobileQueuePlayPendingUri || "").trim(),
    pendingIndex,
    pendingPlayerId: String(state?.mobileQueuePlayPendingPlayerId || "").trim()
  };
}
function hasPendingQueuePlay(state = {}, now = Date.now()) {
  return pendingQueuePlayState(state, now).hasPendingPlay;
}
function buildPendingQueuePlayPatch({
  item = null,
  playIndex = null,
  selectedPlayerId = "",
  now = Date.now(),
  durationMs = 8500,
  accessors = {}
} = {}) {
  const normalizedIndex = normalizeFiniteNumber(playIndex);
  if (!item && !Number.isFinite(normalizedIndex)) return null;
  const {
    getQueueItemPlaybackId: getQueueItemPlaybackId2,
    getQueueItemStableId: getQueueItemStableId2,
    getQueueItemKey: getQueueItemKey2,
    getQueueItemUri: getQueueItemUri2
  } = queueItemIdentityAccessors(accessors);
  return {
    mobileQueuePlayPendingUntil: Number(now || Date.now()) + Math.max(1, Number(durationMs || 8500)),
    mobileQueuePlayPendingKey: item ? getQueueItemPlaybackId2(item) || getQueueItemStableId2(item) || getQueueItemKey2(item) || getQueueItemUri2(item) || "" : "",
    mobileQueuePlayPendingIndex: Number.isFinite(normalizedIndex) ? normalizedIndex : null,
    mobileQueuePlayPendingUri: item ? getQueueItemUri2(item) || "" : "",
    mobileQueuePlayPendingPlayerId: String(selectedPlayerId || "").trim()
  };
}
function clearPendingQueuePlayPatch() {
  return {
    mobileQueuePlayPendingUntil: 0,
    mobileQueuePlayPendingKey: "",
    mobileQueuePlayPendingIndex: null,
    mobileQueuePlayPendingUri: "",
    mobileQueuePlayPendingPlayerId: ""
  };
}
function queueItemMatchesPendingPlay(item = null, pendingState = {}, accessors = {}, compareMediaRefs = mediaRefsEquivalent) {
  if (!item) return false;
  const pending = Object.prototype.hasOwnProperty.call(pendingState || {}, "hasPendingPlay") ? pendingState : pendingQueuePlayState(pendingState);
  if (!pending?.hasPendingPlay) return false;
  const {
    getQueueItemPlaybackId: getQueueItemPlaybackId2,
    getQueueItemStableId: getQueueItemStableId2,
    getQueueItemKey: getQueueItemKey2,
    getQueueItemUri: getQueueItemUri2
  } = queueItemIdentityAccessors(accessors);
  const pendingKey = String(pending.pendingKey || "").trim();
  const pendingUri = String(pending.pendingUri || "").trim();
  const pendingIndex = normalizeFiniteNumber(pending.pendingIndex);
  const itemIds = [
    getQueueItemPlaybackId2(item),
    getQueueItemStableId2(item),
    getQueueItemKey2(item),
    getQueueItemUri2(item)
  ].map((value) => String(value || "").trim()).filter(Boolean);
  if (pendingKey && itemIds.includes(pendingKey)) return true;
  const itemUri = getQueueItemUri2(item);
  if (pendingUri && itemUri && compareMediaRefs(itemUri, pendingUri, item?.media_item?.media_type || item?.media_type || "track")) {
    return true;
  }
  return Number.isFinite(pendingIndex) && Number(item?.sort_index) === pendingIndex;
}
function resolvePendingQueueItem({
  state = {},
  pendingState = null,
  currentQueueItem = null,
  stack = null,
  queueItems = [],
  accessors = {},
  compareMediaRefs = mediaRefsEquivalent
} = {}) {
  const pending = pendingState || pendingQueuePlayState(state);
  if (!pending?.hasPendingPlay) return null;
  const candidates = [
    state?.maQueueState?.current_item,
    currentQueueItem,
    stack?.current,
    ...Array.isArray(queueItems) ? queueItems : []
  ].filter(Boolean);
  return candidates.find(
    (item) => queueItemMatchesPendingPlay(item, pending, accessors, compareMediaRefs)
  ) || state?.maQueueState?.current_item || currentQueueItem || stack?.current || null;
}
function nowPlayingDisplaySource({
  player = null,
  currentQueueItem = null,
  stack = null,
  queueItems = [],
  state = {},
  pendingState = null,
  fallbackTitle = "",
  accessors = {},
  compareMediaRefs = mediaRefsEquivalent,
  artworkUrlFn = null,
  size = 420
} = {}) {
  const pending = pendingState || pendingQueuePlayState(state);
  const hasPendingPlay = !!pending?.hasPendingPlay;
  const queueItem = hasPendingPlay ? resolvePendingQueueItem({
    state,
    pendingState: pending,
    currentQueueItem,
    stack,
    queueItems,
    accessors,
    compareMediaRefs
  }) || currentQueueItem : currentQueueItem;
  const media = queueItem?.media_item || {};
  const queueArtists = Array.isArray(media?.artists) ? media.artists.map((artist2) => artist2?.name).filter(Boolean).join(", ") : "";
  const queueTitle = media?.name || queueItem?.media_title || queueItem?.name || "";
  const queueArtist = queueItem?.media_artist || queueItem?.artist_str || queueArtists || queueItem?.artist || "";
  const queueAlbum = media?.album?.name || queueItem?.media_album_name || queueItem?.album || "";
  const preferQueueText = hasPendingPlay || !player?.attributes?.media_title;
  const title = preferQueueText ? queueTitle || player?.attributes?.media_title || fallbackTitle : player?.attributes?.media_title || queueTitle || fallbackTitle;
  const artist = preferQueueText ? queueArtist || player?.attributes?.media_artist || "" : player?.attributes?.media_artist || queueArtist || "";
  const album = preferQueueText ? queueAlbum || player?.attributes?.media_album_name || "" : player?.attributes?.media_album_name || queueAlbum || "";
  const mediaType = String(
    preferQueueText ? media?.media_type || queueItem?.media_type || player?.attributes?.media_content_type || player?.attributes?.media_channel || "" : player?.attributes?.media_content_type || player?.attributes?.media_channel || media?.media_type || queueItem?.media_type || ""
  ).toLowerCase();
  return {
    hasPendingPlay,
    queueItem,
    media,
    title,
    artist,
    album,
    mediaType,
    art: typeof artworkUrlFn === "function" ? artworkUrlFn(player, queueItem, { pending: hasPendingPlay, size }) : "",
    uri: String(queueItem ? queueItemIdentityAccessors(accessors).getQueueItemUri(queueItem) || "" : "").trim()
  };
}
function pendingPlayerLockState(state = {}, players = [], now = Date.now()) {
  const pending = pendingQueuePlayState(state, now);
  const entityIds = new Set((Array.isArray(players) ? players : []).map((player) => String(player?.entity_id || "").trim()).filter(Boolean));
  const selectedPlayerId = String(state?.selectedPlayer || "").trim();
  const selectedPlayerExists = !!selectedPlayerId && entityIds.has(selectedPlayerId);
  const pendingPlayerExists = !!pending.pendingPlayerId && entityIds.has(pending.pendingPlayerId);
  return {
    ...pending,
    lockedPlayerId: pending.hasPendingPlay && pendingPlayerExists ? pending.pendingPlayerId : "",
    shouldHoldSelectedPlayer: pending.hasPendingPlay && (pendingPlayerExists || selectedPlayerExists)
  };
}
const HomeiiNowPlayingFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  buildPendingQueuePlayPatch,
  clearPendingQueuePlayPatch,
  hasPendingQueuePlay,
  nowPlayingDisplaySource,
  pendingPlayerLockState,
  pendingQueuePlayState,
  queueItemMatchesPendingPlay,
  resolvePendingQueueItem
}, Symbol.toStringTag, { value: "Module" }));
function buildCurrentTrackInfo({ player = null, queueItem = null } = {}) {
  const currentQueueItem = queueItem || null;
  const media = currentQueueItem?.media_item || {};
  const title = media?.name || player?.attributes?.media_title || "";
  const artists = Array.isArray(media?.artists) ? media.artists.map((artist2) => artist2?.name).filter(Boolean).join(", ") : "";
  const artist = artists || player?.attributes?.media_artist || "";
  const album = media?.album?.name || player?.attributes?.media_album_name || "";
  const duration = coercePlaybackSeconds(currentQueueItem?.duration || player?.attributes?.media_duration || 0);
  const key = [title, artist, album].map((part) => String(part || "").trim().toLowerCase()).join("|");
  return { title, artist, album, duration, key };
}
function sourceProviderMeta(value = "", { libraryLabel = "Library", radioLabel = "Radio" } = {}) {
  const raw = String(value || "").trim();
  const normalized = raw.toLowerCase();
  if (!normalized) return null;
  if (/(spotify)/.test(normalized)) return { key: "spotify", label: "Spotify" };
  if (/(tidal)/.test(normalized)) return { key: "tidal", label: "TIDAL" };
  if (/(youtube|ytmusic)/.test(normalized)) return { key: "youtube", label: "YouTube Music" };
  if (/(apple)/.test(normalized)) return { key: "apple", label: "Apple Music" };
  if (/(qobuz)/.test(normalized)) return { key: "qobuz", label: "Qobuz" };
  if (/(deezer)/.test(normalized)) return { key: "deezer", label: "Deezer" };
  if (/(filesystem|local|library|file)/.test(normalized)) return { key: "library", label: libraryLabel };
  if (/(radio_browser|radiobrowser|tunein|radio)/.test(normalized)) return { key: "radio", label: radioLabel };
  return {
    key: normalized.replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 24) || "source",
    label: raw
  };
}
function qualityBadgeLabel(values = []) {
  const haystack = (Array.isArray(values) ? values : [values]).flatMap((value) => Array.isArray(value) ? value : [value]).map((value) => {
    if (typeof value !== "object" || value === null) return String(value || "");
    try {
      return JSON.stringify(value);
    } catch (_) {
      return String(value || "");
    }
  }).join(" | ").toLowerCase();
  if (!haystack) return "";
  if (/(hi[\s-]?res|24[\s-]?bit|88\.2|96[\s-]?khz|96000|176\.4|192[\s-]?khz|192000)/.test(haystack)) return "Hi-Res";
  if (/(lossless|flac|alac|wav|aiff|pcm)/.test(haystack)) return "Lossless";
  return "";
}
function stripLyricsTimestamps(text = "") {
  return String(text || "").replace(/\r/g, "").replace(/^\[[a-z]+:[^\]]*\]\s*$/gim, "").replace(/\[\d{1,3}:\d{2}(?:[.:]\d{1,3})?\]\s*/g, "").trim();
}
function coerceLyricsRawText(value) {
  if (!value) return "";
  if (typeof value === "string") return String(value || "").replace(/\r/g, "").trim();
  if (typeof value === "object") {
    return String(
      value.syncedLyrics || value.synced_lyrics || value.plainLyrics || value.plain_lyrics || value.lyrics || value.text || ""
    ).replace(/\r/g, "").trim();
  }
  return "";
}
function coerceLyricsText(value) {
  return stripLyricsTimestamps(coerceLyricsRawText(value));
}
function parseLrcLyrics(text = "") {
  const rows = [];
  const raw = String(text || "").replace(/\r/g, "");
  const timeTag = /\[(\d{1,3}):(\d{2})(?:[.:](\d{1,3}))?\]/g;
  raw.split("\n").forEach((line) => {
    const tags = [...line.matchAll(timeTag)];
    if (!tags.length) return;
    const lyric = line.replace(/\[[^\]]+\]/g, "").trim();
    if (!lyric) return;
    tags.forEach((tag) => {
      const minutes = Number(tag[1]);
      const seconds = Number(tag[2]);
      const fraction = String(tag[3] || "0");
      const millis = Number(fraction.padEnd(3, "0").slice(0, 3));
      const time = minutes * 60 + seconds + millis / 1e3;
      if (Number.isFinite(time)) rows.push({ time, text: lyric });
    });
  });
  return rows.sort((left, right) => left.time - right.time).filter((row, index, list) => index === 0 || row.time !== list[index - 1].time || row.text !== list[index - 1].text);
}
function extractCurrentLyricsRawText(queueItem = null) {
  const currentQueueItem = queueItem || {};
  const media = currentQueueItem.media_item || {};
  const metadata = media.metadata || currentQueueItem.metadata || {};
  const candidates = [
    currentQueueItem.lyrics,
    currentQueueItem.plainLyrics,
    currentQueueItem.plain_lyrics,
    currentQueueItem.syncedLyrics,
    currentQueueItem.synced_lyrics,
    media.lyrics,
    media.plainLyrics,
    media.plain_lyrics,
    media.syncedLyrics,
    media.synced_lyrics,
    metadata.lyrics,
    metadata.plainLyrics,
    metadata.plain_lyrics,
    metadata.syncedLyrics,
    metadata.synced_lyrics
  ];
  for (const candidate of candidates) {
    const text = coerceLyricsRawText(candidate);
    if (text) return text;
  }
  return "";
}
function extractCurrentLyricsText(queueItem = null) {
  return stripLyricsTimestamps(extractCurrentLyricsRawText(queueItem));
}
function imageProxyUrl(path, provider = "", size = 300, maUrl = "") {
  const raw = String(path || "").trim();
  if (!raw) return null;
  if (/^(data:|blob:)/i.test(raw)) return raw;
  if (raw.includes("/imageproxy?") || raw.includes("/imageproxy/")) return normalizeImageProxyUrl(raw, size, maUrl);
  if (raw.startsWith("imageproxy?") || raw.startsWith("imageproxy/")) return normalizeImageProxyUrl(`/${raw}`, size, maUrl);
  if (raw.startsWith("/") && !/^\/\//.test(raw)) return raw;
  if (!maUrl) return /^https?:/i.test(raw) ? raw : null;
  const providerKey = provider || "";
  const baseUrl = String(maUrl || "").replace(/\/$/, "");
  return `${baseUrl}/imageproxy?path=${encodeURIComponent(raw)}${providerKey ? `&provider=${encodeURIComponent(providerKey)}` : ""}&size=${normalizeImageProxySize(size)}`;
}
function normalizeImageProxySize(size = 300) {
  const numeric = Number(size);
  if (Number.isFinite(numeric) && numeric <= 0) return 0;
  const requested = Number.isFinite(numeric) ? Math.ceil(numeric) : 300;
  for (const allowed of [80, 160, 256, 512, 1024]) {
    if (requested <= allowed) return allowed;
  }
  return 1024;
}
function imageProxyIdUrl(proxyId = "", size = 300, maUrl = "", format = "jpeg") {
  const raw = String(proxyId || "").trim();
  if (!raw || !maUrl) return null;
  if (!/^[0-9a-f]{64}$/i.test(raw)) return null;
  const baseUrl = String(maUrl || "").replace(/\/$/, "");
  const normalizedId = raw.toLowerCase();
  const normalizedFormat = String(format || "jpeg").trim().toLowerCase() || "jpeg";
  return `${baseUrl}/imageproxy/${encodeURIComponent(normalizedId)}?size=${normalizeImageProxySize(size)}&fmt=${encodeURIComponent(normalizedFormat)}`;
}
function imageDataUrlFromEncoded(value = "") {
  const raw = String(value || "").trim();
  if (!raw || raw.length < 80 || raw.includes("://") || /\s/.test(raw)) return "";
  if (raw.startsWith("/") && !raw.startsWith("/9j/")) return "";
  if (!/^[A-Za-z0-9+/]+={0,2}$/.test(raw)) return "";
  let mime = "image/jpeg";
  if (raw.startsWith("iVBOR")) mime = "image/png";
  else if (raw.startsWith("R0lG")) mime = "image/gif";
  else if (raw.startsWith("UklGR")) mime = "image/webp";
  return `data:${mime};base64,${raw}`;
}
function normalizeImageProxyUrl(value = "", size = 300, maUrl = "") {
  const raw = String(value || "").trim();
  if (!raw || /^(data:|blob:)/i.test(raw)) return raw;
  const isAbsolute = /^https?:\/\//i.test(raw);
  const baseUrl = maUrl || "http://homeii.local";
  try {
    const parsed = new URL(raw, baseUrl);
    if (!parsed.pathname.includes("/imageproxy")) return raw;
    parsed.searchParams.set("size", String(normalizeImageProxySize(size)));
    if (/\/imageproxy\/[^/]+/.test(parsed.pathname) && !parsed.searchParams.has("fmt")) {
      parsed.searchParams.set("fmt", "jpeg");
    }
    if (isAbsolute || maUrl) return parsed.toString();
    return `${parsed.pathname}${parsed.search}${parsed.hash}`;
  } catch {
    return raw;
  }
}
function imageUrl(value, size = 300, { maUrl = "", seen = /* @__PURE__ */ new Set(), depth = 0 } = {}) {
  if (!value || depth > 5) return null;
  if (typeof value === "string") {
    const raw = String(value).trim();
    if (!raw) return null;
    if (/^(data:|blob:)/i.test(raw)) return raw;
    const encodedImage = imageDataUrlFromEncoded(raw);
    if (encodedImage) return encodedImage;
    if (raw.includes("/imageproxy?") || raw.includes("/imageproxy/")) return normalizeImageProxyUrl(raw, size, maUrl);
    if (raw.startsWith("imageproxy?") || raw.startsWith("imageproxy/")) return normalizeImageProxyUrl(`/${raw}`, size, maUrl);
    if (raw.startsWith("/")) return raw;
    return imageProxyUrl(raw, "", size, maUrl);
  }
  if (typeof value !== "object") return null;
  if (seen.has(value)) return null;
  seen.add(value);
  if (Array.isArray(value)) {
    for (const entry of value) {
      const resolved = imageUrl(entry, size, { maUrl, seen, depth: depth + 1 });
      if (resolved) return resolved;
    }
    return null;
  }
  if (value.url) return imageUrl(value.url, size, { maUrl, seen, depth: depth + 1 });
  const proxyId = value.proxy_id || value.proxyId || value.image_id || value.imageId || value.image_proxy_id || value.imageProxyId;
  const proxyResolved = proxyId ? imageProxyIdUrl(proxyId, size, maUrl, value.format || value.fmt || "jpeg") : null;
  if (proxyResolved) return proxyResolved;
  const rawPath = value.path || value.image_path || value.imagePath || value.thumb_path || value.thumbnail_path || value.cover_path || value.coverPath;
  if (rawPath) {
    return imageProxyUrl(
      rawPath,
      value.provider || value.provider_id || value.provider_instance || value.provider_domain || value.provider_name || "",
      size,
      maUrl
    );
  }
  const priorityKeys = [
    "image",
    "images",
    "image_url",
    "imageUrl",
    "media_image",
    "media_image_url",
    "local_image",
    "local_image_url",
    "local_image_encoded",
    "preview_image",
    "preview_image_url",
    "external_image_url",
    "thumb",
    "thumbnail",
    "small",
    "medium",
    "large",
    "cover",
    "cover_image",
    "artwork",
    "picture",
    "fanart",
    "square",
    "album",
    "media_item",
    "metadata"
  ];
  for (const key of priorityKeys) {
    const resolved = imageUrl(value[key], size, { maUrl, seen, depth: depth + 1 });
    if (resolved) return resolved;
  }
  for (const entry of Object.values(value)) {
    if (!entry || typeof entry !== "object") continue;
    const resolved = imageUrl(entry, size, { maUrl, seen, depth: depth + 1 });
    if (resolved) return resolved;
  }
  return null;
}
function artUrl(item = null, maUrl = "", size = 300) {
  return imageUrl(item?.image_url, size, { maUrl }) || imageUrl(item?.image, size, { maUrl }) || imageUrl(item?.thumbnail, size, { maUrl }) || imageUrl(item?.thumb, size, { maUrl }) || imageUrl(item?.cover, size, { maUrl }) || imageUrl(item?.media_image, size, { maUrl }) || imageUrl(item?.media_item?.image_url, size, { maUrl }) || imageUrl(item?.media_item?.image, size, { maUrl }) || imageUrl(item?.media_item?.metadata?.images, size, { maUrl }) || imageUrl(item?.album?.image_url, size, { maUrl }) || imageUrl(item?.album?.image, size, { maUrl }) || imageUrl(item?.metadata?.images, size, { maUrl }) || imageUrl(item?.album?.metadata?.images, size, { maUrl }) || imageUrl(item, size, { maUrl }) || null;
}
function artistName(item = null) {
  return Array.isArray(item?.artists) ? item.artists.map((artist) => artist?.name).join(", ") : "";
}
function coercePlaybackSeconds(value, { allowMilliseconds = true } = {}) {
  if (value === void 0 || value === null || value === "") return 0;
  if (typeof value === "string") {
    const raw = value.trim();
    if (!raw) return 0;
    const parts = raw.match(/^(\d{1,2}:)?\d{1,3}:\d{2}(?:\.\d+)?$/) ? raw.split(":") : null;
    if (parts) {
      const nums = parts.map((part) => Number(part));
      if (nums.every(Number.isFinite)) {
        if (nums.length === 3) return Math.max(0, nums[0] * 3600 + nums[1] * 60 + nums[2]);
        return Math.max(0, nums[0] * 60 + nums[1]);
      }
    }
    const iso = raw.match(/^PT(?:(\d+(?:\.\d+)?)H)?(?:(\d+(?:\.\d+)?)M)?(?:(\d+(?:\.\d+)?)S)?$/i);
    if (iso) {
      return Math.max(0, Number(iso[1] || 0) * 3600 + Number(iso[2] || 0) * 60 + Number(iso[3] || 0));
    }
  }
  const numeric = Number(value);
  if (!Number.isFinite(numeric) || numeric <= 0) return 0;
  if (allowMilliseconds && numeric > 86400 && numeric % 1e3 === 0) return numeric / 1e3;
  return numeric;
}
function parsePlaybackTimestampMs(value, { now = Date.now() } = {}) {
  if (value === void 0 || value === null || value === "") return 0;
  if (typeof value === "number") {
    if (!Number.isFinite(value) || value <= 0) return 0;
    return value > 1e12 ? value : value * 1e3;
  }
  const raw = String(value || "").trim();
  if (!raw) return 0;
  const numeric = Number(raw);
  if (Number.isFinite(numeric) && numeric > 0) return numeric > 1e12 ? numeric : numeric * 1e3;
  let normalized = raw.replace(/^(\d{4}-\d{2}-\d{2})\s+/, "$1T");
  normalized = normalized.replace(/(\.\d{3})\d+/, "$1");
  normalized = normalized.replace(/([+-]\d{2})(\d{2})$/, "$1:$2");
  if (/(?:z|[+-]\d{2}:?\d{2})$/i.test(normalized)) {
    const parsed2 = Date.parse(normalized);
    return Number.isFinite(parsed2) ? parsed2 : 0;
  }
  const candidates = [Date.parse(`${normalized}Z`), Date.parse(normalized)].filter((candidate) => Number.isFinite(candidate));
  if (!candidates.length) return 0;
  const parsed = candidates.reduce((best, candidate) => Math.abs(candidate - now) < Math.abs(best - now) ? candidate : best, candidates[0]);
  return Number.isFinite(parsed) ? parsed : 0;
}
function formatDuration(sec) {
  const normalized = coercePlaybackSeconds(sec);
  if (!normalized) return "0:00";
  const total = Math.floor(normalized);
  const minutes = Math.floor(total / 60);
  const seconds = String(total % 60).padStart(2, "0");
  return `${minutes}:${seconds}`;
}
function normalizeMediaItem(item, maUrl = "") {
  if (!item || typeof item !== "object") return item;
  const resolvedArt = artUrl(item, maUrl);
  return resolvedArt && item.image_url !== resolvedArt ? { ...item, image_url: resolvedArt } : item;
}
const HomeiiMediaPresentationFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  artUrl,
  artistName,
  buildCurrentTrackInfo,
  coerceLyricsRawText,
  coerceLyricsText,
  coercePlaybackSeconds,
  extractCurrentLyricsRawText,
  extractCurrentLyricsText,
  formatDuration,
  imageProxyIdUrl,
  imageProxyUrl,
  imageUrl,
  normalizeImageProxySize,
  normalizeImageProxyUrl,
  normalizeMediaItem,
  parseLrcLyrics,
  parsePlaybackTimestampMs,
  qualityBadgeLabel,
  sourceProviderMeta,
  stripLyricsTimestamps
}, Symbol.toStringTag, { value: "Module" }));
function buildCurrentSourceBadgeMeta({
  player = null,
  queueItem = null
} = {}, {
  parseMediaReferenceFn = parseMediaReference,
  sourceProviderMetaFn = sourceProviderMeta,
  qualityBadgeLabelFn = qualityBadgeLabel,
  libraryLabel = "Library",
  radioLabel = "Radio"
} = {}) {
  const currentQueueItem = queueItem || null;
  const media = currentQueueItem?.media_item || currentQueueItem || {};
  const parsed = parseMediaReferenceFn(
    media?.uri || currentQueueItem?.uri || currentQueueItem?.media_content_id || player?.attributes?.media_content_id || "",
    media?.media_type || currentQueueItem?.media_type || player?.attributes?.media_content_type || "track"
  );
  const mappings = [].concat(Array.isArray(media?.provider_mappings) ? media.provider_mappings : []).concat(Array.isArray(currentQueueItem?.provider_mappings) ? currentQueueItem.provider_mappings : []);
  const providerCandidates = [
    media?.provider,
    media?.provider_name,
    media?.provider_domain,
    media?.provider_instance,
    currentQueueItem?.provider,
    currentQueueItem?.provider_name,
    currentQueueItem?.provider_domain,
    currentQueueItem?.provider_instance,
    player?.attributes?.provider,
    player?.attributes?.provider_name,
    parsed.provider,
    ...mappings.flatMap((mapping) => [mapping?.provider, mapping?.provider_domain, mapping?.provider_instance, mapping?.provider_name])
  ].filter(Boolean);
  const providerMeta = providerCandidates.map((value) => sourceProviderMetaFn(value, { libraryLabel, radioLabel })).find(Boolean) || null;
  const qualityLabel = qualityBadgeLabelFn([
    media?.audio_format,
    media?.quality,
    media?.streamdetails,
    media?.metadata,
    currentQueueItem?.audio_format,
    currentQueueItem?.quality,
    currentQueueItem?.streamdetails,
    currentQueueItem?.metadata,
    player?.attributes?.media_codec,
    player?.attributes?.media_format,
    ...mappings.map((mapping) => mapping?.details),
    ...mappings.map((mapping) => mapping?.quality),
    ...mappings.map((mapping) => mapping?.audio_format)
  ]);
  return {
    providerKey: providerMeta?.key || "",
    providerLabel: providerMeta?.label || "",
    qualityLabel
  };
}
function buildCurrentHistorySnapshot({
  player = null,
  queueItem = null
} = {}, {
  getQueueItemUriFn,
  queueItemImageUrlFn,
  artUrlFn,
  buildCurrentSourceBadgeMetaFn = buildCurrentSourceBadgeMeta
} = {}) {
  const currentQueueItem = queueItem || null;
  const media = currentQueueItem?.media_item || currentQueueItem || {};
  const title = media?.name || currentQueueItem?.name || player?.attributes?.media_title || "";
  const artist = Array.isArray(media?.artists) ? media.artists.map((entry) => entry?.name).filter(Boolean).join(", ") : media?.artist_str || player?.attributes?.media_artist || "";
  const album = media?.album?.name || currentQueueItem?.album || player?.attributes?.media_album_name || "";
  const uri = String(getQueueItemUriFn?.(currentQueueItem) || media?.uri || player?.attributes?.media_content_id || "").trim();
  const mediaType = String(media?.media_type || currentQueueItem?.media_type || player?.attributes?.media_content_type || "track").toLowerCase();
  const key = [title, artist, album].map((part) => String(part || "").trim().toLowerCase()).join("|");
  if (!key || !uri || mediaType === "radio") return null;
  const sourceMeta = buildCurrentSourceBadgeMetaFn({ player, queueItem: currentQueueItem });
  return {
    key,
    uri,
    media_type: mediaType || "track",
    title,
    artist,
    album,
    image: queueItemImageUrlFn?.(currentQueueItem, 180) || artUrlFn?.(media) || artUrlFn?.(currentQueueItem) || "",
    provider_label: sourceMeta.providerLabel || "",
    quality_label: sourceMeta.qualityLabel || ""
  };
}
function applyRecentPlaybackSnapshot(snapshot = null, previousEntry = null, recentHistory = [], maxItems = 10) {
  if (!snapshot) {
    return {
      currentEntry: previousEntry || null,
      recentHistory: Array.isArray(recentHistory) ? recentHistory : [],
      historyChanged: false
    };
  }
  if (previousEntry?.key === snapshot.key && previousEntry?.uri === snapshot.uri) {
    return {
      currentEntry: snapshot,
      recentHistory: Array.isArray(recentHistory) ? recentHistory : [],
      historyChanged: false
    };
  }
  if (previousEntry?.uri && previousEntry?.key && previousEntry.key !== snapshot.key) {
    const next = [
      previousEntry,
      ...(Array.isArray(recentHistory) ? recentHistory : []).filter((entry) => entry?.uri && entry.uri !== previousEntry.uri && entry.key !== snapshot.key)
    ].slice(0, maxItems);
    return {
      currentEntry: snapshot,
      recentHistory: next,
      historyChanged: true
    };
  }
  return {
    currentEntry: snapshot,
    recentHistory: Array.isArray(recentHistory) ? recentHistory : [],
    historyChanged: false
  };
}
function visibleRecentHistoryItems(currentEntry = null, recentHistory = [], limit = 10) {
  const currentKey = currentEntry?.key || "";
  return (Array.isArray(recentHistory) ? recentHistory : []).filter((entry) => entry?.uri && entry?.key && entry.key !== currentKey).slice(0, limit);
}
function mediaFeedbackLabel(uri = "", fallback = "", queueItems = [], {
  getQueueItemUriFn,
  defaultLabel = "Media"
} = {}) {
  const text = String(fallback || "").trim();
  if (text) return text;
  const normalizedUri = String(uri || "").trim();
  if (!normalizedUri) return defaultLabel;
  const queueHit = (Array.isArray(queueItems) ? queueItems : []).find((item) => getQueueItemUriFn?.(item) === normalizedUri);
  if (queueHit?.media_item?.name) return queueHit.media_item.name;
  const separator = normalizedUri.lastIndexOf("/");
  if (separator >= 0 && separator < normalizedUri.length - 1) return decodeURIComponent(normalizedUri.slice(separator + 1));
  return normalizedUri;
}
const HomeiiMediaHistoryFoundationSource = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  applyRecentPlaybackSnapshot,
  buildCurrentHistorySnapshot,
  buildCurrentSourceBadgeMeta,
  mediaFeedbackLabel,
  visibleRecentHistoryItems
}, Symbol.toStringTag, { value: "Module" }));
const VOICE_ASSISTANT_MUSIC_STOP_WORDS = Object.freeze([
  "a",
  "an",
  "and",
  "by",
  "for",
  "from",
  "hey",
  "home",
  "listen",
  "music",
  "of",
  "on",
  "play",
  "please",
  "put",
  "song",
  "songs",
  "start",
  "the",
  "to",
  "track",
  "tracks",
  "את",
  "אלבום",
  "אמן",
  "בבקשה",
  "האלבום",
  "השיר",
  "הום",
  "הפעל",
  "השמע",
  "זמר",
  "זמרת",
  "ל",
  "לי",
  "מאת",
  "מוזיקה",
  "מוסיקה",
  "נגן",
  "נגני",
  "פלייליסט",
  "רצועה",
  "שים",
  "שימי",
  "של",
  "שיר",
  "שירים",
  "תנגן",
  "תנגני",
  "תפעיל",
  "תפעילי",
  "תשמיע",
  "תשמיעי"
]);
const VOICE_ASSISTANT_MUSIC_COMMAND_TERMS = Object.freeze([
  "please",
  "home assistant",
  "hey home assistant",
  "play",
  "put on",
  "listen to",
  "start",
  "music",
  "בבקשה",
  "היי",
  "היי הום אסיסטנט",
  "הום אסיסטנט",
  "נגן",
  "נגני",
  "תנגן",
  "תנגני",
  "שים",
  "שימי",
  "השמע",
  "תשמיע",
  "תשמיעי",
  "הפעל",
  "תפעיל",
  "תפעילי"
]);
function normalizeVoiceCommandText(value = "") {
  return String(value || "").normalize("NFKD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[.,!?;:"'`׳״()[\]{}]/g, " ").replace(/\s+/g, " ").trim();
}
function voiceCommandHasAny(normalizedText = "", terms = []) {
  const text = normalizeVoiceCommandText(normalizedText);
  if (!text) return false;
  const haystack = ` ${text} `;
  return terms.some((term) => {
    const needle = normalizeVoiceCommandText(term);
    if (!needle) return false;
    if (/[\u0590-\u05ff]/.test(needle)) return text.includes(needle);
    return haystack.includes(` ${needle} `) || text === needle;
  });
}
function voiceAssistantAliasIndex(normalizedText = "", alias = "") {
  const text = normalizeVoiceCommandText(normalizedText);
  const needle = normalizeVoiceCommandText(alias);
  if (!text || !needle) return -1;
  if (/[\u0590-\u05ff]/.test(needle)) return text.indexOf(needle);
  const index = ` ${text} `.indexOf(` ${needle} `);
  return index >= 0 ? Math.max(0, index) : -1;
}
function stripVoiceAssistantPlayerAliases(text = "", aliases = []) {
  let next = ` ${normalizeVoiceCommandText(text)} `;
  (Array.isArray(aliases) ? aliases : []).forEach((alias) => {
    const normalizedAlias = normalizeVoiceCommandText(alias);
    if (normalizedAlias) next = next.split(` ${normalizedAlias} `).join(" ");
  });
  return next.replace(/\s+/g, " ").trim();
}
function extractVoiceAssistantMusicQuery(transcript = "", aliases = []) {
  let query = stripVoiceAssistantPlayerAliases(transcript, aliases);
  VOICE_ASSISTANT_MUSIC_COMMAND_TERMS.forEach((term) => {
    const normalized = normalizeVoiceCommandText(term);
    if (!normalized) return;
    query = ` ${query} `.split(` ${normalized} `).join(" ").replace(/\s+/g, " ").trim();
  });
  return query || normalizeVoiceCommandText(transcript);
}
function voiceAssistantVolumeIntent(normalizedText = "") {
  const normalized = normalizeVoiceCommandText(normalizedText);
  if (voiceCommandHasAny(normalized, ["unmute", "sound on", "בטל השתקה", "להחזיר קול", "תחזיר קול"])) return { type: "unmute" };
  if (voiceCommandHasAny(normalized, ["mute", "silence", "השתק", "להשתיק", "שקט"])) return { type: "mute" };
  const hasVolumeWord = voiceCommandHasAny(normalized, ["volume", "sound", "ווליום", "עוצמה", "עוצמת קול"]);
  if (!hasVolumeWord) return null;
  if (voiceCommandHasAny(normalized, ["half", "חצי"])) return { type: "volume_set", level: 0.5 };
  if (voiceCommandHasAny(normalized, ["max", "maximum", "מקסימום", "הכי חזק"])) return { type: "volume_set", level: 1 };
  const numberMatch = normalized.match(/(\d{1,3})/);
  if (numberMatch) {
    const pct = Math.max(0, Math.min(100, Number(numberMatch[1]) || 0));
    return { type: "volume_set", level: pct / 100 };
  }
  if (voiceCommandHasAny(normalized, ["up", "higher", "louder", "increase", "הגבר", "להגביר", "חזק יותר"])) {
    return { type: "volume_delta", delta: 0.1 };
  }
  if (voiceCommandHasAny(normalized, ["down", "lower", "quieter", "decrease", "הנמך", "להנמיך", "חלש יותר"])) {
    return { type: "volume_delta", delta: -0.1 };
  }
  return null;
}
function voiceAssistantRequestedMediaType(query = "") {
  const normalized = normalizeVoiceCommandText(query);
  if (voiceCommandHasAny(normalized, ["playlist", "play list", "פלייליסט", "רשימת השמעה"])) return { type: "playlist", explicit: true };
  if (voiceCommandHasAny(normalized, ["album", "אלבום"])) return { type: "album", explicit: true };
  if (voiceCommandHasAny(normalized, ["artist", "singer", "אמן", "זמר", "זמרת"])) return { type: "artist", explicit: true };
  if (voiceCommandHasAny(normalized, ["radio", "station", "רדיו", "תחנה"])) return { type: "radio", explicit: true };
  if (voiceCommandHasAny(normalized, ["song", "track", "שיר", "רצועה", "by"])) return { type: "track", explicit: true };
  if (` ${normalized} `.includes(" של ") || ` ${normalized} `.includes(" מאת ")) return { type: "track", explicit: true };
  return { type: "track", explicit: false };
}
function voiceAssistantCanonicalMediaType(value = "", fallback = "track") {
  const type = String(value || fallback || "track").toLowerCase().trim();
  if (type === "tracks") return "track";
  if (type === "playlists" || type === "play_list" || type === "play lists") return "playlist";
  if (type === "albums") return "album";
  if (type === "artists") return "artist";
  if (type === "radios" || type === "station" || type === "stations") return "radio";
  if (type === "podcasts") return "podcast";
  if (type === "songs") return "track";
  return type || String(fallback || "track").toLowerCase().trim() || "track";
}
function voiceAssistantImportantMusicTokens(value = "") {
  return normalizeVoiceCommandText(value).split(/\s+/).map((token) => token.trim()).filter((token) => token.length > 1 && !VOICE_ASSISTANT_MUSIC_STOP_WORDS.includes(token));
}
function voiceAssistantCleanMusicPhrase(value = "", { allowStopWordFallback = false } = {}) {
  const normalized = normalizeVoiceCommandText(value);
  if (!normalized) return "";
  const tokens = voiceAssistantImportantMusicTokens(normalized);
  return tokens.join(" ") || (allowStopWordFallback ? normalized : "");
}
function voiceAssistantTransliterateHebrewToken(value = "") {
  const map = {
    א: "a",
    ב: "b",
    ג: "g",
    ד: "d",
    ה: "h",
    ו: "o",
    ז: "z",
    ח: "h",
    ט: "t",
    י: "i",
    כ: "k",
    ך: "k",
    ל: "l",
    מ: "m",
    ם: "m",
    נ: "n",
    ן: "n",
    ס: "s",
    ע: "a",
    פ: "p",
    ף: "p",
    צ: "tz",
    ץ: "tz",
    ק: "k",
    ר: "r",
    ש: "sh",
    ת: "t"
  };
  return Array.from(String(value || "")).map((char) => map[char] ?? char).join("");
}
function voiceAssistantLatinPhoneticKeys(value = "") {
  const raw = voiceAssistantTransliterateHebrewToken(value).normalize("NFKD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]/g, "");
  if (!raw) return [];
  const variants = /* @__PURE__ */ new Set([
    raw,
    raw.replace(/ch/g, "sh"),
    raw.replace(/ch/g, "k"),
    raw.replace(/ph/g, "f")
  ]);
  const keys = /* @__PURE__ */ new Set();
  variants.forEach((variant) => {
    const normalized = variant.replace(/c(?=[eiy])/g, "s").replace(/c/g, "k").replace(/q/g, "k").replace(/x/g, "ks").replace(/(.)\1+/g, "$1");
    const consonants = normalized.replace(/[aeiouy]/g, "");
    [normalized, consonants].forEach((key) => {
      if (key.length >= 2) keys.add(key);
    });
  });
  return Array.from(keys);
}
function voiceAssistantTextHasToken(normalizedText = "", token = "") {
  const haystack = normalizeVoiceCommandText(normalizedText);
  const needle = normalizeVoiceCommandText(token);
  if (!haystack || !needle) return false;
  if (/[\u0590-\u05ff]/.test(needle) && haystack.includes(needle)) return true;
  if (!/[\u0590-\u05ff]/.test(needle) && (` ${haystack} `.includes(` ${needle} `) || haystack.includes(needle))) return true;
  const needleKeys = voiceAssistantLatinPhoneticKeys(needle);
  if (!needleKeys.length) return false;
  return haystack.split(/\s+/).some((word) => {
    const wordKeys = voiceAssistantLatinPhoneticKeys(word);
    return wordKeys.some((wordKey) => needleKeys.some(
      (needleKey) => wordKey === needleKey || needleKey.length >= 4 && wordKey.includes(needleKey) || wordKey.length >= 4 && needleKey.includes(wordKey)
    ));
  });
}
function voiceAssistantMatchedTokenCount(tokens = [], normalizedText = "") {
  const haystack = normalizeVoiceCommandText(normalizedText);
  return (Array.isArray(tokens) ? tokens : []).filter((token) => voiceAssistantTextHasToken(haystack, token)).length;
}
function voiceAssistantMusicQueryParts(query = "") {
  const normalized = normalizeVoiceCommandText(query);
  if (!normalized) return { query: "", title: "", artist: "" };
  const separators = [" של ", " מאת ", " by ", " from ", " ל "];
  for (const separator of separators) {
    const padded = ` ${normalized} `;
    const index = padded.indexOf(separator);
    if (index < 0) continue;
    const title = voiceAssistantCleanMusicPhrase(padded.slice(1, index));
    const artist = voiceAssistantCleanMusicPhrase(padded.slice(index + separator.length, -1));
    if (title && artist) return { query: normalized, title, artist };
  }
  const cleaned = voiceAssistantCleanMusicPhrase(normalized, { allowStopWordFallback: true });
  return { query: cleaned || normalized, title: cleaned || normalized, artist: "" };
}
function voiceAssistantFocusedMusicQuery(query = "") {
  const parts = voiceAssistantMusicQueryParts(query);
  return [parts.title, parts.artist].filter(Boolean).join(" ").trim() || parts.query;
}
function voiceAssistantCandidateScore(candidate = {}, query = "", request = voiceAssistantRequestedMediaType(query)) {
  const type = voiceAssistantCanonicalMediaType(candidate?.media_type || "", "");
  const title = normalizeVoiceCommandText(candidate?.name || candidate?.title || "");
  const artist = normalizeVoiceCommandText(candidate?.artist || "");
  const album = normalizeVoiceCommandText(candidate?.album || "");
  const parts = voiceAssistantMusicQueryParts(query);
  const q = parts.query;
  const titleNeedle = parts.title;
  const artistNeedle = parts.artist;
  const haystack = [title, artist, album].filter(Boolean).join(" ");
  let score = 0;
  if (type === request.type) score += request.explicit ? 24 : 10;
  else if (!request.explicit && type === "track") score += 10;
  else if (!request.explicit && type === "album") score += 3;
  else if (!request.explicit && type === "artist") score += 2;
  else if (!request.explicit && type === "playlist") score -= 8;
  else if (request.explicit) score -= 8;
  if (candidate?._homeiiVoiceFocused === true) score += 8;
  if (q && title === q) score += 28;
  else if (q && title.startsWith(q)) score += 16;
  else if (q && title.includes(q)) score += 10;
  if (titleNeedle && title === titleNeedle) score += 34;
  else if (titleNeedle && title.startsWith(titleNeedle)) score += 22;
  else if (titleNeedle && title.includes(titleNeedle)) score += 15;
  if (artistNeedle) {
    if (artist === artistNeedle) score += 30;
    else if (artist.includes(artistNeedle) || artistNeedle.includes(artist)) score += 18;
    else if (artistNeedle.split(" ").filter(Boolean).every((word) => haystack.includes(word))) score += 12;
  }
  const words = voiceAssistantImportantMusicTokens([parts.title, parts.artist].filter(Boolean).join(" ") || q);
  const matchedWords = voiceAssistantMatchedTokenCount(words, haystack);
  score += matchedWords * 6;
  if (words.length && matchedWords === words.length) score += 12;
  if (words.length && !matchedWords) score -= 30;
  if (!request.explicit && type === "playlist" && (title.includes("hits") || title.includes("top") || title.includes("radio"))) score -= 4;
  return score;
}
function voiceAssistantCandidateMatch(candidate = {}, query = "", request = voiceAssistantRequestedMediaType(query), index = 0) {
  const type = voiceAssistantCanonicalMediaType(candidate?.media_type || "", "");
  const title = normalizeVoiceCommandText(candidate?.name || candidate?.title || "");
  const artist = normalizeVoiceCommandText(candidate?.artist || "");
  const album = normalizeVoiceCommandText(candidate?.album || "");
  const parts = voiceAssistantMusicQueryParts(query);
  const queryTokens = voiceAssistantImportantMusicTokens([parts.title, parts.artist].filter(Boolean).join(" ") || parts.query);
  const titleTokens = voiceAssistantImportantMusicTokens(parts.title);
  const artistTokens = voiceAssistantImportantMusicTokens(parts.artist);
  const haystack = [title, artist, album].filter(Boolean).join(" ");
  const matchedTokens = voiceAssistantMatchedTokenCount(queryTokens, haystack);
  const matchedTitleTokens = voiceAssistantMatchedTokenCount(titleTokens, title);
  const matchedArtistTokens = voiceAssistantMatchedTokenCount(artistTokens, [artist, album, haystack].filter(Boolean).join(" "));
  const exactTitlePhrase = !!parts.title && (title === parts.title || title.startsWith(parts.title) || title.includes(parts.title));
  const exactQueryPhrase = !!parts.query && (title === parts.query || title.startsWith(parts.query) || title.includes(parts.query));
  const titleMatchStrong = exactTitlePhrase || exactQueryPhrase || titleTokens.length > 0 && matchedTitleTokens === titleTokens.length;
  const hasCandidateArtist = !!artist;
  const requiredArtistMatches = artistTokens.length ? Math.min(artistTokens.length, Math.max(1, Math.ceil(artistTokens.length * 0.6))) : 0;
  const artistMatchStrong = artistTokens.length > 0 && matchedArtistTokens >= requiredArtistMatches;
  const score = voiceAssistantCandidateScore(candidate, query, request);
  let accepted = false;
  if (artistTokens.length && !titleTokens.length) {
    accepted = artistMatchStrong;
  } else if (artistTokens.length && titleTokens.length) {
    accepted = titleMatchStrong && (artistMatchStrong || !hasCandidateArtist);
  } else if (queryTokens.length === 1) {
    accepted = matchedTokens === 1 && (exactTitlePhrase || exactQueryPhrase || matchedTitleTokens === 1 || matchedArtistTokens === 1);
  } else if (queryTokens.length > 1) {
    const requiredMatches = Math.min(queryTokens.length, Math.max(2, Math.ceil(queryTokens.length * 0.55)));
    accepted = titleMatchStrong || matchedTokens >= requiredMatches;
  }
  if (request.explicit && type !== request.type) accepted = false;
  return {
    candidate,
    index,
    score,
    accepted,
    matchedTokens,
    queryTokenCount: queryTokens.length,
    exactTitlePhrase,
    exactQueryPhrase
  };
}
function voiceAssistantRankedCandidates(candidates = [], query = "") {
  const items = Array.isArray(candidates) ? candidates : [];
  if (!items.length) return [];
  const request = voiceAssistantRequestedMediaType(query);
  return items.map((candidate, index) => voiceAssistantCandidateMatch(candidate, query, request, index)).sort(
    (left, right) => Number(right.accepted) - Number(left.accepted) || right.score - left.score || right.matchedTokens - left.matchedTokens || left.index - right.index
  );
}
function voiceAssistantBestCandidate(candidates = [], query = "") {
  return voiceAssistantRankedCandidates(candidates, query).find((match) => match.accepted && match.candidate?.uri)?.candidate || null;
}
const RADIO_BROWSER_COUNTRY_CODES = Object.freeze(
  "AD AE AF AG AI AL AM AO AQ AR AS AT AU AW AX AZ BA BB BD BE BF BG BH BI BJ BL BM BN BO BQ BR BS BT BV BW BY BZ CA CC CD CF CG CH CI CK CL CM CN CO CR CU CV CW CX CY CZ DE DJ DK DM DO DZ EC EE EG EH ER ES ET FI FJ FK FM FO FR GA GB GD GE GF GG GH GI GL GM GN GP GQ GR GS GT GU GW GY HK HM HN HR HT HU ID IE IL IM IN IO IQ IR IS IT JE JM JO JP KE KG KH KI KM KN KP KR KW KY KZ LA LB LC LI LK LR LS LT LU LV LY MA MC MD ME MF MG MH MK ML MM MN MO MP MQ MR MS MT MU MV MW MX MY MZ NA NC NE NF NG NI NL NO NP NR NU NZ OM PA PE PF PG PH PK PL PM PN PR PS PT PW PY QA RE RO RS RU RW SA SB SC SD SE SG SH SI SJ SK SL SM SN SO SR SS ST SV SX SY SZ TC TD TF TG TH TJ TK TL TM TN TO TR TT TV TW TZ UA UG UM US UY UZ VA VC VE VG VI VN VU WF WS XK YE YT ZA ZM ZW".split(" ").filter((code, index, list) => code && list.indexOf(code) === index)
);
const RADIO_BROWSER_COUNTRY_I18N_KEYS = Object.freeze({
  AU: "ui.australia",
  CA: "ui.canada",
  DE: "ui.germany",
  ES: "ui.spain",
  FR: "ui.france",
  GB: "ui.united_kingdom",
  GR: "ui.greece",
  IL: "ui.israel",
  IT: "ui.italy",
  NL: "ui.netherlands",
  TR: "ui.turkey",
  US: "ui.united_states"
});
const regionDisplayNamesCache = /* @__PURE__ */ new Map();
function regionDisplayName(code = "", language = "en") {
  const normalized = String(code || "").trim().toUpperCase();
  if (!/^[A-Z]{2}$/.test(normalized)) return normalized;
  const lang = String(language || "en").trim() || "en";
  const cacheKey = `${lang}:${normalized}`;
  if (regionDisplayNamesCache.has(cacheKey)) return regionDisplayNamesCache.get(cacheKey);
  let label = normalized;
  try {
    const displayNames = new Intl.DisplayNames([lang], { type: "region" });
    label = displayNames.of(normalized) || normalized;
  } catch {
  }
  regionDisplayNamesCache.set(cacheKey, label);
  return label;
}
function radioBrowserCountryLabel(code = "", translateFn = null, language = "en") {
  const normalized = String(code || "").trim().toUpperCase();
  const i18nKey = RADIO_BROWSER_COUNTRY_I18N_KEYS[normalized];
  if (i18nKey && typeof translateFn === "function") {
    const translated = translateFn(i18nKey);
    if (translated && translated !== i18nKey) return translated;
  }
  return regionDisplayName(normalized, language);
}
function radioBrowserCountrySelectorOptions(translateFn = null, language = "en") {
  const safeTranslate = typeof translateFn === "function" ? translateFn : (key) => key;
  return [
    { value: "all", label: safeTranslate("ui.all_countries") },
    ...RADIO_BROWSER_COUNTRY_CODES.map((code) => ({
      value: code,
      label: radioBrowserCountryLabel(code, safeTranslate, language)
    }))
  ];
}
function countryFlagEmoji(code = "") {
  const cc = String(code || "").trim().toUpperCase();
  if (!/^[A-Z]{2}$/.test(cc)) return "";
  return String.fromCodePoint(...[...cc].map((char) => 127397 + char.charCodeAt(0)));
}
const HOMEII_VISIBLE_LANGUAGE_OPTIONS = Object.freeze([
  ...LANGUAGE_OPTIONS,
  ...[
    { value: "es", label: "Spanish" },
    { value: "fr", label: "French" },
    { value: "lt", label: "Lithuanian" }
  ].filter((option) => !LANGUAGE_OPTIONS.some((entry) => String(entry?.value || "").toLowerCase() === option.value))
]);
function ensureHaEditorComponents() {
  try {
    if (!customElements.get("ha-form") || !customElements.get("hui-card-features-editor")) {
      customElements.get("hui-tile-card")?.getConfigElement?.();
    }
    if (!customElements.get("ha-entity-picker")) {
      customElements.get("hui-entities-card")?.getConfigElement?.();
    }
    if (!customElements.get("ha-card-conditions-editor")) {
      customElements.get("hui-conditional-card")?.getConfigElement?.();
    }
  } catch (_) {
  }
}
const HOMEII_CARD_VERSION = "5.8.1";
const HOMEII_BROWSER_EDITOR_TAG = "homeii-music-flow-browser-editor-v581";
const HOMEII_MOBILE_EDITOR_TAG = "homeii-music-flow-editor-v581";
const AMBIENT_LIGHT_PAIR_PLAYER_PREFIX = "__homeii_ambient_light_pair_player_";
const AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX = "__homeii_ambient_light_pair_lights_";
const HomeiiEditorLocale = Object.freeze({
  isHebrewLanguageTag,
  pickEditorLanguageCandidate,
  detectEditorHebrew
});
function homeiiEditorI18n(key, params = {}, fallback = "") {
  const language = HomeiiEditorLocale.detectEditorHebrew() ? "he" : "en";
  return translate(language, key, params, fallback);
}
function homeiiEditorSchemaName(schema = {}) {
  if (typeof schema === "string") return schema;
  if (Array.isArray(schema)) return String(schema[schema.length - 1] || schema.join(".") || "");
  if (schema && typeof schema === "object") {
    return String(schema.name || schema.key || schema.path || "");
  }
  return "";
}
function homeiiHumanizeEditorName(name = "") {
  const text = String(name || "").trim().replace(/_/g, " ").replace(/\s+/g, " ");
  if (!text) return "";
  return text.replace(/\b[a-z]/g, (match) => match.toUpperCase());
}
function homeiiEditorLabelFor(schema = {}, labels = {}) {
  const name = homeiiEditorSchemaName(schema);
  return labels?.[name] || schema?.label || schema?.title || homeiiHumanizeEditorName(name);
}
function homeiiEditorHelperFor(schema = {}, helpers = {}) {
  const name = homeiiEditorSchemaName(schema);
  return helpers?.[name] || schema?.helper || "";
}
const HomeiiConfigValidators = Object.freeze({
  assertStringIfDefined,
  assertBooleanIfDefined,
  assertNumberIfDefined,
  assertStringArrayIfDefined,
  assertValueInList,
  assertStringArrayValuesIfDefined,
  validateBaseCardEditorConfig,
  validateMobileCardEditorConfig
});
const HomeiiStateFoundation = Object.freeze({
  createBaseBrowserState,
  normalizeSettingsSource,
  usesVisualSettings,
  mobileCompactModeEnabled,
  mobileCompactEdgeToEdgeEnabled,
  mobileCompactWidgetMode,
  mobileShowUpNextEnabled,
  performanceProfile,
  performanceModeEnabled,
  performanceUltraLiteEnabled,
  mobileDynamicThemeMode,
  mobileBackgroundMotionMode,
  backgroundMotionEnabled,
  backgroundMotionAmount,
  isCompactTileMode
});
const HomeiiMobileSettingsFoundation = Object.freeze({
  ...HomeiiMobileSettingsFoundationSource
});
const HomeiiResponsiveFoundation = Object.freeze({
  ...HomeiiResponsiveFoundationSource
});
const HomeiiPaletteFoundation = Object.freeze({
  ...HomeiiPaletteFoundationSource
});
const HomeiiNightFoundation = Object.freeze({
  ...HomeiiNightFoundationSource
});
const HomeiiMediaQueueFoundation = Object.freeze({
  ...HomeiiMediaQueueFoundationSource
});
const HomeiiFavoritesFoundation = Object.freeze({
  ...HomeiiFavoritesFoundationSource
});
const HomeiiPlayersFoundation = Object.freeze({
  ...HomeiiPlayersFoundationSource
});
const HomeiiMediaPresentationFoundation = Object.freeze({
  ...HomeiiMediaPresentationFoundationSource
});
const HomeiiMediaHistoryFoundation = Object.freeze({
  ...HomeiiMediaHistoryFoundationSource
});
const HomeiiArtworkFoundation = Object.freeze({
  ...HomeiiArtworkFoundationSource
});
const HomeiiNowPlayingFoundation = Object.freeze({
  ...HomeiiNowPlayingFoundationSource
});
configureHomeiiEditorForms({
  homeiiEditorI18n,
  homeiiEditorLabelFor,
  homeiiEditorHelperFor,
  detectEditorHebrew: HomeiiEditorLocale.detectEditorHebrew,
  visibleLanguageOptions: HOMEII_VISIBLE_LANGUAGE_OPTIONS,
  radioBrowserCountrySelectorOptions
});
function getBaseCardConfigForm() {
  return getBaseCardConfigForm$1();
}
function getRadioBrowserCountrySelectorOptions(translateFn = homeiiEditorI18n, language = "") {
  return getRadioBrowserCountrySelectorOptions$1(translateFn, language);
}
function getMobileCardConfigForm() {
  return getMobileCardConfigForm$1();
}
const HomeiiBaseMusicCard = createHomeiiBaseMusicCard({
  HOMEII_CARD_VERSION,
  HOMEII_VISIBLE_LANGUAGE_OPTIONS,
  HomeiiStateFoundation,
  HomeiiConfigValidators,
  HomeiiResponsiveFoundation,
  HomeiiMediaQueueFoundation,
  HomeiiMediaPresentationFoundation,
  HomeiiMediaHistoryFoundation,
  HomeiiNowPlayingFoundation,
  HomeiiFavoritesFoundation,
  HomeiiPlayersFoundation,
  getBaseCardConfigForm,
  getRadioBrowserCountrySelectorOptions,
  homeiiRadioBrowserCountryLabel: radioBrowserCountryLabel,
  homeiiCountryFlagEmoji: countryFlagEmoji,
  homeiiDetectLanguage: detectLanguage,
  homeiiIsRtlLanguage: isRtlLanguage,
  homeiiTranslate: translate,
  homeiiTranslateText: translateText
});
const HomeiiBaseMusicEditor = createHomeiiBaseMusicEditor({
  HomeiiBaseMusicCard,
  ensureHaEditorComponents,
  homeiiIsRtlLanguage: isRtlLanguage,
  homeiiDetectLanguage: detectLanguage,
  HomeiiConfigValidators,
  HomeiiPlayersFoundation,
  HomeiiMobileSettingsFoundation,
  homeiiEditorI18n,
  homeiiEditorLabelFor,
  homeiiEditorHelperFor,
  HOMEII_CARD_VERSION,
  AMBIENT_LIGHT_PAIR_PLAYER_PREFIX,
  AMBIENT_LIGHT_PAIR_LIGHTS_PREFIX
});
if (!customElements.get(HOMEII_BROWSER_EDITOR_TAG)) {
  customElements.define(HOMEII_BROWSER_EDITOR_TAG, HomeiiBaseMusicEditor);
}
if (!Array.isArray(window.customCards)) window.customCards = [];
class HomeiiMusicFlowBaseCard extends HomeiiBaseMusicCard {
  constructor() {
    super();
    this._editMode = false;
    this._state.menuOpen = false;
    this._state.menuPage = "main";
    this._state.menuStack = [];
    this._state.mediaQuery = "";
    this._state.libraryTabSearchQueries = {};
    this._state.libraryTabSearchDrafts = {};
    this._state.libraryTabSearchOpen = false;
    this._state.libraryTabSearchFocusId = "";
    this._state.mobileMediaLayout = this._defaultMobileMediaLayout();
    this._state.mobileMediaDetailLayout = "grid";
    this._state.mobileLibrarySort = "name_asc";
    this._state.mediaSearchToken = 0;
    this._state.mobileQueueActionEntry = null;
    this._state.mobileCustomColor = "#f5a623";
    this._state.mobileDynamicThemeMode = "auto";
    this._state.mobileCustomTextTone = "light";
    this._state.hotelMode = false;
    this._state.mobileFontScale = 1;
    this._state.mobileIconScale = 1;
    this._state.mobileNightMode = "off";
    this._state.mobileNightModeStart = "22:00";
    this._state.mobileNightModeEnd = "06:00";
    this._state.mobileNightModeDays = [0, 1, 2, 3, 4, 5, 6];
    this._state.mobileSleepTimerEndsAt = 0;
    this._state.mobileSleepTimerPlayer = "";
    this._state.mobileSleepTimerOrigin = "";
    this._state.mobileSleepTimerMenuOpen = false;
    this._state.mobileStartTimerEnabled = false;
    this._state.mobileStartTimerTime = "07:00";
    this._state.mobileStartTimerPlayer = "";
    this._state.mobileStartTimerPlaylist = "";
    this._state.mobileStartTimerPlaylistName = "";
    this._state.mobileStartTimerPlaylists = [];
    this._state.mobileStartTimerPlaylistsFetchedAt = 0;
    this._state.mobileStartTimerPlaylistsLoading = false;
    this._state.mobileStartTimerVolume = 35;
    this._state.mobileStartTimerDays = [0, 1, 2, 3, 4, 5, 6];
    this._state.mobileStartTimerLastRunKey = "";
    this._state.mobileStartTimerRunPending = false;
    this._state.mobileStartSchedules = [];
    this._state.mobileStartScheduleEditId = "";
    this._state.mobileStartTimerAfterRun = "keep";
    this._state.mobileSchedulesTab = "timers";
    this._state.mobileScheduleControlActiveUntil = 0;
    this._state.localSendspinDisconnecting = false;
    this._state.controlRoomRevealThisDevicePending = false;
    this._state.mobileLyricsSyncEnabled = true;
    this._state.mobileLyricsSyncOffsetMs = 0;
    this._state.mobileLyricsFontScale = 1;
    this._state.mobileCompactMode = false;
    this._state.mobileCompactWidgetMode = "auto";
    this._state.mobileCompactEdgeToEdge = true;
    this._state.mobileLayoutMode = "auto";
    this._state.mobileLibraryDefaultLayout = this._defaultMobileMediaLayout();
    this._state.mobileMediaLayoutManual = false;
    this._state.mobileShowUpNext = false;
    this._state.mobileRecentHistory = [];
    this._state.mobileRecommendationPlaylists = [];
    this._state.mobileRecommendationPlaylistsFetchedAt = 0;
    this._state.mobileHistoryRenderedHtml = "";
    this._state.mobileCurrentHistoryEntry = null;
    this._state.mobileHistoryDrawerOpen = false;
    this._state.mobileHistoryDrawerTab = "recent";
    this._state.mobileSettingsScrollTop = 0;
    this._state.controlRoomRestoreAfterMenu = false;
    this._state.controlRoomRenderedHtml = "";
    this._state.controlRoomRenderSignature = "";
    this._state.controlRoomQueueSnapshots = {};
    this._state.controlRoomQueueLoading = false;
    this._state.controlRoomRecentItems = [];
    this._state.controlRoomRecentLoading = false;
    this._state.controlRoomFavoritesItems = [];
    this._state.controlRoomFavoritesLoading = false;
    this._state.controlRoomSmartQuery = "";
    this._state.controlRoomAnnouncementText = "";
    this._state.controlRoomAnnouncementVolume = 20;
    this._state.controlRoomCustomScenes = [];
    this._state.controlRoomSceneName = "";
    this._state.mobileCompactExpanded = false;
    this._state.mobileFooterSearchEnabled = false;
    this._state.mobileStudioShortcutEnabled = true;
    this._state.mobileFooterMode = "both";
    this._state.mobileHomeShortcutEnabled = false;
    this._state.mobileHomeShortcutPath = "/";
    this._state.mobileVolumeMode = "button";
    this._state.mobileVolumeStepButtonsEnabled = false;
    this._state.mobileVolumeStepPercent = 5;
    this._state.mobileMicMode = "smart";
    this._state.voiceAssistantEnabled = false;
    this._state.voiceAssistantMode = "hybrid";
    this._state.voiceAssistantAgentId = "";
    this._state.voiceAssistantSpeakFeedback = false;
    this._state.voiceAssistantListening = false;
    this._state.voiceAssistantDialogOpen = false;
    this._state.voiceAssistantDialogStatus = "";
    this._state.voiceAssistantTranscript = "";
    this._state.voiceAssistantResponse = "";
    this._state.voiceAssistantKeepScreensaver = false;
    this._state.mobileLibraryTabs = ["library_search", "library_playlists", "library_artists", "library_albums", "library_tracks", "library_radio", "library_podcasts"];
    this._state.mobileMainBarItems = ["actions", "players", "library", "settings"];
    this._state.mobileQuickActions = ["timer", "like", "lyrics", "queue", "radio", "history"];
    this._state.mobileLikedMode = "ma";
    this._state.mobileSwipeMode = "browse";
    this._state.mobileRadioBrowserCountry = "all";
    this._state.mobileRadioBrowseCountry = "";
    this._state.mobileRadioBrowseCountryName = "";
    this._state.mobileAnnouncementText = "";
    this._state.mobileAnnouncementPresets = this._defaultAnnouncementPresets();
    this._state.mobileAnnouncementVolume = 20;
    this._state.mobileAnnouncementTtsEntity = "";
    this._state.mobileAnnouncementTtsLanguage = "auto";
    this._state.ambientLightEnabled = false;
    this._state.ambientLightEntities = [];
    this._state.ambientLightPlayerMap = [];
    this._state.ambientLightBrightness = 35;
    this._state.ambientLightTransition = 3;
    this._state.ambientLightCooldown = 8;
    this._state.screensaverEnabled = false;
    this._state.screensaverControlsEnabled = false;
    this._state.screensaverControlButtons = ["previous", "next"];
    this._state.screensaverClockMode = "digital";
    this._state.screensaverTimeoutSeconds = 90;
    this._state.screensaverMessage = "";
    this._state.screensaverClockSize = 1;
    this._state.screensaverClockX = 82;
    this._state.screensaverClockY = 24;
    this._state.screensaverOpen = false;
    this._state.powerButtonEnabled = false;
    this._state.powerButtonName = "";
    this._state.powerButtonIcon = "power";
    this._state.powerButtonAction = "stop_player";
    this._state.powerButtonEntity = "";
    this._state.auxiliaryButtons = [];
    this._state.discoveryModeEnabled = true;
    this._state.discoveryCategoryKey = "pop";
    this._state.discoveryGenreKey = "all";
    this._state.discoveryExpandedUri = "";
    this._state.discoverySessionSeed = Date.now();
    this._state.pinnedPlayerEntities = [];
    this._state.excludedPlayerEntities = [];
    this._state.configurableMusicAssistantPlayers = [];
    this._state.playerSortMode = "default";
    this._state.playerOrderEntities = [];
    this._state.mobileArtBrowseOffset = 0;
    this._state.mobileArtAnchorKey = "";
    this._state.activeArtworkTouch = null;
    this._state.mobileArtBrowseOffset = 0;
    this._state.mobileArtBrowseAnchorIndex = -1;
    this._state.mobileArtRenderKey = "";
    this._state.mobileQueuePlayPendingUntil = 0;
    this._state.mobileQueuePlayPendingKey = "";
    this._state.mobileQueuePlayPendingIndex = null;
    this._state.mobileQueuePlayPendingUri = "";
    this._state.mobileQueuePlayPendingPlayerId = "";
    this._mobileEmbla = null;
    this._mobileEmblaLoadPromise = null;
    this._mobileEmblaSyncing = false;
    this._systemMobileStateHydrateKey = "";
    this._systemMobileStateHydratePromise = null;
    this._systemMobileStatePersistTimer = null;
    this._mobileArtBrowseResetTimer = null;
    this._emptyPlaybackLoaderTimer = null;
    this._mobileSmartVoiceTimer = null;
    this._libraryTabSearchTimer = null;
    this._simpleWizardPopupTimer = null;
    this._simpleWizardToken = 0;
    this._mobileDynamicThemePaletteCache = /* @__PURE__ */ new Map();
    this._mobileDynamicThemeToken = 0;
    this._mobileDynamicThemeAppliedSignature = "";
    this._decodedArtworkUrls = /* @__PURE__ */ new Set();
    this._decodedArtworkImages = /* @__PURE__ */ new Map();
    this._artworkDecodePromises = /* @__PURE__ */ new Map();
    this._artworkPrefetchQueue = [];
    this._artworkPrefetchQueuedUrls = /* @__PURE__ */ new Set();
    this._artworkPrefetchActive = 0;
    this._artworkPrefetchTimer = null;
    this._mobileQueueArtworkPrefetchTimer = null;
    this._currentArtworkBackgroundToken = 0;
    this._backgroundCrossfadeTimers = /* @__PURE__ */ new WeakMap();
    this._menuDetailThemeToken = 0;
    this._ambientLightLastSignature = "";
    this._ambientLightLastCallAt = 0;
    this._ambientLightLastBrightness = null;
    this._screensaverTimer = null;
    this._screensaverClockTimer = null;
    this._screensaverSuppressUntil = 0;
    this._screensaverLyricsInactiveSince = 0;
    this._state.mobileSmartVoice = null;
    this._state.simpleWizard = null;
    this._boundMobileMenuClick = this._handleMobileMenuClick.bind(this);
    this._boundMobileMenuChange = this._handleMobileMenuChange.bind(this);
    this._boundMobileMenuKeydown = this._handleMobileMenuKeydown.bind(this);
    this._boundMobileMenuScroll = this._handleMobileMenuScroll.bind(this);
    this._boundMobileMediaInput = this._handleMobileMediaInput.bind(this);
    this._boundScreensaverActivity = this._handleScreensaverActivity.bind(this);
    try {
      this._state.mobileCustomColor = localStorage.getItem("homeii_music_flow_mobile_custom_color") || "#f5a623";
    } catch (_) {
    }
    try {
      const storedPerformanceProfile = localStorage.getItem("homeii_music_flow_mobile_performance_profile");
      const storedPerformanceMode = localStorage.getItem("homeii_music_flow_mobile_performance_mode");
      if (storedPerformanceProfile !== null || storedPerformanceMode !== null) {
        const legacyPerformanceMode = storedPerformanceMode !== null ? JSON.parse(storedPerformanceMode) : false;
        const performanceProfile2 = HomeiiMobileSettingsFoundation.normalizePerformanceProfile(storedPerformanceProfile, legacyPerformanceMode);
        this._state.performanceProfile = performanceProfile2;
        this._state.performanceMode = ["low", "ultra_lite"].includes(performanceProfile2);
        this._state.performanceModeLocalOverride = true;
      }
    } catch (_) {
    }
    try {
      this._state.mobileDynamicThemeMode = localStorage.getItem("homeii_music_flow_mobile_dynamic_theme_mode") || "auto";
    } catch (_) {
    }
    try {
      this._state.mobileBackgroundMotionMode = localStorage.getItem("homeii_music_flow_mobile_background_motion_mode") || "subtle";
    } catch (_) {
    }
    try {
      this._state.mobileCustomTextTone = localStorage.getItem("homeii_music_flow_mobile_custom_text") || "light";
    } catch (_) {
    }
    try {
      this._state.frontPinnedPlayerEntity = localStorage.getItem("homeii_music_flow_front_pinned_player") || "";
    } catch (_) {
    }
    try {
      this._state.mobileFontScale = Math.max(0.5, Math.min(1.5, Number(localStorage.getItem("homeii_music_flow_mobile_font_scale") || 1) || 1));
    } catch (_) {
    }
    try {
      this._state.mobileIconScale = HomeiiMobileSettingsFoundation.clampMobileIconScale(localStorage.getItem("homeii_music_flow_mobile_icon_scale") || 1);
    } catch (_) {
    }
    this._loadControlRoomScenesFromStorage();
    try {
      this._state.mobileNightMode = localStorage.getItem("homeii_music_flow_mobile_night_mode") || "off";
    } catch (_) {
    }
    try {
      this._state.mobileNightModeStart = localStorage.getItem("homeii_music_flow_mobile_night_start") || "22:00";
    } catch (_) {
    }
    try {
      this._state.mobileNightModeEnd = localStorage.getItem("homeii_music_flow_mobile_night_end") || "06:00";
    } catch (_) {
    }
    try {
      this._state.mobileNightModeDays = this._normalizeNightModeDays(localStorage.getItem("homeii_music_flow_mobile_night_days"));
    } catch (_) {
    }
    try {
      this._state.mobileSleepTimerEndsAt = Number(localStorage.getItem("homeii_music_flow_mobile_sleep_timer_at") || 0) || 0;
    } catch (_) {
    }
    try {
      this._state.mobileSleepTimerPlayer = localStorage.getItem("homeii_music_flow_mobile_sleep_timer_player") || "";
    } catch (_) {
    }
    try {
      this._state.mobileSleepTimerOrigin = localStorage.getItem("homeii_music_flow_mobile_sleep_timer_origin") || "";
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_start_timer_enabled") ?? "false");
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerTime = this._normalizeClockTime(localStorage.getItem("homeii_music_flow_mobile_start_timer_time") || "07:00", "07:00");
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerPlayer = localStorage.getItem("homeii_music_flow_mobile_start_timer_player") || "";
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerPlaylist = localStorage.getItem("homeii_music_flow_mobile_start_timer_playlist") || "";
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerPlaylistName = localStorage.getItem("homeii_music_flow_mobile_start_timer_playlist_name") || "";
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerVolume = Math.max(0, Math.min(100, Number(localStorage.getItem("homeii_music_flow_mobile_start_timer_volume") || 35) || 35));
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerDays = this._normalizeNightModeDays(localStorage.getItem("homeii_music_flow_mobile_start_timer_days"));
    } catch (_) {
    }
    try {
      this._state.mobileStartTimerLastRunKey = localStorage.getItem("homeii_music_flow_mobile_start_timer_last_run") || "";
    } catch (_) {
    }
    try {
      this._state.mobileSchedulesTab = localStorage.getItem("homeii_music_flow_mobile_schedules_tab") || "timers";
    } catch (_) {
    }
    try {
      const schedules = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_start_schedules") || "[]");
      if (Array.isArray(schedules)) this._state.mobileStartSchedules = schedules.map((schedule, index) => this._normalizeScheduledStartSchedule(schedule, index));
    } catch (_) {
    }
    if (!this._state.mobileStartSchedules.length && this._state.mobileStartTimerEnabled) {
      this._state.mobileStartSchedules = [this._normalizeScheduledStartSchedule({
        id: "schedule_legacy",
        enabled: true,
        time: this._state.mobileStartTimerTime,
        player: this._state.mobileStartTimerPlayer,
        playlist: this._state.mobileStartTimerPlaylist,
        playlistName: this._state.mobileStartTimerPlaylistName,
        volume: this._state.mobileStartTimerVolume,
        days: this._state.mobileStartTimerDays,
        lastRunKey: this._state.mobileStartTimerLastRunKey
      })];
      this._state.mobileStartScheduleEditId = "schedule_legacy";
    }
    try {
      this._state.mobileLyricsSyncEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_lyrics_sync") ?? "true");
    } catch (_) {
    }
    try {
      this._state.mobileLyricsSyncOffsetMs = Math.max(-1e4, Math.min(1e4, Number(localStorage.getItem("homeii_music_flow_mobile_lyrics_offset_ms") || 0) || 0));
    } catch (_) {
    }
    try {
      this._state.mobileLyricsFontScale = Math.max(0.75, Math.min(1.4, Number(localStorage.getItem("homeii_music_flow_mobile_lyrics_font_scale") || 1) || 1));
    } catch (_) {
    }
    try {
      this._state.mobileCompactMode = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_compact_mode") ?? "false");
    } catch (_) {
    }
    try {
      this._state.mobileCompactWidgetMode = HomeiiMobileSettingsFoundation.normalizeMobileCompactWidgetMode(localStorage.getItem("homeii_music_flow_mobile_compact_widget_mode") || "auto");
    } catch (_) {
    }
    try {
      this._state.mobileCompactEdgeToEdge = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_compact_edge_to_edge") ?? "true");
    } catch (_) {
    }
    try {
      this._state.mobileLayoutMode = HomeiiMobileSettingsFoundation.normalizeMobileLayoutMode(localStorage.getItem("homeii_music_flow_mobile_layout_mode") || "auto");
    } catch (_) {
    }
    try {
      this._state.mobileLibraryDefaultLayout = HomeiiMobileSettingsFoundation.normalizeMobileLibraryDefaultLayout(
        localStorage.getItem("homeii_music_flow_mobile_library_default_layout") || "",
        this._defaultMobileMediaLayout()
      );
      this._state.mobileMediaLayout = this._state.mobileLibraryDefaultLayout;
    } catch (_) {
    }
    try {
      this._state.mobileShowUpNext = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_show_up_next") ?? "false");
    } catch (_) {
    }
    try {
      const rawHistory = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_recent_history") || "[]");
      if (Array.isArray(rawHistory)) this._state.mobileRecentHistory = rawHistory.slice(0, 10);
    } catch (_) {
    }
    try {
      this._state.mobileLibrarySort = localStorage.getItem("homeii_music_flow_mobile_library_sort") || "name_asc";
    } catch (_) {
    }
    try {
      this._state.mobileFooterSearchEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_footer_search") ?? "false");
    } catch (_) {
    }
    try {
      this._state.mobileStudioShortcutEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_studio_shortcut") ?? "true");
    } catch (_) {
    }
    try {
      this._state.mobileFooterMode = localStorage.getItem("homeii_music_flow_mobile_footer_mode") || "both";
    } catch (_) {
    }
    try {
      this._state.mobileHomeShortcutEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_home_shortcut") ?? "false");
    } catch (_) {
    }
    try {
      this._state.mobileHomeShortcutPath = localStorage.getItem("homeii_music_flow_mobile_home_shortcut_path") || "/";
    } catch (_) {
    }
    try {
      this._state.mobileVolumeMode = localStorage.getItem("homeii_music_flow_mobile_volume_mode") || "button";
    } catch (_) {
    }
    try {
      this._state.mobileVolumeStepButtonsEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_volume_step_buttons") ?? "false");
    } catch (_) {
    }
    try {
      this._state.mobileVolumeStepPercent = HomeiiMobileSettingsFoundation.clampMobileVolumeStepPercent(localStorage.getItem("homeii_music_flow_mobile_volume_step_percent") || 5);
    } catch (_) {
    }
    try {
      this._state.mobileMicMode = localStorage.getItem("homeii_music_flow_mobile_mic_mode") || "smart";
    } catch (_) {
    }
    try {
      this._state.voiceAssistantEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_voice_assistant_enabled") ?? "false");
    } catch {
    }
    try {
      this._state.voiceAssistantMode = HomeiiMobileSettingsFoundation.normalizeVoiceAssistantMode(localStorage.getItem("homeii_music_flow_voice_assistant_mode") || "hybrid");
    } catch {
    }
    try {
      this._state.voiceAssistantAgentId = localStorage.getItem("homeii_music_flow_voice_assistant_agent_id") || "";
    } catch {
    }
    try {
      this._state.voiceAssistantSpeakFeedback = JSON.parse(localStorage.getItem("homeii_music_flow_voice_assistant_speak_feedback") ?? "false");
    } catch {
    }
    try {
      this._state.mobileSwipeMode = localStorage.getItem("homeii_music_flow_mobile_swipe_mode") || "browse";
    } catch (_) {
    }
    try {
      this._state.mobileRadioBrowserCountry = localStorage.getItem("homeii_music_flow_mobile_radio_country") || "all";
    } catch (_) {
    }
    try {
      const rawTabs = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_library_tabs") || "[]");
      if (Array.isArray(rawTabs) && rawTabs.length) this._state.mobileLibraryTabs = rawTabs;
    } catch (_) {
    }
    try {
      const rawMainBar = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_main_bar_items") || "[]");
      if (Array.isArray(rawMainBar) && rawMainBar.length) this._state.mobileMainBarItems = rawMainBar;
    } catch (_) {
    }
    try {
      const storedQuickActions = localStorage.getItem("homeii_music_flow_mobile_quick_actions");
      if (storedQuickActions !== null) {
        const rawQuickActions = JSON.parse(storedQuickActions);
        if (Array.isArray(rawQuickActions)) this._state.mobileQuickActions = rawQuickActions;
      }
    } catch (_) {
    }
    try {
      const rawPinned = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_pinned_players") || "[]");
      if (Array.isArray(rawPinned)) this._state.pinnedPlayerEntities = rawPinned.filter(Boolean);
    } catch (_) {
    }
    if (!Array.isArray(this._state.pinnedPlayerEntities) || !this._state.pinnedPlayerEntities.length) {
      try {
        const legacyPinned = localStorage.getItem("homeii_music_flow_mobile_pinned_player") || "";
        this._state.pinnedPlayerEntities = legacyPinned ? [legacyPinned] : [];
      } catch (_) {
      }
    }
    try {
      this._state.mobileLikedMode = localStorage.getItem("homeii_music_flow_mobile_liked_mode") || "ma";
    } catch (_) {
    }
    try {
      const presets = JSON.parse(localStorage.getItem("homeii_music_flow_mobile_announcement_presets") || "[]");
      if (Array.isArray(presets) && presets.length) {
        this._state.mobileAnnouncementPresets = this._isDefaultAnnouncementPresetSet(presets) ? this._defaultAnnouncementPresets() : presets.slice(0, 3);
      }
    } catch (_) {
    }
    try {
      const announcementVolume = Number(localStorage.getItem("homeii_music_flow_mobile_announcement_volume"));
      if (Number.isFinite(announcementVolume)) this._state.mobileAnnouncementVolume = Math.max(20, Math.min(50, announcementVolume));
    } catch (_) {
    }
    try {
      this._state.mobileAnnouncementTtsEntity = localStorage.getItem("homeii_music_flow_mobile_announcement_tts_entity") || this._config?.announcement_tts_entity || "";
    } catch (_) {
    }
    try {
      this._state.mobileAnnouncementTtsLanguage = this._normalizeAnnouncementLanguage(localStorage.getItem("homeii_music_flow_mobile_announcement_tts_language") || this._config?.announcement_tts_language || "auto");
    } catch (_) {
    }
    try {
      this._state.ambientLightEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_ambient_light_enabled") ?? "false");
    } catch {
    }
    try {
      this._state.ambientLightEntities = HomeiiMobileSettingsFoundation.normalizeEntityList(JSON.parse(localStorage.getItem("homeii_music_flow_ambient_light_entities") || "[]"));
    } catch {
    }
    try {
      this._state.ambientLightPlayerMap = HomeiiMobileSettingsFoundation.normalizeStringArray(JSON.parse(localStorage.getItem("homeii_music_flow_ambient_light_player_map") || "[]"));
    } catch {
    }
    try {
      this._state.ambientLightBrightness = HomeiiMobileSettingsFoundation.clampPercent(localStorage.getItem("homeii_music_flow_ambient_light_brightness") || 35, 35, { min: 1, max: 100 });
    } catch {
    }
    try {
      this._state.ambientLightTransition = HomeiiMobileSettingsFoundation.clampSeconds(localStorage.getItem("homeii_music_flow_ambient_light_transition") || 3, 3, { min: 0, max: 120 });
    } catch {
    }
    try {
      this._state.ambientLightCooldown = HomeiiMobileSettingsFoundation.clampSeconds(localStorage.getItem("homeii_music_flow_ambient_light_cooldown") || 8, 8, { min: 0, max: 120 });
    } catch {
    }
    try {
      this._state.screensaverEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_screensaver_enabled") ?? "false");
    } catch {
    }
    try {
      this._state.screensaverControlsEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_screensaver_controls_enabled") ?? "false");
    } catch {
    }
    try {
      const storedScreensaverButtons = localStorage.getItem("homeii_music_flow_screensaver_control_buttons");
      if (storedScreensaverButtons !== null) {
        this._state.screensaverControlButtons = HomeiiMobileSettingsFoundation.normalizeScreensaverControlButtons(JSON.parse(storedScreensaverButtons), ["previous", "next"]);
      }
    } catch {
    }
    try {
      this._state.screensaverClockMode = HomeiiMobileSettingsFoundation.normalizeScreensaverClockMode(localStorage.getItem("homeii_music_flow_screensaver_clock_mode") || "digital");
    } catch {
    }
    try {
      this._state.screensaverTimeoutSeconds = HomeiiMobileSettingsFoundation.clampSeconds(localStorage.getItem("homeii_music_flow_screensaver_timeout_seconds") || 90, 90, { min: 15, max: 3600 });
    } catch {
    }
    try {
      this._state.screensaverMessage = localStorage.getItem("homeii_music_flow_screensaver_message") || "";
    } catch {
    }
    try {
      this._state.screensaverClockSize = HomeiiMobileSettingsFoundation.clampNumber(localStorage.getItem("homeii_music_flow_screensaver_clock_size") || 1, 1, { min: 0.75, max: 1.45 });
    } catch {
    }
    try {
      this._state.screensaverClockX = HomeiiMobileSettingsFoundation.clampNumber(localStorage.getItem("homeii_music_flow_screensaver_clock_x") || 82, 82, { min: 8, max: 92 });
    } catch {
    }
    try {
      this._state.screensaverClockY = HomeiiMobileSettingsFoundation.clampNumber(localStorage.getItem("homeii_music_flow_screensaver_clock_y") || 24, 24, { min: 8, max: 70 });
    } catch {
    }
    try {
      this._state.powerButtonEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_power_button_enabled") ?? "false");
    } catch {
    }
    try {
      this._state.powerButtonName = localStorage.getItem("homeii_music_flow_power_button_name") || "";
    } catch {
    }
    try {
      this._state.powerButtonIcon = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(localStorage.getItem("homeii_music_flow_power_button_icon") || "power");
    } catch {
    }
    try {
      this._state.powerButtonAction = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(localStorage.getItem("homeii_music_flow_power_button_action") || "stop_player");
    } catch {
    }
    try {
      this._state.powerButtonEntity = localStorage.getItem("homeii_music_flow_power_button_entity") || "";
    } catch {
    }
    try {
      this._state.auxiliaryButtons = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtons(JSON.parse(localStorage.getItem("homeii_music_flow_auxiliary_buttons") || "{}")).slice(1);
    } catch {
    }
    try {
      this._state.discoveryModeEnabled = JSON.parse(localStorage.getItem("homeii_music_flow_discovery_mode_enabled") ?? "true");
    } catch {
    }
    try {
      this._state.discoveryCategoryKey = localStorage.getItem("homeii_music_flow_discovery_category_key") || "pop";
    } catch {
    }
    try {
      this._state.discoveryGenreKey = localStorage.getItem("homeii_music_flow_discovery_genre_key") || "all";
    } catch {
    }
    try {
      this._state.excludedPlayerEntities = HomeiiMobileSettingsFoundation.normalizePinnedPlayerEntityList(JSON.parse(localStorage.getItem("homeii_music_flow_excluded_players") || "[]"));
    } catch {
    }
    try {
      this._state.playerSortMode = HomeiiMobileSettingsFoundation.normalizePlayerSortMode(localStorage.getItem("homeii_music_flow_player_sort_mode") || "default");
    } catch {
    }
    try {
      this._state.playerOrderEntities = HomeiiMobileSettingsFoundation.normalizePinnedPlayerEntityList(JSON.parse(localStorage.getItem("homeii_music_flow_player_order") || "[]"));
    } catch {
    }
  }
  _defaultMobileMediaLayout() {
    return HomeiiMobileSettingsFoundation.normalizeMobileLibraryDefaultLayout(
      this._state?.mobileLibraryDefaultLayout,
      HomeiiResponsiveFoundation.defaultMobileMediaLayout(this._layoutModeConfig())
    );
  }
  static getStubConfig() {
    return {
      ...HomeiiBaseMusicCard.getStubConfig(),
      show_ma_button: false,
      layout_mode: "auto",
      settings_source: "visual",
      night_mode: "off",
      night_mode_auto_start: "22:00",
      night_mode_auto_end: "06:00",
      night_mode_days: [0, 1, 2, 3, 4, 5, 6],
      mobile_show_up_next: false,
      favorite_button_entity: "",
      active_player_helper_entity: "",
      allow_local_likes: false,
      use_mass_queue_send_command: false,
      performance_profile: "full",
      performance_mode: false,
      mobile_custom_color: "#f5a623",
      mobile_dynamic_theme_mode: "auto",
      mobile_background_motion_mode: "subtle",
      mobile_custom_text_tone: "light",
      mobile_font_scale: 1,
      mobile_icon_scale: 1,
      mobile_compact_mode: false,
      mobile_compact_widget_mode: "auto",
      mobile_compact_edge_to_edge: true,
      mobile_layout_mode: "auto",
      mobile_library_default_layout: "list",
      mobile_footer_search_enabled: false,
      mobile_footer_mode: "both",
      mobile_studio_shortcut: true,
      mobile_home_shortcut: false,
      mobile_home_shortcut_path: "/",
      mobile_volume_mode: "button",
      mobile_volume_step_buttons: false,
      mobile_volume_step_percent: 5,
      mobile_mic_mode: "smart",
      voice_assistant_enabled: false,
      voice_assistant_mode: "hybrid",
      voice_assistant_agent_id: "",
      voice_assistant_speak_feedback: false,
      flow_assistant_response_timeout_ms: 18e3,
      flow_assistant_listen_timeout_ms: 12e3,
      flow_assistant_auto_close_ms: 4200,
      mobile_library_tabs: ["library_search", "library_playlists", "library_artists", "library_albums", "library_tracks", "library_radio", "library_podcasts"],
      mobile_main_bar_items: ["actions", "players", "library", "settings"],
      mobile_quick_actions: ["timer", "like", "lyrics", "queue", "radio", "history"],
      mobile_quick_action_1: "timer",
      mobile_quick_action_2: "like",
      mobile_quick_action_3: "lyrics",
      mobile_quick_action_4: "queue",
      mobile_quick_action_5: "radio",
      mobile_quick_action_6: "history",
      mobile_quick_action_7: "",
      mobile_quick_action_8: "",
      mobile_quick_action_9: "",
      mobile_quick_action_10: "",
      mobile_liked_mode: "ma",
      mobile_swipe_mode: "browse",
      mobile_radio_browser_country: "all",
      mobile_announcement_presets: [],
      mobile_announcement_volume: 20,
      announcement_tts_entity: "",
      announcement_tts_language: "auto",
      ambient_light_enabled: false,
      ambient_light_entities: [],
      ambient_light_player_map: [],
      ambient_light_brightness: 35,
      ambient_light_transition: 3,
      ambient_light_cooldown: 8,
      screensaver_enabled: false,
      screensaver_controls_enabled: false,
      screensaver_control_buttons: ["previous", "next"],
      screensaver_clock_mode: "digital",
      screensaver_timeout_seconds: 90,
      screensaver_message: "",
      screensaver_clock_size: 1,
      screensaver_clock_x: 82,
      screensaver_clock_y: 24,
      power_button_enabled: false,
      power_button_name: "",
      power_button_icon: "power",
      power_button_action: "stop_player",
      power_button_entity: "",
      aux_button_2_enabled: false,
      aux_button_2_name: "",
      aux_button_2_icon: "power",
      aux_button_2_action: "toggle",
      aux_button_2_entity: "",
      aux_button_3_enabled: false,
      aux_button_3_name: "",
      aux_button_3_icon: "power",
      aux_button_3_action: "toggle",
      aux_button_3_entity: "",
      aux_button_4_enabled: false,
      aux_button_4_name: "",
      aux_button_4_icon: "power",
      aux_button_4_action: "toggle",
      aux_button_4_entity: "",
      discovery_mode_enabled: true,
      pinned_player_entity: "",
      pinned_player_entities: [],
      excluded_player_entities: [],
      player_sort_mode: "default",
      player_order_entities: [],
      ...Object.fromEntries(Array.from({ length: 20 }, (_, index) => [`player_order_entity_${index + 1}`, ""]))
    };
  }
  static getConfigForm() {
    return getMobileCardConfigForm();
  }
  static assertConfig(config) {
    if (!config || typeof config !== "object" || Array.isArray(config)) {
      throw new Error("Card config must be an object");
    }
  }
  setConfig(config) {
    super.setConfig({
      ...HomeiiMusicFlowBaseCard.getStubConfig(),
      ...config,
      settings_source: HomeiiStateFoundation.normalizeSettingsSource(config?.settings_source)
    });
    this._applyConfiguredMobileSettings();
    this._syncScreensaverClockVars();
    if (this._state.screensaverOpen) this._syncScreensaverUi();
    this._hydrateSystemMobileState().catch(() => {
    });
  }
  set editMode(value) {
    const enabled = value === true || String(value || "").toLowerCase() === "true";
    if (this._editMode === enabled) return;
    this._editMode = enabled;
    if (!enabled) return;
    clearTimeout(this._screensaverTimer);
    this._screensaverTimer = null;
    this._hideScreensaver?.();
  }
  get editMode() {
    return this._editMode === true;
  }
  _getConfigValidator() {
    return HomeiiConfigValidators.validateMobileCardEditorConfig;
  }
  _usesVisualSettings() {
    return HomeiiStateFoundation.usesVisualSettings(this._config);
  }
  _performanceProfile() {
    return HomeiiStateFoundation.performanceProfile(this._state);
  }
  _performanceModeEnabled() {
    return HomeiiStateFoundation.performanceModeEnabled(this._state);
  }
  _performanceUltraLiteEnabled() {
    return HomeiiStateFoundation.performanceUltraLiteEnabled(this._state);
  }
  _mobileLayoutMode() {
    const mode = HomeiiMobileSettingsFoundation.normalizeMobileLayoutMode(this._state.mobileLayoutMode);
    this._state.mobileLayoutMode = mode;
    return mode;
  }
  _fullMobileInlineTargetHeight() {
    return 760;
  }
  _preferFullMobileGridRows() {
    const mode = this._mobileLayoutMode();
    return mode === "full" || mode === "auto" && !HomeiiStateFoundation.mobileCompactModeEnabled(this._state);
  }
  _autoCompactModeRecommended(options = {}) {
    const mode = this._mobileLayoutMode();
    if (mode === "full") return false;
    if (mode === "compact") return true;
    const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
    const viewportHeight = this._getViewportHeight(0);
    const width = Number(options?.width || 0) > 0 ? Number(options.width) : this._getCardWidth(this._lastCardWidth || viewportWidth || 0);
    const height = Number(options?.height || 0) > 0 ? Number(options.height) : this._getAllocatedCardHeight(viewportHeight) || viewportHeight;
    return HomeiiResponsiveFoundation.autoCompactRecommended({ width, height });
  }
  _mobileCompactModeEnabled() {
    const mode = this._mobileLayoutMode();
    if (mode === "full") return false;
    if (mode === "compact") return true;
    return HomeiiStateFoundation.mobileCompactModeEnabled(this._state) || this._autoCompactModeRecommended();
  }
  _mobileCompactWidgetMode() {
    return HomeiiStateFoundation.mobileCompactWidgetMode(this._state);
  }
  _mobileCompactEdgeToEdgeEnabled() {
    return true;
  }
  _compactEdgeToEdgeAllowed() {
    return true;
  }
  _compactEdgeToEdgeExpanded() {
    return !!(this._state?.mobileCompactExpanded && this._mobileCompactModeEnabled?.() && this._mobileCompactEdgeToEdgeEnabled?.() && this._compactEdgeToEdgeAllowed?.());
  }
  _compactMiniWidgetRecommended(options = {}) {
    const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
    const width = Number(options?.width || 0) > 0 ? Number(options.width) : this._getCardWidth(this._lastCardWidth || viewportWidth || 0);
    const height = Number(options?.height || 0);
    if (!Number.isFinite(width) || width <= 0) return false;
    if (width <= 430) return true;
    return Number.isFinite(height) && height > 0 && height <= 210;
  }
  _compactMiniWidgetMode(options = {}) {
    const mode = this._mobileCompactWidgetMode();
    if (mode === "mini") return true;
    if (mode === "full") return false;
    return this._compactMiniWidgetRecommended(options);
  }
  _mobileShowUpNextEnabled() {
    return HomeiiStateFoundation.mobileShowUpNextEnabled(this._state);
  }
  _mobileDynamicThemeMode() {
    if (this._performanceModeEnabled()) return "off";
    return HomeiiStateFoundation.mobileDynamicThemeMode(this._state);
  }
  _mobileBackgroundMotionMode() {
    if (this._performanceModeEnabled()) return "off";
    return HomeiiStateFoundation.mobileBackgroundMotionMode(this._state);
  }
  _backgroundMotionEnabled() {
    if (this._performanceModeEnabled()) return false;
    return HomeiiStateFoundation.backgroundMotionEnabled(this._state);
  }
  _backgroundMotionAmount() {
    if (this._performanceModeEnabled()) return "0";
    return HomeiiStateFoundation.backgroundMotionAmount(this._state);
  }
  _isCompactTileMode() {
    return this._mobileCompactModeEnabled() && !this._state?.mobileCompactExpanded;
  }
  _compactTileReservedHeight() {
    const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
    const cardWidth = this._getCardWidth(this._lastCardWidth || viewportWidth || 390);
    const width = Number.isFinite(cardWidth) && cardWidth > 0 ? cardWidth : 390;
    if (this._compactMiniWidgetMode({ width })) {
      return this._sectionGridHeightForRows(5);
    }
    const targetRows = width < 430 ? 8 : width < 560 ? 8 : width < 760 ? 7 : 6;
    return this._sectionGridHeightForRows(targetRows);
  }
  _setCompactExpanded(expanded) {
    const nextExpanded = !!expanded && this._mobileCompactModeEnabled();
    const previousExpanded = !!this._state.mobileCompactExpanded;
    this._state.mobileCompactTransition = nextExpanded ? "expand" : previousExpanded ? "collapse" : "";
    this._state.mobileCompactExpanded = nextExpanded;
    this._layoutResizeHoldUntil = Date.now() + 720;
    const edgeToEdge = nextExpanded && this._compactEdgeToEdgeAllowed();
    const windowPopup = nextExpanded && !edgeToEdge;
    this.classList.toggle("compact-popup-open", edgeToEdge);
    this.classList.toggle("compact-window-popup-open", windowPopup);
    this.classList.remove("compact-inline-popup-open");
    this.classList.toggle("compact-tile-open", !nextExpanded && this._mobileCompactModeEnabled());
    if (!nextExpanded && this._state.controlRoomOpen) {
      this._state.controlRoomOpen = false;
      this._state.controlRoomPanel = "";
      this._state.controlRoomRestoreAfterMenu = false;
      this._syncControlRoomChrome();
    }
    this._build();
    this._init();
    clearTimeout(this._compactTransitionTimer);
    this._compactTransitionTimer = setTimeout(() => {
      this.shadowRoot?.querySelector(".card")?.classList.remove("compact-transition-expand", "compact-transition-collapse");
      if (this._state.mobileCompactTransition === "expand" || this._state.mobileCompactTransition === "collapse") {
        this._state.mobileCompactTransition = "";
      }
    }, 460);
  }
  _applyConfiguredMobileSettings() {
    if (!this._config) return;
    const cfg = this._config;
    if (this._usesVisualSettings()) {
      const visualCfg = this._isDefaultAnnouncementPresetSet(cfg.mobile_announcement_presets) ? { ...cfg, mobile_announcement_presets: [] } : cfg;
      const previousLibraryDefaultLayout = this._state.mobileLibraryDefaultLayout || this._defaultMobileMediaLayout();
      const previousLibraryManual = this._state.mobileMediaLayoutManual === true;
      Object.assign(this._state, HomeiiMobileSettingsFoundation.normalizeVisualMobileState(visualCfg, {
        normalizeClockTime: (value, fallback) => this._normalizeClockTime(value, fallback),
        normalizeNightModeDays: (value) => this._normalizeNightModeDays(value),
        defaultLibraryTabs: this._defaultMobileLibraryTabs(),
        defaultMainBarItems: this._defaultMobileMainBarItems(),
        defaultQuickActions: this._defaultMobileQuickActions(),
        defaultAnnouncementPresets: this._defaultAnnouncementPresets(visualCfg.language || this._state.lang)
      }));
      if (this._state.mobileLibraryDefaultLayout !== previousLibraryDefaultLayout) this._state.mobileMediaLayoutManual = false;
      else this._state.mobileMediaLayoutManual = previousLibraryManual;
      if (!this._state.mobileMediaLayoutManual) this._state.mobileMediaLayout = this._defaultMobileMediaLayout();
      if (!this._mobileCompactModeEnabled()) this._state.mobileCompactExpanded = false;
    } else if (String(cfg.announcement_tts_entity || "").trim() && !String(this._state.mobileAnnouncementTtsEntity || "").trim()) {
      this._state.mobileAnnouncementTtsEntity = String(cfg.announcement_tts_entity || "").trim();
    }
    if (!this._usesVisualSettings() && String(cfg.announcement_tts_language || "").trim()) {
      this._state.mobileAnnouncementTtsLanguage = this._normalizeAnnouncementLanguage(cfg.announcement_tts_language);
    }
    if (!this._usesVisualSettings() && !this._state.performanceModeLocalOverride) {
      const performanceProfile2 = HomeiiMobileSettingsFoundation.normalizePerformanceProfile(cfg.performance_profile, cfg.performance_mode);
      this._state.performanceProfile = performanceProfile2;
      this._state.performanceMode = ["low", "ultra_lite"].includes(performanceProfile2);
    }
  }
  _layoutModeConfig(options = {}) {
    const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
    const viewportHeight = this._getViewportHeight(0);
    const compactPopup = options?.compactPopup === true || !!(this._state?.mobileCompactExpanded && this._mobileCompactModeEnabled?.());
    if (compactPopup) {
      const shortestSide = Math.min(
        Number(viewportWidth || 0) || Infinity,
        Number(viewportHeight || 0) || Infinity
      );
      const longestSide = Math.max(Number(viewportWidth || 0), Number(viewportHeight || 0));
      if (shortestSide < 760 && longestSide < 1180) return "mobile";
    }
    const optionWidth = Number(options?.width || 0);
    const cardWidth = optionWidth > 0 ? optionWidth : compactPopup ? this._getCardWidth(viewportWidth) : this._getCardWidth(this._lastCardWidth || viewportWidth);
    return HomeiiResponsiveFoundation.resolveLayoutMode(this._config?.layout_mode, {
      rectWidth: cardWidth,
      hostWidth: 0,
      viewportWidth: cardWidth > 0 ? 0 : viewportWidth
    });
  }
  _m(en, he, params = {}) {
    return translateText(
      this._language(),
      en,
      params,
      this._isHebrew() ? he : en
    );
  }
  _effectiveTheme() {
    if (this._isNightModeActive()) return "dark";
    if (this._state.cardTheme === "dark" || this._state.cardTheme === "light") return this._state.cardTheme;
    if (this._state.cardTheme === "custom") return this._customIsDark() ? "dark" : "light";
    return super._effectiveTheme();
  }
  _visualTheme() {
    if (this._isNightModeActive()) return "dark";
    if (this._state.cardTheme === "custom") return "custom";
    return this._effectiveTheme();
  }
  _customIsDark() {
    return HomeiiPaletteFoundation.isRgbTupleDark(
      HomeiiPaletteFoundation.hexToRgbTuple(this._state.mobileCustomColor || "#f5a623"),
      0.58
    );
  }
  _customTextColor() {
    return "#ffffff";
  }
  _customRgb() {
    return HomeiiPaletteFoundation.rgbTupleToString(
      HomeiiPaletteFoundation.hexToRgbTuple(this._state.mobileCustomColor || "#f5a623")
    );
  }
  _clampByte(value) {
    return HomeiiPaletteFoundation.clampRgbByte(value);
  }
  _normalizeRgbTuple(value, fallback = [245, 166, 35]) {
    return HomeiiPaletteFoundation.normalizeRgbTuple(value, fallback);
  }
  _rgbTupleToString(tuple = [245, 166, 35]) {
    return HomeiiPaletteFoundation.rgbTupleToString(tuple);
  }
  _rgbTupleToHex(tuple = [245, 166, 35]) {
    return HomeiiPaletteFoundation.rgbTupleToHex(tuple);
  }
  _mixRgb(left = [245, 166, 35], right = [255, 255, 255], ratio = 0.5) {
    return HomeiiPaletteFoundation.mixRgb(left, right, ratio);
  }
  _rgbToHsl(tuple = [245, 166, 35]) {
    return HomeiiPaletteFoundation.rgbToHsl(tuple);
  }
  _hslToRgb(hue = 0, saturation = 0, lightness = 0.5) {
    return HomeiiPaletteFoundation.hslToRgb(hue, saturation, lightness);
  }
  _tunePaletteColor(tuple = [245, 166, 35], options = {}) {
    return HomeiiPaletteFoundation.tunePaletteColor(tuple, options);
  }
  _dynamicThemePalette() {
    return this._mobileDynamicThemeMode() === "off" ? null : this._state.mobileDynamicThemePalette || null;
  }
  _dynamicThemeActive() {
    return !!this._dynamicThemePalette()?.accent;
  }
  _activeAccentColor() {
    return HomeiiPaletteFoundation.resolveActiveAccentColor(
      this._dynamicThemePalette(),
      this._state.mobileCustomColor || "#f5a623"
    );
  }
  _activeAccentRgb() {
    return HomeiiPaletteFoundation.resolveActiveAccentRgb(
      this._dynamicThemePalette(),
      this._state.mobileCustomColor || "#f5a623"
    );
  }
  _dynamicThemeStrengthValue() {
    return HomeiiPaletteFoundation.dynamicThemeStrengthValue(this._mobileDynamicThemeMode());
  }
  _dynamicThemeStyleSignature(artworkKey = "", artUrl2 = "") {
    const palette = this._dynamicThemePalette();
    const paletteKey = palette ? [
      palette.accent || "",
      palette.accent_rgb || "",
      palette.surface || "",
      palette.surface_rgb || "",
      palette.glow || "",
      palette.glow_rgb || "",
      palette.text || ""
    ].join(",") : "";
    return [
      artworkKey,
      artUrl2,
      this._mobileDynamicThemeMode(),
      this._effectiveTheme(),
      this._activeAccentColor(),
      this._dynamicThemeStrengthValue(),
      this._isHotelMode() ? "hotel" : "normal",
      paletteKey
    ].join("||");
  }
  _applyDynamicThemeRenderState(artworkKey = "", artUrl2 = "") {
    const signature = this._dynamicThemeStyleSignature(artworkKey, artUrl2);
    if (signature === this._mobileDynamicThemeAppliedSignature) return false;
    this._mobileDynamicThemeAppliedSignature = signature;
    this._applyDynamicThemeStyles();
    this._applyBackgroundMotionStyles();
    this._syncCurrentArtworkBackgrounds(artUrl2);
    return true;
  }
  _mobileBackdropOverlay(theme = this._effectiveTheme()) {
    const palette = this._dynamicThemePalette();
    if (!palette) {
      return theme === "light" ? `radial-gradient(circle at 18% 18%, rgba(255,187,88,.16), transparent 26%), radial-gradient(circle at 82% 14%, rgba(255,150,108,.1), transparent 18%), linear-gradient(180deg, rgba(255,255,255,.08), rgba(224,232,242,.3) 22%, rgba(197,208,222,.58) 62%, rgba(183,195,210,.74))` : `radial-gradient(circle at 18% 20%, rgba(255,181,64,.24), transparent 32%), radial-gradient(circle at 82% 16%, rgba(255,128,76,.12), transparent 20%), linear-gradient(180deg, rgba(9,12,19,.26), rgba(9,12,19,.82), rgba(9,12,19,.98))`;
    }
    const accent = palette.accent_rgb || this._activeAccentRgb();
    const surface = palette.surface_rgb || accent;
    const glow = palette.glow_rgb || accent;
    return theme === "light" ? `radial-gradient(circle at 18% 18%, rgba(${accent} / .16), transparent 28%), radial-gradient(circle at 82% 14%, rgba(${glow} / .1), transparent 20%), linear-gradient(180deg, rgba(255,255,255,.08), rgba(${surface} / .24) 22%, rgba(${surface} / .42) 60%, rgba(${surface} / .54))` : `radial-gradient(circle at 18% 20%, rgba(${accent} / .24), transparent 32%), radial-gradient(circle at 82% 16%, rgba(${glow} / .14), transparent 20%), linear-gradient(180deg, rgba(${surface} / .18), rgba(9,12,19,.82), rgba(9,12,19,.98))`;
  }
  _applyDynamicThemeStyles() {
    const host = this;
    const card = this.shadowRoot?.querySelector(".card");
    const accent = this._activeAccentColor();
    const palette = this._dynamicThemePalette();
    const artworkUrl = String(this._state.mobileDynamicThemeArtworkUrl || "").trim();
    const artworkCssUrl = artworkUrl ? `url(${JSON.stringify(artworkUrl)})` : "";
    host.style?.setProperty("--accent-color", accent);
    host.style?.setProperty("--ma-accent", accent);
    if (artworkCssUrl) {
      host.style?.setProperty("--dynamic-art-url", artworkCssUrl);
      card?.style?.setProperty("--dynamic-art-url", artworkCssUrl);
    } else {
      host.style?.removeProperty("--dynamic-art-url");
      card?.style?.removeProperty("--dynamic-art-url");
    }
    if (card) {
      card.style?.setProperty("--accent-color", accent);
      card.style?.setProperty("--ma-accent", accent);
      card.classList.toggle("dynamic-theme", !!palette);
    }
    if (!palette) {
      host.style?.removeProperty("--dynamic-accent-rgb");
      host.style?.removeProperty("--dynamic-surface-rgb");
      host.style?.removeProperty("--dynamic-glow-rgb");
      host.style?.removeProperty("--dynamic-theme-strength");
      card?.style?.removeProperty("--dynamic-accent-rgb");
      card?.style?.removeProperty("--dynamic-surface-rgb");
      card?.style?.removeProperty("--dynamic-glow-rgb");
      card?.style?.removeProperty("--dynamic-theme-strength");
      return;
    }
    const pairs = {
      "--dynamic-accent-rgb": palette.accent_rgb || this._activeAccentRgb(),
      "--dynamic-surface-rgb": palette.surface_rgb || this._activeAccentRgb(),
      "--dynamic-glow-rgb": palette.glow_rgb || this._activeAccentRgb(),
      "--dynamic-theme-strength": this._dynamicThemeStrengthValue()
    };
    Object.entries(pairs).forEach(([key, value]) => {
      host.style?.setProperty(key, value);
      card?.style?.setProperty(key, value);
    });
  }
  _applyBackgroundMotionStyles() {
    const host = this;
    const card = this.shadowRoot?.querySelector(".card");
    const mode = this._mobileBackgroundMotionMode();
    const vars = mode === "extreme" ? {
      "--bg-motion-strength": "1.85",
      "--bg-motion-shift": "78px",
      "--bg-motion-scale": "1.24",
      "--bg-motion-duration": "12s",
      "--glow-motion-duration": "8s",
      "--aura-motion-duration": "11s",
      "--shade-motion-duration": "9s"
    } : mode === "strong" ? {
      "--bg-motion-strength": "1.35",
      "--bg-motion-shift": "46px",
      "--bg-motion-scale": "1.18",
      "--bg-motion-duration": "20s",
      "--glow-motion-duration": "14s",
      "--aura-motion-duration": "18s",
      "--shade-motion-duration": "16s"
    } : {
      "--bg-motion-strength": "1",
      "--bg-motion-shift": "28px",
      "--bg-motion-scale": "1.13",
      "--bg-motion-duration": "24s",
      "--glow-motion-duration": "18s",
      "--aura-motion-duration": "22s",
      "--shade-motion-duration": "20s"
    };
    Object.entries(vars).forEach(([key, value]) => {
      host.style?.setProperty(key, value);
      card?.style?.setProperty(key, value);
    });
    card?.classList.toggle("background-motion", mode !== "off");
    card?.classList.toggle("motion-subtle", mode === "subtle");
    card?.classList.toggle("motion-strong", mode === "strong");
    card?.classList.toggle("motion-extreme", mode === "extreme");
  }
  _currentServerDynamicThemePalette() {
    const player = this._getSelectedPlayer?.() || null;
    const attrs = player?.attributes || {};
    const currentQueueItem = this._state.maQueueState?.current_item || null;
    const currentMedia = currentQueueItem?.media_item || {};
    const rawCurrentMedia = player?.__homeiiRawPlayer?.current_media || attrs.current_media || attrs.currentMedia || {};
    const candidates = [
      attrs.media_palette,
      attrs.current_media_palette,
      rawCurrentMedia?.palette,
      currentQueueItem?.palette,
      currentQueueItem?.media_palette,
      currentQueueItem?.streamdetails?.stream_metadata?.palette,
      currentMedia?.palette,
      currentMedia?.metadata?.palette
    ];
    for (const candidate of candidates) {
      const palette = HomeiiPaletteFoundation.normalizeMaPalette(candidate, {
        mode: this._mobileDynamicThemeMode()
      });
      if (palette) return palette;
    }
    return null;
  }
  _libraryDetailArtworkUrl(detail = {}, size = 960) {
    const mediaType = String(detail?.media_type || detail?.type || "").toLowerCase();
    const artistInfo = detail?.artistInfo || null;
    const candidates = mediaType === "artist" ? [artistInfo, detail, detail?.media_item, detail?.metadata] : [detail, detail?.media_item, detail?.album, detail?.metadata, artistInfo];
    for (const candidate of candidates) {
      const art = this._artUrl(candidate, { size });
      if (art) return art;
    }
    return "";
  }
  _libraryDetailServerPalette(detail = {}) {
    if (this._mobileDynamicThemeMode() === "off") return null;
    const mediaType = String(detail?.media_type || detail?.type || "").toLowerCase();
    const browse = mediaType === "album" ? this._albumBrowseState(detail) : null;
    const selectedAlbum = browse?.albums?.[browse.index] || null;
    const artistInfo = detail?.artistInfo || null;
    const candidates = [
      detail?.palette,
      detail?.media_palette,
      detail?.image_palette,
      detail?.color_palette,
      detail?.metadata?.palette,
      detail?.metadata?.media_palette,
      detail?.metadata?.image_palette,
      detail?.media_item?.palette,
      detail?.media_item?.media_palette,
      detail?.media_item?.metadata?.palette,
      detail?.album?.palette,
      detail?.album?.metadata?.palette,
      selectedAlbum?.palette,
      selectedAlbum?.media_palette,
      selectedAlbum?.metadata?.palette,
      artistInfo?.palette,
      artistInfo?.media_palette,
      artistInfo?.metadata?.palette
    ];
    for (const candidate of candidates) {
      const palette = HomeiiPaletteFoundation.normalizeMaPalette(candidate, {
        mode: this._mobileDynamicThemeMode()
      });
      if (palette) return palette;
    }
    return null;
  }
  _setMenuDetailPalette(menu = null, palette = null) {
    if (!menu) return;
    const keys = [
      "--menu-detail-accent-rgb",
      "--menu-detail-surface-rgb",
      "--menu-detail-glow-rgb",
      "--ma-accent",
      "--accent-color"
    ];
    if (!palette) {
      keys.forEach((key) => menu.style.removeProperty(key));
      menu.classList.remove("has-menu-detail-palette");
      return;
    }
    const accentRgb = palette.accent_rgb || this._activeAccentRgb();
    const pairs = {
      "--menu-detail-accent-rgb": accentRgb,
      "--menu-detail-surface-rgb": palette.surface_rgb || accentRgb,
      "--menu-detail-glow-rgb": palette.glow_rgb || accentRgb,
      "--ma-accent": palette.accent || this._activeAccentColor(),
      "--accent-color": palette.accent || this._activeAccentColor()
    };
    Object.entries(pairs).forEach(([key, value]) => menu.style.setProperty(key, value));
    menu.classList.add("has-menu-detail-palette");
  }
  _clearMenuDetailTheme(menu = null) {
    if (!menu) return;
    this._menuDetailThemeToken += 1;
    menu.classList.remove("has-menu-detail-theme");
    this._setMenuDetailPalette(menu, null);
  }
  _applyMenuDetailTheme(menu = null, detailArt = "", detail = {}) {
    if (!menu) return;
    const serverPalette = this._libraryDetailServerPalette(detail);
    menu.classList.toggle("has-menu-detail-theme", !!(detailArt || serverPalette));
    this._setMenuDetailPalette(menu, serverPalette);
    const token = ++this._menuDetailThemeToken;
    if (serverPalette || this._mobileDynamicThemeMode() === "off" || !detailArt) return;
    this._extractDynamicThemePalette(detailArt).then((palette) => {
      if (!palette || token !== this._menuDetailThemeToken) return;
      const currentMenu = this.$("mobileMenu");
      if (currentMenu !== menu || this._state.menuPage !== "media_detail") return;
      this._setMenuDetailPalette(menu, palette);
    }).catch(() => {
    });
  }
  _applyMenuLibraryThemeFromItems(menu = null, items = [], mediaType = "") {
    if (!menu) return;
    const item = (Array.isArray(items) ? items : []).find((entry) => this._artUrl(entry, { size: 960 }));
    const art = item ? this._artUrl(item, { size: 960 }) : "";
    if (!item || !art) return;
    menu.style.setProperty("--menu-dynamic-art", `url(${JSON.stringify(art)})`);
    menu.classList.add("has-menu-art");
    this._applyMenuDetailTheme(menu, art, { ...item, media_type: item.media_type || item.type || mediaType });
  }
  async _extractDynamicThemePalette(artUrl2 = "") {
    const normalizedArt = String(artUrl2 || "").trim();
    const cacheKey = `${this._mobileDynamicThemeMode()}:${normalizedArt}`;
    if (!normalizedArt) return null;
    if (this._mobileDynamicThemePaletteCache.has(cacheKey)) {
      return this._mobileDynamicThemePaletteCache.get(cacheKey);
    }
    const promise = new Promise((resolve) => {
      try {
        const img = new Image();
        img.crossOrigin = "anonymous";
        img.referrerPolicy = "no-referrer";
        img.decoding = "async";
        img.onload = () => {
          try {
            const canvas = document.createElement("canvas");
            const size = 40;
            canvas.width = size;
            canvas.height = size;
            const ctx = canvas.getContext("2d", { willReadFrequently: true });
            if (!ctx) {
              resolve(null);
              return;
            }
            ctx.drawImage(img, 0, 0, size, size);
            const { data } = ctx.getImageData(0, 0, size, size);
            let sum = [0, 0, 0];
            let sumWeight = 0;
            let vivid = [0, 0, 0];
            let vividWeight = 0;
            for (let index = 0; index < data.length; index += 16) {
              const alpha = (data[index + 3] || 0) / 255;
              if (alpha < 0.08) continue;
              const rgb = [data[index], data[index + 1], data[index + 2]];
              const [hue, saturation, lightness] = this._rgbToHsl(rgb);
              const balancedLight = 1 - Math.abs(lightness - 0.52);
              const weight = alpha * (0.35 + saturation * 0.9 + balancedLight * 0.55);
              sum = sum.map((entry, rgbIndex) => entry + rgb[rgbIndex] * weight);
              sumWeight += weight;
              const vividSample = this._tunePaletteColor(rgb, { minSaturation: 0.48, minLightness: 0.4, maxLightness: 0.58 });
              const vividSampleWeight = alpha * (0.2 + saturation * 1.9 + balancedLight * 0.85 + hue * 0.05);
              vivid = vivid.map((entry, rgbIndex) => entry + vividSample[rgbIndex] * vividSampleWeight);
              vividWeight += vividSampleWeight;
            }
            if (!sumWeight || !vividWeight) {
              resolve(null);
              return;
            }
            const base = sum.map((entry) => this._clampByte(entry / sumWeight));
            const vividTuple = vivid.map((entry) => this._clampByte(entry / vividWeight));
            resolve(HomeiiPaletteFoundation.buildDynamicThemePalette({
              baseTuple: base,
              vividTuple,
              mode: this._mobileDynamicThemeMode()
            }));
          } catch (_) {
            resolve(null);
          }
        };
        img.onerror = () => resolve(null);
        img.src = normalizedArt;
      } catch (_) {
        resolve(null);
      }
    });
    this._mobileDynamicThemePaletteCache.set(cacheKey, promise);
    const resolved = await promise;
    this._mobileDynamicThemePaletteCache.set(cacheKey, resolved);
    return resolved;
  }
  async _syncDynamicThemeArtwork(artUrl2 = "") {
    const normalizedArt = String(artUrl2 || "").trim();
    const mode = this._mobileDynamicThemeMode();
    const artworkKey = normalizedArt ? `${mode}:${normalizedArt}` : "";
    if (mode === "off" || !normalizedArt) {
      this._mobileDynamicThemeToken += 1;
      this._state.mobileDynamicThemeArtwork = "";
      this._state.mobileDynamicThemeArtworkUrl = "";
      this._state.mobileDynamicThemePalette = null;
      this._applyDynamicThemeRenderState(`off:${normalizedArt}`, normalizedArt);
      this._syncAmbientLightForCurrentMedia("theme-off");
      return;
    }
    if (this._state.mobileDynamicThemeArtwork === artworkKey) {
      this._state.mobileDynamicThemeArtworkUrl = normalizedArt;
      const serverPalette2 = this._currentServerDynamicThemePalette();
      if (serverPalette2) {
        this._state.mobileDynamicThemePalette = serverPalette2;
        this._state.controlRoomRenderedHtml = "";
        this._state.controlRoomRenderSignature = "";
      }
      this._applyDynamicThemeRenderState(artworkKey, normalizedArt);
      this._syncAmbientLightForCurrentMedia("theme-cache");
      return;
    }
    this._state.mobileDynamicThemeArtwork = artworkKey;
    this._state.mobileDynamicThemeArtworkUrl = normalizedArt;
    const token = ++this._mobileDynamicThemeToken;
    const serverPalette = this._currentServerDynamicThemePalette();
    const palette = serverPalette || await this._extractDynamicThemePalette(normalizedArt);
    if (token !== this._mobileDynamicThemeToken) return;
    this._state.mobileDynamicThemePalette = palette;
    this._state.controlRoomRenderedHtml = "";
    this._state.controlRoomRenderSignature = "";
    this._applyDynamicThemeRenderState(artworkKey, normalizedArt);
    this._syncAmbientLightForCurrentMedia("theme-palette");
    if (this._state.controlRoomOpen) this._syncControlRoomUi();
  }
  _mobileNightMode() {
    return HomeiiNightFoundation.normalizeNightMode(this._state.mobileNightMode);
  }
  _normalizeClockTime(value, fallback = "22:00") {
    return HomeiiNightFoundation.normalizeClockTime(value, fallback);
  }
  _clockMinutesOfDay(value, fallback = "22:00") {
    return HomeiiNightFoundation.clockMinutesOfDay(value, fallback);
  }
  _defaultNightModeDays() {
    return HomeiiNightFoundation.defaultNightModeDays();
  }
  _normalizeNightModeDays(value) {
    return HomeiiNightFoundation.normalizeNightModeDays(value, this._defaultNightModeDays());
  }
  _nightModeDays() {
    return this._normalizeNightModeDays(this._state.mobileNightModeDays);
  }
  _nightModeDayOptions() {
    return [
      [0, this._i18n("ui.sun")],
      [1, this._i18n("ui.mon")],
      [2, this._i18n("ui.tue")],
      [3, this._i18n("ui.wed")],
      [4, this._i18n("ui.thu")],
      [5, this._i18n("ui.fri")],
      [6, this._i18n("ui.sat")]
    ];
  }
  _nightModeWindow() {
    return HomeiiNightFoundation.resolveNightModeWindow(
      this._state.mobileNightModeStart || "22:00",
      this._state.mobileNightModeEnd || "06:00",
      { start: "22:00", end: "06:00" }
    );
  }
  _isMinutesInsideWindow(minutes, startMinutes, endMinutes) {
    return HomeiiNightFoundation.isMinutesInsideWindow(minutes, startMinutes, endMinutes);
  }
  _isNightModeActive(date = /* @__PURE__ */ new Date()) {
    const windowRange = this._nightModeWindow();
    return HomeiiNightFoundation.isNightModeActive({
      mode: this._mobileNightMode(),
      start: windowRange.start,
      end: windowRange.end,
      days: this._nightModeDays(),
      date
    });
  }
  _sleepTimerRemainingMs(now = Date.now()) {
    return HomeiiNightFoundation.sleepTimerRemainingMs(this._state.mobileSleepTimerEndsAt || 0, now);
  }
  _sleepTimerRemainingLabel() {
    return HomeiiNightFoundation.sleepTimerRemainingLabel(this._sleepTimerRemainingMs());
  }
  _sleepTimerFooterLabel() {
    return HomeiiNightFoundation.sleepTimerFooterLabel(this._sleepTimerRemainingMs());
  }
  _sleepTimerStartedFromNightMode() {
    return HomeiiNightFoundation.sleepTimerStartedFromNightMode(
      this._sleepTimerRemainingMs(),
      this._state.mobileSleepTimerOrigin || ""
    );
  }
  _sleepTimerChipVisible() {
    return HomeiiNightFoundation.sleepTimerChipVisible(
      this._sleepTimerRemainingMs(),
      this._state.mobileSleepTimerOrigin || ""
    );
  }
  _sleepTimerCornerInnerHtml() {
    const label = this._sleepTimerFooterLabel();
    const active = !!label && this._sleepTimerChipVisible();
    if (!active) return "";
    const menuOpen = !!this._state.mobileSleepTimerMenuOpen;
    return `
      <div class="sleep-timer-menu" id="sleepTimerMenu"${menuOpen ? `` : ` hidden`}>
        <button class="sleep-timer-menu-btn" data-sleep-timer-add="15">+15</button>
        <button class="sleep-timer-menu-btn" data-sleep-timer-add="30">+30</button>
        <button class="sleep-timer-menu-btn" data-sleep-timer-add="60">+60</button>
        <button class="sleep-timer-menu-btn danger" data-sleep-timer-clear>${this._esc(this._i18n("ui.cancel_2"))}</button>
        <button class="sleep-timer-menu-btn ghost" data-sleep-timer-close>${this._esc(this._i18n("ui.close"))}</button>
      </div>
      <button class="sleep-timer-chip active" id="sleepTimerChip" title="${this._esc(this._i18n("ui.sleep_timer"))}">
        ${this._iconSvg("timer")}
        <span id="sleepTimerChipLabel">${this._esc(label)}</span>
      </button>
    `;
  }
  _scheduledStartDays() {
    return this._normalizeNightModeDays(this._state.mobileStartTimerDays);
  }
  _newScheduledStartId() {
    return `schedule_${Date.now()}_${Math.random().toString(36).slice(2, 7)}`;
  }
  _normalizeScheduledStartSchedule(schedule = {}, index = 0) {
    const id = String(schedule?.id || "").trim() || `schedule_${index + 1}`;
    const volume = Math.max(0, Math.min(100, Number(schedule?.volume ?? schedule?.mobileStartTimerVolume ?? 35) || 35));
    const afterRun = ["disable", "off"].includes(String(schedule?.afterRun || schedule?.after_run || "").trim()) ? "disable" : "keep";
    return {
      id,
      enabled: schedule?.enabled !== false,
      time: this._normalizeClockTime(schedule?.time || "07:00", "07:00"),
      player: String(schedule?.player || "").trim(),
      playlist: String(schedule?.playlist || "").trim(),
      playlistName: String(schedule?.playlistName || "").trim(),
      volume,
      days: this._normalizeNightModeDays(schedule?.days),
      lastRunKey: String(schedule?.lastRunKey || "").trim(),
      afterRun
    };
  }
  _scheduledStartSchedules() {
    const raw = Array.isArray(this._state.mobileStartSchedules) ? this._state.mobileStartSchedules : [];
    const schedules = raw.map((schedule, index) => this._normalizeScheduledStartSchedule(schedule, index)).filter((schedule, index, list) => schedule.id && list.findIndex((candidate) => candidate.id === schedule.id) === index);
    this._state.mobileStartSchedules = schedules;
    return schedules;
  }
  _activeScheduledStartSchedules() {
    return this._scheduledStartSchedules().filter((schedule) => schedule.enabled !== false);
  }
  _scheduledStartPlayerId(schedule = null) {
    const configured = String(schedule?.player || this._state.mobileStartTimerPlayer || "").trim();
    if (configured && this._playerByEntityId(configured)) return configured;
    return String(this._state.selectedPlayer || this._getSelectedPlayer()?.entity_id || "").trim();
  }
  _scheduledStartMorningKeywords() {
    return [
      "morning",
      "sunrise",
      "coffee",
      "breakfast",
      "wake",
      "wakeup",
      "wake up",
      "calm",
      "soft",
      "easy",
      "acoustic",
      "chill",
      "lofi",
      "lo-fi",
      "pleasant",
      "בוקר",
      "זריחה",
      "קפה",
      "רגוע",
      "רגועה",
      "שקט",
      "שקטה",
      "נעים",
      "נעימה",
      "צ'יל",
      "צ׳יל"
    ];
  }
  async _loadScheduledStartPlaylists(force = false) {
    const now = Date.now();
    const cached = Array.isArray(this._state.mobileStartTimerPlaylists) ? this._state.mobileStartTimerPlaylists : [];
    const fresh = cached.length && !force && now - Number(this._state.mobileStartTimerPlaylistsFetchedAt || 0) < 10 * 60 * 1e3;
    if (fresh || this._state.mobileStartTimerPlaylistsLoading) return cached;
    this._state.mobileStartTimerPlaylistsLoading = true;
    try {
      const [allPlaylists, likedPlaylists, randomPlaylists] = await Promise.allSettled([
        this._fetchLibrary("playlist", "sort_name", 500, false),
        this._fetchLibrary("playlist", "sort_name", 220, true),
        this._fetchLibrary("playlist", "random", 80, false)
      ]);
      const playlists = [
        ...Array.isArray(allPlaylists.value) ? allPlaylists.value : [],
        ...Array.isArray(likedPlaylists.value) ? likedPlaylists.value : [],
        ...Array.isArray(randomPlaylists.value) ? randomPlaylists.value : []
      ].map((item) => this._normalizeMediaItem(item)).filter((item) => String(item?.uri || "").trim()).filter((item) => String(item?.media_type || "playlist").toLowerCase() === "playlist").filter((item, index, list) => list.findIndex((candidate) => String(candidate?.uri || "").trim() === String(item?.uri || "").trim()) === index);
      this._state.mobileStartTimerPlaylists = playlists;
      this._state.mobileStartTimerPlaylistsFetchedAt = Date.now();
      if (!Array.isArray(this._state.mobileRecommendationPlaylists) || !this._state.mobileRecommendationPlaylists.length) {
        this._state.mobileRecommendationPlaylists = playlists.slice(0, 24);
        this._state.mobileRecommendationPlaylistsFetchedAt = Date.now();
      }
      return playlists;
    } catch (_) {
      return cached;
    } finally {
      this._state.mobileStartTimerPlaylistsLoading = false;
    }
  }
  _scheduledStartPlaylistLabel(schedule = null) {
    const selected = String(schedule?.playlist || this._state.mobileStartTimerPlaylist || "").trim();
    if (!selected) return this._i18n("ui.random_gentle_morning_mix");
    const playlists = Array.isArray(this._state.mobileStartTimerPlaylists) ? this._state.mobileStartTimerPlaylists : [];
    const match = playlists.find((item) => String(item?.uri || "").trim() === selected);
    return match?.name || match?.title || schedule?.playlistName || this._state.mobileStartTimerPlaylistName || this._i18n("ui.selected_playlist");
  }
  _scheduledStartPlaylistOptionsHtml(schedule = null) {
    const selected = String(schedule?.playlist || this._state.mobileStartTimerPlaylist || "").trim();
    const playlists = Array.isArray(this._state.mobileStartTimerPlaylists) ? this._state.mobileStartTimerPlaylists : [];
    const selectedKnown = selected && playlists.some((item) => String(item?.uri || "").trim() === selected);
    const options = [
      `<option value="" ${selected ? "" : "selected"}>${this._esc(this._i18n("ui.random_gentle_morning_mix"))}</option>`
    ];
    if (selected && !selectedKnown) {
      options.push(`<option value="${this._esc(selected)}" selected>${this._esc(schedule?.playlistName || this._state.mobileStartTimerPlaylistName || this._i18n("ui.selected_playlist"))}</option>`);
    }
    playlists.forEach((item) => {
      const uri = String(item?.uri || "").trim();
      if (!uri) return;
      const name = item.name || item.title || uri;
      options.push(`<option value="${this._esc(uri)}" ${uri === selected ? "selected" : ""}>${this._esc(name)}</option>`);
    });
    return options.join("");
  }
  _pickScheduledStartPlaylist(playlists = [], schedule = null) {
    const selected = String(schedule?.playlist || this._state.mobileStartTimerPlaylist || "").trim();
    const candidates = (Array.isArray(playlists) ? playlists : []).map((item) => this._normalizeMediaItem(item)).filter((item) => String(item?.uri || "").trim()).filter((item) => String(item?.media_type || "playlist").toLowerCase() === "playlist");
    if (!candidates.length) return null;
    if (selected) {
      const match = candidates.find((item) => String(item?.uri || "").trim() === selected);
      if (match) return match;
    }
    const keywords = this._scheduledStartMorningKeywords();
    const morningMatches = candidates.filter((item) => {
      const haystack = [
        item?.name,
        item?.title,
        item?.metadata?.description,
        item?.description,
        item?.provider_label
      ].filter(Boolean).join(" ").toLowerCase();
      return keywords.some((keyword) => haystack.includes(keyword));
    });
    const pool = morningMatches.length ? morningMatches : candidates;
    return pool[Math.floor(Math.random() * pool.length)] || null;
  }
  _scheduledStartStatusLabel() {
    const schedules = this._scheduledStartSchedules();
    const activeSchedules = schedules.filter((schedule2) => schedule2.enabled !== false);
    if (!activeSchedules.length) {
      return this._i18n("ui.no_scheduled_start_is_active");
    }
    if (activeSchedules.length > 1) {
      return this._i18n("ui.scheduled_starts_active_count", { count: activeSchedules.length });
    }
    const schedule = activeSchedules[0];
    const player = this._playerByEntityId(this._scheduledStartPlayerId(schedule));
    const playerName = player?.attributes?.friendly_name || this._i18n("ui.selected_player_3");
    const dayLabels = this._nightModeDayOptions().filter(([value]) => this._normalizeNightModeDays(schedule.days).includes(value)).map(([, label]) => label).join(" ");
    const time = this._normalizeClockTime(schedule.time || "07:00", "07:00");
    const volume = Math.max(0, Math.min(100, Number(schedule.volume || 35) || 35));
    const playlist = this._scheduledStartPlaylistLabel(schedule);
    return `${time} · ${playerName} · ${playlist} · ${volume}% · ${dayLabels}`;
  }
  _setScheduledStartFromMenu() {
    const timeInput = this.$("scheduledStartTimeInput");
    const playerSelect = this.$("scheduledStartPlayerSelect");
    const playlistSelect = this.$("scheduledStartPlaylistSelect");
    const volumeInput = this.$("scheduledStartVolumeInput");
    const afterRunSelect = this.$("scheduledStartAfterRunSelect");
    const checkedDays = Array.from(this.shadowRoot?.querySelectorAll("input[data-start-timer-day]:checked") || []).map((input) => Number(input.dataset.startTimerDay)).filter((value) => Number.isInteger(value) && value >= 0 && value <= 6);
    const playerId = String(playerSelect?.value || this._state.selectedPlayer || "").trim();
    if (!playerId) {
      this._toastError(this._i18n("ui.select_a_player_first"));
      return false;
    }
    const editId = String(this._state.mobileStartScheduleEditId || "").trim();
    const schedule = this._normalizeScheduledStartSchedule({
      id: editId && editId !== "__new__" ? editId : this._newScheduledStartId(),
      enabled: true,
      time: this._normalizeClockTime(timeInput?.value || "07:00", "07:00"),
      player: playerId,
      playlist: String(playlistSelect?.value || "").trim(),
      playlistName: String(playlistSelect?.value || "").trim() ? String(playlistSelect?.selectedOptions?.[0]?.textContent || "").trim() : "",
      volume: Math.max(0, Math.min(100, Number(volumeInput?.value || 35) || 35)),
      days: this._normalizeNightModeDays(checkedDays),
      lastRunKey: "",
      afterRun: String(afterRunSelect?.value || "keep") === "disable" ? "disable" : "keep"
    });
    const schedules = this._scheduledStartSchedules();
    const existingIndex = schedules.findIndex((item) => item.id === schedule.id);
    if (existingIndex >= 0) schedules[existingIndex] = schedule;
    else schedules.push(schedule);
    this._state.mobileStartSchedules = schedules;
    this._state.mobileStartScheduleEditId = "";
    this._state.mobileStartTimerEnabled = schedules.some((item) => item.enabled !== false);
    this._state.mobileStartTimerTime = schedule.time;
    this._state.mobileStartTimerPlayer = schedule.player;
    this._state.mobileStartTimerPlaylist = schedule.playlist;
    this._state.mobileStartTimerPlaylistName = schedule.playlistName;
    this._state.mobileStartTimerVolume = schedule.volume;
    this._state.mobileStartTimerDays = schedule.days;
    this._state.mobileStartTimerLastRunKey = schedule.lastRunKey;
    this._state.mobileStartTimerAfterRun = schedule.afterRun || "keep";
    this._persistMobileAppearance();
    this._toastSuccess(this._i18n("ui.scheduled_start_saved"));
    return true;
  }
  _clearScheduledStart(showToast = false) {
    const editId = String(this._state.mobileStartScheduleEditId || "").trim();
    if (editId && editId !== "__new__") {
      this._state.mobileStartSchedules = this._scheduledStartSchedules().filter((schedule) => schedule.id !== editId);
    } else if (!editId) {
      this._state.mobileStartSchedules = [];
    }
    this._state.mobileStartScheduleEditId = "";
    this._state.mobileStartTimerEnabled = this._activeScheduledStartSchedules().length > 0;
    this._state.mobileStartTimerLastRunKey = "";
    this._persistMobileAppearance();
    if (showToast) this._toast(this._i18n("ui.scheduled_start_cleared"));
  }
  _editScheduledStart(id = "") {
    const schedule = this._scheduledStartSchedules().find((item) => item.id === id);
    if (!schedule) return false;
    this._state.mobileStartScheduleEditId = schedule.id;
    this._state.mobileStartTimerEnabled = schedule.enabled !== false;
    this._state.mobileStartTimerTime = schedule.time;
    this._state.mobileStartTimerPlayer = schedule.player;
    this._state.mobileStartTimerPlaylist = schedule.playlist;
    this._state.mobileStartTimerPlaylistName = schedule.playlistName;
    this._state.mobileStartTimerVolume = schedule.volume;
    this._state.mobileStartTimerDays = schedule.days;
    this._state.mobileStartTimerLastRunKey = schedule.lastRunKey || "";
    this._state.mobileStartTimerAfterRun = schedule.afterRun || "keep";
    return true;
  }
  _newScheduledStartDraft() {
    this._state.mobileStartScheduleEditId = "__new__";
    this._state.mobileStartTimerEnabled = false;
    this._state.mobileStartTimerTime = "07:00";
    this._state.mobileStartTimerPlayer = this._state.selectedPlayer || "";
    this._state.mobileStartTimerPlaylist = "";
    this._state.mobileStartTimerPlaylistName = "";
    this._state.mobileStartTimerVolume = 35;
    this._state.mobileStartTimerDays = [0, 1, 2, 3, 4, 5, 6];
    this._state.mobileStartTimerLastRunKey = "";
    this._state.mobileStartTimerAfterRun = "keep";
  }
  _toggleScheduledStart(id = "") {
    const schedules = this._scheduledStartSchedules();
    const index = schedules.findIndex((schedule) => schedule.id === id);
    if (index < 0) return false;
    schedules[index] = { ...schedules[index], enabled: schedules[index].enabled === false };
    this._state.mobileStartSchedules = schedules;
    this._state.mobileStartTimerEnabled = schedules.some((item) => item.enabled !== false);
    this._persistMobileAppearance();
    return true;
  }
  _deleteScheduledStart(id = "") {
    const schedules = this._scheduledStartSchedules().filter((schedule) => schedule.id !== id);
    this._state.mobileStartSchedules = schedules;
    if (this._state.mobileStartScheduleEditId === id) this._state.mobileStartScheduleEditId = "";
    this._state.mobileStartTimerEnabled = schedules.some((item) => item.enabled !== false);
    this._persistMobileAppearance();
    return true;
  }
  async _runScheduledStart(entityId, schedule = null) {
    if (!entityId || this._state.mobileStartTimerRunPending) return;
    this._state.mobileStartTimerRunPending = true;
    try {
      const activeSchedule = schedule ? this._normalizeScheduledStartSchedule(schedule) : null;
      const volume = Math.max(0, Math.min(100, Number(activeSchedule?.volume ?? this._state.mobileStartTimerVolume ?? 35) || 35));
      this._setPlayerVolumeFor(entityId, volume / 100);
      const playlists = await this._loadScheduledStartPlaylists();
      const pick = this._pickScheduledStartPlaylist(playlists, activeSchedule);
      let ok = false;
      if (pick?.uri) {
        ok = await this._playMediaOnPlayer(entityId, pick.uri, pick.media_type || "playlist", "play", {
          label: pick.name || pick.title || this._i18n("ui.morning_mix"),
          silent: true
        });
      }
      if (!ok) {
        if (this._isDirectMaPlayer(entityId)) {
          await this._callDirectMaPlayerCommand(entityId, "players/cmd/play");
          await this._refreshDirectMaPlayers().catch(() => {
          });
        } else {
          await this._hass?.callService?.("media_player", "media_play", { entity_id: entityId });
        }
      }
      const label = pick?.name || pick?.title || this._i18n("ui.scheduled_start");
      this._toastSuccess(this._i18n("ui.scheduled_start_activated_label", { label }));
    } catch (error) {
      this._toastError(error?.message || this._i18n("ui.scheduled_start_failed"));
    } finally {
      this._state.mobileStartTimerRunPending = false;
    }
  }
  _syncScheduledStartState(date = /* @__PURE__ */ new Date()) {
    const schedules = this._activeScheduledStartSchedules();
    if (!schedules.length) return;
    let changed = false;
    schedules.forEach((schedule) => {
      const time = this._normalizeClockTime(schedule.time || "07:00", "07:00");
      const [hours, minutes] = time.split(":").map((part) => Number(part) || 0);
      if (date.getHours() !== hours || date.getMinutes() !== minutes) return;
      const enabledDays = new Set(this._normalizeNightModeDays(schedule.days));
      if (!enabledDays.has(Number(date.getDay()))) return;
      const runKey = `${schedule.id}-${date.getFullYear()}-${date.getMonth() + 1}-${date.getDate()}-${time}`;
      if (schedule.lastRunKey === runKey) return;
      const entityId = this._scheduledStartPlayerId(schedule);
      if (!entityId) return;
      schedule.lastRunKey = runKey;
      if (schedule.afterRun === "disable") schedule.enabled = false;
      changed = true;
      this._runScheduledStart(entityId, schedule).catch((error) => {
        this._toastError(error?.message || this._i18n("ui.scheduled_start_failed"));
      });
    });
    if (changed) {
      const byId = new Map(this._state.mobileStartSchedules.map((schedule) => [schedule.id, schedule]));
      schedules.forEach((schedule) => byId.set(schedule.id, schedule));
      this._state.mobileStartSchedules = Array.from(byId.values());
      this._state.mobileStartTimerEnabled = this._state.mobileStartSchedules.some((schedule) => schedule.enabled !== false);
      this._persistMobileAppearance();
    }
  }
  _tabletAutoFitEnabled() {
    return HomeiiResponsiveFoundation.tabletAutoFitEnabled(this._layoutModeConfig());
  }
  _tabletAutoFitDense(showNightRow = this._mobileNightMode() !== "off", showUpNext = false) {
    return HomeiiResponsiveFoundation.tabletAutoFitDense(this._layoutModeConfig(), {
      showNightRow,
      showUpNext
    });
  }
  _mobileContentDense(layoutMode = this._layoutModeConfig(), layoutProfile = null, options = {}) {
    if (layoutMode === "tablet") return false;
    const profile = layoutProfile || this._layoutProfileConfig(layoutMode);
    const width = Number(profile?.width || this._getCardWidth(this._lastCardWidth || 0));
    const height = Number(profile?.height || this._lastCardHeight || 0);
    const showNightRow = typeof options.showNightRow === "boolean" ? options.showNightRow : this._mobileNightMode() !== "off";
    const showUpNextInline = typeof options.showUpNextInline === "boolean" ? options.showUpNextInline : this._mobileShowUpNextEnabled() && !!this._mobileUpNextItem();
    const narrowPhone = width > 0 && width <= 620;
    const kioskLikePortrait = narrowPhone && profile?.aspect === "portrait";
    const shortHeight = profile?.heightSize === "short" || profile?.tight || height > 0 && height < 780;
    const denseContent = showNightRow || showUpNextInline;
    return !!(denseContent || shortHeight || kioskLikePortrait || profile?.size === "xs");
  }
  _syncTabletAutoFitUi(hasUpNext = null) {
    const card = this.shadowRoot?.querySelector(".card");
    if (!card) return;
    const showNightRow = this._mobileNightMode() !== "off";
    const upNextVisible = typeof hasUpNext === "boolean" ? hasUpNext : this._mobileShowUpNextEnabled() && !!this._mobileUpNextItem();
    const layoutMode = this._layoutModeConfig();
    const flags = HomeiiResponsiveFoundation.resolveTabletAutoFitFlags(layoutMode, {
      showNightRow,
      showUpNext: upNextVisible
    });
    const layoutProfile = this._layoutProfileConfig(layoutMode);
    card.classList.toggle("tablet-auto-fit", flags.autoFit);
    card.classList.toggle("tablet-fit-night", flags.showNight);
    card.classList.toggle("tablet-fit-up-next", flags.showUpNext);
    card.classList.toggle("tablet-fit-dense", flags.dense);
    card.classList.toggle("mobile-content-dense", this._mobileContentDense(layoutMode, layoutProfile, {
      showNightRow,
      showUpNextInline: upNextVisible
    }));
  }
  _setSleepTimerMinutes(minutes = 15, source = "general") {
    const amount = Math.max(1, Number(minutes) || 0);
    const player = this._getSelectedPlayer();
    if (!player?.entity_id) {
      this._toastError(this._i18n("ui.select_a_player_first"));
      return false;
    }
    this._state.mobileSleepTimerEndsAt = HomeiiNightFoundation.createSleepTimerTargetAt(amount, Date.now());
    this._state.mobileSleepTimerPlayer = player.entity_id;
    this._state.mobileSleepTimerOrigin = HomeiiNightFoundation.normalizeSleepTimerOrigin(source);
    this._state.mobileSleepTimerMenuOpen = false;
    this._persistMobileAppearance();
    this._syncNightModeUi();
    this._syncSleepTimerChip();
    this._toastSuccess(this._i18n("ui.sleep_timer_set_minutes", { minutes: amount }));
    return true;
  }
  _addSleepTimerMinutes(minutes = 15) {
    const amount = Math.max(1, Number(minutes) || 0);
    const player = this._getSelectedPlayer();
    this._state.mobileSleepTimerEndsAt = HomeiiNightFoundation.extendSleepTimerTargetAt(
      this._state.mobileSleepTimerEndsAt || 0,
      amount,
      Date.now()
    );
    this._state.mobileSleepTimerPlayer = player?.entity_id || this._state.mobileSleepTimerPlayer || this._state.selectedPlayer || "";
    this._persistMobileAppearance();
    this._syncNightModeUi();
    this._syncSleepTimerChip();
    this._toastSuccess(this._i18n("ui.sleep_timer_added_minutes", { minutes: amount }));
  }
  _toggleSleepTimerMenu(force = null) {
    const next = typeof force === "boolean" ? force : !this._state.mobileSleepTimerMenuOpen;
    this._state.mobileSleepTimerMenuOpen = !!next && this._sleepTimerChipVisible();
    this._syncSleepTimerChip();
  }
  _cycleNightMode() {
    const order = ["auto", "on", "off"];
    const current = this._mobileNightMode();
    const next = order[(order.indexOf(current) + 1) % order.length];
    this._state.mobileNightMode = next;
    this._persistMobileAppearance();
    this._rebuildMobileUi({ reopenPage: this._state.menuOpen ? this._state.menuPage || "settings" : "", reopenStudio: this._state.controlRoomOpen });
  }
  _clearSleepTimer(showToast = false) {
    this._state.mobileSleepTimerEndsAt = 0;
    this._state.mobileSleepTimerPlayer = "";
    this._state.mobileSleepTimerOrigin = "";
    this._state.mobileSleepTimerMenuOpen = false;
    this._persistMobileAppearance();
    this._syncNightModeUi();
    this._syncSleepTimerChip();
    if (showToast) {
      this._toast(this._i18n("ui.sleep_timer_cleared"));
    }
  }
  _cycleSleepTimer(source = "general") {
    const currentRemaining = this._sleepTimerRemainingMs();
    const steps = [15, 30, 45, 60, 0];
    const normalizedSource = HomeiiNightFoundation.normalizeSleepTimerOrigin(source);
    if (!currentRemaining) {
      const player2 = this._getSelectedPlayer();
      if (!player2?.entity_id) {
        this._toastError(this._i18n("ui.select_a_player_first"));
        return;
      }
      this._state.mobileSleepTimerEndsAt = HomeiiNightFoundation.createSleepTimerTargetAt(steps[0], Date.now());
      this._state.mobileSleepTimerPlayer = player2.entity_id;
      this._state.mobileSleepTimerOrigin = normalizedSource;
      this._state.mobileSleepTimerMenuOpen = false;
      this._persistMobileAppearance();
      this._syncNightModeUi();
      this._syncSleepTimerChip();
      this._toastSuccess(this._i18n("ui.sleep_timer_set_for_15_minutes"));
      return;
    }
    const nextStep = HomeiiNightFoundation.nextSleepTimerStep(currentRemaining, steps);
    if (!nextStep) {
      this._clearSleepTimer(true);
      return;
    }
    const player = this._getSelectedPlayer();
    this._state.mobileSleepTimerEndsAt = HomeiiNightFoundation.createSleepTimerTargetAt(nextStep, Date.now());
    this._state.mobileSleepTimerPlayer = player?.entity_id || this._state.mobileSleepTimerPlayer || "";
    if (normalizedSource === "night") this._state.mobileSleepTimerOrigin = normalizedSource;
    this._state.mobileSleepTimerMenuOpen = false;
    this._persistMobileAppearance();
    this._syncNightModeUi();
    this._syncSleepTimerChip();
    this._toastSuccess(this._i18n("ui.sleep_timer_updated_minutes", { minutes: nextStep }));
  }
  async _playNightMix() {
    try {
      const [allPlaylists, likedPlaylists] = await Promise.allSettled([
        this._fetchLibrary("playlist", "sort_name", 500, false),
        this._fetchLibrary("playlist", "sort_name", 220, true)
      ]);
      const playlists = [
        ...Array.isArray(allPlaylists.value) ? allPlaylists.value : [],
        ...Array.isArray(likedPlaylists.value) ? likedPlaylists.value : []
      ].filter((item) => item?.uri).filter((item, index, list) => list.findIndex((candidate) => candidate?.uri === item?.uri) === index);
      if (!playlists.length) {
        await this._playRandomFromPlaylists();
        return;
      }
      const keywords = ["sleep", "night", "chill", "calm", "relax", "ambient", "meditation", "dream", "lofi", "lo-fi", "soft"];
      const matches = playlists.filter((item) => {
        const haystack = [
          item?.name,
          item?.metadata?.description,
          item?.description
        ].filter(Boolean).join(" ").toLowerCase();
        return keywords.some((keyword) => haystack.includes(keyword));
      });
      const pool = matches.length ? matches : playlists;
      const pick = pool[Math.floor(Math.random() * pool.length)];
      const ok = await this._playMedia(pick.uri, pick.media_type || "playlist", "play", {
        label: pick.name || this._i18n("ui.chill_mix"),
        silent: true
      });
      if (ok) {
        this._toastSuccess(this._i18n("ui.starting_a_chill_mix"));
      }
    } catch (error) {
      this._toastError(error?.message || this._i18n("ui.could_not_start_chill_mix"));
    }
  }
  async _resolveQuickMixEntry() {
    const currentQueueItem = this._state.maQueueState?.current_item || null;
    const currentMedia = currentQueueItem?.media_item || {};
    const player = this._getSelectedPlayer();
    const image = this._currentArtworkUrl(player, currentQueueItem, 420, { preferPlayerArtwork: true });
    const uri = String(
      this._getQueueItemUri(currentQueueItem) || currentMedia?.uri || player?.attributes?.media_content_id || ""
    ).trim();
    const rawMediaType = String(
      currentMedia?.media_type || currentQueueItem?.media_type || player?.attributes?.media_content_type || ""
    ).toLowerCase();
    const parsedRef = this._parseMediaReference(uri, rawMediaType || "track");
    const parsedType = String(parsedRef?.media_type || rawMediaType || "").toLowerCase();
    const normalizedType = this._supportsMusicAssistantRadioMode(parsedType) ? parsedType : this._supportsMusicAssistantRadioMode(rawMediaType) ? rawMediaType : "";
    const title = this._queueItemPrimaryTitle(currentQueueItem) || player?.attributes?.media_title || "";
    const artist = this._queueItemPrimaryArtist(currentQueueItem) || player?.attributes?.media_artist || "";
    if (uri && normalizedType) {
      return {
        uri,
        media_type: normalizedType,
        name: title || this._i18n("ui.quick_mix"),
        image
      };
    }
    const query = [title, artist].filter(Boolean).join(" ").trim();
    if (!query) return null;
    const results = await this._searchEverything(query);
    const candidates = [
      ...Array.isArray(results?.tracks) ? results.tracks.map((item) => ({ ...item, media_type: item?.media_type || "track" })) : [],
      ...Array.isArray(results?.albums) ? results.albums.map((item) => ({ ...item, media_type: item?.media_type || "album" })) : [],
      ...Array.isArray(results?.artists) ? results.artists.map((item) => ({ ...item, media_type: item?.media_type || "artist" })) : [],
      ...Array.isArray(results?.playlists) ? results.playlists.map((item) => ({ ...item, media_type: item?.media_type || "playlist" })) : []
    ].filter((item) => String(item?.uri || "").trim()).filter((item) => this._supportsMusicAssistantRadioMode(String(item?.media_type || "").toLowerCase()));
    if (!candidates.length) return null;
    const titleNeedle = String(title || "").trim().toLowerCase();
    const artistNeedle = String(artist || "").trim().toLowerCase();
    const scoreOf = (item) => {
      const itemTitle = String(item?.name || item?.title || "").trim().toLowerCase();
      const itemArtist = String(item?.artist || item?.artist_str || this._artistName(item) || "").trim().toLowerCase();
      let score = 0;
      if (titleNeedle && itemTitle === titleNeedle) score += 6;
      else if (titleNeedle && itemTitle.includes(titleNeedle)) score += 3;
      if (artistNeedle && itemArtist === artistNeedle) score += 4;
      else if (artistNeedle && itemArtist.includes(artistNeedle)) score += 2;
      if (String(item?.media_type || "").toLowerCase() === "track") score += 2;
      return score;
    };
    const best = [...candidates].sort((a, b) => scoreOf(b) - scoreOf(a))[0];
    return best ? {
      uri: String(best.uri || "").trim(),
      media_type: String(best.media_type || "track").toLowerCase(),
      name: best.name || title || this._i18n("ui.quick_mix"),
      image: this._artUrl(best) || image
    } : null;
  }
  _rememberQuickMixRecommendationSeed(limit = 10) {
    const currentItem = this._state.maQueueState?.current_item || null;
    const currentKey = currentItem ? this._getQueueItemKey(currentItem) : "";
    const queueItems = this._getNowPlayingQueueItems();
    const ordered = [
      currentItem,
      ...queueItems.filter((item) => this._getQueueItemKey(item) !== currentKey)
    ].filter(Boolean);
    const seen = /* @__PURE__ */ new Set();
    this._state.quickMixRecommendationItems = ordered.map((item) => {
      const media = item.media_item || {};
      const uri = String(media.uri || this._getQueueItemUri(item) || "").trim();
      if (!uri || seen.has(uri)) return null;
      seen.add(uri);
      return {
        uri,
        media_type: media.media_type || item.media_type || "track",
        title: media.name || item.name || this._i18n("ui.recommended_track"),
        artist: media.artists?.map((artist) => artist?.name).filter(Boolean).join(", ") || item.media_artist || media.album?.name || "",
        album: media.album?.name || item.album || "",
        image: this._queueItemImageUrl(item, 120) || this._artUrl(media) || ""
      };
    }).filter(Boolean).slice(0, limit);
  }
  async _startQuickMix() {
    try {
      const entry = await this._resolveQuickMixEntry();
      if (!entry?.uri || !this._supportsMusicAssistantRadioMode(entry.media_type || "")) {
        this._toastError(this._i18n("ui.could_not_build_quick_mix_from_the_current_song"));
        return;
      }
      this._rememberQuickMixRecommendationSeed();
      this._state.quickMixPendingUntil = Date.now() + 6500;
      this._state.quickMixPendingEntry = entry;
      this._syncNowPlayingUI();
      const ok = await this._playMedia(entry.uri, entry.media_type || "track", "play", {
        label: entry.name || this._i18n("ui.quick_mix"),
        radioMode: true,
        silent: true
      });
      if (ok) {
        this._toastSuccess(this._i18n("ui.quick_mix_started"), { position: "center", duration: 5e3 });
        this._syncRecentHistoryUi();
        return;
      }
      this._state.quickMixPendingUntil = 0;
      this._state.quickMixPendingEntry = null;
      this._toastError(this._i18n("ui.could_not_start_quick_mix"));
    } catch (error) {
      this._state.quickMixPendingUntil = 0;
      this._state.quickMixPendingEntry = null;
      this._toastError(error?.message || this._i18n("ui.could_not_start_quick_mix"));
    }
  }
  _syncSleepTimerState() {
    const target = Number(this._state.mobileSleepTimerEndsAt || 0);
    if (!target) return;
    if (target > Date.now()) return;
    const entityId = String(this._state.mobileSleepTimerPlayer || this._state.selectedPlayer || "").trim();
    this._state.mobileSleepTimerEndsAt = 0;
    this._state.mobileSleepTimerPlayer = "";
    this._state.mobileSleepTimerOrigin = "";
    this._state.mobileSleepTimerMenuOpen = false;
    this._persistMobileAppearance();
    this._syncSleepTimerChip();
    this._syncNightModeUi();
    if (entityId) {
      this._hass.callService("media_player", "media_pause", { entity_id: entityId });
    }
    this._toastSuccess(this._i18n("ui.sleep_timer_finished"));
  }
  _syncNightModeUi() {
    const card = this.shadowRoot?.querySelector(".card");
    const active = this._isNightModeActive();
    const mode = this._mobileNightMode();
    const sleepActive = this._sleepTimerRemainingMs() > 0;
    if (this._state.mobileNightRenderedActive !== active || this._state.mobileNightRenderedMode !== mode) {
      this._state.mobileNightRenderedActive = active;
      this._state.mobileNightRenderedMode = mode;
      if (this._isScheduleFormEditing()) {
        if (card) {
          card.classList.toggle("night-mode", active);
          card.classList.toggle("night-mode-enabled", mode !== "off");
        }
        return;
      }
      const reopenMenu = this._state.menuOpen ? this._state.menuPage || "settings" : "";
      this._rebuildMobileUi({ reopenPage: reopenMenu, reopenStudio: this._state.controlRoomOpen });
      return;
    }
    if (card) {
      card.classList.toggle("night-mode", active);
      card.classList.toggle("night-mode-enabled", mode !== "off");
    }
    this._syncTabletAutoFitUi();
    const row = this.$("nightQuickRow");
    if (row) {
      row.hidden = mode === "off";
      row.classList.toggle("auto-mode", mode === "auto");
      row.classList.toggle("on-mode", mode === "on");
    }
    const modeBtn = this.$("nightModeQuickBtn");
    if (modeBtn) {
      modeBtn.hidden = mode === "off";
      modeBtn.classList.toggle("active", active || mode === "on");
      modeBtn.classList.toggle("soft", mode === "auto" && !active);
      modeBtn.title = mode === "auto" ? this._i18n("ui.night_mode_auto_window", {
        start: this._nightModeWindow().start,
        end: this._nightModeWindow().end
      }) : mode === "on" ? this._i18n("ui.night_mode_is_always_on") : this._i18n("ui.night_mode_is_off");
    }
    const sleepBtn = this.$("nightSleepBtn");
    if (sleepBtn) {
      sleepBtn.hidden = mode !== "on";
      sleepBtn.classList.toggle("active", sleepActive);
      sleepBtn.title = sleepActive ? this._i18n("ui.sleep_timer_active_remaining", { remaining: this._sleepTimerRemainingLabel() }) : this._i18n("ui.tap_to_start_a_sleep_timer");
    }
    const chillBtn = this.$("nightChillBtn");
    if (chillBtn) {
      chillBtn.hidden = mode !== "on";
    }
  }
  _syncMobileTimerAction() {
    const btn = this.$("mobileTimerBtn");
    if (!btn) return;
    const remainingLabel = this._sleepTimerFooterLabel();
    const active = !!remainingLabel && this._sleepTimerChipVisible();
    const configured = this._mobileQuickActions().includes("timer");
    if (!active && !configured) {
      btn.hidden = true;
      btn.classList.add("hidden");
      return;
    }
    const label = btn.querySelector(".mobile-timer-label");
    btn.hidden = false;
    btn.classList.remove("hidden");
    btn.classList.toggle("active", active);
    btn.title = active ? this._i18n("ui.timer_active_remaining", { remaining: remainingLabel }) : this._i18n("ui.schedules");
    if (label) {
      label.hidden = !active;
      label.textContent = active ? remainingLabel : "";
    }
  }
  _onHassReady() {
    this._hydrateSystemMobileState().catch(() => {
    });
  }
  _systemMobileStateKey() {
    const configEntry = String(this._config?.config_entry_id || "").trim();
    const maUrl = String(this._config?.ma_url || "").trim();
    const base = configEntry || maUrl || "default";
    const safeBase = base.replace(/[^a-zA-Z0-9._:-]+/g, "_").slice(0, 96) || "default";
    return `homeii_music_flow_mobile_state_v1_${safeBase}`;
  }
  _callHomeAssistantWs(message = {}, options = {}) {
    const timeoutMs = Number(options?.timeoutMs || this._musicAssistantTimeoutMs());
    if (typeof this._hass?.callWS === "function") {
      return this._withTimeout(this._hass.callWS(message), timeoutMs, this._timeoutMessage("Home Assistant"));
    }
    if (typeof this._hass?.connection?.sendMessagePromise === "function") {
      return this._withTimeout(this._hass.connection.sendMessagePromise(message), timeoutMs, this._timeoutMessage("Home Assistant"));
    }
    return Promise.reject(new Error("Home Assistant WebSocket API is unavailable"));
  }
  async _getHomeAssistantUserData(key = "") {
    const result = await this._callHomeAssistantWs({ type: "frontend/get_user_data", key });
    if (result && typeof result === "object" && Object.prototype.hasOwnProperty.call(result, "value")) return result.value;
    return result;
  }
  async _setHomeAssistantUserData(key = "", value = {}) {
    await this._callHomeAssistantWs({ type: "frontend/set_user_data", key, value });
  }
  _systemMobileStatePayload() {
    const schedules = this._scheduledStartSchedules();
    const nightWindow = this._nightModeWindow();
    return {
      version: 1,
      updatedAt: Date.now(),
      schedulesTab: this._state.mobileSchedulesTab || "timers",
      startScheduleEditId: this._state.mobileStartScheduleEditId || "",
      startSchedules: schedules,
      startTimerEnabled: schedules.some((schedule) => schedule.enabled !== false),
      startTimerLastRunKey: this._state.mobileStartTimerLastRunKey || "",
      nightMode: this._mobileNightMode(),
      nightModeStart: nightWindow.start,
      nightModeEnd: nightWindow.end,
      nightModeDays: this._nightModeDays()
    };
  }
  _applySystemMobileStatePayload(payload = null) {
    if (!payload || typeof payload !== "object") return false;
    const rawSchedules = Array.isArray(payload.startSchedules) ? payload.startSchedules : Array.isArray(payload.mobileStartSchedules) ? payload.mobileStartSchedules : null;
    const rawNightMode = String(payload.nightMode || payload.mobileNightMode || "").trim();
    const hasNightPayload = ["off", "auto", "on"].includes(rawNightMode) || Object.prototype.hasOwnProperty.call(payload, "nightModeStart") || Object.prototype.hasOwnProperty.call(payload, "nightModeEnd") || Object.prototype.hasOwnProperty.call(payload, "nightModeDays") || Object.prototype.hasOwnProperty.call(payload, "mobileNightModeStart") || Object.prototype.hasOwnProperty.call(payload, "mobileNightModeEnd") || Object.prototype.hasOwnProperty.call(payload, "mobileNightModeDays");
    if (!rawSchedules && !hasNightPayload) return false;
    if (rawSchedules) {
      const schedules = rawSchedules.map((schedule, index) => this._normalizeScheduledStartSchedule(schedule, index));
      this._state.mobileStartSchedules = schedules;
      this._state.mobileStartTimerEnabled = schedules.some((schedule) => schedule.enabled !== false);
      const editId = String(payload.startScheduleEditId || "").trim();
      this._state.mobileStartScheduleEditId = schedules.some((schedule) => schedule.id === editId) ? editId : "";
      const activeTab = String(payload.schedulesTab || "").trim();
      if (["timers", "wake", "night"].includes(activeTab)) this._state.mobileSchedulesTab = activeTab;
      const reference = schedules.find((schedule) => schedule.id === this._state.mobileStartScheduleEditId) || schedules[0] || null;
      if (reference) {
        this._state.mobileStartTimerTime = reference.time;
        this._state.mobileStartTimerPlayer = reference.player;
        this._state.mobileStartTimerPlaylist = reference.playlist;
        this._state.mobileStartTimerPlaylistName = reference.playlistName;
        this._state.mobileStartTimerVolume = reference.volume;
        this._state.mobileStartTimerDays = reference.days;
        this._state.mobileStartTimerLastRunKey = reference.lastRunKey || "";
      } else {
        this._state.mobileStartTimerLastRunKey = "";
      }
    }
    if (hasNightPayload) {
      this._state.mobileNightMode = ["off", "auto", "on"].includes(rawNightMode) ? rawNightMode : this._mobileNightMode();
      this._state.mobileNightModeStart = this._normalizeClockTime(payload.nightModeStart || payload.mobileNightModeStart || this._state.mobileNightModeStart || "22:00", "22:00");
      this._state.mobileNightModeEnd = this._normalizeClockTime(payload.nightModeEnd || payload.mobileNightModeEnd || this._state.mobileNightModeEnd || "06:00", "06:00");
      this._state.mobileNightModeDays = this._normalizeNightModeDays(payload.nightModeDays || payload.mobileNightModeDays || this._state.mobileNightModeDays);
    }
    return true;
  }
  async _hydrateSystemMobileState() {
    if (!this._hass) return false;
    const key = this._systemMobileStateKey();
    if (this._systemMobileStateHydrateKey === key && this._systemMobileStateHydratePromise) {
      return this._systemMobileStateHydratePromise;
    }
    this._systemMobileStateHydrateKey = key;
    this._systemMobileStateHydratePromise = (async () => {
      try {
        const payload = await this._getHomeAssistantUserData(key);
        const applied = this._applySystemMobileStatePayload(payload);
        if (applied) {
          this._writeSchedulesToLocalStorage();
          if (this._built) {
            if (this._state.menuOpen && this._state.menuPage === "sleep_timer") await this._renderMobileMenu();
            this._syncSleepTimerChip();
          }
          return true;
        }
        if (this._scheduledStartSchedules().length) this._scheduleSystemMobileStatePersist(250);
      } catch (_) {
        return false;
      }
      return false;
    })();
    return this._systemMobileStateHydratePromise;
  }
  _writeSchedulesToLocalStorage() {
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_enabled", JSON.stringify(!!this._state.mobileStartTimerEnabled));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_time", this._normalizeClockTime(this._state.mobileStartTimerTime || "07:00", "07:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_player", this._state.mobileStartTimerPlayer || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_playlist", this._state.mobileStartTimerPlaylist || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_playlist_name", this._state.mobileStartTimerPlaylistName || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_volume", String(Math.max(0, Math.min(100, Number(this._state.mobileStartTimerVolume || 35) || 35))));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_days", JSON.stringify(this._scheduledStartDays()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_last_run", this._state.mobileStartTimerLastRunKey || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_schedules", JSON.stringify(this._scheduledStartSchedules()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_schedules_tab", this._state.mobileSchedulesTab || "timers");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_mode", this._mobileNightMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_start", this._normalizeClockTime(this._state.mobileNightModeStart || "22:00", "22:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_end", this._normalizeClockTime(this._state.mobileNightModeEnd || "06:00", "06:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_days", JSON.stringify(this._nightModeDays()));
    } catch (_) {
    }
  }
  _scheduleSystemMobileStatePersist(delayMs = 450) {
    if (!this._hass) return;
    clearTimeout(this._systemMobileStatePersistTimer);
    this._systemMobileStatePersistTimer = setTimeout(() => {
      this._systemMobileStatePersistTimer = null;
      this._persistSystemMobileState().catch(() => {
      });
    }, Math.max(0, Number(delayMs) || 0));
  }
  async _persistSystemMobileState() {
    if (!this._hass) return false;
    await this._setHomeAssistantUserData(this._systemMobileStateKey(), this._systemMobileStatePayload());
    return true;
  }
  _syncSleepTimerChip() {
    const card = this.shadowRoot?.querySelector(".card");
    const remainingLabel = this._sleepTimerFooterLabel();
    const active = !!remainingLabel && this._sleepTimerChipVisible();
    const timerConfigured = this._mobileQuickActions().includes("timer");
    const needsTemporaryTimerUi = active && !timerConfigured && (card?.classList.contains("layout-tablet") && !this.$("sleepTimerCorner") || !card?.classList.contains("layout-tablet") && !this.$("mobileTimerBtn"));
    if (needsTemporaryTimerUi) {
      const reopenPage = this._state.menuOpen ? this._state.menuPage || "sleep_timer" : "";
      this._rebuildMobileUi({ reopenPage, reopenStudio: this._state.controlRoomOpen });
      return;
    }
    card?.classList.toggle("has-sleep-timer", active);
    this._syncMobileTimerAction();
    const corner = this.$("sleepTimerCorner");
    if (!corner) return;
    if (!active) {
      this._state.mobileSleepTimerMenuOpen = false;
      if (corner.innerHTML !== "") corner.innerHTML = "";
      corner.hidden = true;
      return;
    }
    const nextHtml = this._sleepTimerCornerInnerHtml();
    if (corner.innerHTML !== nextHtml) corner.innerHTML = nextHtml;
    corner.hidden = false;
  }
  _persistMobileAppearance() {
    this._scheduleSystemMobileStatePersist();
    if (this._usesVisualSettings()) return;
    const storedDynamicThemeMode = ["off", "auto", "strong"].includes(String(this._state.mobileDynamicThemeMode || "auto").toLowerCase()) ? String(this._state.mobileDynamicThemeMode || "auto").toLowerCase() : "auto";
    const storedBackgroundMotionMode = ["off", "subtle", "strong", "extreme"].includes(String(this._state.mobileBackgroundMotionMode || "subtle").toLowerCase()) ? String(this._state.mobileBackgroundMotionMode || "subtle").toLowerCase() : "subtle";
    const storedPerformanceProfile = HomeiiMobileSettingsFoundation.normalizePerformanceProfile(this._state.performanceProfile, this._state.performanceMode);
    try {
      localStorage.setItem("homeii_music_flow_mobile_custom_color", this._state.mobileCustomColor || "#f5a623");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_performance_profile", storedPerformanceProfile);
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_performance_mode", JSON.stringify(!!this._state.performanceMode));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_dynamic_theme_mode", storedDynamicThemeMode);
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_background_motion_mode", storedBackgroundMotionMode);
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_custom_text", this._state.mobileCustomTextTone || "light");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_font_scale", String(this._state.mobileFontScale || 1));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_icon_scale", String(this._mobileIconScale()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_mode", this._mobileNightMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_start", this._normalizeClockTime(this._state.mobileNightModeStart || "22:00", "22:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_end", this._normalizeClockTime(this._state.mobileNightModeEnd || "06:00", "06:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_night_days", JSON.stringify(this._nightModeDays()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_sleep_timer_at", String(Number(this._state.mobileSleepTimerEndsAt || 0) || 0));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_sleep_timer_player", this._state.mobileSleepTimerPlayer || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_sleep_timer_origin", this._state.mobileSleepTimerOrigin || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_enabled", JSON.stringify(!!this._state.mobileStartTimerEnabled));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_time", this._normalizeClockTime(this._state.mobileStartTimerTime || "07:00", "07:00"));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_player", this._state.mobileStartTimerPlayer || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_playlist", this._state.mobileStartTimerPlaylist || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_playlist_name", this._state.mobileStartTimerPlaylistName || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_volume", String(Math.max(0, Math.min(100, Number(this._state.mobileStartTimerVolume || 35) || 35))));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_days", JSON.stringify(this._scheduledStartDays()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_timer_last_run", this._state.mobileStartTimerLastRunKey || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_start_schedules", JSON.stringify(this._scheduledStartSchedules()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_schedules_tab", this._state.mobileSchedulesTab || "timers");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_lyrics_sync", JSON.stringify(this._state.mobileLyricsSyncEnabled !== false));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_lyrics_offset_ms", String(this._lyricsSyncOffsetMs()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_lyrics_font_scale", String(this._lyricsFontScale()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_compact_mode", JSON.stringify(!!this._state.mobileCompactMode));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_compact_widget_mode", this._mobileCompactWidgetMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_compact_edge_to_edge", JSON.stringify(this._mobileCompactEdgeToEdgeEnabled()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_layout_mode", this._mobileLayoutMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_library_default_layout", this._defaultMobileMediaLayout());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_show_up_next", JSON.stringify(this._mobileShowUpNextEnabled()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_footer_search", JSON.stringify(!!this._state.mobileFooterSearchEnabled));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_studio_shortcut", JSON.stringify(this._mobileStudioShortcutEnabled()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_footer_mode", this._state.mobileFooterMode || "both");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_home_shortcut", JSON.stringify(!!this._state.mobileHomeShortcutEnabled));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_home_shortcut_path", this._mobileHomeShortcutPath());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_volume_mode", this._mobileVolumeMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_volume_step_buttons", JSON.stringify(this._mobileVolumeStepButtonsEnabled()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_volume_step_percent", String(this._mobileVolumeStepPercent()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_mic_mode", this._mobileMicMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_voice_assistant_enabled", JSON.stringify(this._voiceAssistantEnabled()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_voice_assistant_mode", this._voiceAssistantMode());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_voice_assistant_agent_id", this._voiceAssistantAgentId());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_voice_assistant_speak_feedback", JSON.stringify(this._voiceAssistantSpeakFeedbackEnabled()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_library_tabs", JSON.stringify(this._mobileLibraryTabs()));
    } catch (_) {
    }
    try {
      const storedMainBarItems = HomeiiMobileSettingsFoundation.normalizeMobileMainBarItems(this._state.mobileMainBarItems, {
        usesVisualSettings: this._usesVisualSettings(),
        hidePlayers: false,
        fallbackItems: this._defaultMobileMainBarItems()
      });
      localStorage.setItem("homeii_music_flow_mobile_main_bar_items", JSON.stringify(storedMainBarItems));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_quick_actions", JSON.stringify(this._mobileQuickActions()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_liked_mode", this._state.mobileLikedMode || "ma");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_swipe_mode", this._state.mobileSwipeMode || "play");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_radio_country", this._mobileRadioBrowserCountry());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_announcement_presets", JSON.stringify(this._state.mobileAnnouncementPresets || []));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_announcement_volume", String(this._announcementVolumePct()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_announcement_tts_entity", this._state.mobileAnnouncementTtsEntity || "");
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_announcement_tts_language", this._announcementLanguageSetting());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_enabled", JSON.stringify(!!this._state.ambientLightEnabled));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_entities", JSON.stringify(this._ambientLightEntities()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_player_map", JSON.stringify(this._ambientLightPlayerMap()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_brightness", String(this._ambientLightBrightness()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_transition", String(this._ambientLightTransition()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_ambient_light_cooldown", String(this._ambientLightCooldown()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_enabled", JSON.stringify(!!this._state.screensaverEnabled));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_controls_enabled", JSON.stringify(!!this._state.screensaverControlsEnabled));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_control_buttons", JSON.stringify(this._screensaverControlButtons({ includeDisabled: true })));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_clock_mode", this._screensaverClockMode());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_timeout_seconds", String(this._screensaverTimeoutSeconds()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_message", this._screensaverMessage());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_clock_size", String(this._screensaverClockSize()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_clock_x", String(this._screensaverClockX()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_screensaver_clock_y", String(this._screensaverClockY()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_power_button_enabled", JSON.stringify(!!this._state.powerButtonEnabled));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_power_button_name", this._state.powerButtonName || "");
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_power_button_icon", this._powerButtonIcon());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_power_button_action", this._powerButtonAction());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_power_button_entity", this._powerButtonEntity());
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_auxiliary_buttons", JSON.stringify(this._auxiliaryButtonsConfigPayload()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_discovery_mode_enabled", JSON.stringify(this._discoveryModeEnabled()));
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_discovery_category_key", this._state.discoveryCategoryKey || "pop");
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_discovery_genre_key", this._state.discoveryGenreKey || "all");
    } catch {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_pinned_players", JSON.stringify(this._pinnedPlayerPreferences()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_mobile_pinned_player", this._pinnedPlayerPreference());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_excluded_players", JSON.stringify(this._excludedPlayerPreferences()));
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_player_sort_mode", this._playerSortMode());
    } catch (_) {
    }
    try {
      localStorage.setItem("homeii_music_flow_player_order", JSON.stringify(this._playerOrderPreferences()));
    } catch (_) {
    }
  }
  _defaultMobileLibraryTabs() {
    return ["library_search", "library_playlists", "library_artists", "library_albums", "library_tracks", "library_radio", "library_podcasts"];
  }
  _defaultMobileMainBarItems() {
    return ["actions", "players", "library", "settings"];
  }
  _defaultMobileQuickActions() {
    return ["timer", "like", "lyrics", "queue", "radio", "history"];
  }
  _defaultAnnouncementPresets(lang = this._state?.lang || this._config?.language || "en") {
    return HomeiiEditorLocale.isHebrewLanguageTag(lang) ? ["האוכל מוכן", "נא להגיע לסלון", "יוצאים בעוד חמש דקות"] : ["Dinner is ready", "Please come to the living room", "Leaving in five minutes"];
  }
  _isDefaultAnnouncementPresetSet(presets = []) {
    if (!Array.isArray(presets) || !presets.length) return false;
    const normalize = (items) => items.slice(0, 3).map((item) => String(item || "").trim()).join("\n");
    const current = normalize(presets);
    return current === normalize(this._defaultAnnouncementPresets("he")) || current === normalize(this._defaultAnnouncementPresets("en"));
  }
  _mobileHomeShortcutEnabled() {
    return !!this._state.mobileHomeShortcutEnabled;
  }
  _mobileHomeShortcutPath() {
    return HomeiiMobileSettingsFoundation.normalizeHomeShortcutPath(this._state.mobileHomeShortcutPath, { leadingSlash: true });
  }
  _voiceAssistantEnabled() {
    return this._state.voiceAssistantEnabled === true;
  }
  _voiceAssistantMode() {
    return HomeiiMobileSettingsFoundation.normalizeVoiceAssistantMode(this._state.voiceAssistantMode);
  }
  _voiceAssistantAgentId() {
    return String(this._state.voiceAssistantAgentId || "").trim();
  }
  _voiceAssistantSpeakFeedbackEnabled() {
    return this._state.voiceAssistantSpeakFeedback === true;
  }
  _voiceAssistantAgentOptions() {
    const options = [{
      value: "",
      label: this._i18n("ui.default_assist_agent")
    }];
    const current = this._voiceAssistantAgentId();
    const states = Object.values(this._hass?.states || {}).filter((entity) => entity?.entity_id?.startsWith?.("conversation.")).map((entity) => ({
      value: entity.entity_id,
      label: entity.attributes?.friendly_name || entity.entity_id
    })).sort((left, right) => String(left.label).localeCompare(String(right.label), void 0, { sensitivity: "base" }));
    states.forEach((option) => {
      if (!options.some((item) => item.value === option.value)) options.push(option);
    });
    if (current && !options.some((item) => item.value === current)) {
      options.push({ value: current, label: current });
    }
    return options;
  }
  _ambientLightEnabled() {
    return this._state.ambientLightEnabled === true;
  }
  _ambientLightEntities() {
    const entities = HomeiiMobileSettingsFoundation.normalizeEntityList(this._state.ambientLightEntities);
    this._state.ambientLightEntities = entities;
    return entities;
  }
  _ambientLightPlayerMap() {
    const mappings = HomeiiMobileSettingsFoundation.normalizeStringArray(this._state.ambientLightPlayerMap);
    this._state.ambientLightPlayerMap = mappings;
    return mappings;
  }
  _ambientLightPlayerMapEntries() {
    return HomeiiMobileSettingsFoundation.parseAmbientLightPlayerMap(this._ambientLightPlayerMap());
  }
  _ambientLightEntitiesForPlayer(player = this._getSelectedPlayer()) {
    const playerId = String(player?.entity_id || this._state.selectedPlayer || "").trim();
    const mapped = this._ambientLightPlayerMapEntries().find((entry) => entry.player === playerId);
    const entities = mapped ? mapped.lights : this._ambientLightEntities();
    return HomeiiMobileSettingsFoundation.normalizeEntityList(entities).filter((entityId) => entityId.startsWith("light."));
  }
  _ambientLightBrightnessForPlayer(player = this._getSelectedPlayer()) {
    const maxBrightness = this._ambientLightBrightness();
    const volumeLevel = Number(player?.attributes?.volume_level);
    if (!Number.isFinite(volumeLevel)) return maxBrightness;
    const ratio = Math.max(0, Math.min(1, volumeLevel));
    return Math.max(1, Math.min(maxBrightness, Math.round(maxBrightness * ratio)));
  }
  _ambientLightBrightness() {
    const brightness = HomeiiMobileSettingsFoundation.clampPercent(this._state.ambientLightBrightness, 35, { min: 1, max: 100 });
    this._state.ambientLightBrightness = brightness;
    return brightness;
  }
  _ambientLightTransition() {
    const transition = HomeiiMobileSettingsFoundation.clampSeconds(this._state.ambientLightTransition, 3, { min: 0, max: 120 });
    this._state.ambientLightTransition = transition;
    return transition;
  }
  _ambientLightCooldown() {
    const cooldown = HomeiiMobileSettingsFoundation.clampSeconds(this._state.ambientLightCooldown, 8, { min: 0, max: 120 });
    this._state.ambientLightCooldown = cooldown;
    return cooldown;
  }
  _isVisualEditorContext() {
    if (this._editMode === true) return true;
    const attr = String(this.getAttribute?.("edit-mode") || this.getAttribute?.("data-edit-mode") || "").toLowerCase();
    if (attr === "true" || attr === "1") return true;
    let node = this;
    for (let depth = 0; node && depth < 14; depth += 1) {
      const signature = [
        node.localName,
        node.tagName,
        node.id,
        node.className,
        node.getAttribute?.("id"),
        node.getAttribute?.("class")
      ].map((value) => String(value || "").toLowerCase()).join(" ");
      if (signature.includes("hui-card-preview") || signature.includes("hui-card-editor") || signature.includes("hui-dialog-edit-card") || signature.includes("hui-card-options") || signature.includes("lovelace-card-editor")) {
        return true;
      }
      const root = node.getRootNode?.();
      node = node.parentElement || node.parentNode || (root?.host && root.host !== node ? root.host : null);
    }
    return false;
  }
  _screensaverSuppressedByEditor() {
    return this._isVisualEditorContext();
  }
  _screensaverEnabled() {
    if (this._state.screensaverEnabled !== true) return false;
    const rect = this.getBoundingClientRect?.();
    const width = Math.max(
      Number(rect?.width || 0),
      Number(this.offsetWidth || 0),
      typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0
    );
    const height = Math.max(
      Number(rect?.height || 0),
      typeof window !== "undefined" ? Number(window.innerHeight || 0) : 0
    );
    return width >= 760 && height >= 620;
  }
  _screensaverControlsEnabled() {
    return this._state.screensaverControlsEnabled === true;
  }
  _defaultScreensaverControlButtons() {
    return ["previous", "next"];
  }
  _screensaverControlButtons(options = {}) {
    const buttons = HomeiiMobileSettingsFoundation.normalizeScreensaverControlButtons(
      this._state.screensaverControlButtons,
      this._defaultScreensaverControlButtons()
    );
    this._state.screensaverControlButtons = buttons;
    if (options.includeDisabled === true) return buttons;
    return this._screensaverControlsEnabled() ? buttons : [];
  }
  _screensaverControlButtonOptions() {
    return [
      { value: "previous", icon: "previous", label: this._i18n("ui.previous") },
      { value: "play_pause", icon: this._playPauseIconName(this._getSelectedPlayer()), label: this._i18n("ui.play_pause") },
      { value: "next", icon: "next", label: this._i18n("ui.next") },
      { value: "mute", icon: this._volumeIconName(this._getSelectedPlayer()), label: this._i18n("ui.mute") },
      { value: "power", icon: this._powerButtonIcon(), label: this._i18n("ui.auxiliary_button") },
      { value: "like", icon: this._currentMediaFavoriteState() ? "heart_filled" : "heart_outline", label: this._i18n("ui.like_2") },
      { value: "lyrics", icon: "lyrics", label: this._i18n("ui.lyrics") },
      { value: "voice", icon: "mic", label: this._flowAssistantLabel() }
    ];
  }
  _screensaverControlButtonHtml(value = "") {
    const option = this._screensaverControlButtonOptions().find((item) => item.value === value);
    if (!option) return "";
    if (value === "voice" && !this._voiceAssistantEnabled()) return "";
    const idMap = {
      previous: "screensaverPrevBtn",
      play_pause: "screensaverPlayPauseBtn",
      next: "screensaverNextBtn",
      mute: "screensaverMuteBtn",
      power: "screensaverPowerBtn",
      lyrics: "screensaverLyricsBtn",
      voice: "screensaverVoiceBtn"
    };
    const id = idMap[value];
    if (!id) return "";
    const voiceAttrs = value === "voice" ? " data-screensaver-voice" : "";
    const pressedClass = value === "voice" && this._state.voiceAssistantListening ? " listening" : "";
    const primaryClass = value === "play_pause" ? " primary" : "";
    return `<button class="screensaver-voice-btn screensaver-control-btn${primaryClass}${pressedClass}" id="${id}" data-screensaver-control="${this._esc(value)}"${voiceAttrs} title="${this._esc(option.label)}" aria-label="${this._esc(option.label)}">${this._iconSvg(option.icon)}</button>`;
  }
  _screensaverClockMode() {
    const mode = HomeiiMobileSettingsFoundation.normalizeScreensaverClockMode(this._state.screensaverClockMode);
    this._state.screensaverClockMode = mode;
    return mode;
  }
  _screensaverClockSize() {
    const size = HomeiiMobileSettingsFoundation.clampNumber(this._state.screensaverClockSize, 1, { min: 0.75, max: 1.45 });
    this._state.screensaverClockSize = size;
    return size;
  }
  _screensaverClockX() {
    const x = HomeiiMobileSettingsFoundation.clampNumber(this._state.screensaverClockX, 82, { min: 8, max: 92 });
    this._state.screensaverClockX = x;
    return x;
  }
  _screensaverClockY() {
    const y = HomeiiMobileSettingsFoundation.clampNumber(this._state.screensaverClockY, 24, { min: 8, max: 70 });
    this._state.screensaverClockY = y;
    return y;
  }
  _syncScreensaverClockVars() {
    const card = this.shadowRoot?.querySelector?.(".card");
    if (!card) return;
    card.style.setProperty("--screensaver-clock-scale", this._screensaverClockSize().toFixed(2));
    card.style.setProperty("--screensaver-clock-x", `${this._screensaverClockX().toFixed(1)}%`);
    card.style.setProperty("--screensaver-clock-y", `${this._screensaverClockY().toFixed(1)}%`);
  }
  _screensaverTimeoutSeconds() {
    const seconds = HomeiiMobileSettingsFoundation.clampSeconds(this._state.screensaverTimeoutSeconds, 90, { min: 15, max: 3600 });
    this._state.screensaverTimeoutSeconds = seconds;
    return seconds;
  }
  _screensaverMessage() {
    return String(this._state.screensaverMessage || "").trim();
  }
  _powerButtonEnabled() {
    return this._state.powerButtonEnabled === true;
  }
  _powerButtonAction() {
    const action = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(this._state.powerButtonAction);
    this._state.powerButtonAction = action;
    return action;
  }
  _powerButtonIcon() {
    const icon = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(this._state.powerButtonIcon || "power");
    this._state.powerButtonIcon = icon;
    return icon;
  }
  _powerButtonEntity() {
    return String(this._state.powerButtonEntity || "").trim();
  }
  _auxiliaryButtonsConfigPayload() {
    const payload = {};
    (this._state.auxiliaryButtons || []).forEach((button, offset) => {
      const index = offset + 2;
      payload[`aux_button_${index}_enabled`] = button?.enabled === true;
      payload[`aux_button_${index}_name`] = String(button?.name || "").trim();
      payload[`aux_button_${index}_icon`] = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(button?.icon || "power");
      payload[`aux_button_${index}_action`] = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(button?.action);
      payload[`aux_button_${index}_entity`] = String(button?.entity || "").trim();
    });
    return payload;
  }
  _auxiliaryButtonConfigs() {
    const first = {
      enabled: this._powerButtonEnabled(),
      name: String(this._state.powerButtonName || "").trim() || this._i18n("ui.auxiliary_button"),
      icon: this._powerButtonIcon(),
      action: this._powerButtonAction(),
      entity: this._powerButtonEntity()
    };
    const extras = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtons({
      power_button_enabled: false,
      ...this._auxiliaryButtonsConfigPayload()
    }).slice(1);
    this._state.auxiliaryButtons = extras;
    return [first, ...extras].slice(0, 4);
  }
  _enabledAuxiliaryButtons() {
    return this._auxiliaryButtonConfigs().map((button, index) => ({ ...button, index })).filter((button) => button.enabled === true);
  }
  _discoveryModeEnabled() {
    return this._state.discoveryModeEnabled !== false;
  }
  _activeAccentRgbTuple() {
    const parts = String(this._activeAccentRgb() || "").split(/[\s,]+/).map((value) => Number(value)).filter((value) => Number.isFinite(value));
    if (parts.length >= 3) return parts.slice(0, 3).map((value) => this._clampByte(value));
    return HomeiiPaletteFoundation.hexToRgbTuple(this._activeAccentColor() || "#f5a623");
  }
  _ambientLightTrackSignature(player = this._getSelectedPlayer()) {
    const queueItem = this._state.maQueueState?.current_item || null;
    return [
      player?.entity_id,
      player?.attributes?.media_content_id,
      player?.attributes?.media_title,
      player?.attributes?.media_artist,
      this._getQueueItemStableId?.(queueItem),
      this._getQueueItemUri?.(queueItem),
      this._state.mobileDynamicThemeArtwork || ""
    ].map((value) => String(value || "").trim()).filter(Boolean).join("|");
  }
  _syncAmbientLightForCurrentMedia(reason = "") {
    if (!this._ambientLightEnabled() || !this._hass?.callService) return;
    const player = this._getSelectedPlayer();
    if (!player || player.state !== "playing") return;
    const entities = this._ambientLightEntitiesForPlayer(player);
    if (!entities.length) return;
    const rgb = this._activeAccentRgbTuple();
    const brightness = this._ambientLightBrightnessForPlayer(player);
    const trackSignature = this._ambientLightTrackSignature(player);
    const signature = [
      trackSignature,
      entities.join(","),
      rgb.join(","),
      brightness,
      this._ambientLightTransition()
    ].join("|");
    if (signature === this._ambientLightLastSignature) return;
    const now = Date.now();
    const trackChanged = trackSignature && trackSignature !== this._ambientLightLastTrackSignature;
    const brightnessChanged = Math.abs(brightness - Number(this._ambientLightLastBrightness || 0)) >= 3;
    const settingsUpdate = reason === "settings";
    const cooldownMs = this._ambientLightCooldown() * 1e3;
    if (!trackChanged && !brightnessChanged && !settingsUpdate && cooldownMs > 0 && now - Number(this._ambientLightLastCallAt || 0) < cooldownMs) return;
    this._ambientLightLastSignature = signature;
    this._ambientLightLastTrackSignature = trackSignature;
    this._ambientLightLastCallAt = now;
    this._ambientLightLastBrightness = brightness;
    const serviceCall = this._hass.callService("light", "turn_on", {
      entity_id: entities,
      rgb_color: rgb,
      brightness_pct: brightness,
      transition: this._ambientLightTransition()
    });
    serviceCall?.catch?.(() => {
    });
  }
  async _runAuxiliaryButtonAction(index = 0, options = {}) {
    const button = this._auxiliaryButtonConfigs()[Math.max(0, Number(index) || 0)];
    if (!button?.enabled && options.force !== true) return;
    const action = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(button.action);
    const entityId = String(button.entity || "").trim();
    try {
      if (action === "stop_player" || !entityId) {
        const target = entityId && entityId.startsWith("media_player.") ? entityId : this._state.selectedPlayer;
        await this._stopPlayer(target);
        this._toastSuccess(this._i18n("ui.power_action_sent"));
        return;
      }
      const domain = String(entityId).split(".")[0] || "homeassistant";
      const service = action === "script" ? "turn_on" : action === "scene" ? "turn_on" : action;
      const serviceDomain = action === "script" ? "script" : action === "scene" ? "scene" : domain;
      await this._hass?.callService?.(serviceDomain, service, { entity_id: entityId });
      this._toastSuccess(this._i18n("ui.power_action_sent"));
    } catch (error) {
      this._toastError(this._i18n("ui.power_action_failed") + (error?.message ? `: ${error.message}` : ""));
    }
  }
  async _runPowerButtonAction() {
    await this._runAuxiliaryButtonAction(0);
  }
  _handleScreensaverActivity(event = null) {
    if (event && event.isTrusted === false) return;
    if (event?.target?.closest?.("[data-screensaver-control], [data-screensaver-voice]")) return;
    if (this._state.voiceAssistantKeepScreensaver === true && event?.target?.closest?.("#voiceAssistantDialog, .voice-assistant-panel")) {
      return;
    }
    if (event?.target?.closest?.("#screensaverBackdrop")) {
      event.preventDefault?.();
      event.stopPropagation?.();
    }
    this._resetScreensaverTimer({ hide: true, activity: true });
  }
  _startScreensaverVisibilityTracking() {
    if (this._screensaverVisibilityObserver || typeof IntersectionObserver === "undefined") {
      this._markScreensaverPageEntry("connected");
      return;
    }
    this._screensaverVisibilityObserver = new IntersectionObserver((entries = []) => {
      const visible = entries.some((entry) => entry.isIntersecting && Number(entry.intersectionRatio || 0) > 0);
      const wasVisible = this._screensaverVisible !== false;
      this._screensaverVisibilityKnown = true;
      this._screensaverVisible = visible;
      if (visible && !wasVisible) {
        this._markScreensaverPageEntry("visible");
      } else if (!visible && wasVisible) {
        this._pauseScreensaverWhileHidden();
      } else if (visible && this._screensaverPageEntryPending) {
        this._markScreensaverPageEntry("visible");
      }
    }, { threshold: 0.01 });
    this._screensaverVisibilityObserver.observe(this);
  }
  _stopScreensaverVisibilityTracking() {
    if (this._screensaverVisibilityObserver) {
      this._screensaverVisibilityObserver.disconnect();
      this._screensaverVisibilityObserver = null;
    }
    this._screensaverVisibilityKnown = false;
    this._screensaverVisible = true;
  }
  _pauseScreensaverWhileHidden() {
    clearTimeout(this._screensaverTimer);
    this._screensaverTimer = null;
    this._hideScreensaver();
  }
  _markScreensaverPageEntry(reason = "entry") {
    this._screensaverPageEntryPending = true;
    this._screensaverPageEntryReason = reason;
    this._screensaverSuppressUntil = Date.now() + this._screensaverTimeoutSeconds() * 1e3;
    this._resetScreensaverTimer({ hide: true, activity: true });
  }
  _resetScreensaverTimer({ hide = false, delayMs = null, activity = false } = {}) {
    clearTimeout(this._screensaverTimer);
    this._screensaverTimer = null;
    if (hide) this._hideScreensaver();
    if (this._screensaverSuppressedByEditor()) {
      this._hideScreensaver();
      return;
    }
    if (!this._screensaverEnabled() || !this.isConnected) return;
    if (this._screensaverVisibilityKnown && this._screensaverVisible === false) return;
    const defaultDelayMs = this._screensaverTimeoutSeconds() * 1e3;
    if (activity || hide) {
      this._screensaverSuppressUntil = Date.now() + defaultDelayMs;
    }
    const timeoutMs = Number.isFinite(Number(delayMs)) ? Math.max(500, Number(delayMs)) : defaultDelayMs;
    this._screensaverTimer = setTimeout(() => {
      this._showScreensaver();
    }, timeoutMs);
  }
  _screensaverBlocked() {
    return !!(this._screensaverSuppressedByEditor() || this._state.menuOpen || this._state.controlRoomOpen || this._state.mobileHistoryDrawerOpen || this._state.voiceAssistantDialogOpen && this._state.voiceAssistantKeepScreensaver !== true || this.$("mobileQueueActionModal")?.classList?.contains("open") || this.$("mobileVolumePresetModal")?.classList?.contains("open") || this.$("mobileSmartVoiceModal")?.classList?.contains("open"));
  }
  _showScreensaver(options = {}) {
    const force = options?.force === true;
    if (this._screensaverSuppressedByEditor()) {
      this._hideScreensaver();
      return;
    }
    if (!force && !this._screensaverEnabled()) return;
    const now = Date.now();
    const suppressUntil = Number(this._screensaverSuppressUntil || 0);
    if (!force && suppressUntil > now) {
      this._resetScreensaverTimer({ delayMs: suppressUntil - now });
      return;
    }
    if (!force && this._screensaverBlocked()) {
      this._resetScreensaverTimer({ delayMs: 2e3 });
      return;
    }
    this._state.screensaverOpen = true;
    const overlay = this.$("screensaverBackdrop");
    if (!overlay) return;
    clearTimeout(this._screensaverExitTimer);
    this._screensaverExitTimer = null;
    this.classList.add("screensaver-page-open");
    this.shadowRoot?.querySelector?.(".card")?.classList?.add("screensaver-active");
    overlay.classList.remove("closing");
    overlay.classList.add("open");
    overlay.setAttribute("aria-hidden", "false");
    if (this._state.lyricsOpen) {
      this._state.screensaverLyricsOpen = true;
      this._closeLyricsModal?.({ preserveLyrics: true, sync: false });
    }
    if (this._lyricsSessionActive?.()) this._syncLyricsForCurrentTrack();
    this._ensureQueueSnapshot(true).then(() => {
      if (this._state.screensaverOpen) this._syncScreensaverUi();
    }).catch(() => {
    });
    this._syncScreensaverUi();
    clearInterval(this._screensaverClockTimer);
    this._screensaverClockTimer = setInterval(() => this._syncScreensaverUi(), 1e3);
  }
  _hideScreensaver() {
    const overlay = this.$("screensaverBackdrop");
    const wasVisible = !!(this._state.screensaverOpen || overlay?.classList?.contains("open") || overlay?.classList?.contains("closing"));
    if (!wasVisible) {
      this.classList.remove("screensaver-page-open");
      this.shadowRoot?.querySelector?.(".card")?.classList?.remove("screensaver-active");
      return;
    }
    this._state.screensaverOpen = false;
    this._screensaverLyricsInactiveSince = 0;
    this._state.screensaverLyricsOpen = false;
    if (!this._state.lyricsOpen) this._clearLyricsState?.();
    overlay?.classList.remove("open");
    overlay?.classList.add("closing");
    overlay?.setAttribute("aria-hidden", "true");
    if (this._state.voiceAssistantKeepScreensaver) {
      this._state.voiceAssistantKeepScreensaver = false;
      this._syncVoiceAssistantDialog();
    }
    clearInterval(this._screensaverClockTimer);
    this._screensaverClockTimer = null;
    clearTimeout(this._screensaverExitTimer);
    const finishHide = () => {
      if (this._state.screensaverOpen) return;
      overlay?.classList.remove("closing");
      this.classList.remove("screensaver-page-open");
      this.shadowRoot?.querySelector?.(".card")?.classList?.remove("screensaver-active");
      this._screensaverExitTimer = null;
      this._syncNowPlayingUI();
    };
    const reduceMotion = (() => {
      try {
        return window.matchMedia?.("(prefers-reduced-motion: reduce)")?.matches === true;
      } catch {
        return false;
      }
    })();
    this._screensaverExitTimer = setTimeout(finishHide, reduceMotion ? 0 : 520);
  }
  _restoreScreensaverIfOpen() {
    if (!this._state.screensaverOpen) return;
    const overlay = this.$("screensaverBackdrop");
    if (!overlay) return;
    clearTimeout(this._screensaverExitTimer);
    this._screensaverExitTimer = null;
    this.classList.add("screensaver-page-open");
    this.shadowRoot?.querySelector?.(".card")?.classList?.add("screensaver-active");
    overlay.classList.remove("closing");
    overlay.classList.add("open");
    overlay.setAttribute("aria-hidden", "false");
    this._syncScreensaverUi();
    if (!this._screensaverClockTimer) {
      this._screensaverClockTimer = setInterval(() => this._syncScreensaverUi(), 1e3);
    }
  }
  _setScreensaverImageHost(host, url = "", fallbackHtml = "") {
    if (!host) return;
    const nextUrl = String(url || "").trim();
    if (host.dataset.artUrl === nextUrl) return;
    host.dataset.artUrl = nextUrl;
    host.dataset.artReady = nextUrl ? "0" : "1";
    if (!nextUrl) {
      host.innerHTML = fallbackHtml;
      return;
    }
    const img = document.createElement("img");
    img.alt = "";
    img.decoding = "async";
    img.loading = "eager";
    const applyImage = () => {
      if (!host.isConnected || host.dataset.artUrl !== nextUrl) return;
      host.dataset.artReady = "1";
      host.replaceChildren(img);
    };
    img.addEventListener("load", applyImage, { once: true });
    img.addEventListener("error", () => {
      if (!host.isConnected || host.dataset.artUrl !== nextUrl) return;
      host.dataset.artReady = "1";
      host.innerHTML = fallbackHtml;
    }, { once: true });
    img.src = nextUrl;
    if (img.complete) applyImage();
  }
  _setScreensaverBackgroundArt(overlay, url = "") {
    if (!overlay) return;
    const nextUrl = String(url || "").trim();
    if (overlay.dataset.bgArtUrl === nextUrl) return;
    overlay.dataset.bgArtUrl = nextUrl;
    if (!nextUrl) {
      overlay.style.setProperty("--screensaver-art-url", "none");
      return;
    }
    const img = new Image();
    img.decoding = "async";
    const applyImage = () => {
      if (!overlay.isConnected || overlay.dataset.bgArtUrl !== nextUrl) return;
      overlay.style.setProperty("--screensaver-art-url", `url(${JSON.stringify(nextUrl)})`);
    };
    img.addEventListener("load", applyImage, { once: true });
    img.addEventListener("error", () => {
      if (!overlay.isConnected || overlay.dataset.bgArtUrl !== nextUrl) return;
      if (!overlay.style.getPropertyValue("--screensaver-art-url")) {
        overlay.style.setProperty("--screensaver-art-url", "none");
      }
    }, { once: true });
    img.src = nextUrl;
    if (img.complete) applyImage();
  }
  _screensaverLyricsModeActive(player = null) {
    if (!(this._state.lyricsOpen || this._state.screensaverLyricsOpen) || !this._state.screensaverOpen) {
      this._screensaverLyricsInactiveSince = 0;
      return false;
    }
    if (player?.state === "playing") {
      this._screensaverLyricsInactiveSince = 0;
      return true;
    }
    const now = Date.now();
    if (!this._screensaverLyricsInactiveSince) this._screensaverLyricsInactiveSince = now;
    const active = now - this._screensaverLyricsInactiveSince < 3e4;
    if (!active) {
      this._state.screensaverLyricsOpen = false;
      if (!this._state.lyricsOpen) this._clearLyricsState?.();
    }
    return active;
  }
  _screensaverLyricsRows() {
    const lines = Array.isArray(this._state.lyricsLines) ? this._state.lyricsLines : [];
    if (lines.length) {
      const activeIndex = Math.max(0, this._currentLyricsActiveIndex(lines));
      return [
        { kind: "muted", text: lines[activeIndex - 1]?.text || "" },
        { kind: "current", text: lines[activeIndex]?.text || "" },
        { kind: "muted", text: lines[activeIndex + 1]?.text || "" }
      ].filter((row) => String(row.text || "").trim());
    }
    if (this._state.lyricsLoading) {
      return [{ kind: "current", text: this._i18n("ui.loading_lyrics") }];
    }
    const textRows = String(this._state.lyricsText || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean).slice(0, 4);
    if (textRows.length) {
      return textRows.map((text, index) => ({ kind: index === 0 ? "current" : "muted", text }));
    }
    return [{ kind: "current", text: this._i18n("ui.no_lyrics_found") }];
  }
  _syncScreensaverLyricsUi(player = null) {
    const overlay = this.$("screensaverBackdrop");
    const host = this.$("screensaverLyrics");
    if (!overlay || !host) return;
    const active = this._screensaverLyricsModeActive(player);
    overlay.classList.toggle("lyrics-mode", active);
    if (!active) {
      host.dataset.lyricsSignature = "";
      host.innerHTML = "";
      return;
    }
    const rows = this._screensaverLyricsRows();
    const signature = rows.map((row) => `${row.kind}:${row.text}`).join("\n");
    if (host.dataset.lyricsSignature === signature) return;
    host.dataset.lyricsSignature = signature;
    host.innerHTML = rows.map((row) => `
      <div class="screensaver-lyric-line ${this._esc(row.kind)}">${this._esc(row.text)}</div>
    `).join("");
  }
  _openTabletLyricsScreensaver() {
    if (this._layoutModeConfig() !== "tablet") return false;
    if (!this._getSelectedPlayer()) return false;
    if (this._screensaverSuppressedByEditor()) return false;
    this._screensaverSuppressUntil = 0;
    this._state.lyricsOpen = false;
    this._state.screensaverLyricsOpen = true;
    this._closeLyricsModal?.({ preserveLyrics: true, sync: false });
    this._showScreensaver({ force: true });
    this._syncScreensaverUi();
    return true;
  }
  _syncScreensaverUi() {
    const overlay = this.$("screensaverBackdrop");
    if (!overlay) return;
    const mode = this._screensaverClockMode();
    const now = /* @__PURE__ */ new Date();
    const player = this._getSelectedPlayer();
    const queueItem = this._state.maQueueState?.current_item || null;
    this._syncLocalSendspinMediaSession(player, queueItem);
    const art = this._currentArtworkUrl(player, queueItem, 720, { preferPlayerArtwork: true });
    const mediaTitle = player?.attributes?.media_title || queueItem?.media_item?.name || "";
    const mediaArtist = player?.attributes?.media_artist || (queueItem?.media_item?.artists || []).map((artistEntry) => artistEntry?.name).filter(Boolean).join(", ") || "";
    const hasMedia = !!(mediaTitle || mediaArtist || art);
    ["screensaverPrevBtn", "screensaverPlayPauseBtn", "screensaverNextBtn", "screensaverMuteBtn", "screensaverPowerBtn", "screensaverLyricsBtn"].forEach((id) => {
      const btn = this.$(id);
      if (!btn) return;
      const available = id === "screensaverPowerBtn" ? !!this._hass : !!player;
      btn.disabled = !available;
      btn.setAttribute("aria-disabled", available ? "false" : "true");
    });
    this._setButtonIcon(this.$("screensaverPlayPauseBtn"), this._playPauseIconName(player));
    this._setButtonIcon(this.$("screensaverMuteBtn"), this._volumeIconName(player));
    const muteBtn = this.$("screensaverMuteBtn");
    if (muteBtn) muteBtn.classList.toggle("active", this._isMuted(player));
    const likeBtn = this.$("screensaverLikeBtn");
    if (likeBtn) {
      likeBtn.hidden = !hasMedia;
      likeBtn.disabled = !hasMedia;
      likeBtn.setAttribute("aria-disabled", hasMedia ? "false" : "true");
      const liked = this._currentMediaFavoriteState();
      likeBtn.classList.toggle("active", liked);
      this._setButtonIcon(likeBtn, liked ? "heart_filled" : "heart_outline");
    }
    const title = mediaTitle || this._i18n("ui.nothing_playing");
    const nextItem = this._mobileUpNextItem();
    const nextTitle = nextItem ? this._queueItemPrimaryTitle(nextItem) : "";
    const nextArtist = nextItem ? this._queueItemPrimaryArtist(nextItem) : "";
    const nextArt = nextItem ? this._queueItemImageUrl(nextItem, 96) : "";
    const message = this._screensaverMessage();
    overlay.classList.toggle("analog-mode", mode === "analog");
    overlay.classList.toggle("digital-mode", mode !== "analog");
    overlay.classList.toggle("empty-mode", !hasMedia);
    this._setScreensaverBackgroundArt(overlay, art);
    this._syncScreensaverLyricsUi(player);
    const lyricsBtn = this.$("screensaverLyricsBtn");
    if (lyricsBtn) {
      const lyricsActive = overlay.classList.contains("lyrics-mode");
      lyricsBtn.classList.toggle("active", lyricsActive);
      lyricsBtn.setAttribute("aria-pressed", lyricsActive ? "true" : "false");
    }
    const clock = this.$("screensaverClock");
    if (clock) {
      clock.textContent = new Intl.DateTimeFormat(this._language(), {
        hour: "2-digit",
        minute: "2-digit"
      }).format(now);
    }
    this._setScreensaverImageHost(this.$("screensaverArt"), art, this._tabletBrandSignatureHtml("screensaver-empty-logo"));
    if (this.$("screensaverTitle")) this.$("screensaverTitle").textContent = title;
    if (this.$("screensaverArtist")) this.$("screensaverArtist").textContent = hasMedia ? mediaArtist || this._selectedPlayerName() : "";
    const messageEl = this.$("screensaverMessage");
    if (messageEl) {
      messageEl.hidden = !message;
      messageEl.textContent = message;
    }
    const nextEl = this.$("screensaverNext");
    if (nextEl) {
      nextEl.hidden = !nextTitle;
      if (nextTitle) {
        this._setScreensaverImageHost(this.$("screensaverNextArt"), nextArt, this._iconSvg("tracks"));
        if (this.$("screensaverNextLabel")) this.$("screensaverNextLabel").textContent = this._i18n("ui.up_next_2");
        if (this.$("screensaverNextTitle")) this.$("screensaverNextTitle").textContent = nextTitle;
        if (this.$("screensaverNextArtist")) this.$("screensaverNextArtist").textContent = nextArtist;
      }
    }
    const seconds = now.getSeconds();
    const minutes = now.getMinutes() + seconds / 60;
    const hours = now.getHours() % 12 + minutes / 60;
    this.$("screensaverHour")?.style?.setProperty("--hand-rotation", `${hours * 30}deg`);
    this.$("screensaverMinute")?.style?.setProperty("--hand-rotation", `${minutes * 6}deg`);
    this.$("screensaverSecond")?.style?.setProperty("--hand-rotation", `${seconds * 6}deg`);
  }
  _mobileStudioShortcutEnabled() {
    return this._state.mobileStudioShortcutEnabled !== false;
  }
  _suppressHomeShortcutNavigation(durationMs = 900) {
    this._homeShortcutSuppressUntil = Date.now() + Math.max(0, Number(durationMs) || 0);
  }
  _homeShortcutNavigationSuppressed() {
    return Date.now() < Number(this._homeShortcutSuppressUntil || 0);
  }
  _bindMobileHomeQuickButton() {
    const btn = this.$("mobileHomeQuickBtn");
    if (!btn || btn.dataset.homeiiHomeBound === "1") return;
    btn.dataset.homeiiHomeBound = "1";
    btn.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (this._state.controlRoomOpen || this._homeShortcutNavigationSuppressed()) return;
      if (!this._pressUiButton(e.currentTarget)) return;
      this._goHomeAssistantDashboard();
    });
  }
  _bindMobileQuickActionButtons() {
    const bindButton = (id, handler) => {
      const btn = this.$(id);
      if (!btn || btn.dataset.homeiiQuickBound === "1") return;
      btn.dataset.homeiiQuickBound = "1";
      btn.addEventListener("click", handler);
    };
    bindButton("mobileLyricsBtn", (e) => {
      this._pressUiButton(e.currentTarget);
      if (this._openTabletLyricsScreensaver()) return;
      this._openLyricsModal();
    });
    bindButton("mobileLikeBtn", (e) => {
      this._pressUiButton(e.currentTarget);
      this._toggleLikeCurrentMedia(e.currentTarget);
    });
    bindButton("mobileQueueBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._openMobileMenu("queue");
    });
    bindButton("mobileQuickSearchBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._openMobileMenu("quick_search");
    });
    bindButton("mobileRandomBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._startQuickMix();
    });
    bindButton("mobileTimerBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._openMobileMenu("sleep_timer");
    });
    bindButton("mobileInfoBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._openSelectedPlayerMoreInfo();
    });
    bindButton("mobileVoiceAssistantBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget, [8, 18, 8])) return;
      this._startVoiceAssistantCommand();
    });
    bindButton("mobileDisconnectAllBtn", (e) => {
      if (!this._pressUiButton(e.currentTarget, [12, 18])) return;
      this._openCleanAllConfirm();
    });
    this.shadowRoot?.querySelectorAll?.("[data-auxiliary-index]")?.forEach((btn) => {
      if (!btn || btn.dataset.homeiiQuickBound === "1") return;
      btn.dataset.homeiiQuickBound = "1";
      btn.addEventListener("click", async (e) => {
        if (!this._pressUiButton(e.currentTarget, [12, 18])) return;
        await this._runAuxiliaryButtonAction(Number(e.currentTarget.dataset.auxiliaryIndex || 0));
      });
    });
    bindButton("historyToggleFab", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._toggleHistoryDrawer();
    });
    this._bindMobileHomeQuickButton();
  }
  _goHomeAssistantDashboard() {
    if (this._homeShortcutNavigationSuppressed()) return;
    if (this._state.controlRoomOpen) return;
    const path = this._mobileHomeShortcutPath();
    const emitLocationChanged = () => {
      try {
        window.dispatchEvent(new Event("location-changed"));
      } catch (_) {
      }
      try {
        window.dispatchEvent(new CustomEvent("location-changed", { detail: { replace: false } }));
      } catch (_) {
      }
    };
    try {
      const targetUrl = new URL(path, window.location.origin);
      const nextPath = `${targetUrl.pathname}${targetUrl.search}${targetUrl.hash}`;
      const currentPath = `${window.location.pathname || ""}${window.location.search || ""}${window.location.hash || ""}`;
      if (targetUrl.origin === window.location.origin && nextPath !== currentPath) {
        window.history.pushState(null, "", nextPath);
        emitLocationChanged();
        return;
      }
    } catch (_) {
    }
    try {
      window.location.assign(path);
    } catch (_) {
      try {
        window.location.href = path;
      } catch (_2) {
      }
    }
  }
  _mobileMainBarItems() {
    if (this._isHotelMode()) return [];
    const hidePlayers = false;
    return HomeiiMobileSettingsFoundation.normalizeMobileMainBarItems(this._state.mobileMainBarItems, {
      usesVisualSettings: this._usesVisualSettings(),
      hidePlayers,
      fallbackItems: this._defaultMobileMainBarItems()
    });
  }
  _mobileLibraryTabs() {
    return HomeiiMobileSettingsFoundation.normalizeMobileLibraryTabs(
      this._state.mobileLibraryTabs,
      this._defaultMobileLibraryTabs()
    );
  }
  _mobileFooterMode() {
    return HomeiiMobileSettingsFoundation.normalizeMobileFooterMode(this._state.mobileFooterMode);
  }
  _mobileIconScale() {
    const scale = HomeiiMobileSettingsFoundation.clampMobileIconScale(this._state.mobileIconScale);
    this._state.mobileIconScale = scale;
    return scale;
  }
  _mobileMicMode() {
    return HomeiiMobileSettingsFoundation.normalizeMobileMicMode(this._state.mobileMicMode);
  }
  _pinnedPlayerPreferences() {
    const next = HomeiiMobileSettingsFoundation.normalizePinnedPlayerEntityList(this._state.pinnedPlayerEntities);
    this._state.pinnedPlayerEntities = next;
    return next;
  }
  _pinnedPlayerPreference() {
    return this._pinnedPlayerPreferences()[0] || "";
  }
  _excludedPlayerPreferences() {
    const next = HomeiiMobileSettingsFoundation.normalizePinnedPlayerEntityList(this._state.excludedPlayerEntities);
    this._state.excludedPlayerEntities = next;
    return next;
  }
  _isPlayerExcluded(playerOrEntityId = "") {
    const entityId = typeof playerOrEntityId === "string" ? playerOrEntityId : playerOrEntityId?.entity_id;
    return !!entityId && this._excludedPlayerPreferences().includes(entityId);
  }
  _playerSortMode() {
    const mode = HomeiiMobileSettingsFoundation.normalizePlayerSortMode(this._state.playerSortMode);
    this._state.playerSortMode = mode;
    return mode;
  }
  _playerOrderPreferences() {
    const next = HomeiiMobileSettingsFoundation.normalizePinnedPlayerEntityList(this._state.playerOrderEntities);
    this._state.playerOrderEntities = next;
    return next;
  }
  _playerDisplayName(player = null) {
    return String(player?.attributes?.friendly_name || player?.entity_id || "").trim();
  }
  _sortPlayerList(players = []) {
    const list = Array.isArray(players) ? players.slice() : [];
    const locale = this._state.lang === "he" || this._state.lang === "en" ? this._state.lang : void 0;
    const byName = (left, right) => this._playerDisplayName(left).localeCompare(this._playerDisplayName(right), locale, { sensitivity: "base", numeric: true });
    const mode = this._playerSortMode();
    if (mode === "alphabetical") return list.sort(byName);
    if (mode !== "custom") return list;
    const order = this._playerOrderPreferences();
    const orderIndex = new Map(order.map((entityId, index) => [entityId, index]));
    return list.sort((left, right) => {
      const leftIndex = orderIndex.has(left?.entity_id) ? orderIndex.get(left.entity_id) : Number.MAX_SAFE_INTEGER;
      const rightIndex = orderIndex.has(right?.entity_id) ? orderIndex.get(right.entity_id) : Number.MAX_SAFE_INTEGER;
      if (leftIndex !== rightIndex) return leftIndex - rightIndex;
      return byName(left, right);
    });
  }
  _pinnedPlayerOptionPlayers(extraPlayers = [], options = {}) {
    const includeExcluded = options?.includeExcluded === true;
    const byId = /* @__PURE__ */ new Map();
    const add = (player) => {
      if (!player?.entity_id) return;
      if (!this._isMusicAssistantPlayer(player)) return;
      if (this._isLikelyBrowserPlayer(player) || this._isLocalSendspinPlayer(player)) return;
      if (!includeExcluded && this._isPlayerExcluded(player)) return;
      byId.set(player.entity_id, player);
    };
    Object.values(this._hass?.states || {}).forEach(add);
    (Array.isArray(extraPlayers) ? extraPlayers : []).forEach(add);
    (this._state.configurableMusicAssistantPlayers || []).forEach(add);
    (this._state.players || []).forEach(add);
    return this._sortPlayerList(Array.from(byId.values()));
  }
  _resolvedPinnedPlayerEntities(players = this._state.players || []) {
    const next = HomeiiPlayersFoundation.resolvePinnedPlayerEntities(this._pinnedPlayerPreferences(), players);
    this._state.pinnedPlayerEntities = next;
    return next;
  }
  _resolvedPinnedPlayerEntity(players = this._state.players || []) {
    return this._resolvedPinnedPlayerEntities(players)[0] || "";
  }
  _pinnedPlayerCount(players = this._state.players || []) {
    return this._resolvedPinnedPlayerEntities(players).length;
  }
  _hasPinnedPlayer() {
    return this._pinnedPlayerCount() > 0;
  }
  _isHotelMode() {
    return this._config?.hotel_mode === true || this._state.hotelMode === true;
  }
  _frontPinnedPlayerEntity(players = this._state.players || []) {
    const entityId = String(this._state.frontPinnedPlayerEntity || "").trim();
    if (!entityId) return "";
    const exists = (Array.isArray(players) ? players : []).some((player) => player?.entity_id === entityId);
    if (exists && !this._isPlayerExcluded(entityId)) return entityId;
    this._state.frontPinnedPlayerEntity = "";
    try {
      localStorage.removeItem("homeii_music_flow_front_pinned_player");
    } catch (_) {
    }
    return "";
  }
  _manualFrontPlayerEntity(players = this._state.players || []) {
    const entityId = String(this._state.manualFrontPlayerEntity || "").trim();
    const until = Number(this._state.manualFrontPlayerUntil || 0);
    if (!entityId || !until || until <= Date.now()) {
      const player = this._playerByEntityId(entityId);
      if (player?.state === "playing" && !this._isPlayerExcluded(entityId)) {
        this._setManualFrontPlayer(entityId, this._manualFrontDefaultHoldMs());
        return entityId;
      }
      this._state.manualFrontPlayerEntity = "";
      this._state.manualFrontPlayerUntil = 0;
      return "";
    }
    const exists = (Array.isArray(players) ? players : []).some((player) => player?.entity_id === entityId);
    if (exists && !this._isPlayerExcluded(entityId)) return entityId;
    this._state.manualFrontPlayerEntity = "";
    this._state.manualFrontPlayerUntil = 0;
    return "";
  }
  _manualFrontDefaultHoldMs() {
    return 5 * 60 * 1e3;
  }
  _manualFrontContentHoldMs() {
    return 5 * 60 * 1e3;
  }
  _isManualFrontContentSelectionPage(page = this._state?.menuPage || "") {
    const value = String(page || "").trim().toLowerCase();
    return value === "media_detail" || value === "discovery" || value === "quick_search" || value === "simple_wizard" || value.startsWith("library_");
  }
  _shouldHoldManualFrontForContentSelection() {
    if (!this._state?.menuOpen) return false;
    if (this._isManualFrontContentSelectionPage(this._state.menuPage)) return true;
    if (String(this._state.menuPage || "") !== "players") return false;
    const previousPage = Array.isArray(this._state.menuStack) ? this._state.menuStack[this._state.menuStack.length - 1] : "";
    return this._isManualFrontContentSelectionPage(previousPage);
  }
  _manualFrontHoldMsForSelection() {
    return this._shouldHoldManualFrontForContentSelection() ? this._manualFrontContentHoldMs() : this._manualFrontDefaultHoldMs();
  }
  _refreshManualFrontPlayerHold(durationMs = this._manualFrontHoldMsForSelection()) {
    const entityId = String(this._state.manualFrontPlayerEntity || "").trim();
    const until = Number(this._state.manualFrontPlayerUntil || 0);
    if (!entityId || until <= Date.now() || !this._playerByEntityId(entityId)) return false;
    this._setManualFrontPlayer(entityId, durationMs);
    return true;
  }
  _shortenManualFrontPlayerHold(durationMs = this._manualFrontDefaultHoldMs()) {
    const entityId = String(this._state.manualFrontPlayerEntity || "").trim();
    const until = Number(this._state.manualFrontPlayerUntil || 0);
    if (!entityId || until <= Date.now() || !this._playerByEntityId(entityId)) return false;
    const duration = Math.max(1e3, Number(durationMs || this._manualFrontDefaultHoldMs()));
    if (until <= Date.now() + duration) return false;
    this._setManualFrontPlayer(entityId, duration);
    return true;
  }
  _setManualFrontPlayer(entityId = "", durationMs = 0) {
    const nextEntityId = String(entityId || "").trim();
    if (!nextEntityId) {
      this._clearManualFrontPlayer({ sync: false });
      return;
    }
    const duration = Math.max(1e3, Number(durationMs || this._manualFrontDefaultHoldMs()));
    this._state.manualFrontPlayerEntity = nextEntityId;
    this._state.manualFrontPlayerUntil = Date.now() + duration;
    this._manualFrontLocationKey = this._currentWindowLocationKey();
    this._startManualFrontRouteTracking();
    clearTimeout(this._manualFrontPlayerTimer);
    const scheduledEntityId = nextEntityId;
    this._manualFrontPlayerTimer = setTimeout(() => {
      if (String(this._state.manualFrontPlayerEntity || "") !== scheduledEntityId) return;
      if (Number(this._state.manualFrontPlayerUntil || 0) > Date.now()) return;
      const scheduledPlayer = this._playerByEntityId(scheduledEntityId);
      if (scheduledPlayer?.state === "playing") {
        this._setManualFrontPlayer(scheduledEntityId, this._manualFrontDefaultHoldMs());
        return;
      }
      if (this._shouldHoldManualFrontForContentSelection() && this._playerByEntityId(scheduledEntityId)) {
        this._setManualFrontPlayer(scheduledEntityId, this._manualFrontContentHoldMs());
        return;
      }
      this._clearManualFrontPlayer();
    }, duration + 80);
  }
  _clearManualFrontPlayer(options = {}) {
    const hadManualFront = !!(this._state.manualFrontPlayerEntity || this._state.manualFrontPlayerUntil);
    clearTimeout(this._manualFrontPlayerTimer);
    this._manualFrontPlayerTimer = null;
    this._state.manualFrontPlayerEntity = "";
    this._state.manualFrontPlayerUntil = 0;
    this._manualFrontLocationKey = "";
    this._stopManualFrontRouteTracking();
    if (!hadManualFront || options.sync === false) return;
    this._loadPlayers();
    this._syncNowPlayingUI();
    this._renderPlayerSummary();
    if (this._state.menuOpen && this._state.menuPage === "players") this._renderMobileMenu().catch(() => {
    });
  }
  _currentWindowLocationKey() {
    try {
      return `${window.location?.pathname || "/"}${window.location?.search || ""}${window.location?.hash || ""}`;
    } catch {
      return "/";
    }
  }
  _startManualFrontRouteTracking() {
    if (this._manualFrontRouteListening || typeof window === "undefined" || typeof window.addEventListener !== "function") return;
    window.addEventListener("location-changed", this._boundManualFrontRouteChange);
    window.addEventListener("popstate", this._boundManualFrontRouteChange);
    this._manualFrontRouteListening = true;
  }
  _stopManualFrontRouteTracking() {
    if (!this._manualFrontRouteListening || typeof window === "undefined" || typeof window.removeEventListener !== "function") return;
    window.removeEventListener("location-changed", this._boundManualFrontRouteChange);
    window.removeEventListener("popstate", this._boundManualFrontRouteChange);
    this._manualFrontRouteListening = false;
  }
  _handleManualFrontRouteChange() {
    if (!this._state?.manualFrontPlayerEntity || !this._manualFrontLocationKey) return;
    if (this._currentWindowLocationKey() !== this._manualFrontLocationKey) this._clearManualFrontPlayer();
  }
  _setFrontPinnedPlayer(entityId = "") {
    const nextEntityId = String(entityId || "").trim();
    this._state.frontPinnedPlayerEntity = nextEntityId;
    try {
      if (nextEntityId) localStorage.setItem("homeii_music_flow_front_pinned_player", nextEntityId);
      else localStorage.removeItem("homeii_music_flow_front_pinned_player");
    } catch (_) {
    }
  }
  _toggleFrontPinnedPlayer(entityId = "") {
    const target = String(entityId || "").trim();
    if (!target) return;
    const next = this._frontPinnedPlayerEntity() === target ? "" : target;
    this._setFrontPinnedPlayer(next);
    if (next) this._selectPlayer(next, true);
    this._loadPlayers();
    this._syncNowPlayingUI();
    this._toast(next ? this._m("Player pinned to front", "הנגן נעוץ לחזית") : this._m("Front pin cleared", "נעץ החזית בוטל"));
  }
  _mobileVolumeMode() {
    return HomeiiMobileSettingsFoundation.normalizeMobileVolumeMode(this._state.mobileVolumeMode);
  }
  _mobileVolumeStepButtonsEnabled() {
    return this._state.mobileVolumeStepButtonsEnabled === true;
  }
  _mobileVolumeStepPercent() {
    const step = HomeiiMobileSettingsFoundation.clampMobileVolumeStepPercent(this._state.mobileVolumeStepPercent);
    this._state.mobileVolumeStepPercent = step;
    return step;
  }
  _entityMatchTokens(value = "") {
    return HomeiiPlayersFoundation.entityMatchTokens(value);
  }
  _favoriteButtonEntityForPlayer(player = null) {
    return HomeiiPlayersFoundation.favoriteButtonEntityForPlayer({
      player: player || this._getSelectedPlayer(),
      hassStates: this._hass?.states || {},
      explicitEntity: this._config?.favorite_button_entity || "",
      fallbackEntity: "button.bathroom_favorite_current_song_2"
    });
  }
  _favoriteButtonDeviceId(entityId = "") {
    return HomeiiPlayersFoundation.favoriteButtonDeviceId(entityId, this._hass?.entities || {});
  }
  _favoriteButtonEntity() {
    return this._favoriteButtonEntityForPlayer(this._getSelectedPlayer());
  }
  _entryTargetsCurrentMedia(entry = {}) {
    return HomeiiMediaQueueFoundation.entryTargetsCurrentMedia(
      entry,
      this._currentMediaLikeMeta(),
      (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
    );
  }
  async _pressFavoriteButtonEntity(entityId = "") {
    const target = String(entityId || "").trim();
    if (!target) return false;
    const deviceId = this._favoriteButtonDeviceId(target);
    if (deviceId) {
      await this._hass.callService("button", "press", { device_id: deviceId });
      return true;
    }
    await this._hass.callService("button", "press", { entity_id: target });
    return true;
  }
  async _unfavoriteCurrentViaMassQueue() {
    const player = this._getSelectedPlayer();
    const entityId = String(player?.entity_id || this._state.selectedPlayer || "").trim();
    if (!entityId || !this._hass?.services?.mass_queue?.unfavorite_current_item) return false;
    await this._hass.callService("mass_queue", "unfavorite_current_item", { entity: entityId });
    return true;
  }
  async _refreshFavoriteState(force = true) {
    this._cache.library.delete("liked:ma");
    await Promise.allSettled([
      this._loadMaLikedEntries(force),
      this._ensureQueueSnapshot(force)
    ]);
    const override = this._state.currentMediaFavoriteOverride || null;
    if (override) {
      const currentUri = String(this._getCurrentMediaUri() || "").trim();
      const queueFavorite = this._state.maQueueState?.current_item?.media_item?.favorite;
      if (!currentUri || currentUri !== override.uri || Date.now() - Number(override.ts || 0) > 8e3) {
        this._clearCurrentMediaFavoriteOverride();
      } else if (typeof queueFavorite === "boolean" && queueFavorite === !!override.liked) {
        this._clearCurrentMediaFavoriteOverride();
      }
    }
    this._syncNowPlayingUI();
    this._syncLikeButtons();
    if (this._state.menuOpen && this._state.menuPage === "library_liked") {
      this._renderMobileMenu().catch(() => {
      });
    }
  }
  async _waitForFavoriteState(entry = {}, expected = true, delays = [500, 1500, 3200]) {
    for (const delay of delays) {
      await new Promise((resolve) => window.setTimeout(resolve, delay));
      await this._refreshFavoriteState(true);
      if (!!this._isEntryLiked(entry) === !!expected) return true;
    }
    return !!this._isEntryLiked(entry) === !!expected;
  }
  _useMaLikedMode() {
    if (this._config?.allow_local_likes !== true) return true;
    return String(this._state.mobileLikedMode || "ma") === "ma";
  }
  _mobileSwipeMode() {
    return String(this._state.mobileSwipeMode || "play") === "browse" ? "browse" : "play";
  }
  _activePlayingPlayers() {
    return (this._state.players || []).filter((p) => p?.state === "playing");
  }
  _activePlaybackPlayers() {
    return [
      ...Array.isArray(this._state.players) ? this._state.players : [],
      ...Array.isArray(this._directMaPlayers) ? this._directMaPlayers : []
    ].filter((player) => {
      const state = String(player?.state || "").toLowerCase();
      return state === "playing" || state === "paused" || state === "buffering";
    });
  }
  _isStopClearTarget(entityId, groupedIds = []) {
    const targetId = String(entityId || "").trim();
    if (!targetId) return false;
    const player = this._playerByEntityId(targetId);
    if (!player) return false;
    if (this._isLocalSendspinPlayer(player)) return false;
    if (this._isExternalBrowserPlayer(player)) return false;
    const state = String(player.state || "").toLowerCase();
    if (state === "unavailable" || state === "unknown" || state === "off") return false;
    if (this._isDirectMaPlayer(player)) return true;
    if (groupedIds.includes(targetId)) return true;
    return true;
  }
  _clearLocalPlaybackStateForPlayers(entityIds = []) {
    const ids = new Set((Array.isArray(entityIds) ? entityIds : []).map((id) => String(id || "").trim()).filter(Boolean));
    if (!ids.size) return;
    const mediaKeys = [
      "media_content_id",
      "media_content_type",
      "media_title",
      "media_artist",
      "media_album_name",
      "media_album_artist",
      "media_duration",
      "media_position",
      "media_position_updated_at",
      "media_image",
      "media_image_url",
      "entity_picture",
      "entity_picture_local",
      "active_queue",
      "active_queue_items"
    ];
    const cleanPlayer = (player) => {
      if (!player?.entity_id || !ids.has(player.entity_id)) return player;
      const attrs = { ...player.attributes || {} };
      mediaKeys.forEach((key) => {
        delete attrs[key];
      });
      return { ...player, state: "idle", attributes: attrs };
    };
    if (Array.isArray(this._state.players)) this._state.players = this._state.players.map(cleanPlayer);
    if (Array.isArray(this._directMaPlayers)) this._directMaPlayers = this._directMaPlayers.map(cleanPlayer);
    if (ids.has(this._state.selectedPlayer)) {
      this._state.maQueueState = null;
      this._state.queueItems = [];
      this._state.forceRadioHero = false;
      this._state.mobileDynamicThemeArtwork = "";
      this._state.mobileDynamicThemeArtworkUrl = "";
    }
  }
  _announcementEligiblePlayers() {
    return HomeiiPlayersFoundation.announcementEligiblePlayers(this._state.players || []);
  }
  _announcementTargetValue() {
    const raw = String(this._state.mobileAnnouncementTarget || "").trim();
    if (raw === "all") return "all";
    const eligible = this._announcementEligiblePlayers();
    if (eligible.some((player) => player.entity_id === raw)) return raw;
    return this._state.selectedPlayer || eligible[0]?.entity_id || "";
  }
  _announcementVolumePct() {
    const raw = Number(this._state.mobileAnnouncementVolume ?? this._config?.mobile_announcement_volume ?? 20);
    return Number.isFinite(raw) ? Math.max(20, Math.min(50, raw)) : 20;
  }
  _mobileNavigableActivePlayers() {
    return HomeiiPlayersFoundation.mobileNavigableActivePlayers(
      this._state.players || [],
      this._resolvedPinnedPlayerEntities(),
      (player) => this._isPlayerActive(player)
    );
  }
  _syncMobilePlayerNavButtons() {
    const enabled = this._mobileNavigableActivePlayers().length > 1;
    ["activePlayerPrevBtn", "activePlayerNextBtn"].forEach((id) => {
      const btn = this.$(id);
      if (!btn) return;
      btn.disabled = !enabled;
      btn.setAttribute("aria-disabled", enabled ? "false" : "true");
    });
  }
  _setMobileRandomFabDisabled(disabled) {
    const btn = this.$("mobileRandomBtn");
    if (!btn) return;
    btn.disabled = !!disabled;
    btn.classList.toggle("disabled", !!disabled);
    btn.setAttribute("aria-disabled", disabled ? "true" : "false");
  }
  _setMobileRandomFabVisible(visible) {
    const btn = this.$("mobileRandomBtn");
    if (!btn) return;
    btn.hidden = !visible;
    btn.classList.toggle("hidden", !visible);
  }
  _mobileQuickActionOptions() {
    return [
      { value: "home", icon: "home", label: this._i18n("ui.home") },
      { value: "search", icon: "search", label: this._i18n("ui.search") },
      { value: "timer", icon: "timer", label: this._i18n("ui.timer") },
      { value: "like", icon: "heart_outline", label: this._i18n("ui.liked") },
      { value: "lyrics", icon: "lyrics", label: this._i18n("ui.lyrics") },
      { value: "queue", icon: "queue", label: this._i18n("ui.queue_2") },
      { value: "radio", icon: "radio", label: this._i18n("ui.quick_mix") },
      { value: "voice", icon: "mic", label: this._flowAssistantLabel() },
      { value: "history", icon: "history", label: this._i18n("ui.history") },
      { value: "info", icon: "info", label: this._i18n("ui.info") },
      { value: "disconnect_all", icon: "close", label: this._cleanAllLabel(), tone: "danger" }
    ];
  }
  _mobileQuickActionButtonHtml(action = "", historyToggleButtonHtml = "") {
    const auxiliaryMatch = /^auxiliary:(\d+)$/.exec(String(action || ""));
    if (auxiliaryMatch) {
      const index = Math.max(0, Number(auxiliaryMatch[1]) || 0);
      const button = this._auxiliaryButtonConfigs()[index];
      if (!button?.enabled) return "";
      const label = String(button.name || "").trim() || this._i18n("ui.auxiliary_button");
      const icon = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(button.icon || "power");
      return `<button class="mobile-art-fab power-fab auxiliary-fab" data-auxiliary-index="${this._esc(index)}" title="${this._esc(label)}" aria-label="${this._esc(label)}">${this._iconSvg(icon)}</button>`;
    }
    switch (action) {
      case "timer": {
        const label = this._sleepTimerFooterLabel();
        const active = !!label && this._sleepTimerChipVisible();
        return `
          <button class="mobile-art-fab mobile-timer-fab ${active ? "active" : ""}" id="mobileTimerBtn" title="${this._esc(this._i18n("ui.schedules"))}">
            ${this._iconSvg("timer")}
            <span class="mobile-timer-label" ${active ? "" : "hidden"}>${this._esc(active ? label : "")}</span>
          </button>
        `;
      }
      case "home":
        return `<button class="mobile-art-fab" id="mobileHomeQuickBtn" title="${this._i18n("ui.home")}">${this._iconSvg("home")}</button>`;
      case "search":
        return `<button class="mobile-art-fab" id="mobileQuickSearchBtn" title="${this._i18n("ui.search")}" aria-label="${this._i18n("ui.search")}">${this._iconSvg("search")}</button>`;
      case "like":
        return `<button class="mobile-art-fab" id="mobileLikeBtn" title="${this._i18n("ui.like")}">${this._iconSvg(this._currentMediaFavoriteState() ? "heart_filled" : "heart_outline")}</button>`;
      case "lyrics":
        return `<button class="mobile-art-fab" id="mobileLyricsBtn" title="${this._i18n("ui.lyrics")}">${this._iconSvg("lyrics")}</button>`;
      case "queue":
        return `<button class="mobile-art-fab" id="mobileQueueBtn" title="${this._i18n("ui.open_queue")}">${this._iconSvg("queue")}</button>`;
      case "radio":
        return `<button class="mobile-art-fab" id="mobileRandomBtn" title="${this._i18n("ui.quick_mix")}">${this._iconSvg("radio")}</button>`;
      case "voice":
        return `<button class="mobile-art-fab voice-assistant-fab ${this._state.voiceAssistantListening ? "listening" : ""}" id="mobileVoiceAssistantBtn" title="${this._esc(this._flowAssistantLabel())}" aria-label="${this._esc(this._flowAssistantLabel())}">${this._iconSvg("mic")}</button>`;
      case "history":
        return historyToggleButtonHtml || `<button class="mobile-art-fab history-toggle-fab empty-history-fab" id="historyToggleFab" title="${this._i18n("ui.recently_played_2")}" aria-expanded="false">${this._iconSvg("history")}</button>`;
      case "info":
        return `<button class="mobile-art-fab" id="mobileInfoBtn" title="${this._i18n("ui.more_info")}">${this._iconSvg("info")}</button>`;
      case "disconnect_all":
        return `<button class="mobile-art-fab danger-fab" id="mobileDisconnectAllBtn" title="${this._esc(this._cleanAllLabel())}" aria-label="${this._esc(this._cleanAllLabel())}">${this._iconSvg("close")}</button>`;
      case "power":
        return this._mobileQuickActionButtonHtml("auxiliary:0", historyToggleButtonHtml);
      default:
        return "";
    }
  }
  _mobileActionsWithAuxiliary(actions = []) {
    if (this._isHotelMode()) return (Array.isArray(actions) ? actions : []).filter((action) => action === "search");
    const next = Array.isArray(actions) ? actions.slice() : [];
    this._enabledAuxiliaryButtons().forEach((button) => {
      const id = `auxiliary:${button.index}`;
      if (!next.includes(id)) next.push(id);
    });
    return next;
  }
  _mobileVisibleQuickActions(actions = this._mobileQuickActions(), player = this._getSelectedPlayer()) {
    if (this._isHotelMode()) return ["search"];
    const blocked = this._isLocalSendspinPlayer(player) ? /* @__PURE__ */ new Set(["like", "radio"]) : null;
    return (Array.isArray(actions) ? actions : this._mobileQuickActions()).filter((action) => action !== "voice" || this._voiceAssistantEnabled()).filter((action) => !blocked?.has(action));
  }
  _mobileEmptyVisibleQuickActions(actions = this._mobileQuickActions(), player = this._getSelectedPlayer()) {
    const relevant = /* @__PURE__ */ new Set(["home", "search", "timer", "radio", "voice", "history"]);
    if (this._activePlayingPlayers().length) relevant.add("disconnect_all");
    return this._mobileVisibleQuickActions(actions, player).filter((action) => relevant.has(action));
  }
  _mobileQuickActionButtonsHtml(historyToggleButtonHtml = "", actions = this._mobileQuickActions()) {
    return this._mobileVisibleQuickActions(actions).map((action) => this._mobileQuickActionButtonHtml(action, historyToggleButtonHtml)).filter(Boolean).join("");
  }
  _activeQuickActionRowState() {
    const layoutMode = this._layoutModeConfig();
    const actions = this._mobileActionsWithAuxiliary(
      this._mobileVisibleQuickActions(this._mobileQuickActions())
    );
    const rowActions = layoutMode === "tablet" ? actions.filter((action) => action !== "history" && action !== "timer") : actions;
    const historyEdgeClass = this._isHebrew() ? "left-edge" : "right-edge";
    const historyToggleButtonHtml = layoutMode !== "tablet" && actions.includes("history") ? `<button class="history-toggle-fab ${historyEdgeClass}" id="historyToggleFab" title="${this._i18n("ui.recently_played_2")}" aria-expanded="false" hidden>${this._iconSvg("history")}</button>` : "";
    return { layoutMode, actions, rowActions, historyToggleButtonHtml };
  }
  _syncActiveQuickActionRow(options = {}) {
    if (this._isCompactTileMode()) return;
    const host = this.$("mobileArtActions");
    if (!host) return;
    const { layoutMode, rowActions, historyToggleButtonHtml } = this._activeQuickActionRowState();
    const signature = [
      layoutMode,
      rowActions.join(","),
      historyToggleButtonHtml ? "history-inline" : "history-floating",
      this._enabledAuxiliaryButtons().map((button) => `${button.index}:${button.icon}:${button.name}`).join("|"),
      this._voiceAssistantEnabled() ? "voice-on" : "voice-off"
    ].join(";");
    const needsRefresh = options.force === true || host.dataset.homeiiActionSignature !== signature || host.classList.contains("empty-quick-actions") || host.querySelector?.(".empty-history-fab");
    if (!needsRefresh) {
      host.removeAttribute("hidden");
      return;
    }
    const html = this._mobileQuickActionButtonsHtml(historyToggleButtonHtml, rowActions);
    host.className = `mobile-art-actions count-${Math.max(1, rowActions.length)}`;
    host.dataset.homeiiActionSignature = signature;
    host.innerHTML = html;
    if (html) host.removeAttribute("hidden");
    else host.setAttribute("hidden", "");
    this._bindMobileQuickActionButtons();
  }
  _pressUiButton(btn, pattern = [6]) {
    if (!btn || btn.disabled) return false;
    this._hapticTap(pattern);
    btn.classList.remove("pressed");
    void btn.offsetWidth;
    btn.classList.add("pressed");
    clearTimeout(btn._pressUiTimer);
    btn._pressUiTimer = setTimeout(() => btn.classList.remove("pressed"), 180);
    return true;
  }
  _libraryInteractionTarget(target = null) {
    const selector = [
      ".media-entry",
      ".discovery-orb",
      ".library-nav-btn",
      ".media-layout-btn",
      ".media-detail-play-btn",
      ".media-detail-action-btn",
      ".media-detail-nav-btn",
      ".artist-hero-icon-btn",
      ".radio-stage-fab",
      ".library-tab-search-submit",
      ".library-tab-search-clear",
      ".radio-country-entry",
      ".media-category-row",
      ".media-detail-hero",
      ".library-player-focus"
    ].join(",");
    const el = target?.closest?.(selector) || target;
    return el?.classList ? el : null;
  }
  _showLibraryInteractionFeedback(target = null, options = {}) {
    const el = this._libraryInteractionTarget(target);
    if (!el) return null;
    const loading = options.loading === true;
    if (options.press !== false) this._flashInteraction(el);
    el.classList.add("library-action-feedback");
    clearTimeout(el._homeiiLibraryFeedbackTimer);
    el._homeiiLibraryFeedbackTimer = setTimeout(() => {
      el?.classList?.remove("library-action-feedback");
    }, Math.max(140, Number(options.pressMs || 220) || 220));
    if (loading) {
      el.classList.add("library-action-loading");
      el.dataset.homeiiLibraryActionBusy = "1";
      el.setAttribute("aria-busy", "true");
      clearTimeout(el._homeiiLibraryLoadingTimer);
      const loadingMs = Math.max(0, Number(options.loadingMs || 0) || 0);
      if (loadingMs && options.hold !== true) {
        el._homeiiLibraryLoadingTimer = setTimeout(() => this._clearLibraryInteractionFeedback(el), loadingMs);
      }
    }
    return el;
  }
  _clearLibraryInteractionFeedback(target = null) {
    const el = this._libraryInteractionTarget(target);
    if (!el) return;
    clearTimeout(el._homeiiLibraryFeedbackTimer);
    clearTimeout(el._homeiiLibraryLoadingTimer);
    el._homeiiLibraryFeedbackTimer = null;
    el._homeiiLibraryLoadingTimer = null;
    el.classList.remove("library-action-feedback", "library-action-loading");
    if (el.dataset?.homeiiLibraryActionBusy === "1") {
      delete el.dataset.homeiiLibraryActionBusy;
      el.removeAttribute("aria-busy");
    }
  }
  _lockUiButton(btn, pattern = [6], options = {}) {
    if (!btn || btn.disabled || btn.dataset.homeiiActionLocked === "1") return false;
    const lockMs = Math.max(350, Number(options.lockMs || 900) || 900);
    if (!this._pressUiButton(btn, pattern)) return false;
    btn.dataset.homeiiActionLocked = "1";
    btn.disabled = options.disabled !== false;
    btn.classList.add("busy");
    btn.setAttribute("aria-busy", "true");
    clearTimeout(btn._homeiiActionLockTimer);
    btn._homeiiActionLockTimer = setTimeout(() => this._unlockUiButton(btn), lockMs);
    return true;
  }
  _unlockUiButton(btn) {
    if (!btn) return;
    clearTimeout(btn._homeiiActionLockTimer);
    btn._homeiiActionLockTimer = null;
    delete btn.dataset.homeiiActionLocked;
    btn.disabled = false;
    btn.classList.remove("busy");
    btn.removeAttribute("aria-busy");
  }
  async _runLockedUiAction(btn, action, options = {}) {
    if (!this._lockUiButton(btn, options.pattern || [6], options)) return null;
    try {
      return await action();
    } finally {
      if (options.hold !== true) this._unlockUiButton(btn);
    }
  }
  async _runMenuButtonLoading(btn, loadingText, action, options = {}) {
    if (!btn || btn.disabled || btn.dataset.homeiiActionLocked === "1") return false;
    const originalHtml = btn.innerHTML;
    const loadingKind = String(options.kind || "").trim();
    btn.dataset.homeiiActionLocked = "1";
    btn.disabled = true;
    btn.classList.add("busy");
    if (loadingKind) btn.classList.add(`busy-${loadingKind}`);
    btn.setAttribute("aria-busy", "true");
    btn.innerHTML = `<span>${this._esc(loadingText || this._i18n("ui.loading"))}</span>`;
    try {
      return await action();
    } finally {
      btn.innerHTML = originalHtml;
      btn.disabled = false;
      btn.classList.remove("busy");
      if (loadingKind) btn.classList.remove(`busy-${loadingKind}`);
      btn.removeAttribute("aria-busy");
      delete btn.dataset.homeiiActionLocked;
    }
  }
  _showEmptyMagicRipple(sourceEl = null) {
    const card = this.shadowRoot?.querySelector?.(".card.empty-media");
    if (!card) return;
    const rect = card.getBoundingClientRect?.();
    const sourceRect = sourceEl?.getBoundingClientRect?.();
    if (!rect) return;
    const x = sourceRect ? sourceRect.left + sourceRect.width / 2 - rect.left : rect.width / 2;
    const y = sourceRect ? sourceRect.top + sourceRect.height / 2 - rect.top : rect.height / 2;
    const ripple = document.createElement("span");
    ripple.className = "empty-magic-screen-ripple";
    ripple.style.setProperty("--empty-ripple-x", `${Math.round(x)}px`);
    ripple.style.setProperty("--empty-ripple-y", `${Math.round(y)}px`);
    card.appendChild(ripple);
    setTimeout(() => ripple.remove(), 1100);
  }
  _showEmptyPlaybackLoading(sourceEl = null) {
    const card = this.shadowRoot?.querySelector?.(".card.empty-media");
    if (!card) return;
    this._hideEmptyPlaybackLoading();
    const rect = card.getBoundingClientRect?.();
    const sourceRect = sourceEl?.getBoundingClientRect?.();
    const loader = document.createElement("span");
    loader.className = "empty-playback-loader";
    if (rect && sourceRect) {
      loader.style.setProperty("--empty-loader-x", `${Math.round(sourceRect.left + sourceRect.width / 2 - rect.left)}px`);
      loader.style.setProperty("--empty-loader-y", `${Math.round(sourceRect.top + sourceRect.height / 2 - rect.top)}px`);
    }
    loader.innerHTML = `<span></span><span></span><span></span>`;
    card.appendChild(loader);
    clearTimeout(this._emptyPlaybackLoaderTimer);
    this._emptyPlaybackLoaderTimer = setTimeout(() => this._hideEmptyPlaybackLoading(), 6500);
  }
  _hideEmptyPlaybackLoading() {
    clearTimeout(this._emptyPlaybackLoaderTimer);
    this._emptyPlaybackLoaderTimer = null;
    this.shadowRoot?.querySelectorAll?.(".empty-playback-loader")?.forEach((loader) => loader.remove());
  }
  _cycleActivePlayer(step = 1) {
    const players = this._mobileNavigableActivePlayers();
    if (players.length < 2) {
      this._syncMobilePlayerNavButtons();
      return;
    }
    const currentId = this._state.selectedPlayer;
    const currentIndex = players.findIndex((player) => player.entity_id === currentId);
    const baseIndex = currentIndex >= 0 ? currentIndex : 0;
    const nextIndex = (baseIndex + step + players.length) % players.length;
    this._selectPlayer(players[nextIndex]?.entity_id, true);
  }
  _playerGroupMemberIds(player) {
    return HomeiiPlayersFoundation.playerGroupMemberIds(player);
  }
  _playerGroupCount(player) {
    return HomeiiPlayersFoundation.playerGroupCount(player);
  }
  _playerGroupMemberNames(player) {
    return HomeiiPlayersFoundation.playerGroupMemberNames(player, this._state.players || []);
  }
  _isStaticGroupPlayer(player) {
    return HomeiiPlayersFoundation.isStaticGroupPlayer(player);
  }
  _groupAverageVolume(player) {
    return HomeiiPlayersFoundation.groupAverageVolume(player, this._state.players || []);
  }
  _groupedPlayerIds() {
    return HomeiiPlayersFoundation.groupedPlayerIds(this._state.players || []);
  }
  async _disconnectPlayerGroups(options = {}) {
    const ids = this._groupedPlayerIds();
    if (!ids.length) {
      if (!options.silent) this._toastSuccess(this._i18n("ui.no_player_groups_to_disconnect"));
      return { ok: true, count: 0, failed: false };
    }
    const dynamicIds = ids.filter((id) => {
      const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
      return target && !this._isStaticGroupPlayer(target) && !this._isLocalSendspinPlayer(target);
    });
    if (!dynamicIds.length) {
      if (!options.silent) this._toastSuccess(this._i18n("ui.no_dynamic_player_groups_to_disconnect"));
      return { ok: true, count: 0, failed: false };
    }
    const results = await Promise.allSettled(dynamicIds.map((id) => this._callHaMediaPlayerService(id, "unjoin")));
    const succeeded = results.filter((result) => result.status === "fulfilled").length;
    const failed = succeeded === 0;
    setTimeout(() => {
      this._loadPlayers();
      this._refreshGroupingState();
      if (this._state.menuOpen) this._renderMobileMenu();
    }, 550);
    if (!options.silent) {
      (failed ? this._toastError : this._toastSuccess).call(this, failed ? this._i18n("ui.player_groups_could_not_be_disconnected") : this._i18n("ui.all_player_groups_disconnected"));
    }
    return { ok: !failed, count: dynamicIds.length, failed };
  }
  async _stopAllPlayers() {
    const activePlayers = this._activePlaybackPlayers();
    const groupedIds = this._groupedPlayerIds();
    const knownPlayers = [
      ...Array.isArray(this._state.players) ? this._state.players : [],
      ...Array.isArray(this._directMaPlayers) ? this._directMaPlayers : [],
      ...activePlayers
    ];
    const targetIds = [...new Set([
      ...knownPlayers.map((player) => player?.entity_id).filter(Boolean),
      ...groupedIds,
      this._state.selectedPlayer
    ].filter(Boolean))].filter((entityId) => this._isStopClearTarget(entityId, groupedIds));
    const hasLocalSendspin = this._isLocalSendspinDesired() || this._localSendspinConnected || this._localSendspinConnecting || !!this._localSendspinPlayer || !!this._localSendspinSocket;
    if (!targetIds.length && !hasLocalSendspin) {
      this._toastError(this._i18n("ui.no_active_players"));
      return;
    }
    this._hapticTap([18, 24, 18]);
    const groupResult = await this._disconnectPlayerGroups({ silent: true });
    if (groupResult.ok) groupedIds.forEach((entityId) => this._clearLocalGroupState(entityId));
    const results = await Promise.allSettled(targetIds.map(async (entityId) => {
      const errors = [];
      try {
        await this._stopPlayer(entityId);
      } catch (error) {
        errors.push(error);
      }
      try {
        await this._clearQueueForPlayer(entityId);
      } catch (error) {
        errors.push(error);
      }
      if (errors.length) throw errors[0];
    }));
    const locallyClearedIds = targetIds.filter((entityId, index) => results[index]?.status === "fulfilled");
    this._clearLocalPlaybackStateForPlayers(locallyClearedIds);
    if (hasLocalSendspin) {
      this._adoptLocalSendspinGlobalSession();
      const selectedPlayer = this._getSelectedPlayer();
      if (this._isLocalSendspinPlayer(selectedPlayer)) {
        this._state.selectedPlayer = null;
        this._state.hasAutoSelectedPlayer = false;
      }
      this._state.localSendspinDisconnecting = true;
      this._setLocalSendspinDesired(false);
      this._rememberThisDevicePlayer("");
      this._state.awaitingThisDevicePlayer = false;
      this._state.controlRoomRevealThisDevicePending = false;
      this._state.knownBrowserPlayerIds = [];
      this._directMaPlayers = [];
      this._stopLocalSendspinPlayer("stop_all");
      this._state.localSendspinDisconnecting = false;
      this._state.localSendspinStatus = "disconnected";
      this._syncLocalSendspinGlobalSession();
    }
    const failed = results.some((result) => result.status === "rejected") || groupResult.failed;
    (failed ? this._toastError : this._toastSuccess).call(this, failed ? this._i18n("ui.some_players_could_not_be_stopped_or_cleared") : this._i18n("ui.stopped_all_players_cleared_playlists_and_disconnected_groups"));
    this._syncNowPlayingUI();
    if (this._state.menuOpen) this._renderMobileMenu();
    if (this._state.controlRoomOpen) this._syncControlRoomUi({ force: true });
    setTimeout(() => this._updateNowPlayingState(), 500);
  }
  async _ungroupAllPlayers() {
    this._hapticTap([14, 18, 14]);
    await this._disconnectPlayerGroups();
  }
  _menuPageIcon(page) {
    const map = {
      main: "menu",
      discovery: "grid",
      settings: "settings",
      queue: "queue",
      players: "speaker",
      players_active: "stats",
      transfer: "repeat",
      group: "speaker",
      ungroup_all: "speaker",
      stop_all: "stop",
      simple_wizard: "wand",
      sleep_timer: "timer",
      announcements: "announcement",
      library_liked: "heart_filled",
      library_playlists: "playlist",
      library_artists: "artist",
      library_albums: "album",
      library_tracks: "tracks",
      library_radio: "radio",
      library_podcasts: "podcast",
      library_search: "search",
      quick_search: "search",
      media_detail: this._state.mobileLibraryDetail?.media_type === "playlist" ? "playlist" : this._state.mobileLibraryDetail?.media_type === "artist" ? "artist" : "album"
    };
    return map[page] || "menu";
  }
  _setMobileMenuHeader(label, iconName, titleAction = "") {
    const title = this.$("mobileMenuTitle");
    if (!title) return;
    title.innerHTML = `<span class="menu-title-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("menu-title-logo")}</span><span class="menu-title-main"><span class="menu-title-icon">${this._iconSvg(iconName)}</span><span class="menu-title-text">${this._esc(label)}</span></span>`;
    if (titleAction) {
      title.dataset.menuTitleAction = titleAction;
      title.classList.add("clickable");
    } else {
      delete title.dataset.menuTitleAction;
      title.classList.remove("clickable");
    }
  }
  _updateActivePlayersBubble() {
    const bubble = this.$("activePlayersBubble");
    if (!bubble) return;
    const activePlayers = this._activePlayingPlayers();
    if (!activePlayers.length) {
      bubble.hidden = true;
      bubble.classList.remove("open");
      return;
    }
    const countEl = this.$("activePlayersCount");
    bubble.style.color = "var(--ma-accent)";
    if (countEl) {
      countEl.textContent = String(activePlayers.length);
      countEl.style.color = "var(--ma-accent)";
    }
    bubble.hidden = false;
    bubble.classList.add("open");
  }
  _setPlayerVolumeFor(entityId, level) {
    const normalized = Math.max(0, Math.min(1, Number(level) || 0));
    if (!entityId) return;
    this._setPlayerVolumeOptimistic(entityId, normalized, normalized <= 0 ? true : false);
    if (this._isDirectMaPlayer(entityId)) {
      return this._callDirectMaPlayerCommand(entityId, "players/cmd/volume_set", { volume_level: Math.round(normalized * 100) }).then(() => this._refreshDirectMaPlayers().catch(() => {
      })).catch(() => {
      });
    }
    return this._callHaServiceRaw("media_player", "volume_set", { entity_id: entityId, volume_level: normalized }).catch((error) => {
      this._toastError(this._mediaControlFailureMessage(error));
      return false;
    });
  }
  _effectivePlayerVolumeLevel(playerOrEntityId = null) {
    const entityId = String(typeof playerOrEntityId === "string" ? playerOrEntityId : playerOrEntityId?.entity_id || "").trim();
    const optimistic = entityId ? this._optimisticVolumeByPlayer.get(entityId) : null;
    if (optimistic && Date.now() - Number(optimistic.ts || 0) < 5e3) {
      return Math.max(0, Math.min(1, Number(optimistic.level) || 0));
    }
    const player = typeof playerOrEntityId === "string" ? (this._state.players || []).find((candidate) => candidate.entity_id === entityId) || this._hass?.states?.[entityId] : playerOrEntityId;
    const value = Number(player?.attributes?.volume_level);
    return Number.isFinite(value) ? Math.max(0, Math.min(1, value)) : 0;
  }
  async _setPlayerVolumeForAnnouncement(entityId, level) {
    const normalized = Math.max(0, Math.min(1, Number(level) || 0));
    if (!entityId) return false;
    if (this._isDirectMaPlayer(entityId)) {
      await this._callDirectMaPlayerCommand(entityId, "players/cmd/volume_set", { volume_level: Math.round(normalized * 100) });
      this._refreshDirectMaPlayers().catch(() => {
      });
      return true;
    }
    await this._hass?.callService?.("media_player", "volume_set", { entity_id: entityId, volume_level: normalized });
    return true;
  }
  _playerVolumeLevel(entityId) {
    const player = this._hass?.states?.[entityId] || (this._state.players || []).find((p) => p.entity_id === entityId);
    const value = Number(player?.attributes?.volume_level);
    return Number.isFinite(value) ? Math.max(0, Math.min(1, value)) : null;
  }
  _setGroupVolumeFor(entityId, level) {
    const player = (this._state.players || []).find((p) => p.entity_id === entityId) || this._hass?.states?.[entityId];
    const ids = this._playerGroupMemberIds(player).filter(Boolean);
    const targets = ids.length ? ids.filter((id) => {
      const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
      return !this._isStaticGroupPlayer(target);
    }) : [entityId];
    targets.forEach((id) => this._setPlayerVolumeFor(id, level));
  }
  async _toggleMuteFor(entityId) {
    if (!entityId) return;
    const player = (this._state.players || []).find((p) => p.entity_id === entityId) || this._hass?.states?.[entityId];
    if (!player) return;
    const currentVolume = Math.max(0, Math.min(1, Number(player.attributes?.volume_level ?? 0) || 0));
    const currentlyMuted = this._isMuted(player) || currentVolume <= 0;
    const callMuteFlag = (muted) => {
      if (this._isDirectMaPlayer(player)) {
        this._callDirectMaPlayerCommand(player, "players/cmd/volume_mute", { muted }).catch(() => {
        });
        return;
      }
      const mutePromise = this._callHaServiceRaw("media_player", "volume_mute", { entity_id: entityId, is_volume_muted: muted });
      if (mutePromise?.catch) mutePromise.catch(() => {
      });
    };
    if (!currentlyMuted && currentVolume > 0) this._lastVolumeByPlayer.set(entityId, currentVolume);
    if (!currentlyMuted) {
      this._setPlayerVolumeOptimistic(entityId, 0, true);
      await this._setPlayerVolumeFor(entityId, 0);
      callMuteFlag(true);
    } else {
      const restoreVolume = currentVolume > 0 ? currentVolume : this._lastVolumeByPlayer.get(entityId) ?? 0.35;
      this._setPlayerVolumeOptimistic(entityId, restoreVolume, false);
      await this._setPlayerVolumeFor(entityId, restoreVolume);
      callMuteFlag(false);
    }
    if (this._isDirectMaPlayer(player)) this._refreshDirectMaPlayers().catch(() => {
    });
  }
  async _toggleGroupMuteFor(entityId) {
    if (!entityId) return;
    const player = (this._state.players || []).find((p) => p.entity_id === entityId) || this._hass?.states?.[entityId];
    const ids = this._playerGroupMemberIds(player).filter(Boolean);
    const targets = (ids.length ? ids : [entityId]).filter((id) => {
      const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
      return !this._isStaticGroupPlayer(target);
    });
    const shouldMute = targets.some((id) => {
      const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
      return !this._isMuted(target);
    });
    await Promise.all(targets.map(async (id) => {
      try {
        await this._hass.callService("media_player", "volume_mute", { entity_id: id, is_volume_muted: shouldMute });
      } catch (_) {
      }
      if (shouldMute) this._softMutedPlayers.add(id);
      else this._softMutedPlayers.delete(id);
    }));
    setTimeout(() => this._renderMobileMenu(), 120);
  }
  _isGroupMuted(player) {
    const ids = this._playerGroupMemberIds(player).filter(Boolean);
    if (!ids.length) return this._isMuted(player);
    return ids.every((id) => {
      const target = (this._state.players || []).find((p) => p.entity_id === id) || this._hass?.states?.[id];
      return this._isMuted(target);
    });
  }
  _mobileLibraryOrderBy() {
    const sort = String(this._state.mobileLibrarySort || "name_asc");
    if (sort === "date_desc" || sort === "date_asc") return "last_modified";
    return "sort_name";
  }
  _mobileFooterButtonInner(iconName, label) {
    const mode = this._mobileFooterMode();
    const icon = this._iconSvg(iconName);
    const text = `<span class="footer-btn-label">${this._esc(label)}</span>`;
    if (mode === "icon") return icon;
    if (mode === "text") return text;
    return `${icon}${text}`;
  }
  _mobileThemeFooterInner() {
    const mode = this._mobileFooterMode();
    const effective = this._effectiveTheme();
    const icon = `<span class="footer-theme-ic" aria-hidden="true">${effective === "dark" ? "☀" : "☾"}</span>`;
    const label = `<span class="footer-btn-label">${this._esc(this._i18n("ui.theme_2"))}</span>`;
    if (mode === "icon") return icon;
    if (mode === "text") return label;
    return `${icon}${label}`;
  }
  _mobileCurrentQueueIndex() {
    return HomeiiMediaQueueFoundation.mobileCurrentQueueIndex(this._state.maQueueState?.current_index);
  }
  _mobileQueueItemsSorted() {
    return HomeiiMediaQueueFoundation.sortQueueItems(this._state.queueItems || []);
  }
  _mobileArtStackContext() {
    return HomeiiMediaQueueFoundation.resolveMobileArtStackContext({
      queueItems: this._state.queueItems || [],
      currentIndexValue: this._state.maQueueState?.current_index,
      player: this._getSelectedPlayer(),
      currentItem: this._state.maQueueState?.current_item || null,
      hasPendingPlay: Number(this._state.mobileQueuePlayPendingUntil || 0) > Date.now(),
      pendingKey: String(this._state.mobileQueuePlayPendingKey || "").trim(),
      pendingUri: String(this._state.mobileQueuePlayPendingUri || "").trim(),
      pendingIndexValue: this._state.mobileQueuePlayPendingIndex,
      browseOffset: this._state.mobileArtBrowseOffset || 0
    }, (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType));
  }
  _mobileArtStackItems() {
    const { queueItems, displayIndex, offset } = this._mobileArtStackContext();
    const current = queueItems[displayIndex] || this._state.maQueueState?.current_item || null;
    const previous = displayIndex > 0 ? queueItems[displayIndex - 1] : null;
    const next = displayIndex < queueItems.length - 1 ? queueItems[displayIndex + 1] : this._state.maQueueState?.next_item || null;
    return { previous, current, next, offset };
  }
  _hasPendingMobileQueuePlay(now = Date.now()) {
    return HomeiiNowPlayingFoundation.hasPendingQueuePlay(this._state, now);
  }
  _queueItemIdentityAccessors() {
    return {
      getQueueItemPlaybackId: (entry) => this._getQueueItemPlaybackId(entry),
      getQueueItemStableId: (entry) => this._getQueueItemStableId(entry),
      getQueueItemKey: (entry) => this._getQueueItemKey(entry),
      getQueueItemUri: (entry) => this._getQueueItemUri(entry)
    };
  }
  _queueItemMatchesPendingMobilePlay(item = null) {
    return HomeiiNowPlayingFoundation.queueItemMatchesPendingPlay(
      item,
      HomeiiNowPlayingFoundation.pendingQueuePlayState(this._state),
      this._queueItemIdentityAccessors(),
      (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
    );
  }
  _pendingMobileQueueItem(stack = null) {
    return HomeiiNowPlayingFoundation.resolvePendingQueueItem({
      state: this._state,
      currentQueueItem: this._state.maQueueState?.current_item || null,
      stack,
      queueItems: this._mobileQueueItemsSorted(),
      accessors: this._queueItemIdentityAccessors(),
      compareMediaRefs: (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
    });
  }
  _queueItemArtworkCacheKey(item = null) {
    return HomeiiArtworkFoundation.queueItemArtworkCacheKey(item, {
      ...this._queueItemIdentityAccessors()
    });
  }
  _queueItemArtworkUrl(item = null, size = 420, player = this._getSelectedPlayer()) {
    if (!item) return "";
    const cacheKey = this._queueItemArtworkCacheKey(item);
    return this._bestArtworkUrl([
      this._queueItemImageUrl(item, size),
      item?.streamdetails?.stream_metadata?.image_url,
      item?.media_image,
      item?.image,
      item?.image_url,
      item?.media_item?.image,
      item?.media_item?.image_url,
      item?.media_item?.metadata?.image,
      item?.media_item?.metadata?.image_url,
      item?.media_item?.album?.image,
      item?.media_item?.album?.image_url
    ], {
      size,
      cacheKey
    });
  }
  _displayArtworkForQueueItem(player = null, item = null, { pending = false, size = 420 } = {}) {
    if (!item) return this._currentArtworkUrl(player, null, size, { preferPlayerArtwork: true });
    const queueArt = this._queueItemArtworkUrl(item, size, player);
    if (queueArt) return queueArt;
    return this._currentArtworkUrl(player, item, size, { preferPlayerArtwork: !pending });
  }
  _mobileNowPlayingDisplaySource(player = null, currentQueueItem = null, stack = null) {
    return HomeiiNowPlayingFoundation.nowPlayingDisplaySource({
      player,
      currentQueueItem,
      stack,
      queueItems: this._mobileQueueItemsSorted(),
      state: this._state,
      fallbackTitle: this._i18n("ui.nothing_playing"),
      accessors: this._queueItemIdentityAccessors(),
      compareMediaRefs: (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType),
      artworkUrlFn: (displayPlayer, queueItem, { pending, size } = {}) => this._displayArtworkForQueueItem(displayPlayer, queueItem, { pending, size }),
      size: 420
    });
  }
  _preloadPendingMobileQueueItemArtwork(item = null) {
    const art = this._displayArtworkForQueueItem(this._getSelectedPlayer(), item, { pending: true, size: 420 });
    if (art) this._decodeArtworkUrl(art).catch(() => {
    });
  }
  _queueArtworkPrefetchUrls(items = this._mobileQueueItemsSorted(), options = {}) {
    const queueItems = Array.isArray(items) ? items.filter(Boolean) : [];
    if (!queueItems.length) return [];
    return HomeiiArtworkFoundation.queueArtworkPrefetchUrls(queueItems, {
      ...options,
      currentIndex: this._state.maQueueState?.current_index,
      isCurrentItemFn: (item) => this._isQueueItemCurrent(item)
    }, (item, size) => {
      return this._queueItemArtworkUrl(item, size);
    });
  }
  _scheduleArtworkPrefetchDrain(immediate = false) {
    if (this._artworkPrefetchTimer) return;
    const run = () => {
      this._artworkPrefetchTimer = null;
      this._drainArtworkPrefetchQueue();
    };
    if (!immediate && typeof requestIdleCallback === "function") {
      this._artworkPrefetchTimer = requestIdleCallback(run, { timeout: 900 });
      return;
    }
    this._artworkPrefetchTimer = setTimeout(run, immediate ? 0 : 120);
  }
  _enqueueArtworkPrefetch(urls = [], options = {}) {
    const uniqueUrls = (Array.isArray(urls) ? urls : [urls]).map((url) => String(url || "").trim()).filter(Boolean);
    if (!uniqueUrls.length) return;
    const immediate = options.immediate === true;
    const pendingUrls = [];
    uniqueUrls.forEach((url) => {
      if (this._decodedArtworkUrls.has(url) || this._artworkDecodePromises.has(url) || this._artworkPrefetchQueuedUrls.has(url)) return;
      pendingUrls.push(url);
      this._artworkPrefetchQueuedUrls.add(url);
    });
    if (immediate) this._artworkPrefetchQueue.unshift(...pendingUrls);
    else this._artworkPrefetchQueue.push(...pendingUrls);
    while (this._artworkPrefetchQueue.length > 96) {
      const dropped = this._artworkPrefetchQueue.pop();
      if (dropped) this._artworkPrefetchQueuedUrls.delete(dropped);
    }
    this._scheduleArtworkPrefetchDrain(immediate);
  }
  _drainArtworkPrefetchQueue() {
    const maxConcurrent = 3;
    while (this._artworkPrefetchActive < maxConcurrent && this._artworkPrefetchQueue.length) {
      const url = this._artworkPrefetchQueue.shift();
      this._artworkPrefetchQueuedUrls.delete(url);
      if (!url || this._decodedArtworkUrls.has(url)) continue;
      this._artworkPrefetchActive += 1;
      this._decodeArtworkUrl(url).catch(() => {
      }).finally(() => {
        this._artworkPrefetchActive = Math.max(0, this._artworkPrefetchActive - 1);
        if (this._artworkPrefetchQueue.length) this._scheduleArtworkPrefetchDrain(false);
      });
    }
  }
  _prefetchQueueArtworkWindow(items = this._mobileQueueItemsSorted(), options = {}) {
    const urls = this._queueArtworkPrefetchUrls(items, options);
    this._enqueueArtworkPrefetch(urls, { immediate: options.immediate === true });
  }
  _queueVisibleArtworkWindowFromMenuBody(body = this.$("mobileMenuBody")) {
    if (!body || this._state.menuPage !== "queue") return null;
    const rows = Array.from(body.querySelectorAll?.(".queue-row[data-queue-position]") || []);
    if (!rows.length || typeof body.getBoundingClientRect !== "function") return null;
    const bodyRect = body.getBoundingClientRect();
    let firstIndex = Infinity;
    let lastIndex = -1;
    rows.forEach((row) => {
      const rect = row.getBoundingClientRect?.();
      if (!rect || rect.bottom < bodyRect.top - 160 || rect.top > bodyRect.bottom + 160) return;
      const index = Number(row.dataset.queuePosition || 0) - 1;
      if (!Number.isFinite(index) || index < 0) return;
      firstIndex = Math.min(firstIndex, index);
      lastIndex = Math.max(lastIndex, index);
    });
    if (!Number.isFinite(firstIndex) || lastIndex < firstIndex) return null;
    return {
      visibleStartIndex: Math.max(0, firstIndex),
      visibleCount: Math.min(40, Math.max(12, lastIndex - firstIndex + 1 + 8))
    };
  }
  _handleMobileMenuScroll(e) {
    if (this._state.menuPage !== "queue") return;
    clearTimeout(this._mobileQueueArtworkPrefetchTimer);
    const body = e?.currentTarget || this.$("mobileMenuBody");
    this._mobileQueueArtworkPrefetchTimer = setTimeout(() => {
      const windowOptions = this._queueVisibleArtworkWindowFromMenuBody(body);
      if (!windowOptions) return;
      this._prefetchQueueArtworkWindow(this._getNowPlayingQueueItems(), {
        ...windowOptions,
        before: 2,
        after: 14
      });
    }, 80);
  }
  _setOptimisticMobileQueueItem(item) {
    if (!item) return;
    const queueItems = this._mobileQueueItemsSorted();
    const stableKey = this._getQueueItemStableId(item);
    const uri = this._getQueueItemUri(item);
    const key = this._getQueueItemKey(item) || stableKey || uri;
    const itemIndex = HomeiiMediaQueueFoundation.findQueueItemIndex(
      queueItems,
      { queueItemId: key, uri, sortIndex: item?.sort_index },
      (uriA, uriB, fallbackType) => this._mediaRefsEquivalent(uriA, uriB, fallbackType)
    );
    const sortIndex = Number(item?.sort_index);
    const currentIndex = Number.isFinite(sortIndex) ? sortIndex : itemIndex >= 0 ? itemIndex : this._state.maQueueState?.current_index;
    this._markMobileQueuePlayPending(item, Number(currentIndex));
    this._state.maQueueState = {
      ...this._state.maQueueState || {},
      current_index: currentIndex,
      current_item: item,
      next_item: itemIndex >= 0 ? queueItems[itemIndex + 1] || null : this._state.maQueueState?.next_item || null,
      elapsed_time: 0,
      elapsed_time_last_updated: (/* @__PURE__ */ new Date()).toISOString()
    };
    this._state.mobileArtAnchorKey = key || "";
    this._state.mobileArtBrowseOffset = 0;
    this._preloadPendingMobileQueueItemArtwork(item);
    this._prefetchQueueArtworkWindow(queueItems, { immediate: true, before: 2, after: 16 });
  }
  _mobileArtStackRenderKey() {
    const stack = this._mobileArtStackItems();
    return [
      this._getQueueItemStableId(stack.previous) || this._getQueueItemUri(stack.previous) || this._getQueueItemKey(stack.previous) || "p0",
      this._mobileStackItemArtwork(stack.previous, "prev"),
      this._getQueueItemStableId(stack.current) || this._getQueueItemUri(stack.current) || this._getQueueItemKey(stack.current) || "c0",
      this._mobileStackItemArtwork(stack.current, "center"),
      this._getQueueItemStableId(stack.next) || this._getQueueItemUri(stack.next) || this._getQueueItemKey(stack.next) || "n0",
      this._mobileStackItemArtwork(stack.next, "next"),
      this._hasPendingMobileQueuePlay() ? this._state.mobileQueuePlayPendingKey || "" : "",
      this._hasPendingMobileQueuePlay() ? this._state.mobileQueuePlayPendingUri || "" : "",
      this._mobileSwipeMode()
    ].join("|");
  }
  _mobileArtFallbackHtml(icon = "music_note") {
    const fallbackIcon = icon === "brand" ? "brand" : icon || "music_note";
    return `
      <div class="art-stack-fallback static-fallback ${fallbackIcon === "brand" ? "brand-fallback" : ""}">
        <div class="fallback-aura"></div>
        <div class="fallback-disc">${fallbackIcon === "brand" ? this._tabletBrandSignatureHtml("art-stack-brand-logo") : this._iconSvg(fallbackIcon)}</div>
      </div>
    `;
  }
  _mobileQuickActions() {
    const actions = HomeiiMobileSettingsFoundation.normalizeMobileQuickActions(
      this._state.mobileQuickActions,
      this._defaultMobileQuickActions()
    );
    this._state.mobileQuickActions = actions;
    return actions;
  }
  _decodeArtworkUrl(url = "") {
    const normalized = String(url || "").trim();
    if (!normalized) return Promise.resolve(false);
    if (this._decodedArtworkUrls.has(normalized)) return Promise.resolve(true);
    if (this._artworkDecodePromises.has(normalized)) return this._artworkDecodePromises.get(normalized);
    const promise = new Promise((resolve) => {
      let settled = false;
      let timeout = null;
      const img = new Image();
      const markReady = () => {
        this._decodedArtworkUrls.add(normalized);
        this._decodedArtworkImages.set(normalized, img);
        while (this._decodedArtworkUrls.size > 180) {
          const oldest = this._decodedArtworkUrls.values().next().value;
          if (!oldest) break;
          this._decodedArtworkUrls.delete(oldest);
          this._decodedArtworkImages.delete(oldest);
        }
        this._artworkDecodePromises.delete(normalized);
        resolve(true);
      };
      const finish = (ok) => {
        if (settled) return;
        settled = true;
        clearTimeout(timeout);
        img.onload = null;
        img.onerror = null;
        if (!ok) {
          this._artworkDecodePromises.delete(normalized);
          resolve(false);
          return;
        }
        if (typeof img.decode === "function") {
          Promise.race([
            img.decode(),
            new Promise((resolveRace) => setTimeout(resolveRace, 240))
          ]).catch(() => {
          }).finally(markReady);
          return;
        }
        markReady();
      };
      try {
        img.decoding = "async";
        img.loading = "eager";
        if ("fetchPriority" in img) img.fetchPriority = "high";
        img.onload = () => finish(true);
        img.onerror = () => finish(false);
        timeout = setTimeout(() => finish(false), 5200);
        img.src = normalized;
        if (img.complete && img.naturalWidth > 0) finish(true);
      } catch (_) {
        finish(false);
      }
    });
    this._artworkDecodePromises.set(normalized, promise);
    return promise;
  }
  _decodedArtworkImgHtml(art = "", alt = "", { current = false, fallbackIcon = "music_note" } = {}) {
    const normalized = String(art || "").trim();
    if (!normalized) return this._mobileArtFallbackHtml(fallbackIcon);
    const ready = this._decodedArtworkUrls.has(normalized);
    const priority = current ? "high" : "low";
    return ready ? `<img src="${this._esc(normalized)}" alt="${this._esc(alt)}" decoding="async" loading="eager" fetchpriority="${priority}" data-homeii-art-ready="1" data-homeii-applied-art-src="${this._esc(normalized)}" data-homeii-art-fallback="1" data-homeii-art-fallback-icon="${this._esc(fallbackIcon)}">` : `<img src="${this._esc(normalized)}" alt="${this._esc(alt)}" decoding="async" loading="eager" fetchpriority="${priority}" data-homeii-art-src="${this._esc(normalized)}" data-homeii-art-ready="0" data-homeii-art-fallback="1" data-homeii-art-fallback-icon="${this._esc(fallbackIcon)}">`;
  }
  _setDecodedArtworkImage(img, url = "", alt = "") {
    if (!img) return;
    const nextUrl = String(url || "").trim();
    if (alt !== void 0) img.alt = String(alt || "");
    const currentSrc = img.getAttribute("src") || "";
    if (!nextUrl) {
      img.removeAttribute("src");
      img.dataset.homeiiArtSrc = "";
      img.dataset.homeiiAppliedArtSrc = "";
      img.dataset.homeiiArtLoadToken = "";
      img.dataset.homeiiArtPendingAt = "";
      img.dataset.homeiiArtReady = "0";
      return;
    }
    if (currentSrc === nextUrl && img.dataset.homeiiArtReady === "1") return;
    if (this._decodedArtworkUrls.has(nextUrl)) {
      img.src = nextUrl;
      img.dataset.homeiiArtSrc = nextUrl;
      img.dataset.homeiiAppliedArtSrc = nextUrl;
      img.dataset.homeiiArtLoadToken = "";
      img.dataset.homeiiArtPendingAt = "";
      img.dataset.homeiiArtReady = "1";
      return;
    }
    if (currentSrc === nextUrl) {
      img.dataset.homeiiArtSrc = nextUrl;
      this._decodeArtworkUrl(nextUrl).then((ok) => {
        if (!ok || !img.isConnected || img.dataset.homeiiArtSrc !== nextUrl) return;
        img.dataset.homeiiAppliedArtSrc = nextUrl;
        img.dataset.homeiiArtLoadToken = "";
        img.dataset.homeiiArtPendingAt = "";
        img.dataset.homeiiArtReady = "1";
      }).catch(() => {
      });
      return;
    }
    const pendingAt = Number(img.dataset.homeiiArtPendingAt || 0);
    if (img.dataset.homeiiArtSrc === nextUrl && img.dataset.homeiiAppliedArtSrc !== nextUrl && pendingAt > 0 && Date.now() - pendingAt < 1800) {
      return;
    }
    img.dataset.homeiiArtSrc = nextUrl;
    const token = `${Date.now()}:${Math.random().toString(36).slice(2)}`;
    img.dataset.homeiiArtLoadToken = token;
    img.dataset.homeiiArtPendingAt = String(Date.now());
    img.dataset.homeiiArtReady = "0";
    img.src = nextUrl;
    const applyImage = () => {
      if (!img.isConnected || img.dataset.homeiiArtSrc !== nextUrl || img.dataset.homeiiArtLoadToken !== token) return;
      img.src = nextUrl;
      img.dataset.homeiiAppliedArtSrc = nextUrl;
      img.dataset.homeiiArtLoadToken = "";
      img.dataset.homeiiArtPendingAt = "";
      img.dataset.homeiiArtReady = "1";
    };
    this._decodeArtworkUrl(nextUrl).then((ok) => {
      if (!ok && currentSrc) {
        if (img.dataset.homeiiArtLoadToken === token) {
          img.dataset.homeiiArtLoadToken = "";
          img.dataset.homeiiArtPendingAt = "";
        }
        return;
      }
      applyImage();
    }).catch(() => {
    });
  }
  _hydrateDecodedArtworkImages(root = this.shadowRoot) {
    root?.querySelectorAll?.("img[data-homeii-art-src]")?.forEach((img) => {
      this._setDecodedArtworkImage(img, img.dataset.homeiiArtSrc || "", img.getAttribute("alt") || "");
    });
  }
  _setDecodedBackgroundImage(el, url = "", valueForUrl = null) {
    if (!el) return;
    const nextUrl = String(url || "").trim();
    if (!nextUrl) {
      el.dataset.homeiiBgArtSrc = "";
      el.dataset.homeiiBgArtValue = "";
      el.style.backgroundImage = "";
      return;
    }
    const nextValue = typeof valueForUrl === "function" ? valueForUrl(nextUrl) : `url(${JSON.stringify(nextUrl)})`;
    if (el.dataset.homeiiBgArtSrc === nextUrl && el.dataset.homeiiBgArtValue === nextValue) return;
    el.dataset.homeiiBgArtSrc = nextUrl;
    el.dataset.homeiiBgArtValue = nextValue;
    if (this._decodedArtworkUrls.has(nextUrl)) {
      el.style.backgroundImage = nextValue;
      return;
    }
    const applyBackground = () => {
      if (!el.isConnected || el.dataset.homeiiBgArtSrc !== nextUrl) return;
      el.style.backgroundImage = el.dataset.homeiiBgArtValue || nextValue;
    };
    const fallbackTimer = setTimeout(applyBackground, el.style.backgroundImage ? 5200 : 2400);
    this._decodeArtworkUrl(nextUrl).then((ok) => {
      clearTimeout(fallbackTimer);
      if (!ok) {
        if (el.isConnected && el.dataset.homeiiBgArtSrc === nextUrl) el.dataset.homeiiBgArtSrc = "";
        return;
      }
      applyBackground();
    }).catch(() => {
    });
  }
  _setDecodedBackgroundCrossfade(el, url = "", overlay = "") {
    if (!el) return;
    const nextUrl = String(url || "").trim();
    const overlayValue = String(overlay || "").trim();
    el.style.backgroundImage = overlayValue || "";
    if (!nextUrl) {
      const timer = this._backgroundCrossfadeTimers?.get(el);
      if (timer) clearTimeout(timer);
      this._backgroundCrossfadeTimers?.delete(el);
      el.dataset.homeiiBgArtSrc = "";
      el.dataset.homeiiBgArtValue = "";
      el.dataset.homeiiBgArtReady = "0";
      el.classList.remove("bg-art-transitioning");
      el.style.removeProperty("--homeii-bg-art-current");
      el.style.removeProperty("--homeii-bg-art-next");
      return;
    }
    const nextValue = `url(${JSON.stringify(nextUrl)})`;
    if (el.dataset.homeiiBgArtSrc === nextUrl && el.dataset.homeiiBgArtValue === nextValue) return;
    el.dataset.homeiiBgArtSrc = nextUrl;
    el.dataset.homeiiBgArtValue = nextValue;
    const applyCrossfade = () => {
      if (!el.isConnected || el.dataset.homeiiBgArtSrc !== nextUrl) return;
      const currentValue = String(el.style.getPropertyValue("--homeii-bg-art-current") || "").trim();
      const existingTimer = this._backgroundCrossfadeTimers?.get(el);
      if (existingTimer) clearTimeout(existingTimer);
      this._backgroundCrossfadeTimers?.delete(el);
      if (!currentValue || currentValue === "none" || el.dataset.homeiiBgArtReady !== "1") {
        el.classList.remove("bg-art-transitioning");
        el.style.setProperty("--homeii-bg-art-current", nextValue);
        el.style.removeProperty("--homeii-bg-art-next");
        el.dataset.homeiiBgArtReady = "1";
        return;
      }
      el.style.setProperty("--homeii-bg-art-next", nextValue);
      el.classList.remove("bg-art-transitioning");
      const raf = typeof requestAnimationFrame === "function" ? requestAnimationFrame : (callback) => setTimeout(callback, 0);
      raf(() => {
        if (!el.isConnected || el.dataset.homeiiBgArtSrc !== nextUrl) return;
        el.classList.add("bg-art-transitioning");
        const settleTimer = setTimeout(() => {
          if (!el.isConnected || el.dataset.homeiiBgArtSrc !== nextUrl) return;
          el.style.setProperty("--homeii-bg-art-current", nextValue);
          el.style.removeProperty("--homeii-bg-art-next");
          el.classList.remove("bg-art-transitioning");
          el.dataset.homeiiBgArtReady = "1";
          this._backgroundCrossfadeTimers?.delete(el);
        }, 560);
        this._backgroundCrossfadeTimers?.set(el, settleTimer);
      });
    };
    if (this._decodedArtworkUrls.has(nextUrl)) {
      applyCrossfade();
      return;
    }
    this._decodeArtworkUrl(nextUrl).then((ok) => {
      if (!ok) return;
      applyCrossfade();
    }).catch(() => {
    });
  }
  _syncCurrentArtworkBackgrounds(artUrl2 = "") {
    const art = String(artUrl2 || this._state.mobileDynamicThemeArtworkUrl || "").trim();
    const effectiveArt = !this._isHotelMode() ? art : "";
    const overlay = this._mobileBackdropOverlay(this._effectiveTheme());
    const token = ++this._currentArtworkBackgroundToken;
    const applyBackgrounds = () => {
      if (token !== this._currentArtworkBackgroundToken) return;
      this._setDecodedBackgroundImage(this.$("mobileArtAura"), effectiveArt);
      this._setDecodedBackgroundImage(this.$("mobileHeroAura"), effectiveArt);
      this._setDecodedBackgroundImage(this.$("compactBackdropArt"), effectiveArt);
      this._setDecodedBackgroundImage(this.$("compactCoverAura"), effectiveArt);
      this._setDecodedBackgroundCrossfade(this.$("mobileBg"), effectiveArt, overlay);
    };
    if (!effectiveArt || this._decodedArtworkUrls.has(effectiveArt)) {
      applyBackgrounds();
      return;
    }
    this._decodeArtworkUrl(effectiveArt).then((ok) => {
      if (ok) applyBackgrounds();
    }).catch(() => {
    });
  }
  _mobileStackItemArtwork(item, position = "center") {
    if (!item) return "";
    const isCurrent = position === "center";
    const size = 420;
    const selectedPlayer = this._getSelectedPlayer();
    if (isCurrent && this._queueItemMatchesPendingMobilePlay(item)) {
      return this._displayArtworkForQueueItem(selectedPlayer, item, { pending: true, size });
    }
    const isRadioItem = this._isLikelyRadioPlayback(selectedPlayer, item, item?.media_item);
    const currentArtwork = isCurrent ? this._currentArtworkUrl(selectedPlayer, item, size, { preferPlayerArtwork: isRadioItem }) : "";
    return this._bestArtworkUrl([
      isRadioItem ? currentArtwork : "",
      this._queueItemArtworkUrl(item, size, selectedPlayer),
      !isRadioItem ? currentArtwork : ""
    ], {
      size,
      cacheKey: this._queueItemArtworkCacheKey(item) || this._currentArtworkCacheKey(selectedPlayer, item)
    });
  }
  _mobileStackCardHtml(item, position = "center") {
    const isCurrent = position === "center";
    const art = this._mobileStackItemArtwork(item, position);
    const label = item?.media_item?.name || item?.name || (isCurrent ? this._getSelectedPlayer()?.attributes?.media_title || "" : "");
    const selectedPlayer = this._getSelectedPlayer();
    const isRadioItem = this._isLikelyRadioPlayback(selectedPlayer, item, item?.media_item);
    const fallbackIcon = isRadioItem ? "brand" : "music_note";
    return `
      <div class="art-stack-card ${position} ${!art ? "placeholder" : ""}">
        ${this._decodedArtworkImgHtml(art, label, { current: isCurrent, fallbackIcon })}
      </div>
    `;
  }
  _mobileStackSlideHtml(item, position = "center") {
    const queueItemId = this._getQueueItemStableId(item) || this._getQueueItemKey(item);
    const uri = this._getQueueItemUri(item);
    const mediaType = item?.media_item?.media_type || item?.media_type || "track";
    const sortIndex = Number.isFinite(Number(item?.sort_index)) ? Number(item.sort_index) : "";
    return `
      <div class="art-stack-slide ${position}" data-art-position="${position}" data-queue-item-id="${this._esc(queueItemId || "")}" data-uri="${this._esc(uri)}" data-type="${this._esc(mediaType)}" data-sort-index="${this._esc(sortIndex)}">
        ${this._mobileStackCardHtml(item, position)}
      </div>
    `;
  }
  _mobileArtworkStackHtml() {
    const { previous, current, next } = this._mobileArtStackItems();
    return `
      <div class="art-stack-viewport">
        <div class="art-stack-container">
          ${previous ? this._mobileStackSlideHtml(previous, "prev") : `<div class="art-stack-slide prev"><div class="art-stack-card prev ghost"></div></div>`}
          ${this._mobileStackSlideHtml(current, "center")}
          ${next ? this._mobileStackSlideHtml(next, "next") : `<div class="art-stack-slide next"><div class="art-stack-card next ghost"></div></div>`}
        </div>
      </div>
    `;
  }
  _preloadMobileArtImages(stack = this._mobileArtStackItems()) {
    const urls = [
      this._mobileStackItemArtwork(stack.previous, "prev"),
      this._mobileStackItemArtwork(stack.current, "center"),
      this._mobileStackItemArtwork(stack.next, "next")
    ].filter(Boolean);
    for (const src of urls) {
      this._decodeArtworkUrl(src).catch(() => {
      });
    }
    this._prefetchQueueArtworkWindow(this._mobileQueueItemsSorted(), { immediate: false, before: 2, after: 12 });
  }
  async _ensureEmblaLoaded() {
    if (window.EmblaCarousel) return window.EmblaCarousel;
    if (this._mobileEmblaLoadPromise) return this._mobileEmblaLoadPromise;
    const src = this._config.mobile_embla_url || new URL(
      /* @vite-ignore */
      "./vendor/embla-carousel.umd.js",
      import.meta.url
    ).href;
    this._mobileEmblaLoadPromise = new Promise((resolve, reject) => {
      const existing = document.querySelector("script[data-homeii-embla]");
      if (existing) {
        if (window.EmblaCarousel) return resolve(window.EmblaCarousel);
        existing.addEventListener("load", () => resolve(window.EmblaCarousel), { once: true });
        existing.addEventListener("error", reject, { once: true });
        return;
      }
      const script = document.createElement("script");
      script.src = src;
      script.async = true;
      script.dataset.homeiiEmbla = "1";
      script.onload = () => resolve(window.EmblaCarousel);
      script.onerror = reject;
      document.head.appendChild(script);
    });
    return this._mobileEmblaLoadPromise;
  }
  _destroyMobileEmbla() {
    if (!this._mobileEmbla) return;
    try {
      this._mobileEmbla.destroy();
    } catch (_) {
    }
    this._mobileEmbla = null;
  }
  _scheduleMobileArtBrowseReset() {
    clearTimeout(this._mobileArtBrowseResetTimer);
    this._mobileArtBrowseResetTimer = null;
    if (this._mobileSwipeMode() !== "browse" || !Number(this._state.mobileArtBrowseOffset || 0)) return;
    this._mobileArtBrowseResetTimer = setTimeout(() => {
      this._state.mobileArtBrowseOffset = 0;
      this._refreshMobileArtStack(true);
    }, 7e3);
  }
  _bindMobileArtFallbackGestures() {
    const root = this.$("npArt");
    if (!root || root.dataset.fallbackBound === "1") return;
    root.dataset.fallbackBound = "1";
    root.addEventListener("touchstart", (e) => {
      this._onArtTouchStart(e);
    }, { passive: true });
    root.addEventListener("touchmove", (e) => {
      this._onArtTouchMove(e);
    }, { passive: false });
    root.addEventListener("touchend", (e) => {
      this._onArtTouchEnd(e);
    }, { passive: true });
    root.addEventListener("touchcancel", () => {
      this._state.activeArtworkTouch = null;
      this._clearArtDragOffset();
      this.$("mobileArtShell")?.classList.remove("dragging");
    }, { passive: true });
  }
  async _initMobileArtCarousel() {
    this._destroyMobileEmbla();
    this._bindMobileArtFallbackGestures();
  }
  _bindActivePlayerChipSwipe() {
    const chip = this.$("activePlayerChip");
    if (!chip || chip.dataset.swipeBound === "1" || this._layoutModeConfig() === "tablet") return;
    chip.dataset.swipeBound = "1";
    chip.addEventListener("touchstart", (e) => this._onActivePlayerChipTouchStart(e), { passive: true });
    chip.addEventListener("touchend", (e) => this._onActivePlayerChipTouchEnd(e), { passive: true });
    chip.addEventListener("touchcancel", () => {
      this._state.activePlayerChipTouch = null;
    }, { passive: true });
  }
  _onActivePlayerChipTouchStart(e) {
    const touch = e.touches?.[0];
    if (!touch) return;
    this._state.activePlayerChipTouch = { x: touch.clientX, y: touch.clientY, t: Date.now() };
  }
  _onActivePlayerChipTouchEnd(e) {
    const start = this._state.activePlayerChipTouch;
    this._state.activePlayerChipTouch = null;
    const touch = e.changedTouches?.[0];
    if (!start || !touch) return;
    const dx = touch.clientX - start.x;
    const dy = touch.clientY - start.y;
    const dt = Date.now() - start.t;
    if (Math.abs(dx) < 36 || Math.abs(dx) <= Math.abs(dy) || dt > 650) return;
    this._state.activePlayerSwipeLockUntil = Date.now() + 280;
    this._hapticTap([8]);
    this._cycleActivePlayer(dx < 0 ? 1 : -1);
  }
  _onArtTouchStart(e) {
    const touch = e.touches?.[0];
    if (!touch) return;
    this._state.activeArtworkTouch = { x: touch.clientX, y: touch.clientY, t: Date.now(), dx: 0, active: true };
    const artShell = this.$("mobileArtShell");
    const artHost = this.$("npArt");
    if (artShell) {
      artShell.classList.remove("commit-next", "commit-prev");
      artShell.classList.add("dragging");
    }
    artHost?.classList.remove("resetting");
    artHost?.classList.add("dragging");
    this._setArtDragOffset(0);
  }
  _onArtTouchMove(e) {
    const start = this._state.activeArtworkTouch;
    const touch = e.touches?.[0];
    if (!start || !touch) return;
    const dx = touch.clientX - start.x;
    const dy = touch.clientY - start.y;
    start.dx = dx;
    if (Math.abs(dx) <= Math.abs(dy)) return;
    if (e.cancelable) e.preventDefault();
    const limited = Math.max(-92, Math.min(92, dx * 0.72));
    this._setArtDragOffset(limited);
  }
  _setArtDragOffset(px = 0) {
    const artHost = this.$("npArt");
    if (artHost) artHost.style.setProperty("--art-drag-x", `${Math.round(px)}px`);
  }
  _clearArtDragOffset() {
    const artHost = this.$("npArt");
    if (artHost) artHost.style.setProperty("--art-drag-x", "0px");
  }
  _commitArtSwipe(direction, applyChange) {
    const artShell = this.$("mobileArtShell");
    const artHost = this.$("npArt");
    if (!artShell) {
      applyChange?.();
      return;
    }
    artShell.classList.remove("dragging", "commit-next", "commit-prev");
    artHost?.classList.remove("dragging", "resetting");
    this._setArtDragOffset(direction === "next" ? -132 : 132);
    setTimeout(() => {
      artHost?.classList.add("resetting");
      applyChange?.();
      this._clearArtDragOffset();
      requestAnimationFrame(() => {
        artHost?.classList.remove("resetting", "dragging");
        artShell.classList.remove("commit-next", "commit-prev", "swipe-next", "swipe-prev");
      });
    }, 105);
  }
  _onArtTouchEnd(e) {
    const start = this._state.activeArtworkTouch;
    this._state.activeArtworkTouch = null;
    const touch = e.changedTouches?.[0];
    if (!start || !touch) return;
    const dx = touch.clientX - start.x;
    const dy = touch.clientY - start.y;
    const dt = Date.now() - start.t;
    const artShell = this.$("mobileArtShell");
    const artHost = this.$("npArt");
    if (artShell) artShell.classList.remove("dragging");
    artHost?.classList.remove("dragging");
    if (Math.abs(dx) < 34 || Math.abs(dx) < Math.abs(dy) || dt > 650) {
      this._clearArtDragOffset();
      return;
    }
    if (this._mobileSwipeMode() === "browse") {
      const { queueItems: queueItems2, displayIndex: displayIndex2 } = this._mobileArtStackContext();
      if (!queueItems2.length) return;
      const canMove = dx < 0 ? displayIndex2 < queueItems2.length - 1 : displayIndex2 > 0;
      if (!canMove) {
        this._clearArtDragOffset();
        return;
      }
      this._state.mobileArtJustSwipedAt = Date.now();
      this._commitArtSwipe(dx < 0 ? "next" : "prev", () => {
        this._state.mobileArtBrowseOffset += dx < 0 ? 1 : -1;
        this._refreshMobileArtStack();
      });
      this._scheduleMobileArtBrowseReset();
      this._hapticTap([8]);
      return;
    }
    const { queueItems, displayIndex } = this._mobileArtStackContext();
    const targetIndex = dx < 0 ? displayIndex + 1 : displayIndex - 1;
    const targetItem = queueItems[targetIndex] || null;
    const targetQueueItemId = targetItem ? this._getQueueItemKey(targetItem) : "";
    const targetUri = targetItem?.media_item?.uri || targetItem?.uri || "";
    const targetType = targetItem?.media_item?.media_type || targetItem?.media_type || "track";
    const targetSortIndex = Number.isFinite(Number(targetItem?.sort_index)) ? Number(targetItem.sort_index) : "";
    this._commitArtSwipe(dx < 0 ? "next" : "prev", () => {
      if (targetItem) {
        this._playQueueItem(targetQueueItemId, targetUri, targetType, targetSortIndex).then((ok) => {
          if (!ok) this._ensureQueueSnapshot(true);
        }).catch(() => this._ensureQueueSnapshot(true));
      } else {
        this._playAdjacentRadioStation(dx < 0 ? "next" : "previous").then((playedRadio) => {
          if (playedRadio) return;
          this._state.mobileArtBrowseOffset = dx < 0 ? 1 : -1;
          this._refreshMobileArtStack(true);
          this._playerCmd(dx < 0 ? "next" : "previous");
        });
      }
    });
  }
  _refreshMobileArtStack(force = false) {
    if (this._isCompactTileMode()) {
      this._syncNowPlayingUI();
      return;
    }
    const artHost = this.$("npArt");
    const artAura = this.$("mobileArtAura");
    const heroAura = this.$("mobileHeroAura");
    const player = this._getSelectedPlayer();
    const stack = this._mobileArtStackItems();
    this._preloadMobileArtImages(stack);
    const renderKey = this._mobileArtStackRenderKey();
    if (artHost && (force || this._state.mobileArtRenderKey !== renderKey)) {
      artHost.innerHTML = this._mobileArtworkStackHtml();
      this._state.mobileArtRenderKey = renderKey;
      queueMicrotask(() => {
        this._initMobileArtCarousel();
      });
    }
    if (artHost) this._hydrateDecodedArtworkImages(artHost);
    const currentQueueItem = this._state.maQueueState?.current_item || null;
    const displaySource = this._mobileNowPlayingDisplaySource(player, currentQueueItem, stack);
    const playingArt = displaySource.art || this._displayArtworkForQueueItem(player, displaySource.queueItem || currentQueueItem, { pending: displaySource.hasPendingPlay, size: 420 });
    const previewArt = this._queueItemArtworkUrl(stack.current, 420, player) || playingArt;
    const art = this._mobileSwipeMode() === "browse" && stack.offset !== 0 ? playingArt : displaySource.hasPendingPlay ? playingArt : previewArt;
    this._setDecodedBackgroundImage(artAura, !this._isHotelMode() ? art : "");
    this._setDecodedBackgroundImage(heroAura, !this._isHotelMode() ? art : "");
    if (this._mobileSwipeMode() === "browse" && stack.offset !== 0) {
      const browseTitle = stack.current?.media_item?.name || stack.current?.name || player?.attributes?.media_title || this._i18n("ui.nothing_playing");
      const browseArtist = stack.current?.media_item?.artists?.map((a) => a.name).join(", ") || stack.current?.artist_str || player?.attributes?.media_artist || "";
      const browseAlbum = stack.current?.media_item?.album?.name || player?.attributes?.media_album_name || "";
      if (this.$("npTitle")) this.$("npTitle").textContent = browseTitle;
      this._setNowPlayingSubtitle([browseArtist, browseAlbum].filter(Boolean).join(" · ") || "—");
      this._scheduleMobileArtBrowseReset();
    } else {
      if (this.$("npTitle")) this.$("npTitle").textContent = displaySource.title || this._i18n("ui.nothing_playing");
      this._setNowPlayingSubtitle([displaySource.artist, displaySource.album].filter(Boolean).join(" · ") || "—");
      clearTimeout(this._mobileArtBrowseResetTimer);
      this._mobileArtBrowseResetTimer = null;
    }
  }
  async _handleMobileArtTap(e) {
    if (Date.now() - Number(this._state.mobileArtJustSwipedAt || 0) < 260) return;
    const slide = e.target.closest(".art-stack-slide");
    if (!slide) return;
    const position = slide.dataset.artPosition || "center";
    if (this._mobileSwipeMode() !== "browse") {
      if (position === "center") this._hapticTap([6]);
      return;
    }
    if (position === "prev" || position === "next") {
      const { queueItems, displayIndex } = this._mobileArtStackContext();
      if (!queueItems.length) return;
      const nextIndex = position === "prev" ? displayIndex - 1 : displayIndex + 1;
      if (nextIndex < 0 || nextIndex >= queueItems.length) return;
      this._state.mobileArtBrowseOffset += position === "prev" ? -1 : 1;
      this._refreshMobileArtStack(true);
      this._scheduleMobileArtBrowseReset();
      this._hapticTap([8]);
      return;
    }
    const context = this._mobileArtStackContext();
    const selectedItem = context.queueItems?.[context.displayIndex] || null;
    const queueItemId = selectedItem ? this._getQueueItemStableId(selectedItem) || this._getQueueItemKey(selectedItem) : slide.dataset.queueItemId || "";
    const uri = selectedItem ? this._getQueueItemUri(selectedItem) : slide.dataset.uri || "";
    const mediaType = selectedItem?.media_item?.media_type || selectedItem?.media_type || slide.dataset.type || "track";
    const sortIndex = selectedItem && Number.isFinite(Number(selectedItem?.sort_index)) ? Number(selectedItem.sort_index) : slide.dataset.sortIndex || "";
    if (!queueItemId && !uri) return;
    const played = await this._playQueueItem(queueItemId, uri, mediaType, sortIndex);
    if (!played) return;
    this._state.mobileArtBrowseOffset = 0;
    clearTimeout(this._mobileArtBrowseResetTimer);
    this._mobileArtBrowseResetTimer = null;
    this._refreshMobileArtStack(true);
  }
  async _handleCompactCoverTap(e) {
    if (Date.now() - Number(this._state.mobileArtJustSwipedAt || 0) < 260) return;
    const artHost = this.$("npArt");
    if (artHost?.dataset.emptyAction === "random") {
      this._pressUiButton(artHost);
      await this._playRandomFromPlaylists();
      return;
    }
    if (this._mobileSwipeMode() !== "browse" || !Number(this._state.mobileArtBrowseOffset || 0)) {
      this._hapticTap([6]);
      return;
    }
    const context = this._mobileArtStackContext();
    const selectedItem = context.queueItems?.[context.displayIndex] || null;
    const queueItemId = selectedItem ? this._getQueueItemStableId(selectedItem) || this._getQueueItemKey(selectedItem) : artHost?.dataset.queueItemId || "";
    const uri = selectedItem ? this._getQueueItemUri(selectedItem) : artHost?.dataset.uri || "";
    const mediaType = selectedItem?.media_item?.media_type || selectedItem?.media_type || artHost?.dataset.type || "track";
    const sortIndex = selectedItem && Number.isFinite(Number(selectedItem?.sort_index)) ? Number(selectedItem.sort_index) : artHost?.dataset.sortIndex || "";
    if (!queueItemId && !uri) return;
    this._pressUiButton(artHost);
    const played = await this._playQueueItem(queueItemId, uri, mediaType, sortIndex);
    if (!played) return;
    this._state.mobileArtBrowseOffset = 0;
    clearTimeout(this._mobileArtBrowseResetTimer);
    this._mobileArtBrowseResetTimer = null;
    this._refreshMobileArtStack(true);
  }
  _build() {
    const rtl = this._isHebrew();
    const visualTheme = this._visualTheme();
    const mobileLayoutMode = this._mobileLayoutMode();
    const compactMode = this._mobileCompactModeEnabled();
    const compactTileMode = this._isCompactTileMode();
    const compactPopupMode = compactMode && !compactTileMode;
    const compactEdgeToEdgePopupMode = compactPopupMode && this._compactEdgeToEdgeAllowed();
    const compactWindowPopupMode = compactPopupMode && !compactEdgeToEdgePopupMode;
    const compactPopupLayoutMode = compactEdgeToEdgePopupMode || compactWindowPopupMode;
    const compactLayoutWidth = this._getCardWidth(this._lastCardWidth || (typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0) || 390);
    const compactMiniWidget = compactTileMode && this._compactMiniWidgetMode({ width: compactLayoutWidth });
    const compactTileReservedHeight = this._compactTileReservedHeight();
    this.style?.setProperty("--homeii-compact-tile-height", `${compactTileReservedHeight}px`);
    const viewportWidth = typeof window !== "undefined" ? Number(window.innerWidth || 0) : 0;
    const fallbackHeight = this._configuredCardHeightFallback(760) || 760;
    const viewportHeight = this._getViewportHeight(fallbackHeight);
    const compactWindowChrome = viewportWidth > 0 && viewportWidth < 760 ? 156 : 112;
    const compactWindowWidth = compactWindowPopupMode ? Math.max(320, Math.min(720, Math.max(0, viewportWidth || compactLayoutWidth || 390) - 32)) : 0;
    const compactWindowHeight = compactWindowPopupMode ? Math.max(340, Math.min(860, Math.max(0, viewportHeight - compactWindowChrome), fallbackHeight)) : 0;
    if (compactWindowPopupMode) {
      this.style?.setProperty("--homeii-compact-window-width", `${Math.round(compactWindowWidth)}px`);
      this.style?.setProperty("--homeii-compact-window-height", `${Math.round(compactWindowHeight)}px`);
    } else {
      this.style?.removeProperty("--homeii-compact-window-width");
      this.style?.removeProperty("--homeii-compact-window-height");
    }
    const layoutMode = this._layoutModeConfig({ compactPopup: compactPopupLayoutMode, width: compactWindowPopupMode ? compactWindowWidth : 0 });
    const mobileIconScale = this._mobileIconScale();
    const allocatedHeight = compactEdgeToEdgePopupMode ? viewportHeight : compactWindowPopupMode ? compactWindowHeight : compactTileMode ? compactTileReservedHeight : this._getAllocatedCardHeight(fallbackHeight);
    const layoutProfile = this._layoutProfileConfig(layoutMode, {
      width: compactEdgeToEdgePopupMode ? this._getCardWidth(viewportWidth) : compactWindowPopupMode ? compactWindowWidth : 0,
      height: allocatedHeight,
      compactPopup: compactPopupLayoutMode
    });
    const layoutProfileClass = this._layoutProfileClassNames(layoutProfile);
    const layoutProfileStyle = this._layoutProfileStyleVars(layoutProfile);
    const performanceProfile2 = this._performanceProfile();
    const performanceMode = this._performanceModeEnabled();
    const performanceUltraLite = this._performanceUltraLiteEnabled();
    this.classList.toggle("compact-popup-open", compactEdgeToEdgePopupMode);
    this.classList.toggle("compact-window-popup-open", compactWindowPopupMode);
    this.classList.remove("compact-inline-popup-open");
    this.classList.toggle("compact-tile-open", compactMode && compactTileMode);
    this.classList.toggle("compact-menu-open", this._compactMenuOverlayOpen());
    const nightMode = this._mobileNightMode();
    const nightActive = this._isNightModeActive();
    const sleepTimerActive = this._sleepTimerRemainingMs() > 0;
    const showUpNext = this._mobileShowUpNextEnabled();
    const hasUpNextItem = !!this._mobileUpNextItem();
    const showUpNextInline = showUpNext && hasUpNextItem;
    const showNightRow = nightMode !== "off";
    const tabletAutoFit = this._tabletAutoFitEnabled();
    const tabletDenseUi = this._tabletAutoFitDense(showNightRow, showUpNextInline) || layoutMode === "tablet" && layoutProfile.heightSize === "short";
    const mobileDenseContent = this._mobileContentDense(layoutMode, layoutProfile, {
      showNightRow,
      showUpNextInline
    });
    const fullInlineTargetHeight = this._fullMobileInlineTargetHeight();
    const height = Math.max(280, Math.min(2200, Math.round(allocatedHeight || (mobileLayoutMode === "full" ? fullInlineTargetHeight : fallbackHeight))));
    const minCardHeight = layoutProfile.heightSize === "short" ? 280 : layoutMode === "tablet" ? 420 : 360;
    const hostMinWidth = layoutMode === "tablet" ? "min(calc(100vw - 32px), 720px)" : "0px";
    const screensaverClockSize = this._screensaverClockSize();
    const screensaverClockX = this._screensaverClockX();
    const screensaverClockY = this._screensaverClockY();
    const screensaverControlButtons = this._screensaverControlButtons();
    const screensaverActionButtonsHtml = screensaverControlButtons.filter((value) => value !== "like").map((value) => this._screensaverControlButtonHtml(value)).filter(Boolean).join("");
    const screensaverLikeButtonHtml = screensaverControlButtons.includes("like") ? `<button class="screensaver-voice-btn screensaver-control-btn screensaver-like-btn ${this._currentMediaFavoriteState() ? "active" : ""}" id="screensaverLikeBtn" data-screensaver-control="like" title="${this._esc(this._i18n("ui.like_2"))}" aria-label="${this._esc(this._i18n("ui.like_2"))}">${this._iconSvg(this._currentMediaFavoriteState() ? "heart_filled" : "heart_outline")}</button>` : "";
    const compactTransition = String(this._state.mobileCompactTransition || "");
    const compactTransitionClass = compactTransition === "expand" ? " compact-transition-expand" : compactTransition === "collapse" ? " compact-transition-collapse" : "";
    this._renderedLayoutMode = layoutMode;
    this._lastCardWidth = Math.max(0, Math.round(layoutProfile.width || this._lastCardWidth || 0));
    this._lastCardHeight = Math.max(0, Math.round(height || this._lastCardHeight || 0));
    this._state.mobileNightRenderedActive = nightActive;
    this._state.mobileNightRenderedMode = nightMode;
    const nightQuickRowHtml = showNightRow ? `
      <div class="night-quick-row ${nightMode === "auto" ? "auto-mode" : "on-mode"}" id="nightQuickRow" ${showNightRow ? "" : "hidden"}>
        <button class="night-quick-btn icon-only ${nightActive || nightMode === "on" ? "active" : "soft"}" id="nightModeQuickBtn" title="${this._esc(this._i18n("ui.night_mode"))}">
          ${this._iconSvg("moon")}
        </button>
        <button class="night-quick-btn icon-only ${sleepTimerActive ? "active" : ""}" id="nightSleepBtn" title="${this._esc(this._i18n("ui.sleep_timer"))}" ${nightMode === "on" ? "" : "hidden"}>
          ${this._iconSvg("timer")}
        </button>
        <button class="night-quick-btn icon-only soft" id="nightChillBtn" title="${this._esc(this._i18n("ui.chill_mix"))}" ${nightMode === "on" ? "" : "hidden"}>
          ${this._iconSvg("wand")}
        </button>
      </div>` : ``;
    const playerFocusCoreHtml = `
        <button class="player-focus" id="activePlayerChip" title="${this._i18n("ui.choose_player")}">
          <span class="player-focus-copy">
            <span class="player-focus-name" id="selectedPlayerTitle">${this._i18n("ui.selected_player")}</span>
            <span class="player-focus-tags" id="selectedPlayerTags"></span>
          </span>
          <span class="player-focus-art-wrap" aria-hidden="true">
            <span class="player-focus-art" id="selectedPlayerThumb"></span>
          </span>
        </button>
      `;
    const playerFocusHtml = playerFocusCoreHtml;
    const hotelMode = this._isHotelMode();
    const volumeMode = hotelMode ? "always" : layoutMode === "tablet" ? this._mobileVolumeMode() : "always";
    const mainBarItems = this._mobileMainBarItems();
    const configuredQuickActions = this._mobileVisibleQuickActions(this._mobileQuickActions());
    const quickActions = configuredQuickActions;
    const quickActionsWithVoice = quickActions;
    const quickActionsWithHome = quickActionsWithVoice;
    const quickActionsWithPower = this._mobileActionsWithAuxiliary(quickActionsWithHome);
    const controlRoomEnabled = this._controlRoomEnabled();
    const studioShortcutEnabled = this._mobileStudioShortcutEnabled();
    const mainBarButtons = [];
    if (!hotelMode && controlRoomEnabled && studioShortcutEnabled) {
      mainBarButtons.push(`<button class="footer-btn control-room-entry" data-mainbar-action="control_room" title="${this._controlRoomLabel()}">${this._mobileFooterButtonInner("grid", this._controlRoomLabel())}</button>`);
    }
    if (mainBarItems.includes("actions")) {
      mainBarButtons.push(`<button class="footer-btn" data-mainbar-action="actions" title="${this._i18n("ui.actions_2")}">${this._mobileFooterButtonInner("menu", this._i18n("ui.actions_2"))}</button>`);
    }
    if (mainBarItems.includes("players")) {
      mainBarButtons.push(`<button class="footer-btn" data-mainbar-action="players" title="${this._i18n("ui.players")}">${this._mobileFooterButtonInner("speaker", this._i18n("ui.players"))}</button>`);
    }
    if (mainBarItems.includes("library")) {
      mainBarButtons.push(`<button class="footer-btn soft-accent" data-mainbar-action="library" title="${this._i18n("ui.library")}">${this._mobileFooterButtonInner("library_music", this._i18n("ui.library"))}</button>`);
    }
    if (mainBarItems.includes("search")) {
      mainBarButtons.push(`<button class="footer-btn" data-mainbar-action="search" title="${this._i18n("ui.search")}">${this._mobileFooterButtonInner("search", this._i18n("ui.search"))}</button>`);
    }
    if (mainBarItems.includes("settings")) {
      mainBarButtons.push(`<button class="footer-btn accent" data-mainbar-action="settings" title="${this._i18n("ui.settings")}">${this._mobileFooterButtonInner("settings", this._i18n("ui.settings"))}</button>`);
    }
    if (mainBarItems.includes("theme")) {
      mainBarButtons.push(`<button class="footer-btn" data-mainbar-action="theme" title="${this._i18n("ui.theme_2")}">${this._mobileThemeFooterInner()}</button>`);
    }
    const volumeStepButtonsEnabled = hotelMode || this._mobileVolumeStepButtonsEnabled();
    const volumeStepPercent = this._mobileVolumeStepPercent();
    const volumeDownButtonHtml = volumeStepButtonsEnabled ? `<button class="volume-step-btn volume-step-minus" id="mobileVolumeDownBtn" title="${this._esc(this._i18n("ui.volume_down"))}" aria-label="${this._esc(this._i18n("ui.volume_down"))}" data-volume-step="-1">${this._iconSvg("minus")}</button>` : ``;
    const volumeUpButtonHtml = volumeStepButtonsEnabled ? `<button class="volume-step-btn volume-step-plus" id="mobileVolumeUpBtn" title="${this._esc(this._i18n("ui.volume_up"))}" aria-label="${this._esc(this._i18n("ui.volume_up"))}" data-volume-step="1">${this._iconSvg("plus")}</button>` : ``;
    const volumeInlineClass = volumeStepButtonsEnabled ? " has-volume-step-buttons" : "";
    const volumeHtml = `
      <div class="mobile-volume-inline${layoutMode === "tablet" ? " tablet-volume-inline" : ""}${volumeInlineClass}" data-volume-step-percent="${this._esc(String(volumeStepPercent))}">
        <button class="volume-value" id="mobileVolPctLabel" title="${this._i18n("ui.volume_presets")}">50%</button>
        ${volumeDownButtonHtml}
        <div class="tablet-volume-track">
          <input class="volume-slider" id="volSlider" type="range" min="0" max="100" value="50" style="--vol-pct:50%">
        </div>
        ${volumeUpButtonHtml}
        <button class="volume-btn" id="btnMute">${this._iconSvg("volume_high")}</button>
      </div>`;
    const footerHtml = mainBarButtons.length ? `<div class="footer-nav count-${mainBarButtons.length}">${mainBarButtons.join("")}</div>` : "";
    const compactCollapseFabHtml = compactPopupMode ? `<button class="compact-collapse-fab ${rtl ? "rtl" : "ltr"}" id="compactCollapseBtn" title="${this._i18n("ui.collapse_compact_player")}">${this._iconSvg("close")}</button>` : ``;
    const homeShortcutFabHtml = ``;
    const historyEdgeClass = rtl ? "left-edge" : "right-edge";
    const historyToggleButtonHtml = !compactTileMode ? `<button class="history-toggle-fab ${layoutMode === "tablet" ? "tablet-history-fab history-drawer-fab" : ""} ${historyEdgeClass}" id="historyToggleFab" title="${this._i18n("ui.recently_played_2")}" aria-expanded="false" hidden>${this._iconSvg(layoutMode === "tablet" ? "drawer_handle" : "history")}</button>` : ``;
    const floatingHistoryToggleFabHtml = layoutMode === "tablet" && quickActionsWithPower.includes("history") ? historyToggleButtonHtml : ``;
    const mobileHistoryToggleButtonHtml = layoutMode !== "tablet" && quickActionsWithPower.includes("history") ? historyToggleButtonHtml : ``;
    const sleepTimerCornerMarkup = !compactTileMode ? `
      <div class="sleep-timer-corner ${rtl ? "left" : "right"}" id="sleepTimerCorner" hidden></div>
    ` : ``;
    const floatingSleepTimerCornerHtml = layoutMode === "tablet" && (quickActionsWithPower.includes("timer") || sleepTimerActive) ? sleepTimerCornerMarkup : ``;
    const tabletBrandWatermarkHtml = layoutMode === "tablet" && !compactTileMode ? `<div class="tablet-brand-watermark" aria-hidden="true">${this._tabletBrandSignatureHtml()}</div>` : ``;
    const mobileBrandSignatureHtml = layoutMode !== "tablet" && !compactTileMode ? `<div class="mobile-brand-signature" aria-hidden="true">${this._tabletBrandSignatureHtml("mobile-brand-logo")}</div>` : ``;
    const quickActionsInArtRow = layoutMode === "tablet" ? quickActionsWithPower.filter((action) => action !== "history" && action !== "timer") : quickActionsWithPower;
    const mobileQuickActionsHtml = this._mobileQuickActionButtonsHtml(mobileHistoryToggleButtonHtml, quickActionsInArtRow);
    const controlRoomBackdropHtml = !hotelMode && controlRoomEnabled ? `
      <div class="control-room-backdrop" id="controlRoomBackdrop">
        <div class="control-room-shell">
          <div class="control-room-head">
            <div class="control-room-head-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("control-room-head-logo")}</div>
            <button class="control-room-close" id="controlRoomCloseBtn" title="${this._esc(this._i18n("ui.close"))}">${this._iconSvg("close")}</button>
          </div>
          <div class="control-room-body-host" id="controlRoomBody"></div>
        </div>
      </div>
    ` : ``;
    const compactTileHtml = `
      <div class="compact-shell premium-player-tile">
        <div class="compact-backdrop-art" id="compactBackdropArt"></div>
        <div class="compact-backdrop-shade"></div>
        <div class="compact-sheen"></div>
        <div class="compact-content">
          <div class="compact-header">
            <button class="compact-expand-btn compact-expand-ref" id="compactExpandBtn" title="${this._i18n("ui.expand_player")}">
              ${this._iconSvg("fullscreen")}
            </button>
            <button class="compact-player-chip" id="activePlayerChip" title="${this._i18n("ui.choose_player")}">
              <span class="compact-player-copy">
                <span class="compact-player-label" id="selectedPlayerTitle">${this._i18n("ui.selected_player")}</span>
              </span>
            </button>
          </div>
          <div class="compact-stage">
            <div class="compact-cover-wrap">
              <div class="compact-brand-signature" aria-hidden="true">${this._tabletBrandSignatureHtml("compact-brand-logo")}</div>
              <div class="art-source-badges compact-source-badges" data-art-source-badges hidden></div>
              <div class="compact-cover-echo" id="compactCoverAura"></div>
              <button class="compact-cover" id="npArt" title="${this._i18n("ui.play_artwork_action")}">
                <img class="compact-cover-image" id="compactCoverImage" alt="">
                <span class="compact-cover-placeholder">${this._artPlaceholderHtml("music_note")}</span>
              </button>
            </div>
            <div class="compact-main">
              <div class="compact-copy">
                <div class="compact-title np-title" id="npTitle">${this._i18n("ui.nothing_playing")}</div>
                <div class="compact-sub np-sub" id="npSub">—</div>
                <button class="up-next-inline compact-up-next" data-up-next-inline hidden>
                  <span class="up-next-art"></span>
                  <span class="up-next-line">
                    <span class="up-next-prefix">${this._i18n("ui.up_next_2")}</span>
                    <span class="up-next-title"></span>
                  </span>
                </button>
                ${nightQuickRowHtml}
              </div>
            </div>
          </div>
          <div class="compact-controls">
            <button class="side-btn compact-control-btn" id="btnPrev">${this._iconSvg("previous")}</button>
            <button class="main-btn compact-main-btn" id="btnPlay">${this._iconSvg("play")}</button>
            <button class="side-btn compact-control-btn" id="btnNext">${this._iconSvg("next")}</button>
          </div>
          <div class="compact-progress-row">
            <span class="compact-progress-time" id="bigCurTime">0:00</span>
            <div class="progress compact-progress-track" id="progressBar"><div class="progress-fill" id="progressFill"></div></div>
            <span class="compact-progress-time" id="bigTotalTime">0:00</span>
          </div>
          <div class="compact-volume-inline${volumeInlineClass}" data-volume-step-percent="${this._esc(String(volumeStepPercent))}">
            <button class="volume-btn compact-mute-btn" id="btnMute">${this._iconSvg("volume_high")}</button>
            ${volumeDownButtonHtml}
            <div class="tablet-volume-track compact-volume-track">
              <input class="volume-slider compact-volume-slider" id="volSlider" type="range" min="0" max="100" value="50" style="--vol-pct:50%">
            </div>
            ${volumeUpButtonHtml}
            <button class="volume-value compact-volume-value" id="mobileVolPctLabel" title="${this._i18n("ui.volume_presets")}">50%</button>
          </div>
        </div>
      </div>`;
    const centerHtml = `
      <div class="center">
        <div class="hero-aura" id="mobileHeroAura"></div>
        ${layoutMode === "tablet" ? `` : `<div class="hero-mobile-top">${playerFocusHtml}</div>`}
        <div class="hero-split-shell">
          <div class="hero-visual">
            <div class="art-stage">
              <div class="art-aura" id="mobileArtAura"></div>
              <div class="mobile-art-shell" id="mobileArtShell">
                <div class="art-source-badges" data-art-source-badges hidden></div>
                <div class="art-stack-view" id="npArt">
                  ${this._mobileArtworkStackHtml()}
                </div>
              </div>
            </div>
          </div>
          <div class="hero-info">
            <div class="hero-copy">
              <div class="hero-title np-title" id="npTitle">${this._i18n("ui.nothing_playing")}</div>
              <div class="hero-sub np-sub" id="npSub">—</div>
              <button class="up-next-inline hero-up-next" data-up-next-inline hidden>
                <span class="up-next-art"></span>
                <span class="up-next-line">
                  <span class="up-next-prefix">${this._i18n("ui.up_next_2")}</span>
                  <span class="up-next-title"></span>
                </span>
              </button>
            </div>
            <div class="mobile-action-row-wrap">
              <div class="mobile-art-actions count-${Math.max(1, quickActionsInArtRow.length)}" id="mobileArtActions">
                ${mobileQuickActionsHtml}
              </div>
            </div>
            ${nightQuickRowHtml}
          </div>
        </div>
      </div>`;
    const bottomHtml = `
        <div class="bottom">
        <div class="notice" id="mobileNotice"></div>
        <div class="empty-quick-shelf" id="emptyQuickShelf" hidden></div>
        <div class="progress-line">
          <span class="progress-time" id="bigCurTime">0:00</span>
          <div class="progress" id="progressBar"><div class="progress-fill" id="progressFill"></div></div>
          <span class="progress-time" id="bigTotalTime">0:00</span>
        </div>
        <div class="controls">
          <button class="side-btn minor-btn" id="mobileShuffleBtn">${this._iconSvg("shuffle")}</button>
          <button class="side-btn" id="btnPrev">${this._iconSvg("previous")}</button>
          <button class="main-btn" id="btnPlay">${this._iconSvg("play")}</button>
          <button class="side-btn" id="btnNext">${this._iconSvg("next")}</button>
          ${volumeMode === "button" ? `<button class="side-btn" id="controlVolumeBtn">${this._iconSvg("volume_high")}</button>` : ``}
          <button class="side-btn minor-btn" id="mobileRepeatBtn">${this._iconSvg("repeat")}</button>
        </div>
        ${volumeMode === "always" ? volumeHtml : ``}
      </div>`;
    const tabletNavRailHtml = `<aside class="tablet-rail">${playerFocusHtml}${footerHtml}</aside>`;
    const tabletStageHtml = rtl ? `<div class="tablet-shell"><div class="tablet-main">${centerHtml}${bottomHtml}</div>${tabletNavRailHtml}</div>` : `<div class="tablet-shell">${tabletNavRailHtml}<div class="tablet-main">${centerHtml}${bottomHtml}</div></div>`;
    this.shadowRoot.innerHTML = `
      <style>
        :host { display:block; container-type:inline-size; container-name:homeii-flow-host; width:100%; min-width:${hostMinWidth}; max-width:100%; margin:0 !important; padding:0 !important; background:transparent !important; border:none !important; box-shadow:none !important; overflow:hidden !important; border-radius:var(--ma-radius-xl); --ma-radius-xl:28px; --mobile-height:${height}px; --mobile-min-height:${minCardHeight}px; --v2-font-scale:${this._state.mobileFontScale || 1}; --v2-icon-scale:${mobileIconScale.toFixed(2)}; --v2-custom-rgb:${this._customRgb()}; --v2-custom-text:${this._customTextColor()}; --accent-color:${this._state.mobileCustomColor || "#e0a11b"}; --ma-accent: var(--accent-color, #e0a11b); }
        ha-card { background:transparent !important; border:none !important; box-shadow:none !important; overflow:hidden !important; border-radius:var(--ma-radius-xl); }
        * { box-sizing:border-box; }
        .side-btn .ui-ic,
        .main-btn .ui-ic,
        .volume-btn .ui-ic,
        .mobile-art-fab .ui-ic,
        .footer-btn .ui-ic,
        .compact-control-btn .ui-ic,
        .compact-main-btn .ui-ic,
        .empty-voice-btn .ui-ic {
          transform:scale(var(--v2-icon-scale, 1));
          transform-origin:center;
        }
        .card {
          position:relative; overflow:hidden; isolation:isolate; color:#fff;
          container-type:inline-size;
          container-name:homeii-flow-card;
          width:100%;
          max-width:100%;
          height:var(--mobile-height);
          min-height:min(var(--mobile-min-height), var(--mobile-height));
          max-height:var(--mobile-height);
          border-radius:var(--ma-radius-xl); border:1px solid rgba(255,255,255,.1);
          background:#0c0f16; box-shadow:0 24px 56px rgba(0,0,0,.3);
          font-size:calc(16px * var(--v2-font-scale));
          --flow-stage-pad-block:16px;
          --flow-stage-pad-inline:14px;
          --flow-shell-gap:28px;
          --flow-tablet-rail:102px;
          --flow-tablet-max:calc(100cqi - 34px);
          --flow-hero-gap:40px;
          --flow-hero-info-min:300px;
          --flow-hero-aura-width:min(980px, 74cqi);
          --flow-hero-aura-height:260px;
          --flow-tablet-aura-width:min(820px, 56cqi);
          --flow-tablet-aura-height:320px;
          --flow-tablet-title-size:30px;
          --flow-tablet-sub-size:14px;
          --flow-compact-title-size:64px;
          --flow-side-btn-size:58px;
          --flow-minor-btn-size:48px;
          --flow-main-btn-size:118px;
          --flow-tablet-side-btn-size:64px;
          --flow-tablet-minor-btn-size:54px;
          --flow-tablet-main-btn-size:110px;
          --flow-art-aura-size:min(420px, 42cqi);
          --flow-tablet-art-aura-size:min(520px, 42cqi);
          --flow-mobile-art-size:clamp(190px, min(72cqi, var(--flow-mobile-art-budget)), 420px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
          --flow-art-card-max-size:var(--flow-art-stack-height);
          --flow-autofit-art-height:clamp(320px, min(34cqi, var(--flow-tablet-art-budget)), 620px);
          --flow-dense-art-height:clamp(250px, min(28cqi, var(--flow-tablet-dense-art-budget)), 480px);
          --flow-sheet-gutter:96px;
          --flow-sheet-queue-gutter:160px;
          --flow-sheet-narrow-gutter:180px;
          --flow-sheet-pad-block:18px;
          --flow-sheet-pad-inline:24px;
          --flow-media-grid-thumb:clamp(148px, 42cqi, 188px);
          --flow-tablet-media-grid-thumb:clamp(178px, 18cqi, 224px);
          --flow-control-room-brand-width:clamp(118px, 18cqi, 210px);
          --flow-full-mobile-target-height:${fullInlineTargetHeight}px;
        }
        .card.size-xs {
          --flow-stage-pad-block:12px;
          --flow-stage-pad-inline:10px;
          --flow-shell-gap:16px;
          --flow-hero-gap:20px;
          --flow-tablet-rail:86px;
          --flow-tablet-title-size:24px;
          --flow-tablet-sub-size:12px;
          --flow-compact-title-size:44px;
          --flow-side-btn-size:48px;
          --flow-minor-btn-size:40px;
          --flow-main-btn-size:84px;
          --flow-tablet-side-btn-size:54px;
          --flow-tablet-minor-btn-size:46px;
          --flow-tablet-main-btn-size:92px;
          --flow-mobile-art-size:clamp(156px, min(66cqi, var(--flow-mobile-short-art-budget)), 286px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
          --flow-autofit-art-height:clamp(230px, min(52cqi, var(--flow-tablet-art-budget)), 340px);
          --flow-dense-art-height:clamp(210px, min(48cqi, var(--flow-tablet-dense-art-budget)), 300px);
          --flow-sheet-gutter:28px;
          --flow-sheet-queue-gutter:28px;
          --flow-sheet-narrow-gutter:28px;
          --flow-sheet-pad-inline:12px;
          --flow-media-grid-thumb:clamp(132px, 52cqi, 164px);
        }
        .card.size-sm {
          --flow-stage-pad-block:14px;
          --flow-stage-pad-inline:12px;
          --flow-shell-gap:20px;
          --flow-hero-gap:24px;
          --flow-tablet-rail:92px;
          --flow-tablet-title-size:26px;
          --flow-compact-title-size:52px;
          --flow-side-btn-size:52px;
          --flow-main-btn-size:96px;
          --flow-tablet-side-btn-size:58px;
          --flow-tablet-main-btn-size:100px;
          --flow-mobile-art-size:clamp(170px, min(68cqi, var(--flow-mobile-art-budget)), 340px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
          --flow-sheet-gutter:40px;
          --flow-sheet-queue-gutter:48px;
          --flow-sheet-narrow-gutter:48px;
          --flow-media-grid-thumb:clamp(142px, 44cqi, 174px);
        }
        .card.size-md {
          --flow-shell-gap:22px;
          --flow-hero-gap:28px;
          --flow-tablet-rail:96px;
          --flow-tablet-title-size:28px;
          --flow-compact-title-size:58px;
          --flow-sheet-gutter:56px;
          --flow-sheet-queue-gutter:72px;
          --flow-sheet-narrow-gutter:72px;
        }
        .card.size-lg {
          --flow-shell-gap:28px;
          --flow-hero-gap:36px;
        }
        .card.size-xl {
          --flow-shell-gap:34px;
          --flow-hero-gap:46px;
          --flow-tablet-title-size:34px;
          --flow-tablet-sub-size:16px;
          --flow-compact-title-size:76px;
        }
        .card.height-short {
          --flow-stage-pad-block:10px;
          --flow-shell-gap:16px;
          --flow-hero-gap:22px;
          --flow-tablet-title-size:24px;
          --flow-tablet-sub-size:12px;
          --flow-compact-title-size:42px;
          --flow-side-btn-size:48px;
          --flow-minor-btn-size:40px;
          --flow-main-btn-size:86px;
          --flow-tablet-side-btn-size:54px;
          --flow-tablet-minor-btn-size:46px;
          --flow-tablet-main-btn-size:92px;
          --flow-hero-aura-height:190px;
          --flow-tablet-aura-height:230px;
          --flow-mobile-art-size:clamp(142px, min(58cqi, var(--flow-mobile-short-art-budget)), 280px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
          --flow-autofit-art-height:clamp(220px, min(24cqi, var(--flow-tablet-art-budget)), 360px);
          --flow-dense-art-height:clamp(200px, min(22cqi, var(--flow-tablet-dense-art-budget)), 320px);
          --flow-sheet-pad-block:12px;
        }
        .card.height-tight {
          --flow-stage-pad-block:8px;
          --flow-shell-gap:12px;
          --flow-hero-gap:14px;
          --flow-tablet-rail:78px;
          --flow-tablet-title-size:20px;
          --flow-tablet-sub-size:11px;
          --flow-compact-title-size:34px;
          --flow-side-btn-size:42px;
          --flow-minor-btn-size:36px;
          --flow-main-btn-size:72px;
          --flow-tablet-side-btn-size:44px;
          --flow-tablet-minor-btn-size:38px;
          --flow-tablet-main-btn-size:76px;
          --flow-hero-aura-height:150px;
          --flow-tablet-aura-height:180px;
          --flow-mobile-art-size:clamp(122px, min(50cqi, var(--flow-mobile-tight-art-budget)), 220px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
          --flow-autofit-art-height:clamp(168px, min(20cqi, var(--flow-tablet-art-budget)), 260px);
          --flow-dense-art-height:clamp(152px, min(18cqi, var(--flow-tablet-dense-art-budget)), 230px);
          --flow-sheet-pad-block:10px;
        }
        .card button,
        .card input,
        .card select,
        .card textarea { font:inherit; }
        .theme-light.card {
          color:#1f2633;
          border-color:rgba(135,152,178,.22);
          background:#eef2f7;
          box-shadow:0 22px 56px rgba(73,89,110,.18);
        }
        .theme-custom.card {
          color:var(--v2-custom-text, #fff);
          border-color:rgba(var(--v2-custom-rgb) / .18);
          background:rgba(var(--v2-custom-rgb) / .14);
          box-shadow:0 22px 56px rgba(0,0,0,.16);
        }
        .card.dynamic-theme {
          border-color:rgba(var(--dynamic-accent-rgb, 224 161 27) / .24);
          box-shadow:
            0 24px 56px rgba(0,0,0,.28),
            0 0 0 1px rgba(var(--dynamic-accent-rgb, 224 161 27) / .08),
            0 18px 42px rgba(var(--dynamic-glow-rgb, 255 178 56) / .12);
        }
        .theme-light.card.dynamic-theme {
          box-shadow:
            0 22px 56px rgba(73,89,110,.16),
            0 0 0 1px rgba(var(--dynamic-accent-rgb, 224 161 27) / .08),
            0 18px 40px rgba(var(--dynamic-glow-rgb, 255 178 56) / .1);
        }
        .bg,.shade,.glow { position:absolute; inset:0; pointer-events:none; }
        .bg {
          background:
            radial-gradient(circle at 18% 20%, rgba(255,181,64,.18), transparent 34%),
            linear-gradient(180deg, rgba(9,12,19,.34), rgba(9,12,19,.94)),
            #0c0f16;
          background-position:center center; background-size:cover; background-repeat:no-repeat;
          filter:blur(30px) saturate(1.08); transform:scale(1.08); opacity:.9;
          overflow:hidden;
          will-change:transform, opacity;
        }
        .bg::before,
        .bg::after {
          content:"";
          position:absolute;
          inset:-1px;
          background:var(--homeii-bg-art-current, none) center / cover no-repeat;
          opacity:.72;
          pointer-events:none;
          transition:opacity .52s cubic-bezier(.22,.78,.24,1);
          will-change:opacity;
        }
        .bg::after {
          background:var(--homeii-bg-art-next, none) center / cover no-repeat;
          opacity:0;
        }
        .bg.bg-art-transitioning::before {
          opacity:0;
        }
        .bg.bg-art-transitioning::after {
          opacity:.72;
        }
        @media (prefers-reduced-motion: reduce) {
          .bg::before,
          .bg::after {
            transition:none !important;
          }
        }
        .shade {
          background:linear-gradient(180deg, rgba(9,12,19,.18), rgba(9,12,19,.78) 38%, rgba(9,12,19,.98));
          will-change:opacity, filter;
        }
        .glow {
          background:radial-gradient(circle at 50% 76%, rgba(255,178,56,.2), transparent 30%);
          will-change:transform, opacity;
        }
        @keyframes backgroundFloat {
          0% { transform:translate3d(0, 0, 0) scale(var(--bg-motion-scale, 1.13)); opacity:.9; }
          22% { transform:translate3d(calc(var(--bg-motion-shift, 28px) * .62), calc(var(--bg-motion-shift, 28px) * -.42), 0) scale(calc(var(--bg-motion-scale, 1.13) + .024)); opacity:.98; }
          52% { transform:translate3d(calc(var(--bg-motion-shift, 28px) * -.78), calc(var(--bg-motion-shift, 28px) * .52), 0) scale(calc(var(--bg-motion-scale, 1.13) + .05)); opacity:1; }
          78% { transform:translate3d(calc(var(--bg-motion-shift, 28px) * .28), calc(var(--bg-motion-shift, 28px) * .22), 0) scale(calc(var(--bg-motion-scale, 1.13) + .016)); opacity:.95; }
          100% { transform:translate3d(0, 0, 0) scale(var(--bg-motion-scale, 1.13)); opacity:.9; }
        }
        @keyframes glowDrift {
          0% { transform:translate3d(0, 0, 0) scale(1); opacity:.74; }
          50% { transform:translate3d(calc(var(--bg-motion-shift, 28px) * -.58), calc(var(--bg-motion-shift, 28px) * .38), 0) scale(calc(1 + (.085 * var(--bg-motion-strength, 1)))); opacity:1; }
          100% { transform:translate3d(0, 0, 0) scale(1); opacity:.74; }
        }
        @keyframes shadeBreathe {
          0% { opacity:.94; filter:saturate(1); }
          50% { opacity:calc(.82 - (.04 * (var(--bg-motion-strength, 1) - 1))); filter:saturate(calc(1 + (.08 * var(--bg-motion-strength, 1)))); }
          100% { opacity:.94; filter:saturate(1); }
        }
        @keyframes auraDrift {
          0% { transform:translate3d(0, 0, 0) scale(1.08); }
          50% { transform:translate3d(calc(var(--bg-motion-shift, 28px) * .34), calc(var(--bg-motion-shift, 28px) * -.26), 0) scale(calc(1.08 + (.042 * var(--bg-motion-strength, 1)))); opacity:1; }
          100% { transform:translate3d(0, 0, 0) scale(1.08); }
        }
        @keyframes idleLightOrbsDrift {
          0%,100% { transform:translate3d(-2%, 1%, 0) scale(.98); opacity:.34; }
          28% { transform:translate3d(4%, -3%, 0) scale(1.06); opacity:.48; }
          58% { transform:translate3d(-5%, -1%, 0) scale(1.12); opacity:.4; }
          82% { transform:translate3d(2%, 4%, 0) scale(1.02); opacity:.46; }
        }
        @keyframes idleLightOrbsBloom {
          0%,100% { transform:translate3d(3%, -2%, 0) scale(1.02); opacity:.26; filter:blur(2px) saturate(1.06); }
          42% { transform:translate3d(-4%, 4%, 0) scale(1.16); opacity:.38; filter:blur(4px) saturate(1.18); }
          72% { transform:translate3d(2%, 1%, 0) scale(.96); opacity:.32; filter:blur(1px) saturate(1.1); }
        }
        @keyframes idleElementFloat {
          0%,100% { transform:translate3d(0, 0, 0); }
          50% { transform:translate3d(0, -4px, 0); }
        }
        .card.background-motion .bg {
          animation:backgroundFloat var(--bg-motion-duration, 24s) ease-in-out infinite;
        }
        .card.background-motion .shade {
          animation:shadeBreathe var(--shade-motion-duration, 20s) ease-in-out infinite;
        }
        .card.background-motion .glow {
          animation:glowDrift var(--glow-motion-duration, 18s) ease-in-out infinite;
          mix-blend-mode:screen;
        }
        .card.background-motion.motion-strong .glow {
          opacity:1;
        }
        .theme-light .bg {
          filter:blur(32px) saturate(1.1) brightness(1.06);
          opacity:.86;
        }
        .theme-light .shade {
          background:
            linear-gradient(180deg, rgba(255,255,255,.06), rgba(228,235,244,.22) 18%, rgba(210,220,232,.54) 58%, rgba(196,207,221,.78));
        }
        .theme-light .glow {
          background:
            radial-gradient(circle at 18% 20%, rgba(255,193,92,.14), transparent 22%),
            radial-gradient(circle at 82% 16%, rgba(255,153,84,.1), transparent 20%),
            radial-gradient(circle at 50% 78%, rgba(255,188,74,.12), transparent 26%);
        }
        .theme-custom .bg {
          filter:blur(32px) saturate(1.1);
          opacity:.96;
        }
        .theme-custom .shade {
          background:
            linear-gradient(180deg, rgba(var(--v2-custom-rgb) / .18), rgba(var(--v2-custom-rgb) / .22) 20%, rgba(14,18,28,.18) 56%, rgba(14,18,28,.1));
        }
        .theme-custom .glow {
          background:
            radial-gradient(circle at 18% 20%, rgba(var(--v2-custom-rgb) / .28), transparent 26%),
            radial-gradient(circle at 82% 16%, rgba(var(--v2-custom-rgb) / .18), transparent 22%),
            radial-gradient(circle at 50% 78%, rgba(var(--v2-custom-rgb) / .22), transparent 30%);
        }
        .card.empty-media::before,
        .card.empty-media::after {
          content:"";
          position:absolute;
          inset:-18%;
          z-index:0;
          pointer-events:none;
          mix-blend-mode:screen;
          filter:blur(2px) saturate(1.16);
          opacity:.42;
          will-change:transform, opacity, filter;
        }
        .card.empty-media::before {
          background:
            radial-gradient(circle at 18% 72%, rgba(var(--dynamic-accent-rgb, 245 166 35) / .24), transparent 15%),
            radial-gradient(circle at 76% 18%, rgba(96,165,250,.18), transparent 13%),
            radial-gradient(circle at 58% 88%, rgba(244,114,182,.13), transparent 12%),
            radial-gradient(circle at 34% 28%, rgba(255,255,255,.10), transparent 10%);
          animation:idleLightOrbsDrift 24s ease-in-out infinite;
        }
        .card.empty-media::after {
          inset:-22%;
          background:
            radial-gradient(circle at 82% 68%, rgba(45,212,191,.16), transparent 16%),
            radial-gradient(circle at 12% 22%, rgba(250,204,21,.15), transparent 12%),
            radial-gradient(circle at 52% 46%, rgba(147,197,253,.12), transparent 18%);
          opacity:.34;
          animation:idleLightOrbsBloom 30s ease-in-out infinite;
        }
        .card.empty-media .hero-copy,
        .card.empty-media .empty-magic-stack {
          animation:idleElementFloat 11s ease-in-out infinite;
          will-change:transform;
        }
        .card.empty-media .empty-magic-stack {
          animation-delay:-3.5s;
        }
        .card.performance-lite.empty-media::before,
        .card.performance-lite.empty-media::after,
        .card.performance-lite.empty-media .hero-copy,
        .card.performance-lite.empty-media .empty-magic-stack {
          animation:none;
        }
        .theme-light.card.empty-media::before,
        .theme-light.card.empty-media::after {
          mix-blend-mode:normal;
          opacity:.3;
          filter:blur(1px) saturate(1.05);
        }
        @media (prefers-reduced-motion: reduce) {
          .card.empty-media::before,
          .card.empty-media::after,
          .card.empty-media .hero-copy,
          .card.empty-media .empty-magic-stack {
            animation:none !important;
          }
        }
        .card.dynamic-theme .bg {
          background:
            radial-gradient(circle at 18% 18%, rgba(var(--dynamic-glow-rgb, 255 178 56) / calc(.16 * var(--dynamic-theme-strength, .82))), transparent 32%),
            radial-gradient(circle at 82% 12%, rgba(var(--dynamic-accent-rgb, 224 161 27) / calc(.12 * var(--dynamic-theme-strength, .82))), transparent 26%),
            linear-gradient(180deg, rgba(var(--dynamic-surface-rgb, 12 15 22) / .42), rgba(var(--dynamic-surface-rgb, 12 15 22) / .96)),
            #0c0f16;
        }
        .card.dynamic-theme .shade {
          background:
            linear-gradient(180deg, rgba(var(--dynamic-surface-rgb, 12 15 22) / .12), rgba(var(--dynamic-surface-rgb, 12 15 22) / .64) 34%, rgba(9,12,19,.96)),
            radial-gradient(circle at 50% 84%, rgba(var(--dynamic-accent-rgb, 224 161 27) / calc(.12 * var(--dynamic-theme-strength, .82))), transparent 28%);
        }
        .card.dynamic-theme .glow {
          background:
            radial-gradient(circle at 50% 76%, rgba(var(--dynamic-glow-rgb, 255 178 56) / calc(.22 * var(--dynamic-theme-strength, .82))), transparent 32%),
            radial-gradient(circle at 16% 18%, rgba(var(--dynamic-accent-rgb, 224 161 27) / calc(.14 * var(--dynamic-theme-strength, .82))), transparent 24%);
        }
        .theme-light.card.dynamic-theme .bg {
          filter:blur(32px) saturate(calc(1.04 + (.08 * var(--dynamic-theme-strength, .82)))) brightness(1.05);
          opacity:.88;
        }
        .theme-light.card.dynamic-theme .shade {
          background:
            linear-gradient(180deg, rgba(255,255,255,.06), rgba(239,244,250,.18) 16%, rgba(var(--dynamic-surface-rgb, 224 232 244) / .38) 56%, rgba(204,214,228,.76)),
            radial-gradient(circle at 50% 82%, rgba(var(--dynamic-accent-rgb, 224 161 27) / calc(.06 * var(--dynamic-theme-strength, .82))), transparent 26%);
        }
        .theme-light.card.dynamic-theme .glow {
          background:
            radial-gradient(circle at 18% 20%, rgba(var(--dynamic-glow-rgb, 255 178 56) / calc(.12 * var(--dynamic-theme-strength, .82))), transparent 24%),
            radial-gradient(circle at 82% 16%, rgba(var(--dynamic-accent-rgb, 224 161 27) / calc(.08 * var(--dynamic-theme-strength, .82))), transparent 20%),
            radial-gradient(circle at 50% 78%, rgba(var(--dynamic-glow-rgb, 255 178 56) / calc(.1 * var(--dynamic-theme-strength, .82))), transparent 24%);
        }
        .card.night-mode {
          border-color:rgba(168,182,255,.18);
          box-shadow:0 28px 60px rgba(8,12,26,.42);
        }
        .card.night-mode.dynamic-theme {
          border-color:rgba(var(--dynamic-accent-rgb, 168 182 255) / .22);
          box-shadow:
            0 28px 60px rgba(8,12,26,.42),
            0 0 0 1px rgba(var(--dynamic-accent-rgb, 168 182 255) / .08),
            0 18px 42px rgba(var(--dynamic-accent-rgb, 168 182 255) / .12);
        }
        .card.night-mode .shade {
          background:
            linear-gradient(180deg, rgba(8,11,24,.22), rgba(8,11,24,.82) 34%, rgba(7,10,20,.98)),
            radial-gradient(circle at 50% 84%, rgba(106,125,255,.14), transparent 28%);
        }
        .card.night-mode .glow {
          background:
            radial-gradient(circle at 50% 76%, rgba(126,142,255,.18), transparent 30%),
            radial-gradient(circle at 16% 18%, rgba(118,140,255,.12), transparent 24%);
        }
        .theme-light.card.night-mode {
          background:#e7edf7;
          box-shadow:0 24px 58px rgba(57,72,105,.24);
        }
        .theme-light.card.night-mode .shade {
          background:
            linear-gradient(180deg, rgba(230,236,247,.28), rgba(214,224,240,.56) 20%, rgba(202,213,232,.82) 64%, rgba(196,208,228,.92));
        }
        .theme-light.card.night-mode .glow {
          background:
            radial-gradient(circle at 22% 18%, rgba(142,160,255,.18), transparent 22%),
            radial-gradient(circle at 50% 78%, rgba(114,132,255,.12), transparent 30%);
        }
        .card.layout-tablet {
          --tablet-max: var(--flow-tablet-max);
          --tablet-rail: var(--flow-tablet-rail);
          --flow-art-card-max-size:var(--flow-autofit-art-height);
        }
        .tablet-brand-watermark {
          display:none;
        }
        .mobile-brand-signature {
          display:none;
        }
        .control-room-brand-watermark,
        .compact-brand-signature {
          display:none;
        }
        .card.layout-tablet > .tablet-brand-watermark {
          position:absolute;
          inset-block-end:28px;
          width:clamp(156px, 15cqi, 250px);
          height:auto;
          display:grid;
          place-items:center;
          pointer-events:none;
          z-index:4;
          color:rgba(245,248,255,.38);
          opacity:.9;
          mix-blend-mode:screen;
          filter:drop-shadow(0 10px 24px rgba(0,0,0,.2)) saturate(.92);
        }
        .card:not(.layout-tablet) > .mobile-brand-signature {
          position:absolute;
          inset-block-start:6px;
          left:50%;
          right:auto;
          width:clamp(210px, 58cqi, 326px);
          max-width:calc(100% - 104px);
          min-height:64px;
          display:grid;
          place-items:center;
          color:rgba(248,251,255,.7);
          opacity:1;
          pointer-events:none;
          z-index:8;
          mix-blend-mode:normal;
          transform:translateX(-50%);
          filter:drop-shadow(0 10px 22px rgba(0,0,0,.28)) saturate(.96);
        }
        .card.layout-tablet .control-room-scene > .control-room-brand-watermark {
          position:absolute;
          inset-block-end:28px;
          width:clamp(156px, 15cqi, 250px);
          height:auto;
          display:grid;
          place-items:center;
          pointer-events:none;
          z-index:1;
          color:rgba(245,248,255,.36);
          opacity:.88;
          mix-blend-mode:screen;
          filter:drop-shadow(0 10px 24px rgba(0,0,0,.2)) saturate(.9);
        }
        .card.layout-tablet.rtl > .tablet-brand-watermark {
          inset-inline-start:48px;
          inset-inline-end:auto;
        }
        .card.layout-tablet:not(.rtl) > .tablet-brand-watermark {
          inset-inline-end:48px;
          inset-inline-start:auto;
        }
        .card.layout-tablet.rtl .control-room-brand-watermark {
          inset-inline-start:48px;
          inset-inline-end:auto;
        }
        .card.layout-tablet:not(.rtl) .control-room-brand-watermark {
          inset-inline-end:48px;
          inset-inline-start:auto;
        }
        .tablet-brand-logo,
        .control-room-brand-logo,
        .control-room-head-logo,
        .menu-title-logo,
        .lyrics-title-logo,
        .queue-action-logo,
        .smart-voice-logo,
        .voice-assistant-logo,
        .screensaver-brand-logo,
        .screensaver-empty-logo,
        .art-stack-brand-logo {
          width:100%;
          height:auto;
          display:block;
          overflow:visible;
        }
        .mobile-brand-logo {
          width:100%;
          height:auto;
          max-height:62px;
          display:block;
          overflow:visible;
        }
        .brand-signature-logo .tablet-brand-home,
        .brand-signature-logo .tablet-brand-flow {
          fill:currentColor;
          font-family:Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
          paint-order:stroke;
          stroke:rgba(2,6,14,.08);
          stroke-width:.7px;
        }
        .brand-signature-logo .tablet-brand-home {
          font-size:22px;
          font-weight:700;
          letter-spacing:8px;
          text-transform:uppercase;
        }
        .brand-signature-logo .tablet-brand-flow {
          font-size:11px;
          font-weight:650;
          letter-spacing:6px;
          opacity:.72;
          text-transform:uppercase;
        }
        .brand-signature-logo .tablet-brand-wave,
        .brand-signature-logo .tablet-brand-rule {
          stroke:currentColor;
          stroke-linecap:round;
          stroke-width:1.15;
          opacity:.46;
        }
        .brand-signature-logo .tablet-brand-rule {
          stroke-width:.75;
          opacity:.32;
        }
        .mobile-brand-logo .tablet-brand-home {
          font-size:20px;
          letter-spacing:7px;
        }
        .mobile-brand-logo .tablet-brand-flow {
          font-size:10px;
          letter-spacing:5px;
          opacity:.56;
        }
        .mobile-brand-logo .tablet-brand-wave {
          opacity:.22;
        }
        .mobile-brand-logo .tablet-brand-rule {
          opacity:.16;
        }
        .theme-light.card.layout-tablet > .tablet-brand-watermark {
          color:rgba(20,26,38,.18);
          opacity:.72;
          mix-blend-mode:multiply;
          filter:drop-shadow(0 10px 22px rgba(255,255,255,.2)) saturate(.82);
        }
        .theme-light.card.layout-tablet .control-room-brand-watermark {
          color:rgba(20,26,38,.16);
          opacity:.7;
          mix-blend-mode:multiply;
          filter:drop-shadow(0 10px 22px rgba(255,255,255,.2)) saturate(.82);
        }
        .theme-light.card.layout-tablet .tablet-brand-logo .tablet-brand-home,
        .theme-light.card.layout-tablet .tablet-brand-logo .tablet-brand-flow {
          stroke:rgba(255,255,255,.12);
        }
        .theme-light.card:not(.layout-tablet) > .mobile-brand-signature {
          color:rgba(22,28,40,.36);
          opacity:.9;
          mix-blend-mode:normal;
          filter:drop-shadow(0 8px 18px rgba(255,255,255,.24)) saturate(.84);
        }
        .theme-light.card:not(.layout-tablet) .mobile-brand-logo .tablet-brand-home,
        .theme-light.card:not(.layout-tablet) .mobile-brand-logo .tablet-brand-flow {
          stroke:rgba(255,255,255,.12);
        }
        @media (max-height: 620px) {
          .card.layout-tablet > .tablet-brand-watermark {
            width:clamp(116px, 11cqi, 170px);
            inset-block-end:18px;
            opacity:.62;
          }
          .card:not(.layout-tablet) > .mobile-brand-signature {
            width:clamp(158px, 44cqi, 232px);
            min-height:52px;
            opacity:.84;
          }
        }
        @keyframes homeiiCompactExpandCard {
          0% {
            opacity:.74;
            transform:translateY(12px) scale(.972);
            filter:blur(2px) saturate(.92);
          }
          62% {
            opacity:1;
            transform:translateY(-2px) scale(1.004);
            filter:blur(0) saturate(1.03);
          }
          100% {
            opacity:1;
            transform:translateY(0) scale(1);
            filter:none;
          }
        }
        @keyframes homeiiCompactContentRise {
          0% { opacity:.64; transform:translateY(14px) scale(.985); }
          100% { opacity:1; transform:translateY(0) scale(1); }
        }
        @keyframes homeiiCompactTileReveal {
          0% {
            opacity:.72;
            transform:translateY(-8px) scale(.975);
            filter:blur(1px) saturate(.95);
          }
          100% {
            opacity:1;
            transform:translateY(0) scale(1);
            filter:none;
          }
        }
        .card.compact-transition-expand {
          animation:homeiiCompactExpandCard .42s cubic-bezier(.22, .78, .24, 1) both;
          transform-origin:center top;
          will-change:transform, opacity, filter;
        }
        .card.compact-transition-expand .hero-split-shell,
        .card.compact-transition-expand .bottom,
        .card.compact-transition-expand .footer-nav {
          animation:homeiiCompactContentRise .38s cubic-bezier(.2, .82, .22, 1) both;
          will-change:transform, opacity;
        }
        .card.compact-transition-expand .bottom { animation-delay:.04s; }
        .card.compact-transition-expand .footer-nav { animation-delay:.07s; }
        .card.compact-transition-collapse .compact-shell {
          animation:homeiiCompactTileReveal .34s cubic-bezier(.22, .78, .24, 1) both;
          transform-origin:center top;
          will-change:transform, opacity, filter;
        }
        @media (prefers-reduced-motion: reduce) {
          .card.compact-transition-expand,
          .card.compact-transition-expand .hero-split-shell,
          .card.compact-transition-expand .bottom,
          .card.compact-transition-expand .footer-nav,
          .card.compact-transition-collapse .compact-shell {
            animation:none !important;
          }
        }
        :host(.screensaver-page-open) {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          max-width:none !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          z-index:2147483200 !important;
          display:block !important;
          overflow:visible !important;
          contain:none !important;
          border-radius:0 !important;
          background:#02060d;
        }
        :host(.screensaver-page-open) ha-card,
        :host(.screensaver-page-open) .card.screensaver-active {
          width:100vw !important;
          max-width:none !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          border-radius:0 !important;
          overflow:visible !important;
          box-shadow:none !important;
        }
        :host(.screensaver-page-open) .card.screensaver-active {
          position:fixed !important;
          inset:0 !important;
          border:0 !important;
          background:#02060d !important;
        }
        :host(.screensaver-page-open) .screensaver-backdrop {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          height:100dvh !important;
          max-height:100dvh !important;
          border-radius:0 !important;
          z-index:2147483201 !important;
          padding:clamp(20px, 3vw, 42px) !important;
        }
        :host(.compact-popup-open) {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          z-index:2147483000 !important;
          display:block !important;
          pointer-events:auto !important;
          contain:none !important;
          overflow:visible !important;
          background:rgba(0,0,0,.08);
        }
        :host(.compact-window-popup-open) {
          position:fixed !important;
          top:max(76px, calc(env(safe-area-inset-top, 0px) + 18px)) !important;
          left:50% !important;
          right:auto !important;
          bottom:auto !important;
          transform:translateX(-50%) !important;
          width:min(var(--homeii-compact-window-width, 720px), calc(100vw - 32px)) !important;
          height:min(var(--homeii-compact-window-height, 760px), calc(100dvh - 112px)) !important;
          min-width:0 !important;
          min-height:360px !important;
          max-width:calc(100vw - 32px) !important;
          max-height:calc(100dvh - 112px) !important;
          z-index:2147482500 !important;
          display:block !important;
          pointer-events:auto !important;
          contain:none !important;
          overflow:visible !important;
          box-sizing:border-box !important;
          background:transparent !important;
        }
        :host(.compact-window-popup-open) ha-card {
          width:100% !important;
          height:100% !important;
          min-height:100% !important;
          max-height:100% !important;
          overflow:visible !important;
          pointer-events:auto !important;
          border-radius:32px !important;
          box-shadow:0 34px 90px rgba(0,0,0,.44), 0 0 0 1px rgba(255,255,255,.12) !important;
        }
        :host(.compact-window-popup-open) .card.compact-expanded,
        :host(.compact-window-popup-open) .card.compact-mode.compact-expanded {
          position:relative !important;
          inset:auto !important;
          width:100% !important;
          height:100% !important;
          min-width:0 !important;
          min-height:100% !important;
          max-width:100% !important;
          max-height:100% !important;
          pointer-events:auto !important;
          touch-action:auto !important;
          border-radius:32px !important;
          overflow:hidden !important;
        }
        :host(.compact-window-popup-open) .card.compact-expanded .stage,
        :host(.compact-window-popup-open) .card.compact-mode.compact-expanded .stage {
          height:100% !important;
          max-height:100% !important;
          min-height:0 !important;
          pointer-events:auto !important;
        }
        :host(.compact-window-popup-open) .card.compact-expanded .menu-backdrop,
        :host(.compact-window-popup-open) .card.compact-expanded .screensaver-backdrop,
        :host(.compact-window-popup-open) .card.compact-mode.compact-expanded .menu-backdrop,
        :host(.compact-window-popup-open) .card.compact-mode.compact-expanded .screensaver-backdrop {
          position:absolute !important;
          inset:0 !important;
        }
        @media (max-width: 760px) {
          :host(.compact-window-popup-open) {
            top:max(72px, calc(env(safe-area-inset-top, 0px) + 14px)) !important;
            width:min(var(--homeii-compact-window-width, 560px), calc(100vw - 20px)) !important;
            height:min(var(--homeii-compact-window-height, 680px), calc(100dvh - 156px)) !important;
            min-height:min(340px, calc(100dvh - 156px)) !important;
            max-width:calc(100vw - 20px) !important;
            max-height:calc(100dvh - 156px) !important;
          }
          :host(.compact-window-popup-open) ha-card,
          :host(.compact-window-popup-open) .card.compact-expanded,
          :host(.compact-window-popup-open) .card.compact-mode.compact-expanded {
            border-radius:28px !important;
          }
        }
        :host(.compact-tile-open) {
          display:block !important;
          position:relative !important;
          height:var(--homeii-compact-tile-height, 504px) !important;
          min-height:var(--homeii-compact-tile-height, 336px) !important;
          max-height:var(--homeii-compact-tile-height, 504px) !important;
          overflow:hidden !important;
          z-index:auto !important;
        }
        :host(.compact-tile-open) ha-card {
          height:100% !important;
          min-height:var(--homeii-compact-tile-height, 336px) !important;
          max-height:var(--homeii-compact-tile-height, 504px) !important;
          overflow:hidden !important;
          border-radius:32px;
        }
        :host(.compact-tile-open.compact-menu-open) {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          z-index:2147483050 !important;
          overflow:visible !important;
          contain:none !important;
          pointer-events:auto !important;
          border-radius:0 !important;
        }
        :host(.compact-tile-open.compact-menu-open) ha-card,
        :host(.compact-tile-open.compact-menu-open) .card.compact-mode {
          width:100vw !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          overflow:visible !important;
          contain:none !important;
          border-radius:0 !important;
        }
        :host(.compact-tile-open.compact-menu-open) .card.compact-mode .stage {
          height:100dvh !important;
          min-height:0 !important;
          max-height:100dvh !important;
          overflow:visible !important;
        }
        :host(.compact-tile-open.compact-menu-open) .card.compact-mode.compact-collapsed > .stage {
          opacity:0 !important;
          pointer-events:none !important;
        }
        :host(.compact-tile-open.compact-menu-open) .card.compact-mode.compact-collapsed .compact-shell {
          display:none !important;
        }
        :host(.compact-tile-open.compact-menu-open) .menu-backdrop.open {
          position:fixed !important;
          inset:0 !important;
          z-index:2147483051 !important;
          display:flex !important;
          align-items:stretch !important;
          justify-content:center !important;
          padding:max(14px, env(safe-area-inset-top)) 14px max(14px, env(safe-area-inset-bottom)) !important;
          background:rgba(5,7,12,.76) !important;
          pointer-events:auto !important;
        }
        :host(.compact-tile-open.compact-menu-open) .menu-backdrop.open .menu-sheet {
          width:min(960px, calc(100vw - 28px)) !important;
          max-width:min(960px, calc(100vw - 28px)) !important;
          height:calc(100dvh - 28px) !important;
          max-height:calc(100dvh - 28px) !important;
          margin:0 auto !important;
          border-radius:28px !important;
        }
        :host(.compact-tile-open.compact-menu-open) .menu-body.sheet-players {
          justify-items:center !important;
          align-content:start !important;
          padding:clamp(14px, 3vw, 28px) clamp(14px, 4vw, 36px) max(28px, env(safe-area-inset-bottom)) !important;
        }
        :host(.compact-tile-open.compact-menu-open) .menu-body.sheet-players .players-premium-grid {
          width:min(100%, 900px) !important;
          grid-template-columns:repeat(auto-fit, minmax(min(100%, 260px), 1fr)) !important;
          gap:14px !important;
        }
        @media (max-width: 640px) {
          :host(.compact-tile-open.compact-menu-open) .menu-backdrop.open {
            padding:0 !important;
          }
          :host(.compact-tile-open.compact-menu-open) .menu-backdrop.open .menu-sheet {
            width:100vw !important;
            max-width:100vw !important;
            height:100dvh !important;
            max-height:100dvh !important;
            border-radius:0 !important;
          }
        }
        :host(.volume-preset-open) {
          position:relative !important;
          z-index:2147483100 !important;
          overflow:visible !important;
          contain:none !important;
          isolation:isolate !important;
        }
        :host(.volume-preset-open) ha-card,
        :host(.volume-preset-open) .card.compact-mode {
          overflow:visible !important;
          contain:none !important;
        }
        :host(.volume-preset-open) .queue-action-backdrop.open {
          z-index:2147483101 !important;
        }
        :host(.volume-preset-open) .queue-action-sheet {
          z-index:2147483102 !important;
          position:relative;
        }
        :host(.compact-popup-open) ha-card {
          width:100vw !important;
          height:100dvh !important;
          min-height:100dvh !important;
          max-height:100dvh !important;
          overflow:visible !important;
          pointer-events:auto !important;
          border-radius:0 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          height:100dvh !important;
          min-width:0 !important;
          min-height:100dvh !important;
          max-width:none !important;
          max-height:100dvh !important;
          z-index:2147483001 !important;
          pointer-events:auto !important;
          touch-action:auto !important;
          border-radius:0 !important;
          box-shadow:0 34px 90px rgba(0,0,0,.46), 0 0 0 1px rgba(255,255,255,.1) !important;
          overflow:hidden !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .stage,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .stage {
          height:100% !important;
          max-height:100% !important;
          min-height:0 !important;
          pointer-events:auto !important;
        }
        :host(.compact-popup-open) .card.compact-expanded button,
        :host(.compact-popup-open) .card.compact-expanded input,
        :host(.compact-popup-open) .card.compact-expanded select,
        :host(.compact-popup-open) .card.compact-expanded textarea,
        :host(.compact-popup-open) .card.compact-expanded .progress,
        :host(.compact-popup-open) .card.compact-expanded [role="button"],
        :host(.compact-popup-open) .card.compact-mode.compact-expanded button,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded input,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded select,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded textarea,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .progress,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded [role="button"] {
          pointer-events:auto !important;
        }
        :host(.compact-popup-open) .card.compact-expanded button,
        :host(.compact-popup-open) .card.compact-expanded [role="button"],
        :host(.compact-popup-open) .card.compact-mode.compact-expanded button,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded [role="button"] {
          touch-action:manipulation !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop,
        :host(.compact-popup-open) .card.compact-expanded .screensaver-backdrop,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .screensaver-backdrop {
          position:absolute !important;
          inset:0 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open {
          z-index:64 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open .menu-sheet,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open .menu-sheet {
          position:relative;
          z-index:65 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open .menu-head,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open .menu-head {
          position:relative;
          z-index:66 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open,
        :host(.compact-popup-open) .card.compact-expanded .queue-action-backdrop.open,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .queue-action-backdrop.open,
        :host(.compact-popup-open) .card.compact-expanded .control-room-backdrop.open,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .control-room-backdrop.open,
        :host(.compact-popup-open) .card.compact-expanded .screensaver-backdrop.open,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .screensaver-backdrop.open {
          position:absolute !important;
          inset:0 !important;
          width:100% !important;
          height:100% !important;
          min-width:0 !important;
          min-height:100% !important;
          max-width:100% !important;
          max-height:100% !important;
          padding:0 !important;
          margin:0 !important;
          border-radius:0 !important;
          align-items:stretch !important;
          justify-content:center !important;
          overflow:hidden !important;
          z-index:90 !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open .menu-sheet,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open .menu-sheet,
        :host(.compact-popup-open) .card.compact-expanded .queue-action-backdrop.open .queue-action-sheet,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .queue-action-backdrop.open .queue-action-sheet,
        :host(.compact-popup-open) .card.compact-expanded .control-room-backdrop.open .control-room-shell,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .control-room-backdrop.open .control-room-shell {
          width:100% !important;
          max-width:100% !important;
          height:100% !important;
          max-height:100% !important;
          min-height:100% !important;
          margin:0 !important;
          border-radius:0 !important;
          box-shadow:none !important;
        }
        :host(.compact-popup-open) .card.compact-expanded .menu-backdrop.open .menu-body,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .menu-backdrop.open .menu-body,
        :host(.compact-popup-open) .card.compact-expanded .queue-action-backdrop.open .queue-action-sheet,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded .queue-action-backdrop.open .queue-action-sheet {
          overflow:auto !important;
          -webkit-overflow-scrolling:touch;
        }
        @media (max-width: 760px) {
          :host(.compact-popup-open) .card.compact-expanded,
          :host(.compact-popup-open) .card.compact-mode.compact-expanded {
            inset:0 !important;
            border-radius:0 !important;
          }
        }
        .card.compact-mode.compact-collapsed {
          height:100%;
          min-height:100%;
          max-height:100%;
          min-width:0;
          border:none;
          border-radius:0;
          background:transparent !important;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          overflow:hidden;
        }
        .card.compact-mode.compact-collapsed > .bg,
        .card.compact-mode.compact-collapsed > .shade,
        .card.compact-mode.compact-collapsed > .glow,
        .card.compact-mode.compact-collapsed > .compact-collapse-fab,
        .card.compact-mode.compact-collapsed > .top-settings-fab,
        .card.compact-mode.compact-collapsed > .home-shortcut-fab {
          display:none !important;
        }
        .card.compact-mode.compact-collapsed .stage {
          display:flex;
          justify-content:center;
          width:100%;
          height:100%;
          min-height:0;
          padding:max(8px, env(safe-area-inset-top)) max(8px, env(safe-area-inset-right)) max(8px, env(safe-area-inset-bottom)) max(8px, env(safe-area-inset-left));
        }
        .compact-shell {
          position:relative;
          display:grid;
          gap:16px;
          width:100%;
          height:100%;
          min-width:0;
          max-height:100%;
          overflow:hidden;
          padding:18px 20px;
          border-radius:32px;
          border:1px solid rgba(255,255,255,.14);
          background:
            linear-gradient(180deg, rgba(14,18,28,.78), rgba(10,12,20,.94)),
            rgba(9,12,18,.82);
          box-shadow:
            0 24px 54px rgba(0,0,0,.28),
            inset 0 1px 0 rgba(255,255,255,.06);
          backdrop-filter:blur(28px);
          -webkit-backdrop-filter:blur(28px);
        }
        .compact-backdrop-art,
        .compact-backdrop-shade,
        .compact-sheen {
          position:absolute;
          inset:0;
          pointer-events:none;
        }
        .compact-backdrop-art {
          inset:-24%;
          background-position:center;
          background-size:cover;
          filter:blur(52px) saturate(1.2) brightness(1.04);
          opacity:.58;
          transform:scale(1.18);
        }
        .compact-backdrop-shade {
          background:
            radial-gradient(circle at 16% 18%, rgba(255,210,126,.2), transparent 26%),
            linear-gradient(135deg, rgba(255,255,255,.08), transparent 34%),
            linear-gradient(180deg, rgba(5,8,14,.12), rgba(5,8,14,.6) 42%, rgba(5,8,14,.82));
        }
        .compact-sheen {
          background:
            radial-gradient(circle at 16% 22%, rgba(241,186,83,.22), transparent 24%),
            radial-gradient(circle at 82% 18%, rgba(255,255,255,.09), transparent 16%),
            radial-gradient(circle at 56% 100%, rgba(225,163,49,.15), transparent 28%);
        }
        .compact-content {
          position:relative;
          z-index:1;
          display:grid;
          gap:18px;
          width:min(100%, 720px);
          margin-inline:auto;
        }
        .compact-header {
          position:relative;
          display:flex;
          justify-content:center;
          align-items:center;
          min-height:38px;
        }
        .compact-player-chip {
          display:inline-flex;
          align-items:center;
          justify-content:center;
          min-width:0;
          max-width:min(100%, 220px);
          min-height:auto;
          padding:0;
          border:none;
          border-radius:0;
          background:transparent;
          color:#f9f5eb;
          cursor:pointer;
          box-shadow:none;
          backdrop-filter:none;
          text-align:center;
        }
        .compact-player-chip.is-playing {
          box-shadow:none;
        }
        .compact-player-copy {
          min-width:0;
          display:block;
        }
        .compact-player-label {
          display:block;
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:12px;
          font-weight:800;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
          letter-spacing:.01em;
        }
        .compact-player-tags {
          display:none;
        }
        .compact-expand-btn,
        .compact-collapse-fab {
          width:38px;
          height:38px;
          border:none;
          border-radius:12px;
          display:grid;
          place-items:center;
          background:transparent;
          color:#f5efe2;
          cursor:pointer;
          box-shadow:none;
          backdrop-filter:none;
        }
        .compact-expand-ref {
          position:absolute;
          top:0;
          right:0;
          border:1px solid rgba(255,255,255,.2);
          border-radius:15px;
          background:
            linear-gradient(145deg, rgba(255,255,255,.18), rgba(255,255,255,.07)),
            rgba(12,16,24,.24);
          box-shadow:
            0 14px 30px rgba(0,0,0,.2),
            inset 0 1px 0 rgba(255,255,255,.18);
          backdrop-filter:blur(16px);
          -webkit-backdrop-filter:blur(16px);
          transition:transform .16s ease, box-shadow .18s ease, background-color .18s ease;
        }
        .card.rtl .compact-expand-ref {
          right:0;
          left:auto;
        }
        .compact-expand-ref:hover {
          transform:translateY(-1px);
          box-shadow:
            0 18px 34px rgba(0,0,0,.24),
            0 0 0 1px color-mix(in srgb, var(--ma-accent) 18%, transparent),
            inset 0 1px 0 rgba(255,255,255,.22);
        }
        .compact-expand-ref:active {
          transform:translateY(1px) scale(.98);
        }
        .compact-expand-ref svg {
          width:24px;
          height:24px;
          opacity:.92;
          filter:drop-shadow(0 2px 5px rgba(0,0,0,.22));
        }
        .compact-collapse-fab {
          position:absolute;
          top:16px;
          z-index:9;
        }
        .compact-collapse-fab,
        .compact-collapse-fab.ltr,
        .compact-collapse-fab.rtl {
          left:16px;
          right:auto;
          inset-inline-start:auto;
          inset-inline-end:auto;
        }
        .card.compact-mode:not(.compact-collapsed) > .top-settings-fab.ltr { inset-inline-start:16px; inset-inline-end:auto; }
        .card.compact-mode:not(.compact-collapsed) > .top-settings-fab.rtl { inset-inline-end:16px; inset-inline-start:auto; }
        .card.control-room-open > .compact-collapse-fab,
        .card.control-room-open > .top-settings-fab,
        .card.control-room-open > .home-shortcut-fab {
          opacity:0 !important;
          pointer-events:none !important;
        }
        .compact-stage {
          display:grid;
          grid-template-columns:132px minmax(0, 1fr);
          gap:16px;
          align-items:center;
          min-width:0;
          width:100%;
          direction:ltr;
        }
        .compact-cover-wrap {
          position:relative;
          display:grid;
          place-items:center;
          justify-self:start;
          width:132px;
          height:132px;
          min-height:0;
          align-self:center;
        }
        .compact-brand-signature {
          position:absolute;
          inset-block-start:-34px;
          left:50%;
          width:142px;
          max-width:150%;
          display:grid;
          place-items:center;
          pointer-events:none;
          z-index:4;
          color:rgba(250,247,238,.72);
          opacity:.98;
          mix-blend-mode:normal;
          transform:translateX(-50%);
          filter:drop-shadow(0 10px 18px rgba(0,0,0,.28)) saturate(.96);
        }
        .compact-brand-logo {
          width:100%;
          height:auto;
          display:block;
          overflow:visible;
        }
        .compact-brand-logo .tablet-brand-home {
          font-size:19px;
          letter-spacing:6.5px;
        }
        .compact-brand-logo .tablet-brand-flow {
          font-size:9.5px;
          letter-spacing:4.5px;
          opacity:.64;
        }
        .compact-brand-logo .tablet-brand-wave {
          opacity:.32;
        }
        .compact-brand-logo .tablet-brand-rule {
          opacity:.2;
        }
        .theme-light .compact-brand-signature {
          color:rgba(24,30,42,.42);
          opacity:.9;
          mix-blend-mode:normal;
          filter:drop-shadow(0 8px 16px rgba(255,255,255,.28)) saturate(.82);
        }
        .art-source-badges {
          position:absolute;
          inset-block-start:10px;
          inset-inline-start:10px;
          z-index:7;
          display:flex;
          align-items:center;
          gap:6px;
          max-width:calc(100% - 20px);
          pointer-events:none;
        }
        .compact-source-badges {
          inset-block-start:6px;
          inset-inline-start:6px;
          max-width:calc(100% - 12px);
        }
        .source-badge {
          min-height:22px;
          padding:0 8px;
          border-radius:999px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          max-width:100%;
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:10px;
          font-weight:900;
          letter-spacing:.01em;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
          color:#f7fbff;
          background:rgba(10,14,22,.66);
          border:1px solid rgba(255,255,255,.14);
          box-shadow:0 10px 24px rgba(0,0,0,.18);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
        }
        .source-badge.quality {
          color:rgba(255,255,255,.92);
          background:rgba(255,255,255,.12);
        }
        .source-badge.provider-spotify { color:#1ed760; }
        .source-badge.provider-tidal { color:#f8f8f8; }
        .source-badge.provider-youtube { color:#ff6b6b; }
        .source-badge.provider-apple { color:#ffd2df; }
        .source-badge.provider-qobuz { color:#8cd0ff; }
        .source-badge.provider-deezer { color:#ffb36a; }
        .source-badge.provider-library,
        .source-badge.provider-radio { color:#ffe29a; }
        .card.hotel-mode .art-source-badges,
        .card.hotel-mode .media-more-btn,
        .card.hotel-mode .media-layout-btn,
        .card.hotel-mode .queue-head-transfer-btn,
        .card.hotel-mode [data-media-surprise],
        .card.hotel-mode [data-menu-action="connect_this_device"],
        .card.hotel-mode [data-menu-action="disconnect_this_device"] {
          display:none !important;
        }
        .card.hotel-mode .bg {
          background:
            radial-gradient(circle at 50% 18%, color-mix(in srgb, var(--ma-accent) 22%, transparent), transparent 42%),
            linear-gradient(180deg, rgba(12,15,22,.98), rgba(8,10,15,.99)) !important;
          filter:none !important;
          opacity:1 !important;
          transform:none !important;
        }
        .card.hotel-mode .compact-backdrop-art,
        .card.hotel-mode .compact-cover-echo,
        .card.hotel-mode .hero-aura,
        .card.hotel-mode .art-aura {
          background-image:none !important;
        }
        .card.hotel-mode .menu-backdrop::before,
        .card.hotel-mode .menu-sheet::before {
          background-image:none !important;
          opacity:0 !important;
        }
        .card.hotel-mode > .mobile-brand-signature,
        .card.hotel-mode > .tablet-brand-watermark {
          display:flex !important;
          opacity:.88;
        }
        .card.hotel-mode #mobileQuickSearchBtn {
          width:64px;
          min-width:64px;
          height:64px;
          border-radius:22px;
          background:
            radial-gradient(circle at 50% 22%, rgba(255,255,255,.24), transparent 58%),
            linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 38%, rgba(255,255,255,.12)), rgba(255,255,255,.08));
          border-color:color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.16));
        }
        .theme-light .source-badge {
          color:#1f2633;
          background:rgba(255,255,255,.84);
          border-color:rgba(143,159,181,.18);
          box-shadow:0 10px 22px rgba(95,112,136,.14);
        }
        .theme-light .source-badge.quality {
          color:#3b4a61;
          background:rgba(240,245,252,.88);
        }
        .compact-cover-echo {
          position:absolute;
          inset:-18px;
          border-radius:30px;
          pointer-events:none;
          background-position:center;
          background-size:cover;
          filter:blur(28px) saturate(1.12);
          opacity:.42;
          transform:scale(1.06);
        }
        .compact-cover {
          position:relative;
          z-index:1;
          width:124px;
          height:124px;
          border:none;
          border-radius:32px;
          background:rgba(255,255,255,.05);
          box-shadow:
            0 14px 28px rgba(0,0,0,.22),
            inset 0 1px 0 rgba(255,255,255,.08),
            0 0 0 1px rgba(255,255,255,.07);
          cursor:pointer;
          overflow:hidden;
        }
        .compact-cover.placeholder {
          display:grid;
          place-items:center;
        }
        .compact-cover-image {
          width:100%;
          height:100%;
          display:block;
          object-fit:contain;
          object-position:center;
          border-radius:inherit;
          padding:2px;
          opacity:.96;
        }
        .compact-cover-image[data-homeii-art-ready="0"] {
          opacity:.96;
        }
        .compact-cover-image[data-homeii-art-ready="1"] {
          transition:opacity .12s ease-out;
        }
        .compact-cover-placeholder {
          position:absolute;
          inset:0;
          display:grid;
          place-items:center;
          color:rgba(255,255,255,.7);
          font-size:22px;
          pointer-events:none;
          opacity:0;
        }
        .compact-cover.placeholder .compact-cover-placeholder {
          opacity:1;
        }
        .compact-cover.placeholder .compact-cover-image {
          display:none;
        }
        .theme-light .compact-cover {
          background:rgba(255,255,255,.14);
        }
        .compact-main {
          min-width:0;
          display:grid;
          gap:4px;
          align-content:start;
          text-align:start;
          padding-top:2px;
        }
        .compact-copy {
          min-width:0;
          display:grid;
          gap:5px;
        }
        .compact-title {
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:clamp(15px, 2.4vw, 19px);
          font-weight:700;
          line-height:1.08;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
          overflow:hidden;
          text-wrap:balance;
          color:#fffdf7;
        }
        .compact-sub {
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:12px;
          font-weight:500;
          line-height:1.3;
          color:rgba(255,255,255,.68);
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .up-next-inline {
          min-width:0;
          display:inline-flex;
          align-items:center;
          gap:8px;
          padding:0;
          border:none;
          background:transparent;
          color:inherit;
          text-align:inherit;
          cursor:pointer;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          transition:transform .16s ease, opacity .18s ease;
        }
        .up-next-inline[hidden] {
          display:none !important;
        }
        .up-next-inline:hover {
          transform:translateY(-1px);
          opacity:.92;
        }
        .up-next-inline:active {
          transform:translateY(1px) scale(.99);
        }
        .card.rtl .up-next-inline {
          flex-direction:row-reverse;
        }
        .up-next-art {
          width:22px;
          height:22px;
          min-width:22px;
          border-radius:7px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08);
        }
        .up-next-art img {
          width:100%;
          height:100%;
          display:block;
          object-fit:cover;
        }
        .up-next-art-fallback {
          width:14px;
          height:14px;
          display:grid;
          place-items:center;
          color:rgba(255,255,255,.72);
        }
        .up-next-art-fallback .ui-ic {
          width:14px;
          height:14px;
        }
        .up-next-line {
          min-width:0;
          display:flex;
          align-items:center;
          gap:5px;
          line-height:1;
        }
        .card.rtl .up-next-line {
          flex-direction:row-reverse;
        }
        .up-next-prefix {
          flex:0 0 auto;
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:11px;
          font-weight:800;
          color:color-mix(in srgb, var(--ma-accent) 62%, rgba(255,255,255,.66));
        }
        .up-next-title {
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:12px;
          font-weight:700;
          line-height:1.15;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .compact-up-next {
          margin-top:2px;
          justify-content:flex-start;
          text-align:start;
          max-width:min(320px, 100%);
        }
        .card.rtl .compact-up-next {
          direction:rtl;
          flex-direction:row;
          justify-content:flex-start;
          text-align:right;
        }
        .compact-up-next .up-next-line {
          min-width:0;
          max-width:100%;
        }
        .night-quick-row {
          display:flex;
          flex-wrap:wrap;
          gap:8px;
          align-items:center;
          margin-top:10px;
          justify-content:center;
        }
        .night-quick-row.auto-mode {
          justify-content:center;
        }
        .night-quick-row.on-mode {
          justify-content:center;
        }
        .night-quick-btn {
          min-height:34px;
          padding:0 12px;
          border-radius:999px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(255,255,255,.08);
          color:inherit;
          display:inline-flex;
          align-items:center;
          gap:8px;
          font:inherit;
          font-size:12px;
          font-weight:850;
          letter-spacing:.01em;
          cursor:pointer;
          box-shadow:0 10px 24px rgba(0,0,0,.12);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          transition:transform .16s ease, border-color .18s ease, background-color .18s ease, box-shadow .18s ease;
        }
        .night-quick-btn .ui-ic {
          width:16px;
          height:16px;
        }
        .night-quick-btn.icon-only {
          width:34px;
          min-width:34px;
          height:34px;
          min-height:34px;
          padding:0;
          justify-content:center;
          border-radius:999px;
        }
        .night-quick-btn.icon-only .ui-ic {
          width:17px;
          height:17px;
        }
        .night-quick-btn.soft {
          background:rgba(255,255,255,.05);
        }
        .night-quick-btn.active {
          border-color:color-mix(in srgb, var(--ma-accent) 32%, rgba(171,185,255,.46));
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 16%, rgba(111,126,255,.16)), rgba(255,255,255,.08));
          box-shadow:0 12px 28px color-mix(in srgb, var(--ma-accent) 12%, rgba(7,10,20,.22));
        }
        .theme-light .night-quick-btn {
          background:rgba(255,255,255,.72);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 10px 24px rgba(110,127,153,.12);
        }
        .theme-light .night-quick-btn.soft {
          background:rgba(255,255,255,.56);
        }
        .theme-light .night-quick-btn.active {
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 20%, white 80%), rgba(255,255,255,.88));
        }
        .compact-copy .night-quick-row {
          margin-top:8px;
        }
        .compact-copy .night-quick-btn {
          min-height:30px;
          padding:0 10px;
          font-size:11px;
          box-shadow:none;
          background:rgba(255,255,255,.06);
        }
        .compact-copy .night-quick-btn.icon-only {
          width:30px;
          min-width:30px;
          height:30px;
          min-height:30px;
          padding:0;
        }
        .compact-controls {
          display:flex;
          align-items:center;
          justify-content:center;
          gap:22px;
          min-width:0;
          direction:ltr;
          margin-top:2px;
        }
        .compact-controls button {
          color:#fffdf8;
        }
        .compact-control-btn {
          width:48px !important;
          height:48px !important;
          border-radius:50% !important;
          background:transparent !important;
          border:none !important;
          box-shadow:none !important;
          backdrop-filter:none !important;
        }
        .compact-control-btn .ui-ic {
          width:24px;
          height:24px;
        }
        .compact-main-btn {
          width:66px !important;
          height:66px !important;
          border-radius:50% !important;
          background:rgba(255,255,255,.09) !important;
          border:1px solid rgba(255,255,255,.1) !important;
          box-shadow:
            0 10px 22px rgba(0,0,0,.15),
            inset 0 1px 0 rgba(255,255,255,.07) !important;
        }
        .compact-main-btn .ui-ic {
          width:28px;
          height:28px;
        }
        .compact-progress-row {
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          align-items:center;
          gap:10px;
          direction:ltr;
          width:min(100%, 92%);
          margin:2px auto 0;
        }
        .compact-progress-time {
          font-size:11px;
          font-weight:700;
          color:rgba(255,255,255,.6);
          font-variant-numeric:tabular-nums;
        }
        .compact-progress-track {
          min-width:0;
          height:5px;
          border-radius:999px;
          background:rgba(255,255,255,.18);
          box-shadow:inset 0 1px 2px rgba(0,0,0,.12);
          overflow:hidden;
        }
        .compact-progress-track .progress-fill {
          background:linear-gradient(90deg, rgba(250,226,157,.98), rgba(255,255,255,.9));
          opacity:.98;
        }
        .compact-volume-inline {
          width:min(100%, 300px);
          margin-inline:auto;
          padding:0;
          border-radius:0;
          background:transparent;
          border:none;
          backdrop-filter:none;
          display:grid !important;
          grid-template-columns:auto minmax(0,1fr) auto;
          gap:10px;
          align-items:center;
          box-shadow:none;
        }
        .compact-volume-inline.has-volume-step-buttons {
          grid-template-columns:auto auto minmax(0,1fr) auto auto;
          gap:8px;
        }
        .compact-volume-inline .volume-step-btn {
          width:32px;
          min-width:32px;
          height:32px;
          color:rgba(255,248,232,.9);
        }
        .compact-volume-track {
          min-width:0;
        }
        .compact-volume-slider {
          width:100%;
          height:18px;
          appearance:none;
          direction:ltr;
          background:transparent;
          outline:none;
        }
        .compact-volume-slider::-webkit-slider-runnable-track {
          height:4px;
          border-radius:999px;
          background:linear-gradient(to right,var(--ma-accent) 0%,var(--ma-accent) var(--vol-pct,50%),rgba(255,255,255,.22) var(--vol-pct,50%),rgba(255,255,255,.22) 100%);
        }
        .compact-volume-slider::-webkit-slider-thumb {
          appearance:none;
          width:14px;
          height:14px;
          margin-top:-5px;
          border-radius:50%;
          background:#fff7dc;
          border:none;
          box-shadow:0 2px 8px rgba(0,0,0,.18);
        }
        .compact-volume-slider::-moz-range-track {
          height:4px;
          border-radius:999px;
          background:rgba(255,255,255,.22);
        }
        .compact-volume-slider::-moz-range-progress {
          height:4px;
          border-radius:999px;
          background:var(--ma-accent);
        }
        .compact-volume-slider::-moz-range-thumb {
          width:14px;
          height:14px;
          border-radius:50%;
          background:#fff7dc;
          border:none;
          box-shadow:0 2px 8px rgba(0,0,0,.18);
        }
        .compact-volume-value {
          min-width:40px;
          font-weight:700;
          font-size:11px;
          text-align:end;
          color:rgba(255,255,255,.62);
        }
        .compact-mute-btn {
          width:36px !important;
          height:36px !important;
          border-radius:50% !important;
          background:
            linear-gradient(145deg, rgba(255,255,255,.14), rgba(255,255,255,.055)) !important;
          border:1px solid rgba(255,255,255,.14) !important;
          box-shadow:
            0 10px 20px rgba(0,0,0,.14),
            inset 0 1px 0 rgba(255,255,255,.13) !important;
          padding:0 !important;
          color:rgba(255,248,232,.94) !important;
        }
        .compact-mute-btn .ui-ic {
          width:28px;
          height:28px;
        }
        .compact-mute-btn.muted {
          color:#ffcfbd !important;
        }
        .theme-light .compact-shell {
          border-color:rgba(255,255,255,.28);
          background:
            linear-gradient(180deg, rgba(255,255,255,.28), rgba(255,255,255,.16)),
            rgba(244,248,252,.18);
          box-shadow:
            0 24px 52px rgba(101,116,143,.16),
            inset 0 1px 0 rgba(255,255,255,.38);
        }
        .theme-light .compact-backdrop-art {
          opacity:.58;
          filter:blur(58px) saturate(1.04) brightness(1.02);
        }
        .theme-light .compact-backdrop-shade {
          background:
            radial-gradient(circle at 18% 22%, rgba(255,236,192,.22), transparent 24%),
            linear-gradient(180deg, rgba(248,250,255,.18), rgba(235,241,248,.42)),
            rgba(232,238,246,.34);
        }
        .theme-light .compact-sheen {
          background:
            radial-gradient(circle at 16% 18%, rgba(255,220,160,.16), transparent 24%),
            radial-gradient(circle at 72% 56%, rgba(255,255,255,.2), transparent 30%),
            radial-gradient(circle at 50% 100%, rgba(216,186,144,.14), transparent 24%);
        }
        .theme-light .compact-player-chip,
        .theme-light .compact-volume-inline {
          color:#243144;
          background:transparent;
          border-color:rgba(153,167,188,.24);
        }
        .theme-light .compact-progress-time,
        .theme-light .compact-volume-value,
        .theme-light .compact-sub {
          color:rgba(43,57,76,.78);
        }
        .theme-light .compact-title {
          color:#1d2938;
        }
        .theme-light .compact-controls button,
        .theme-light .compact-mute-btn {
          color:#243144;
        }
        .theme-light .compact-expand-ref,
        .theme-light .compact-mute-btn {
          background:
            linear-gradient(145deg, rgba(255,255,255,.76), rgba(255,255,255,.38)) !important;
          border-color:rgba(142,157,180,.2) !important;
          box-shadow:
            0 12px 24px rgba(104,120,145,.16),
            inset 0 1px 0 rgba(255,255,255,.72) !important;
        }
        .card.rtl .compact-main {
          direction:rtl;
          text-align:right;
        }
        @media (max-width: 760px) {
          .compact-shell {
            gap:16px;
            padding:16px;
          }
          .compact-stage {
            grid-template-columns:116px minmax(0,1fr);
            gap:14px;
          }
          .compact-cover-wrap {
            width:116px;
            height:116px;
          }
          .compact-cover {
            width:108px;
            height:108px;
            border-radius:28px;
          }
          .compact-title {
            font-size:clamp(15px, 3.2vw, 18px);
          }
        }
        @media (max-width: 520px) {
          .compact-shell {
            border-radius:28px;
            padding:16px;
          }
          .compact-content {
            gap:16px;
          }
          .compact-header {
            min-height:36px;
          }
          .compact-stage {
            grid-template-columns:110px minmax(0,1fr);
            gap:12px;
            align-items:center;
          }
          .compact-cover-wrap {
            width:110px;
            height:110px;
          }
          .compact-cover {
            width:104px;
            height:104px;
            border-radius:28px;
          }
          .compact-main {
            gap:5px;
          }
          .compact-title {
            font-size:clamp(14px, 4.1vw, 17px);
          }
          .compact-sub {
            font-size:calc(11px * var(--v2-font-scale));
          }
          .compact-controls {
            gap:22px;
          }
          .compact-control-btn {
            width:44px !important;
            height:44px !important;
          }
          .compact-control-btn .ui-ic { width:22px; height:22px; }
          .compact-main-btn {
            width:58px !important;
            height:58px !important;
          }
          .compact-main-btn .ui-ic { width:25px; height:25px; }
          .compact-progress-row {
            gap:7px;
            width:100%;
          }
          .compact-progress-time {
            font-size:10px;
          }
          .compact-volume-inline {
            width:min(100%, 260px);
          }
        }
        .card.compact-mode.compact-mini-widget .stage {
          padding:max(6px, env(safe-area-inset-top)) max(6px, env(safe-area-inset-right)) max(6px, env(safe-area-inset-bottom)) max(6px, env(safe-area-inset-left)) !important;
        }
        .card.compact-mode.compact-mini-widget .compact-shell {
          gap:0;
          padding:14px 14px 15px;
          border-radius:24px;
          min-height:0;
          position:relative;
        }
        .card.compact-mode.compact-mini-widget .compact-content {
          width:100%;
          max-width:none;
          height:100%;
          min-height:0;
          align-content:center;
          gap:10px 12px;
          grid-template-columns:82px minmax(0,1fr) 34px;
          grid-template-rows:34px minmax(58px, auto) 46px 42px;
          grid-template-areas:
            ". player ."
            "cover copy ."
            "cover controls controls"
            "volume volume volume";
          align-items:center;
        }
        .card.compact-mode.compact-mini-widget .compact-header,
        .card.compact-mode.compact-mini-widget .compact-stage {
          display:contents;
        }
        .card.compact-mode.compact-mini-widget .compact-brand-signature,
        .card.compact-mode.compact-mini-widget .compact-source-badges,
        .card.compact-mode.compact-mini-widget .compact-cover-echo,
        .card.compact-mode.compact-mini-widget .compact-progress-row,
        .card.compact-mode.compact-mini-widget .compact-up-next,
        .card.compact-mode.compact-mini-widget .night-quick-row {
          display:none !important;
        }
        .card.compact-mode.compact-mini-widget .compact-player-chip {
          grid-area:player;
          display:inline-flex !important;
          justify-self:center;
          align-self:center;
          width:auto;
          max-width:min(230px, calc(100% - 44px));
          min-height:30px !important;
          height:30px !important;
          padding:0 14px !important;
          border:1px solid rgba(255,255,255,.16) !important;
          border-radius:999px !important;
          background:rgba(18,22,31,.46) !important;
          color:rgba(255,255,255,.9) !important;
          box-shadow:inset 0 1px 0 rgba(255,255,255,.12), 0 8px 20px rgba(0,0,0,.18) !important;
          backdrop-filter:blur(14px) !important;
          -webkit-backdrop-filter:blur(14px) !important;
          z-index:5;
        }
        .card.compact-mode.compact-mini-widget .compact-player-copy {
          min-width:0;
          display:block;
        }
        .card.compact-mode.compact-mini-widget .compact-player-label {
          display:block;
          max-width:100%;
          font-size:11px;
          line-height:1;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .card.compact-mode.compact-mini-widget .compact-expand-ref {
          position:absolute !important;
          top:9px;
          right:9px;
          width:30px !important;
          min-width:30px !important;
          height:30px !important;
          min-height:30px !important;
          border-radius:10px;
          z-index:6;
          padding:0 !important;
        }
        .card.compact-mode.compact-mini-widget .compact-expand-ref svg {
          width:15px;
          height:15px;
        }
        .card.compact-mode.compact-mini-widget .compact-cover-wrap {
          grid-area:cover;
          width:82px;
          height:82px;
          justify-self:start;
          align-self:center;
        }
        .card.compact-mode.compact-mini-widget .compact-cover {
          width:78px !important;
          height:78px !important;
          border-radius:18px;
        }
        .card.compact-mode.compact-mini-widget .compact-cover-image {
          object-fit:contain;
          padding:0;
        }
        .card.compact-mode.compact-mini-widget .compact-main {
          grid-area:copy;
          padding:0;
          align-self:center;
          min-width:0;
          padding-inline-end:6px;
        }
        .card.compact-mode.compact-mini-widget .compact-copy {
          gap:2px;
        }
        .card.compact-mode.compact-mini-widget .compact-title {
          font-size:clamp(15px, 3.8vw, 18px);
          line-height:1.12;
          -webkit-line-clamp:1;
          text-wrap:normal;
        }
        .card.compact-mode.compact-mini-widget .compact-sub {
          font-size:11.5px;
          line-height:1.2;
        }
        .card.compact-mode.compact-mini-widget .compact-controls {
          grid-area:controls;
          justify-content:center;
          justify-self:stretch;
          gap:10px;
          margin:0;
        }
        .card.compact-mode.compact-mini-widget .compact-control-btn {
          width:34px !important;
          min-width:34px !important;
          height:34px !important;
          min-height:34px !important;
        }
        .card.compact-mode.compact-mini-widget .compact-control-btn .ui-ic {
          width:17px;
          height:17px;
        }
        .card.compact-mode.compact-mini-widget .compact-main-btn {
          width:46px !important;
          min-width:46px !important;
          height:46px !important;
          min-height:46px !important;
          border-width:1px !important;
        }
        .card.compact-mode.compact-mini-widget .compact-main-btn .ui-ic {
          width:20px;
          height:20px;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline {
          grid-area:volume;
          width:100%;
          max-width:none;
          justify-self:stretch;
          margin:0;
          gap:8px;
          min-height:40px;
          grid-template-columns:auto minmax(118px,1fr) auto;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline.has-volume-step-buttons {
          grid-template-columns:auto auto minmax(118px,1fr) auto auto;
          gap:8px;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-track {
          display:block !important;
          min-width:118px;
          order:3;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-slider {
          height:20px;
        }
        .card.compact-mode.compact-mini-widget .compact-mute-btn {
          width:34px !important;
          min-width:34px !important;
          height:34px !important;
          min-height:34px !important;
          order:1;
        }
        .card.compact-mode.compact-mini-widget .compact-mute-btn .ui-ic {
          width:18px;
          height:18px;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline .volume-step-btn {
          width:34px;
          min-width:34px;
          height:34px;
          min-height:34px;
          border-radius:999px;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline .volume-step-btn .ui-ic {
          width:16px;
          height:16px;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline .volume-step-minus {
          order:2;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-inline .volume-step-plus {
          order:4;
        }
        .card.compact-mode.compact-mini-widget .compact-volume-value {
          min-width:40px;
          font-size:11px;
          text-align:center;
          order:5;
        }
        @media (max-width: 360px) {
          .card.compact-mode.compact-mini-widget .compact-content {
            grid-template-columns:66px minmax(0,1fr) 30px;
            grid-template-rows:30px minmax(50px, auto) 40px 38px;
            grid-template-areas:
              ". player ."
              "cover copy ."
              "cover controls controls"
              "volume volume volume";
            gap:8px 8px;
          }
          .card.compact-mode.compact-mini-widget .compact-cover-wrap {
            width:66px;
            height:66px;
          }
          .card.compact-mode.compact-mini-widget .compact-cover {
            width:62px !important;
            height:62px !important;
            border-radius:15px;
          }
          .card.compact-mode.compact-mini-widget .compact-control-btn {
            width:30px !important;
            min-width:30px !important;
            height:30px !important;
            min-height:30px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-main-btn {
            width:40px !important;
            min-width:40px !important;
            height:40px !important;
            min-height:40px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-expand-ref {
            top:9px;
            right:9px;
            width:28px !important;
            min-width:28px !important;
            height:28px !important;
            min-height:28px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-player-chip {
            height:28px !important;
            min-height:28px !important;
            max-width:min(174px, calc(100% - 36px));
            padding:0 10px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-volume-inline {
            width:100%;
            gap:6px;
            min-height:36px;
            grid-template-columns:auto minmax(84px,1fr) auto;
          }
          .card.compact-mode.compact-mini-widget .compact-volume-inline.has-volume-step-buttons {
            grid-template-columns:auto auto minmax(84px,1fr) auto auto;
          }
          .card.compact-mode.compact-mini-widget .compact-volume-track {
            min-width:84px;
          }
          .card.compact-mode.compact-mini-widget .compact-volume-inline .volume-step-btn,
          .card.compact-mode.compact-mini-widget .compact-mute-btn {
            width:30px !important;
            min-width:30px !important;
            height:30px !important;
            min-height:30px !important;
          }
        }
        @media (max-width: 320px) {
          .card.compact-mode.compact-mini-widget .compact-shell {
            padding:11px 10px 12px;
          }
          .card.compact-mode.compact-mini-widget .compact-content {
            grid-template-columns:60px minmax(0,1fr) 28px;
            grid-template-rows:28px minmax(46px, auto) 38px 36px;
            grid-template-areas:
              ". player ."
              "cover copy ."
              "cover controls controls"
              "volume volume volume";
            gap:7px 6px;
          }
          .card.compact-mode.compact-mini-widget .compact-cover-wrap {
            width:60px;
            height:60px;
          }
          .card.compact-mode.compact-mini-widget .compact-cover {
            width:56px !important;
            height:56px !important;
            border-radius:14px;
          }
          .card.compact-mode.compact-mini-widget .compact-control-btn {
            width:28px !important;
            min-width:28px !important;
            height:28px !important;
            min-height:28px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-main-btn {
            width:38px !important;
            min-width:38px !important;
            height:38px !important;
            min-height:38px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-expand-ref {
            top:8px;
            right:8px;
            width:25px !important;
            min-width:25px !important;
            height:25px !important;
            min-height:25px !important;
          }
          .card.compact-mode.compact-mini-widget .compact-title {
            font-size:13px;
          }
          .card.compact-mode.compact-mini-widget .compact-sub,
          .card.compact-mode.compact-mini-widget .compact-volume-value {
            font-size:10px;
          }
        }
        .card.compact-mode {
          border-radius:22px;
        }
        .card.compact-mode .stage {
          gap:6px;
          padding:max(12px, env(safe-area-inset-top)) max(12px, env(safe-area-inset-right)) max(12px, env(safe-area-inset-bottom)) max(12px, env(safe-area-inset-left));
        }
        .card.compact-mode .center,
        .card.compact-mode .bottom,
        .card.compact-mode .tablet-main {
          gap:8px !important;
        }
        .card.compact-mode .hero-title {
          font-size:var(--flow-compact-title-size) !important;
          line-height:.92 !important;
        }
        .card.compact-mode .hero-sub {
          font-size:14px !important;
          line-height:1.25 !important;
        }
        .card.compact-mode .mobile-art-fab {
          width:40px !important;
          min-width:40px !important;
          height:40px !important;
        }
        .card.compact-mode .side-btn {
          width:54px !important;
          height:54px !important;
        }
        .card.compact-mode .main-btn {
          width:82px !important;
          height:82px !important;
        }
        .card.compact-mode .footer-btn {
          min-height:42px !important;
          padding:10px 12px !important;
        }
        .card.compact-mode .progress-line {
          margin-top:0 !important;
        }
        .card.compact-mode .progress-time {
          font-size:12px !important;
        }
        .card.compact-mode .notice {
          padding:8px 10px !important;
          min-height:0 !important;
        }
        .card.compact-mode .mobile-volume-inline {
          margin-top:2px;
        }
        .stage {
          position:relative; z-index:1; height:100%;
          width:100%;
          max-width:100%;
          min-width:0;
          display:grid; grid-template-rows:minmax(0,1fr) auto auto;
          gap:8px;
          padding:max(var(--flow-stage-pad-block), env(safe-area-inset-top)) max(var(--flow-stage-pad-inline), env(safe-area-inset-right)) max(var(--flow-stage-pad-block), env(safe-area-inset-bottom)) max(var(--flow-stage-pad-inline), env(safe-area-inset-left));
        }
        .card.mobile-layout-forced-full:not(.layout-tablet).height-short .stage,
        .card.mobile-layout-forced-full:not(.layout-tablet).height-tight .stage {
          overflow-y:auto;
          overscroll-behavior:contain;
          -webkit-overflow-scrolling:touch;
          grid-template-rows:auto auto auto;
          align-content:start;
        }
        .card.mobile-layout-forced-full:not(.layout-tablet).height-short .center,
        .card.mobile-layout-forced-full:not(.layout-tablet).height-tight .center {
          overflow:visible;
        }
        .card.layout-tablet .stage {
          gap:0;
          padding:max(var(--flow-stage-pad-block), env(safe-area-inset-top)) max(var(--flow-stage-pad-block), env(safe-area-inset-right)) max(var(--flow-stage-pad-block), env(safe-area-inset-bottom)) max(var(--flow-stage-pad-block), env(safe-area-inset-left));
          grid-template-rows:minmax(0,1fr);
          align-content:stretch;
          min-height:0;
        }
        .card.layout-tablet .tablet-shell {
          width:100%;
          height:100%;
          display:grid;
          grid-template-columns:var(--tablet-rail) minmax(0, 1fr);
          gap:var(--flow-shell-gap);
          align-items:stretch;
          min-height:0;
          overflow:hidden;
        }
        .card.layout-tablet .tablet-main {
          min-width:0;
          display:grid;
          grid-template-rows:minmax(0,1fr) auto;
          gap:12px;
          align-content:stretch;
          justify-items:center;
          min-height:0;
          height:100%;
        }
        .card.layout-tablet .tablet-rail {
          min-width:0;
          display:flex;
          flex-direction:column;
          align-items:stretch;
          gap:10px;
          min-height:0;
          height:100%;
          overflow:hidden;
          position:relative;
          z-index:7;
        }
        .card.layout-tablet.rtl .tablet-shell {
          grid-template-columns:minmax(0,1fr) var(--tablet-rail);
        }
        .hero-aura {
          position:absolute;
          top:-2%;
          left:50%;
          width:var(--flow-hero-aura-width);
          height:var(--flow-hero-aura-height);
          border-radius:999px;
          background:center / cover no-repeat;
          filter:blur(74px) saturate(1.08);
          opacity:.18;
          transform:translateX(-50%) scale(1.04);
          pointer-events:none;
          will-change:transform, opacity;
        }
        .theme-light .hero-aura {
          opacity:.12;
          filter:blur(82px) saturate(1.02) brightness(1.04);
        }
        .card.layout-tablet .hero-aura {
          left:32%;
          width:var(--flow-tablet-aura-width);
          height:var(--flow-tablet-aura-height);
          opacity:.14;
        }
        .theme-light.card.layout-tablet .hero-aura {
          opacity:.08;
        }
        .card.background-motion .hero-aura {
          animation:auraDrift var(--aura-motion-duration, 22s) ease-in-out infinite;
        }
        .card.dynamic-theme .hero-aura {
          filter:blur(80px) saturate(calc(1.02 + (.08 * var(--dynamic-theme-strength, .82))));
          opacity:calc(.14 + (.12 * var(--dynamic-theme-strength, .82)));
          mix-blend-mode:screen;
        }
        .theme-light.card.dynamic-theme .hero-aura {
          opacity:calc(.1 + (.08 * var(--dynamic-theme-strength, .82)));
        }
        .player-chip,.status-pill,.accent-row,.menu-sheet,.notice,.menu-item,.menu-list-item,.queue-row {
          background:rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.12); backdrop-filter:blur(16px); -webkit-backdrop-filter:blur(16px);
        }
        .theme-light .player-chip,.theme-light .status-pill,.theme-light .accent-row,.theme-light .menu-sheet,.theme-light .notice,.theme-light .menu-item,.theme-light .menu-list-item,.theme-light .queue-row {
          background:rgba(255,255,255,.62);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 14px 34px rgba(110,127,153,.12);
          color:#1f2633;
        }
        .player-chip {
          min-width:0; width:100%; max-width:100%; padding:2px 4px 0; border-radius:0; direction:inherit; cursor:pointer; margin-top:0;
          overflow:visible; position:relative; z-index:1; background:none; border:none; box-shadow:none; backdrop-filter:none; -webkit-backdrop-filter:none;
        }
        .player-chip-inner { display:grid; grid-template-columns:30px minmax(0,1fr); gap:10px; align-items:center; min-width:0; }
        .player-chip-ico {
          width:30px; height:30px; border-radius:10px; display:grid; place-items:center;
          background:linear-gradient(135deg, rgba(247,191,92,.18), rgba(245,166,35,.08));
          border:1px solid rgba(245,166,35,.18); color:#ffc96b;
        }
        .player-chip-ico .ui-ic { width:46%; height:46%; }
.player-kicker { font-size:calc(9px * var(--v2-font-scale)); letter-spacing:.12em; text-transform:uppercase; color:rgba(255,255,255,.56); margin-bottom:2px; }
.player-name { font-size:calc(15px * var(--v2-font-scale)); font-weight:900; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.player-sub { margin-top:1px; font-size:calc(11px * var(--v2-font-scale)); color:rgba(255,255,255,.72); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
        .theme-light .player-chip {
          background:none;
          border:none;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
        }
        .player-focus {
          display:inline-flex;
          align-items:center;
          justify-content:center;
          gap:10px;
          margin:8px auto 0;
          width:auto;
          max-width:min(calc(100% - 32px), 430px);
          min-height:34px;
          padding:6px 12px;
          border-radius:18px;
          background:linear-gradient(180deg, rgba(255,255,255,.12), rgba(255,255,255,.05));
          border:1px solid rgba(255,255,255,.14);
          color:inherit;
          font:inherit;
          cursor:pointer;
          position:relative;
          z-index:1;
          overflow:hidden;
          backdrop-filter:blur(22px);
          -webkit-backdrop-filter:blur(22px);
          box-sizing:border-box;
          box-shadow:0 14px 34px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.09);
          transition:transform .18s ease, background-color .18s ease, border-color .18s ease, box-shadow .18s ease, opacity .18s ease;
        }
        .player-focus-nav {
          width:min(100%, 292px);
          margin-inline:auto;
          display:grid;
          grid-template-columns:44px minmax(0,1fr) 44px;
          align-items:center;
          gap:8px;
        }
        .player-focus-nav .player-focus {
          width:100%;
          max-width:100%;
          margin-top:0;
        }
        .player-focus-nav-btn {
          width:44px;
          min-width:44px;
          height:44px;
          padding:0;
          border:1px solid rgba(255,255,255,.12);
          border-radius:999px;
          background:rgba(255,255,255,.08);
          color:rgba(255,255,255,.92);
          box-shadow:0 10px 24px rgba(0,0,0,.12);
          display:grid;
          place-items:center;
          cursor:pointer;
          transition:transform .16s ease, color .16s ease, opacity .16s ease, background-color .16s ease, border-color .16s ease;
        }
        .player-focus-nav-btn .ui-ic {
          width:18px;
          height:18px;
        }
        .player-focus-nav-btn:disabled {
          opacity:.18;
          cursor:default;
        }
        .theme-light .player-focus-nav-btn {
          color:rgba(38,52,70,.84);
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 10px 24px rgba(110,127,153,.12);
        }
        .player-focus-copy {
          min-width:0;
          display:flex;
          align-items:center;
          gap:8px;
          justify-content:center;
        }
        .player-focus-tags {
          display:flex;
          align-items:center;
          justify-content:center;
          flex-wrap:nowrap;
          gap:6px;
          min-height:0;
          min-width:0;
        }
        .player-focus-pill {
          min-height:20px;
          padding:0 8px;
          border-radius:999px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          gap:5px;
          font-size:10px;
          font-weight:950;
          line-height:1;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(255,255,255,.08);
          color:rgba(255,255,255,.92);
        }
        .player-focus-pill.playing {
          color:#18120a;
          border-color:color-mix(in srgb, var(--ma-accent) 30%, transparent);
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 70%, white 30%));
          box-shadow:0 10px 18px color-mix(in srgb, var(--ma-accent) 20%, transparent);
        }
        .player-focus-pill.night {
          border-color:rgba(168,182,255,.18);
          background:rgba(110,126,255,.12);
          color:#dce2ff;
        }
        .player-focus-pill.night.active {
          border-color:rgba(186,198,255,.28);
          background:linear-gradient(135deg, rgba(110,126,255,.26), rgba(141,154,255,.16));
          color:#f4f7ff;
          box-shadow:0 10px 18px rgba(69,82,145,.18);
        }
        .player-focus-pill .eq-icon {
          display:inline-flex !important;
          width:12px;
          height:10px;
          gap:2px;
        }
        .player-focus-pill .eq-icon span {
          width:2px;
        }
        .theme-light .player-focus-pill {
          color:#435066;
          border-color:rgba(147,161,183,.2);
          background:rgba(255,255,255,.74);
        }
        .theme-light .player-focus-pill.night {
          color:#506087;
          border-color:rgba(157,171,198,.24);
          background:rgba(235,240,255,.88);
        }
        .player-focus-art-wrap {
          display:flex;
          align-items:center;
          justify-content:center;
          width:auto;
          flex:none;
        }
        .player-focus-art {
          width:32px;
          height:32px;
          border-radius:999px;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08) center/cover no-repeat;
          border:1px solid rgba(255,255,255,.14);
          box-shadow:0 12px 26px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08);
          position:relative;
          overflow:hidden;
        }
        .player-focus-art.placeholder {
          background:
            radial-gradient(circle at 32% 24%, color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.14)), transparent 42%),
            linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.04));
        }
        .player-focus-art .ui-ic {
          width:54%;
          height:54%;
          opacity:.7;
          position:relative;
          z-index:1;
        }
        .player-focus-art::after {
          content:"";
          position:absolute;
          inset:0;
          border-radius:inherit;
          background:radial-gradient(circle at 32% 22%, rgba(255,255,255,.22), transparent 44%);
          pointer-events:none;
        }
        .player-focus::before {
          content:"";
          position:absolute;
          inset:-18%;
          background:radial-gradient(circle at 22% 18%, color-mix(in srgb, var(--ma-accent) 28%, transparent), transparent 34%);
          opacity:.55;
          pointer-events:none;
        }
        .player-focus::after {
          content:"";
          position:absolute;
          inset:0;
          border-radius:inherit;
          pointer-events:none;
          box-shadow:inset 0 0 0 1px rgba(255,255,255,.06);
        }
        .card.layout-tablet .player-focus {
          justify-self:stretch;
          margin-top:0;
          max-width:100%;
          min-height:126px;
          padding:14px 12px 16px;
          border-radius:32px;
          flex-direction:column;
          gap:10px;
          justify-content:flex-start;
          align-items:center;
          text-align:center;
          background:
            radial-gradient(circle at 50% 0%, color-mix(in srgb, var(--ma-accent) 14%, transparent), transparent 52%),
            linear-gradient(180deg, rgba(255,255,255,.12), rgba(255,255,255,.045));
          border-color:rgba(255,255,255,.14);
          box-shadow:0 16px 36px rgba(0,0,0,.12), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .card.layout-tablet .player-focus-art-wrap {
          display:flex;
          order:2;
        }
        .card.layout-tablet .player-focus-art {
          width:68px;
          height:68px;
          border-radius:999px;
        }
        .player-focus-name {
          font-size:calc(12px * var(--v2-font-scale));
          font-weight:900;
          line-height:1;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
          max-width:220px;
        }
        .card.layout-tablet .player-focus-name {
          order:1;
          font-size:calc(14px * var(--v2-font-scale));
          max-width:100%;
          text-align:center;
          line-height:1.15;
          white-space:normal;
          text-wrap:balance;
          font-weight:900;
        }
        .card.layout-tablet .player-focus-copy {
          order:1;
          display:grid;
          justify-items:center;
          gap:6px;
        }
        .card.layout-tablet .player-focus-tags {
          justify-content:center;
          flex-wrap:wrap;
          min-height:20px;
        }
        .player-focus-meta {
          display:flex;
          flex-direction:row;
          align-items:center;
          justify-content:center;
          gap:5px;
          min-width:0;
        }
        .player-focus-sub {
          max-width:100%;
          font-size:10px;
          font-weight:800;
          line-height:1.15;
          color:rgba(255,255,255,.66);
          text-align:center;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .theme-light .player-focus-sub {
          color:rgba(33,41,57,.66);
        }
        .theme-light .player-focus-nav-btn:disabled {
          opacity:.26;
        }
        .card.layout-tablet .player-focus-meta {
          display:none;
        }
        .card.layout-tablet .player-focus-sub {
          display:none;
        }
        .player-focus.is-playing {
          box-shadow:0 20px 44px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.08), 0 0 0 1px rgba(255,255,255,.08);
        }
        .player-focus.is-playing .player-focus-name {
          color:color-mix(in srgb, currentColor 84%, var(--ma-accent) 16%);
        }
        .player-focus.is-playing::before {
          animation:playerFocusPulse 3.4s ease-in-out infinite;
        }
        .player-focus.is-playing .player-focus-art {
          box-shadow:0 0 0 1px color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.08)), 0 0 0 12px color-mix(in srgb, var(--ma-accent) 12%, transparent), 0 16px 30px rgba(0,0,0,.18);
          animation:playerThumbPulse 3.2s ease-in-out infinite;
        }
        .eq-icon {
          display:none;
          align-items:flex-end;
          gap:3px;
          height:18px;
          width:20px;
          color:var(--ma-accent);
        }
        .player-focus .eq-icon,
        .player-focus.is-playing .eq-icon { display:none !important; }
        .menu-list-item.is-playing .eq-icon { display:inline-flex; }
        .eq-icon span {
          width:3px;
          border-radius:999px;
          background:currentColor;
          animation:eqPulse 1.15s ease-in-out infinite;
          transform-origin:center bottom;
        }
        .eq-icon span:nth-child(1) { height:9px; animation-delay:0s; }
        .eq-icon span:nth-child(2) { height:15px; animation-delay:.18s; }
        .eq-icon span:nth-child(3) { height:11px; animation-delay:.36s; }
        @keyframes playerFocusPulse {
          0%, 100% { transform:scale(1); opacity:.45; }
          50% { transform:scale(1.05); opacity:.72; }
        }
        @keyframes playerThumbPulse {
          0%,100% { transform:scale(1); }
          50% { transform:scale(1.04); }
        }
        @keyframes eqPulse {
          0%,100% { transform:scaleY(.55); opacity:.55; }
          50% { transform:scaleY(1.08); opacity:1; }
        }
        .hero-mobile-top {
          width:100%;
          display:grid;
          justify-items:center;
          margin-bottom:6px;
          position:relative;
          z-index:3;
        }
        .card:not(.layout-tablet):not(.height-short):not(.height-tight):not(.compact-collapsed) .hero-mobile-top {
          margin-top:-10px;
          margin-bottom:16px;
        }
        .card:not(.layout-tablet):not(.height-short):not(.height-tight):not(.compact-collapsed) .player-focus {
          margin-top:0;
        }
        .hero-split-shell {
          width:min(1180px, 100%);
          display:grid;
          gap:14px;
          align-items:center;
          position:relative;
          z-index:2;
        }
        .card.layout-tablet .hero-split-shell {
          width:100%;
          direction:ltr;
          grid-template-columns:minmax(0, 1.86fr) minmax(var(--flow-hero-info-min), 1fr);
          gap:var(--flow-hero-gap);
        }
        .hero-visual {
          position:relative;
          min-width:0;
          display:grid;
          justify-items:center;
          align-items:center;
        }
        .hero-info {
          min-width:0;
          display:grid;
          gap:14px;
          align-content:center;
          justify-items:center;
          text-align:center;
          position:relative;
          z-index:2;
        }
        .card.layout-tablet .hero-info {
          width:100%;
          max-width:390px;
          justify-items:start;
          text-align:start;
        }
        .card.rtl .hero-info {
          direction:rtl;
        }
        .hero-copy {
          display:grid;
          gap:8px;
          justify-items:center;
          text-align:center;
          width:100%;
          max-width:min(680px, 88%);
          min-width:0;
          position:relative;
          z-index:1;
          margin-top:0;
        }
        .card.layout-tablet .hero-copy {
          max-width:100%;
          gap:10px;
          justify-items:start;
          text-align:start;
          margin:0;
        }
        .hero-title {
          width:100%;
          max-width:100%;
          font-size:calc(28px * var(--v2-font-scale));
          font-weight:900;
          line-height:1.06;
          letter-spacing:-.035em;
          white-space:normal;
          overflow:hidden;
          text-overflow:ellipsis;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
        }
        .card.layout-tablet .hero-title {
          font-size:var(--flow-tablet-title-size);
          text-wrap:balance;
          line-height:1.08;
        }
        .hero-sub {
          width:100%;
          max-width:100%;
          font-size:calc(13px * var(--v2-font-scale));
          color:rgba(255,255,255,.72);
          white-space:normal;
          overflow:hidden;
          text-overflow:ellipsis;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
        }
        .np-sub.scroll-when-overflow {
          display:block;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:clip;
          text-align:start;
          -webkit-line-clamp:unset;
          -webkit-box-orient:initial;
        }
        .np-sub.scroll-when-overflow .scrolling-text-inner {
          display:inline-flex;
          align-items:center;
          min-width:0;
          max-width:none;
          width:max-content;
          gap:var(--scroll-gap, 3rem);
        }
        .np-sub.scroll-when-overflow .scrolling-text-item {
          flex:0 0 auto;
          white-space:nowrap;
        }
        .np-sub.scroll-when-overflow.is-overflowing {
          -webkit-mask-image:linear-gradient(90deg, transparent 0, #000 12%, #000 88%, transparent 100%);
          mask-image:linear-gradient(90deg, transparent 0, #000 12%, #000 88%, transparent 100%);
        }
        .np-sub.scroll-when-overflow.is-overflowing .scrolling-text-inner {
          animation:homeiiSubtitleScroll var(--scroll-duration, 12s) linear infinite;
          will-change:transform;
        }
        @keyframes homeiiSubtitleScroll {
          from { transform:translateX(0); }
          to { transform:translateX(calc(-1 * var(--scroll-distance, 0px))); }
        }
        @media (prefers-reduced-motion: reduce) {
          .np-sub.scroll-when-overflow {
            text-overflow:ellipsis;
          }
          .np-sub.scroll-when-overflow.is-overflowing .scrolling-text-inner {
            max-width:100%;
            gap:0;
            animation:none;
          }
        }
        .hero-up-next {
          justify-content:center;
          text-align:center;
          width:auto;
          max-width:min(520px, 94%);
          margin-top:0;
        }
        .card.layout-tablet .hero-up-next {
          justify-content:flex-start;
          text-align:start;
          width:100%;
          max-width:100%;
        }
        .hero-info .night-quick-row {
          justify-content:center;
          margin-top:2px;
        }
        .card.layout-tablet .hero-info .night-quick-row {
          justify-content:flex-start;
        }
        .card.layout-tablet .hero-sub {
          font-size:var(--flow-tablet-sub-size);
          text-wrap:balance;
        }
        .center {
          display:flex;
          flex-direction:column;
          align-items:center;
          justify-content:center;
          width:100%;
          max-width:100%;
          min-width:0;
          gap:8px;
          min-height:0;
          padding-top:0;
          margin-top:0;
          overflow:hidden;
          position:relative;
        }
        .card.layout-tablet .center {
          width:100%;
          max-width:var(--tablet-max);
          margin-inline:auto;
          gap:14px;
          overflow:hidden;
          padding-bottom:2px;
          z-index:4;
          min-height:0;
          justify-content:center;
        }
        .art-stage {
          position:relative;
          width:min(760px, 100%);
          max-width:100%;
          margin-inline:auto;
          display:grid;
          justify-items:center;
          align-items:center;
          gap:10px;
          padding:0;
          justify-self:center;
          z-index:1;
        }
        .card.layout-tablet .art-stage {
          width:100%;
          display:grid;
          grid-template-columns:minmax(0,1fr);
          grid-template-areas:"shell";
          column-gap:0;
          row-gap:0;
          align-items:center;
          justify-content:center;
        }
        .mobile-art-shell {
          overflow:visible;
        }
        .mobile-art-shell > .art-source-badges {
          inset-block-start:4px;
          inset-inline-start:clamp(4px, 1.1cqi, 12px);
        }
        .card.layout-tablet .mobile-art-shell > .art-source-badges {
          inset-block-start:max(-24px, calc((var(--flow-autofit-art-height) - var(--flow-art-card-max-size)) / 2 - 24px));
          inset-inline-start:13%;
          max-width:64%;
          gap:5px;
          transform:translateX(8px);
        }
        .card.layout-tablet .source-badge {
          min-height:18px;
          padding:0 7px;
          font-size:9px;
          font-weight:850;
          color:rgba(255,255,255,.78);
          background:rgba(10,14,22,.34);
          border-color:rgba(255,255,255,.1);
          box-shadow:0 8px 18px rgba(0,0,0,.12);
          backdrop-filter:blur(10px);
          -webkit-backdrop-filter:blur(10px);
        }
        .card.layout-tablet .source-badge.quality {
          color:rgba(255,255,255,.82);
          background:rgba(255,255,255,.08);
        }
        .art-aura {
          position:absolute;
          top:50%;
          left:50%;
          width:var(--flow-art-aura-size);
          height:var(--flow-art-aura-size);
          border-radius:38%;
          background:center / cover no-repeat;
          filter:blur(44px) saturate(1.08);
          opacity:.62;
          transform:translate(-50%, -48%) scale(.98);
          pointer-events:none;
          will-change:transform, opacity;
        }
        .theme-light .art-aura {
          opacity:.28;
          filter:blur(50px) saturate(1.02) brightness(1.03);
        }
        .card.layout-tablet .art-aura {
          width:var(--flow-tablet-art-aura-size);
          height:var(--flow-tablet-art-aura-size);
          transform:translate(-50%, -44%) scale(1.02);
        }
        .card.background-motion .art-aura {
          animation:auraDrift calc(var(--aura-motion-duration, 22s) * .88) ease-in-out infinite reverse;
        }
        @media (prefers-reduced-motion: reduce) {
          .card.background-motion .bg,
          .card.background-motion .shade,
          .card.background-motion .glow,
          .card.background-motion .hero-aura,
          .card.background-motion .art-aura {
            animation:none !important;
          }
        }
        .card.dynamic-theme .art-aura {
          filter:blur(48px) saturate(calc(1.04 + (.08 * var(--dynamic-theme-strength, .82))));
          opacity:calc(.38 + (.14 * var(--dynamic-theme-strength, .82)));
          mix-blend-mode:screen;
        }
        .theme-light.card.dynamic-theme .art-aura {
          opacity:calc(.2 + (.08 * var(--dynamic-theme-strength, .82)));
        }
        .art-stack-view {
          position:relative;
          width:100%;
          height:var(--flow-art-stack-height);
          min-height:var(--flow-art-stack-height);
          isolation:isolate;
          direction:ltr;
          overflow:hidden;
          --art-drag-x:0px;
        }
        .art-stack-viewport {
          position:relative;
          width:100%;
          height:var(--flow-art-stack-height);
          min-height:var(--flow-art-stack-height);
          overflow:hidden;
          direction:ltr;
          touch-action:pan-y;
          -webkit-user-select:none;
          user-select:none;
        }
        .art-stack-container {
          position:relative;
          width:100%;
          height:var(--flow-art-stack-height);
          min-height:var(--flow-art-stack-height);
          touch-action:pan-y;
          user-select:none;
          -webkit-user-select:none;
          direction:ltr;
        }
        .art-stack-slide {
          position:absolute;
          inset-block:0;
          left:50%;
          width:68%;
          min-width:0;
          display:flex;
          align-items:center;
          justify-content:center;
          padding:0;
          transition:
            transform .16s cubic-bezier(.22,.8,.24,1),
            opacity .16s ease,
            filter .16s ease;
          will-change:transform, opacity, filter;
        }
        .art-stack-card {
          position:relative;
          width:min(var(--flow-art-card-size), var(--flow-art-stack-height), 100%, var(--flow-art-card-max-size));
          height:auto;
          max-width:100%;
          max-height:var(--flow-art-card-max-size);
          aspect-ratio:1/1;
          border-radius:34px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.1);
          box-shadow:0 16px 30px rgba(0,0,0,.14);
          transition:transform .16s cubic-bezier(.22,.8,.24,1), opacity .16s ease, filter .16s ease;
          will-change:transform, opacity, filter;
          contain:paint;
        }
        .art-stack-view.dragging .art-stack-slide,
        .art-stack-view.dragging .art-stack-card {
          transition:none !important;
        }
        .art-stack-card img {
          position:absolute;
          inset:0;
          width:100%;
          height:100%;
          max-width:100%;
          max-height:100%;
          object-fit:contain;
          object-position:center;
          display:block;
          pointer-events:none;
          -webkit-user-drag:none;
        }
        .art-stack-card img[data-homeii-art-ready="0"] {
          opacity:1;
        }
        .art-stack-card img[data-homeii-art-ready="1"] {
          transition:opacity .12s ease-out;
        }
        .art-stack-slide.center {
          width:72%;
          z-index:3;
          transform:translateX(calc(-50% + var(--art-drag-x))) scale(1);
          opacity:1;
        }
        .art-stack-slide.center .art-stack-card {
          transform:translateY(0) scale(1);
          border-color:rgba(255,255,255,.16);
          box-shadow:
            0 30px 60px rgba(0,0,0,.26),
            0 0 0 1px rgba(255,255,255,.06),
            0 22px 48px rgba(var(--dynamic-glow-rgb, 255 178 56) / .14);
        }
        .art-stack-slide.prev,
        .art-stack-slide.next {
          width:50%;
          z-index:1;
        }
        .art-stack-slide.prev {
          transform:translateX(calc(-122% + (var(--art-drag-x) * .14))) scale(.72);
          opacity:.3;
        }
        .art-stack-slide.next {
          transform:translateX(calc(22% + (var(--art-drag-x) * .14))) scale(.72);
          opacity:.3;
        }
        .art-stack-slide.prev .art-stack-card {
          opacity:1;
          filter:saturate(.68) brightness(.82) contrast(.96);
          transform:perspective(900px) rotateY(16deg) scale(.82);
        }
        .art-stack-slide.next .art-stack-card {
          opacity:1;
          filter:saturate(.68) brightness(.82) contrast(.96);
          transform:perspective(900px) rotateY(-16deg) scale(.82);
        }
        .art-stack-slide.prev .art-stack-card::after,
        .art-stack-slide.next .art-stack-card::after {
          content:"";
          position:absolute;
          inset:0;
          background:
            linear-gradient(180deg, rgba(8,11,18,.26), rgba(8,11,18,.44)),
            radial-gradient(circle at center, transparent 32%, rgba(8,11,18,.18) 100%);
          pointer-events:none;
        }
        .theme-light .art-stack-slide.prev .art-stack-card::after,
        .theme-light .art-stack-slide.next .art-stack-card::after {
          background:
            linear-gradient(180deg, rgba(82,95,112,.14), rgba(82,95,112,.26)),
            radial-gradient(circle at center, transparent 36%, rgba(82,95,112,.08) 100%);
        }
        .art-stack-card.ghost {
          background:rgba(255,255,255,.04);
          border-color:rgba(255,255,255,.06);
          opacity:.16;
        }
        .art-stack-card.placeholder {
          background:
            radial-gradient(circle at 35% 25%, color-mix(in srgb, var(--ma-accent) 24%, transparent), transparent 34%),
            linear-gradient(135deg, rgba(255,255,255,.12), rgba(255,255,255,.03));
          display:grid;
          place-items:center;
        }
        .art-stack-fallback {
          width:100%;
          height:100%;
          max-width:100%;
          max-height:100%;
          display:grid;
          place-items:center;
          align-content:center;
          justify-content:center;
          margin:auto;
          color:color-mix(in srgb, var(--ma-accent) 76%, white 24%);
          position:relative;
          overflow:hidden;
          inset:0;
          transform:translateZ(0);
          background:
            radial-gradient(circle at 24% 20%, color-mix(in srgb, var(--ma-accent) 30%, transparent), transparent 28%),
            radial-gradient(circle at 74% 26%, color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.18)), transparent 30%),
            linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.03));
        }
        .art-stack-fallback::before {
          content:"";
          position:absolute;
          inset:0;
          border-radius:inherit;
          background:radial-gradient(circle at center, color-mix(in srgb, var(--ma-accent) 34%, transparent), transparent 62%);
          filter:blur(24px);
          opacity:.74;
        }
        .art-stack-fallback::after {
          content:"";
          position:absolute;
          inset:12%;
          border-radius:28px;
          border:1px solid rgba(255,255,255,.12);
          background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
          box-shadow:inset 0 1px 0 rgba(255,255,255,.12);
        }
        .fallback-disc {
          width:104px;
          height:104px;
          border-radius:999px;
          display:grid;
          place-items:center;
          background:rgba(12,16,24,.36);
          border:1px solid rgba(255,255,255,.18);
          z-index:1;
          box-shadow:0 18px 42px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.12);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
        }
        .fallback-disc .ui-ic { width:46px; height:46px; }
        .art-stack-fallback.brand-fallback .fallback-disc {
          width:62%;
          height:34%;
          min-width:118px;
          min-height:58px;
          border-radius:999px;
          background:rgba(12,16,24,.28);
        }
        .art-stack-brand-logo {
          width:82%;
          height:auto;
          max-height:58px;
          opacity:.78;
        }
        .surprise-me-card .surprise-me-wand {
          z-index:3;
        }
        .fallback-aura {
          position:absolute;
          inset:0;
          border-radius:inherit;
          background:
            radial-gradient(circle at 50% 50%, color-mix(in srgb, var(--ma-accent) 24%, transparent), transparent 38%),
            radial-gradient(circle at 50% 72%, color-mix(in srgb, var(--ma-accent) 14%, transparent), transparent 52%);
          opacity:.95;
        }
        .fallback-note {
          color:#fff6e2;
          animation:fallbackNotePulse 1.9s ease-in-out infinite;
        }
        @keyframes fallbackPulse {
          0%, 100% { transform:scale(.92); opacity:.48; }
          50% { transform:scale(1.08); opacity:.78; }
        }
        @keyframes fallbackNotePulse {
          0%,100% { transform:scale(.96); box-shadow:0 18px 42px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.12); }
          50% { transform:scale(1.06); box-shadow:0 22px 46px color-mix(in srgb, var(--ma-accent) 18%, rgba(0,0,0,.22)), inset 0 1px 0 rgba(255,255,255,.18); }
        }
        .art-stack-cue {
          position:absolute;
          inset-block-start:50%;
          transform:translateY(-50%);
          width:28px;
          height:28px;
          display:grid;
          place-items:center;
          font-size:24px;
          font-weight:900;
          line-height:1;
          color:rgba(255,255,255,.42);
          text-shadow:0 10px 18px rgba(0,0,0,.2);
          pointer-events:none;
          animation:stackCueFade 2.6s ease-in-out infinite;
          z-index:4;
        }
        .art-stack-cue.start { inset-inline-start:8px; }
        .art-stack-cue.end { inset-inline-end:8px; animation-delay:1.25s; }
        .theme-light .art-stack-card {
          background:rgba(255,255,255,.58);
          border-color:rgba(142,157,180,.2);
          box-shadow:0 22px 42px rgba(111,126,150,.16);
        }
        .theme-light .art-stack-cue {
          color:rgba(44,56,72,.36);
          text-shadow:none;
        }
        @keyframes stackCueFade {
          0%, 100% { opacity:.08; }
          45% { opacity:.38; }
          60% { opacity:.2; }
        }
        .mobile-art-shell {
          position:relative;
          width:min(var(--flow-mobile-art-size), calc(100% - 28px));
          max-width:100%;
          margin-inline:auto;
          flex:0 0 auto;
          z-index:1;
          justify-self:center;
          display:block;
          box-sizing:border-box;
          padding:0;
          border-radius:0;
          background:transparent;
          border:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          box-shadow:none;
          transition:transform .24s ease, opacity .24s ease;
        }
        .card.layout-tablet .mobile-art-shell {
          grid-area:shell;
          width:min(760px, 100%);
          padding:0;
          border-radius:0;
          box-shadow:none;
        }
        .card.layout-tablet .art-stack-view,
        .card.layout-tablet .art-stack-viewport,
        .card.layout-tablet .art-stack-container {
          height:var(--flow-autofit-art-height);
          min-height:var(--flow-autofit-art-height);
          max-height:var(--flow-autofit-art-height);
        }
        .card.layout-tablet .art-stack-slide {
          width:70%;
        }
        .card.layout-tablet .art-stack-slide.center {
          width:74%;
        }
        .card.layout-tablet .art-stack-slide.prev,
        .card.layout-tablet .art-stack-slide.next {
          width:44%;
        }
        .card.layout-tablet .art-stack-slide.prev {
          transform:translateX(calc(-118% + (var(--art-drag-x) * .12))) scale(.76);
          opacity:.34;
        }
        .card.layout-tablet .art-stack-slide.next {
          transform:translateX(calc(18% + (var(--art-drag-x) * .12))) scale(.76);
          opacity:.34;
        }
        .card.layout-tablet .art-stack-card {
          width:min(var(--flow-autofit-art-height), 100%, var(--flow-art-card-max-size));
          max-width:min(var(--flow-autofit-art-height), var(--flow-art-card-max-size));
          max-height:var(--flow-autofit-art-height);
        }
        .card.layout-tablet .art-stack-slide.prev .art-stack-card {
          transform:perspective(1080px) rotateY(14deg) scale(.84);
        }
        .card.layout-tablet .art-stack-slide.next .art-stack-card {
          transform:perspective(1080px) rotateY(-14deg) scale(.84);
        }
        .card.layout-tablet .art-stack-cue {
          width:34px;
          height:34px;
          font-size:28px;
        }
        .card.layout-tablet .art-stack-cue.start { inset-inline-start:18px; }
        .card.layout-tablet .art-stack-cue.end { inset-inline-end:18px; }
        .theme-light .mobile-art-shell { background:transparent; border:none; box-shadow:none; }
        .mobile-art-shell.swipe-next,
        .mobile-art-shell.swipe-prev { animation:none; }
        .np-art.mobile-art {
          width:100%; aspect-ratio:1/1; border-radius:36px; overflow:hidden; display:grid; place-items:center; margin-inline:auto;
          background:rgba(255,255,255,.08); border:1px solid rgba(255,255,255,.12); box-shadow:0 24px 48px rgba(0,0,0,.24); font-size:72px; color:rgba(255,255,255,.72);
        }
        .np-art.mobile-art img { width:100%; height:100%; object-fit:contain; object-position:center center; }
        .homeii-art-fallback {
          width:100%;
          height:100%;
          display:grid;
          place-items:center;
          border-radius:inherit;
          background:
            radial-gradient(circle at 50% 36%, color-mix(in srgb, var(--accent) 28%, transparent), transparent 52%),
            linear-gradient(145deg, rgba(255,255,255,.1), rgba(255,255,255,.025));
          color:rgba(255,255,255,.78);
          box-shadow:inset 0 0 0 1px rgba(255,255,255,.08);
        }
        .homeii-art-fallback-disc {
          width:46%;
          height:46%;
          min-width:34px;
          min-height:34px;
          display:grid;
          place-items:center;
          border-radius:50%;
          background:rgba(0,0,0,.2);
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 16px 34px rgba(0,0,0,.18);
        }
        .homeii-art-fallback svg { width:48%; height:48%; color:currentColor; opacity:.92; }
        .mobile-art-actions {
          position:relative;
          inset:auto;
          display:flex;
          align-items:center;
          justify-content:center;
          gap:10px;
          width:auto;
          margin:0;
          padding:0;
          border-radius:0;
          background:transparent;
          border:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          box-shadow:none;
          z-index:1;
        }
        .card.layout-tablet .mobile-art-actions {
          width:100%;
          margin:0;
          justify-content:flex-start;
          position:relative;
          z-index:4;
        }
        .mobile-art-actions.count-3 { grid-template-columns:none; }
        .mobile-art-fab {
          position:static;
          width:42px; min-width:42px; height:42px; border-radius:999px; border:1px solid rgba(255,255,255,.1);
          background:rgba(14,18,28,.26); backdrop-filter:blur(10px); -webkit-backdrop-filter:blur(10px);
          color:#fff; display:grid; place-items:center; cursor:pointer; box-shadow:0 8px 18px rgba(0,0,0,.14);
          transition:transform .16s ease, background-color .16s ease, border-color .16s ease, color .16s ease, box-shadow .16s ease;
        }
        .card.layout-tablet .mobile-art-fab {
          width:40px;
          min-width:40px;
          height:40px;
          border-radius:999px;
        }
        .mobile-art-fab .ui-ic { width:20px; height:20px; }
        .mobile-art-fab.active { color:#f5a623; border-color:rgba(245,166,35,.34); background:rgba(245,166,35,.14); }
        .mobile-art-fab.danger-fab {
          color:#ffd6d6;
          border-color:rgba(255,105,115,.28);
          background:linear-gradient(145deg, rgba(255,85,95,.2), rgba(255,255,255,.06));
          box-shadow:0 10px 22px rgba(255,70,80,.1), 0 8px 18px rgba(0,0,0,.14);
        }
        .mobile-art-fab.danger-fab.pressed,
        .mobile-art-fab.danger-fab:active,
        .mobile-art-fab.danger-fab.busy {
          color:#fff;
          border-color:rgba(255,130,138,.48);
          background:linear-gradient(145deg, rgba(255,85,95,.32), rgba(255,255,255,.08));
        }
        .voice-assistant-fab { position:relative; overflow:visible; }
        .voice-assistant-fab.listening {
          color:var(--ma-accent);
          border-color:color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.16));
          box-shadow:0 0 0 7px color-mix(in srgb, var(--ma-accent) 11%, transparent), 0 14px 26px rgba(0,0,0,.2);
        }
        .voice-assistant-fab.listening::after {
          content:"";
          position:absolute;
          inset:-8px;
          border-radius:999px;
          border:1px solid color-mix(in srgb, var(--ma-accent) 44%, transparent);
          animation:voiceAssistantListenPulse 1.1s ease-out infinite;
          pointer-events:none;
        }
        @keyframes voiceAssistantListenPulse {
          0% { transform:scale(.88); opacity:.78; }
          100% { transform:scale(1.38); opacity:0; }
        }
        .theme-light .np-art.mobile-art {
          background:rgba(255,255,255,.56);
          border-color:rgba(142,157,180,.2);
          box-shadow:0 22px 42px rgba(111,126,150,.18);
          color:rgba(64,74,87,.6);
        }
        .theme-light .homeii-art-fallback {
          color:rgba(48,55,68,.7);
          background:
            radial-gradient(circle at 50% 36%, color-mix(in srgb, var(--accent) 18%, transparent), transparent 54%),
            linear-gradient(145deg, rgba(255,255,255,.88), rgba(238,242,247,.58));
          box-shadow:inset 0 0 0 1px rgba(90,105,125,.12);
        }
        .theme-light .homeii-art-fallback-disc {
          background:rgba(255,255,255,.48);
          border-color:rgba(82,96,118,.12);
        }
        .theme-light .player-chip-ico,
        .theme-light .mobile-art-fab {
          background:rgba(255,255,255,.76);
          border-color:rgba(141,155,177,.22);
          box-shadow:0 12px 26px rgba(111,126,150,.16);
        }
        .theme-light .player-focus,
        .theme-light .mobile-art-actions {
          background:transparent;
          border-color:transparent;
          box-shadow:none;
        }
        .theme-light .mobile-art-fab { color:#1f2633; }
        .theme-light .mobile-art-fab.active {
          color:#f5a623;
          border-color:rgba(245,166,35,.38);
          background:rgba(245,166,35,.16);
        }
        .theme-light .mobile-art-fab.danger-fab {
          color:#b4232b;
          background:#fff1f2;
          border-color:rgba(180,35,43,.18);
          box-shadow:0 12px 26px rgba(180,35,43,.1);
        }
        .mobile-meta { display:none; }
        .bottom { display:grid; width:100%; max-width:100%; min-width:0; min-height:0; gap:12px; align-content:end; }
        .card.layout-tablet .bottom {
          width:100%;
          max-width:min(980px, 100%);
          margin-inline:auto;
          gap:12px;
          position:relative;
          z-index:5;
          margin-top:-2px;
        }
        .card.layout-tablet.tablet-auto-fit .tablet-shell {
          height:100%;
          min-height:0;
          align-items:stretch;
        }
        .card.layout-tablet.tablet-auto-fit .tablet-main {
          height:100%;
          min-height:0;
        }
        .card.layout-tablet.tablet-auto-fit .center {
          gap:12px;
        }
        .card.layout-tablet.tablet-auto-fit .hero-split-shell {
          grid-template-columns:minmax(0, 1.82fr) minmax(min(280px, var(--flow-hero-info-min)), 1fr);
          gap:var(--flow-hero-gap);
        }
        .card.layout-tablet.tablet-auto-fit .hero-info {
          max-width:360px;
        }
        .card.layout-tablet.tablet-auto-fit .art-stage {
          gap:8px;
        }
        .card.layout-tablet.tablet-auto-fit .mobile-art-shell {
          width:min(700px, 100%);
          padding:0;
          border-radius:0;
        }
        .card.layout-tablet.tablet-auto-fit .art-stack-view,
        .card.layout-tablet.tablet-auto-fit .art-stack-viewport,
        .card.layout-tablet.tablet-auto-fit .art-stack-container {
          height:var(--flow-autofit-art-height);
          min-height:var(--flow-autofit-art-height);
          max-height:var(--flow-autofit-art-height);
        }
        .card.layout-tablet.tablet-auto-fit .mobile-art-actions {
          margin:0;
        }
        .card.layout-tablet.tablet-auto-fit .hero-copy {
          max-width:100%;
          gap:8px;
          margin-top:0;
        }
        .card.layout-tablet.tablet-auto-fit .hero-title {
          font-size:calc(var(--flow-tablet-title-size) - 2px);
        }
        .card.layout-tablet.tablet-auto-fit .hero-sub {
          font-size:var(--flow-tablet-sub-size);
        }
        .card.layout-tablet.tablet-auto-fit .hero-up-next {
          max-width:min(460px, 90%);
          margin-top:0;
        }
        .card.layout-tablet.tablet-auto-fit .night-quick-row {
          gap:8px;
          transform:none;
        }
        .card.layout-tablet.tablet-auto-fit .bottom {
          gap:10px;
          margin-top:0;
        }
        .card.layout-tablet.tablet-auto-fit .progress-line {
          margin-top:0;
        }
        .card.layout-tablet.tablet-auto-fit .controls {
          gap:16px;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .mobile-art-shell {
          --flow-art-card-max-size:var(--flow-dense-art-height);
          width:min(620px, 100%);
          padding:0;
          border-radius:0;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .hero-split-shell {
          grid-template-columns:minmax(0, 1.72fr) minmax(min(250px, var(--flow-hero-info-min)), 1fr);
          gap:calc(var(--flow-hero-gap) * .78);
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-view,
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-viewport,
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-container {
          height:var(--flow-dense-art-height);
          min-height:var(--flow-dense-art-height);
          max-height:var(--flow-dense-art-height);
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-card {
          width:min(var(--flow-dense-art-height), 100%, var(--flow-art-card-max-size));
          max-width:min(var(--flow-dense-art-height), var(--flow-art-card-max-size));
          max-height:var(--flow-dense-art-height);
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-slide {
          width:66%;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-slide.center {
          width:70%;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-slide.prev,
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .art-stack-slide.next {
          width:40%;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .hero-copy {
          gap:6px;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .hero-title {
          font-size:max(18px, calc(var(--flow-tablet-title-size) - 8px));
          line-height:1.02;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .hero-sub {
          font-size:max(11px, calc(var(--flow-tablet-sub-size) - 1px));
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .hero-up-next {
          transform:scale(.94);
          transform-origin:center top;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .night-quick-row {
          gap:6px;
          transform:scale(.9);
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .mobile-art-actions {
          margin-top:-10px;
          margin-bottom:2px;
        }
        .card.layout-tablet.tablet-auto-fit.tablet-fit-dense .mobile-art-fab {
          width:38px;
          min-width:38px;
          height:38px;
        }
        .history-chip {
          width:100%;
          display:grid;
          grid-template-columns:36px minmax(0,1fr);
          align-items:center;
          gap:10px;
          padding:8px 10px;
          border:none;
          border-radius:18px;
          color:inherit;
          text-align:start;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.1);
          box-shadow:0 12px 24px rgba(0,0,0,.12);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
          cursor:pointer;
          transition:transform .16s ease, opacity .18s ease, border-color .18s ease;
        }
        .history-chip:hover {
          transform:translateY(-1px);
          border-color:rgba(var(--dynamic-accent-rgb, 224 161 27) / .28);
        }
        .history-chip:active {
          transform:translateY(1px) scale(.985);
        }
        .history-chip-art {
          width:36px;
          height:36px;
          border-radius:12px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08);
        }
        .history-chip-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .history-chip-copy {
          min-width:0;
          display:grid;
          gap:2px;
        }
        .history-chip-title,
        .history-chip-sub {
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .history-chip-title {
          font-size:12px;
          font-weight:800;
          color:#fffdf7;
        }
        .history-chip-sub {
          font-size:10px;
          font-weight:600;
          color:rgba(255,255,255,.62);
        }
        .theme-light .history-chip {
          background:rgba(255,255,255,.76);
          border-color:rgba(143,159,181,.16);
          box-shadow:0 12px 24px rgba(95,112,136,.12);
        }
        .theme-light .history-chip-title {
          color:#1f2633;
        }
        .theme-light .history-chip-sub {
          color:#5d6b7f;
        }
        .time-row,.controls,.accent-row,.progress-line { direction:ltr; }
        .time-row { display:contents; }
        .progress-line {
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          align-items:center;
          gap:10px;
        }
        .empty-quick-shelf {
          --empty-quick-edge-fade:42px;
          --empty-quick-gap:16px;
          --empty-quick-card-width:214px;
          --empty-quick-two-card-min:calc((var(--empty-quick-edge-fade) * 2) + (var(--empty-quick-card-width) * 2) + var(--empty-quick-gap));
          display:flex;
          gap:var(--empty-quick-gap);
          width:min(980px, 100%);
          box-sizing:border-box;
          margin:0 auto;
          padding:8px var(--empty-quick-edge-fade) 10px;
          overflow-x:auto;
          scrollbar-width:none;
          -ms-overflow-style:none;
          scroll-snap-type:x proximity;
          scroll-padding-inline:var(--empty-quick-edge-fade);
          -webkit-mask-image:linear-gradient(90deg, transparent 0, rgba(0,0,0,.22) 10px, rgba(0,0,0,.72) 26px, #000 var(--empty-quick-edge-fade), #000 calc(100% - var(--empty-quick-edge-fade)), rgba(0,0,0,.72) calc(100% - 26px), rgba(0,0,0,.22) calc(100% - 10px), transparent 100%);
          mask-image:linear-gradient(90deg, transparent 0, rgba(0,0,0,.22) 10px, rgba(0,0,0,.72) 26px, #000 var(--empty-quick-edge-fade), #000 calc(100% - var(--empty-quick-edge-fade)), rgba(0,0,0,.72) calc(100% - 26px), rgba(0,0,0,.22) calc(100% - 10px), transparent 100%);
        }
        .rtl .empty-quick-shelf {
          padding-inline:var(--empty-quick-edge-fade);
        }
        .empty-quick-shelf::-webkit-scrollbar {
          display:none;
        }
        .empty-quick-card {
          position:relative;
          min-width:var(--empty-quick-card-width);
          max-width:var(--empty-quick-card-width);
          min-height:78px;
          padding:12px 14px;
          display:grid;
          grid-template-columns:48px minmax(0,1fr);
          gap:12px;
          align-items:center;
          border:none;
          border-radius:24px;
          color:inherit;
          background:linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.05));
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 18px 34px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          cursor:pointer;
          scroll-snap-align:center;
          transition:transform .16s ease, border-color .16s ease, box-shadow .16s ease, background-color .16s ease;
        }
        .empty-quick-card:active,
        .empty-quick-card.pressed,
        .empty-quick-card.busy {
          transform:translateY(1px) scale(.98);
          border-color:color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.14));
          box-shadow:0 12px 24px rgba(0,0,0,.12), 0 0 0 1px color-mix(in srgb, var(--ma-accent) 24%, transparent);
        }
        .empty-quick-card.busy,
        .empty-quick-card:disabled {
          cursor:progress;
        }
        .empty-quick-card.busy::after {
          content:"";
          position:absolute;
          inset:0;
          border-radius:inherit;
          background:linear-gradient(120deg, transparent 0%, color-mix(in srgb, var(--ma-accent) 18%, transparent) 45%, transparent 76%);
          transform:translateX(-100%);
          animation:emptyActionSweep .9s ease-in-out infinite;
          pointer-events:none;
        }
        .empty-quick-art {
          width:48px;
          height:48px;
          border-radius:16px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 20%, transparent), rgba(255,255,255,.06));
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 10px 24px rgba(0,0,0,.16);
        }
        .empty-quick-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .empty-quick-art .ui-ic {
          width:22px;
          height:22px;
        }
        .empty-quick-copy {
          min-width:0;
          display:grid;
          gap:4px;
          text-align:start;
        }
        .empty-quick-kicker {
          font-size:10px;
          font-weight:900;
          letter-spacing:.08em;
          text-transform:uppercase;
          color:rgba(255,255,255,.5);
        }
        .empty-quick-title {
          font-size:14px;
          font-weight:900;
          line-height:1.2;
          color:#f4f6fb;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .card.layout-tablet .progress-line,
        .card.layout-tablet .controls {
          width:min(900px, 100%);
          margin-inline:auto;
        }
        .card.layout-tablet .progress-line {
          gap:12px;
          margin-top:4px;
        }
        .progress-time {
          font-size:13px;
          color:rgba(255,255,255,.76);
          min-width:40px;
          text-align:center;
        }
        .card.layout-tablet .progress-time {
          font-size:15px;
          min-width:52px;
          letter-spacing:.01em;
        }
        .theme-light .progress-time {
          color:#4b5c73;
        }
        .progress {
          height:10px;
          min-width:0;
          overflow:hidden;
          cursor:pointer;
          border-radius:999px;
          background:linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.08));
          border:1px solid rgba(255,255,255,.08);
          box-shadow:inset 0 1px 2px rgba(255,255,255,.05), inset 0 -1px 2px rgba(0,0,0,.18);
        }
        .card.layout-tablet .progress {
          height:8px;
        }
        .progress-fill {
          width:0%;
          height:100%;
          background:linear-gradient(90deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 0 18px color-mix(in srgb, var(--ma-accent) 20%, transparent);
        }
        .controls { display:flex; align-items:center; justify-content:center; gap:12px; }
        .card.layout-tablet .controls {
          gap:18px;
          margin-top:4px;
        }
        .side-btn,.main-btn,.volume-btn,.menu-head button,.queue-actions .chip-btn,.action-btn {
          border:none; cursor:pointer; color:#fff; font:inherit;
          display:grid; place-items:center;
          background:linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.06));
          border:1px solid rgba(255,255,255,.14);
          backdrop-filter:blur(14px) saturate(138%);
          -webkit-backdrop-filter:blur(14px) saturate(138%);
        }
        .side-btn,.volume-btn {
          width:var(--flow-side-btn-size);
          height:var(--flow-side-btn-size);
          border-radius:19px;
          position:relative;
          overflow:visible;
          box-shadow:0 12px 26px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.08);
          transition:transform .16s ease, background .18s ease, border-color .18s ease, color .18s ease, box-shadow .18s ease;
        }
        .side-btn.minor-btn { width:var(--flow-minor-btn-size); height:var(--flow-minor-btn-size); border-radius:16px; }
        .main-btn {
          width:var(--flow-main-btn-size);
          height:var(--flow-main-btn-size);
          border-radius:50%;
          box-shadow:0 18px 34px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.18);
          background:linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,.07));
          position:relative;
          overflow:visible;
        }
        .card.layout-tablet .side-btn,
        .card.layout-tablet .volume-btn {
          width:var(--flow-tablet-side-btn-size);
          height:var(--flow-tablet-side-btn-size);
          border-radius:999px;
          background:linear-gradient(180deg, rgba(255,255,255,.11), rgba(255,255,255,.045));
          box-shadow:0 10px 22px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.07);
        }
        .card.layout-tablet .side-btn.minor-btn { width:var(--flow-tablet-minor-btn-size); height:var(--flow-tablet-minor-btn-size); border-radius:999px; }
        .card.layout-tablet .main-btn { width:var(--flow-tablet-main-btn-size); height:var(--flow-tablet-main-btn-size); }
        .card.layout-tablet .main-btn::after {
          content:"";
          position:absolute;
          inset:-6px;
          border-radius:inherit;
          border:1px solid transparent;
          opacity:0;
          pointer-events:none;
        }
        .card.layout-tablet .main-btn.is-playing::after {
          border-color:rgba(255,255,255,.22);
          box-shadow:0 0 16px rgba(255,255,255,.12);
          animation:main-btn-pulse 2.4s ease-out infinite;
          opacity:.75;
        }
        @keyframes main-btn-pulse {
          0% { transform:scale(1); opacity:.58; }
          65% { transform:scale(1.08); opacity:.14; }
          100% { transform:scale(1.12); opacity:0; }
        }
        .side-btn .ui-ic,.volume-btn .ui-ic {
          width:56%;
          height:56%;
          filter:drop-shadow(0 2px 5px rgba(0,0,0,.22));
        }
        .side-btn.minor-btn .ui-ic {
          width:64%;
          height:64%;
        }
        .main-btn .ui-ic {
          width:52%;
          height:52%;
          filter:drop-shadow(0 3px 8px rgba(0,0,0,.24));
        }
        .card.layout-tablet .side-btn .ui-ic,
        .card.layout-tablet .volume-btn .ui-ic {
          width:48%;
          height:48%;
          opacity:.92;
          filter:drop-shadow(0 1px 3px rgba(0,0,0,.16));
        }
        .card.layout-tablet .side-btn.minor-btn .ui-ic {
          width:54%;
          height:54%;
        }
        .card.layout-tablet .main-btn .ui-ic {
          width:48%;
          height:48%;
          opacity:.94;
          filter:drop-shadow(0 2px 5px rgba(0,0,0,.18));
        }
        .side-btn .ui-ic *,
        .volume-btn .ui-ic *,
        .main-btn .ui-ic * {
          stroke-linecap:round;
          stroke-linejoin:round;
          vector-effect:non-scaling-stroke;
        }
        .side-btn.active {
          color:#fff7e4;
          border-color:color-mix(in srgb, var(--ma-accent) 48%, rgba(255,255,255,.16));
          background:
            radial-gradient(circle at 50% 28%, color-mix(in srgb, var(--ma-accent) 30%, rgba(255,255,255,.08)), transparent 58%),
            linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 24%, rgba(255,255,255,.10)), rgba(255,255,255,.06));
          box-shadow:0 14px 30px color-mix(in srgb, var(--ma-accent) 20%, rgba(0,0,0,.18)), inset 0 1px 0 rgba(255,255,255,.14);
        }
        .side-btn.minor-btn.active::after {
          content:"";
          position:absolute;
          left:50%;
          bottom:8px;
          width:5px;
          height:5px;
          border-radius:999px;
          transform:translateX(-50%);
          background:currentColor;
          box-shadow:0 0 12px color-mix(in srgb, var(--ma-accent) 54%, transparent);
          opacity:.96;
        }
        #mobileShuffleBtn.minor-btn.active::after {
          content:none;
          display:none;
        }
        #mobileShuffleBtn.active {
          border-color:color-mix(in srgb, var(--ma-accent) 58%, rgba(255,255,255,.18));
          box-shadow:
            0 0 0 1px color-mix(in srgb, var(--ma-accent) 35%, transparent),
            0 0 22px color-mix(in srgb, var(--ma-accent) 24%, transparent),
            inset 0 1px 0 rgba(255,255,255,.16);
        }
        #mobileRepeatBtn[data-repeat-mode="one"]::before,
        #mobileRepeatBtn[data-repeat-mode="all"]::before {
          position:absolute;
          inset-block-start:-5px;
          inset-inline-end:-5px;
          min-width:16px;
          height:16px;
          padding:0 2px;
          border-radius:999px;
          display:grid;
          place-items:center;
          font-size:9px;
          line-height:1;
          font-weight:950;
          color:#1a1306;
          background:color-mix(in srgb, var(--ma-accent) 78%, white 22%);
          box-shadow:0 4px 10px color-mix(in srgb, var(--ma-accent) 28%, transparent), inset 0 1px 0 rgba(255,255,255,.45);
          z-index:3;
          pointer-events:none;
        }
        #mobileRepeatBtn[data-repeat-mode="one"]::before { content:"1"; }
        #mobileRepeatBtn[data-repeat-mode="all"]::before { content:"∞"; font-size:10px; }
        .card.layout-tablet .side-btn.minor-btn.active::after {
          bottom:13px;
          width:7px;
          height:7px;
        }
        .card.layout-tablet #mobileRepeatBtn[data-repeat-mode="one"]::before,
        .card.layout-tablet #mobileRepeatBtn[data-repeat-mode="all"]::before {
          inset-block-start:-4px;
          inset-inline-end:-4px;
          min-width:16px;
          height:16px;
          font-size:10px;
        }
        .side-btn.muted {
          border-color:rgba(214,76,76,.26);
          background:rgba(214,76,76,.18);
          color:#ff8f8f;
        }
        .volume-btn.active {
          color:#fff7e8;
          border-color:color-mix(in srgb, var(--ma-accent) 48%, transparent);
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 34%, transparent), color-mix(in srgb, var(--ma-accent) 18%, transparent));
          box-shadow:0 12px 28px color-mix(in srgb, var(--ma-accent) 26%, transparent);
        }
        .accent-row { display:grid; grid-template-columns:auto minmax(0,1fr) auto; gap:10px; align-items:center; padding:7px 9px; border-radius:18px; }
        .card.layout-tablet .accent-row {
          position:relative;
          inset:auto;
          transform:none;
          z-index:6;
          width:min(920px, 100%);
          min-height:0;
          grid-template-columns:auto minmax(0,1fr) auto;
          grid-template-rows:auto;
          justify-items:stretch;
          gap:12px;
          padding:10px 12px;
          border-radius:26px;
          background:rgba(14,18,28,.42);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          box-shadow:0 14px 28px rgba(0,0,0,.18);
          justify-self:center;
          align-self:start;
        }
        .theme-light .card.layout-tablet .accent-row {
          background:rgba(255,255,255,.58);
          border-color:rgba(141,155,177,.18);
          box-shadow:0 18px 34px rgba(111,126,150,.12);
        }
        .card.layout-tablet .accent-row .volume-btn {
          grid-row:auto;
          width:46px;
          height:46px;
          border-radius:999px;
          align-self:center;
        }
        .card.layout-tablet .accent-row .volume-value {
          grid-row:auto;
          min-width:54px;
          width:auto;
          text-align:end;
          font-size:14px;
          font-weight:900;
          align-self:center;
        }
        .card.layout-tablet .accent-row .tablet-volume-track {
          grid-row:auto;
          width:100%;
          height:auto;
          display:grid;
          place-items:center;
          align-self:center;
          justify-self:stretch;
        }
        .card.layout-tablet .accent-row .volume-slider {
          -webkit-appearance:none;
          appearance:none;
          writing-mode:horizontal-tb;
          direction:ltr;
          width:100%;
          height:7px;
          margin:0;
          align-self:center;
          justify-self:stretch;
          background:linear-gradient(to right,var(--ma-accent) 0%,var(--ma-accent) var(--vol-pct,50%),rgba(255,255,255,.22) var(--vol-pct,50%),rgba(255,255,255,.22) 100%);
          accent-color:var(--ma-accent);
        }
        .tablet-volume-popup {
          width:min(760px, calc(100cqi - 104px));
          border-radius:0;
          padding:0;
          background:transparent;
          border:none;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          align-items:center;
          gap:16px;
        }
        .theme-light .tablet-volume-popup {
          background:transparent;
          border:none;
          box-shadow:none;
        }
        .tablet-volume-popup-head {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:14px;
        }
        .tablet-volume-popup-title {
          font-size:20px;
          font-weight:900;
          letter-spacing:.01em;
        }
        .tablet-volume-popup-value {
          min-width:58px;
          min-height:44px;
          display:flex;
          align-items:center;
          justify-content:center;
          text-align:center;
          font-size:20px;
          font-weight:950;
          color:var(--ma-accent);
        }
        .tablet-volume-popup-body {
          display:grid;
          grid-template-columns:minmax(0,1fr) auto;
          gap:18px;
          align-items:center;
        }
        .tablet-volume-popup .volume-slider {
          width:100%;
          height:8px;
          margin:0;
          align-self:center;
          border-radius:999px;
          background:linear-gradient(to right,var(--ma-accent) 0%,var(--ma-accent) var(--vol-pct,50%),rgba(255,255,255,.16) var(--vol-pct,50%),rgba(255,255,255,.16) 100%);
        }
        .tablet-volume-popup .volume-slider::-webkit-slider-runnable-track {
          height:8px;
          border-radius:999px;
          background:transparent;
        }
        .tablet-volume-popup .volume-slider::-webkit-slider-thumb {
          width:30px;
          height:30px;
          margin-top:-11px;
          border-radius:999px;
          background:var(--ma-accent);
          border:2px solid rgba(255,255,255,.7);
          box-shadow:0 8px 18px rgba(0,0,0,.18);
        }
        .tablet-volume-popup .volume-slider::-moz-range-track {
          height:8px;
          border-radius:999px;
          background:transparent;
        }
        .tablet-volume-popup .volume-slider::-moz-range-thumb {
          width:30px;
          height:30px;
          border-radius:999px;
          background:var(--ma-accent);
          border:2px solid rgba(255,255,255,.7);
          box-shadow:0 8px 18px rgba(0,0,0,.18);
        }
        .theme-light .tablet-volume-popup .volume-slider {
          background:linear-gradient(to right,var(--ma-accent) 0%,var(--ma-accent) var(--vol-pct,50%),rgba(95,108,128,.14) var(--vol-pct,50%),rgba(95,108,128,.14) 100%);
        }
        .mobile-volume-inline {
          display:grid;
          grid-template-columns:auto minmax(0, 1fr) auto;
          align-items:center;
          gap:14px;
          width:100%;
        }
        .mobile-volume-inline .tablet-volume-track {
          min-width:0;
          width:100%;
        }
        .mobile-volume-inline .volume-value {
          min-width:52px;
          text-align:center;
        }
        .mobile-volume-inline .volume-btn {
          flex:0 0 auto;
          width:calc(var(--flow-side-btn-size) - 6px);
          height:calc(var(--flow-side-btn-size) - 6px);
        }
        .card.layout-tablet .mobile-volume-inline .volume-btn {
          width:46px;
          height:46px;
        }
        .mobile-volume-inline.has-volume-step-buttons {
          grid-template-columns:auto auto minmax(0, 1fr) auto auto;
          gap:8px;
        }
        .volume-step-btn {
          width:38px;
          min-width:38px;
          height:38px;
          display:grid;
          place-items:center;
          border:0;
          border-radius:999px;
          background:transparent;
          color:rgba(255,255,255,.9);
          cursor:pointer;
          touch-action:manipulation;
          padding:0;
        }
        .volume-step-btn:hover,
        .volume-step-btn:focus-visible {
          background:rgba(255,255,255,.11);
          outline:none;
        }
        .volume-step-btn .ui-ic {
          width:20px;
          height:20px;
        }
        .card.layout-tablet .tablet-volume-inline {
          width:min(700px, 84%);
          margin:6px auto 0;
          padding:0;
          background:transparent;
          border:none;
          box-shadow:none;
        }
        .card.layout-tablet .tablet-volume-inline .volume-btn {
          width:46px;
          height:46px;
          border-radius:999px;
        }
        .card.layout-tablet .tablet-volume-inline .volume-btn .ui-ic,
        .card.layout-tablet .mobile-volume-inline .volume-btn .ui-ic {
          width:19px;
          height:19px;
        }
        .card.layout-tablet .tablet-volume-inline .volume-value {
          min-width:60px;
          text-align:center;
        }
        .volume-value {
          min-width:46px;
          text-align:end;
          font-size:13px;
          font-weight:900;
          color:rgba(255,255,255,.82);
          flex-shrink:0;
          border:none;
          background:transparent;
          padding:0;
          cursor:pointer;
        }
        .volume-slider {
          width:100%;
          height:18px;
          appearance:none;
          direction:ltr;
          background:transparent;
          outline:none;
          accent-color:var(--ma-accent);
        }
        .volume-slider::-webkit-slider-runnable-track {
          height:5px;
          border-radius:999px;
          background:linear-gradient(to right, var(--ma-accent) 0%, var(--ma-accent) var(--vol-pct,50%), rgba(255,255,255,.16) var(--vol-pct,50%), rgba(255,255,255,.16) 100%);
          box-shadow:inset 0 1px 1px rgba(255,255,255,.04), inset 0 -1px 1px rgba(0,0,0,.18);
        }
        .volume-slider::-webkit-slider-thumb {
          appearance:none;
          width:16px;
          height:16px;
          margin-top:-5.5px;
          border-radius:50%;
          background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 76%, white 24%), color-mix(in srgb, var(--ma-accent) 88%, black 12%));
          border:2px solid rgba(255,255,255,.72);
          box-shadow:0 5px 14px rgba(0,0,0,.18);
        }
        .volume-slider::-moz-range-track {
          height:5px;
          border-radius:999px;
          background:rgba(255,255,255,.16);
          box-shadow:inset 0 1px 1px rgba(255,255,255,.04), inset 0 -1px 1px rgba(0,0,0,.18);
        }
        .volume-slider::-moz-range-progress {
          height:5px;
          border-radius:999px;
          background:linear-gradient(90deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
        }
        .volume-slider::-moz-range-thumb {
          width:16px;
          height:16px;
          border-radius:50%;
          background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 76%, white 24%), color-mix(in srgb, var(--ma-accent) 88%, black 12%));
          border:2px solid rgba(255,255,255,.72);
          box-shadow:0 5px 14px rgba(0,0,0,.18);
        }
        .footer-nav {
          display:flex;
          align-items:stretch;
          gap:10px;
          width:100%;
          margin-top:12px;
          padding:10px;
          border-radius:28px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.14);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          box-shadow:0 20px 40px rgba(0,0,0,.18);
        }
        .card.layout-tablet .footer-nav {
          width:100%;
          margin-inline:auto;
          justify-content:flex-start;
          align-items:stretch;
          flex-direction:column;
          gap:12px;
          padding:0;
          border-radius:0;
          position:relative;
          z-index:5;
          background:transparent;
          border:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          box-shadow:none;
          flex:1 1 auto;
        }
        .footer-btn {
          flex:1 1 0;
          min-width:0;
          min-height:72px;
          padding:10px 8px;
          border:none;
          border-radius:20px;
          display:grid;
          place-items:center;
          gap:6px;
          font:inherit;
          font-weight:800;
          font-size:calc(12px * var(--v2-font-scale));
          color:#fff;
          background:linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.04));
          border:1px solid rgba(255,255,255,.12);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
          cursor:pointer;
          transition:transform .18s ease, background-color .18s ease, border-color .18s ease, box-shadow .18s ease, color .18s ease;
        }
        .card.layout-tablet .footer-btn {
          flex:0 0 auto;
          min-height:78px;
          padding:10px 8px;
          border-radius:24px;
          font-size:calc(10px * var(--v2-font-scale));
          gap:7px;
        }
        .card.layout-tablet .footer-btn .ui-ic {
          width:22px;
          height:22px;
        }
        .footer-btn .ui-ic { width:22px; height:22px; }
        .footer-btn-label { white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:100%; }
        .footer-btn.accent {
          background:linear-gradient(135deg, rgba(245,166,35,.62), rgba(247,191,92,.48));
          border-color:rgba(255,203,101,.28);
          box-shadow:0 14px 28px rgba(224,161,27,.16), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .footer-btn.soft-accent {
          background:linear-gradient(135deg, rgba(245,166,35,.22), rgba(247,191,92,.14));
          border-color:rgba(245,166,35,.22);
          color:#ffcb73;
        }
        .footer-nav .footer-btn,
        .footer-nav .footer-btn.accent,
        .footer-nav .footer-btn.soft-accent {
          color:inherit;
          background:linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.04));
          border-color:rgba(255,255,255,.12);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
        }
        .footer-nav > #playersFooterBtn { grid-column:1; }
        .footer-nav > #libraryToggleBtn { grid-column:2; }
        .footer-nav > #menuToggleBtn { grid-column:3; }
        .footer-nav > #settingsFooterBtn { grid-column:4; }
        .card.layout-tablet .active-players-bubble { display:none !important; }
        .theme-light .player-kicker { color:rgba(66,79,97,.58); }
        .theme-light .player-sub,
        .theme-light .hero-sub,
        .theme-light .time-row,
        .theme-light .menu-item-sub,
        .theme-light .queue-sub,
        .theme-light .media-section-title { color:rgba(55,68,85,.68); }
        .theme-light .hero-title,
        .theme-light .menu-title,
        .theme-light .menu-item-title,
        .theme-light .queue-title,
        .theme-light .player-name,
        .theme-light .status-pill,
        .theme-light .progress-time { color:#1f2633; }
        .theme-light .card,
        .theme-light .card button,
        .theme-light .card input,
        .theme-light .card select,
        .theme-light .card textarea {
          color:#1f2633;
        }
        .theme-light .card .settings-value,
        .theme-light .card .settings-check-pill span,
        .theme-light .card .footer-btn,
        .theme-light .card .player-focus-name,
        .theme-light .card .player-focus-kicker,
        .theme-light .card .player-focus-pill,
        .theme-light .card .menu-item-sub,
        .theme-light .card .queue-sub,
        .theme-light .card .hero-sub,
        .theme-light .card .np-sub,
        .theme-light .card .empty-quick-kicker {
          color:rgba(43,54,70,.84);
        }
        .theme-light .up-next-inline {
          background:transparent;
          box-shadow:none;
        }
        .theme-light .up-next-prefix {
          color:color-mix(in srgb, var(--ma-accent) 54%, #62708a);
        }
        .theme-light .up-next-art {
          background:rgba(232,238,247,.95);
        }
        .theme-light .up-next-art-fallback {
          color:#65758d;
        }
        .theme-light .card.empty-media .hero-sub,
        .theme-light .card.empty-media .np-sub {
          color:rgba(48,58,74,.8);
        }
        .theme-light .volume-value { color:rgba(45,57,73,.76); }
        .theme-light .progress {
          background:linear-gradient(180deg, rgba(255,255,255,.4), rgba(229,236,244,.26));
          border-color:rgba(109,123,145,.12);
          box-shadow:inset 0 1px 1px rgba(255,255,255,.58), inset 0 -1px 2px rgba(102,118,140,.08);
        }
        .theme-light .side-btn,.theme-light .main-btn,.theme-light .volume-btn,.theme-light .menu-head button,.theme-light .queue-actions .chip-btn,.theme-light .action-btn {
          color:#1f2633;
          background:linear-gradient(180deg, rgba(255,255,255,.54), rgba(243,247,252,.24));
          border-color:rgba(141,155,177,.26);
          box-shadow:0 12px 26px rgba(111,126,150,.14), inset 0 1px 0 rgba(255,255,255,.62);
        }
        .theme-light .side-btn.active {
          color:#7a5210;
          border-color:color-mix(in srgb, var(--ma-accent) 46%, rgba(141,155,177,.22));
          background:
            radial-gradient(circle at 50% 28%, color-mix(in srgb, var(--ma-accent) 22%, white 78%), transparent 58%),
            linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 18%, white 82%), rgba(255,255,255,.58));
          box-shadow:0 14px 28px color-mix(in srgb, var(--ma-accent) 16%, rgba(111,126,150,.12)), inset 0 1px 0 rgba(255,255,255,.72);
        }
        .theme-light .side-btn.muted {
          border-color:rgba(214,76,76,.24);
          background:rgba(214,76,76,.12);
          color:#b13d3d;
        }
        .theme-light .volume-btn.active {
          color:#8b5e12;
          border-color:color-mix(in srgb, var(--ma-accent) 42%, transparent);
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 18%, white 82%), color-mix(in srgb, var(--ma-accent) 10%, white 90%));
        }
        .theme-light .main-btn {
          background:linear-gradient(180deg, rgba(255,255,255,.8), rgba(237,242,249,.56));
        }
        .theme-light .card.layout-tablet .main-btn.is-playing::after {
          border-color:rgba(31,38,51,.16);
          box-shadow:0 0 16px rgba(31,38,51,.08);
        }
        .theme-light .volume-slider::-webkit-slider-runnable-track {
          background:linear-gradient(to right, var(--ma-accent) 0%, var(--ma-accent) var(--vol-pct,50%), rgba(95,108,128,.16) var(--vol-pct,50%), rgba(95,108,128,.16) 100%);
          box-shadow:inset 0 1px 1px rgba(255,255,255,.55), inset 0 -1px 1px rgba(102,118,140,.08);
        }
        .theme-light .volume-slider::-webkit-slider-thumb {
          border-color:rgba(255,255,255,.94);
          box-shadow:0 5px 14px rgba(96,112,135,.18);
        }
        .theme-light .volume-slider::-moz-range-track {
          background:rgba(95,108,128,.16);
          box-shadow:inset 0 1px 1px rgba(255,255,255,.55), inset 0 -1px 1px rgba(102,118,140,.08);
        }
        .theme-light .volume-slider::-moz-range-thumb {
          border-color:rgba(255,255,255,.94);
          box-shadow:0 5px 14px rgba(96,112,135,.18);
        }
        .theme-light .footer-nav {
          background:rgba(255,255,255,.66);
          border-color:rgba(147,161,183,.18);
          box-shadow:0 16px 34px rgba(110,127,153,.14);
        }
        .theme-light .top-settings-fab,
        .theme-light .home-shortcut-fab {
          background:rgba(255,255,255,.8);
          border-color:rgba(147,161,183,.18);
          color:#1f2633;
          box-shadow:0 12px 24px rgba(111,126,150,.14);
        }
        .theme-light .footer-btn {
          color:#1f2633;
          background:linear-gradient(180deg, rgba(255,255,255,.82), rgba(247,249,252,.72));
          border-color:rgba(147,161,183,.18);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.42);
        }
        .theme-light .footer-btn.accent {
          color:#111827;
          background:linear-gradient(135deg, rgba(245,166,35,.72), rgba(247,191,92,.58));
          border-color:rgba(245,166,35,.26);
          box-shadow:0 14px 28px rgba(224,161,27,.14), inset 0 1px 0 rgba(255,255,255,.28);
        }
        .theme-light .footer-btn.soft-accent {
          color:#8b5e12;
          background:linear-gradient(135deg, rgba(245,166,35,.16), rgba(247,191,92,.12));
          border-color:rgba(245,166,35,.18);
        }
        .theme-light .footer-nav .footer-btn,
        .theme-light .footer-nav .footer-btn.accent,
        .theme-light .footer-nav .footer-btn.soft-accent {
          color:#1f2633;
          background:linear-gradient(180deg, rgba(255,255,255,.82), rgba(247,249,252,.72));
          border-color:rgba(147,161,183,.18);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.42);
        }
        .theme-light .card.layout-tablet .footer-nav .footer-btn,
        .theme-light .card.layout-tablet .footer-nav .footer-btn.accent,
        .theme-light .card.layout-tablet .footer-nav .footer-btn.soft-accent {
          background:rgba(255,255,255,.5);
          border-color:rgba(147,161,183,.16);
          box-shadow:0 10px 22px rgba(111,126,150,.1), inset 0 1px 0 rgba(255,255,255,.34);
        }
        .theme-light .card.layout-tablet .player-focus {
          background:rgba(255,255,255,.5);
          border-color:rgba(147,161,183,.16);
          box-shadow:0 16px 30px rgba(111,126,150,.1);
        }
        .theme-custom .footer-nav,
        .theme-custom .menu-sheet,
        .theme-custom .notice,
        .theme-custom .accent-row,
        .theme-custom .menu-item,
        .theme-custom .menu-list-item,
        .theme-custom .queue-row {
          background:rgba(var(--v2-custom-rgb) / .16);
          border-color:rgba(var(--v2-custom-rgb) / .22);
          box-shadow:0 16px 34px rgba(0,0,0,.14);
          color:var(--v2-custom-text, #fff);
        }
        .theme-custom .footer-btn {
          color:var(--v2-custom-text, #fff);
          background:linear-gradient(180deg, rgba(var(--v2-custom-rgb) / .2), rgba(var(--v2-custom-rgb) / .12));
          border-color:rgba(var(--v2-custom-rgb) / .2);
        }
        .theme-custom .footer-btn.accent,
        .theme-custom .footer-btn.soft-accent {
          background:linear-gradient(135deg, rgba(var(--v2-custom-rgb) / .42), rgba(var(--v2-custom-rgb) / .26));
          border-color:rgba(var(--v2-custom-rgb) / .34);
        }
        .theme-custom .footer-nav .footer-btn,
        .theme-custom .footer-nav .footer-btn.accent,
        .theme-custom .footer-nav .footer-btn.soft-accent {
          color:var(--v2-custom-text, #fff);
          background:linear-gradient(180deg, rgba(var(--v2-custom-rgb) / .2), rgba(var(--v2-custom-rgb) / .12));
          border-color:rgba(var(--v2-custom-rgb) / .22);
        }
        .theme-custom .progress-time,
        .theme-custom .menu-item-title,
        .theme-custom .queue-title,
        .theme-custom .hero-title,
        .theme-custom .hero-sub,
        .theme-custom .up-next-title,
        .theme-custom .menu-title,
        .theme-custom .volume-value,
        .theme-custom .player-focus,
        .theme-custom .footer-btn,
        .theme-custom .menu-item-ico,
        .theme-custom .media-category-ico {
          color:#fff !important;
        }
        .theme-custom .up-next-prefix,
        .theme-custom .up-next-art-fallback {
          color:var(--v2-custom-text, #fff) !important;
        }
        .theme-custom .up-next-inline {
          background:transparent;
          border-color:transparent;
        }
        .status-pill { grid-area:status; display:inline-flex; align-items:center; gap:8px; width:fit-content; max-width:100%; padding:9px 14px; border-radius:999px; font-size:12px; font-weight:700; justify-self:start; cursor:pointer; }
        .status-pill.offline { background:rgba(214,86,86,.16); border-color:rgba(214,86,86,.26); }
        .status-dot { width:8px; height:8px; border-radius:50%; background:#4bd06e; box-shadow:0 0 10px rgba(75,208,110,.42); }
        .status-pill.offline .status-dot { background:#e26b6b; box-shadow:0 0 10px rgba(226,107,107,.42); }
        .notice { display:none; padding:14px 16px; border-radius:22px; line-height:1.45; }
        .notice.open { display:block; }
        .homeii-loading-state,
        .notice.open.homeii-loading-notice {
          min-height:180px;
          display:grid;
          place-items:center;
          text-align:center;
          padding:28px 16px;
          color:var(--ma-text-2);
          background:transparent;
          border:0;
        }
        .homeii-loading-state.compact {
          min-height:120px;
        }
        .homeii-loading-content {
          display:grid;
          justify-items:center;
          gap:12px;
        }
        .homeii-loading-mark {
          position:relative;
          width:72px;
          height:72px;
          display:grid;
          place-items:center;
          filter:drop-shadow(0 18px 32px rgba(0,0,0,.20));
        }
        .homeii-loading-ring {
          position:absolute;
          inset:8px;
          border-radius:999px;
          border:2px solid rgba(255,255,255,.10);
          border-top-color:color-mix(in srgb, var(--ma-accent) 88%, #fff 8%);
          animation:homeiiLoadingSpin 1.4s linear infinite;
        }
        .homeii-loading-ring.secondary {
          inset:18px;
          opacity:.58;
          animation-duration:2.1s;
          animation-direction:reverse;
        }
        .homeii-loading-core {
          width:15px;
          height:15px;
          border-radius:999px;
          background:var(--ma-accent);
          box-shadow:0 0 26px color-mix(in srgb, var(--ma-accent) 58%, transparent);
          animation:homeiiLoadingPulse 1.2s ease-in-out infinite;
        }
        .homeii-loading-text {
          color:var(--ma-text-2);
          font-size:13px;
          font-weight:850;
        }
        @keyframes homeiiLoadingSpin { to { transform:rotate(360deg); } }
        @keyframes homeiiLoadingPulse {
          0%,100% { transform:scale(.86); opacity:.62; }
          50% { transform:scale(1.08); opacity:1; }
        }
        @media (prefers-reduced-motion: reduce) {
          .homeii-loading-ring,
          .homeii-loading-core {
            animation:none;
          }
        }
        .menu-backdrop {
          position:absolute; inset:0; z-index:30; display:none; align-items:stretch; justify-content:center;
          padding:max(30px, env(safe-area-inset-top)) max(10px, env(safe-area-inset-right)) max(12px, env(safe-area-inset-bottom)) max(10px, env(safe-area-inset-left));
          background:rgba(8,10,16,.48); backdrop-filter:blur(18px); -webkit-backdrop-filter:blur(18px);
        }
        .menu-backdrop.open { display:flex; }
        .menu-backdrop.search-open {
          align-items:stretch;
        }
        .card.layout-tablet .menu-backdrop {
          align-items:stretch;
          justify-content:flex-start;
          padding:max(14px, env(safe-area-inset-top)) 14px max(14px, env(safe-area-inset-bottom)) 14px;
        }
        .card.layout-tablet.rtl .menu-backdrop {
          justify-content:flex-start;
        }
        .queue-action-backdrop {
          position:fixed; inset:0; z-index:85; display:none; align-items:center; justify-content:center;
          padding:max(20px, env(safe-area-inset-top)) 16px max(20px, env(safe-area-inset-bottom));
          background:rgba(8,10,16,.28);
          backdrop-filter:blur(12px);
          -webkit-backdrop-filter:blur(12px);
        }
        .queue-action-backdrop.open { display:flex; }
        .queue-action-sheet {
          width:min(100%, 292px);
          max-height:min(78vh, 520px);
          overflow:auto;
          padding:12px;
          border-radius:24px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(17,19,28,.92);
          box-shadow:0 24px 56px rgba(0,0,0,.28);
          display:grid;
          gap:10px;
        }
        .queue-action-sheet.tablet-volume-sheet-host {
          width:min(760px, calc(100cqi - 72px));
          padding:0;
          border:none;
          background:transparent;
          box-shadow:none;
        }
        .theme-light .queue-action-sheet {
          background:rgba(255,255,255,.94);
          border-color:rgba(141,155,177,.22);
          box-shadow:0 18px 38px rgba(111,126,150,.18);
        }
        .queue-action-item {
          min-height:56px;
          border:none;
          border-radius:18px;
          display:flex;
          align-items:center;
          justify-content:center;
          gap:10px;
          padding:12px 14px;
          color:inherit;
          background:transparent;
          font:inherit;
          font-size:14px;
          font-weight:800;
          cursor:pointer;
          text-align:center;
        }
        .queue-action-item.compact {
          min-height:46px;
          border-radius:15px;
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.12);
        }
        .queue-move-control {
          display:grid;
          grid-template-columns:minmax(0,1fr) auto;
          gap:8px;
          align-items:end;
          padding:8px;
          border-radius:18px;
          background:rgba(255,255,255,.055);
          border:1px solid rgba(255,255,255,.1);
        }
        .queue-move-control label,
        .queue-inline-move {
          min-width:0;
          display:grid;
          gap:5px;
          color:rgba(255,255,255,.72);
          font-size:10px;
          font-weight:850;
        }
        .queue-move-control input,
        .queue-inline-move input {
          width:100%;
          min-width:0;
          height:38px;
          border-radius:13px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(255,255,255,.08);
          color:inherit;
          text-align:center;
          font:inherit;
          font-size:14px;
          font-weight:900;
          outline:none;
        }
        .theme-light .queue-move-control {
          background:rgba(31,38,51,.045);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .queue-move-control label,
        .theme-light .queue-inline-move {
          color:#5b687c;
        }
        .theme-light .queue-move-control input,
        .theme-light .queue-inline-move input {
          background:rgba(255,255,255,.88);
          border-color:rgba(147,161,183,.24);
          color:#172033;
        }
        .queue-action-item:hover {
          background:rgba(255,255,255,.06);
          transform:translateY(-1px);
        }
        .theme-light .queue-action-item:hover {
          background:rgba(31,38,51,.06);
        }
        .queue-action-item .ui-ic {
          width:16px;
          height:16px;
        }
        .queue-action-item.warn {
          color:#ffcf86;
        }
        .queue-action-item:not(.warn) .ui-ic,
        .queue-action-item:not(.warn) {
          color:var(--ma-accent);
        }
        .queue-action-header {
          display:grid;
          gap:4px;
          padding:8px 10px 10px;
          text-align:center;
          border-bottom:1px solid rgba(255,255,255,.08);
          margin-bottom:4px;
        }
        .queue-action-brand,
        .smart-voice-brand {
          width:118px;
          max-width:56%;
          margin-inline:auto;
          color:rgba(255,255,255,.68);
          opacity:.96;
          display:grid;
          place-items:center;
        }
        .theme-light .queue-action-header {
          border-bottom-color:rgba(141,155,177,.16);
        }
        .theme-light .queue-action-brand,
        .theme-light .smart-voice-brand {
          color:rgba(31,38,51,.48);
        }
        .queue-action-player {
          font-size:11px;
          font-weight:900;
          letter-spacing:.08em;
          text-transform:uppercase;
          color:rgba(255,255,255,.56);
        }
        .theme-light .queue-action-player {
          color:rgba(55,68,85,.54);
        }
        .queue-action-title {
          font-size:16px;
          font-weight:900;
          line-height:1.2;
          color:inherit;
        }
        .confirm-sheet {
          width:min(100%, 460px);
          max-width:calc(100% - 28px);
          min-height:0;
          padding:24px;
        }
        .confirm-copy {
          color:var(--muted);
          line-height:1.75;
          font-size:15px;
        }
        .confirm-actions {
          display:grid;
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:12px;
          margin-top:20px;
        }
        .confirm-actions .menu-item {
          min-height:54px;
          justify-content:center;
          text-align:center;
        }
        .clean-all-confirm-backdrop {
          position:absolute;
          z-index:92;
          background:rgba(8,10,16,.24);
          backdrop-filter:blur(10px);
          -webkit-backdrop-filter:blur(10px);
        }
        .clean-all-confirm-sheet {
          position:relative;
          width:min(420px, calc(100vw - 36px));
          max-height:none;
          padding:18px;
          gap:16px;
          border-radius:26px;
          background:rgba(18,21,30,.9);
          box-shadow:0 22px 60px rgba(0,0,0,.3);
        }
        .clean-all-confirm-head {
          display:grid;
          grid-template-columns:48px minmax(0, 1fr);
          gap:14px;
          align-items:start;
          padding-inline-end:40px;
        }
        .clean-all-confirm-icon {
          width:48px;
          height:48px;
          border-radius:18px;
          display:grid;
          place-items:center;
          background:linear-gradient(145deg, rgba(255,85,95,.2), rgba(255,255,255,.06));
          border:1px solid rgba(255,105,115,.24);
        }
        .clean-all-confirm-icon .ui-ic {
          width:22px;
          height:22px;
        }
        .clean-all-confirm-title {
          font-size:18px;
          font-weight:950;
          line-height:1.2;
          color:#fff;
          margin-block-end:6px;
        }
        .clean-all-confirm-close {
          position:absolute;
          inset-block-start:12px;
          inset-inline-end:12px;
          width:34px;
          height:34px;
          border-radius:14px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.08);
          color:#fff;
          font-size:22px;
          line-height:1;
          cursor:pointer;
        }
        .confirm-actions.clean-all-confirm-actions {
          margin-top:0;
        }
        .clean-all-confirm-actions .menu-item {
          min-height:48px;
          border-radius:18px;
        }
        .confirm-actions .menu-item.danger-confirm-action {
          color:#ffd9d9;
          background:linear-gradient(145deg, rgba(255,85,95,.24), rgba(255,85,95,.1));
          border:1px solid rgba(255,105,115,.3);
        }
        .danger-confirm-icon {
          color:#ffb6b6;
        }
        .theme-light .confirm-actions .menu-item.danger-confirm-action {
          color:#b4232b;
          background:#fff1f2;
          border-color:rgba(180,35,43,.18);
        }
        .theme-light .clean-all-confirm-sheet {
          background:rgba(255,255,255,.94);
          border-color:rgba(141,155,177,.22);
          box-shadow:0 18px 44px rgba(111,126,150,.2);
        }
        .theme-light .clean-all-confirm-title {
          color:#16202d;
        }
        .theme-light .clean-all-confirm-close {
          color:#16202d;
          background:rgba(31,38,51,.06);
          border-color:rgba(31,38,51,.1);
        }
        .theme-light .danger-confirm-icon {
          color:#b4232b;
        }
        .smart-voice-sheet {
          width:min(100%, 520px);
          gap:18px;
        }
        .smart-voice-head {
          display:grid;
          gap:6px;
          text-align:center;
        }
        .smart-voice-title {
          font-size:22px;
          font-weight:950;
        }
        .smart-voice-target {
          color:var(--muted);
          font-size:13px;
          font-weight:800;
        }
        .smart-voice-card {
          display:grid;
          gap:12px;
          padding:18px;
          border-radius:24px;
          background:rgba(255,255,255,.06);
          border:1px solid rgba(255,255,255,.1);
          text-align:center;
        }
        .theme-light .smart-voice-card {
          background:rgba(255,255,255,.82);
          border-color:rgba(147,161,183,.18);
        }
        .smart-voice-chip {
          width:fit-content;
          max-width:100%;
          margin:0 auto;
          display:inline-flex;
          align-items:center;
          gap:8px;
          padding:8px 14px;
          border-radius:999px;
          background:color-mix(in srgb, var(--ma-accent) 14%, rgba(255,255,255,.08));
          color:var(--ma-accent);
          font-size:12px;
          font-weight:900;
        }
        .smart-voice-chip .ui-ic { width:16px; height:16px; }
        .smart-voice-name {
          font-size:24px;
          font-weight:950;
          line-height:1.15;
        }
        .smart-voice-sub {
          color:var(--muted);
          font-size:14px;
          line-height:1.5;
        }
        .smart-voice-countdown {
          width:72px;
          height:72px;
          margin:6px auto 0;
          border-radius:50%;
          display:grid;
          place-items:center;
          background:color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.12));
          color:var(--ma-accent);
          font-size:28px;
          font-weight:950;
          box-shadow:0 18px 34px rgba(0,0,0,.14);
        }
        .smart-voice-actions {
          grid-template-columns:repeat(3, minmax(0, 1fr));
        }
        .voice-assistant-dialog {
          position:absolute;
          inset:0;
          z-index:86;
          display:none;
          align-items:center;
          justify-content:center;
          padding:20px;
          pointer-events:none;
          background:rgba(2,6,14,.16);
          backdrop-filter:blur(8px) saturate(1.08);
          -webkit-backdrop-filter:blur(8px) saturate(1.08);
        }
        .voice-assistant-dialog.open {
          display:flex;
        }
        .voice-assistant-dialog.keep-screensaver {
          z-index:96;
          background:transparent;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
        }
        :host(.screensaver-page-open) .voice-assistant-dialog.keep-screensaver {
          position:fixed !important;
          inset:0 !important;
          width:100vw !important;
          height:100dvh !important;
          z-index:2147483202 !important;
          display:flex;
          padding:max(18px, env(safe-area-inset-top)) max(18px, env(safe-area-inset-right)) max(18px, env(safe-area-inset-bottom)) max(18px, env(safe-area-inset-left));
          pointer-events:none;
          background:transparent;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
        }
        :host(.screensaver-page-open) .voice-assistant-dialog.keep-screensaver .voice-assistant-panel {
          pointer-events:auto;
        }
        .voice-assistant-panel {
          position:relative;
          width:min(560px, calc(100% - 18px));
          display:grid;
          gap:18px;
          padding:22px;
          border-radius:34px;
          overflow:hidden;
          border:1px solid rgba(255,255,255,.18);
          background:
            linear-gradient(145deg, rgba(255,255,255,.16), rgba(255,255,255,.045) 42%, rgba(255,255,255,.08)),
            radial-gradient(circle at 18% 0%, color-mix(in srgb, var(--ma-accent) 22%, transparent), transparent 42%),
            rgba(8,12,22,.78);
          box-shadow:0 34px 92px rgba(0,0,0,.46), inset 0 1px 0 rgba(255,255,255,.16);
          backdrop-filter:blur(30px) saturate(1.18);
          -webkit-backdrop-filter:blur(30px) saturate(1.18);
          pointer-events:auto;
          color:#fff;
          animation:voiceAssistantPanelIn .26s cubic-bezier(.2,.85,.22,1) both;
          transform-origin:center;
        }
        .voice-assistant-dialog.keep-screensaver .voice-assistant-panel {
          width:min(520px, calc(100% - 28px));
          background:
            linear-gradient(145deg, rgba(255,255,255,.13), rgba(255,255,255,.035) 44%, rgba(255,255,255,.065)),
            radial-gradient(circle at 18% 0%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 42%),
            rgba(8,12,22,.66);
          box-shadow:0 26px 72px rgba(0,0,0,.38), inset 0 1px 0 rgba(255,255,255,.14);
        }
        .voice-assistant-panel::before {
          content:"";
          position:absolute;
          inset:0;
          pointer-events:none;
          background:linear-gradient(120deg, rgba(255,255,255,.18), transparent 28%, transparent 68%, rgba(255,255,255,.08));
          opacity:.42;
        }
        .voice-assistant-panel > * {
          position:relative;
          z-index:1;
        }
        @keyframes voiceAssistantPanelIn {
          from { opacity:0; transform:translateY(18px) scale(.96); filter:blur(8px); }
          to { opacity:1; transform:translateY(0) scale(1); filter:blur(0); }
        }
        .theme-light .voice-assistant-panel {
          background:
            linear-gradient(145deg, rgba(255,255,255,.94), rgba(255,255,255,.74)),
            radial-gradient(circle at 18% 0%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 42%);
          border-color:rgba(105,119,142,.18);
          color:#17202d;
          box-shadow:0 28px 76px rgba(86,103,127,.26), inset 0 1px 0 rgba(255,255,255,.78);
        }
        .theme-light .voice-assistant-brand {
          color:rgba(31,38,51,.42);
        }
        .voice-assistant-head {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
        }
        .voice-assistant-title-row {
          min-width:0;
          display:flex;
          align-items:center;
          gap:12px;
        }
        .voice-assistant-copy {
          min-width:0;
          display:grid;
          gap:3px;
        }
        .voice-assistant-brand {
          width:94px;
          max-width:42vw;
          color:rgba(255,255,255,.58);
          opacity:.9;
          display:block;
          pointer-events:none;
        }
        .voice-assistant-icon {
          width:58px;
          height:58px;
          border-radius:24px;
          display:grid;
          place-items:center;
          color:var(--ma-accent);
          border:1px solid color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.16));
          background:
            linear-gradient(145deg, rgba(255,255,255,.18), rgba(255,255,255,.055)),
            color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.08));
          box-shadow:0 18px 38px color-mix(in srgb, var(--ma-accent) 16%, transparent), inset 0 1px 0 rgba(255,255,255,.16);
          transition:transform .2s ease, background-color .2s ease, box-shadow .2s ease;
        }
        .voice-assistant-dialog.status-listening .voice-assistant-icon {
          animation:voiceAssistantIconPulse 1.25s ease-in-out infinite;
        }
        .voice-assistant-dialog.status-success .voice-assistant-icon {
          transform:scale(1.05);
          color:#c9ffdf;
          background:rgba(73,214,127,.18);
          box-shadow:0 16px 32px rgba(73,214,127,.16);
        }
        .voice-assistant-dialog.status-error .voice-assistant-icon {
          color:#ffb7b7;
          background:rgba(255,107,107,.16);
          box-shadow:0 16px 32px rgba(255,107,107,.14);
        }
        @keyframes voiceAssistantIconPulse {
          0%,100% { transform:scale(1); box-shadow:0 18px 38px color-mix(in srgb, var(--ma-accent) 16%, transparent), inset 0 1px 0 rgba(255,255,255,.16); }
          50% { transform:scale(1.06); box-shadow:0 0 0 12px color-mix(in srgb, var(--ma-accent) 10%, transparent), 0 24px 48px color-mix(in srgb, var(--ma-accent) 20%, transparent), inset 0 1px 0 rgba(255,255,255,.18); }
        }
        .voice-assistant-icon .ui-ic { width:25px; height:25px; }
        .voice-assistant-title {
          display:block;
          font-size:22px;
          font-weight:950;
          line-height:1.1;
          letter-spacing:0;
        }
        .voice-assistant-status {
          display:block;
          margin-top:4px;
          color:rgba(255,255,255,.66);
          font-size:12px;
          font-weight:850;
        }
        .theme-light .voice-assistant-status { color:rgba(36,45,58,.64); }
        .voice-assistant-close {
          width:42px;
          height:42px;
          border-radius:16px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(255,255,255,.08);
          color:inherit;
          display:grid;
          place-items:center;
          cursor:pointer;
        }
        .theme-light .voice-assistant-close {
          background:rgba(255,255,255,.72);
          border-color:rgba(105,119,142,.16);
        }
        .voice-assistant-close .ui-ic { width:19px; height:19px; }
        .voice-assistant-meter {
          height:5px;
          border-radius:999px;
          overflow:hidden;
          background:rgba(255,255,255,.095);
        }
        .voice-assistant-meter span {
          display:block;
          width:42%;
          height:100%;
          border-radius:999px;
          background:linear-gradient(90deg, transparent, var(--ma-accent), transparent);
          animation:voiceAssistantMeter 1.2s ease-in-out infinite;
        }
        .voice-assistant-dialog.status-success .voice-assistant-meter span,
        .voice-assistant-dialog.status-error .voice-assistant-meter span {
          width:100%;
          animation:none;
          background:var(--ma-accent);
        }
        .voice-assistant-dialog.status-error .voice-assistant-meter span {
          background:#ff6b6b;
        }
        @keyframes voiceAssistantMeter {
          0% { transform:translateX(-110%); }
          100% { transform:translateX(260%); }
        }
        .voice-assistant-wave {
          display:flex;
          align-items:center;
          justify-content:center;
          gap:6px;
          width:max-content;
          min-width:118px;
          height:38px;
          margin:0 auto;
          padding:0 16px;
          border-radius:999px;
          background:rgba(255,255,255,.065);
          border:1px solid rgba(255,255,255,.08);
        }
        .voice-assistant-wave span {
          width:5px;
          height:10px;
          border-radius:999px;
          background:color-mix(in srgb, var(--ma-accent) 72%, white 28%);
          opacity:.72;
          transform-origin:center;
          animation:voiceAssistantWave 1s ease-in-out infinite;
        }
        .voice-assistant-wave span:nth-child(2) { animation-delay:.1s; }
        .voice-assistant-wave span:nth-child(3) { animation-delay:.2s; }
        .voice-assistant-wave span:nth-child(4) { animation-delay:.3s; }
        .voice-assistant-wave span:nth-child(5) { animation-delay:.4s; }
        .voice-assistant-dialog.status-processing .voice-assistant-wave span {
          animation-duration:.72s;
        }
        .voice-assistant-dialog.status-success .voice-assistant-wave span,
        .voice-assistant-dialog.status-error .voice-assistant-wave span {
          animation:none;
          height:8px;
          opacity:.36;
        }
        @keyframes voiceAssistantWave {
          0%,100% { transform:scaleY(.62); opacity:.42; }
          50% { transform:scaleY(2.25); opacity:1; }
        }
        .voice-assistant-lines {
          display:grid;
          gap:10px;
        }
        .voice-assistant-line {
          display:grid;
          gap:6px;
          padding:13px 15px;
          border-radius:20px;
          background:rgba(255,255,255,.062);
          border:1px solid rgba(255,255,255,.085);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.045);
        }
        .theme-light .voice-assistant-line {
          background:rgba(255,255,255,.72);
          border-color:rgba(105,119,142,.12);
        }
        .voice-assistant-line-label {
          color:rgba(255,255,255,.56);
          font-size:11px;
          font-weight:950;
          text-transform:uppercase;
        }
        .theme-light .voice-assistant-line-label { color:rgba(36,45,58,.52); }
        .voice-assistant-line-text {
          min-height:22px;
          font-size:15px;
          font-weight:850;
          line-height:1.45;
          overflow-wrap:anywhere;
        }
        .voice-assistant-placeholder {
          color:rgba(255,255,255,.45);
        }
        .theme-light .voice-assistant-placeholder { color:rgba(36,45,58,.42); }
        .voice-assistant-actions {
          display:flex;
          gap:10px;
          justify-content:flex-end;
          flex-wrap:wrap;
        }
        .voice-assistant-actions button {
          min-height:42px;
          padding:0 15px;
          border-radius:14px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.08);
          color:inherit;
          font-weight:900;
          cursor:pointer;
        }
        .voice-assistant-actions button[hidden] {
          display:none !important;
        }
        .voice-assistant-actions .primary {
          color:#17120a;
          border-color:transparent;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
        }
        .menu-sheet {
          width:min(100%, 720px);
          max-height:calc(100% - 8px);
          margin-top:auto;
          position:relative;
          display:grid;
          grid-template-rows:auto minmax(0,1fr);
          overflow:hidden;
          border-radius:30px;
          box-shadow:0 24px 60px rgba(0,0,0,.34);
        }
        .action-grid {
          display:grid;
          grid-template-columns:minmax(0,1fr);
          gap:12px;
          align-content:start;
          width:100%;
        }
        .menu-sheet.sheet-actions,
        .menu-sheet.sheet-simple {
          height:calc(100% - 8px);
          max-height:calc(100% - 8px);
          margin-top:0;
        }
        .menu-sheet.sheet-media-detail {
          width:min(calc(100% - 20px), 720px);
          height:min(760px, calc(100% - 34px));
          max-height:calc(100% - 34px);
          align-self:center;
          margin:auto;
          border-radius:28px;
        }
        .menu-sheet.sheet-artist-detail {
          width:min(100%, 920px);
          height:calc(100% - 8px);
          max-height:calc(100% - 8px);
          margin-top:0;
        }
        .menu-body.sheet-media-detail {
          padding:14px;
          overflow:hidden;
        }
        .menu-body.sheet-artist-detail {
          padding:14px;
          overflow:auto;
        }
        .menu-sheet.sheet-schedules {
          height:calc(100% - 8px);
          max-height:calc(100% - 8px);
          margin-top:0;
        }
        .menu-body.sheet-actions,
        .menu-body.sheet-simple {
          padding:16px 14px 20px;
        }
        .menu-body.sheet-schedules {
          min-height:0;
          padding:16px 14px 20px;
          overflow:auto;
        }
        .menu-body.sheet-schedules .settings-shell {
          min-height:0;
          height:100%;
          display:grid;
          grid-template-rows:auto minmax(0,1fr);
          align-content:start;
          gap:14px;
        }
        .menu-body.sheet-schedules .settings-shell > .settings-group,
        .menu-body.sheet-schedules .settings-shell > .scheduled-start-card {
          align-self:start;
        }
        .menu-body.sheet-actions .menu-item {
          min-height:112px;
        }
        .menu-backdrop.search-open .menu-sheet {
          height:calc(100% - 8px);
          max-height:calc(100% - 8px);
          margin-top:0;
        }
        .card.layout-tablet .menu-sheet {
          width:min(calc(100% - 72px), 920px);
          max-width:min(calc(100% - 72px), 920px);
          max-height:calc(100% - 18px);
          height:calc(100% - 18px);
          margin-inline:auto;
          border-radius:28px;
          box-shadow:0 30px 70px rgba(0,0,0,.3);
        }
        .card.layout-tablet .menu-sheet.sheet-library,
        .card.layout-tablet .menu-sheet.sheet-search {
          width:min(calc(100% - 72px), 1120px);
          max-width:min(calc(100% - 72px), 1120px);
        }
        .card.layout-tablet .menu-sheet.sheet-actions,
        .card.layout-tablet .menu-sheet.sheet-simple,
        .card.layout-tablet .menu-sheet.sheet-schedules,
        .card.layout-tablet .menu-sheet.sheet-transfer,
        .card.layout-tablet .menu-sheet.sheet-announcements,
        .card.layout-tablet .menu-sheet.sheet-settings {
          width:min(calc(100% - 120px), 760px);
          max-width:min(calc(100% - 120px), 760px);
        }
        .card.layout-tablet .menu-sheet.sheet-players,
        .card.layout-tablet .menu-sheet.sheet-group {
          width:min(calc(100% - 100px), 900px);
          max-width:min(calc(100% - 100px), 900px);
        }
        .card.layout-tablet .menu-sheet.sheet-media-detail {
          width:min(calc(100% - 120px), 860px);
          max-width:min(calc(100% - 120px), 860px);
          height:min(820px, calc(100% - 46px));
          max-height:calc(100% - 46px);
          align-self:center;
        }
        .card.layout-tablet .menu-sheet.sheet-artist-detail {
          width:min(calc(100% - 72px), 1120px);
          max-width:min(calc(100% - 72px), 1120px);
        }
        .card.layout-tablet .menu-sheet.sheet-queue {
          width:min(calc(100% - 120px), 840px);
          max-width:min(calc(100% - 120px), 840px);
        }
        .card.layout-tablet .menu-sheet.confirm-sheet,
        .card.layout-tablet .menu-sheet.smart-voice-sheet {
          width:min(620px, calc(100% - 80px));
          max-width:min(620px, calc(100% - 80px));
          height:auto;
          max-height:min(760px, calc(100% - 48px));
          align-self:center;
          margin-inline:auto;
        }
        .menu-head { position:relative; display:grid; grid-template-columns:52px minmax(0,1fr) 52px; gap:10px; align-items:center; padding:18px 16px 14px; border-bottom:1px solid rgba(255,255,255,.08); }
        .card.layout-tablet .menu-head {
          grid-template-columns:48px minmax(0,1fr) 48px;
          gap:12px;
          padding:12px 14px 10px;
        }
        .menu-head button { width:44px; height:44px; border-radius:16px; font-size:22px; }
        .card.layout-tablet .menu-head button {
          width:42px;
          height:42px;
          border-radius:14px;
        }
        .card.layout-tablet .menu-backdrop {
          justify-content:center;
          align-items:stretch;
          padding:14px 18px;
        }
        .card.layout-tablet .players-premium-grid {
          direction:ltr;
          grid-template-columns:repeat(2,minmax(0,1fr));
          gap:16px;
        }
        .card.layout-tablet.rtl .players-premium-grid {
          direction:rtl;
        }
        .card.layout-tablet .player-menu-card {
          min-height:144px;
          padding:18px;
          gap:14px;
          border-radius:24px;
          background:rgba(255,255,255,.10);
          border:1px solid rgba(255,255,255,.16);
          backdrop-filter:blur(24px);
          -webkit-backdrop-filter:blur(24px);
          box-shadow:0 24px 48px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.12);
          text-align:start;
        }
        .card.layout-tablet.rtl .player-menu-card {
          direction:rtl;
          text-align:right;
        }
        .card.layout-tablet .player-premium-head {
          display:grid;
          grid-template-columns:64px minmax(0,1fr);
          gap:14px;
          align-items:center;
        }
        .card.layout-tablet.rtl .player-premium-head {
          grid-template-columns:minmax(0,1fr) 64px;
        }
        .card.layout-tablet .player-premium-art {
          width:58px;
          height:58px;
          border-radius:18px;
          overflow:hidden;
          background:rgba(255,255,255,.08);
        }
        .card.layout-tablet .player-premium-text {
          min-width:0;
        }
        .card.layout-tablet .player-premium-sub {
          margin-top:4px;
          opacity:.8;
        }
        .card.layout-tablet .player-volume-row {
          display:grid;
          grid-template-columns:44px minmax(0,1fr);
          gap:12px;
          align-items:center;
          padding-top:4px;
        }
        .card.layout-tablet.rtl .player-volume-row {
          grid-template-columns:minmax(0,1fr) 44px;
        }
        .card.layout-tablet .queue-list {
          width:min(100%, 780px);
          margin-inline:auto;
        }
        .card.layout-tablet .queue-row {
          min-height:78px;
          padding-inline:16px;
        }
        .card.layout-tablet #activePlayersBubble {
          display:none !important;
        }
        .card.layout-tablet .player-focus {
          min-height:122px;
          padding:16px 14px;
          border-radius:28px;
          display:grid;
          gap:10px;
          justify-items:center;
          background:rgba(255,255,255,.12);
          border:1px solid rgba(255,255,255,.18);
          backdrop-filter:blur(28px);
          -webkit-backdrop-filter:blur(28px);
          box-shadow:0 22px 40px rgba(0,0,0,.12), inset 0 1px 0 rgba(255,255,255,.12);
        }
        .card.empty-media .mobile-art-shell {
          width:min(174px, 38vw);
          padding:0;
          border-radius:999px;
          background:transparent;
          border:none;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
        }
        .card.empty-media .art-stack-view,
        .card.empty-media .art-stack-viewport,
        .card.empty-media .art-stack-container {
          height:170px;
          min-height:170px;
          max-height:170px;
        }
        .card.empty-media .center {
          gap:18px;
        }
        .card.empty-media .art-stage {
          min-height:0;
          display:grid;
          place-items:center;
        }
        .card.empty-media .hero-copy {
          margin-top:12px;
          gap:12px;
        }
        .card.empty-media .mobile-art-actions:not(.empty-quick-actions),
        .card.empty-media #mobileArtActions:not(.empty-quick-actions) {
          display:none !important;
        }
        .card.empty-media .mobile-art-actions.empty-quick-actions,
        .card.empty-media #mobileArtActions.empty-quick-actions {
          display:flex !important;
          justify-content:center;
          margin-top:0;
        }
        .card.empty-media .hero-title {
          max-width:min(900px, 100%);
          margin-inline:auto;
          font-size:clamp(28px, 4vw, 56px);
          line-height:1.06;
          letter-spacing:-.04em;
        }
        .card.empty-media .hero-sub {
          max-width:min(620px, 100%);
          margin-inline:auto;
          font-size:clamp(13px, 1.45vw, 17px);
          line-height:1.5;
          color:rgba(236,241,248,.76);
        }
        .card.empty-media .bottom {
          gap:22px;
        }
        .card.empty-media .progress-line {
          display:none;
        }
        .card.layout-tablet.empty-media .hero-split-shell {
          grid-template-columns:minmax(220px, 320px) minmax(0, 1fr) !important;
          align-items:center;
          justify-items:center;
          gap:clamp(46px, 5cqi, 92px);
          padding-left:clamp(42px, 4.4cqi, 84px);
        }
        .card.layout-tablet.empty-media .hero-visual {
          display:grid !important;
          justify-self:start;
          width:min(286px, 28vw);
          min-width:220px;
        }
        .card.layout-tablet.empty-media .art-stage {
          display:grid !important;
          place-items:center;
          width:100%;
          min-height:0;
          padding:0;
        }
        .card.layout-tablet.empty-media .mobile-art-shell {
          display:grid !important;
          place-items:center;
          width:168px;
          height:168px;
          padding:0;
          border-radius:999px;
          background:transparent;
          border:none;
          box-shadow:none;
          overflow:visible;
        }
        .card.layout-tablet.empty-media .empty-magic-stack.has-voice {
          padding-inline-end:clamp(52px, 4.2cqi, 70px);
        }
        .card.layout-tablet.empty-media .empty-voice-btn {
          inset-inline-end:0;
          inset-block-end:50%;
          transform:translate(72%, 50%);
        }
        .card.layout-tablet.empty-media .empty-voice-btn.pressed,
        .card.layout-tablet.empty-media .empty-voice-btn.busy {
          transform:translate(72%, 50%) scale(.94);
        }
        .card.layout-tablet.empty-media .art-stack-view {
          display:grid !important;
          place-items:center;
          width:168px;
          min-height:168px;
          background:transparent;
          border:none;
          box-shadow:none;
          overflow:visible;
        }
        .card.layout-tablet.empty-media .art-stack-viewport,
        .card.layout-tablet.empty-media .art-stack-container {
          display:contents !important;
          min-height:0 !important;
        }
        .card.layout-tablet.empty-media .hero-info {
          max-width:min(820px, 100%);
          align-items:center;
          text-align:center;
        }
        .card.layout-tablet.empty-media .empty-quick-shelf {
          width:min(1040px, max(var(--empty-quick-two-card-min), calc(100% - clamp(320px, 24cqi, 440px))));
          margin-left:clamp(300px, 22cqi, 420px);
          margin-right:auto;
          padding-inline:max(var(--empty-quick-edge-fade), calc(50% - 452px));
          scroll-padding-inline:max(var(--empty-quick-edge-fade), calc(50% - 452px));
        }
        .card.layout-tablet.empty-media .empty-quick-card {
          scroll-snap-align:start;
        }
        .card.radio-media .empty-quick-shelf {
          display:none !important;
        }
        .card.radio-media.empty-media .mobile-art-shell {
          width:min(840px, 100%);
          padding:14px 14px 10px;
          border-radius:42px;
        }
        .card.radio-media.empty-media .art-stack-view {
          height:clamp(246px, 26cqi, 356px);
          min-height:clamp(246px, 26cqi, 356px);
          max-height:clamp(246px, 26cqi, 356px);
          display:grid;
          place-items:center;
        }
        .card.layout-tablet.radio-media.empty-media .mobile-art-shell {
          width:min(520px, 56cqi);
          height:min(520px, 56cqi);
          border-radius:var(--ma-radius-xl);
        }
        .card.layout-tablet.radio-media.empty-media .art-stack-view,
        .card.layout-tablet.radio-media.empty-media .art-stack-viewport,
        .card.layout-tablet.radio-media.empty-media .art-stack-container {
          width:100%;
          height:100%;
          min-height:100%;
          max-height:100%;
          display:grid !important;
          place-items:center;
        }
        .radio-stage {
          position:relative;
          width:min(100%, 760px);
          min-height:clamp(230px, 24cqi, 330px);
          display:grid;
          place-items:center;
        }
        .radio-stage-card {
          position:absolute;
          inset-block-start:50%;
          transform:translateY(-50%);
          border-radius:36px;
          background:rgba(255,255,255,.28);
          border:1px solid rgba(255,255,255,.22);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          box-shadow:0 18px 34px rgba(0,0,0,.10);
        }
        .radio-stage-card-main {
          width:min(360px, 44vw);
          aspect-ratio:1/1;
          z-index:2;
        }
        .radio-stage-card-side {
          width:min(248px, 28vw);
          aspect-ratio:1/1;
          inset-inline-start:12%;
          opacity:.42;
          filter:saturate(.82);
        }
        .radio-stage-card-side-end {
          inset-inline-start:auto;
          inset-inline-end:12%;
        }
        .radio-stage-fab {
          position:relative;
          z-index:3;
          width:152px;
          height:152px;
          border:none;
          border-radius:999px;
          display:grid;
          place-items:center;
          color:#fff8df;
          background:linear-gradient(180deg, rgba(66,63,55,.72), rgba(114,106,84,.52));
          box-shadow:0 26px 46px rgba(0,0,0,.18), 0 0 30px color-mix(in srgb, var(--ma-accent) 18%, transparent);
          cursor:pointer;
        }
        .radio-stage-fab .ui-ic {
          width:42px;
          height:42px;
        }
        .theme-light .radio-stage-card {
          background:rgba(255,255,255,.34);
          border-color:rgba(141,155,177,.18);
          box-shadow:0 18px 34px rgba(111,126,150,.12);
        }
        .theme-light .radio-stage-fab {
          color:#fffaf0;
          background:linear-gradient(180deg, rgba(112,106,85,.68), rgba(157,145,106,.48));
          box-shadow:0 22px 38px rgba(111,126,150,.14), 0 0 26px color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .card.layout-tablet .player-focus-name {
          font-size:13px;
          font-weight:900;
          text-align:center;
          line-height:1.3;
        }
        .card.layout-tablet .player-focus-art-wrap {
          width:60px;
          height:60px;
          display:grid;
          place-items:center;
        }
        .card.layout-tablet .player-focus-art {
          width:60px;
          height:60px;
          border-radius:999px;
          overflow:hidden;
          background-size:cover;
          background-position:center;
          box-shadow:0 12px 28px rgba(0,0,0,.18);
        }
        .card.layout-tablet .player-focus.playing .player-focus-art {
          animation:playerFocusPulse 3s ease-in-out infinite;
        }
        @keyframes playerFocusPulse {
          0%,100% { transform:scale(1); box-shadow:0 12px 28px rgba(0,0,0,.18); }
          50% { transform:scale(1.04); box-shadow:0 18px 32px rgba(245,166,35,.22); }
        }
        .card:not(.layout-tablet) .side-btn,
        .card:not(.layout-tablet) .side-btn.minor-btn,
        .card:not(.layout-tablet) .volume-btn {
          border-radius:999px;
        }
        .card:not(.layout-tablet) .mobile-volume-inline {
          grid-template-columns:auto minmax(0,1fr) auto;
          gap:10px;
          align-items:center;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons {
          grid-template-columns:auto auto minmax(0,1fr) auto auto;
          gap:8px;
        }
        .card:not(.layout-tablet) .mobile-volume-inline .volume-value {
          order:3;
          min-width:46px;
          text-align:center;
        }
        .card:not(.layout-tablet) .mobile-volume-inline .tablet-volume-track {
          order:2;
        }
        .card:not(.layout-tablet) .mobile-volume-inline .volume-btn {
          order:1;
          width:42px;
          height:42px;
          border-radius:999px;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-value {
          order:1;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-step-minus {
          order:2;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .tablet-volume-track {
          order:3;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-step-plus {
          order:4;
        }
        .card:not(.layout-tablet) .mobile-volume-inline.has-volume-step-buttons .volume-btn {
          order:5;
        }
        .card:not(.layout-tablet) .mobile-volume-inline .volume-btn.active {
          background:rgba(255,69,58,.24);
          border-color:rgba(255,69,58,.34);
          box-shadow:0 12px 22px rgba(255,69,58,.12), inset 0 1px 0 rgba(255,255,255,.16);
          color:#ff6c63;
        }
        .mobile-art-actions {
          inset-inline:auto 18px;
          inset-block-end:18px;
          display:flex;
          gap:10px;
          padding:0;
          background:transparent;
          border:none;
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
        }
        .card.layout-tablet .mobile-art-actions {
          inset-inline:auto 20px;
          inset-block-end:18px;
        }
        .mobile-art-fab {
          width:46px;
          min-width:46px;
          height:46px;
          border-radius:999px;
          background:rgba(14,18,28,.34);
          border:1px solid rgba(255,255,255,.16);
          box-shadow:0 12px 24px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .card.layout-tablet .mobile-art-fab {
          width:44px;
          min-width:44px;
          height:44px;
        }
        .art-stack-fallback {
          background:radial-gradient(circle at 50% 35%, color-mix(in srgb, var(--ma-accent) 26%, rgba(255,255,255,.08)), transparent 55%);
        }
        .art-stack-fallback .fallback-disc {
          box-shadow:0 0 0 1px rgba(255,255,255,.08), 0 22px 48px color-mix(in srgb, var(--ma-accent) 18%, transparent);
        }
        .surprise-me-card {
          position:relative;
          width:100%;
          height:100%;
          border:none;
          border-radius:28px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.04));
          color:#fff;
          font:inherit;
          cursor:pointer;
          box-shadow:0 26px 54px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.14);
        }
        .surprise-me-card.compact {
          border-radius:16px;
          box-shadow:0 12px 24px rgba(0,0,0,.12), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .empty-magic-stack {
          position:relative;
          display:grid;
          place-items:center;
          width:max-content;
          max-width:100%;
          margin:auto;
          overflow:visible;
        }
        .surprise-me-card.magic-empty {
          width:168px;
          height:168px;
          border-radius:999px;
          background:
            radial-gradient(circle at 50% 20%, rgba(255,255,255,.18), transparent 26%),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 48%, transparent), transparent 34%),
            linear-gradient(180deg, rgba(255,255,255,.12), rgba(255,255,255,.04));
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 26px 46px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.14);
          transition:transform .16s ease, box-shadow .18s ease, border-color .18s ease;
          animation:magic-empty-pulse 4.8s ease-in-out infinite;
        }
        .surprise-me-card.magic-empty::before {
          content:"";
          position:absolute;
          inset:-10px;
          border-radius:inherit;
          border:1px solid color-mix(in srgb, var(--ma-accent) 26%, transparent);
          box-shadow:0 0 0 8px color-mix(in srgb, var(--ma-accent) 7%, transparent);
          opacity:.38;
          animation:magic-empty-halo 4.8s ease-in-out infinite;
          pointer-events:none;
        }
        .surprise-me-card.magic-empty.has-art {
          width:min(360px, 44cqi);
          height:min(360px, 44cqi);
          max-width:100%;
          max-height:100%;
          border-radius:var(--ma-radius-xl);
          background:#11151d;
          animation:none;
        }
        .surprise-me-card.magic-empty.has-art::before {
          display:none;
        }
        .surprise-me-card.magic-empty.has-art img {
          position:absolute;
          inset:0;
          width:100%;
          height:100%;
          object-fit:cover;
        }
        .surprise-me-card.magic-empty.has-art .art-overlay {
          position:absolute;
          inset:auto 14px 14px auto;
          width:52px;
          height:52px;
          border-radius:999px;
          background:rgba(0,0,0,.34);
          border:1px solid rgba(255,255,255,.16);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
        }
        .surprise-me-card.magic-empty.has-art .art-overlay .ui-ic {
          width:24px;
          height:24px;
        }
        .surprise-me-glow {
          position:absolute;
          inset:0;
          background:
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 34%, transparent), transparent 32%),
            radial-gradient(circle at 82% 20%, rgba(255,255,255,.12), transparent 26%),
            radial-gradient(circle at 50% 72%, color-mix(in srgb, var(--ma-accent) 20%, transparent), transparent 34%);
          opacity:.95;
        }
        .surprise-me-label {
          position:relative;
          z-index:1;
          padding:14px 22px;
          border-radius:999px;
          background:rgba(255,255,255,.14);
          border:1px solid rgba(255,255,255,.18);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          font-size:clamp(18px,2.1vw,26px);
          font-weight:900;
        }
        .surprise-me-card.compact .surprise-me-label {
          padding:8px 12px;
          font-size:clamp(12px, 1.4vw, 16px);
        }
        .surprise-me-wand {
          position:relative;
          z-index:1;
          width:100%;
          height:100%;
          display:grid;
          place-items:center;
          border-radius:inherit;
          background:transparent;
          border:none;
          box-shadow:none;
          color:#fff6d8;
        }
        .surprise-me-wand .ui-ic {
          width:60px;
          height:60px;
          filter:drop-shadow(0 0 10px color-mix(in srgb, var(--ma-accent) 34%, transparent));
        }
        .surprise-me-card.compact .surprise-me-wand .ui-ic {
          width:44px;
          height:44px;
        }
        .empty-voice-btn {
          position:absolute;
          inset-inline-end:-8px;
          inset-block-end:-8px;
          z-index:4;
          width:46px;
          height:46px;
          border-radius:999px;
          border:1px solid color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.16));
          background:linear-gradient(180deg, rgba(255,255,255,.16), rgba(255,255,255,.07));
          color:#fff7df;
          display:grid;
          place-items:center;
          cursor:pointer;
          box-shadow:0 14px 28px rgba(0,0,0,.24), 0 0 0 5px rgba(255,255,255,.045), inset 0 1px 0 rgba(255,255,255,.14);
          backdrop-filter:blur(16px) saturate(130%);
          -webkit-backdrop-filter:blur(16px) saturate(130%);
          transition:transform .16s ease, border-color .18s ease, background .18s ease, box-shadow .18s ease, color .18s ease;
        }
        .empty-voice-btn .ui-ic {
          width:20px;
          height:20px;
        }
        .empty-voice-btn:hover,
        .empty-voice-btn.listening,
        .empty-voice-btn.pressed,
        .empty-voice-btn.busy {
          color:#fff;
          border-color:color-mix(in srgb, var(--ma-accent) 54%, rgba(255,255,255,.18));
          background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.14)), rgba(255,255,255,.07));
          box-shadow:0 16px 32px color-mix(in srgb, var(--ma-accent) 20%, rgba(0,0,0,.22)), 0 0 0 7px color-mix(in srgb, var(--ma-accent) 10%, transparent), inset 0 1px 0 rgba(255,255,255,.16);
        }
        .empty-voice-btn.pressed,
        .empty-voice-btn.busy {
          transform:scale(.94);
        }
        .empty-voice-btn.busy {
          cursor:progress;
        }
        .empty-voice-btn.listening::after {
          content:"";
          position:absolute;
          inset:-8px;
          border-radius:inherit;
          border:1px solid color-mix(in srgb, var(--ma-accent) 44%, transparent);
          animation:voiceAssistantListenPulse 1.1s ease-out infinite;
        }
        .empty-voice-btn.busy:not(.listening)::after {
          content:"";
          position:absolute;
          inset:-6px;
          border-radius:inherit;
          border:1px solid color-mix(in srgb, var(--ma-accent) 38%, transparent);
          animation:voiceAssistantListenPulse 1s ease-out infinite;
          pointer-events:none;
        }
        .card.layout-tablet .empty-voice-btn {
          width:50px;
          height:50px;
          inset-inline-end:-10px;
          inset-block-end:-10px;
        }
        .surprise-me-card.magic-empty:active,
        .surprise-me-card.magic-empty.pressed,
        .surprise-me-card.magic-empty.busy {
          transform:scale(.96);
          box-shadow:0 16px 30px rgba(0,0,0,.18), 0 0 0 1px color-mix(in srgb, var(--ma-accent) 34%, transparent), 0 0 26px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .surprise-me-card.magic-empty.busy,
        .surprise-me-card.magic-empty:disabled {
          cursor:progress;
        }
        .surprise-me-card.magic-empty.busy::after {
          content:"";
          position:absolute;
          inset:12px;
          border-radius:inherit;
          border:1px solid color-mix(in srgb, var(--ma-accent) 44%, transparent);
          animation:voiceAssistantListenPulse 1.05s ease-out infinite;
          pointer-events:none;
        }
        @keyframes emptyActionSweep {
          0% { transform:translateX(-100%); opacity:.4; }
          45% { opacity:.95; }
          100% { transform:translateX(100%); opacity:.25; }
        }
        @keyframes magic-empty-pulse {
          0%,100% { box-shadow:0 26px 46px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.14); border-color:rgba(255,255,255,.12); }
          50% { box-shadow:0 28px 50px rgba(0,0,0,.18), 0 0 28px color-mix(in srgb, var(--ma-accent) 14%, transparent), inset 0 1px 0 rgba(255,255,255,.16); border-color:color-mix(in srgb, var(--ma-accent) 24%, rgba(255,255,255,.14)); }
        }
        @keyframes magic-empty-halo {
          0%,100% { opacity:.24; box-shadow:0 0 0 5px color-mix(in srgb, var(--ma-accent) 5%, transparent); }
          50% { opacity:.48; box-shadow:0 0 0 10px color-mix(in srgb, var(--ma-accent) 9%, transparent); }
        }
        .empty-magic-screen-ripple {
          position:absolute;
          left:var(--empty-ripple-x, 50%);
          top:var(--empty-ripple-y, 50%);
          width:16px;
          height:16px;
          border-radius:999px;
          pointer-events:none;
          z-index:8;
          transform:translate(-50%, -50%) scale(.2);
          background:
            radial-gradient(circle, color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.38)) 0 10%, transparent 54%),
            radial-gradient(circle, transparent 38%, color-mix(in srgb, var(--ma-accent) 28%, transparent) 40% 42%, transparent 62%);
          filter:blur(.2px);
          animation:emptyMagicRipple 1s ease-out forwards;
        }
        @keyframes emptyMagicRipple {
          0% { opacity:.72; transform:translate(-50%, -50%) scale(.2); }
          60% { opacity:.34; }
          100% { opacity:0; transform:translate(-50%, -50%) scale(18); }
        }
        .empty-playback-loader {
          position:absolute;
          left:var(--empty-loader-x, 50%);
          top:var(--empty-loader-y, 50%);
          z-index:9;
          width:92px;
          height:42px;
          border-radius:999px;
          display:grid;
          grid-auto-flow:column;
          place-content:center;
          align-items:center;
          gap:8px;
          pointer-events:none;
          color:#fff8df;
          background:linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.06));
          border:1px solid color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.16));
          box-shadow:0 18px 36px rgba(0,0,0,.22), 0 0 0 7px color-mix(in srgb, var(--ma-accent) 9%, transparent), inset 0 1px 0 rgba(255,255,255,.14);
          backdrop-filter:blur(18px) saturate(125%);
          -webkit-backdrop-filter:blur(18px) saturate(125%);
          transform:translate(-50%, -50%);
          animation:emptyPlaybackLoaderIn .18s ease-out both;
        }
        .empty-playback-loader span {
          width:8px;
          height:8px;
          border-radius:999px;
          background:currentColor;
          opacity:.52;
          animation:emptyPlaybackDot 1s ease-in-out infinite;
        }
        .empty-playback-loader span:nth-child(2) { animation-delay:.14s; }
        .empty-playback-loader span:nth-child(3) { animation-delay:.28s; }
        @keyframes emptyPlaybackLoaderIn {
          from { opacity:0; transform:translate(-50%, -50%) scale(.92); }
          to { opacity:1; transform:translate(-50%, -50%) scale(1); }
        }
        @keyframes emptyPlaybackDot {
          0%,100% { opacity:.38; transform:translateY(0); }
          45% { opacity:1; transform:translateY(-5px); }
        }
        .top-settings-fab {
          position:absolute;
          inset-block-start:18px;
          z-index:6;
          width:38px;
          height:38px;
          border-radius:14px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(14,18,28,.54);
          color:#fff;
          display:grid;
          place-items:center;
          box-shadow:0 12px 24px rgba(0,0,0,.18);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
        }
        .top-settings-fab,
        .top-settings-fab.ltr,
        .top-settings-fab.rtl {
          right:18px;
          left:auto;
          inset-inline-start:auto;
          inset-inline-end:auto;
        }
        .card.has-home-shortcut:not(.compact-expanded) > .top-settings-fab {
          right:70px;
        }
        .home-shortcut-fab {
          position:absolute;
          z-index:6;
          width:44px;
          height:44px;
          border-radius:999px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(14,18,28,.58);
          color:#fff;
          display:grid;
          place-items:center;
          box-shadow:0 14px 30px rgba(0,0,0,.2);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
        }
        .home-shortcut-fab .ui-ic { width:20px; height:20px; }
        .home-shortcut-fab.mobile { inset-block-start:18px; }
        .home-shortcut-fab.tablet { inset-block-end:22px; }
        .home-shortcut-fab,
        .home-shortcut-fab.ltr,
        .home-shortcut-fab.rtl {
          right:18px;
          left:auto;
          inset-inline-start:auto;
          inset-inline-end:auto;
        }
        .card.compact-expanded > .home-shortcut-fab.mobile {
          top:18px !important;
          right:auto !important;
          left:18px !important;
          inset-block-start:18px !important;
          inset-inline-start:auto !important;
          inset-inline-end:auto !important;
          z-index:8 !important;
        }
        .card.compact-expanded > .top-settings-fab {
          top:18px !important;
          right:66px !important;
          left:auto !important;
          inset-block-start:18px !important;
          inset-inline-start:auto !important;
          inset-inline-end:auto !important;
          z-index:9 !important;
        }
        .card.compact-expanded > .compact-collapse-fab {
          top:18px !important;
          right:18px !important;
          left:auto !important;
          inset-block-start:18px !important;
          inset-inline-start:auto !important;
          inset-inline-end:auto !important;
          z-index:10 !important;
        }
        .card.compact-expanded.has-top-settings > .compact-collapse-fab {
          right:18px !important;
        }
        .history-toggle-fab {
          position:absolute;
          inset-block-start:50%;
          z-index:7;
          width:40px;
          height:78px;
          border-radius:16px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(14,18,28,.42);
          color:rgba(248,251,255,.86);
          display:grid;
          place-items:center;
          box-shadow:0 14px 28px rgba(0,0,0,.16);
          backdrop-filter:blur(12px);
          -webkit-backdrop-filter:blur(12px);
          transform:translateY(-50%);
          opacity:.86;
          transition:transform .18s ease, background-color .18s ease, border-color .18s ease, box-shadow .18s ease, opacity .18s ease;
        }
        .history-toggle-fab.left-edge { inset-inline-start:14px; }
        .history-toggle-fab.right-edge { inset-inline-end:14px; }
        .history-toggle-fab .ui-ic {
          width:18px;
          height:18px;
        }
        .history-toggle-fab.active {
          color:var(--ma-accent);
          background:rgba(245,166,35,.12);
          border-color:rgba(245,166,35,.24);
          box-shadow:0 16px 30px rgba(0,0,0,.18), 0 0 0 1px rgba(245,166,35,.1);
          opacity:.96;
        }
        .history-toggle-fab:active {
          transform:translateY(-50%) scale(.97);
        }
        @keyframes historyTabletSignal {
          0%,100% { opacity:.68; filter:drop-shadow(0 0 0 transparent); }
          50% { opacity:1; filter:drop-shadow(0 0 9px color-mix(in srgb, var(--ma-accent) 34%, transparent)); }
        }
        .history-toggle-fab.tablet-history-fab {
          inset-block-start:50%!important;
          inset-block-end:auto!important;
          width:38px;
          height:78px;
          border-radius:0;
          border:0;
          background:transparent;
          color:rgba(248,251,255,.82);
          box-shadow:none;
          backdrop-filter:none;
          -webkit-backdrop-filter:none;
          filter:drop-shadow(0 10px 18px rgba(0,0,0,.32));
        }
        .history-toggle-fab.tablet-history-fab.left-edge { inset-inline-start:2px!important; }
        .history-toggle-fab.tablet-history-fab.right-edge { inset-inline-end:2px!important; }
        .history-toggle-fab.tablet-history-fab .ui-ic {
          width:29px;
          height:29px;
          animation:historyTabletSignal 2.4s ease-in-out infinite;
          transform:none;
        }
        .history-toggle-fab.tablet-history-fab.right-edge .ui-ic {
          transform:scaleX(-1);
          animation:historyTabletSignal 2.4s ease-in-out infinite;
        }
        .sleep-timer-corner {
          position:absolute;
          inset-block-start:auto;
          inset-block-end:calc(102px + env(safe-area-inset-bottom, 0px));
          z-index:12;
          display:grid;
          gap:8px;
          align-items:start;
          pointer-events:auto;
        }
        .sleep-timer-corner.right,
        .sleep-timer-corner.left {
          inset-inline-start:50%;
          inset-inline-end:auto;
          justify-items:center;
          transform:translateX(-50%);
        }
        .sleep-timer-corner[hidden],
        .sleep-timer-chip[hidden],
        .sleep-timer-menu[hidden] {
          display:none !important;
        }
        .card:not(.layout-tablet).has-sleep-timer .player-focus,
        .card:not(.layout-tablet).has-sleep-timer .player-focus-nav {
          margin-top:0;
        }
        .sleep-timer-chip,
        .sleep-timer-menu-btn {
          border:none;
          color:inherit;
          font:inherit;
        }
        .sleep-timer-chip {
          min-height:40px;
          padding:0 12px;
          border-radius:999px;
          display:inline-flex;
          align-items:center;
          gap:8px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(12,16,24,.68);
          color:#fff;
          box-shadow:0 14px 26px rgba(0,0,0,.22);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          font-size:calc(12px * var(--v2-font-scale));
          font-weight:800;
        }
        .sleep-timer-chip .ui-ic { width:16px; height:16px; }
        .sleep-timer-menu {
          display:grid;
          grid-auto-flow:column;
          gap:8px;
          padding:8px;
          border-radius:18px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(9,12,18,.74);
          box-shadow:0 18px 34px rgba(0,0,0,.24);
          backdrop-filter:blur(20px);
          -webkit-backdrop-filter:blur(20px);
        }
        .sleep-timer-menu-btn {
          min-width:54px;
          min-height:38px;
          padding:0 12px;
          border-radius:14px;
          background:rgba(255,255,255,.08);
          color:#fff;
          font-size:calc(12px * var(--v2-font-scale));
          font-weight:800;
        }
        .sleep-timer-menu-btn.danger {
          background:rgba(255,122,122,.14);
          color:#ffd2d2;
        }
        .sleep-timer-menu-btn.ghost {
          background:rgba(255,255,255,.06);
          color:rgba(255,255,255,.82);
        }
        .theme-light .sleep-timer-chip {
          background:rgba(255,255,255,.86);
          border-color:rgba(147,161,183,.18);
          color:#1f2633;
        }
        .theme-light .sleep-timer-menu {
          background:rgba(255,255,255,.88);
          border-color:rgba(147,161,183,.18);
        }
        .theme-light .sleep-timer-menu-btn {
          background:rgba(240,244,250,.96);
          color:#1f2633;
        }
        .theme-light .sleep-timer-menu-btn.danger {
          background:rgba(255,236,236,.92);
          color:#8b2935;
        }
        .theme-light .sleep-timer-menu-btn.ghost {
          background:rgba(245,248,252,.84);
          color:#516177;
        }
        .history-drawer {
          position:absolute;
          inset-block:18px 18px;
          z-index:6;
          width:min(320px, calc(100% - 72px));
          display:grid;
          grid-template-rows:auto minmax(0,1fr);
          border-radius:26px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(12,16,24,.74);
          box-shadow:0 22px 44px rgba(0,0,0,.26);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          opacity:0;
          pointer-events:none;
          transition:transform .24s ease, opacity .2s ease;
        }
        .history-drawer.left-edge {
          inset-inline-start:66px;
          transform:translateX(calc(-100% - 18px));
        }
        .history-drawer.right-edge {
          inset-inline-end:66px;
          transform:translateX(calc(100% + 18px));
        }
        .history-drawer.open {
          opacity:1;
          pointer-events:auto;
          transform:translateX(0);
        }
        .history-drawer-head {
          display:grid;
          gap:10px;
          align-items:start;
          min-height:58px;
          padding:16px 16px 10px;
          border-bottom:1px solid rgba(255,255,255,.08);
        }
        .history-drawer-title {
          font-size:14px;
          font-weight:900;
          letter-spacing:.08em;
          text-transform:uppercase;
          color:rgba(255,255,255,.66);
        }
        .history-drawer-tabs {
          display:flex;
          align-items:center;
          gap:6px;
          padding:4px;
          border-radius:999px;
          background:rgba(255,255,255,.06);
          border:1px solid rgba(255,255,255,.08);
        }
        .history-tab {
          min-height:30px;
          flex:1 1 0;
          padding:0 10px;
          border:none;
          border-radius:999px;
          background:transparent;
          color:rgba(255,255,255,.68);
          font:inherit;
          font-size:11px;
          font-weight:900;
          cursor:pointer;
        }
        .history-tab.active {
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 8px 16px color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .history-drawer-body {
          overflow:auto;
          display:grid;
          align-content:start;
          gap:8px;
          padding:12px 12px 14px;
        }
        .history-actions {
          display:flex;
          justify-content:flex-end;
          padding-bottom:2px;
        }
        .history-play-all-btn {
          min-height:32px;
          gap:7px;
        }
        .history-play-all-btn .ui-ic {
          width:14px;
          height:14px;
        }
        .history-empty {
          min-height:96px;
          display:grid;
          place-items:center;
          padding:12px;
          text-align:center;
          font-size:13px;
          font-weight:700;
          line-height:1.45;
          color:rgba(255,255,255,.66);
        }
        .theme-light .history-toggle-fab {
          color:#1f2633;
          background:transparent;
          border-color:transparent;
          box-shadow:none;
          filter:drop-shadow(0 10px 18px rgba(95,112,136,.24));
        }
        .theme-light .history-drawer {
          background:rgba(255,255,255,.88);
          border-color:rgba(143,159,181,.18);
          box-shadow:0 20px 40px rgba(95,112,136,.16);
        }
        .theme-light .history-drawer-head {
          border-bottom-color:rgba(143,159,181,.14);
        }
        .theme-light .history-drawer-title {
          color:#7b889b;
        }
        .theme-light .history-drawer-tabs {
          background:rgba(236,241,247,.86);
          border-color:rgba(143,159,181,.14);
        }
        .theme-light .history-tab {
          color:#69778c;
        }
        .theme-light .history-tab.active {
          color:#392805;
        }
        .theme-light .history-empty {
          color:#6f7d91;
        }
        .footer-theme-ic {
          width:22px;
          height:22px;
          display:grid;
          place-items:center;
          font-size:18px;
          line-height:1;
        }
        .menu-head button[hidden] { visibility:hidden; display:grid; }
        .menu-aux-btn {
          position:absolute;
          inset-block-start:18px;
          inset-inline-end:68px;
          z-index:2;
        }
        .menu-title {
          grid-column:2 / 3;
          justify-self:center;
          text-align:center;
          font-size:20px;
          font-weight:900;
          letter-spacing:-.02em;
          display:flex;
          align-items:center;
          justify-content:center;
          gap:10px;
          width:min(100%, max-content);
          max-width:min(100%, calc(100% - 32px));
          margin-inline:auto;
        }
        .menu-title-main {
          display:inline-flex;
          align-items:center;
          justify-content:center;
          gap:8px;
          min-width:0;
        }
        .menu-title-brand {
          position:absolute;
          left:18px;
          right:auto;
          top:50%;
          transform:translateY(-50%);
          width:92px;
          max-width:34vw;
          display:inline-grid;
          place-items:center;
          color:rgba(255,255,255,.62);
          opacity:.94;
          flex:0 0 auto;
          pointer-events:none;
        }
        .menu-head:has(#mobileMenuBackBtn:not([hidden])) .menu-title-brand {
          left:72px;
          width:78px;
        }
        .theme-light .menu-title-brand {
          color:rgba(31,38,51,.42);
        }
        .menu-title-logo {
          max-height:30px;
        }
        .card.layout-tablet .menu-title {
          font-size:19px;
        }
        .menu-title-icon {
          width:20px;
          height:20px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          color:var(--ma-accent);
          flex-shrink:0;
        }
        .menu-title-icon .ui-ic { width:20px; height:20px; }
        .rtl .menu-title { flex-direction:row-reverse; }
        .menu-aux-btn .ui-ic { width:20px; height:20px; }
        .menu-title.clickable { cursor:pointer; }
        .menu-body { overflow:auto; padding:16px; display:grid; gap:12px; align-content:start; min-height:0; position:relative; }
        .menu-body.sheet-settings {
          overflow-anchor:none;
          scroll-behavior:auto;
        }
        .card.layout-tablet .menu-body {
          padding:14px 16px 16px;
          gap:10px;
        }
        .menu-body.library-mode { overflow:hidden; display:grid; min-height:0; }
        .menu-body.library-mode {
          height:100%;
          max-height:100%;
          grid-template-rows:minmax(0,1fr);
        }
        .menu-body.search-mode .library-shell,
        .menu-body.search-mode .media-home-shell {
          min-height:0;
          height:100%;
        }
        .menu-body.search-mode .media-home-shell {
          grid-template-rows:auto minmax(0,1fr);
        }
        #mobileMediaSearchResults {
          min-height:0;
          overflow:auto;
        }
        .menu-body.search-mode .media-search-zone {
          position:sticky;
          top:0;
          z-index:3;
          padding-bottom:10px;
          background:linear-gradient(180deg, rgba(15,18,27,.92), rgba(15,18,27,.68), transparent);
        }
        .theme-light .menu-body.search-mode .media-search-zone {
          background:linear-gradient(180deg, rgba(239,244,250,.94), rgba(239,244,250,.72), transparent);
        }
        .theme-light .menu-backdrop {
          background:rgba(229,236,245,.58);
          backdrop-filter:blur(20px);
          -webkit-backdrop-filter:blur(20px);
        }
        .lyrics-backdrop {
          position:absolute; inset:0; z-index:70; display:none; align-items:center; justify-content:center;
          padding:max(12px, env(safe-area-inset-top)) max(12px, env(safe-area-inset-right)) max(12px, env(safe-area-inset-bottom)) max(12px, env(safe-area-inset-left));
          background:
            radial-gradient(circle at 18% 16%, rgba(var(--dynamic-accent-rgb,245 166 35) / .22), transparent 28%),
            linear-gradient(180deg, rgba(8,10,16,.62), rgba(6,8,14,.86));
          backdrop-filter:blur(18px); -webkit-backdrop-filter:blur(18px);
          overscroll-behavior:contain;
          overflow:hidden;
          isolation:isolate;
        }
        .lyrics-backdrop.open { display:flex; }
        .lyrics-backdrop::before {
          content:"";
          position:absolute;
          inset:-14%;
          z-index:0;
          pointer-events:none;
          background:var(--lyrics-dynamic-art, none) center/cover no-repeat;
          filter:blur(52px) saturate(1.18) brightness(.82);
          opacity:0;
          transform:scale(1.12);
          transition:opacity .24s ease;
        }
        .lyrics-backdrop.has-lyrics-art::before { opacity:.52; }
        .lyrics-backdrop::after {
          content:"";
          position:absolute;
          inset:0;
          z-index:0;
          pointer-events:none;
          background:
            radial-gradient(circle at 50% 18%, rgba(255,255,255,.10), transparent 30%),
            linear-gradient(180deg, rgba(6,8,14,.34), rgba(6,8,14,.78) 48%, rgba(4,6,12,.92));
        }
        .card.layout-tablet.lyrics-modal-open {
          overflow:hidden;
        }
        .card:not(.layout-tablet).lyrics-modal-open {
          overflow:hidden;
        }
        .lyrics-sheet {
          position:relative; z-index:1; isolation:isolate;
          width:min(1160px, calc(100% - 8px)); max-height:calc(100% - 8px); overflow:hidden; display:grid; grid-template-rows:auto minmax(0,1fr);
          border-radius:28px; border:1px solid rgba(255,255,255,.14);
          background:linear-gradient(180deg, rgba(18,21,32,.74), rgba(9,11,18,.88));
          box-shadow:0 28px 72px rgba(0,0,0,.38), inset 0 1px 0 rgba(255,255,255,.08);
          backdrop-filter:blur(26px) saturate(1.08);
          -webkit-backdrop-filter:blur(26px) saturate(1.08);
        }
        .theme-light .lyrics-sheet { background:rgba(255,255,255,.92); border-color:rgba(147,161,183,.2); }
        .lyrics-sheet::before {
          content:"";
          position:absolute;
          inset:-22%;
          z-index:0;
          pointer-events:none;
          background:var(--lyrics-dynamic-art, none) center/cover no-repeat;
          filter:blur(42px) saturate(1.16);
          transform:scale(1.1);
          opacity:.18;
        }
        .lyrics-sheet::after {
          content:"";
          position:absolute;
          inset:0;
          z-index:0;
          pointer-events:none;
          background:
            linear-gradient(180deg, rgba(18,21,32,.78), rgba(11,13,21,.86)),
            radial-gradient(circle at 50% 0%, rgba(var(--dynamic-accent-rgb,245 166 35) / .14), transparent 42%);
        }
        .theme-light .lyrics-sheet::after {
          background:
            linear-gradient(180deg, rgba(255,255,255,.82), rgba(245,248,253,.92)),
            radial-gradient(circle at 50% 0%, rgba(var(--dynamic-accent-rgb,245 166 35) / .12), transparent 42%);
        }
        .lyrics-head,
        .lyrics-body {
          position:relative;
          z-index:1;
        }
        .lyrics-head { display:grid; grid-template-columns:minmax(0,1fr) auto; align-items:start; gap:14px; padding:18px 16px 14px; border-bottom:1px solid rgba(255,255,255,.08); }
        .theme-light .lyrics-head { border-bottom-color:rgba(143,159,181,.16); }
        .lyrics-title-wrap { min-width:0; display:grid; gap:6px; text-align:center; }
        .lyrics-title-brand {
          width:120px;
          max-width:56%;
          margin-inline:auto;
          color:rgba(255,255,255,.64);
          opacity:.96;
          display:grid;
          place-items:center;
        }
        .theme-light .lyrics-title-brand {
          color:rgba(31,38,51,.42);
        }
        .lyrics-title { font-size:22px; font-weight:900; line-height:1.08; }
        .lyrics-sub { font-size:13px; color:rgba(255,255,255,.72); }
        .theme-light .lyrics-sub { color:rgba(55,68,85,.68); }
        .lyrics-head-actions { display:flex; align-items:center; justify-content:flex-end; flex-wrap:wrap; gap:10px; justify-self:end; }
        .lyrics-offset-controls,
        .lyrics-font-controls {
          display:inline-flex;
          align-items:center;
          gap:4px;
          padding:4px;
          border-radius:999px;
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.08);
        }
        .lyrics-offset-btn,
        .lyrics-offset-label {
          border:none;
          min-width:34px;
          height:32px;
          border-radius:999px;
          display:inline-grid;
          place-items:center;
          background:transparent;
          color:rgba(255,255,255,.88);
          font:inherit;
          font-size:13px;
          font-weight:900;
        }
        .lyrics-offset-btn:active,
        .lyrics-offset-label:active {
          transform:scale(.96);
        }
        .lyrics-offset-label {
          min-width:52px;
          padding:0 8px;
          background:rgba(255,255,255,.08);
          color:#fff;
        }
        .lyrics-sync-btn {
          border:none;
          min-height:40px;
          padding:0 12px;
          border-radius:999px;
          display:inline-flex;
          align-items:center;
          gap:8px;
          background:rgba(255,255,255,.08);
          color:rgba(255,255,255,.86);
          font:inherit;
          font-size:13px;
          font-weight:800;
        }
        .lyrics-sync-btn.active {
          background:color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.08));
          color:#fff;
          box-shadow:0 10px 24px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .theme-light .lyrics-sync-btn {
          background:rgba(240,244,250,.92);
          color:#253041;
        }
        .theme-light .lyrics-offset-controls,
        .theme-light .lyrics-font-controls {
          background:rgba(240,244,250,.86);
          border-color:rgba(143,159,181,.16);
        }
        .theme-light .lyrics-offset-btn,
        .theme-light .lyrics-offset-label {
          color:#253041;
        }
        .theme-light .lyrics-offset-label {
          background:rgba(255,255,255,.82);
        }
        .theme-light .lyrics-sync-btn.active {
          background:color-mix(in srgb, var(--ma-accent) 16%, rgba(240,244,250,.96));
        }
        .lyrics-body { min-height:0; height:100%; overflow:auto; padding:clamp(24px, 4vw, 42px) clamp(18px, 5vw, 72px) 32px; white-space:pre-wrap; line-height:1.92; font-size:calc(clamp(18px, 3vw, 30px) * var(--lyrics-font-scale, 1)); color:#fff; text-align:center; scroll-behavior:smooth; display:grid; justify-items:center; overscroll-behavior:contain; -webkit-overflow-scrolling:touch; }
        .theme-light .lyrics-body { color:#1f2633; }
        .lyrics-state { display:grid; place-items:center; min-height:220px; text-align:center; color:rgba(255,255,255,.72); }
        .theme-light .lyrics-state { color:rgba(55,68,85,.68); }
        .lyrics-pre { margin:0; font:inherit; white-space:pre-wrap; text-align:center; }
        .lyrics-timeline {
          width:min(100%, 920px);
          margin-inline:auto;
          display:grid;
          gap:14px;
          padding:10px 4px 44vh;
        }
        .lyrics-line {
          width:100%;
          text-align:center;
          opacity:.42;
          transform:scale(.96);
          transform-origin:center;
          color:rgba(255,255,255,.78);
          font-weight:800;
          line-height:1.48;
          letter-spacing:.01em;
          transition:opacity .22s ease, transform .22s ease, color .22s ease, text-shadow .22s ease;
        }
        .lyrics-line.active {
          opacity:1;
          transform:scale(1.14);
          color:#fff;
          font-weight:950;
          text-shadow:0 0 22px color-mix(in srgb, var(--ma-accent) 46%, transparent), 0 8px 28px rgba(0,0,0,.34);
        }
        .theme-light .lyrics-line { color:rgba(31,38,51,.6); }
        .theme-light .lyrics-line.active {
          color:#101722;
          text-shadow:0 10px 26px color-mix(in srgb, var(--ma-accent) 28%, transparent);
        }
        .card.layout-tablet .lyrics-backdrop {
          align-items:center;
          padding:max(44px, env(safe-area-inset-top)) max(34px, env(safe-area-inset-right)) max(34px, env(safe-area-inset-bottom)) max(34px, env(safe-area-inset-left));
        }
        .card.layout-tablet .lyrics-sheet {
          width:min(1320px, calc(100% - 96px));
          max-height:calc(100% - 96px);
          border-radius:34px;
        }
        .card.layout-tablet .lyrics-head {
          align-items:center;
          gap:16px;
          padding:22px 24px 18px;
        }
        .card.layout-tablet .lyrics-title-wrap {
          min-width:0;
          padding-inline:24px;
        }
        .card.layout-tablet .lyrics-title,
        .card.layout-tablet .lyrics-sub {
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .card.layout-tablet .lyrics-title {
          font-size:clamp(24px, 2.8vw, 34px);
          line-height:1.18;
        }
        .card.layout-tablet .lyrics-body {
          padding-block-start:clamp(34px, 5vh, 56px);
        }
        @media (max-width: 600px) {
          .lyrics-backdrop {
            align-items:center;
            padding:max(8px, env(safe-area-inset-top)) max(8px, env(safe-area-inset-right)) max(8px, env(safe-area-inset-bottom)) max(8px, env(safe-area-inset-left));
          }
          .lyrics-sheet {
            width:100%;
            height:min(560px, calc(100% - 18px));
            max-height:calc(100% - 18px);
            border-radius:22px;
          }
          .lyrics-head {
            grid-template-columns:minmax(0,1fr);
            align-items:start;
            gap:8px;
            padding:12px 10px 10px;
          }
          .lyrics-title-wrap {
            gap:4px;
            text-align:center;
          }
          .rtl .lyrics-title-wrap {
            text-align:center;
          }
          .lyrics-title,
          .lyrics-sub {
            overflow:hidden;
            text-overflow:ellipsis;
          }
          .lyrics-title {
            font-size:16px;
            line-height:1.1;
            white-space:nowrap;
          }
          .lyrics-sub {
            font-size:12px;
            display:-webkit-box;
            -webkit-line-clamp:2;
            -webkit-box-orient:vertical;
          }
          .lyrics-head-actions {
            justify-self:center;
            flex-wrap:nowrap;
            gap:6px;
            max-width:100%;
            overflow:auto;
            scrollbar-width:none;
          }
          .lyrics-head-actions::-webkit-scrollbar {
            display:none;
          }
          .lyrics-sync-btn {
            min-height:36px;
            padding:0 10px;
            font-size:12px;
            white-space:nowrap;
          }
          .lyrics-offset-btn,
          .lyrics-offset-label {
            min-width:30px;
            height:30px;
            font-size:12px;
          }
          .lyrics-offset-label {
            min-width:46px;
          }
          .lyrics-body {
            padding:16px 14px 24px;
            font-size:calc(clamp(18px, 5.6vw, 26px) * var(--lyrics-font-scale, 1));
            align-content:start;
          }
          .lyrics-line.active {
            transform:scale(1.06);
          }
          .lyrics-timeline {
            width:100%;
            gap:16px;
            padding-bottom:42vh;
          }
        }
        .theme-light .menu-head {
          border-bottom-color:rgba(143,159,181,.16);
        }
        .menu-item,.menu-list-item,.queue-row,.media-search-shell,.media-category-row { width:100%; min-width:0; overflow:hidden; display:flex; align-items:center; gap:12px; padding:14px; border-radius:22px; color:#fff; text-align:inherit; transition:transform .16s ease, border-color .16s ease, background-color .16s ease, box-shadow .16s ease; }
        .menu-item,.menu-list-item { border:none; cursor:pointer; }
        .menu-item:active,
        .menu-list-item:active,
        .queue-row:active,
        .footer-btn:active,
        .control-btn:active,
        .main-btn:active,
        .settings-pill:active,
        .settings-check-pill:active,
        .library-nav-btn:active,
        .media-entry-main:active,
        .action-btn:active,
        .chip-btn:active {
          transform:scale(.985);
          border-color:color-mix(in srgb, var(--ma-accent) 34%, transparent);
          background-color:color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .menu-item-main { display:flex; align-items:center; gap:14px; min-width:0; flex:1; }
        .action-tile {
          min-height:104px;
          padding:0;
          align-items:stretch;
          border-radius:22px;
          background:
            linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.05)),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 40%);
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 18px 32px rgba(0,0,0,.16);
          position:relative;
        }
        .action-tile .menu-item-main {
          display:grid;
          grid-template-columns:58px minmax(0,1fr);
          align-items:center;
          gap:12px;
          width:100%;
          min-height:100%;
          padding:12px 14px;
        }
        .action-tile .menu-item-ico {
          width:58px;
          height:58px;
          border-radius:18px;
          box-shadow:0 14px 24px color-mix(in srgb, var(--ma-accent) 14%, transparent);
        }
        .action-tile .menu-item-title {
          font-size:19px;
          font-weight:950;
          letter-spacing:-.02em;
        }
        .action-tile-kicker {
          display:none;
          margin-bottom:4px;
          font-size:10px;
          font-weight:900;
          letter-spacing:.08em;
          text-transform:uppercase;
          color:rgba(255,255,255,.54);
        }
        .menu-body.sheet-actions .action-tile .menu-item-sub {
          display:-webkit-box !important;
          margin-top:4px;
          color:rgba(255,255,255,.68);
          font-size:13px;
          line-height:1.3;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
          overflow:hidden;
        }
        .action-tile-arrow {
          display:none;
        }
        .action-tile.tone-stop .menu-item-ico {
          background:linear-gradient(135deg, rgba(255,132,132,.24), rgba(255,180,120,.12));
          border-color:rgba(255,132,132,.26);
          color:#ff9d85;
        }
        .action-tile.tone-announcement .menu-item-ico {
          background:linear-gradient(135deg, rgba(126,214,255,.24), rgba(94,165,255,.12));
          border-color:rgba(126,214,255,.24);
          color:#8edaff;
        }
        .action-tile.tone-players .menu-item-ico,
        .action-tile.tone-group .menu-item-ico {
          background:linear-gradient(135deg, rgba(255,217,135,.28), rgba(245,166,35,.12));
        }
        .action-tile.tone-simple .menu-item-ico {
          background:
            linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.18)), rgba(126,214,255,.14)),
            radial-gradient(circle at 34% 22%, rgba(255,255,255,.3), transparent 38%);
          border-color:color-mix(in srgb, var(--ma-accent) 36%, transparent);
          color:#fff;
        }
        .simple-wizard-shell {
          display:grid;
          gap:18px;
          min-height:100%;
          align-content:start;
        }
        .simple-wizard-shell button {
          font:inherit;
          cursor:pointer;
        }
        .simple-wizard-progress {
          display:grid;
          grid-template-columns:repeat(3, minmax(0, 1fr));
          gap:10px;
        }
        .simple-wizard-progress-step {
          min-width:0;
          display:grid;
          grid-template-columns:28px minmax(0,1fr);
          align-items:center;
          gap:8px;
          padding:9px;
          border-radius:18px;
          background:linear-gradient(145deg, rgba(255,255,255,.08), rgba(255,255,255,.04));
          border:1px solid rgba(255,255,255,.12);
          color:rgba(255,255,255,.68);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08);
        }
        .simple-wizard-progress-step span {
          width:28px;
          height:28px;
          display:grid;
          place-items:center;
          border-radius:12px;
          background:rgba(255,255,255,.08);
          color:#fff;
          font-size:13px;
          font-weight:950;
        }
        .simple-wizard-progress-step strong {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          font-size:12px;
          font-weight:900;
        }
        .simple-wizard-progress-step.active {
          color:#fff;
          border-color:color-mix(in srgb, var(--ma-accent) 34%, transparent);
          background:
            linear-gradient(145deg, color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.08)), rgba(255,255,255,.05)),
            radial-gradient(circle at 15% 20%, rgba(255,255,255,.16), transparent 45%);
          box-shadow:0 16px 30px color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .simple-wizard-progress-step.done span {
          background:color-mix(in srgb, var(--ma-accent) 72%, #fff 10%);
        }
        .simple-wizard-toolbar {
          display:flex;
          justify-content:flex-end;
          margin-top:-4px;
        }
        .simple-wizard-reset-btn {
          min-height:38px;
          padding:0 18px;
          border-radius:999px;
          border:1px solid rgba(255,255,255,.14);
          background:linear-gradient(145deg, rgba(255,255,255,.1), rgba(255,255,255,.045));
          color:rgba(255,255,255,.82);
          font-size:13px;
          font-weight:900;
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08), 0 12px 22px rgba(0,0,0,.12);
        }
        .simple-wizard-reset-btn:active {
          transform:translateY(1px);
        }
        .simple-wizard-panel {
          display:grid;
          gap:16px;
          animation:simpleWizardIn .2s ease;
        }
        @keyframes simpleWizardIn {
          from { opacity:0; transform:translateY(8px); }
          to { opacity:1; transform:translateY(0); }
        }
        .simple-wizard-title {
          color:#fff;
          font-size:24px;
          line-height:1.12;
          font-weight:950;
        }
        .simple-wizard-player-grid,
        .simple-wizard-option-grid {
          display:grid;
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:12px;
        }
        .simple-wizard-player,
        .simple-wizard-option,
        .simple-wizard-source {
          min-width:0;
          width:100%;
          display:grid;
          align-items:center;
          gap:12px;
          border:none;
          color:#fff;
          text-align:inherit;
          background:
            linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.045)),
            radial-gradient(circle at 14% 18%, rgba(255,255,255,.08), transparent 42%);
          border:1px solid rgba(255,255,255,.13);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08), 0 20px 38px rgba(0,0,0,.17);
          transition:transform .16s ease, border-color .16s ease, background-color .16s ease, box-shadow .16s ease;
        }
        .simple-wizard-player {
          grid-template-columns:54px minmax(0,1fr) 34px;
          min-height:88px;
          padding:12px;
          border-radius:22px;
        }
        .simple-wizard-option {
          grid-template-columns:48px minmax(0,1fr);
          min-height:92px;
          padding:14px;
          border-radius:22px;
        }
        .simple-wizard-source-grid {
          display:grid;
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:12px;
        }
        .simple-wizard-source {
          min-height:86px;
          justify-items:center;
          text-align:center;
          padding:14px;
          border-radius:22px;
        }
        .simple-wizard-source span,
        .simple-wizard-option-icon,
        .simple-wizard-player-icon,
        .simple-wizard-player-art,
        .simple-wizard-check {
          display:grid;
          place-items:center;
        }
        .simple-wizard-source .ui-ic {
          width:28px;
          height:28px;
        }
        .simple-wizard-source strong {
          font-size:18px;
          font-weight:950;
        }
        .simple-wizard-player-art,
        .simple-wizard-player-icon,
        .simple-wizard-option-icon {
          width:48px;
          height:48px;
          border-radius:16px;
          overflow:hidden;
          background:rgba(255,255,255,.08);
          color:var(--ma-accent);
          border:1px solid rgba(255,255,255,.12);
        }
        .simple-wizard-player-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .simple-wizard-option-icon .ui-ic,
        .simple-wizard-player-icon .ui-ic,
        .simple-wizard-player-art .ui-ic {
          width:24px;
          height:24px;
        }
        .simple-wizard-player-copy,
        .simple-wizard-review-copy {
          min-width:0;
          display:grid;
          gap:3px;
        }
        .simple-wizard-option-title {
          display:block;
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          font-size:17px;
          line-height:1.2;
          font-weight:950;
        }
        .simple-wizard-option-sub {
          display:block;
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          color:rgba(255,255,255,.68);
          font-size:12px;
          line-height:1.25;
          font-weight:750;
        }
        .simple-wizard-check {
          width:34px;
          height:34px;
          border-radius:14px;
          background:rgba(255,255,255,.08);
          color:rgba(255,255,255,.76);
        }
        .simple-wizard-check .ui-ic {
          width:18px;
          height:18px;
        }
        .simple-wizard-player.active,
        .simple-wizard-option.active,
        .simple-wizard-source.active {
          border-color:color-mix(in srgb, var(--ma-accent) 42%, transparent);
          background:
            linear-gradient(145deg, color-mix(in srgb, var(--ma-accent) 20%, rgba(255,255,255,.09)), rgba(255,255,255,.055)),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 22%, transparent), transparent 46%);
          box-shadow:0 20px 36px color-mix(in srgb, var(--ma-accent) 18%, rgba(0,0,0,.16));
        }
        .simple-wizard-option.free {
          border-style:solid;
          background:
            linear-gradient(145deg, rgba(255,255,255,.13), rgba(255,255,255,.05)),
            radial-gradient(circle at 84% 18%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 42%);
        }
        .simple-wizard-player.active .simple-wizard-check,
        .simple-wizard-option.active .simple-wizard-option-icon {
          background:color-mix(in srgb, var(--ma-accent) 72%, rgba(255,255,255,.16));
          color:#fff;
        }
        .simple-wizard-player.is-playing .simple-wizard-player-art {
          box-shadow:0 0 0 2px color-mix(in srgb, var(--ma-accent) 48%, transparent);
        }
        .simple-wizard-search {
          display:grid;
          gap:8px;
          color:rgba(255,255,255,.76);
          font-size:13px;
          font-weight:850;
        }
        .simple-wizard-search input {
          width:100%;
          min-height:58px;
          border-radius:20px;
          border:1px solid rgba(255,255,255,.13);
          background:rgba(255,255,255,.08);
          color:#fff;
          padding:0 16px;
          font:inherit;
          font-size:16px;
          outline:none;
        }
        .simple-wizard-search input:focus {
          border-color:color-mix(in srgb, var(--ma-accent) 46%, transparent);
          box-shadow:0 0 0 3px color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .simple-wizard-footer {
          position:sticky;
          inset-block-end:0;
          display:grid;
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:10px;
          padding-top:8px;
          background:linear-gradient(180deg, transparent, rgba(14,16,24,.86) 30%, rgba(14,16,24,.94));
          z-index:2;
        }
        .simple-wizard-footer.triple {
          grid-template-columns:minmax(0,.8fr) minmax(0,.8fr) minmax(0,1.2fr);
        }
        .simple-wizard-footer.single {
          grid-template-columns:minmax(0,1fr);
        }
        .simple-wizard-primary,
        .simple-wizard-secondary {
          min-width:0;
          min-height:58px;
          border-radius:19px;
          border:1px solid rgba(255,255,255,.14);
          color:#fff;
          font-size:16px;
          font-weight:950;
          text-align:center;
          display:grid;
          place-items:center;
        }
        .simple-wizard-primary {
          background:
            linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 76%, #ffffff 10%), color-mix(in srgb, var(--ma-accent) 48%, #4f7cff 34%));
          box-shadow:0 18px 32px color-mix(in srgb, var(--ma-accent) 22%, rgba(0,0,0,.2));
        }
        .simple-wizard-secondary {
          background:rgba(255,255,255,.08);
        }
        .simple-wizard-primary:disabled {
          opacity:.44;
          cursor:not-allowed;
          box-shadow:none;
        }
        .simple-wizard-loading {
          min-height:260px;
          justify-items:center;
          align-content:center;
          text-align:center;
        }
        .simple-wizard-loader {
          width:82px;
          height:82px;
          display:grid;
          place-items:center;
          border-radius:28px;
          color:#fff;
          background:color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 32%, transparent);
          animation:simpleWizardPulse 1.25s ease-in-out infinite;
        }
        .simple-wizard-loader .ui-ic {
          width:34px;
          height:34px;
        }
        @keyframes simpleWizardPulse {
          0%, 100% { transform:scale(1); opacity:.84; }
          50% { transform:scale(1.06); opacity:1; }
        }
        .simple-wizard-review-card {
          min-width:0;
          display:grid;
          grid-template-columns:108px minmax(0,1fr);
          gap:14px;
          align-items:center;
          padding:14px;
          border-radius:26px;
          background:
            linear-gradient(145deg, rgba(255,255,255,.13), rgba(255,255,255,.06)),
            radial-gradient(circle at 18% 16%, color-mix(in srgb, var(--ma-accent) 22%, transparent), transparent 48%);
          border:1px solid color-mix(in srgb, var(--ma-accent) 26%, rgba(255,255,255,.12));
          box-shadow:0 22px 42px rgba(0,0,0,.18);
        }
        .simple-wizard-review-art {
          width:108px;
          aspect-ratio:1/1;
          border-radius:24px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08);
          color:var(--ma-accent);
        }
        .simple-wizard-review-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .simple-wizard-review-kicker {
          color:color-mix(in srgb, var(--ma-accent) 78%, #fff 18%);
          font-size:13px;
          font-weight:900;
        }
        .simple-wizard-review-title {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
          color:#fff;
          font-size:24px;
          line-height:1.12;
          font-weight:950;
        }
        .simple-wizard-review-sub {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          color:rgba(255,255,255,.7);
          font-size:14px;
          font-weight:750;
        }
        .simple-wizard-section-head {
          min-width:0;
          display:flex;
          align-items:end;
          justify-content:space-between;
          gap:10px;
          color:#fff;
          padding:0 2px;
        }
        .simple-wizard-section-head span {
          font-size:18px;
          font-weight:950;
        }
        .simple-wizard-section-head small {
          min-width:0;
          color:rgba(255,255,255,.6);
          font-size:12px;
          font-weight:800;
          text-align:end;
        }
        .simple-wizard-result-grid {
          display:grid;
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:12px;
        }
        .simple-wizard-result {
          min-width:0;
          width:100%;
          min-height:112px;
          display:grid;
          grid-template-columns:76px minmax(0,1fr) 34px;
          align-items:center;
          gap:12px;
          padding:12px;
          border-radius:24px;
          border:1px solid rgba(255,255,255,.13);
          background:
            linear-gradient(145deg, rgba(255,255,255,.115), rgba(255,255,255,.045)),
            radial-gradient(circle at 16% 16%, rgba(255,255,255,.08), transparent 42%);
          color:#fff;
          text-align:inherit;
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08), 0 20px 36px rgba(0,0,0,.16);
          transition:transform .16s ease, border-color .16s ease, background .16s ease, box-shadow .16s ease;
        }
        .simple-wizard-result.active {
          border-color:color-mix(in srgb, var(--ma-accent) 48%, transparent);
          background:
            linear-gradient(145deg, color-mix(in srgb, var(--ma-accent) 21%, rgba(255,255,255,.095)), rgba(255,255,255,.055)),
            radial-gradient(circle at 18% 14%, color-mix(in srgb, var(--ma-accent) 24%, transparent), transparent 48%);
          box-shadow:0 22px 42px color-mix(in srgb, var(--ma-accent) 18%, rgba(0,0,0,.18));
        }
        .simple-wizard-result-art {
          width:76px;
          aspect-ratio:1/1;
          display:grid;
          place-items:center;
          overflow:hidden;
          border-radius:22px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          color:var(--ma-accent);
        }
        .simple-wizard-result-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .simple-wizard-result-art .ui-ic {
          width:28px;
          height:28px;
        }
        .simple-wizard-result-copy {
          min-width:0;
          display:grid;
          gap:4px;
        }
        .simple-wizard-result-kicker {
          color:color-mix(in srgb, var(--ma-accent) 78%, #fff 16%);
          font-size:12px;
          font-weight:900;
        }
        .simple-wizard-result-title {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
          color:#fff;
          font-size:17px;
          line-height:1.16;
          font-weight:950;
        }
        .simple-wizard-result-sub {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          color:rgba(255,255,255,.65);
          font-size:12px;
          font-weight:760;
        }
        .simple-wizard-result-check {
          width:34px;
          height:34px;
          display:grid;
          place-items:center;
          border-radius:14px;
          background:rgba(255,255,255,.08);
          color:rgba(255,255,255,.78);
        }
        .simple-wizard-result.active .simple-wizard-result-check {
          background:color-mix(in srgb, var(--ma-accent) 72%, rgba(255,255,255,.16));
          color:#fff;
        }
        .simple-wizard-result-check .ui-ic {
          width:18px;
          height:18px;
        }
        .simple-wizard-candidates {
          display:flex;
          gap:8px;
          overflow:auto;
          padding-bottom:2px;
          scrollbar-width:none;
        }
        .simple-wizard-candidates::-webkit-scrollbar { display:none; }
        .simple-wizard-candidate {
          flex:0 0 auto;
          max-width:190px;
          min-height:42px;
          padding:0 14px;
          border-radius:16px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.07);
          color:#fff;
          font-size:13px;
          font-weight:850;
        }
        .simple-wizard-candidate span {
          display:block;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .simple-wizard-candidate.active {
          border-color:color-mix(in srgb, var(--ma-accent) 42%, transparent);
          background:color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.08));
        }
        .surprise-popup.simple-wizard-popup {
          z-index:92;
        }
        .simple-wizard-popup-card {
          width:min(300px, calc(100% - 28px));
        }
        .theme-light .simple-wizard-progress-step,
        .theme-light .simple-wizard-player,
        .theme-light .simple-wizard-option,
        .theme-light .simple-wizard-source,
        .theme-light .simple-wizard-search input,
        .theme-light .simple-wizard-secondary,
        .theme-light .simple-wizard-reset-btn,
        .theme-light .simple-wizard-result,
        .theme-light .simple-wizard-candidate {
          background:rgba(255,255,255,.84);
          border-color:rgba(147,161,183,.2);
          color:#16202d;
          box-shadow:0 14px 28px rgba(110,127,153,.12);
        }
        .theme-light .simple-wizard-title,
        .theme-light .simple-wizard-option-title,
        .theme-light .simple-wizard-review-title,
        .theme-light .simple-wizard-section-head,
        .theme-light .simple-wizard-result-title {
          color:#16202d;
        }
        .theme-light .simple-wizard-option-sub,
        .theme-light .simple-wizard-review-sub,
        .theme-light .simple-wizard-section-head small,
        .theme-light .simple-wizard-result-sub,
        .theme-light .simple-wizard-search {
          color:#5f6c80;
        }
        .theme-light .simple-wizard-footer {
          background:linear-gradient(180deg, transparent, rgba(248,250,253,.9) 30%, rgba(248,250,253,.96));
        }
        @media (max-width:560px) {
          .simple-wizard-player-grid,
          .simple-wizard-option-grid,
          .simple-wizard-source-grid,
          .simple-wizard-result-grid {
            grid-template-columns:minmax(0,1fr);
          }
          .simple-wizard-toolbar {
            justify-content:stretch;
          }
          .simple-wizard-reset-btn {
            width:100%;
          }
          .simple-wizard-review-card {
            grid-template-columns:82px minmax(0,1fr);
          }
          .simple-wizard-result {
            grid-template-columns:66px minmax(0,1fr) 32px;
            min-height:94px;
            border-radius:22px;
          }
          .simple-wizard-result-art {
            width:66px;
            border-radius:19px;
          }
          .simple-wizard-review-art {
            width:82px;
            border-radius:20px;
          }
          .simple-wizard-review-title {
            font-size:20px;
          }
          .simple-wizard-footer.triple {
            grid-template-columns:minmax(0,1fr);
          }
        }
        .theme-light .action-tile {
          background:
            linear-gradient(145deg, rgba(255,255,255,.92), rgba(246,249,252,.84)),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 16%, transparent), transparent 42%);
          border-color:rgba(147,161,183,.18);
          box-shadow:0 18px 32px rgba(110,127,153,.14);
        }
        .theme-light .action-tile-kicker {
          color:#8b97a8;
        }
        .menu-body.sheet-actions .theme-light .action-tile .menu-item-sub,
        .theme-light .menu-body.sheet-actions .action-tile .menu-item-sub {
          color:#5a6679;
        }
        .theme-light .action-tile-arrow {
          color:#8a96a8;
        }
        .menu-item-ico,.menu-thumb,.media-category-ico {
          width:48px;
          height:48px;
          border-radius:16px;
          overflow:hidden;
          flex-shrink:0;
          display:grid;
          place-items:center;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 24%, transparent), color-mix(in srgb, var(--ma-accent) 12%, transparent));
          border:1px solid color-mix(in srgb, var(--ma-accent) 24%, transparent);
          color:var(--ma-accent);
        }
        .menu-item-ico .ui-ic { width:46%; height:46%; }
        .menu-thumb img { width:100%; height:100%; object-fit:cover; display:block; }
        .media-entry .menu-thumb img { object-fit:contain; object-position:center; }
        .player-thumb { position:relative; overflow:visible; }
        .player-thumb img { border-radius:inherit; }
        .player-group-badge {
          min-width:22px;
          height:22px;
          padding:0 6px;
          border-radius:999px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          color:#18120a;
          font-size:12px;
          line-height:1;
          font-weight:950;
          box-shadow:0 8px 18px color-mix(in srgb, var(--ma-accent) 24%, transparent);
          flex-shrink:0;
        }
        .player-thumb .player-group-badge {
          position:absolute;
          inset-inline-end:-7px;
          inset-block-start:-7px;
          z-index:2;
        }
        .player-focus-badge,
        .library-focus-badge {
          height:20px;
          min-width:20px;
          font-size:11px;
        }
        .menu-item-title,.queue-title { display:block; max-width:100%; font-size:16px; font-weight:800; line-height:1.25; white-space:normal; overflow:hidden; text-overflow:ellipsis; display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:2; }
        .menu-item-sub,.queue-sub { display:block; max-width:100%; margin-top:4px; font-size:12px; color:rgba(255,255,255,.72); line-height:1.25; white-space:normal; overflow:hidden; text-overflow:ellipsis; display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp:2; }
        .menu-body .menu-item-sub,
        .menu-body .queue-sub { display:none !important; }
        .menu-item.active,.menu-list-item.active {
          border-color:color-mix(in srgb, var(--ma-accent) 28%, transparent);
          background:color-mix(in srgb, var(--ma-accent) 12%, transparent);
        }
        .menu-list-item.tap-feedback,
        .queue-row.tap-feedback {
          transform:translateY(-2px) scale(.988);
          border-color:color-mix(in srgb, var(--ma-accent) 34%, transparent);
          box-shadow:0 12px 30px color-mix(in srgb, var(--ma-accent) 16%, transparent);
        }
        .media-entry,
        .media-category-row,
        .radio-country-entry,
        .media-detail-hero,
        .library-nav-btn,
        .media-layout-btn,
        .media-detail-action-btn,
        .media-detail-play-btn,
        .media-detail-nav-btn,
        .artist-hero-icon-btn,
        .radio-stage-fab,
        .library-tab-search-submit,
        .library-tab-search-clear,
        .library-player-focus {
          position:relative;
        }
        .media-entry.library-action-feedback,
        .media-category-row.library-action-feedback,
        .radio-country-entry.library-action-feedback,
        .media-detail-hero.library-action-feedback {
          transform:translateY(-2px) scale(.988);
          border-color:color-mix(in srgb, var(--ma-accent) 36%, transparent);
          box-shadow:0 14px 32px color-mix(in srgb, var(--ma-accent) 18%, transparent), inset 0 1px 0 rgba(255,255,255,.12);
          filter:saturate(1.04);
        }
        .library-nav-btn.library-action-feedback,
        .media-layout-btn.library-action-feedback,
        .media-detail-action-btn.library-action-feedback,
        .media-detail-play-btn.library-action-feedback,
        .media-detail-nav-btn.library-action-feedback,
        .artist-hero-icon-btn.library-action-feedback,
        .radio-stage-fab.library-action-feedback,
        .library-tab-search-submit.library-action-feedback,
        .library-tab-search-clear.library-action-feedback,
        .library-player-focus.library-action-feedback {
          transform:translateY(-2px) scale(.96);
          box-shadow:0 12px 24px color-mix(in srgb, var(--ma-accent) 22%, transparent);
          color:color-mix(in srgb, var(--ma-accent) 72%, white 28%);
        }
        .media-entry.library-action-loading,
        .media-category-row.library-action-loading,
        .radio-country-entry.library-action-loading,
        .media-detail-hero.library-action-loading,
        .library-nav-btn.library-action-loading,
        .media-layout-btn.library-action-loading,
        .media-detail-action-btn.library-action-loading,
        .media-detail-play-btn.library-action-loading,
        .media-detail-nav-btn.library-action-loading,
        .artist-hero-icon-btn.library-action-loading,
        .radio-stage-fab.library-action-loading,
        .library-tab-search-submit.library-action-loading,
        .library-player-focus.library-action-loading {
          isolation:isolate;
        }
        .media-entry.library-action-loading::after,
        .media-category-row.library-action-loading::after,
        .radio-country-entry.library-action-loading::after,
        .media-detail-hero.library-action-loading::after,
        .library-nav-btn.library-action-loading::after,
        .media-layout-btn.library-action-loading::after,
        .media-detail-action-btn.library-action-loading::after,
        .media-detail-play-btn.library-action-loading::after,
        .media-detail-nav-btn.library-action-loading::after,
        .artist-hero-icon-btn.library-action-loading::after,
        .radio-stage-fab.library-action-loading::after,
        .library-tab-search-submit.library-action-loading::after,
        .library-player-focus.library-action-loading::after {
          content:"";
          position:absolute;
          width:18px;
          height:18px;
          border-radius:999px;
          border:2px solid rgba(255,255,255,.28);
          border-inline-end-color:color-mix(in srgb, var(--ma-accent) 88%, white 12%);
          pointer-events:none;
          z-index:6;
          animation:homeiiLoadingSpin .74s linear infinite;
          box-shadow:0 0 18px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .media-entry.list.library-action-loading::after,
        .media-category-row.library-action-loading::after,
        .radio-country-entry.library-action-loading::after,
        .media-detail-hero.library-action-loading::after {
          inset-inline-end:14px;
          inset-block-start:50%;
          margin-block-start:-9px;
        }
        .media-entry.grid.library-action-loading::after {
          inset-inline-end:12px;
          inset-block-end:12px;
        }
        .library-nav-btn.library-action-loading::after,
        .media-layout-btn.library-action-loading::after,
        .media-detail-action-btn.library-action-loading::after,
        .media-detail-play-btn.library-action-loading::after,
        .media-detail-nav-btn.library-action-loading::after,
        .artist-hero-icon-btn.library-action-loading::after,
        .radio-stage-fab.library-action-loading::after,
        .library-tab-search-submit.library-action-loading::after,
        .library-player-focus.library-action-loading::after {
          width:14px;
          height:14px;
          inset-inline-end:5px;
          inset-block-start:5px;
          border-width:2px;
        }
        .theme-light .media-entry.library-action-loading::after,
        .theme-light .media-category-row.library-action-loading::after,
        .theme-light .radio-country-entry.library-action-loading::after,
        .theme-light .media-detail-hero.library-action-loading::after,
        .theme-light .library-nav-btn.library-action-loading::after,
        .theme-light .media-layout-btn.library-action-loading::after,
        .theme-light .media-detail-action-btn.library-action-loading::after,
        .theme-light .media-detail-play-btn.library-action-loading::after,
        .theme-light .media-detail-nav-btn.library-action-loading::after,
        .theme-light .artist-hero-icon-btn.library-action-loading::after,
        .theme-light .radio-stage-fab.library-action-loading::after,
        .theme-light .library-tab-search-submit.library-action-loading::after,
        .theme-light .library-player-focus.library-action-loading::after {
          border-color:rgba(30,41,59,.24);
          border-inline-end-color:var(--ma-accent);
        }
        @media (prefers-reduced-motion: reduce) {
          .media-entry.library-action-loading::after,
          .media-category-row.library-action-loading::after,
          .radio-country-entry.library-action-loading::after,
          .media-detail-hero.library-action-loading::after,
          .library-nav-btn.library-action-loading::after,
          .media-layout-btn.library-action-loading::after,
          .media-detail-action-btn.library-action-loading::after,
          .media-detail-play-btn.library-action-loading::after,
          .media-detail-nav-btn.library-action-loading::after,
          .artist-hero-icon-btn.library-action-loading::after,
          .radio-stage-fab.library-action-loading::after,
          .library-tab-search-submit.library-action-loading::after,
          .library-player-focus.library-action-loading::after {
            animation:none;
          }
        }
        .theme-dark .menu-sheet,
        .theme-dark .notice,
        .theme-dark .menu-item,
        .theme-dark .menu-list-item,
        .theme-dark .queue-row,
        .theme-dark .media-search-shell,
        .theme-dark .media-category-row {
          background:linear-gradient(180deg, rgba(44,46,52,.58), rgba(24,26,31,.50));
          border-color:rgba(255,255,255,.12);
          color:#f4f6fb;
          backdrop-filter:blur(22px);
          -webkit-backdrop-filter:blur(22px);
        }
        .theme-dark .media-sort-select,
        .theme-dark .settings-select,
        .theme-dark .settings-text-input,
        .theme-dark .announcement-textarea {
          background:linear-gradient(180deg, rgba(34,36,42,.62), rgba(16,18,22,.58));
          color:#f4f6fb;
          border-color:rgba(255,255,255,.12);
          color-scheme:dark;
        }
        .theme-dark .media-sort-select option,
        .theme-dark .settings-select option {
          background:#1a1d22;
          color:#f4f6fb;
        }
        .theme-dark .menu-item-sub,
        .theme-dark .queue-sub,
        .theme-dark .settings-hint {
          color:rgba(236,241,248,.68);
        }
        .theme-dark .menu-backdrop,
        .theme-dark .queue-action-backdrop {
          background:rgba(4,5,8,.34);
          backdrop-filter:blur(24px);
          -webkit-backdrop-filter:blur(24px);
        }
        .theme-dark .queue-action-sheet,
        .theme-dark .lyrics-sheet,
        .theme-dark .player-menu-card,
        .theme-dark .group-player-card,
        .theme-dark .settings-group,
        .theme-dark .announcement-target,
        .theme-dark .surprise-popup-card,
        .theme-dark .player-focus {
          background:linear-gradient(180deg, rgba(46,48,54,.62), rgba(24,26,31,.52));
          border-color:rgba(255,255,255,.12);
          box-shadow:0 22px 48px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.08);
          backdrop-filter:blur(22px);
          -webkit-backdrop-filter:blur(22px);
        }
        .theme-dark .announcement-target-select,
        .theme-dark #mobileAnnouncementTargetSelect {
          background:linear-gradient(180deg, rgba(36,38,44,.68), rgba(18,20,24,.58));
          border-color:rgba(255,255,255,.12);
          color:#f4f6fb;
        }
        .theme-dark #mobileAnnouncementTargetSelect option {
          background:#1a1d22;
          color:#f4f6fb;
        }
        .theme-dark .settings-pill,
        .theme-dark .settings-check-pill,
        .theme-dark .empty-quick-card {
          background:linear-gradient(180deg, rgba(50,52,58,.54), rgba(24,26,31,.44));
          border-color:rgba(255,255,255,.12);
          box-shadow:0 16px 34px rgba(0,0,0,.18);
        }
        .theme-dark .empty-quick-kicker {
          color:rgba(236,241,248,.56);
        }
        .theme-light .empty-quick-card {
          background:linear-gradient(145deg, rgba(255,255,255,.88), rgba(242,246,251,.76));
          border-color:rgba(147,161,183,.18);
          box-shadow:0 16px 30px rgba(111,126,150,.14);
        }
        .theme-light .empty-quick-kicker {
          color:#7f8a9b;
        }
        .theme-light .empty-quick-title {
          color:#1f2633;
        }
        .theme-light .surprise-me-card.magic-empty {
          background:
            radial-gradient(circle at 50% 24%, rgba(31,38,51,.08), transparent 30%),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 28%, rgba(31,38,51,.06)), transparent 34%),
            linear-gradient(180deg, rgba(255,255,255,.82), rgba(240,244,250,.66));
          border-color:rgba(147,161,183,.18);
          box-shadow:0 18px 36px rgba(111,126,150,.14), inset 0 1px 0 rgba(255,255,255,.46);
        }
        .theme-light .surprise-me-wand {
          color:#1f2633;
        }
        .theme-light .surprise-me-wand .ui-ic {
          filter:drop-shadow(0 0 12px rgba(31,38,51,.10));
        }
        .theme-light .surprise-me-card.magic-empty:active,
        .theme-light .surprise-me-card.magic-empty.pressed {
          box-shadow:0 14px 28px rgba(111,126,150,.12), 0 0 0 1px rgba(31,38,51,.10), 0 0 18px rgba(31,38,51,.08);
        }
        .theme-light .empty-voice-btn {
          color:#7a5210;
          background:linear-gradient(180deg, rgba(255,255,255,.88), rgba(239,244,250,.68));
          border-color:color-mix(in srgb, var(--ma-accent) 30%, rgba(147,161,183,.2));
          box-shadow:0 14px 28px rgba(111,126,150,.14), 0 0 0 5px rgba(255,255,255,.45), inset 0 1px 0 rgba(255,255,255,.78);
        }
        .theme-light .empty-voice-btn:hover,
        .theme-light .empty-voice-btn.listening {
          color:#6e4b10;
          background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 16%, white 84%), rgba(255,255,255,.8));
          box-shadow:0 16px 32px color-mix(in srgb, var(--ma-accent) 14%, rgba(111,126,150,.14)), 0 0 0 7px color-mix(in srgb, var(--ma-accent) 8%, transparent), inset 0 1px 0 rgba(255,255,255,.82);
        }
        .theme-light .menu-item.active,
        .theme-light .menu-list-item.active,
        .theme-light .queue-row.active {
          background:color-mix(in srgb, var(--ma-accent) 14%, transparent);
          border-color:color-mix(in srgb, var(--ma-accent) 30%, transparent);
        }
        .media-search-shell,.media-category-row {
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.08);
        }
        .theme-light .media-search-shell,
        .theme-light .media-category-row {
          background:rgba(255,255,255,.62);
          border-color:rgba(147,161,183,.2);
          color:#1f2633;
        }
        .media-home-shell { display:grid; gap:14px; align-content:start; }
        .library-shell {
          position:relative;
          display:grid;
          grid-template-rows:auto minmax(0,1fr) auto;
          gap:14px;
          min-height:100%;
          height:100%;
        }
        .library-player-focus {
          display:grid;
          grid-template-columns:44px minmax(0,1fr) auto auto;
          align-items:center;
          justify-content:stretch;
          gap:10px;
          width:100%;
          min-height:62px;
          padding:10px 14px;
          border:none;
          border-radius:20px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          color:inherit;
          font:inherit;
          text-align:start;
          cursor:pointer;
        }
        .library-player-art {
          width:44px;
          height:44px;
          border-radius:15px;
          display:grid;
          place-items:center;
          overflow:hidden;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
        }
        .library-player-art img {
          width:100%;
          height:100%;
          object-fit:cover;
          display:block;
        }
        .library-player-art .ui-ic {
          width:20px;
          height:20px;
          opacity:.72;
        }
        .library-player-copy {
          min-width:0;
          display:grid;
          gap:3px;
        }
        .library-player-name {
          font-size:15px;
          font-weight:750;
          overflow:hidden;
          white-space:nowrap;
          text-overflow:ellipsis;
        }
        .library-player-state {
          width:max-content;
          max-width:100%;
          padding:3px 9px;
          border-radius:999px;
          font-size:11px;
          font-weight:850;
          color:#58d68d;
          background:rgba(54,183,113,.14);
          border:1px solid rgba(88,214,141,.25);
        }
        .library-player-focus.is-playing .library-player-state {
          color:#ff6f6f;
          background:rgba(221,62,62,.16);
          border-color:rgba(255,111,111,.28);
        }
        .theme-light .library-player-focus {
          background:rgba(255,255,255,.72);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .library-player-art {
          background:rgba(255,255,255,.9);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .library-player-name { color:#172033; }
        .theme-light .library-player-state {
          color:#138a54;
          background:rgba(46,178,112,.13);
          border-color:rgba(34,154,96,.22);
        }
        .theme-light .library-player-focus.is-playing .library-player-state {
          color:#c83e43;
          background:rgba(216,72,78,.13);
          border-color:rgba(196,59,65,.22);
        }
        .library-player-focus .eq-icon {
          display:inline-flex;
          color:rgba(255,255,255,.42);
        }
        .library-player-focus:not(.is-playing) .eq-icon span {
          animation:none;
          height:5px;
          background:currentColor;
        }
        .library-player-focus.is-playing .eq-icon {
          color:var(--ma-accent);
        }
        .theme-light .library-player-focus .eq-icon {
          color:rgba(23,32,51,.34);
        }
        .theme-light .library-player-focus.is-playing .eq-icon {
          color:var(--ma-accent);
        }
        .library-body {
          display:grid;
          gap:14px;
          align-content:start;
          min-height:0;
          overflow:auto;
          padding-inline-end:2px;
        }
        .card.layout-tablet .library-body {
          gap:14px;
        }
        .library-nav {
          display:grid;
          grid-template-columns:repeat(7, minmax(0, 1fr));
          gap:8px;
          padding:8px;
          border-radius:22px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          position:sticky;
          bottom:0;
          align-self:end;
          z-index:5;
        }
        .rtl .library-nav {
          direction:rtl;
        }
        .rtl .library-nav-btn {
          direction:ltr;
        }
        .card.layout-tablet .library-nav {
          gap:6px;
          padding:6px;
          border-radius:16px;
        }
        .theme-light .library-nav {
          background:rgba(255,255,255,.72);
          border-color:rgba(147,161,183,.2);
        }
        .library-nav-btn {
          min-height:56px;
          border:none;
          border-radius:18px;
          display:grid;
          place-items:center;
          background:transparent;
          color:inherit;
          cursor:pointer;
          transition:transform .16s ease, background-color .16s ease, box-shadow .16s ease, color .16s ease;
        }
        .card.layout-tablet .library-nav-btn {
          min-height:46px;
          border-radius:16px;
        }
        .library-nav-btn.tap-feedback {
          transform:translateY(-2px) scale(.96);
          box-shadow:0 12px 22px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .library-nav-btn .ui-ic { width:22px; height:22px; }
        .library-nav-btn.active {
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 10px 18px color-mix(in srgb, var(--ma-accent) 18%, transparent);
        }
        .media-search-zone {
          position:sticky;
          top:-2px;
          z-index:8;
          padding:2px 0 10px;
          background:linear-gradient(180deg, rgba(12,15,22,.96), rgba(12,15,22,.78) 72%, rgba(12,15,22,0));
        }
        .theme-light .media-search-zone {
          background:linear-gradient(180deg, rgba(247,250,253,.98), rgba(247,250,253,.82) 72%, rgba(247,250,253,0));
        }
        .media-search-shell {
          display:grid;
          grid-template-columns:20px minmax(0,1fr) 38px 20px;
          padding:12px 14px;
          gap:10px;
          align-items:center;
          position:relative;
          z-index:1;
        }
        .media-search-shell input {
          border:none;
          background:transparent;
          color:#fff;
          font:inherit;
          outline:none;
          min-width:0;
          width:100%;
          text-align:start;
          direction:auto;
        }
        .theme-light .media-search-shell input { color:#1f2633; }
        .media-search-shell input::placeholder { color:rgba(255,255,255,.52); }
        .theme-light .media-search-shell input::placeholder { color:rgba(55,68,85,.52); }
        .media-search-clear {
          border:none;
          background:transparent;
          color:rgba(255,255,255,.72);
          width:34px;
          height:34px;
          border-radius:12px;
          display:grid;
          place-items:center;
          font-size:22px;
          font-weight:950;
          line-height:1;
          cursor:pointer;
          transition:transform .16s ease, background-color .16s ease, color .16s ease, box-shadow .16s ease;
        }
        .theme-light .media-search-clear:not(.visible) { color:rgba(31,38,51,.68); }
        .media-search-clear.visible {
          color:#fff;
          background:linear-gradient(135deg, rgba(225,63,63,.98), rgba(180,36,36,.92));
          box-shadow:0 12px 22px rgba(197,48,48,.22);
        }
        .media-search-clear.visible:active {
          transform:scale(.92);
        }
        .media-voice-btn {
          width:38px;
          height:38px;
          border:none;
          border-radius:14px;
          display:grid;
          place-items:center;
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 10px 20px color-mix(in srgb, var(--ma-accent) 18%, transparent);
          cursor:pointer;
          transition:transform .16s ease, box-shadow .16s ease, opacity .16s ease;
        }
        .media-voice-btn .ui-ic { width:19px; height:19px; }
        .media-voice-btn:active { transform:scale(.94); }
        .media-voice-btn.unsupported { opacity:.52; filter:saturate(.55); }
        .media-voice-btn.listening {
          animation:voicePulse 1s ease-in-out infinite;
          box-shadow:0 0 0 8px color-mix(in srgb, var(--ma-accent) 14%, transparent), 0 14px 26px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .theme-light .media-search-clear:not(.visible) { color:rgba(55,68,85,.76); }
        @keyframes voicePulse {
          0%,100% { transform:scale(1); }
          50% { transform:scale(1.07); }
        }
        .media-categories {
          display:grid;
          gap:10px;
        }
        .media-home-content {
          display:grid;
          gap:14px;
          align-content:start;
        }
        .media-category-row {
          border:none;
          cursor:pointer;
          justify-content:flex-start;
        }
        .media-category-ico .ui-ic { width:46%; height:46%; }
        .media-results {
          display:grid;
          gap:18px;
        }
        .media-toolbar {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          margin:2px 0 8px;
        }
        .media-toolbar-left,
        .media-toolbar-right {
          display:flex;
          align-items:center;
          gap:10px;
          min-width:0;
        }
        .library-toolbar-actions {
          display:flex;
          align-items:center;
          gap:8px;
        }
        .media-sort-select {
          min-height:42px;
          min-width:112px;
          padding:0 14px;
          border-radius:16px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.08);
          color:inherit;
          font:inherit;
          font-size:13px;
          font-weight:800;
          outline:none;
        }
        .theme-light .media-sort-select {
          background:rgba(255,255,255,.68);
          border-color:rgba(147,161,183,.2);
          color:#1f2633;
        }
        .media-layout-toggle {
          display:inline-flex;
          align-items:center;
          gap:6px;
          padding:6px;
          border-radius:18px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(16px);
          -webkit-backdrop-filter:blur(16px);
        }
        .theme-light .media-layout-toggle {
          background:rgba(255,255,255,.68);
          border-color:rgba(147,161,183,.2);
        }
        .media-layout-btn {
          min-width:40px;
          min-height:38px;
          padding:8px 10px;
          border:none;
          border-radius:14px;
          background:transparent;
          color:inherit;
          font:inherit;
          font-size:0;
          font-weight:800;
          cursor:pointer;
          display:grid;
          place-items:center;
        }
        .media-layout-btn.tap-feedback {
          transform:translateY(-2px) scale(.96);
          box-shadow:0 12px 22px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .media-layout-btn .ui-ic { width:18px; height:18px; }
        .media-layout-btn.active {
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 10px 18px color-mix(in srgb, var(--ma-accent) 18%, transparent);
        }
        .media-items-list {
          display:grid;
          gap:16px;
          align-content:start;
        }
        .media-items-list.layout-grid {
          --media-grid-thumb-size:var(--flow-media-grid-thumb);
          grid-template-columns:repeat(auto-fill, minmax(156px, 1fr));
          grid-auto-rows:auto;
        }
        .card.layout-tablet .media-items-list.layout-grid {
          --media-grid-thumb-size:var(--flow-tablet-media-grid-thumb);
          grid-template-columns:repeat(auto-fill, minmax(204px, 1fr));
          gap:18px;
        }
        .media-items-list.layout-list {
          grid-template-columns:1fr;
        }
        .media-entry {
          width:100%;
          min-width:0;
          overflow:hidden;
          border:none;
          gap:10px;
        }
        .media-entry-main {
          min-width:0;
          flex:1;
          display:flex;
          align-items:center;
          gap:12px;
          border:none;
          background:none;
          color:inherit;
          padding:0;
          text-align:inherit;
          cursor:pointer;
        }
        .media-entry-actions {
          flex-shrink:0;
          display:inline-flex;
          align-items:center;
          gap:8px;
        }
        .media-more-btn {
          flex-shrink:0;
          min-width:38px;
          min-height:38px;
          width:38px;
          border-radius:14px;
          display:grid;
          place-items:center;
          color:var(--ma-accent);
        }
        .media-play-btn {
          flex-shrink:0;
          min-width:38px;
          min-height:38px;
          width:38px;
          border-radius:14px;
          display:grid;
          place-items:center;
          color:#fff;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.12)), rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.14));
          box-shadow:0 12px 22px color-mix(in srgb, var(--ma-accent) 14%, transparent), inset 0 1px 0 rgba(255,255,255,.12);
        }
        .media-more-btn .ui-ic {
          width:17px;
          height:17px;
        }
        .media-play-btn .ui-ic {
          width:16px;
          height:16px;
          margin-inline-start:2px;
        }
        .theme-light .media-more-btn {
          color:var(--ma-accent);
          background:color-mix(in srgb, var(--ma-accent) 14%, rgba(255,255,255,.86));
          border:1px solid color-mix(in srgb, var(--ma-accent) 30%, rgba(147,161,183,.2));
          box-shadow:0 10px 18px color-mix(in srgb, var(--ma-accent) 14%, rgba(111,126,150,.12));
        }
        .theme-light .media-play-btn {
          color:#172033;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 22%, white 78%), rgba(255,255,255,.9));
          border-color:color-mix(in srgb, var(--ma-accent) 32%, rgba(147,161,183,.22));
          box-shadow:0 10px 18px color-mix(in srgb, var(--ma-accent) 12%, rgba(111,126,150,.12));
        }
        .media-entry.list {
          min-height:82px;
          gap:12px;
          padding:14px 14px;
          align-items:center;
          border-radius:22px;
        }
        .media-entry.list .menu-thumb {
          width:52px;
          height:52px;
          border-radius:16px;
        }
        .media-entry .menu-thumb .ui-ic {
          width:44%;
          height:44%;
          opacity:.68;
        }
        .flag-thumb {
          font-size:26px;
          line-height:1;
        }
        .flag-emoji {
          display:block;
          filter:saturate(1.05);
        }
        .media-entry.grid {
          position:relative;
          display:grid;
          grid-template-columns:minmax(0,1fr);
          align-content:start;
          justify-items:center;
          gap:9px;
          padding:12px 12px 14px;
          min-height:calc(var(--media-grid-thumb-size, 188px) + 62px);
          border-radius:24px;
          background:linear-gradient(180deg, rgba(255,255,255,.055), rgba(255,255,255,.025));
          border:1px solid rgba(255,255,255,.07);
          box-shadow:0 12px 26px rgba(0,0,0,.1), inset 0 1px 0 rgba(255,255,255,.045);
          text-align:center;
        }
        .media-entry.grid .media-entry-main {
          display:grid;
          justify-items:center;
          align-content:start;
          gap:9px;
          width:100%;
          min-width:0;
        }
        .media-entry.grid .menu-thumb {
          width:min(100%, var(--media-grid-thumb-size, 150px));
          max-width:var(--media-grid-thumb-size, 188px);
          height:var(--media-grid-thumb-size, 150px);
          min-height:var(--media-grid-thumb-size, 150px);
          aspect-ratio:1/1;
          border-radius:20px;
          flex:0 0 auto;
          justify-self:center;
          box-shadow:0 16px 34px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .media-entry.grid .menu-thumb img {
          width:100%;
          height:100%;
          object-fit:contain;
          display:block;
        }
        .media-entry.grid .media-more-btn {
          position:absolute;
          inset-block-start:14px;
          inset-inline-start:14px;
          z-index:2;
          width:34px;
          min-width:34px;
          height:34px;
          min-height:34px;
          border-radius:13px;
          background:rgba(13,16,23,.44);
          border:1px solid rgba(255,255,255,.18);
          color:rgba(255,255,255,.74);
          box-shadow:0 10px 22px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.08);
          backdrop-filter:blur(12px);
          -webkit-backdrop-filter:blur(12px);
        }
        .media-entry.grid .media-play-btn {
          position:absolute;
          inset-block-start:14px;
          inset-inline-end:14px;
          z-index:2;
          width:34px;
          min-width:34px;
          height:34px;
          min-height:34px;
          border-radius:13px;
          background:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);
          border:1px solid rgba(255,255,255,.2);
          color:rgba(255,255,255,.92);
          box-shadow:0 10px 22px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.1);
          backdrop-filter:blur(12px);
          -webkit-backdrop-filter:blur(12px);
        }
        .media-entry.grid .media-more-btn .ui-ic {
          width:15px;
          height:15px;
        }
        .media-entry.grid .media-play-btn .ui-ic {
          width:14px;
          height:14px;
        }
        .media-entry.grid .menu-item-title {
          width:100%;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
          overflow:hidden;
          font-size:14px;
          line-height:1.16;
          font-weight:850;
          letter-spacing:0;
        }
        .media-entry.grid .menu-item-sub {
          margin-top:4px;
          display:-webkit-box;
          -webkit-line-clamp:1;
          -webkit-box-orient:vertical;
          overflow:hidden;
          font-size:11px;
          line-height:1.25;
          opacity:.72;
        }
        .theme-light .media-entry.grid {
          background:linear-gradient(180deg, rgba(255,255,255,.78), rgba(247,250,253,.58));
          border-color:rgba(132,147,168,.14);
          box-shadow:0 12px 26px rgba(80,98,124,.09), inset 0 1px 0 rgba(255,255,255,.52);
        }
        .theme-light .media-entry.grid .media-more-btn {
          background:rgba(255,255,255,.72);
          border-color:rgba(132,147,168,.22);
          color:#526176;
        }
        .theme-light .media-entry.grid .media-play-btn {
          background:color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.82));
          border-color:color-mix(in srgb, var(--ma-accent) 30%, rgba(132,147,168,.22));
          color:#172033;
        }
        .media-detail-shell {
          display:grid;
          gap:14px;
          align-content:start;
        }
        .detail-popup-shell {
          height:100%;
          min-height:0;
          grid-template-rows:minmax(0,1fr);
        }
        .detail-popup-shell .library-body {
          height:100%;
          overflow:hidden;
          align-content:stretch;
        }
        .artist-detail-page-shell .library-body {
          overflow:auto;
          align-content:start;
        }
        .album-detail-shell {
          height:100%;
          min-height:0;
          grid-template-rows:auto auto minmax(0,1fr);
          align-content:stretch;
        }
        .media-detail-hero {
          display:grid;
          grid-template-columns:72px minmax(0,1fr) auto;
          align-items:center;
          gap:14px;
          padding:14px;
          border-radius:24px;
        }
        .album-detail-player {
          position:sticky;
          top:0;
          z-index:3;
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
        }
        .media-detail-art {
          width:72px;
          height:72px;
          border-radius:20px;
          display:grid;
          place-items:center;
          overflow:hidden;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.08)), rgba(255,255,255,.06));
          border:1px solid rgba(255,255,255,.14);
          color:var(--ma-accent);
        }
        .media-detail-art img {
          width:100%;
          height:100%;
          object-fit:contain;
          object-position:center;
          display:block;
        }
        .media-detail-art .ui-ic {
          width:34%;
          height:34%;
        }
        .media-detail-copy {
          min-width:0;
          display:grid;
          gap:4px;
        }
        .media-detail-kicker {
          font-size:11px;
          font-weight:850;
          text-transform:uppercase;
          color:color-mix(in srgb, var(--ma-accent) 78%, white 22%);
          letter-spacing:.08em;
        }
        .media-detail-title {
          font-size:18px;
          font-weight:950;
          line-height:1.15;
          overflow:hidden;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
        }
        .media-detail-sub {
          font-size:12px;
          color:rgba(255,255,255,.66);
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .media-detail-kind-badge {
          display:inline-flex;
          vertical-align:middle;
          margin-inline-start:8px;
          padding:3px 8px;
          border-radius:999px;
          font-size:10px;
          font-weight:900;
          color:color-mix(in srgb, var(--ma-accent) 78%, white 22%);
          background:color-mix(in srgb, var(--ma-accent) 14%, rgba(255,255,255,.07));
          border:1px solid color-mix(in srgb, var(--ma-accent) 26%, rgba(255,255,255,.12));
        }
        .media-detail-album-picker {
          min-width:0;
          display:grid;
          grid-template-columns:auto minmax(0,1fr);
          align-items:center;
          gap:8px;
          margin-top:4px;
        }
        .media-detail-picker-label {
          font-size:10px;
          font-weight:950;
          letter-spacing:.08em;
          text-transform:uppercase;
          color:rgba(255,255,255,.48);
        }
        .media-detail-album-picker select {
          min-width:0;
          width:100%;
          height:36px;
          border-radius:14px;
          padding:0 34px 0 12px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.075);
          color:rgba(255,255,255,.9);
          font:inherit;
          font-size:12px;
          font-weight:850;
          outline:none;
        }
        .media-detail-album-picker select option {
          color:#172033;
          background:#fff;
        }
        .media-detail-play-btn {
          width:46px;
          min-width:46px;
          height:46px;
          border-radius:16px;
          display:grid;
          place-items:center;
          color:#fff;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.12)), rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 30%, rgba(255,255,255,.14));
          box-shadow:0 14px 28px color-mix(in srgb, var(--ma-accent) 16%, transparent), inset 0 1px 0 rgba(255,255,255,.14);
        }
        .media-detail-play-btn .ui-ic {
          width:18px;
          height:18px;
          margin-inline-start:2px;
        }
        .media-detail-play-btn.subtle {
          background:rgba(255,255,255,.08);
          border-color:rgba(255,255,255,.14);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.1);
        }
        .media-detail-player-actions {
          display:flex;
          align-items:center;
          justify-content:flex-end;
          gap:8px;
        }
        .media-detail-nav-btn {
          width:38px;
          min-width:38px;
          height:38px;
          border-radius:14px;
          display:grid;
          place-items:center;
          padding:0;
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.12);
          color:rgba(255,255,255,.82);
        }
        .media-detail-nav-btn[disabled] {
          opacity:.36;
          cursor:default;
        }
        .media-detail-browse-count {
          min-width:38px;
          text-align:center;
          font-size:11px;
          font-weight:900;
          color:rgba(255,255,255,.58);
        }
        .media-detail-nav-btn .ui-ic {
          width:17px;
          height:17px;
        }
        .media-detail-toolbar {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:10px;
          min-width:0;
        }
        .media-detail-toolbar .media-section-title {
          min-width:0;
          margin:0;
        }
        .media-detail-count-badge {
          padding:6px 10px;
          border-radius:999px;
          font-size:11px;
          font-weight:850;
          color:rgba(255,255,255,.68);
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.1);
        }
        .media-detail-empty {
          min-height:88px;
          display:grid;
          align-items:center;
        }
        .media-detail-track-list {
          min-height:0;
          overflow:auto;
          display:grid;
          align-content:start;
          gap:10px;
          padding:0 2px 4px;
        }
        .media-detail-track-row {
          min-height:64px;
          display:grid;
          grid-template-columns:28px minmax(0,1fr) auto auto;
          align-items:center;
          gap:10px;
          padding:10px;
          border-radius:18px;
          border:1px solid rgba(255,255,255,.1);
          background:linear-gradient(180deg, rgba(255,255,255,.075), rgba(255,255,255,.035));
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08);
        }
        .media-detail-track-index {
          width:28px;
          height:28px;
          border-radius:999px;
          display:grid;
          place-items:center;
          font-size:12px;
          font-weight:900;
          color:rgba(255,255,255,.62);
          background:rgba(255,255,255,.06);
        }
        .media-detail-track-copy {
          min-width:0;
          display:grid;
          gap:3px;
        }
        .media-detail-track-title {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          font-size:14px;
          font-weight:850;
          color:rgba(255,255,255,.94);
        }
        .media-detail-track-sub {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          font-size:11px;
          font-weight:650;
          color:rgba(255,255,255,.56);
        }
        .media-detail-track-duration {
          font-size:11px;
          font-weight:800;
          color:rgba(255,255,255,.48);
        }
        .media-detail-track-actions {
          display:flex;
          align-items:center;
          gap:6px;
        }
        .media-detail-action-btn {
          width:34px;
          min-width:34px;
          height:34px;
          border-radius:12px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.07);
          color:rgba(255,255,255,.9);
          display:grid;
          place-items:center;
          padding:0;
          cursor:pointer;
        }
        .media-detail-action-btn.primary {
          color:#15110b;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          border-color:transparent;
        }
        .media-detail-action-btn.active {
          color:color-mix(in srgb, var(--ma-accent) 82%, white 18%);
          border-color:color-mix(in srgb, var(--ma-accent) 38%, rgba(255,255,255,.14));
          background:color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.07));
        }
        .media-detail-action-btn .ui-ic {
          width:17px;
          height:17px;
        }
        .theme-light .media-detail-track-row {
          background:linear-gradient(180deg, rgba(255,255,255,.78), rgba(247,250,253,.56));
          border-color:rgba(132,147,168,.16);
        }
        .theme-light .media-detail-track-title { color:#202736; }
        .theme-light .media-detail-track-sub,
        .theme-light .media-detail-track-duration,
        .theme-light .media-detail-track-index { color:#68748a; }
        .theme-light .media-detail-action-btn {
          background:rgba(255,255,255,.72);
          border-color:rgba(132,147,168,.2);
          color:#526176;
        }
        .theme-light .media-detail-nav-btn {
          background:rgba(255,255,255,.72);
          border-color:rgba(132,147,168,.2);
          color:#526176;
        }
        .theme-light .media-detail-browse-count {
          color:#68748a;
        }
        .theme-light .media-detail-action-btn.primary {
          color:#14110a;
          border-color:transparent;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
        }
        @media (max-width:520px) {
          .media-detail-hero {
            grid-template-columns:58px minmax(0,1fr) auto;
            gap:10px;
            padding:11px;
            border-radius:20px;
          }
          .media-detail-art {
            width:58px;
            height:58px;
            border-radius:16px;
          }
          .media-detail-player-actions {
            flex-direction:column;
            gap:5px;
          }
          .media-detail-play-btn {
            width:38px;
            min-width:38px;
            height:38px;
            border-radius:13px;
          }
          .media-detail-track-row {
            grid-template-columns:24px minmax(0,1fr) auto;
            gap:8px;
            padding:9px;
          }
          .media-detail-track-duration {
            display:none;
          }
          .media-detail-track-actions {
            gap:4px;
          }
          .media-detail-action-btn {
            width:31px;
            min-width:31px;
            height:31px;
            border-radius:11px;
          }
        }
        .artist-detail-shell {
          gap:16px;
        }
        .artist-detail-hero {
          display:grid;
          grid-template-columns:96px minmax(0,1fr);
          align-items:center;
          gap:16px;
          padding:16px;
          border-radius:24px;
          background:linear-gradient(145deg, color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.08)), rgba(255,255,255,.055));
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 18px 36px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.06);
        }
        .artist-detail-art {
          width:96px;
          aspect-ratio:1/1;
          border-radius:22px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08);
          color:var(--ma-accent);
          border:1px solid rgba(255,255,255,.14);
        }
        .artist-detail-art img {
          width:100%;
          height:100%;
          object-fit:contain;
          object-position:center;
          display:block;
        }
        .artist-detail-copy {
          min-width:0;
          display:grid;
          gap:7px;
          justify-items:start;
        }
        .artist-detail-title {
          font-size:24px;
          line-height:1.08;
          font-weight:950;
          overflow:hidden;
          display:-webkit-box;
          -webkit-line-clamp:2;
          -webkit-box-orient:vertical;
        }
        .artist-detail-description {
          color:rgba(255,255,255,.72);
          font-size:13px;
          line-height:1.35;
          display:-webkit-box;
          -webkit-line-clamp:4;
          -webkit-box-orient:vertical;
          overflow:hidden;
        }
        .artist-detail-description.muted {
          color:rgba(255,255,255,.52);
        }
        .artist-info-btn {
          width:38px;
          min-width:38px;
          min-height:36px;
          padding:0;
          border-radius:14px;
          border:1px solid color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.14));
          background:color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.07));
          color:color-mix(in srgb, var(--ma-accent) 82%, white 18%);
          display:inline-flex;
          align-items:center;
          justify-content:center;
          font:inherit;
          font-size:12px;
          font-weight:950;
          cursor:pointer;
        }
        .artist-info-btn .ui-ic {
          width:16px;
          height:16px;
        }
        .artist-info-backdrop {
          position:absolute;
          inset:0;
          z-index:25;
          display:flex;
          align-items:center;
          justify-content:center;
          padding:18px;
          background:rgba(5,7,12,.58);
          backdrop-filter:blur(12px);
          -webkit-backdrop-filter:blur(12px);
        }
        .artist-info-dialog {
          width:min(100%, 620px);
          max-height:min(620px, calc(100% - 20px));
          display:grid;
          grid-template-rows:auto minmax(0,1fr);
          overflow:hidden;
          border-radius:24px;
          border:1px solid rgba(255,255,255,.14);
          background:linear-gradient(180deg, rgba(26,29,40,.96), rgba(12,14,22,.98));
          box-shadow:0 24px 60px rgba(0,0,0,.34), inset 0 1px 0 rgba(255,255,255,.08);
          color:rgba(255,255,255,.9);
        }
        .artist-info-head {
          min-height:58px;
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          padding:14px 16px;
          border-bottom:1px solid rgba(255,255,255,.1);
        }
        .artist-info-title {
          min-width:0;
          font-size:18px;
          font-weight:950;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .artist-info-close {
          width:40px;
          min-width:40px;
          height:40px;
          border-radius:14px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(255,255,255,.08);
          color:inherit;
          display:grid;
          place-items:center;
          padding:0;
          cursor:pointer;
        }
        .artist-info-body {
          overflow:auto;
          padding:16px;
          color:rgba(255,255,255,.74);
          font-size:14px;
          line-height:1.48;
          white-space:pre-wrap;
        }
        .theme-light .artist-info-btn {
          background:color-mix(in srgb, var(--ma-accent) 13%, rgba(255,255,255,.9));
          border-color:color-mix(in srgb, var(--ma-accent) 26%, rgba(147,161,183,.22));
          color:#172033;
        }
        .theme-light .artist-info-backdrop {
          background:rgba(225,232,243,.58);
        }
        .theme-light .artist-info-dialog {
          background:linear-gradient(180deg, rgba(255,255,255,.98), rgba(245,248,253,.98));
          border-color:rgba(147,161,183,.2);
          color:#172033;
          box-shadow:0 24px 60px rgba(80,98,124,.18), inset 0 1px 0 rgba(255,255,255,.62);
        }
        .theme-light .artist-info-head {
          border-bottom-color:rgba(147,161,183,.18);
        }
        .theme-light .artist-info-close {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.22);
        }
        .theme-light .artist-info-body {
          color:#536176;
        }
        .artist-detail-search,
        .artist-detail-section,
        .artist-year-group {
          min-width:0;
          display:grid;
          gap:10px;
        }
        .artist-section-head {
          min-width:0;
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:10px;
        }
        .artist-search-shell {
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          align-items:center;
        }
        .artist-search-btn {
          min-height:38px;
          padding:0 14px;
          border-radius:14px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          gap:6px;
        }
        .artist-search-btn .ui-ic {
          width:16px;
          height:16px;
        }
        .artist-year-title {
          color:color-mix(in srgb, var(--ma-accent) 78%, white 22%);
          font-size:13px;
          font-weight:950;
          padding-inline:2px;
        }
        .theme-light .artist-detail-hero {
          background:linear-gradient(145deg, color-mix(in srgb, var(--ma-accent) 13%, rgba(255,255,255,.9)), rgba(246,249,253,.74));
          border-color:rgba(147,161,183,.18);
          box-shadow:0 18px 34px rgba(110,127,153,.13), inset 0 1px 0 rgba(255,255,255,.48);
        }
        .theme-light .artist-detail-description {
          color:#586579;
        }
        .theme-light .artist-detail-description.muted {
          color:#7e8999;
        }
        @media (max-width: 430px) {
          .artist-detail-hero {
            grid-template-columns:78px minmax(0,1fr);
            gap:12px;
            padding:13px;
          }
          .artist-detail-art {
            width:78px;
            border-radius:20px;
          }
          .artist-detail-title {
            font-size:20px;
          }
          .artist-detail-description {
            -webkit-line-clamp:3;
          }
          .artist-search-shell {
            grid-template-columns:auto minmax(0,1fr);
          }
          .artist-search-btn {
            grid-column:1 / -1;
            width:100%;
          }
        }
        .theme-light .media-detail-sub {
          color:#536176;
        }
        .theme-light .media-detail-kind-badge {
          color:#172033;
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.82));
          border-color:color-mix(in srgb, var(--ma-accent) 24%, rgba(147,161,183,.22));
        }
        .theme-light .media-detail-picker-label {
          color:#768196;
        }
        .theme-light .media-detail-album-picker select {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.22);
          color:#172033;
        }
        .theme-light .media-detail-play-btn {
          color:#172033;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 24%, white 76%), rgba(255,255,255,.9));
          border-color:color-mix(in srgb, var(--ma-accent) 32%, rgba(147,161,183,.22));
        }
        .media-section-title {
          font-size:12px;
          letter-spacing:.12em;
          text-transform:uppercase;
          color:rgba(255,255,255,.56);
          margin:4px 2px 0;
        }
        .radio-browser-country-head {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          margin:4px 0 12px;
        }
        .radio-browser-country-head .media-section-title {
          flex:1;
          margin:0;
        }
        .radio-country-entry {
          cursor:pointer;
        }
        .queue-list {
          display:grid;
          gap:16px;
          align-content:start;
        }
        .card.layout-tablet .queue-page-head {
          width:min(100%, 760px);
          margin:0 auto 14px;
        }
        .card.layout-tablet .queue-list {
          width:min(100%, 760px);
          margin:0 auto;
          gap:10px;
        }
        .queue-page-head {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          margin-bottom:10px;
        }
        .queue-page-head-title {
          font-size:15px;
          font-weight:900;
          letter-spacing:.02em;
        }
        .queue-page-head-actions {
          display:flex;
          align-items:center;
          gap:8px;
        }
        .queue-page-count {
          margin-inline-start:auto;
          display:inline-flex;
          align-items:center;
          gap:7px;
          min-height:42px;
          padding:0 12px;
          border-radius:14px;
          background:rgba(255,255,255,.065);
          border:1px solid rgba(255,255,255,.11);
          color:rgba(255,255,255,.78);
          font-size:11px;
          font-weight:850;
        }
        .queue-page-count strong {
          color:var(--ma-accent);
          font-size:14px;
          font-weight:950;
        }
        .queue-head-transfer-btn {
          min-width:132px;
          height:42px;
          border:none;
          border-radius:14px;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          color:inherit;
          cursor:pointer;
          backdrop-filter:blur(16px);
          -webkit-backdrop-filter:blur(16px);
          padding:0 12px;
          gap:5px;
          font-size:11px;
          font-weight:900;
        }
        .queue-head-transfer-label {
          white-space:nowrap;
          line-height:1;
        }
        .queue-head-transfer-count {
          line-height:1;
          color:var(--ma-accent);
          font-size:12px;
          font-weight:900;
        }
        .theme-light .queue-head-transfer-btn {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .queue-page-count {
          background:rgba(255,255,255,.82);
          border-color:rgba(147,161,183,.2);
          color:#4c5b70;
        }
        .queue-row {
          display:grid;
          grid-template-columns:18px 46px minmax(0,1fr) auto;
          grid-template-areas:
            "idx thumb meta actions"
            "inline inline inline inline";
          align-items:center;
          cursor:pointer;
          min-height:58px;
          padding:7px 11px;
          row-gap:0;
          column-gap:9px;
          border-radius:18px;
        }
        .card.layout-tablet .queue-row {
          min-height:74px;
          padding:10px 14px;
          border-radius:22px;
          grid-template-columns:48px minmax(0,1fr) 56px;
          grid-template-areas:
            "actions meta thumb"
            "inline inline inline";
          column-gap:14px;
        }
        .queue-row.expanded {
          min-height:132px;
          row-gap:8px;
          padding-bottom:10px;
          overflow:visible;
          background:color-mix(in srgb, var(--ma-soft) 88%, transparent);
          box-shadow:inset 0 0 0 1px color-mix(in srgb, var(--ma-accent) 22%, transparent);
        }
        .card.layout-tablet .queue-row.expanded {
          min-height:136px;
        }
        .queue-row .menu-thumb { grid-area:thumb; }
        .queue-thumb-stack {
          grid-area:thumb;
          display:grid;
          justify-items:center;
          align-items:start;
          gap:5px;
          min-width:0;
          align-self:center;
        }
        .queue-thumb-stack .menu-thumb {
          grid-area:auto;
        }
        .queue-row .menu-thumb {
          width:42px;
          height:42px;
          border-radius:14px;
        }
        .card.layout-tablet .queue-row .menu-thumb {
          width:56px;
          height:56px;
          border-radius:18px;
          justify-self:end;
        }
        .queue-thumb-like {
          display:none;
          width:34px;
          height:30px;
          min-width:34px;
          min-height:30px;
          padding:0;
          border-radius:11px;
          border:1px solid rgba(255,255,255,.12);
          background:rgba(255,255,255,.065);
          color:rgba(255,255,255,.84);
          align-items:center;
          justify-content:center;
          cursor:pointer;
          box-shadow:none;
        }
        .queue-row.expanded .queue-thumb-like {
          display:inline-flex;
        }
        .queue-thumb-like.active {
          border-color:color-mix(in srgb, #ff5d8f 58%, rgba(255,255,255,.18));
          background:
            radial-gradient(circle at 50% 36%, rgba(255,255,255,.24), transparent 42%),
            linear-gradient(135deg, rgba(255,93,143,.34), rgba(255,93,143,.16));
          color:#ff5d8f;
          box-shadow:
            0 0 0 1px rgba(255,93,143,.12),
            0 10px 24px rgba(255,93,143,.2);
        }
        .queue-thumb-like .ui-ic {
          width:16px;
          height:16px;
        }
        .queue-thumb-like.active .ui-ic path {
          fill:currentColor;
          stroke:currentColor;
        }
        .theme-light .queue-thumb-like {
          border-color:rgba(147,161,183,.18);
          background:rgba(255,255,255,.74);
          color:#4b586b;
        }
        .theme-light .queue-thumb-like.active {
          border-color:rgba(233,64,117,.34);
          background:linear-gradient(135deg, rgba(255,93,143,.22), rgba(255,255,255,.82));
          color:#e94075;
        }
        .card.layout-tablet .queue-thumb-like {
          width:38px;
          height:32px;
          min-width:38px;
          min-height:32px;
          border-radius:12px;
        }
        .queue-index {
          grid-area:idx;
          width:20px;
          padding-top:0;
          align-self:center;
          margin-top:0;
          text-align:center;
          font-size:12px;
          color:rgba(255,255,255,.58);
          flex-shrink:0;
        }
        .card.layout-tablet .queue-index {
          display:none;
        }
        .queue-meta { grid-area:meta; min-width:0; flex:1; padding-inline-end:4px; }
        .card.layout-tablet .queue-meta { padding-inline-end:0; }
        .queue-actions { grid-area:actions; display:flex; gap:8px; flex-wrap:nowrap; direction:ltr; margin-top:0; align-items:center; }
        .card.layout-tablet .queue-actions { justify-self:start; }
        .queue-actions .chip-btn { min-width:34px; min-height:34px; width:34px; border-radius:12px; font-size:14px; font-weight:800; padding:0; }
        .card.layout-tablet .queue-actions .chip-btn { min-width:42px; min-height:42px; width:42px; border-radius:14px; }
        .queue-actions .chip-btn.warn { color:#ffcf86; }
        .queue-inline-actions {
          grid-area:inline;
          display:flex;
          justify-content:flex-end;
          align-items:center;
          gap:6px;
          width:100%;
          max-height:0;
          opacity:0;
          overflow:hidden;
          transform:translateY(-4px);
          pointer-events:none;
          padding:0;
          border-radius:16px;
          border:1px solid transparent;
          background:transparent;
          transition:max-height 180ms ease, opacity 160ms ease, transform 180ms ease, padding 180ms ease, border-color 180ms ease, background-color 180ms ease;
          direction:ltr;
        }
        .queue-row.expanded .queue-inline-actions {
          max-height:58px;
          opacity:1;
          transform:translateY(0);
          pointer-events:auto;
          padding:5px;
          border-color:rgba(255,255,255,.11);
          background:rgba(255,255,255,.055);
        }
        .queue-inline-actions .chip-btn {
          min-width:0;
          width:auto;
          min-height:34px;
          height:34px;
          border-radius:12px;
          padding:0 10px;
          font-size:11.5px;
          font-weight:900;
          display:inline-flex;
          align-items:center;
          justify-content:center;
          gap:4px;
          border:1px solid rgba(255,255,255,.11);
          background:rgba(255,255,255,.06);
          box-shadow:none;
          color:rgba(255,255,255,.86);
          flex:0 0 38px;
        }
        .queue-inline-actions .chip-btn.primary {
          flex:1 1 96px;
          max-width:132px;
          border-color:color-mix(in srgb, var(--ma-accent) 44%, rgba(255,255,255,.14));
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 90%, white 10%), color-mix(in srgb, var(--ma-accent) 70%, black 30%));
          color:#111827;
          box-shadow:0 8px 18px color-mix(in srgb, var(--ma-accent) 20%, transparent);
        }
        .queue-inline-actions .chip-btn.secondary {
          flex:1 1 118px;
          max-width:150px;
          color:rgba(255,255,255,.9);
        }
        .queue-inline-actions .chip-btn.text-action {
          flex:1 1 104px;
          max-width:150px;
          padding:0 10px;
          letter-spacing:0;
        }
        .queue-inline-actions .chip-btn.queue-inline-like {
          flex:0 0 42px;
          width:42px;
          min-width:42px;
          padding:0;
          color:rgba(255,255,255,.9);
        }
        .queue-inline-actions .chip-btn.queue-inline-like.active {
          border-color:color-mix(in srgb, #ff5d8f 58%, rgba(255,255,255,.18));
          background:linear-gradient(135deg, rgba(255,93,143,.28), rgba(255,93,143,.12));
          color:#ff5d8f;
          box-shadow:0 0 0 1px rgba(255,93,143,.10), 0 8px 18px rgba(255,93,143,.16);
        }
        .queue-inline-actions .chip-btn.queue-inline-like.active .ui-ic path {
          fill:currentColor;
          stroke:currentColor;
        }
        .queue-inline-actions .chip-btn.move-action {
          flex:0 0 58px;
          width:58px;
        }
        .queue-inline-move {
          flex:0 0 74px;
        }
        .queue-inline-move input {
          height:34px;
          border-radius:12px;
        }
        .queue-inline-actions .chip-btn.icon-only {
          padding:0;
        }
        .queue-inline-actions .chip-btn.warn {
          flex:0 0 46px;
          width:46px;
          border-color:rgba(255,96,96,.26);
          background:rgba(255,74,74,.13);
          color:#ff8f8f;
          box-shadow:none;
        }
        .queue-inline-actions .ui-ic {
          width:15px;
          height:15px;
          display:block;
          margin:auto;
        }
        .queue-inline-actions .chip-btn span {
          min-width:0;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .queue-eq {
          display:inline-flex;
          align-items:flex-end;
          justify-content:center;
          gap:2px;
          width:16px;
          height:14px;
          margin-inline:auto;
          color:var(--ma-accent);
        }
        .queue-eq span {
          width:2px;
          border-radius:999px;
          background:currentColor;
          animation:eqPulse 1.15s ease-in-out infinite;
          transform-origin:center bottom;
        }
        .queue-eq span:nth-child(1) { height:7px; animation-delay:0s; }
        .queue-eq span:nth-child(2) { height:12px; animation-delay:.18s; }
        .queue-eq span:nth-child(3) { height:8px; animation-delay:.36s; }
        .player-menu-card {
          position:relative;
          display:grid;
          gap:14px;
          padding:16px;
          border-radius:28px;
          background:
            linear-gradient(135deg, rgba(255,255,255,.12), rgba(255,255,255,.04)),
            radial-gradient(circle at 16% 18%, color-mix(in srgb, var(--ma-accent) 16%, transparent), transparent 34%);
          border:1px solid rgba(255,255,255,.14);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
          box-shadow:0 18px 34px rgba(0,0,0,.14);
        }
        .card.layout-tablet .player-menu-card,
        .card.layout-tablet .group-player-card {
          gap:7px;
          padding:18px 14px 10px;
          border-radius:22px;
          min-height:0;
        }
        .theme-light .player-menu-card {
          background:
            linear-gradient(135deg, rgba(255,255,255,.94), rgba(245,248,252,.82)),
            radial-gradient(circle at 18% 18%, color-mix(in srgb, var(--ma-accent) 16%, transparent), transparent 34%);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 18px 34px rgba(111,126,150,.12);
        }
        .player-menu-card.active {
          background:
            linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.08)), rgba(255,255,255,.08)),
            radial-gradient(circle at 16% 18%, color-mix(in srgb, var(--ma-accent) 18%, transparent), transparent 34%);
          border-color:color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.14));
          box-shadow:0 18px 36px color-mix(in srgb, var(--ma-accent) 20%, rgba(0,0,0,.18));
        }
        .theme-light .player-menu-card.active {
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.78)), rgba(255,255,255,.82));
          border-color:color-mix(in srgb, var(--ma-accent) 30%, rgba(147,161,183,.2));
        }
        .player-menu-card .menu-list-item {
          min-height:0;
          padding:0;
          background:transparent;
          border:none;
          box-shadow:none;
        }
        .player-menu-card .menu-thumb {
          width:48px;
          height:48px;
          border-radius:16px;
        }
        .player-menu-card .menu-thumb .ui-ic {
          width:46%;
          height:46%;
          opacity:.72;
        }
        .player-volume-row {
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          align-items:center;
          gap:7px;
          min-height:34px;
          padding:2px 4px;
          border-radius:14px;
          background:rgba(255,255,255,.04);
        }
        .card.layout-tablet .player-volume-row {
          min-height:34px;
          padding:2px 6px;
          gap:8px;
          grid-template-columns:30px minmax(0,1fr) 36px;
        }
        .player-menu-card.active .player-volume-row {
          background:color-mix(in srgb, var(--ma-accent) 10%, transparent);
        }
        .player-mini-mute {
          width:30px;
          height:30px;
          border:none;
          border-radius:12px;
          display:grid;
          place-items:center;
          color:inherit;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          cursor:pointer;
        }
        .player-mini-mute.active {
          color:#fff7e8;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 34%, transparent), color-mix(in srgb, var(--ma-accent) 18%, transparent));
          border-color:color-mix(in srgb, var(--ma-accent) 40%, transparent);
        }
        .player-mini-mute .ui-ic { width:15px; height:15px; }
        .player-mini-volume {
          width:100%;
          appearance:none;
          height:5px;
          border-radius:999px;
          outline:none;
          background:linear-gradient(to right,var(--ma-accent) 0%,var(--ma-accent) var(--vol-pct,50%),rgba(255,255,255,.2) var(--vol-pct,50%),rgba(255,255,255,.2) 100%);
        }
        .player-mini-volume::-webkit-slider-thumb { appearance:none; width:11px; height:11px; border-radius:50%; background:var(--ma-accent); border:none; }
        .player-mini-volume::-moz-range-thumb { width:11px; height:11px; border-radius:50%; background:var(--ma-accent); border:none; }
        .player-mini-value {
          display:block;
          min-width:34px;
          text-align:end;
          font-size:11px;
          font-weight:800;
          color:rgba(255,255,255,.78);
        }
        .theme-light .player-mini-mute {
          color:#1f2633;
          background:rgba(255,255,255,.74);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .player-mini-mute.active {
          color:#8b5e12;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 18%, white 82%), color-mix(in srgb, var(--ma-accent) 10%, white 90%));
        }
        .theme-light .player-mini-value { color:#4b5c73; }
        .players-premium-grid {
          display:grid;
          grid-template-columns:minmax(0, 1fr);
          gap:14px;
        }
        .card.layout-tablet .players-premium-grid {
          grid-template-columns:repeat(2, minmax(0, 1fr));
          gap:16px;
          width:min(100%, 720px);
          margin:0 auto;
        }
        .card.layout-tablet.rtl .players-premium-grid {
          direction:rtl;
        }
        .card.layout-tablet .menu-body {
          padding:18px 20px 22px;
        }
        .card.layout-tablet .menu-body.sheet-actions,
        .card.layout-tablet .menu-body.sheet-schedules,
        .card.layout-tablet .menu-body.sheet-players,
        .card.layout-tablet .menu-body.sheet-queue,
        .card.layout-tablet .menu-body.sheet-transfer,
        .card.layout-tablet .menu-body.sheet-group,
        .card.layout-tablet .menu-body.sheet-announcements,
        .card.layout-tablet .menu-body.sheet-settings {
          justify-items:center;
        }
        .card.layout-tablet .action-grid {
          display:grid;
          grid-template-columns:repeat(2, minmax(0,1fr));
          gap:10px;
          align-content:start;
          width:min(100%, 660px);
          margin:0 auto;
        }
        .card.layout-tablet .action-grid .menu-item {
          min-height:114px;
          border-radius:22px;
        }
        .theme-light .card.layout-tablet .action-grid .menu-item {
          box-shadow:0 16px 30px rgba(110,127,153,.12);
        }
        .card.layout-tablet .action-grid .menu-item-main {
          gap:12px;
          align-items:center;
        }
        .card.layout-tablet .action-grid .menu-item-ico {
          width:50px;
          height:50px;
          border-radius:16px;
        }
        .card.layout-tablet .action-grid .menu-item-title {
          font-size:18px;
          font-weight:900;
          letter-spacing:-.02em;
        }
        .card.layout-tablet .player-menu-card {
          min-height:142px;
          padding:20px 16px 10px;
          border-radius:26px;
          background:
            linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.06)),
            radial-gradient(circle at top, color-mix(in srgb, var(--ma-accent) 10%, transparent), transparent 55%);
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 18px 40px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.08);
          gap:8px;
        }
        .theme-light .card.layout-tablet .player-menu-card,
        .theme-light .card.layout-tablet .group-player-card {
          background:rgba(255,255,255,.5);
          border-color:rgba(147,161,183,.18);
          box-shadow:0 16px 30px rgba(110,127,153,.12);
        }
        .card.layout-tablet .player-premium-head {
          grid-template-columns:58px minmax(0,1fr);
          gap:14px;
          padding:10px 50px 0 0;
          align-items:center;
        }
        .card.layout-tablet .player-menu-card .menu-thumb {
          width:58px;
          height:58px;
          border-radius:20px;
        }
        .card.layout-tablet .player-premium-name {
          font-size:17px;
          line-height:1.12;
          letter-spacing:-.02em;
        }
        .card.layout-tablet .player-premium-track {
          font-size:13px;
          line-height:1.35;
          color:rgba(255,255,255,.62);
        }
        .theme-light .card.layout-tablet .player-premium-track {
          color:rgba(31,38,51,.58);
        }
        .card.layout-tablet .player-premium-side {
          display:none;
        }
        .card.layout-tablet .player-mini-mute {
          width:26px;
          height:26px;
          border-radius:10px;
        }
        .card.layout-tablet .player-mini-mute .ui-ic { width:13px; height:13px; }
        .card.layout-tablet .player-mini-volume {
          height:5px;
        }
        .card.layout-tablet .player-mini-value {
          display:block;
        }
        .player-premium-head {
          width:100%;
          border:none;
          background:transparent;
          padding:10px 50px 0 0;
          display:grid;
          grid-template-columns:auto minmax(0,1fr) auto;
          gap:12px;
          align-items:center;
          color:inherit;
          text-align:inherit;
          cursor:pointer;
          position:relative;
        }
        .player-premium-art {
          position:relative;
          width:66px;
          height:66px;
          border-radius:20px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          box-shadow:0 12px 24px rgba(0,0,0,.14);
        }
        .player-premium-art img { width:100%; height:100%; object-fit:cover; display:block; }
        .player-premium-art .ui-ic { width:46%; height:46%; opacity:.68; }
        .player-premium-copy {
          min-width:0;
          display:grid;
          gap:5px;
        }
        .player-premium-kicker {
          font-size:11px;
          font-weight:900;
          letter-spacing:.04em;
          text-transform:uppercase;
          color:rgba(255,255,255,.54);
        }
        .card.layout-tablet .player-premium-art {
          width:60px;
          height:60px;
          border-radius:18px;
        }
        .player-premium-name {
          font-size:20px;
          font-weight:650;
          line-height:1.15;
          color:inherit;
        }
        .card.layout-tablet .player-premium-name {
          font-size:19px;
        }
        .player-premium-meta {
          display:flex;
          align-items:center;
          gap:8px;
          flex-wrap:wrap;
          color:var(--muted);
          font-size:12px;
          font-weight:800;
        }
        .player-premium-track {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
          margin-top:2px;
          font-size:13px;
          font-weight:650;
          line-height:1.28;
          color:rgba(255,255,255,.62);
        }
        .card.layout-tablet .player-premium-track {
          font-size:12px;
        }
        .player-premium-state {
          display:inline-flex;
          align-items:center;
          gap:6px;
          padding:5px 10px;
          border-radius:999px;
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.08));
          color:var(--ma-accent);
          border:1px solid color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.12));
          width:max-content;
          max-width:100%;
        }
        .player-premium-state.idle {
          color:#58d68d;
          background:rgba(54,183,113,.14);
          border-color:rgba(88,214,141,.28);
        }
        .player-premium-state.playing {
          color:#ff6f6f;
          background:rgba(221,62,62,.16);
          border-color:rgba(255,111,111,.3);
        }
        .player-premium-state .eq-icon { margin:0; width:14px; height:12px; }
        .player-premium-side {
          position:absolute;
          top:10px;
          right:12px;
          left:auto;
          bottom:auto;
          display:grid;
          place-items:center;
          pointer-events:none;
          z-index:2;
        }
        .player-front-pin {
          width:27px;
          height:27px;
          padding:0;
          appearance:none;
          -webkit-appearance:none;
          border-radius:0;
          display:grid;
          place-items:center;
          color:rgba(210,216,226,.52);
          background:transparent;
          border:0;
          box-shadow:none;
          cursor:pointer;
          opacity:.78;
          pointer-events:auto;
          transition:transform .15s ease, opacity .15s ease, color .15s ease, filter .15s ease;
        }
        .player-front-pin:hover,
        .player-front-pin:focus-visible {
          opacity:1;
          transform:translateY(-1px);
          color:rgba(245,248,255,.82);
        }
        .player-front-pin.active {
          color:rgb(var(--dynamic-accent-rgb, 224 161 27));
          background:transparent;
          filter:drop-shadow(0 0 8px rgba(var(--dynamic-accent-rgb, 224 161 27) / .34));
          opacity:1;
        }
        .player-front-pin .ui-ic {
          width:17px;
          height:17px;
        }
        .theme-light .player-front-pin {
          color:rgba(74,84,100,.58);
          background:transparent;
          border:0;
          box-shadow:none;
        }
        .theme-light .player-front-pin.active {
          color:rgb(var(--dynamic-accent-rgb, 224 161 27));
          background:transparent;
        }
        .card.layout-tablet .player-premium-side {
          display:grid;
        }
        .player-favorite-btn {
          width:36px;
          min-width:36px;
          min-height:36px;
          padding:0;
          border-radius:14px;
          display:grid;
          place-items:center;
        }
        .player-favorite-btn .ui-ic { width:18px; height:18px; }
        .player-premium-active {
          min-width:32px;
          height:28px;
          padding:0 10px;
          border-radius:999px;
          display:grid;
          place-items:center;
          font-size:12px;
          font-weight:950;
          color:var(--ma-accent);
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.12));
        }
        .player-menu-card.active .player-premium-name {
          color:color-mix(in srgb, var(--ma-accent) 42%, currentColor);
        }
        .theme-light .player-premium-art {
          background:rgba(255,255,255,.88);
          border-color:rgba(147,161,183,.2);
        }
        .theme-light .player-premium-state,
        .theme-light .player-premium-active {
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.86));
          border-color:color-mix(in srgb, var(--ma-accent) 18%, rgba(147,161,183,.18));
        }
        .theme-light .player-premium-state.idle {
          color:#138a54;
          background:rgba(46,178,112,.13);
          border-color:rgba(34,154,96,.24);
        }
        .theme-light .player-premium-state.playing {
          color:#c83e43;
          background:rgba(216,72,78,.13);
          border-color:rgba(196,59,65,.24);
        }
        .theme-light .player-premium-kicker {
          color:rgba(55,68,85,.52);
        }
        .liked-toolbar {
          display:flex;
          flex-wrap:wrap;
          align-items:center;
          justify-content:space-between;
          gap:10px;
          margin-bottom:12px;
        }
        .liked-toolbar-actions {
          display:flex;
          flex-wrap:wrap;
          align-items:center;
          gap:10px;
          min-width:0;
        }
        .liked-layout-toggle {
          margin-inline-start:auto;
        }
        .card.layout-tablet .liked-toolbar {
          width:min(100%, 940px);
          margin:0 auto 16px;
          align-items:center;
        }
        .card.layout-tablet .liked-layout-toggle {
          background:transparent!important;
          border:none!important;
          box-shadow:none!important;
          padding:0!important;
          gap:8px!important;
          backdrop-filter:none!important;
          -webkit-backdrop-filter:none!important;
        }
        .card.layout-tablet .liked-layout-toggle .media-layout-btn {
          width:44px;
          min-width:44px;
          height:44px;
          min-height:44px;
          border-radius:999px!important;
          background:rgba(255,255,255,.09);
          border:1px solid rgba(255,255,255,.12);
        }
        .liked-select-box {
          width:22px;
          min-width:22px;
          height:22px;
          display:grid;
          place-items:center;
          flex-shrink:0;
          cursor:pointer;
          user-select:none;
        }
        .liked-select-box input {
          position:absolute;
          opacity:0;
          pointer-events:none;
          width:1px;
          height:1px;
        }
        .liked-select-box span {
          width:18px;
          height:18px;
          border-radius:5px;
          border:2px solid rgba(255,255,255,.32);
          background:rgba(255,255,255,.06);
          display:grid;
          place-items:center;
          transition:transform .16s ease, border-color .16s ease, background .16s ease, box-shadow .16s ease;
        }
        .liked-select-box.checked span {
          background:color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.08));
          border-color:color-mix(in srgb, var(--ma-accent) 70%, rgba(255,255,255,.24));
          box-shadow:0 10px 20px color-mix(in srgb, var(--ma-accent) 24%, transparent);
          transform:scale(1.04);
        }
        .liked-select-box.checked span::before {
          content:"✓";
          color:var(--ma-accent);
          font-size:13px;
          font-weight:1000;
          line-height:1;
        }
        .media-entry.grid.liked-entry .liked-select-box {
          position:absolute;
          inset-block-start:18px;
          inset-inline-start:54px;
          z-index:4;
          width:34px;
          min-width:34px;
          height:34px;
        }
        .media-entry.grid.liked-entry .liked-select-box span {
          width:22px;
          height:22px;
          border-radius:8px;
          background:rgba(13,16,23,.44);
          backdrop-filter:blur(10px);
          -webkit-backdrop-filter:blur(10px);
        }
        .theme-light .liked-select-box span {
          border-color:rgba(55,68,85,.28);
          background:rgba(255,255,255,.88);
        }
        .theme-light .media-entry.grid.liked-entry .liked-select-box span {
          background:rgba(255,255,255,.82);
        }
        .liked-remove-btn { min-width:40px; min-height:38px; border-radius:14px; font-size:15px; font-weight:900; color:#ffcf86; flex-shrink:0; }
        .group-connected-row {
          display:grid;
          gap:6px;
          padding:12px 14px;
          border-radius:18px;
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.12));
          color:inherit;
        }
        .group-connected-row span {
          font-size:12px;
          font-weight:800;
          color:rgba(255,255,255,.66);
        }
        .group-connected-row strong {
          font-size:15px;
          line-height:1.35;
          font-weight:900;
        }
        .theme-light .group-connected-row {
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.76));
          border-color:color-mix(in srgb, var(--ma-accent) 20%, rgba(147,161,183,.2));
        }
        .theme-light .group-connected-row span { color:rgba(55,68,85,.64); }
        .group-volume-card {
          display:grid;
          gap:10px;
          padding:14px;
          border-radius:20px;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 14%, rgba(255,255,255,.08)), rgba(255,255,255,.06));
          border:1px solid color-mix(in srgb, var(--ma-accent) 24%, rgba(255,255,255,.12));
          box-shadow:0 16px 34px rgba(0,0,0,.12);
        }
        .theme-light .group-volume-card {
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.82)), rgba(255,255,255,.78));
          border-color:color-mix(in srgb, var(--ma-accent) 22%, rgba(147,161,183,.22));
        }
        .group-volume-title {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          font-size:15px;
          font-weight:950;
        }
        .group-volume-title span {
          min-width:34px;
          height:28px;
          border-radius:999px;
          display:grid;
          place-items:center;
          color:var(--ma-accent);
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.1));
          border:1px solid color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.12));
          font-size:13px;
          font-weight:950;
        }
        .group-player-card {
          position:relative;
          display:grid;
          gap:10px;
          padding:12px;
          border-radius:20px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
        }
        .theme-light .group-player-card {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.18);
        }
        .group-player-card .group-player-row {
          position:relative;
          padding:10px 50px 0 0;
          background:transparent;
          border:none;
          box-shadow:none;
        }
        .group-player-card .player-premium-head {
          align-items:flex-start;
        }
        .group-player-toggle {
          position:absolute;
          top:6px;
          right:7px;
          left:auto;
          bottom:auto;
          width:29px;
          height:29px;
          border-radius:0;
          display:inline-grid;
          place-items:center;
          border:0;
          background:transparent;
          color:rgba(210,216,226,.64);
          box-shadow:none;
          opacity:.9;
          z-index:2;
          pointer-events:none;
          transition:transform .15s ease, opacity .15s ease, color .15s ease, filter .15s ease;
        }
        .group-player-toggle.checked {
          color:rgb(var(--dynamic-accent-rgb, 224 161 27));
          filter:drop-shadow(0 0 8px rgba(var(--dynamic-accent-rgb, 224 161 27) / .34));
          opacity:1;
        }
        .group-player-toggle .ui-ic {
          width:18px;
          height:18px;
        }
        .theme-light .group-player-toggle {
          color:rgba(74,84,100,.62);
        }
        .theme-light .group-player-toggle.checked {
          color:rgb(var(--dynamic-accent-rgb, 224 161 27));
        }
        .group-player-check {
          position:absolute!important;
          width:1px!important;
          height:1px!important;
          opacity:0!important;
          pointer-events:none!important;
          margin:0!important;
        }
        .group-inline-volume {
          padding:4px;
          background:rgba(255,255,255,.06);
        }
        .theme-light .group-inline-volume { background:rgba(242,246,250,.72); }
        .menu-body.sheet-group .group-actions { width:min(100%, 460px); margin:22px auto 0; display:grid; grid-template-columns:repeat(2, minmax(0, 1fr)); gap:16px; }
        .card.layout-tablet .menu-body.sheet-group .group-actions {
          width:min(480px, 100%);
          gap:18px;
          margin-top:26px;
        }
        .menu-body.sheet-group .action-btn { min-height:58px; border-radius:22px; padding:0 22px; font-size:15px; font-weight:850; letter-spacing:0; background:linear-gradient(180deg, rgba(255,255,255,.13), rgba(255,255,255,.055)); border:1px solid rgba(255,255,255,.16); box-shadow:0 14px 26px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.12); }
        .action-btn.busy {
          position:relative;
          display:inline-flex!important;
          align-items:center;
          justify-content:center;
          gap:8px;
          cursor:progress!important;
          pointer-events:none;
          color:var(--ma-accent);
          border-color:rgba(var(--dynamic-accent-rgb, 224 161 27) / .34);
          background:linear-gradient(180deg, rgba(var(--dynamic-accent-rgb, 224 161 27) / .2), rgba(255,255,255,.07));
        }
        .action-btn.busy::before {
          content:"";
          width:14px;
          height:14px;
          border-radius:999px;
          border:2px solid currentColor;
          border-inline-end-color:transparent;
          animation:spin .72s linear infinite;
        }
        .action-btn.busy::after {
          content:"";
          position:absolute;
          inset:-4px;
          border-radius:inherit;
          border:1px solid rgba(var(--dynamic-accent-rgb, 224 161 27) / .28);
          animation:voiceAssistantListenPulse 1s ease-out infinite;
          pointer-events:none;
        }
        .action-btn.busy-connect::before,
        .action-btn.busy-disconnect::before {
          width:28px;
          height:12px;
          border:0;
          border-radius:0;
          background:
            radial-gradient(circle, currentColor 0 4px, transparent 4.4px) left center / 12px 12px no-repeat,
            radial-gradient(circle, currentColor 0 4px, transparent 4.4px) right center / 12px 12px no-repeat,
            linear-gradient(currentColor, currentColor) center / 14px 2px no-repeat;
        }
        .action-btn.busy-connect::before { animation:groupConnectLoad .9s ease-in-out infinite; }
        .action-btn.busy-disconnect::before { animation:groupDisconnectLoad .9s ease-in-out infinite; }
        .action-btn.busy-disconnect::after { border-style:dashed; animation:groupDisconnectAura 1s ease-out infinite; }
        .menu-body.sheet-group .action-btn.warn { color:#ffd88b; border-color:rgba(255,216,139,.22); background:linear-gradient(180deg, rgba(255,216,139,.12), rgba(255,255,255,.055)); }
        @keyframes groupConnectLoad {
          0%,100% { transform:scaleX(.78); opacity:.62; }
          50% { transform:scaleX(1.08); opacity:1; }
        }
        @keyframes groupDisconnectLoad {
          0%,100% { transform:scaleX(1.08); opacity:1; }
          50% { transform:scaleX(.68); opacity:.58; }
        }
        @keyframes groupDisconnectAura {
          0% { opacity:.62; transform:scale(1); }
          100% { opacity:0; transform:scale(1.08); }
        }
        .group-player-row .menu-item-title { font-size:18px; font-weight:900; }
        .group-player-row input[type="checkbox"] {
          position:absolute!important;
          width:1px!important;
          height:1px!important;
          opacity:0!important;
          pointer-events:none!important;
          margin:0!important;
        }
        .settings-shell {
          display:grid;
          gap:16px;
          align-content:start;
        }
        .rtl .settings-shell,
        .rtl .settings-card {
          direction:rtl;
          text-align:right;
        }
        .rtl .settings-shell,
        .rtl .settings-group {
          direction:rtl;
          text-align:right;
        }
        .settings-group {
          display:grid;
          gap:10px;
          padding:16px;
          border-radius:18px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(18px);
          -webkit-backdrop-filter:blur(18px);
        }
        .theme-light .settings-group {
          background:rgba(255,255,255,.72);
          border-color:rgba(147,161,183,.2);
        }
        .settings-label {
          font-size:13px;
          font-weight:900;
          letter-spacing:.06em;
          text-transform:uppercase;
          color:rgba(255,255,255,.64);
        }
        .theme-light .settings-label { color:rgba(55,68,85,.64); }
        .settings-pills,.settings-actions { display:flex; flex-wrap:wrap; gap:10px; }
        .schedule-tabs {
          display:grid;
          grid-template-columns:repeat(3, minmax(0, 1fr));
          gap:8px;
          padding:6px;
          border-radius:18px;
          background:rgba(255,255,255,.07);
          border:1px solid rgba(255,255,255,.1);
        }
        .schedule-tabs .settings-pill {
          min-height:44px;
          padding:0 10px;
          border-radius:14px;
        }
        .schedule-list {
          display:grid;
          gap:10px;
        }
        .schedule-row {
          display:grid;
          grid-template-columns:minmax(0,1fr) auto;
          align-items:center;
          gap:10px;
          padding:10px;
          border-radius:18px;
          background:rgba(255,255,255,.06);
          border:1px solid rgba(255,255,255,.1);
        }
        .schedule-row.disabled {
          opacity:.58;
        }
        .schedule-row.editing {
          border-color:color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.12));
          background:color-mix(in srgb, var(--ma-accent) 12%, rgba(255,255,255,.06));
        }
        .schedule-row-main {
          min-width:0;
          border:none;
          background:transparent;
          color:inherit;
          font:inherit;
          display:grid;
          grid-template-columns:auto minmax(0,1fr);
          align-items:center;
          gap:12px;
          text-align:inherit;
          cursor:pointer;
        }
        .schedule-row-time {
          min-width:58px;
          min-height:42px;
          padding:0 10px;
          border-radius:14px;
          display:grid;
          place-items:center;
          font-size:18px;
          font-weight:950;
          color:var(--ma-accent);
          background:rgba(0,0,0,.16);
          direction:ltr;
        }
        .schedule-row-copy {
          min-width:0;
          display:grid;
          gap:4px;
        }
        .schedule-row-title {
          font-size:14px;
          font-weight:950;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .schedule-row-sub {
          font-size:12px;
          font-weight:800;
          color:rgba(255,255,255,.62);
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .schedule-row-actions {
          display:flex;
          gap:6px;
          align-items:center;
        }
        .schedule-row-actions .settings-pill {
          min-height:38px;
          padding:0 10px;
          border-radius:12px;
        }
        .schedule-row-actions .ui-ic {
          width:16px;
          height:16px;
        }
        .theme-light .schedule-tabs,
        .theme-light .schedule-row {
          background:rgba(255,255,255,.74);
          border-color:rgba(147,161,183,.18);
        }
        .theme-light .schedule-row-time {
          background:rgba(238,243,248,.9);
        }
        .theme-light .schedule-row-sub {
          color:rgba(55,68,85,.62);
        }
        .rtl .settings-pills,
        .rtl .settings-actions,
        .rtl .settings-range {
          direction:rtl;
          justify-content:flex-start;
        }
        .settings-pill {
          min-height:42px;
          padding:0 16px;
          border:none;
          border-radius:14px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          color:inherit;
          font:inherit;
          font-weight:800;
          cursor:pointer;
        }
        .theme-light .settings-pill {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.2);
        }
        .settings-pill.active {
          color:#18120a;
          background:linear-gradient(135deg,#f7bf5c,#f5a623);
          box-shadow:0 10px 18px rgba(224,161,27,.18);
        }
        .settings-color-wrap,.settings-range { display:grid; gap:10px; }
        .night-window-grid {
          display:grid;
          grid-template-columns:repeat(2, minmax(0,1fr));
          gap:12px;
        }
        .scheduled-start-card {
          border-color:color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.12));
          background:
            radial-gradient(circle at 12% 12%, color-mix(in srgb, var(--ma-accent) 14%, transparent), transparent 34%),
            rgba(255,255,255,.08);
        }
        .scheduled-start-grid {
          display:grid;
          grid-template-columns:minmax(116px, .78fr) minmax(0, 1fr) minmax(0, 1.16fr);
          gap:12px;
          align-items:stretch;
        }
        .scheduled-start-grid.two-col {
          grid-template-columns:repeat(2, minmax(0,1fr));
        }
        .wake-schedule-layout {
          min-height:0;
          height:100%;
          display:grid;
          grid-template-columns:minmax(280px, .9fr) minmax(0, 1.25fr);
          gap:14px;
          align-items:start;
          overflow:auto;
          overscroll-behavior:contain;
          padding:0 2px 8px;
        }
        .wake-schedule-list-card,
        .wake-schedule-editor-card {
          min-height:0;
          align-self:start;
        }
        .wake-schedule-list-card {
          max-height:100%;
          overflow:auto;
        }
        .wake-schedule-editor-card {
          overflow:visible;
        }
        .scheduled-start-field {
          display:grid;
          gap:10px;
          padding:14px;
          border-radius:18px;
          background:rgba(255,255,255,.06);
          border:1px solid rgba(255,255,255,.12);
        }
        .theme-light .scheduled-start-card {
          background:
            radial-gradient(circle at 12% 12%, color-mix(in srgb, var(--ma-accent) 12%, transparent), transparent 34%),
            rgba(255,255,255,.72);
        }
        .theme-light .scheduled-start-field {
          background:rgba(255,255,255,.76);
          border-color:rgba(147,161,183,.2);
        }
        .sleep-timer-action-row {
          display:grid;
          grid-template-columns:repeat(3, minmax(0, 1fr));
          gap:10px;
          width:100%;
        }
        .sleep-timer-action-row.with-cancel {
          grid-template-columns:repeat(4, minmax(0, 1fr));
        }
        .sleep-timer-action-btn {
          min-height:52px;
          border:none;
          border-radius:18px;
          display:grid;
          place-items:center;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 24%, rgba(255,255,255,.08)), rgba(255,255,255,.07));
          border:1px solid color-mix(in srgb, var(--ma-accent) 22%, rgba(255,255,255,.1));
          color:#fff;
          font:inherit;
          font-size:16px;
          font-weight:950;
          cursor:pointer;
          box-shadow:0 14px 26px rgba(0,0,0,.12);
        }
        .sleep-timer-action-btn.danger {
          color:#ffe1d9;
          background:rgba(255,100,100,.16);
          border-color:rgba(255,122,122,.24);
        }
        .theme-light .sleep-timer-action-btn {
          color:#1f2633;
          background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.88)), rgba(246,249,252,.82));
          border-color:color-mix(in srgb, var(--ma-accent) 18%, rgba(147,161,183,.2));
          box-shadow:0 14px 26px rgba(110,127,153,.12);
        }
        .theme-light .sleep-timer-action-btn.danger {
          color:#8b2935;
          background:rgba(255,236,236,.92);
          border-color:rgba(255,122,122,.18);
        }
        .night-time-card {
          display:grid;
          gap:10px;
          padding:14px;
          border-radius:18px;
          background:rgba(255,255,255,.06);
          border:1px solid rgba(255,255,255,.12);
          box-shadow:inset 0 1px 0 rgba(255,255,255,.04);
        }
        .theme-light .night-time-card {
          background:rgba(255,255,255,.76);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 10px 24px rgba(110,127,153,.08);
        }
        .night-time-label {
          font-size:12px;
          font-weight:900;
          letter-spacing:.04em;
          color:rgba(255,255,255,.62);
        }
        .theme-light .night-time-label {
          color:rgba(55,68,85,.62);
        }
        .night-time-input {
          width:100%;
          min-width:0;
          max-width:100%;
          box-sizing:border-box;
          min-height:64px;
          padding:0 18px;
          border-radius:20px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(10,14,22,.32);
          color:#f4f7ff;
          font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif;
          font-size:26px;
          font-weight:950;
          letter-spacing:.01em;
          outline:none;
          direction:ltr;
          text-align:center;
          appearance:none;
          -webkit-appearance:none;
          box-shadow:0 12px 26px rgba(0,0,0,.12);
          color-scheme:dark;
        }
        .night-time-input:focus {
          border-color:color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.18));
          box-shadow:0 0 0 4px color-mix(in srgb, var(--ma-accent) 14%, transparent), 0 14px 28px rgba(0,0,0,.14);
        }
        .theme-light .night-time-input {
          background:rgba(255,255,255,.94);
          border-color:rgba(147,161,183,.22);
          color:#1f2633;
          box-shadow:0 12px 26px rgba(111,126,150,.1);
          color-scheme:light;
        }
        .night-time-input::-webkit-calendar-picker-indicator {
          opacity:.82;
          cursor:pointer;
        }
        .theme-light .night-time-input::-webkit-calendar-picker-indicator {
          opacity:.68;
        }
        .settings-select {
          width:100%;
          max-width:none;
        }
        .settings-select-card {
          display:grid;
          grid-template-columns:44px minmax(0,1fr);
          align-items:center;
          gap:10px;
          width:100%;
          padding:8px;
          border-radius:20px;
          border:1px solid rgba(255,255,255,.12);
          background:linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.045));
          box-shadow:inset 0 1px 0 rgba(255,255,255,.08), 0 14px 28px rgba(0,0,0,.12);
        }
        .settings-select-card-icon {
          width:44px;
          height:44px;
          border-radius:15px;
          display:grid;
          place-items:center;
          color:var(--ma-accent);
          background:color-mix(in srgb, var(--ma-accent) 16%, rgba(255,255,255,.08));
          border:1px solid color-mix(in srgb, var(--ma-accent) 20%, rgba(255,255,255,.1));
          flex-shrink:0;
        }
        .settings-select-card-icon .ui-ic {
          width:21px;
          height:21px;
        }
        .settings-select-card .settings-select {
          min-height:44px;
          border-radius:14px;
          background:rgba(7,9,13,.28);
        }
        .theme-light .settings-select-card {
          background:rgba(255,255,255,.68);
          border-color:rgba(147,161,183,.18);
          box-shadow:0 14px 28px rgba(110,127,153,.12);
        }
        .theme-light .settings-select-card .settings-select {
          background:rgba(255,255,255,.72);
        }
        .settings-hint {
          color:rgba(255,255,255,.58);
          font-size:12px;
          line-height:1.45;
          margin-top:2px;
        }
        .theme-light .settings-hint {
          color:rgba(55,68,85,.62);
        }
        .card.layout-mobile .controls .minor-btn,
        .card.layout-mobile .controls .side-btn,
        .card.layout-mobile .controls .play-btn,
        .card.layout-mobile .controls .volume-btn {
          border-radius:999px !important;
        }
        .card.layout-mobile .controls .volume-btn.is-muted,
        .card.layout-mobile .player-mini-mute.is-muted {
          background:rgba(160,42,48,.22) !important;
          border-color:rgba(160,42,48,.28) !important;
          color:#fff !important;
          box-shadow:0 12px 22px rgba(110,18,28,.16), inset 0 1px 0 rgba(255,255,255,.08);
        }
        .player-focus-meta,
        .player-focus-sub,
        .active-players-bubble {
          display:none !important;
        }
        .card.layout-tablet .queue-list {
          width:min(100%, 680px);
          gap:12px;
        }
        .card.layout-tablet .queue-row {
          min-height:68px;
          padding:10px 12px;
          border-radius:20px;
        }
        .card.layout-tablet .players-premium-grid {
          gap:18px;
        }
        .settings-version {
          margin-top:10px;
          text-align:center;
          font-size:12px;
          font-weight:800;
          letter-spacing:.08em;
          color:rgba(255,255,255,.46);
        }
        .theme-light .settings-version {
          color:rgba(33,41,57,.42);
        }
        .settings-text-input,
        .announcement-textarea {
          width:100%;
          min-width:0;
          border:1px solid rgba(255,255,255,.12);
          border-radius:16px;
          background:rgba(255,255,255,.08);
          color:inherit;
          font:inherit;
          font-weight:800;
          padding:12px 14px;
          outline:none;
        }
        .ambient-map-input {
          min-height:76px;
          line-height:1.45;
          resize:vertical;
        }
        .settings-text-input:focus,
        .announcement-textarea:focus {
          border-color:color-mix(in srgb, var(--ma-accent) 44%, transparent);
          box-shadow:0 0 0 4px color-mix(in srgb, var(--ma-accent) 12%, transparent);
        }
        .theme-light .settings-text-input,
        .theme-light .announcement-textarea {
          background:rgba(255,255,255,.74);
          border-color:rgba(147,161,183,.22);
          color:#1f2633;
        }
        @media (max-width: 760px) {
          .menu-body.sheet-schedules .settings-shell {
            height:auto;
            min-height:100%;
            grid-template-rows:auto auto;
          }
          .scheduled-start-grid {
            grid-template-columns:minmax(0,1fr);
          }
          .menu-body.sheet-schedules .scheduled-start-field,
          .menu-body.sheet-schedules .night-time-card {
            min-width:0;
            max-width:100%;
            overflow:hidden;
          }
          .menu-body.sheet-schedules .settings-actions {
            grid-template-columns:minmax(0,1fr);
          }
          .menu-body.sheet-schedules .settings-pill {
            min-width:0;
            max-width:100%;
            white-space:normal;
            overflow-wrap:anywhere;
          }
          .wake-schedule-layout {
            height:auto;
            grid-template-columns:minmax(0,1fr);
            overflow:visible;
          }
          .wake-schedule-list-card {
            max-height:none;
            overflow:visible;
          }
          .night-window-grid {
            grid-template-columns:minmax(0,1fr);
          }
          .night-time-input {
            min-height:58px;
            font-size:22px;
            padding-inline:12px;
          }
        }
        .announcements-shell {
          display:grid;
          gap:14px;
        }
        .announcement-target {
          min-height:68px;
          display:grid;
          grid-template-columns:54px minmax(0,1fr);
          align-items:center;
          gap:12px;
          padding:8px 12px;
          border-radius:22px;
          border:1px solid color-mix(in srgb, var(--ma-accent) 24%, transparent);
          background:color-mix(in srgb, var(--ma-accent) 12%, transparent);
          font-size:17px;
          font-weight:950;
        }
        .announcement-target-icon {
          width:46px;
          height:46px;
          border-radius:17px;
          display:grid;
          place-items:center;
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 14px 24px color-mix(in srgb, var(--ma-accent) 20%, transparent);
        }
        .announcement-target-icon .ui-ic {
          width:22px;
          height:22px;
        }
        .announcement-target-select {
          width:100%;
          min-width:0;
          min-height:48px;
          border:none;
          background:transparent;
          box-shadow:none;
          padding:0;
          font:inherit;
          color:inherit;
          white-space:nowrap;
          overflow:hidden;
          text-overflow:ellipsis;
        }
        .theme-dark .announcement-target {
          background:rgba(20,24,32,.74);
          border-color:rgba(255,255,255,.12);
        }
        .theme-dark .announcement-target-select {
          background:rgba(15,18,28,.82);
          color:#f4f6fb;
          border-radius:14px;
          padding:10px 12px;
          border:1px solid rgba(255,255,255,.12);
          color-scheme:dark;
        }
        .theme-dark #mobileAnnouncementTargetSelect option,
        .theme-dark #mobileAnnouncementTtsLanguageSelect option {
          background:#171d28;
          color:#f4f6fb;
        }
        .announcement-input-wrap {
          position:relative;
          display:grid;
        }
        .announcement-textarea {
          resize:vertical;
          min-height:124px;
          line-height:1.5;
          padding-inline-end:64px;
        }
        .announcement-voice-btn {
          position:absolute;
          inset-block-start:10px;
          inset-inline-end:10px;
          width:44px;
          height:44px;
          border:none;
          border-radius:16px;
          display:grid;
          place-items:center;
          color:#18120a;
          background:linear-gradient(135deg, var(--ma-accent), color-mix(in srgb, var(--ma-accent) 72%, white 28%));
          box-shadow:0 12px 22px color-mix(in srgb, var(--ma-accent) 18%, transparent);
          cursor:pointer;
        }
        .announcement-voice-btn .ui-ic { width:20px; height:20px; }
        .announcement-presets {
          display:flex;
          flex-wrap:wrap;
          gap:10px;
        }
        .surprise-popup {
          position:absolute;
          inset-inline:0;
          inset-block-end:calc(116px + env(safe-area-inset-bottom));
          display:none;
          justify-content:center;
          pointer-events:none;
          z-index:88;
        }
        .surprise-popup.open {
          display:flex;
          animation:toastIn .22s ease;
        }
        .surprise-popup-card {
          width:min(244px, calc(100% - 28px));
          display:grid;
          gap:10px;
          justify-items:center;
          padding:14px;
          border-radius:24px;
          border:1px solid rgba(255,255,255,.14);
          background:rgba(15,18,28,.88);
          backdrop-filter:blur(22px);
          -webkit-backdrop-filter:blur(22px);
          box-shadow:0 22px 48px rgba(0,0,0,.24);
        }
        .surprise-popup-player {
          font-size:13px;
          font-weight:900;
          color:rgba(255,255,255,.78);
          text-align:center;
        }
        .surprise-popup-art {
          width:118px;
          height:118px;
          border-radius:24px;
          overflow:hidden;
          display:grid;
          place-items:center;
          background:linear-gradient(145deg, rgba(255,255,255,.12), rgba(255,255,255,.04));
          border:1px solid rgba(255,255,255,.14);
          color:var(--ma-accent);
        }
        .surprise-popup-art img { width:100%; height:100%; object-fit:cover; display:block; }
        .surprise-popup-art .ui-ic { width:34px; height:34px; }
        .surprise-popup-title {
          font-size:18px;
          font-weight:950;
          line-height:1.15;
          text-align:center;
          color:#fff;
        }
        .theme-light .surprise-popup-card {
          background:rgba(255,255,255,.92);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 18px 34px rgba(110,127,153,.16);
        }
        .theme-light .surprise-popup-player { color:#5f6c80; }
        .theme-light .surprise-popup-title { color:#16202d; }
        .theme-light .surprise-popup-art {
          background:linear-gradient(145deg, rgba(255,255,255,.96), rgba(244,247,251,.88));
          border-color:rgba(147,161,183,.18);
        }
        .announcement-send-btn {
          min-height:62px;
          display:flex !important;
          flex-direction:row !important;
          align-items:center;
          justify-content:center;
          gap:12px;
          font-size:17px;
        }
        .announcement-send-btn .ui-ic { width:24px; height:24px; flex:none; }
        .settings-stat-row {
          display:flex;
          align-items:center;
          justify-content:space-between;
          gap:12px;
          font-size:14px;
          font-weight:800;
        }
        .settings-color-row {
          display:grid;
          grid-template-columns:54px minmax(0,1fr);
          gap:12px;
          align-items:center;
        }
        .rtl .settings-color-row {
          direction:rtl;
        }
        .settings-check-grid {
          display:grid;
          gap:10px;
          grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));
        }
        .settings-check-pill {
          min-height:44px;
          display:flex;
          align-items:center;
          gap:10px;
          padding:0 14px;
          border-radius:14px;
          background:rgba(255,255,255,.08);
          border:1px solid rgba(255,255,255,.12);
          font-weight:800;
        }
        .theme-light .settings-check-pill {
          background:rgba(255,255,255,.78);
          border-color:rgba(147,161,183,.2);
        }
        .settings-check-pill input {
          width:18px;
          height:18px;
          margin:0;
          accent-color:var(--ma-accent);
        }
        .settings-check-pill.is-locked {
          cursor:default;
          opacity:.94;
        }
        .settings-check-pill.is-locked input {
          opacity:.78;
        }
        .settings-check-pill.quick-action-pill.danger {
          border-color:rgba(255,105,115,.24);
          background:linear-gradient(145deg, rgba(255,85,95,.1), rgba(255,255,255,.07));
        }
        .quick-action-setting-icon {
          display:inline-grid;
          place-items:center;
          width:24px;
          height:24px;
          flex:0 0 24px;
        }
        .quick-action-setting-icon .ui-ic {
          width:19px;
          height:19px;
        }
        .quick-action-pill {
          justify-content:flex-start;
          min-width:0;
        }
        .quick-action-pill-label {
          min-width:0;
          overflow:hidden;
          text-overflow:ellipsis;
          white-space:nowrap;
        }
        .quick-action-order-controls {
          margin-inline-start:auto;
          display:flex;
          gap:4px;
        }
        .quick-action-order-btn {
          width:28px;
          height:28px;
          display:grid;
          place-items:center;
          border:0;
          border-radius:999px;
          background:rgba(255,255,255,.1);
          color:inherit;
          padding:0;
          cursor:pointer;
        }
        .quick-action-order-btn[disabled] {
          opacity:.35;
          cursor:default;
        }
        .quick-action-order-btn .ui-ic {
          width:16px;
          height:16px;
        }
        .settings-check-pill.quick-action-pill.danger .quick-action-setting-icon {
          color:#ffb6b6;
        }
        .theme-light .settings-check-pill.quick-action-pill.danger {
          background:#fff1f2;
          border-color:rgba(180,35,43,.18);
        }
        .theme-light .settings-check-pill.quick-action-pill.danger .quick-action-setting-icon {
          color:#b4232b;
        }
        .settings-fixed-badge {
          margin-inline-start:auto;
          padding:3px 8px;
          border-radius:999px;
          background:rgba(245,166,35,.16);
          color:var(--ma-accent);
          font-size:11px;
          font-weight:900;
          letter-spacing:0;
        }
        .rtl .settings-fixed-badge {
          margin-inline-start:0;
          margin-inline-end:auto;
        }
        .rtl .settings-check-pill {
          direction:rtl;
          justify-content:flex-start;
          text-align:right;
        }
        .rtl .settings-select {
          direction:rtl;
          text-align:right;
        }
        .settings-color-picker {
          width:54px;
          height:54px;
          padding:0;
          border:none;
          border-radius:50%;
          overflow:hidden;
          background:none;
          cursor:pointer;
        }
        .settings-range input {
          width:100%;
          accent-color:var(--ma-accent);
        }
        .settings-value {
          font-size:14px;
          font-weight:800;
        }
        .active-players-bubble {
          position:absolute;
          inset-inline-start:12px;
          inset-block-start:14px;
          z-index:4;
          display:none;
          align-items:center;
          justify-content:center;
          min-height:34px;
          width:36px;
          height:36px;
          padding:0;
          border:none;
          border-radius:999px;
          color:var(--ma-accent);
          background:rgba(14,18,28,.46);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(16px);
          -webkit-backdrop-filter:blur(16px);
          box-shadow:0 12px 28px rgba(0,0,0,.18);
          cursor:pointer;
        }
        .active-players-bubble.open { display:inline-flex; }
        .active-players-bubble .eq-icon,
        .active-players-bubble .ui-ic { display:none !important; }
        .active-players-bubble #activePlayersCount {
          font-size:14px;
          font-weight:900;
          line-height:1;
          color:inherit !important;
        }
        .theme-light .active-players-bubble {
          color:var(--ma-accent);
          background:rgba(255,255,255,.74);
          border-color:rgba(147,161,183,.2);
          box-shadow:0 12px 28px rgba(111,126,150,.16);
        }
        .theme-light .active-players-bubble #activePlayersCount {
          color:var(--ma-accent);
        }
        .toast-wrap { position:absolute; inset-inline:16px; bottom:max(16px, env(safe-area-inset-bottom)); z-index:40; display:grid; gap:8px; pointer-events:none; }
        .toast-wrap.studio-toast {
          top:max(16px, env(safe-area-inset-top));
          bottom:auto;
          justify-items:center;
          align-content:start;
          z-index:72;
        }
        .toast-wrap.studio-toast .toast {
          width:min(520px, calc(100% - 24px));
          justify-self:center;
        }
        .toast {
          min-height:48px;
          padding:12px 14px;
          border-radius:16px;
          color:#fff;
          background:rgba(17,19,28,.9);
          border:1px solid rgba(255,255,255,.12);
          backdrop-filter:blur(14px);
          -webkit-backdrop-filter:blur(14px);
          display:flex;
          align-items:center;
          gap:10px;
          box-shadow:0 16px 34px rgba(0,0,0,.22);
          animation:toastIn .18s ease-out;
        }
        .toast-icon {
          width:24px;
          height:24px;
          border-radius:999px;
          display:grid;
          place-items:center;
          font-weight:900;
          flex-shrink:0;
          background:rgba(255,255,255,.12);
        }
        .toast-text { min-width:0; line-height:1.35; font-weight:800; }
        .toast.success { border-color:rgba(102,211,138,.28); background:rgba(22,45,34,.9); }
        .toast.success .toast-icon { color:#dff8e7; background:rgba(102,211,138,.26); }
        .toast.error { border-color:rgba(255,112,112,.28); background:rgba(58,24,28,.9); }
        .toast.error .toast-icon { color:#ffe3e3; background:rgba(255,112,112,.24); }
        @keyframes toastIn {
          from { transform:translateY(8px) scale(.98); opacity:0; }
          to { transform:translateY(0) scale(1); opacity:1; }
        }
        .theme-light .toast {
          color:#1f2633;
          background:rgba(255,255,255,.88);
          border-color:rgba(141,155,177,.22);
        }
        .theme-light .toast.success { background:rgba(237,252,242,.94); border-color:rgba(75,181,111,.26); }
        .theme-light .toast.error { background:rgba(255,241,241,.94); border-color:rgba(218,82,82,.24); }
        .hidden-tools { display:none !important; }
        .homeii-local-audio { position:absolute; width:1px; height:1px; opacity:0; pointer-events:none; inset:auto auto 0 0; }
        .rtl .player-chip,.rtl .menu-item-main,.rtl .menu-list-item,.rtl .queue-row,.rtl .media-category-row,.rtl .media-search-shell { direction:rtl; }
        .rtl .controls,.rtl .accent-row,.rtl .time-row,.rtl .queue-actions { direction:ltr; }
        .rtl .media-search-shell input {
          text-align:right;
        }
        @media (max-width:600px) {
          .card {
            border-radius:22px;
            height:var(--mobile-height);
            min-height:min(var(--mobile-min-height), var(--mobile-height));
            max-height:var(--mobile-height);
          }
          .stage {
            gap:8px;
            grid-template-rows:minmax(0,1fr) auto auto;
            padding:max(12px, env(safe-area-inset-top)) max(12px, env(safe-area-inset-right)) max(10px, env(safe-area-inset-bottom)) max(12px, env(safe-area-inset-left));
          }
          .player-chip { padding:0 2px; }
          .player-focus {
            margin-top:0;
            max-width:min(calc(100% - 28px), 360px);
            min-height:32px;
            padding:6px 10px;
            border-radius:16px;
            gap:8px;
          }
          .player-focus-copy { gap:6px; }
          .player-focus-tags { min-height:0; gap:4px; flex-wrap:nowrap; }
          .player-focus-pill { min-height:18px; padding:0 7px; font-size:calc(9px * var(--v2-font-scale)); }
          .player-focus-name { font-size:calc(11px * var(--v2-font-scale)); max-width:min(46cqi, 180px); }
          .hero-mobile-top { margin-bottom:4px; }
          .hero-split-shell { gap:12px; }
          .hero-info { gap:10px; }
          .hero-copy { gap:4px; margin-top:0; }
          .hero-title { font-size:calc(18px * var(--v2-font-scale)); line-height:1.08; }
          .hero-sub { font-size:calc(12px * var(--v2-font-scale)); margin-top:0; }
          .hero-top { padding-bottom:0; }
          .center { margin-top:0; gap:8px; }
          .art-stage { width:100%; padding:0; gap:6px; }
          .art-stack-view,
          .art-stack-viewport,
          .art-stack-container { height:var(--flow-art-stack-height); min-height:var(--flow-art-stack-height); max-height:var(--flow-art-stack-height); }
          .art-stack-slide { width:74%; }
          .art-stack-slide.center { width:78%; }
          .art-stack-slide.prev,
          .art-stack-slide.next { width:56%; }
          .mobile-art-shell { width:min(var(--flow-mobile-art-size), calc(100% - 28px)); border-radius:0; padding:0; }
          .card.empty-media .mobile-art-shell { width:min(174px, 44cqi); height:min(174px, 44cqi); display:grid; place-items:center; border-radius:999px; overflow:visible; background:transparent; border:none; box-shadow:none; }
          .card.radio-media.empty-media .mobile-art-shell { width:min(292px, 72cqi); height:min(292px, 72cqi); border-radius:24px; }
          .card.empty-media .art-stack-view,
          .card.empty-media .art-stack-viewport,
          .card.empty-media .art-stack-container { width:100%; height:100%; min-height:100%; display:grid; place-items:center; overflow:visible; background:transparent; border:none; box-shadow:none; }
          .card.empty-media .surprise-me-card.magic-empty { width:min(168px, 42cqi); height:min(168px, 42cqi); border-radius:999px; overflow:hidden; }
          .card.empty-media .surprise-me-card.magic-empty.has-art { width:100%; height:100%; border-radius:22px; }
          .np-art.mobile-art { border-radius:22px; }
          .mobile-art-actions { width:auto; margin-top:0; padding:0; gap:8px; }
          .mobile-art-fab { width:38px; min-width:38px; height:38px; border-radius:999px; }
          .bottom { gap:8px; }
          .progress-line { gap:12px; margin-top:4px; }
          .controls { gap:10px; margin-top:4px; flex-wrap:nowrap; }
          .side-btn,.volume-btn { width:var(--flow-side-btn-size); height:var(--flow-side-btn-size); border-radius:18px; flex:0 0 auto; }
          .side-btn.minor-btn { width:var(--flow-minor-btn-size); height:var(--flow-minor-btn-size); border-radius:16px; flex:0 0 auto; }
          .main-btn { width:var(--flow-main-btn-size); height:var(--flow-main-btn-size); flex:0 0 auto; }
          .mobile-volume-inline { margin-top:4px; gap:10px; }
          .footer-nav { gap:8px; margin-top:6px; padding:8px; border-radius:18px; }
          .footer-btn { min-height:52px; border-radius:16px; font-size:calc(10px * var(--v2-font-scale)); gap:4px; }
          .footer-btn .ui-ic { width:20px; height:20px; }
          .empty-quick-shelf { gap:10px; padding:8px var(--empty-quick-edge-fade) 10px; }
          .empty-quick-card { min-width:172px; max-width:172px; min-height:68px; }
          .library-nav { gap:6px; padding:6px; border-radius:18px; }
          .library-nav-btn { min-height:48px; border-radius:14px; }
          .menu-sheet { border-radius:24px; }
          .menu-sheet.sheet-actions,
          .menu-sheet.sheet-schedules {
            width:100%;
            height:calc(100% - 4px);
            max-height:calc(100% - 4px);
            margin-top:0;
            border-radius:24px;
          }
          .menu-backdrop {
            padding:max(36px, env(safe-area-inset-top)) max(10px, env(safe-area-inset-right)) max(12px, env(safe-area-inset-bottom)) max(10px, env(safe-area-inset-left));
          }
          .menu-body.sheet-actions,
          .menu-body.sheet-schedules {
            padding:16px 12px 20px;
          }
          .menu-body.sheet-actions .action-grid {
            gap:12px;
          }
          .menu-body { padding:14px; }
          .media-search-zone {
            padding-bottom:8px;
          }
          .queue-list {
            gap:14px;
          }
          .media-items-list {
            gap:14px;
          }
          .media-items-list.layout-grid {
            --media-grid-thumb-size:var(--flow-media-grid-thumb);
            grid-template-columns:repeat(2, minmax(0, 1fr));
          }
          .media-entry.list {
            min-height:88px;
            padding:16px 14px;
          }
          .media-entry.grid {
            padding:11px 10px 13px;
            min-height:calc(var(--media-grid-thumb-size, 174px) + 58px);
            border-radius:22px;
          }
          .queue-row {
            grid-template-columns:18px 44px minmax(0,1fr) auto;
            min-height:58px;
            padding:8px 10px;
            row-gap:0;
            column-gap:7px;
          }
          .queue-index {
            width:18px;
          }
          .queue-row .menu-thumb {
            width:40px;
            height:40px;
            border-radius:14px;
          }
          .queue-actions .chip-btn { min-width:32px; min-height:32px; width:32px; border-radius:11px; }
          .group-actions { grid-template-columns:1fr; }
        }
        .card.height-short .stage {
          gap:6px;
          min-height:0;
        }
        .card.height-short .center,
        .card.height-short .bottom,
        .card.height-short .tablet-main {
          gap:6px !important;
          min-height:0;
        }
        .card.height-short .hero-split-shell,
        .card.height-short .hero-visual,
        .card.height-short .hero-info,
        .card.height-short .art-stage {
          min-height:0;
        }
        .card.height-short .hero-copy {
          gap:4px;
          min-width:0;
        }
        .card.height-short .hero-title,
        .card.height-short .hero-sub {
          display:-webkit-box;
          -webkit-box-orient:vertical;
          overflow:hidden;
        }
        .card.height-short .hero-title {
          -webkit-line-clamp:1;
          line-height:1.04;
        }
        .card.height-short .hero-sub {
          -webkit-line-clamp:1;
        }
        .card.height-short .progress-line {
          margin-top:0 !important;
        }
        .card.height-short .controls {
          gap:8px !important;
          margin-top:0 !important;
        }
        .card.height-short .mobile-volume-inline {
          margin-top:0 !important;
        }
        .card.height-short:not(.layout-tablet) .footer-nav {
          gap:6px !important;
          margin-top:2px !important;
          padding:6px !important;
          border-radius:16px !important;
        }
        .card.height-short:not(.layout-tablet) .footer-btn {
          min-height:42px !important;
          padding:6px !important;
          border-radius:14px !important;
        }
        .card.height-short:not(.layout-tablet) .footer-btn-label {
          display:none !important;
        }
        .card.height-short:not(.layout-tablet) .mobile-volume-inline {
          gap:8px !important;
        }
        .card.height-short.layout-tablet .footer-nav {
          gap:8px !important;
        }
        .card.height-short.layout-tablet .footer-btn {
          min-height:56px !important;
          border-radius:18px !important;
        }
        .card.height-short.layout-tablet .player-focus {
          min-height:58px !important;
          padding:8px !important;
        }
        .card.aspect-wide:not(.layout-tablet) {
          --flow-stage-pad-block:8px;
          --flow-stage-pad-inline:10px;
          --flow-hero-gap:14px;
          --flow-side-btn-size:42px;
          --flow-minor-btn-size:36px;
          --flow-main-btn-size:72px;
          --flow-mobile-art-size:clamp(156px, min(42cqi, var(--flow-mobile-art-budget)), 360px);
          --flow-art-stack-height:var(--flow-mobile-art-size);
          --flow-art-card-size:var(--flow-mobile-art-size);
        }
        .card.aspect-wide:not(.layout-tablet) > .mobile-brand-signature,
        .card.aspect-wide:not(.layout-tablet) .hero-mobile-top {
          display:none !important;
        }
        .card.aspect-wide:not(.layout-tablet) .stage {
          grid-template-rows:minmax(0,1fr) auto auto !important;
          gap:6px !important;
          min-height:0;
        }
        .card.aspect-wide:not(.layout-tablet) .center {
          justify-content:center;
          min-height:0;
          overflow:hidden;
        }
        .card.aspect-wide:not(.layout-tablet) .hero-split-shell {
          width:100%;
          max-width:min(980px, 100%);
          display:grid;
          grid-template-columns:minmax(180px, 1.16fr) minmax(0, .84fr);
          gap:14px !important;
          align-items:center;
        }
        .card.aspect-wide:not(.layout-tablet).rtl .hero-split-shell {
          direction:rtl;
        }
        .card.aspect-wide:not(.layout-tablet) .hero-info,
        .card.aspect-wide:not(.layout-tablet) .hero-copy {
          justify-items:start;
          text-align:start;
          max-width:100%;
          gap:6px;
        }
        .card.aspect-wide:not(.layout-tablet).rtl .hero-info,
        .card.aspect-wide:not(.layout-tablet).rtl .hero-copy {
          text-align:right;
        }
        .card.aspect-wide:not(.layout-tablet) .hero-title {
          font-size:calc(22px * var(--v2-font-scale));
          line-height:1.04;
          -webkit-line-clamp:2;
        }
        .card.aspect-wide:not(.layout-tablet) .hero-sub {
          font-size:calc(12px * var(--v2-font-scale));
          -webkit-line-clamp:1;
        }
        .card.aspect-wide:not(.layout-tablet) .art-stage,
        .card.aspect-wide:not(.layout-tablet) .hero-visual {
          min-height:0;
          width:100%;
        }
        .card.aspect-wide:not(.layout-tablet) .mobile-art-shell {
          width:var(--flow-mobile-art-size) !important;
          max-width:100% !important;
        }
        .card.aspect-wide:not(.layout-tablet) .art-stack-view,
        .card.aspect-wide:not(.layout-tablet) .art-stack-viewport,
        .card.aspect-wide:not(.layout-tablet) .art-stack-container {
          height:var(--flow-mobile-art-size) !important;
          min-height:var(--flow-mobile-art-size) !important;
          max-height:var(--flow-mobile-art-size) !important;
        }
        .card.aspect-wide:not(.layout-tablet) .mobile-art-actions {
          justify-content:flex-start !important;
          gap:6px !important;
          flex-wrap:nowrap !important;
          overflow:hidden;
        }
        .card.aspect-wide:not(.layout-tablet) .mobile-art-fab,
        .card.aspect-wide:not(.layout-tablet) .mobile-art-actions .history-toggle-fab,
        .card.aspect-wide:not(.layout-tablet) .mobile-timer-fab {
          width:34px !important;
          min-width:34px !important;
          height:34px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .bottom {
          gap:5px !important;
          align-content:end;
        }
        .card.aspect-wide:not(.layout-tablet) .progress-line {
          gap:8px !important;
          margin-top:0 !important;
        }
        .card.aspect-wide:not(.layout-tablet) .progress-time {
          min-width:34px;
          font-size:11px;
        }
        .card.aspect-wide:not(.layout-tablet) .controls {
          gap:8px !important;
          margin-top:0 !important;
        }
        .card.aspect-wide:not(.layout-tablet) .mobile-volume-inline {
          display:grid !important;
          width:min(100%, 540px);
          margin:1px auto 0 !important;
          gap:8px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-nav {
          display:flex !important;
          width:max-content !important;
          max-width:min(100%, 560px) !important;
          min-width:0 !important;
          margin:2px auto 0 !important;
          padding:6px !important;
          gap:6px !important;
          border-radius:16px !important;
          justify-content:flex-start !important;
          align-items:center !important;
          overflow-x:auto !important;
          overflow-y:hidden !important;
          -webkit-overflow-scrolling:touch;
          scrollbar-width:none;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-nav::-webkit-scrollbar {
          display:none;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-btn {
          flex:0 0 42px !important;
          width:42px !important;
          min-width:42px !important;
          min-height:40px !important;
          height:40px !important;
          padding:6px !important;
          border-radius:14px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-btn .ui-ic {
          width:20px !important;
          height:20px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-nav.count-1,
        .card.aspect-wide:not(.layout-tablet) .footer-nav.count-2,
        .card.aspect-wide:not(.layout-tablet) .footer-nav.count-3,
        .card.aspect-wide:not(.layout-tablet) .footer-nav.count-4 {
          justify-content:center !important;
        }
        .card.aspect-wide:not(.layout-tablet) .footer-btn-label {
          display:none !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-backdrop.open {
          align-items:stretch !important;
          justify-content:center !important;
          padding:max(10px, env(safe-area-inset-top)) max(10px, env(safe-area-inset-right)) max(10px, env(safe-area-inset-bottom)) max(10px, env(safe-area-inset-left)) !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-actions,
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-library,
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-search,
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-players,
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-settings,
        .card.aspect-wide:not(.layout-tablet) .menu-sheet.sheet-queue {
          width:min(980px, calc(100% - 20px)) !important;
          max-width:min(980px, calc(100% - 20px)) !important;
          height:calc(100% - 20px) !important;
          max-height:calc(100% - 20px) !important;
          margin:auto !important;
          border-radius:22px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body {
          overflow:auto !important;
          -webkit-overflow-scrolling:touch;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-grid {
          grid-template-columns:repeat(auto-fit, minmax(min(100%, 190px), 1fr)) !important;
          gap:12px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile {
          min-width:0 !important;
          min-height:118px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-main {
          grid-template-columns:58px minmax(0, 1fr) !important;
          gap:12px !important;
          padding:13px 14px !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-title,
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-sub {
          display:block !important;
          white-space:normal !important;
          overflow:visible !important;
          text-overflow:clip !important;
          overflow-wrap:anywhere !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-title {
          font-size:calc(16px * var(--v2-font-scale)) !important;
          line-height:1.16 !important;
          letter-spacing:0 !important;
        }
        .card.aspect-wide:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-sub {
          font-size:calc(12px * var(--v2-font-scale)) !important;
          line-height:1.28 !important;
        }
        .card.aspect-wide:not(.layout-tablet) .media-items-list.layout-grid {
          grid-template-columns:repeat(auto-fill, minmax(118px, 1fr)) !important;
        }
        .card.height-tight:not(.layout-tablet) > .mobile-brand-signature,
        .card.height-tight:not(.layout-tablet) .hero-mobile-top {
          display:none !important;
        }
        .card.height-tight:not(.layout-tablet) .stage {
          grid-template-rows:minmax(0,1fr) auto auto !important;
        }
        .card.height-tight:not(.layout-tablet) .center {
          justify-content:center;
          overflow:hidden;
        }
        .card.height-tight:not(.layout-tablet) .hero-split-shell {
          width:100%;
          max-width:min(820px, 100%);
          display:grid;
          grid-template-columns:minmax(136px, 1.08fr) minmax(0, .92fr);
          gap:12px !important;
          align-items:center;
        }
        .card.height-tight:not(.layout-tablet).rtl .hero-split-shell {
          direction:rtl;
        }
        .card.height-tight:not(.layout-tablet) .hero-info,
        .card.height-tight:not(.layout-tablet) .hero-copy {
          justify-items:start;
          text-align:start;
          max-width:100%;
        }
        .card.height-tight:not(.layout-tablet).rtl .hero-info,
        .card.height-tight:not(.layout-tablet).rtl .hero-copy {
          text-align:right;
        }
        .card.height-tight:not(.layout-tablet) .art-stack-view,
        .card.height-tight:not(.layout-tablet) .art-stack-viewport,
        .card.height-tight:not(.layout-tablet) .art-stack-container {
          height:min(var(--flow-art-stack-height), 48dvh) !important;
          min-height:min(var(--flow-art-stack-height), 48dvh) !important;
          max-height:min(var(--flow-art-stack-height), 48dvh) !important;
        }
        .card.height-tight:not(.layout-tablet) .mobile-art-shell {
          width:min(270px, 46cqi, clamp(124px, calc(var(--flow-available-height) - 150px), 270px)) !important;
        }
        .card.height-tight:not(.layout-tablet) .mobile-art-actions {
          justify-content:flex-start !important;
          gap:6px !important;
          flex-wrap:nowrap !important;
          overflow:hidden;
        }
        .card.height-tight:not(.layout-tablet) .mobile-art-fab {
          width:34px !important;
          min-width:34px !important;
          height:34px !important;
        }
        .card.height-tight:not(.layout-tablet) .bottom {
          align-content:end;
        }
        .card.height-tight:not(.layout-tablet) .progress-time {
          min-width:34px;
          font-size:11px;
        }
        .card.height-tight:not(.layout-tablet) .mobile-volume-inline {
          display:grid !important;
          width:min(100%, 520px);
          margin:1px auto 0 !important;
          gap:7px !important;
        }
        .card.height-tight:not(.layout-tablet) .footer-nav {
          display:flex !important;
          width:max-content !important;
          max-width:min(100%, 540px) !important;
          min-width:0 !important;
          margin:2px auto 0 !important;
          padding:5px !important;
          gap:5px !important;
          border-radius:15px !important;
          justify-content:flex-start !important;
          align-items:center !important;
          overflow-x:auto !important;
          overflow-y:hidden !important;
          -webkit-overflow-scrolling:touch;
          scrollbar-width:none;
        }
        .card.height-tight:not(.layout-tablet) .footer-nav::-webkit-scrollbar {
          display:none;
        }
        .card.height-tight:not(.layout-tablet) .footer-btn {
          flex:0 0 40px !important;
          width:40px !important;
          min-width:40px !important;
          min-height:38px !important;
          height:38px !important;
          padding:5px !important;
          border-radius:13px !important;
        }
        .card.height-tight:not(.layout-tablet) .footer-btn .ui-ic {
          width:19px !important;
          height:19px !important;
        }
        .card.height-tight:not(.layout-tablet) .footer-nav.count-1,
        .card.height-tight:not(.layout-tablet) .footer-nav.count-2,
        .card.height-tight:not(.layout-tablet) .footer-nav.count-3,
        .card.height-tight:not(.layout-tablet) .footer-nav.count-4 {
          justify-content:center !important;
        }
        .card.height-tight:not(.layout-tablet) .footer-btn-label {
          display:none !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-backdrop.open {
          align-items:stretch !important;
          justify-content:center !important;
          padding:max(8px, env(safe-area-inset-top)) max(8px, env(safe-area-inset-right)) max(8px, env(safe-area-inset-bottom)) max(8px, env(safe-area-inset-left)) !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-actions,
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-library,
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-search,
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-players,
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-settings,
        .card.height-tight:not(.layout-tablet) .menu-sheet.sheet-queue {
          width:min(960px, calc(100% - 16px)) !important;
          max-width:min(960px, calc(100% - 16px)) !important;
          height:calc(100% - 16px) !important;
          max-height:calc(100% - 16px) !important;
          margin:auto !important;
          border-radius:20px !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body {
          overflow:auto !important;
          -webkit-overflow-scrolling:touch;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-grid {
          grid-template-columns:repeat(auto-fit, minmax(min(100%, 168px), 1fr)) !important;
          gap:10px !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile {
          min-width:0 !important;
          min-height:108px !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-main {
          grid-template-columns:52px minmax(0, 1fr) !important;
          gap:10px !important;
          padding:12px !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-ico {
          width:52px !important;
          height:52px !important;
          border-radius:17px !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-title,
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-sub {
          display:block !important;
          white-space:normal !important;
          overflow:visible !important;
          text-overflow:clip !important;
          overflow-wrap:anywhere !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-title {
          font-size:calc(15px * var(--v2-font-scale)) !important;
          line-height:1.16 !important;
          letter-spacing:0 !important;
        }
        .card.height-tight:not(.layout-tablet) .menu-body.sheet-actions .action-tile .menu-item-sub {
          font-size:calc(11px * var(--v2-font-scale)) !important;
          line-height:1.24 !important;
        }
        .card.height-tight:not(.layout-tablet) .media-items-list.layout-grid {
          grid-template-columns:repeat(auto-fill, minmax(108px, 1fr)) !important;
        }
        .card.height-tight.layout-tablet .tablet-shell {
          gap:10px !important;
        }
        .card.height-tight.layout-tablet .footer-nav {
          gap:6px !important;
        }
        .card.height-tight.layout-tablet .footer-btn {
          min-height:42px !important;
          padding:6px !important;
          border-radius:16px !important;
        }
        .card.height-tight.layout-tablet .footer-btn-label {
          display:none !important;
        }
        .card.height-tight.layout-tablet .player-focus {
          min-height:44px !important;
          padding:6px !important;
          gap:6px !important;
        }
        .card.height-tight.layout-tablet .player-focus-art {
          width:32px !important;
          height:32px !important;
        }
        .card.height-tight .hero-up-next,
        .card.height-tight .night-quick-row {
          display:none !important;
        }
      .mobile-volume-inline .volume-btn .ui-ic{width:20px;height:20px;}
.card:not(.layout-tablet) .mobile-volume-inline{grid-template-columns:auto minmax(0,1fr) auto;gap:10px;align-items:center;}
.card:not(.layout-tablet) .mobile-volume-inline .volume-value{order:1;min-width:46px;text-align:center;}
.card:not(.layout-tablet) .mobile-volume-inline .tablet-volume-track{order:2;}
.card:not(.layout-tablet) .mobile-volume-inline .volume-btn{order:3;width:38px;height:38px;border-radius:999px;}
.card:not(.layout-tablet) .mobile-volume-inline .volume-btn.active{background:rgba(170,38,38,.28)!important;border-color:rgba(255,98,98,.36)!important;color:#fff!important;box-shadow:0 10px 24px rgba(120,22,22,.22)!important;}
.card:not(.layout-tablet) .queue-action-item{min-height:58px;}
.card.mobile-content-dense:not(.layout-tablet){
  --flow-stage-pad-block:9px;
  --flow-shell-gap:12px;
  --flow-hero-gap:10px;
  --flow-side-btn-size:46px;
  --flow-minor-btn-size:38px;
  --flow-main-btn-size:84px;
  --flow-mobile-art-size:clamp(190px, min(72cqi, var(--flow-mobile-art-budget)), 370px);
  --flow-art-stack-height:var(--flow-mobile-art-size);
  --flow-art-card-size:var(--flow-mobile-art-size);
}
.card.mobile-content-dense:not(.layout-tablet) > .mobile-brand-signature{
  inset-block-start:2px;
  min-height:46px;
  width:clamp(170px, 48cqi, 260px);
  opacity:.52;
  z-index:2;
  transform:translateX(-50%) scale(.86);
  transform-origin:50% 0;
}
.card.mobile-content-dense:not(.layout-tablet) .stage{
  gap:6px!important;
  padding-block-start:max(7px, env(safe-area-inset-top))!important;
  padding-block-end:max(8px, env(safe-area-inset-bottom))!important;
}
.card.mobile-content-dense:not(.layout-tablet) .center{
  justify-content:center!important;
  gap:5px!important;
  padding-top:18px;
  overflow:visible;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-mobile-top{
  min-height:34px;
  margin-bottom:0!important;
  overflow:visible;
  z-index:9;
}
.card.mobile-content-dense:not(.layout-tablet):not(.height-short):not(.height-tight) .center{
  padding-top:6px;
}
.card.mobile-content-dense:not(.layout-tablet):not(.height-short):not(.height-tight) .hero-mobile-top{
  margin-top:-6px!important;
  margin-bottom:14px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus{
  margin-top:0!important;
  max-width:min(calc(100% - 76px), 360px)!important;
  min-height:32px!important;
  padding:5px 10px!important;
  gap:6px!important;
  border-radius:16px!important;
  overflow:visible;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus-copy{
  gap:6px!important;
  min-width:0;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus-tags{
  gap:4px!important;
  min-width:0;
  overflow:visible;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus-name{
  font-size:calc(11px * var(--v2-font-scale))!important;
  line-height:1.08!important;
  max-width:min(40cqi, 162px)!important;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus-art-wrap,
.card.mobile-content-dense:not(.layout-tablet) .player-focus-art{
  width:30px!important;
  height:30px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .player-focus-pill{
  min-height:17px!important;
  padding:0 6px!important;
  font-size:calc(9px * var(--v2-font-scale))!important;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-split-shell,
.card.mobile-content-dense:not(.layout-tablet) .hero-info,
.card.mobile-content-dense:not(.layout-tablet) .hero-copy{
  gap:5px!important;
  min-height:0;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-split-shell{
  align-content:start;
  max-height:100%;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-copy{
  max-width:min(94%, 520px)!important;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-title{
  font-size:calc(18px * var(--v2-font-scale))!important;
  line-height:1.04!important;
  -webkit-line-clamp:1!important;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-sub{
  font-size:calc(11px * var(--v2-font-scale))!important;
  line-height:1.16!important;
  -webkit-line-clamp:1!important;
}
.card.mobile-content-dense:not(.layout-tablet) .hero-up-next{
  max-width:min(94%, 440px)!important;
}
.card.mobile-content-dense:not(.layout-tablet) .up-next-art{
  width:18px!important;
  min-width:18px!important;
  height:18px!important;
  border-radius:6px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .up-next-line{
  gap:4px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .up-next-prefix{
  font-size:10px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .up-next-title{
  font-size:11px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .mobile-action-row-wrap{
  width:100%;
  min-height:38px;
  display:flex;
  justify-content:center;
  align-items:center;
  overflow:visible;
  position:relative;
  z-index:3;
}
.card.mobile-content-dense:not(.layout-tablet) .mobile-art-actions{
  gap:7px!important;
  flex-wrap:nowrap!important;
  overflow:visible!important;
}
.card.mobile-content-dense:not(.layout-tablet) .mobile-art-fab,
.card.mobile-content-dense:not(.layout-tablet) .mobile-art-actions .history-toggle-fab,
.card.mobile-content-dense:not(.layout-tablet) .mobile-timer-fab{
  width:38px!important;
  min-width:38px!important;
  height:38px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .night-quick-row{
  margin-top:0!important;
  gap:5px!important;
  flex-wrap:nowrap!important;
  max-width:100%;
  overflow:visible;
}
.card.mobile-content-dense:not(.layout-tablet) .night-quick-btn.icon-only{
  width:28px!important;
  min-width:28px!important;
  height:28px!important;
  min-height:28px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .night-quick-btn.icon-only .ui-ic{
  width:15px!important;
  height:15px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .bottom{
  gap:6px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .progress-line,
.card.mobile-content-dense:not(.layout-tablet) .controls,
.card.mobile-content-dense:not(.layout-tablet) .mobile-volume-inline{
  margin-top:0!important;
}
.card.mobile-content-dense:not(.layout-tablet) .controls{
  gap:8px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .mobile-volume-inline{
  gap:8px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .footer-nav{
  margin-top:1px!important;
  padding:6px!important;
  gap:6px!important;
}
.card.mobile-content-dense:not(.layout-tablet) .footer-btn{
  min-height:46px!important;
  padding:6px!important;
}
.card.mobile-content-dense.height-tight:not(.layout-tablet){
  --flow-mobile-art-size:clamp(150px, min(58cqi, calc(var(--flow-available-height) * .3)), 260px);
}
.card.mobile-content-dense.height-tight:not(.layout-tablet) .player-focus{
  min-height:28px!important;
  padding:4px 8px!important;
}
.card.mobile-content-dense.height-tight:not(.layout-tablet) .mobile-art-fab,
.card.mobile-content-dense.height-tight:not(.layout-tablet) .mobile-art-actions .history-toggle-fab,
.card.mobile-content-dense.height-tight:not(.layout-tablet) .mobile-timer-fab{
  width:34px!important;
  min-width:34px!important;
  height:34px!important;
}
.mobile-art-actions{position:relative!important;left:auto!important;right:auto!important;transform:none!important;inset-inline:auto!important;inset-block-end:auto!important;display:flex!important;align-items:center!important;justify-content:center!important;gap:10px!important;flex-wrap:wrap!important;margin-top:0!important;padding:0!important;border-radius:0!important;background:transparent!important;border:none!important;box-shadow:none!important;backdrop-filter:none!important;}
.card.layout-tablet .mobile-art-actions{position:relative!important;left:auto!important;right:auto!important;transform:none!important;inset-inline:auto!important;inset-block-end:auto!important;margin:0!important;justify-content:flex-start!important;z-index:4!important;}
.theme-light .mobile-art-actions{background:transparent!important;border-color:transparent!important;box-shadow:none!important;}
        .mobile-art-fab{width:42px;min-width:42px;height:42px;border-radius:999px;}
        .mobile-art-fab.pressed,.mobile-art-fab:active{transform:translateY(1px) scale(.94)!important;box-shadow:0 8px 16px rgba(0,0,0,.14)!important;border-color:color-mix(in srgb, var(--ma-accent) 34%, rgba(255,255,255,.18))!important;color:var(--ma-accent)!important;}
        .mobile-art-fab.disabled,.mobile-art-fab:disabled{opacity:.34!important;pointer-events:none!important;box-shadow:none!important;filter:saturate(.5);}
        .mobile-art-fab.hidden,[hidden].mobile-art-fab{display:none!important;}
        .mobile-art-actions .history-toggle-fab{position:static!important;inset:auto!important;transform:none!important;width:42px!important;min-width:42px!important;height:42px!important;border-radius:999px!important;opacity:1!important;}
        .mobile-art-actions .history-toggle-fab:active{transform:scale(.94)!important;}
        .mobile-timer-fab{width:42px;min-width:42px;gap:5px;padding:0 10px!important;overflow:hidden;white-space:nowrap;}
        .mobile-timer-fab.active{width:auto;min-width:76px;color:var(--ma-accent);}
        .mobile-timer-fab .mobile-timer-label{font-size:calc(11px * var(--v2-font-scale));font-weight:900;line-height:1;direction:ltr;}
        .mobile-timer-fab .mobile-timer-label[hidden]{display:none!important;}
        .player-focus-nav-btn.pressed,.player-focus-nav-btn:active{transform:scale(.9);color:var(--ma-accent);}
        .mobile-art-actions.count-4{gap:10px!important;}
@media (max-width: 600px){.mobile-art-actions{gap:8px!important;padding:0!important;margin-top:0!important;}.mobile-art-actions.count-4{gap:8px!important;}.mobile-art-fab{width:38px;min-width:38px;height:38px;}.mobile-art-actions .history-toggle-fab{width:38px!important;min-width:38px!important;height:38px!important;}.mobile-timer-fab{width:38px;min-width:38px;padding:0 8px!important;}.mobile-timer-fab.active{min-width:66px;}.mobile-timer-fab .mobile-timer-label{font-size:calc(10px * var(--v2-font-scale));}}
.card.aspect-wide:not(.layout-tablet) .mobile-art-actions,
.card.height-tight:not(.layout-tablet) .mobile-art-actions{
  width:min(100%, 186px)!important;
  max-width:min(100%, 186px)!important;
  min-height:34px!important;
  max-height:82px!important;
  display:flex!important;
  flex-wrap:wrap!important;
  justify-content:center!important;
  align-items:center!important;
  align-content:center!important;
  gap:6px 8px!important;
  overflow:visible!important;
}
.card.aspect-wide:not(.layout-tablet) .mobile-art-actions .mobile-art-fab,
.card.aspect-wide:not(.layout-tablet) .mobile-art-actions .history-toggle-fab,
.card.aspect-wide:not(.layout-tablet) .mobile-art-actions .mobile-timer-fab,
.card.height-tight:not(.layout-tablet) .mobile-art-actions .mobile-art-fab,
.card.height-tight:not(.layout-tablet) .mobile-art-actions .history-toggle-fab,
.card.height-tight:not(.layout-tablet) .mobile-art-actions .mobile-timer-fab{
  width:34px!important;
  min-width:34px!important;
  height:34px!important;
  min-height:34px!important;
  flex:0 0 34px!important;
}
.card.aspect-wide:not(.layout-tablet) .mobile-art-actions .mobile-timer-fab.active,
.card.height-tight:not(.layout-tablet) .mobile-art-actions .mobile-timer-fab.active{
  width:auto!important;
  min-width:62px!important;
  flex:0 0 auto!important;
}
@media (max-width: 820px){
  .history-drawer{inset-block:auto 16px;width:min(320px, calc(100% - 76px));max-height:min(56vh, 420px);}
  .history-toggle-fab{top:54%!important;inset-block-start:54%;inset-block-end:auto;width:38px;height:38px;border-radius:999px;transform:translateY(-50%);opacity:.9;}
  .history-toggle-fab .ui-ic{width:16px;height:16px;}
  .history-toggle-fab:active{transform:translateY(-50%) scale(.97);}
  .card:not(.layout-tablet) .sleep-timer-corner{top:calc(74px + env(safe-area-inset-top, 0px))!important;inset-block-start:calc(74px + env(safe-area-inset-top, 0px))!important;inset-block-end:auto;right:16px!important;left:auto!important;inset-inline-start:auto!important;inset-inline-end:16px!important;transform:none;justify-items:end;z-index:13;}
  .card:not(.layout-tablet) .sleep-timer-chip{min-height:30px;padding:0 9px;gap:5px;font-size:calc(11px * var(--v2-font-scale));box-shadow:0 10px 20px rgba(0,0,0,.18);}
  .card:not(.layout-tablet) .sleep-timer-chip .ui-ic{width:13px;height:13px;}
  .card:not(.layout-tablet) .sleep-timer-menu{grid-auto-flow:row;position:absolute;inset-block-start:36px;right:0;left:auto;inset-inline-start:auto;inset-inline-end:0;}
  .history-toggle-fab.left-edge{inset-inline-start:2px!important;}
  .history-toggle-fab.right-edge{inset-inline-end:2px!important;}
  .history-drawer.left-edge{inset-inline-start:14px;transform:translateX(calc(-100% - 14px));}
  .history-drawer.right-edge{inset-inline-end:14px;transform:translateX(calc(100% + 14px));}
  .history-drawer.open{transform:translateX(0);}
}
.card.layout-tablet .menu-backdrop{justify-content:center!important;align-items:stretch!important;padding:var(--flow-sheet-pad-block) var(--flow-sheet-pad-inline)!important;}
.card.layout-tablet .menu-sheet{width:min(calc(100% - var(--flow-sheet-gutter)), 920px)!important;max-width:min(calc(100% - var(--flow-sheet-gutter)), 920px)!important;max-height:calc(100% - 26px)!important;height:calc(100% - 26px)!important;margin-inline:auto!important;}
.card.layout-tablet .menu-sheet.sheet-library,.card.layout-tablet .menu-sheet.sheet-search{width:min(calc(100% - var(--flow-sheet-gutter)), 1120px)!important;max-width:min(calc(100% - var(--flow-sheet-gutter)), 1120px)!important;}
.card.layout-tablet .menu-sheet.sheet-queue{width:min(calc(100% - var(--flow-sheet-queue-gutter)), 980px)!important;max-width:min(calc(100% - var(--flow-sheet-queue-gutter)), 980px)!important;}
.card.layout-tablet .menu-sheet.sheet-actions,.card.layout-tablet .menu-sheet.sheet-schedules,.card.layout-tablet .menu-sheet.sheet-players,.card.layout-tablet .menu-sheet.sheet-groupplayers,.card.layout-tablet .menu-sheet.sheet-settings{width:min(calc(100% - var(--flow-sheet-narrow-gutter)), 860px)!important;max-width:min(calc(100% - var(--flow-sheet-narrow-gutter)), 860px)!important;}
.card.layout-tablet .queue-list{max-width:920px;margin-inline:auto;}
.card.layout-tablet .queue-row{min-height:88px!important;}
.card.layout-tablet .active-player-chip .bars,.card.layout-tablet .active-player-card .bars{display:none!important;}
.card.layout-tablet.rtl,.card.layout-tablet.rtl button,.card.layout-tablet.rtl input,.card.layout-tablet.rtl textarea,.card.layout-tablet.rtl select{font-family:'Heebo','Rubik','Outfit','Segoe UI',system-ui,sans-serif!important;}
.card.layout-tablet.rtl .hero-title,.card.layout-tablet.rtl .menu-title,.card.layout-tablet.rtl .player-premium-name,.card.layout-tablet.rtl .player-premium-kicker,.card.layout-tablet.rtl .settings-label,.card.layout-tablet.rtl .menu-item-title{font-family:'Rubik','Heebo','Outfit','Segoe UI',system-ui,sans-serif!important;}
.card.layout-tablet.rtl .players-premium-grid{direction:ltr!important;}
.card.layout-tablet.rtl .player-menu-card{direction:ltr!important;text-align:start!important;}
.card.layout-tablet.rtl .player-premium-head{grid-template-columns:64px minmax(0,1fr)!important;}
.card.layout-tablet .player-menu-card{min-height:142px!important;padding:20px 16px 10px!important;gap:8px!important;}
.card.layout-tablet .player-premium-head{padding:10px 50px 0 0!important;}
.card.layout-tablet .player-premium-side{top:10px!important;right:12px!important;left:auto!important;}
.card.layout-tablet .player-front-pin{width:27px!important;height:27px!important;}
.card.layout-tablet .player-front-pin .ui-ic{width:17px!important;height:17px!important;}
.card.layout-tablet .player-volume-row{min-height:34px!important;padding:2px 6px!important;gap:8px!important;grid-template-columns:26px minmax(0,1fr) 36px!important;border-radius:14px!important;}
.card.layout-tablet .player-mini-mute{width:26px!important;height:26px!important;border-radius:10px!important;}
.card.layout-tablet .player-mini-mute .ui-ic{width:13px!important;height:13px!important;}
.card.layout-tablet .player-mini-volume{height:5px!important;}
.card.layout-tablet .player-mini-volume::-webkit-slider-thumb{width:11px!important;height:11px!important;}
.card.layout-tablet .player-mini-volume::-moz-range-thumb{width:11px!important;height:11px!important;}
.card.layout-tablet .player-mini-value{min-width:34px!important;font-size:11px!important;font-weight:800!important;}
.card.layout-tablet.rtl .player-premium-copy{direction:rtl;text-align:right;}
.card.layout-tablet.rtl .player-volume-row{grid-template-columns:26px minmax(0,1fr) 36px!important;}
.card.layout-tablet.rtl .player-mini-value{text-align:end!important;}
.theme-light .player-menu-card{color:#1f2633!important;}
.theme-light .player-premium-name{color:#16202d!important;}
.theme-light .player-premium-kicker{color:#6c7889!important;}
.theme-light .player-premium-track{color:#556276!important;}
.theme-light .player-premium-meta{color:#5c687b!important;}
.theme-light .player-mini-value{color:#435066!important;}
.control-room-backdrop{position:absolute;inset:0;display:flex;align-items:stretch;justify-content:center;padding:0;opacity:0;pointer-events:none;transition:opacity .26s ease;z-index:28;background:rgba(6,10,18,.12);backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);}
.control-room-backdrop.open{opacity:1;pointer-events:auto;}
.card:not(.control-room-open) > .control-room-backdrop{opacity:0!important;pointer-events:none!important;}
.control-room-shell{position:relative;width:100%;height:100%;min-height:100%;max-height:none;display:block;border-radius:inherit;overflow:hidden;border:none;background:transparent;box-shadow:none;}
.theme-light .control-room-shell{background:transparent;}
.control-room-head{position:absolute;inset-inline:0;inset-block-start:0;display:flex;align-items:center;justify-content:space-between;gap:12px;padding:18px 22px;z-index:48;pointer-events:none;}
.control-room-head-brand{width:var(--flow-control-room-brand-width);color:rgba(255,255,255,.68);opacity:.96;display:grid;place-items:center;pointer-events:none;}
.theme-light .control-room-head-brand{color:rgba(31,38,51,.42);}
.control-room-close{width:46px;height:46px;border-radius:18px;border:1px solid rgba(255,255,255,.14);background:rgba(10,13,20,.34);color:#fff;display:grid;place-items:center;backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);pointer-events:auto;z-index:49;}
.theme-light .control-room-close{border-color:rgba(26,39,61,.1);background:rgba(255,255,255,.62);color:#1b2740;}
.control-room-close .ui-ic{width:18px;height:18px;}
.control-room-body-host{position:absolute;inset:0;overflow:hidden;padding:0;display:block;}
.control-room-scene{position:relative;height:100%;min-height:0;width:100%;padding:var(--control-room-head-reserve, 74px) 18px 0;display:grid;overflow:hidden;box-sizing:border-box;}
.control-room-scene-bg,.control-room-scene-glow{position:absolute;inset:0;pointer-events:none;}
.control-room-scene-bg{background:
  radial-gradient(circle at 18% 18%, rgba(var(--dynamic-accent-rgb,245 166 35) / .24), transparent 28%),
  radial-gradient(circle at 84% 16%, rgba(var(--dynamic-glow-rgb,255 175 92) / .18), transparent 24%),
  linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.01));
  filter:blur(16px);
  transform:scale(1.08);
  animation:control-room-scene-drift 28s ease-in-out infinite alternate;}
.control-room-scene.has-art .control-room-scene-bg{background:
  radial-gradient(circle at 16% 18%, rgba(var(--dynamic-accent-rgb,245 166 35) / .32), transparent 26%),
  radial-gradient(circle at 82% 18%, rgba(var(--dynamic-glow-rgb,255 175 92) / .24), transparent 24%),
  linear-gradient(180deg, rgba(7,10,17,.08), rgba(7,10,17,.22) 34%, rgba(7,10,17,.68) 100%),
  var(--control-room-scene-art) center/cover no-repeat;
  filter:saturate(1.08) blur(18px);
  opacity:.94;
  transform:scale(1.14);
  animation:control-room-scene-drift 32s ease-in-out infinite alternate;}
.control-room-scene-glow{background:
  radial-gradient(circle at 22% 22%, rgba(var(--dynamic-accent-rgb,245 166 35) / .28), transparent 28%),
  radial-gradient(circle at 76% 62%, rgba(var(--dynamic-glow-rgb,255 175 92) / .18), transparent 30%),
  radial-gradient(circle at 52% 78%, rgba(var(--dynamic-accent-rgb,245 166 35) / .12), transparent 34%),
  linear-gradient(180deg, rgba(255,255,255,.02), rgba(255,255,255,0));
  mix-blend-mode:screen;opacity:.88;animation:control-room-glow-breathe 22s ease-in-out infinite;}
.theme-light .control-room-scene-glow{opacity:.58;}
.control-room-scene.panel-open .control-room-scene-bg,
.control-room-scene.panel-open .control-room-scene-glow{animation:none!important;transform:none!important;}
.control-room-scene.panel-open .control-room-scene-bg{filter:blur(12px) saturate(.92)!important;}
.control-room-scene.panel-open .control-room-tray{background:rgba(21,22,28,.92);backdrop-filter:none;-webkit-backdrop-filter:none;}
.theme-light .control-room-scene.panel-open .control-room-tray{background:rgba(250,252,255,.96);}
.control-room-layout{position:relative;z-index:1;height:100%;min-height:0;display:grid;grid-template-rows:minmax(0,1fr) auto;gap:8px;overflow:hidden;box-sizing:border-box;}
.control-room-grid-wrap{min-height:0;height:100%;max-height:100%;padding:4px 14px 0;overflow:hidden;display:grid;place-items:start center;align-content:start;overscroll-behavior:contain;box-sizing:border-box;}
.control-room-grid{width:100%;max-width:min(var(--control-room-grid-max-width, 100%), 100%);max-height:min(var(--control-room-grid-max-height, 100%), var(--control-room-grid-available-height, 100%));display:grid;grid-template-columns:repeat(var(--control-room-cols, 3), minmax(0,1fr));gap:var(--control-room-gap, 18px);align-content:start;justify-content:stretch;justify-items:stretch;margin-inline:auto;grid-auto-rows:max-content;overflow:visible;box-sizing:border-box;}
.control-room-tile{position:relative;width:100%;aspect-ratio:16 / 9;display:grid;grid-template-rows:minmax(0,1fr) auto;gap:calc(10px * var(--control-room-tile-scale, 1));padding:calc(16px * var(--control-room-tile-scale, 1)) calc(16px * var(--control-room-tile-scale, 1)) calc(14px * var(--control-room-tile-scale, 1));border-radius:clamp(20px, calc(30px * var(--control-room-tile-scale, 1)), 30px);overflow:hidden;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.05);box-shadow:0 20px 44px rgba(0,0,0,.2), inset 0 1px 0 rgba(255,255,255,.12);transition:border-color .14s ease,box-shadow .14s ease,opacity .14s ease;transform:none;backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px);contain:layout paint;}
.control-room-tile:hover{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);box-shadow:0 20px 44px rgba(0,0,0,.22),0 0 0 1px rgba(var(--dynamic-accent-rgb,245 166 35) / .1),inset 0 1px 0 rgba(255,255,255,.12);}
.control-room-tile.selected{transform:none;border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .54);box-shadow:0 30px 70px rgba(0,0,0,.38),0 0 0 1px rgba(var(--dynamic-accent-rgb,245 166 35) / .18),0 16px 34px rgba(var(--dynamic-accent-rgb,245 166 35) / .12);z-index:3;filter:saturate(1.04);}
.control-room-tile.is-playing{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .72);box-shadow:0 28px 64px rgba(0,0,0,.36),0 0 0 2px rgba(var(--dynamic-accent-rgb,245 166 35) / .28),0 0 34px rgba(var(--dynamic-accent-rgb,245 166 35) / .2),inset 0 1px 0 rgba(255,255,255,.14);}
.control-room-tile.is-playing::before{opacity:.9;background:linear-gradient(180deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), rgba(255,255,255,0) 28%, rgba(var(--dynamic-accent-rgb,245 166 35) / .08) 100%);}
.control-room-tile.is-playing .control-room-tile-copy{box-shadow:0 0 0 1px rgba(var(--dynamic-accent-rgb,245 166 35) / .16),0 12px 28px rgba(0,0,0,.22);}
.control-room-tile.primary{outline:1px solid rgba(255,255,255,.12);}
.control-room-grid:hover .control-room-tile:not(.selected){opacity:1;}
.theme-light .control-room-tile{border-color:rgba(27,41,66,.08);background:rgba(255,255,255,.54);box-shadow:0 16px 34px rgba(28,42,68,.12);}
.control-room-tile::before{content:"";position:absolute;inset:1px;border-radius:28px;background:linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,0) 24%, rgba(255,255,255,.03) 100%);pointer-events:none;opacity:.68;z-index:0;}
.theme-light .control-room-tile::before{opacity:.5;}
.control-room-tile::after{content:"";position:absolute;inset:-24% auto auto -14%;width:44%;height:84%;border-radius:50%;background:radial-gradient(circle, rgba(var(--dynamic-accent-rgb,245 166 35) / .22), transparent 70%);pointer-events:none;opacity:.34;filter:blur(10px);z-index:0;}
.theme-light .control-room-tile::after{opacity:.18;}
.control-room-tile-bg,.control-room-tile-shade{position:absolute;inset:0;pointer-events:none;}
.control-room-tile-bg{background:
  radial-gradient(circle at 80% 18%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 26%),
  linear-gradient(180deg, rgba(255,255,255,.05), rgba(255,255,255,.02));
  transform:scale(1.02);}
.control-room-tile[style*='--control-room-tile-art'] .control-room-tile-bg{background:
  var(--control-room-tile-art) center/contain no-repeat,
  var(--control-room-tile-art) center/cover no-repeat;
  filter:saturate(1.08) contrast(1.02);
  transform:scale(1.02);}
.control-room-tile-shade{background:
  linear-gradient(180deg, rgba(10,13,20,.04) 0%, rgba(10,13,20,.08) 18%, rgba(10,13,20,.54) 68%, rgba(10,13,20,.92) 100%),
  linear-gradient(90deg, rgba(10,13,20,.18) 0%, rgba(10,13,20,0) 38%, rgba(10,13,20,.08) 100%);}
.theme-light .control-room-tile-shade{background:
  linear-gradient(180deg, rgba(255,255,255,.04) 0%, rgba(255,255,255,.02) 20%, rgba(245,248,252,.54) 66%, rgba(245,248,252,.9) 100%),
  linear-gradient(90deg, rgba(255,255,255,.08) 0%, rgba(255,255,255,0) 42%, rgba(255,255,255,.04) 100%);}
.control-room-tile-main,.control-room-volume-row,.control-room-select-fab{position:relative;z-index:1;}
.control-room-tile-main{display:flex;align-items:flex-end;justify-content:flex-start;min-height:100%;width:100%;background:none;border:none;color:inherit;text-align:inherit;padding:0;cursor:pointer;touch-action:manipulation;}
.control-room-select-fab{position:absolute;inset-block-start:calc(14px * var(--control-room-tile-scale, 1));inset-inline-end:calc(14px * var(--control-room-tile-scale, 1));width:clamp(34px, calc(48px * var(--control-room-tile-scale, 1)), 48px);height:clamp(34px, calc(48px * var(--control-room-tile-scale, 1)), 48px);padding:0;border-radius:clamp(12px, calc(17px * var(--control-room-tile-scale, 1)), 17px);border:1px solid rgba(255,255,255,.14);background:rgba(9,12,18,.28);color:#fff;display:grid;place-items:center;backdrop-filter:blur(18px);-webkit-backdrop-filter:blur(18px);box-shadow:0 10px 22px rgba(0,0,0,.18);cursor:pointer;touch-action:manipulation;}
.control-room-select-fab.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .24);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .3);}
.theme-light .control-room-select-fab{background:rgba(255,255,255,.58);border-color:rgba(27,40,62,.08);color:#1f2a42;}
.control-room-select-fab .ui-ic{width:16px;height:16px;}
.control-room-tile-copy{display:grid;gap:calc(8px * var(--control-room-tile-scale, 1));min-width:0;padding:calc(10px * var(--control-room-tile-scale, 1)) calc(12px * var(--control-room-tile-scale, 1)) calc(8px * var(--control-room-tile-scale, 1));border-radius:clamp(16px, calc(22px * var(--control-room-tile-scale, 1)), 22px);background:linear-gradient(180deg, rgba(8,11,18,.1), rgba(8,11,18,.26));backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);}
.theme-light .control-room-tile-copy{background:linear-gradient(180deg, rgba(255,255,255,.16), rgba(255,255,255,.28));}
.control-room-tile-pills{display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
.control-room-float-pill,.control-room-primary-pill{display:inline-flex;align-items:center;min-height:clamp(20px, calc(26px * var(--control-room-tile-scale, 1)), 26px);padding:0 clamp(7px, calc(10px * var(--control-room-tile-scale, 1)), 10px);border-radius:999px;border:1px solid rgba(255,255,255,.14);background:rgba(9,12,18,.3);font-size:calc(clamp(8px, calc(10px * var(--control-room-tile-scale, 1)), 10px) * var(--v2-font-scale));font-weight:800;color:#fff;backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);}
.theme-light .control-room-float-pill,.theme-light .control-room-primary-pill{background:rgba(255,255,255,.56);border-color:rgba(27,40,62,.08);color:#223049;}
.control-room-primary-pill{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .22);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .24);}
.control-room-float-pill.live{background:rgba(38,183,108,.2);border-color:rgba(116,227,166,.22);}
.control-room-tile-track{font-size:calc(clamp(9px, calc(12px * var(--control-room-tile-scale, 1)), 12px) * var(--v2-font-scale));font-weight:700;color:rgba(255,255,255,.86);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-shadow:0 8px 18px rgba(0,0,0,.34);}
.control-room-tile-name{font-size:calc(clamp(13px, calc(18px * var(--control-room-tile-scale, 1)), 18px) * var(--v2-font-scale));font-weight:850;line-height:1.02;letter-spacing:0;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-shadow:0 12px 28px rgba(0,0,0,.4);}
.control-room-tile-state{font-size:calc(clamp(9px, calc(11px * var(--control-room-tile-scale, 1)), 11px) * var(--v2-font-scale));font-weight:700;color:rgba(255,255,255,.7);}
.theme-light .control-room-tile-track{color:#33445c;}
.theme-light .control-room-tile-name{color:#162238;}
.theme-light .control-room-tile-state{color:#72829a;}
.control-room-volume-row{display:grid;grid-template-columns:minmax(0,1fr) auto;align-items:center;gap:calc(10px * var(--control-room-tile-scale, 1));padding:calc(8px * var(--control-room-tile-scale, 1)) calc(10px * var(--control-room-tile-scale, 1)) calc(2px * var(--control-room-tile-scale, 1));border-radius:clamp(14px, calc(18px * var(--control-room-tile-scale, 1)), 18px);background:rgba(9,12,18,.22);backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);}
.theme-light .control-room-volume-row{background:rgba(255,255,255,.46);}
.control-room-volume{width:100%;}
.control-room-volume-value{min-width:34px;font-size:calc(11px * var(--v2-font-scale));font-weight:800;color:rgba(255,255,255,.78);text-align:end;}
.theme-light .control-room-volume-value{color:#4f6077;}
.control-room-tray{position:absolute;inset-inline-start:50%;inset-block-end:112px;display:grid;grid-template-rows:auto minmax(0,1fr);gap:12px;align-self:end;width:min(1180px, calc(100% - 44px));max-height:min(68vh, 620px);overflow:hidden;padding:14px 16px;border-radius:30px;border:1px solid rgba(255,255,255,.12);background:rgba(9,12,18,.4);backdrop-filter:blur(26px);-webkit-backdrop-filter:blur(26px);box-shadow:0 24px 60px rgba(0,0,0,.28);z-index:32;transform:translateX(-50%);pointer-events:auto;touch-action:auto;contain:layout paint;}
.control-room-tray.compact{width:min(760px, calc(100% - 44px));}
.control-room-tray.wide{width:min(1100px, 100%);}
.theme-light .control-room-tray{background:rgba(255,255,255,.62);border-color:rgba(27,41,66,.08);box-shadow:0 14px 30px rgba(28,42,68,.12);}
.control-room-tray-head{display:grid;gap:4px;padding:2px 2px 0;}
.control-room-tray-title{font-size:calc(15px * var(--v2-font-scale));font-weight:900;color:#fff;}
.control-room-tray-sub{font-size:calc(12px * var(--v2-font-scale));color:rgba(255,255,255,.62);}
.theme-light .control-room-tray-title{color:#18253a;}
.theme-light .control-room-tray-sub{color:#71829a;}
.control-room-transfer-bar{display:grid;grid-template-columns:minmax(0,1fr) auto minmax(0,1fr) auto;align-items:center;gap:12px;min-height:0;}
.control-room-transfer-bar select,.control-room-search{min-height:52px;border-radius:18px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.08);color:#fff;font:inherit;}
.theme-light .control-room-transfer-bar select,.theme-light .control-room-search{background:rgba(245,248,252,.94);border-color:rgba(27,40,62,.08);color:#18253a;}
.control-room-transfer-bar select{padding:0 14px;outline:none;min-width:0;pointer-events:auto;}
.control-room-transfer-board{display:grid;grid-template-columns:minmax(0,1fr) auto minmax(0,1fr) auto;align-items:stretch;gap:12px;min-height:0;}
.control-room-transfer-column{display:grid;grid-template-rows:auto minmax(0,1fr);gap:8px;min-width:0;min-height:0;}
.control-room-transfer-label{font-size:calc(11px * var(--v2-font-scale));font-weight:900;color:rgba(255,255,255,.6);padding-inline:4px;}
.theme-light .control-room-transfer-label{color:#6f8097;}
.control-room-transfer-list{display:grid;align-content:start;gap:8px;min-height:0;max-height:min(48vh, 430px);overflow:auto;overflow-y:auto;padding-inline-end:4px;overscroll-behavior:contain;-webkit-overflow-scrolling:touch;touch-action:pan-y;scrollbar-gutter:stable;}
.control-room-transfer-choice{display:grid;grid-template-columns:44px minmax(0,1fr) 24px;align-items:center;gap:10px;min-height:58px;padding:8px 10px;border-radius:18px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.06);color:inherit;text-align:inherit;}
.control-room-transfer-choice.active{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32);background:rgba(var(--dynamic-accent-rgb,245 166 35) / .16);}
.theme-light .control-room-transfer-choice{background:rgba(255,255,255,.82);border-color:rgba(28,42,68,.08);}
.control-room-transfer-art{width:44px;height:44px;border-radius:14px;overflow:hidden;background:rgba(255,255,255,.08);display:grid;place-items:center;color:#fff;}
.theme-light .control-room-transfer-art{background:rgba(234,240,247,.96);color:#30415a;}
.control-room-transfer-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-transfer-copy{display:grid;gap:2px;min-width:0;}
.control-room-transfer-title,.control-room-transfer-sub{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-transfer-title{font-size:calc(13px * var(--v2-font-scale));font-weight:850;color:#fff;}
.control-room-transfer-sub{font-size:calc(11px * var(--v2-font-scale));color:rgba(255,255,255,.58);}
.theme-light .control-room-transfer-title{color:#18253a;}
.theme-light .control-room-transfer-sub{color:#73849a;}
.control-room-transfer-check{width:24px;height:24px;display:grid;place-items:center;color:rgba(var(--dynamic-accent-rgb,245 166 35) / .98);}
.control-room-transfer-check .ui-ic{width:14px;height:14px;}
.control-room-transfer-action{align-self:end;}
.control-room-transfer-action:disabled{opacity:.42;cursor:not-allowed;}
.control-room-transfer-arrow{width:44px;height:44px;border-radius:16px;display:grid;place-items:center;color:rgba(255,255,255,.8);background:rgba(255,255,255,.06);}
.theme-light .control-room-transfer-arrow{color:#3d4f69;background:rgba(236,241,247,.94);}
.control-room-tray-btn{width:52px;height:52px;border-radius:18px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.08);color:#fff;display:grid;place-items:center;}
.control-room-tray-btn.primary{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .22);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);}
.control-room-search{display:grid;grid-template-columns:20px minmax(0,1fr) 40px;align-items:center;gap:10px;padding:0 12px 0 16px;}
.control-room-search input{width:100%;background:none;border:none;outline:none;color:inherit;font:inherit;}
.control-room-search .ui-ic{width:18px;height:18px;opacity:.72;}
.control-room-search-mic{width:36px;height:36px;padding:0;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.08);color:inherit;display:grid;place-items:center;}
.theme-light .control-room-search-mic{background:rgba(234,240,247,.9);border-color:rgba(27,40,62,.08);}
.control-room-search-mic .ui-ic{opacity:.9;}
.control-room-library-results{min-height:0;max-height:min(58vh, 540px);overflow:auto;overflow-y:auto;padding-inline-end:4px;overscroll-behavior:contain;-webkit-overflow-scrolling:touch;touch-action:pan-y;scrollbar-gutter:stable;}
.control-room-picker-list{display:grid;gap:10px;min-height:0;max-height:min(58vh, 540px);overflow:auto;overflow-y:auto;padding-inline-end:4px;overscroll-behavior:contain;-webkit-overflow-scrolling:touch;touch-action:pan-y;scrollbar-gutter:stable;}
.control-room-picker-row{display:grid;grid-template-columns:52px minmax(0,1fr) 28px;align-items:center;gap:12px;padding:10px 12px;border-radius:20px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.06);color:inherit;text-align:inherit;}
.control-room-picker-row.active{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);}
.theme-light .control-room-picker-row{background:rgba(255,255,255,.82);border-color:rgba(28,42,68,.08);}
.control-room-picker-art{width:52px;height:52px;border-radius:16px;overflow:hidden;background:rgba(255,255,255,.08);display:grid;place-items:center;color:#fff;}
.theme-light .control-room-picker-art{background:rgba(234,240,247,.96);color:#30415a;}
.control-room-picker-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-picker-art .ui-ic{width:18px;height:18px;}
.control-room-picker-copy{display:grid;gap:2px;min-width:0;}
.control-room-picker-title,.control-room-picker-sub{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-picker-title{font-size:calc(14px * var(--v2-font-scale));font-weight:800;color:#fff;}
.control-room-picker-sub{font-size:calc(12px * var(--v2-font-scale));color:rgba(255,255,255,.58);}
.theme-light .control-room-picker-title{color:#18253a;}
.theme-light .control-room-picker-sub{color:#73849a;}
.control-room-picker-check{width:28px;height:28px;border-radius:999px;background:rgba(255,255,255,.08);display:grid;place-items:center;color:#fff;}
.theme-light .control-room-picker-check{background:rgba(234,240,247,.96);color:#2e3f58;}
.control-room-picker-check .ui-ic{width:14px;height:14px;}
.control-room-picker-row,.control-room-media-card,.control-room-transfer-choice{pointer-events:auto;touch-action:pan-y;}
.control-room-transfer-bar select,.control-room-tray-btn,.control-room-search input,.control-room-search-mic{pointer-events:auto;touch-action:manipulation;}
.control-room-picker-list,.control-room-library-results{scrollbar-width:thin;}
.control-room-transfer-list,
.control-room-picker-list,
.control-room-library-results,
.control-room-saved-scenes,
.control-room-queue-preview,
.control-room-tray.transfer-panel .control-room-transfer-list,
.control-room-tray.transfer-panel .control-room-queue-preview{
  scrollbar-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .58) rgba(255,255,255,.08);
}
.control-room-transfer-list::-webkit-scrollbar,
.control-room-picker-list::-webkit-scrollbar,
.control-room-library-results::-webkit-scrollbar,
.control-room-saved-scenes::-webkit-scrollbar,
.control-room-queue-preview::-webkit-scrollbar,
.control-room-tray.transfer-panel .control-room-transfer-list::-webkit-scrollbar,
.control-room-tray.transfer-panel .control-room-queue-preview::-webkit-scrollbar{
  width:12px;
  height:12px;
}
.control-room-transfer-list::-webkit-scrollbar-track,
.control-room-picker-list::-webkit-scrollbar-track,
.control-room-library-results::-webkit-scrollbar-track,
.control-room-saved-scenes::-webkit-scrollbar-track,
.control-room-queue-preview::-webkit-scrollbar-track,
.control-room-tray.transfer-panel .control-room-transfer-list::-webkit-scrollbar-track,
.control-room-tray.transfer-panel .control-room-queue-preview::-webkit-scrollbar-track{
  border-radius:999px;
  background:rgba(255,255,255,.07);
}
.control-room-transfer-list::-webkit-scrollbar-thumb,
.control-room-picker-list::-webkit-scrollbar-thumb,
.control-room-library-results::-webkit-scrollbar-thumb,
.control-room-saved-scenes::-webkit-scrollbar-thumb,
.control-room-queue-preview::-webkit-scrollbar-thumb,
.control-room-tray.transfer-panel .control-room-transfer-list::-webkit-scrollbar-thumb,
.control-room-tray.transfer-panel .control-room-queue-preview::-webkit-scrollbar-thumb{
  min-height:54px;
  border:3px solid transparent;
  border-radius:999px;
  background-clip:content-box;
  background-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .58);
}
.card.layout-tablet .control-room-transfer-list,
.card.layout-tablet .control-room-picker-list,
.card.layout-tablet .control-room-library-results,
.card.layout-tablet .control-room-saved-scenes,
.card.layout-tablet .control-room-queue-preview{
  padding-inline-end:12px;
  scrollbar-width:auto;
  scrollbar-gutter:stable both-edges;
}
.card.layout-tablet .control-room-transfer-list::-webkit-scrollbar,
.card.layout-tablet .control-room-picker-list::-webkit-scrollbar,
.card.layout-tablet .control-room-library-results::-webkit-scrollbar,
.card.layout-tablet .control-room-saved-scenes::-webkit-scrollbar,
.card.layout-tablet .control-room-queue-preview::-webkit-scrollbar{
  width:16px;
  height:16px;
}
.card.layout-tablet .control-room-group-summary,
.card.layout-tablet .control-room-library-shortcuts{
  scrollbar-width:auto;
  scrollbar-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .58) rgba(255,255,255,.08);
  padding-bottom:10px;
}
.card.layout-tablet .control-room-group-summary::-webkit-scrollbar,
.card.layout-tablet .control-room-library-shortcuts::-webkit-scrollbar{
  display:block;
  height:14px;
}
.card.layout-tablet .control-room-group-summary::-webkit-scrollbar-track,
.card.layout-tablet .control-room-library-shortcuts::-webkit-scrollbar-track{
  border-radius:999px;
  background:rgba(255,255,255,.07);
}
.card.layout-tablet .control-room-group-summary::-webkit-scrollbar-thumb,
.card.layout-tablet .control-room-library-shortcuts::-webkit-scrollbar-thumb{
  border:3px solid transparent;
  border-radius:999px;
  background-clip:content-box;
  background-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .58);
}
.theme-light .control-room-transfer-list,
.theme-light .control-room-picker-list,
.theme-light .control-room-library-results,
.theme-light .control-room-saved-scenes,
.theme-light .control-room-queue-preview,
.theme-light .control-room-group-summary,
.theme-light .control-room-library-shortcuts{
  scrollbar-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .62) rgba(27,41,66,.08);
}
.theme-light .control-room-transfer-list::-webkit-scrollbar-track,
.theme-light .control-room-picker-list::-webkit-scrollbar-track,
.theme-light .control-room-library-results::-webkit-scrollbar-track,
.theme-light .control-room-saved-scenes::-webkit-scrollbar-track,
.theme-light .control-room-queue-preview::-webkit-scrollbar-track,
.theme-light .control-room-group-summary::-webkit-scrollbar-track,
.theme-light .control-room-library-shortcuts::-webkit-scrollbar-track{
  background:rgba(27,41,66,.08);
}
@media (max-width:760px){
  .control-room-tray{inset-block-end:86px;width:calc(100% - 16px);max-height:calc(100dvh - 146px);padding:12px;border-radius:24px;}
  .control-room-transfer-bar,.control-room-transfer-board{grid-template-columns:minmax(0,1fr);}
  .control-room-transfer-arrow{display:none;}
  .control-room-transfer-action{width:100%;}
  .control-room-transfer-list{max-height:min(30dvh, 220px);}
  .control-room-picker-list,.control-room-library-results{max-height:calc(100dvh - 260px);}
}
.control-room-media-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(138px,1fr));gap:12px;}
.control-room-media-card{display:grid;gap:10px;padding:10px;border-radius:20px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.06);color:inherit;text-align:inherit;}
.theme-light .control-room-media-card{background:rgba(255,255,255,.78);border-color:rgba(28,42,68,.08);}
.control-room-media-art{aspect-ratio:1/1;border-radius:16px;overflow:hidden;background:rgba(255,255,255,.08);display:grid;place-items:center;color:rgba(255,255,255,.78);}
.theme-light .control-room-media-art{background:rgba(235,240,246,.96);color:#506178;}
.control-room-media-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-media-copy{display:grid;gap:4px;min-width:0;}
.control-room-media-title,.control-room-media-sub{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-media-title{font-size:calc(13px * var(--v2-font-scale));font-weight:800;color:#fff;}
.control-room-media-sub{font-size:calc(11px * var(--v2-font-scale));color:rgba(255,255,255,.6);}
.theme-light .control-room-media-title{color:#17253a;}
.theme-light .control-room-media-sub{color:#74839a;}
.control-room-empty{min-height:72px;display:grid;place-items:center;padding:16px;text-align:center;font-size:calc(13px * var(--v2-font-scale));color:rgba(255,255,255,.62);}
.control-room-empty.subtle{min-height:92px;border-radius:18px;border:1px dashed rgba(255,255,255,.12);}
.theme-light .control-room-empty{color:#6f8097;}
.theme-light .control-room-empty.subtle{border-color:rgba(27,41,66,.1);}
.control-room-dock{position:relative;left:auto;bottom:auto;display:flex;align-items:center;justify-content:center;gap:12px;flex-wrap:wrap;max-width:calc(100% - 8px);max-height:var(--control-room-dock-reserve, 116px);width:auto;margin:0 auto;padding:10px 14px calc(12px + env(safe-area-inset-bottom, 0px));border-radius:26px 26px 0 0;border:1px solid rgba(255,255,255,.14);border-bottom:none;background:linear-gradient(180deg, rgba(17,22,31,.72), rgba(9,12,18,.56));backdrop-filter:blur(26px);-webkit-backdrop-filter:blur(26px);box-shadow:0 18px 48px rgba(0,0,0,.28);transform:none;align-self:end;z-index:2;overflow:visible;box-sizing:border-box;}
.theme-light .control-room-dock{background:rgba(255,255,255,.68);border-color:rgba(28,42,68,.08);box-shadow:0 14px 30px rgba(28,42,68,.12);}
.control-room-now-pill{display:flex;align-items:center;gap:12px;min-width:300px;max-width:390px;padding:10px 14px 10px 10px;border-radius:999px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.06);box-shadow:0 10px 24px rgba(0,0,0,.16);}
.theme-light .control-room-now-pill{background:rgba(245,248,252,.94);border-color:rgba(28,42,68,.08);}
.control-room-now-art{width:54px;height:54px;border-radius:18px;overflow:hidden;display:grid;place-items:center;flex:none;background:rgba(255,255,255,.08);color:#fff;}
.theme-light .control-room-now-art{background:rgba(229,236,244,.96);color:#2a3a52;}
.control-room-now-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-now-art .ui-ic{width:22px;height:22px;}
.control-room-now-copy{display:grid;gap:2px;min-width:0;}
.control-room-now-kicker{font-size:calc(11px * var(--v2-font-scale));font-weight:800;color:rgba(255,255,255,.56);}
.control-room-now-name{font-size:calc(15px * var(--v2-font-scale));font-weight:900;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-now-track{font-size:calc(12px * var(--v2-font-scale));color:rgba(255,255,255,.72);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.theme-light .control-room-now-kicker{color:#6e7f96;}
.theme-light .control-room-now-name{color:#18253a;}
.theme-light .control-room-now-track{color:#61738b;}
.control-room-dock-section{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:999px;background:rgba(255,255,255,.04);}
.theme-light .control-room-dock-section{background:rgba(255,255,255,.48);}
.control-room-dock-divider{width:1px;height:42px;background:rgba(255,255,255,.12);display:block;}
.theme-light .control-room-dock-divider{background:rgba(28,42,68,.08);}
.control-room-selection-pill{display:inline-flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;min-width:76px;height:58px;padding:6px 14px;border-radius:18px;background:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);border:1px solid rgba(var(--dynamic-accent-rgb,245 166 35) / .32);font-size:calc(13px * var(--v2-font-scale));font-weight:900;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:130px;box-shadow:0 12px 28px rgba(0,0,0,.22);}
.control-room-selection-count{font-size:calc(15px * var(--v2-font-scale));line-height:1;}
.control-room-selection-pill.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .38);}
.theme-light .control-room-selection-pill{color:#433006;}
.control-room-dock-btn{min-width:70px;width:auto;height:58px;padding:6px 10px;border-radius:18px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.08);color:#fff;display:inline-flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;box-shadow:0 10px 24px rgba(0,0,0,.12);cursor:pointer;touch-action:manipulation;}
.control-room-dock-btn.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .26);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32);}
.theme-light .control-room-dock-btn{background:rgba(245,248,252,.94);border-color:rgba(28,42,68,.08);color:#1b2740;}
.control-room-dock-btn .ui-ic,.control-room-tray-btn .ui-ic{width:20px;height:20px;}
.control-room-dock-btn.library-pill{width:auto;min-width:70px;padding:6px 10px;}
.control-room-dock-label{font-size:calc(10px * var(--v2-font-scale));font-weight:850;line-height:1;white-space:nowrap;max-width:100%;overflow:hidden;text-overflow:ellipsis;}
.control-room-dock-btn{position:relative;}
.control-room-badge-count{position:absolute;inset-block-start:4px;inset-inline-end:4px;min-width:18px;height:18px;padding:0 5px;border-radius:999px;background:rgba(var(--dynamic-accent-rgb,245 166 35) / .96);color:#fff;font-size:calc(10px * var(--v2-font-scale));font-weight:900;display:grid;place-items:center;line-height:1;}
.control-room-dock-section.player .control-room-dock-btn:first-child{min-width:78px;}
.control-room-dock-section.player .control-room-dock-btn:first-child .ui-ic{width:22px;height:22px;}
.card.layout-tablet .control-room-scene{padding:var(--control-room-head-reserve, 68px) 12px 0!important;}
.card.layout-tablet .control-room-layout{gap:6px!important;overflow:hidden!important;}
.card.layout-tablet .control-room-grid-wrap{padding:2px 8px 0!important;place-items:start center!important;align-content:start!important;}
.card.layout-tablet .control-room-grid{max-width:min(var(--control-room-grid-max-width, 100%), 100%)!important;max-height:min(var(--control-room-grid-max-height, 100%), 100%)!important;}
.card.layout-tablet .control-room-tile{border-radius:24px;padding:12px 12px 10px;gap:7px;}
.card.layout-tablet .control-room-tile::before{border-radius:22px;}
.card.layout-tablet .control-room-tile-copy{gap:5px;padding:8px 10px 7px;border-radius:18px;}
.card.layout-tablet .control-room-tile-name{font-size:calc(15px * var(--v2-font-scale));}
.card.layout-tablet .control-room-tile-track{font-size:calc(11px * var(--v2-font-scale));}
.card.layout-tablet .control-room-volume-row{gap:8px;padding:6px 8px 0;border-radius:15px;}
.card.layout-tablet .control-room-select-fab{inset-block-start:10px;inset-inline-end:10px;width:42px;height:42px;border-radius:14px;}
.card.layout-tablet .control-room-dock{max-width:calc(100% - 12px)!important;gap:8px!important;flex-wrap:nowrap!important;padding:8px 10px calc(10px + env(safe-area-inset-bottom, 0px))!important;border-radius:24px 24px 0 0!important;overflow:visible!important;}
.card.layout-tablet .control-room-now-pill{min-width:180px;max-width:230px;padding:7px 10px 7px 7px;gap:9px;}
.card.layout-tablet .control-room-now-art{width:42px;height:42px;border-radius:14px;}
.card.layout-tablet .control-room-now-name{font-size:calc(13px * var(--v2-font-scale));}
.card.layout-tablet .control-room-now-track{font-size:calc(11px * var(--v2-font-scale));}
.card.layout-tablet .control-room-dock-section{gap:7px;padding:5px 7px;}
.card.layout-tablet .control-room-dock-btn,.card.layout-tablet .control-room-selection-pill{width:auto!important;height:50px!important;min-width:58px!important;border-radius:15px;padding:5px 8px!important;}
.card.layout-tablet .control-room-selection-pill{max-width:86px;}
.card.layout-tablet .control-room-dock-btn.library-pill{width:auto!important;min-width:58px!important;}
.card.layout-tablet .control-room-dock-section.player .control-room-dock-btn:first-child{min-width:64px!important;}
.card.layout-tablet .control-room-dock-label{font-size:calc(9px * var(--v2-font-scale));}
.card.layout-tablet .control-room-dock-btn .ui-ic,.card.layout-tablet .control-room-tray-btn .ui-ic{width:17px;height:17px;}
@media (max-width: 980px){
  .card.layout-tablet .control-room-now-pill{display:none!important;}
  .card.layout-tablet .control-room-dock{gap:6px!important;padding-inline:8px!important;}
  .card.layout-tablet .control-room-dock-section{gap:6px;padding:5px 6px;}
  .card.layout-tablet .control-room-dock-btn,.card.layout-tablet .control-room-selection-pill{width:auto!important;height:46px!important;min-width:50px!important;border-radius:14px;}
}
@media (max-height: 620px){
  .control-room-scene{padding:var(--control-room-head-reserve, 54px) 10px 0!important;}
  .control-room-layout{gap:4px!important;}
  .control-room-grid-wrap{padding:0 6px!important;}
  .control-room-now-pill{display:none!important;}
  .control-room-dock{gap:6px!important;padding:6px 8px calc(8px + env(safe-area-inset-bottom, 0px))!important;border-radius:20px 20px 0 0!important;}
  .control-room-dock-section{gap:5px!important;padding:4px 5px!important;}
  .control-room-dock-btn,.control-room-selection-pill{width:auto!important;height:42px!important;min-width:48px!important;border-radius:13px!important;}
  .control-room-dock-label{font-size:9px!important;}
  .control-room-dock-section.player .control-room-dock-btn:first-child{min-width:52px!important;}
  .control-room-dock-btn .ui-ic,.control-room-tray-btn .ui-ic{width:16px!important;height:16px!important;}
}
.control-room-scene::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg, rgba(6,9,16,.12), rgba(6,9,16,.16) 44%, rgba(6,9,16,.28) 100%);pointer-events:none;}
@keyframes control-room-scene-drift{
  from{transform:scale(1.14) translate3d(-1.2%, -1.1%, 0);}
  to{transform:scale(1.18) translate3d(1.4%, 1%, 0);}
}
@keyframes control-room-glow-breathe{
  0%,100%{opacity:.8;transform:scale(1);}
  50%{opacity:.94;transform:scale(1.04);}
}
.card.layout-tablet.tablet-stable .control-room-scene-bg,
.card.layout-tablet.tablet-stable .control-room-scene-glow{animation:none!important;filter:blur(10px)!important;transform:none!important;}
.card.layout-tablet.tablet-stable .control-room-tile,
.card.layout-tablet.tablet-stable .control-room-dock,
.card.layout-tablet.tablet-stable .control-room-tray,
.card.layout-tablet.tablet-stable .history-drawer,
.card.layout-tablet.tablet-stable .history-chip,
.card.layout-tablet.tablet-stable .source-badge,
.card.layout-tablet.tablet-stable .menu-sheet,
.card.layout-tablet.tablet-stable .menu-backdrop,
.card.layout-tablet.tablet-stable .control-room-close,
.card.layout-tablet.tablet-stable .control-room-head{transition:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important;}
.card.layout-tablet.tablet-stable .control-room-dock,
.card.layout-tablet.tablet-stable .control-room-tray,
.card.layout-tablet.tablet-stable .history-drawer,
.card.layout-tablet.tablet-stable .menu-sheet{background:rgba(15,20,29,.88)!important;}
.theme-light .card.layout-tablet.tablet-stable .control-room-dock,
.theme-light .card.layout-tablet.tablet-stable .control-room-tray,
.theme-light .card.layout-tablet.tablet-stable .history-drawer,
.theme-light .card.layout-tablet.tablet-stable .menu-sheet{background:rgba(250,252,255,.96)!important;}
.card.layout-tablet.tablet-stable .sidebar,
.card.layout-tablet.tablet-stable .topbar,
.card.layout-tablet.tablet-stable .tablet-rail-card,
.card.layout-tablet.tablet-stable .queue-panel,
.card.layout-tablet.tablet-stable .modal,
.card.layout-tablet.tablet-stable .ctx-menu,
.card.layout-tablet.tablet-stable .lyrics-sheet,
.card.layout-tablet.tablet-stable .active-player-card,
.card.layout-tablet.tablet-stable .player-menu-card,
.card.layout-tablet.tablet-stable .footer-btn,
.card.layout-tablet.tablet-stable .action-btn,
.card.layout-tablet.tablet-stable .queue-row,
.card.layout-tablet.tablet-stable .active-player-chip,
.card.layout-tablet.tablet-stable .player-picker-chip,
.card.layout-tablet.tablet-stable .settings-check-pill,
.card.layout-tablet.tablet-stable .chip-btn,
.card.layout-tablet.tablet-stable .night-quick-btn,
.card.layout-tablet.tablet-stable .up-next-inline,
.card.layout-tablet.tablet-stable .history-toggle-fab,
.card.layout-tablet.tablet-stable .source-badge{
  transition:none!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  transform:translateZ(0);
  will-change:auto!important;
}
.card.layout-tablet.tablet-stable > .history-toggle-fab{
  transform:translateY(-50%) translateZ(0)!important;
}
.card.layout-tablet.tablet-stable > .history-toggle-fab:active{
  transform:translateY(-50%) scale(.97) translateZ(0)!important;
}
.card.layout-tablet.tablet-stable .bg,
.card.layout-tablet.tablet-stable .shade,
.card.layout-tablet.tablet-stable .glow,
.card.layout-tablet.tablet-stable .art-aura,
.card.layout-tablet.tablet-stable .hero-aura,
.card.layout-tablet.tablet-stable .compact-cover-echo{
  transition:none!important;
}
.card.layout-tablet.tablet-stable .control-room-grid-wrap,
.card.layout-tablet.tablet-stable .control-room-grid{
  contain:layout paint!important;
  transform:none!important;
  will-change:auto!important;
}
.card.layout-tablet.tablet-stable .control-room-tile:hover,
.card.layout-tablet.tablet-stable .control-room-tile:active,
.card.layout-tablet.tablet-stable .control-room-tile.tap-feedback,
.card.layout-tablet.tablet-stable .queue-row:hover,
.card.layout-tablet.tablet-stable .queue-row:active,
.card.layout-tablet.tablet-stable .queue-row.tap-feedback{
  transform:none!important;
  filter:none!important;
}
.card.layout-tablet.tablet-stable .control-room-tile::before,
.card.layout-tablet.tablet-stable .control-room-tile::after,
.card.layout-tablet.tablet-stable .control-room-tile-bg,
.card.layout-tablet.tablet-stable .control-room-tile-shade,
.card.layout-tablet.tablet-stable .queue-row::before,
.card.layout-tablet.tablet-stable .queue-row::after{
  animation:none!important;
  transition:none!important;
  transform:none!important;
  will-change:auto!important;
}
.card.layout-tablet.tablet-stable .control-room-tile-bg{
  filter:none!important;
}
.card.layout-tablet.tablet-stable .control-room-tile.is-playing{
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .78)!important;
  box-shadow:0 0 0 2px rgba(var(--dynamic-accent-rgb,245 166 35) / .32),0 16px 30px rgba(0,0,0,.24)!important;
}
.card.layout-tablet .control-room-head{
  padding:12px 16px!important;
  z-index:58!important;
}
.card.layout-tablet .control-room-close{
  z-index:59!important;
  pointer-events:auto!important;
}
.card.layout-tablet .control-room-scene{
  padding:calc(var(--control-room-head-reserve, 64px) + 2px) 12px 0!important;
  height:100%!important;
  max-height:100%!important;
}
.card.layout-tablet .control-room-grid-wrap{
  padding:0 10px 0!important;
  min-height:0!important;
  height:100%!important;
  max-height:var(--control-room-grid-available-height, 100%)!important;
}
.card.layout-tablet .control-room-grid{
  max-height:var(--control-room-grid-available-height, 100%)!important;
  overflow:hidden!important;
  align-content:start!important;
}
.card.layout-tablet .control-room-tray{
  inset-block-end:92px!important;
  max-height:min(calc(100% - var(--control-room-head-reserve, 64px) - 108px), 560px)!important;
}
@media (prefers-reduced-motion: reduce){
  .control-room-scene-bg,.control-room-scene-glow{animation:none!important;}
}
.footer-btn.control-room-entry{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .18);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .22);}
.theme-light .footer-btn.control-room-entry{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .12);}
.history-drawer-title-row{display:flex;align-items:center;justify-content:space-between;gap:10px;min-width:0;}
.history-drawer-title-main{display:grid;gap:4px;min-width:0;}
.history-drawer-brand{width:104px;max-width:52%;color:rgba(255,255,255,.62);opacity:.94;display:grid;place-items:start;}
.theme-light .history-drawer-brand{color:rgba(31,38,51,.42);}
.history-drawer-close{width:34px;height:34px;min-width:34px;border-radius:999px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.08);color:inherit;display:grid;place-items:center;padding:0;cursor:pointer;}
.history-drawer-close .ui-ic{width:16px;height:16px;}
.theme-light .history-drawer-close{background:rgba(238,243,248,.86);border-color:rgba(143,159,181,.18);color:#1f2633;}
.mobile-timer-fab,
.mobile-timer-fab.active{display:inline-flex!important;align-items:center!important;justify-content:center!important;flex-direction:row!important;width:auto!important;min-width:42px!important;height:42px!important;gap:6px!important;padding:0 11px!important;color:#fff!important;background:rgba(14,18,28,.34)!important;border-color:rgba(255,255,255,.16)!important;box-shadow:0 12px 24px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08)!important;}
.mobile-timer-fab[hidden],
.mobile-timer-fab.hidden{display:none!important;}
.theme-light .mobile-timer-fab,
.theme-light .mobile-timer-fab.active{color:#1f2633!important;background:rgba(255,255,255,.76)!important;border-color:rgba(141,155,177,.22)!important;box-shadow:0 12px 26px rgba(111,126,150,.16)!important;}
.mobile-timer-fab.active{min-width:78px!important;}
.mobile-timer-fab .ui-ic{flex:0 0 auto!important;}
.mobile-timer-fab .mobile-timer-label{display:inline-block!important;color:inherit!important;font-weight:950!important;direction:ltr!important;line-height:1!important;white-space:nowrap!important;}
.mobile-timer-fab .mobile-timer-label[hidden]{display:none!important;}
.card.layout-tablet .sleep-timer-corner,
.card.layout-tablet .sleep-timer-corner.left,
.card.layout-tablet .sleep-timer-corner.right{
  top:auto!important;
  bottom:22px!important;
  left:auto!important;
  right:76px!important;
  transform:none!important;
  justify-items:end!important;
  align-items:center!important;
  z-index:8!important;
}
.card.layout-tablet .sleep-timer-chip{
  min-height:34px!important;
  padding:0 12px!important;
  gap:7px!important;
}
.card.layout-tablet > .home-shortcut-fab.tablet{
  position:absolute!important;
  top:18px!important;
  bottom:auto!important;
  transform:none!important;
  z-index:72!important;
  pointer-events:auto!important;
}
.card.layout-tablet.rtl > .home-shortcut-fab.tablet{
  inset:18px auto auto 18px!important;
  right:auto!important;
  left:18px!important;
}
.card.layout-tablet:not(.rtl) > .home-shortcut-fab.tablet{
  inset:18px 18px auto auto!important;
  right:18px!important;
  left:auto!important;
}
.top-settings-fab{
  display:none!important;
  pointer-events:none!important;
}
.card.layout-tablet.rtl > .sleep-timer-corner,
.card.layout-tablet.rtl > .sleep-timer-corner.left,
.card.layout-tablet.rtl > .sleep-timer-corner.right{
  position:absolute!important;
  inset:auto 76px 22px auto!important;
  top:auto!important;
  right:76px!important;
  bottom:22px!important;
  left:auto!important;
  transform:none!important;
  justify-items:end!important;
  z-index:9!important;
}
.card.layout-tablet:not(.rtl) > .sleep-timer-corner,
.card.layout-tablet:not(.rtl) > .sleep-timer-corner.left,
.card.layout-tablet:not(.rtl) > .sleep-timer-corner.right{
  position:absolute!important;
  inset:auto auto 22px 76px!important;
  top:auto!important;
  right:auto!important;
  bottom:22px!important;
  left:76px!important;
  transform:none!important;
  justify-items:start!important;
  z-index:9!important;
}
.card.compact-expanded > .home-shortcut-fab.mobile,
.card.compact-mode.compact-expanded > .home-shortcut-fab.mobile{
  position:absolute!important;
  inset:18px auto auto 18px!important;
  top:18px!important;
  right:auto!important;
  bottom:auto!important;
  left:18px!important;
  transform:none!important;
  z-index:12!important;
}
.card.compact-expanded > .top-settings-fab,
.card.compact-mode.compact-expanded > .top-settings-fab{
  position:absolute!important;
  inset:18px 66px auto auto!important;
  top:18px!important;
  right:66px!important;
  bottom:auto!important;
  left:auto!important;
  transform:none!important;
  z-index:12!important;
}
.card.compact-expanded > .compact-collapse-fab,
.card.compact-mode.compact-expanded > .compact-collapse-fab{
  position:absolute!important;
  inset:18px 18px auto auto!important;
  top:18px!important;
  right:18px!important;
  bottom:auto!important;
  left:auto!important;
  transform:none!important;
  z-index:44!important;
  width:46px!important;
  height:46px!important;
  min-width:46px!important;
  min-height:46px!important;
  padding:0!important;
  display:grid!important;
  place-items:center!important;
  cursor:pointer!important;
  pointer-events:auto!important;
  touch-action:manipulation!important;
  border:1px solid rgba(255,255,255,.2)!important;
  border-radius:15px!important;
  background:linear-gradient(145deg, rgba(255,255,255,.18), rgba(255,255,255,.07)), rgba(12,16,24,.24)!important;
  box-shadow:0 14px 30px rgba(0,0,0,.2), inset 0 1px 0 rgba(255,255,255,.18)!important;
  backdrop-filter:blur(16px)!important;
  -webkit-backdrop-filter:blur(16px)!important;
}
.card.compact-expanded:not(.has-top-settings) > .compact-collapse-fab,
.card.compact-mode.compact-expanded:not(.has-top-settings) > .compact-collapse-fab{
  inset:18px 18px auto auto!important;
  right:18px!important;
}
.card.compact-expanded > .compact-collapse-fab::before,
.card.compact-mode.compact-expanded > .compact-collapse-fab::before{
  content:"";
  position:absolute;
  inset:-8px;
  border-radius:22px;
}
        .card.compact-expanded > .compact-collapse-fab .ui-ic,
        .card.compact-mode.compact-expanded > .compact-collapse-fab .ui-ic{
          width:22px!important;
          height:22px!important;
          pointer-events:none!important;
        }
        .card.compact-menu-open > .compact-collapse-fab,
        .card.compact-mode.compact-menu-open > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-expanded:has(.menu-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-expanded:has(.queue-action-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-expanded:has(.control-room-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-expanded:has(.screensaver-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded:has(.menu-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open) .card.compact-mode.compact-expanded:has(.queue-action-backdrop.open) > .compact-collapse-fab,
        :host(.compact-popup-open.compact-menu-open) .compact-collapse-fab,
        :host(.compact-window-popup-open.compact-menu-open) .compact-collapse-fab{
          opacity:0!important;
          visibility:hidden!important;
          pointer-events:none!important;
        }
        .card.control-room-open > .compact-collapse-fab,
        .card.control-room-open > .top-settings-fab,
        .card.control-room-open > .home-shortcut-fab{
  opacity:0!important;
  visibility:hidden!important;
  display:none!important;
  pointer-events:none!important;
}
.card.control-room-open > .home-shortcut-fab{
  opacity:0!important;
  visibility:hidden!important;
  display:none!important;
  pointer-events:none!important;
  z-index:0!important;
}
.card.layout-tablet.control-room-open > .home-shortcut-fab.tablet,
.card.compact-expanded.control-room-open > .home-shortcut-fab.mobile,
.card.compact-mode.compact-expanded.control-room-open > .home-shortcut-fab.mobile{
  opacity:0!important;
  visibility:hidden!important;
  display:none!important;
  pointer-events:none!important;
  z-index:0!important;
}
.menu-body.library-mode,
.menu-body.library-mode .library-shell,
.menu-body.library-mode .library-body,
.menu-body.library-mode .media-home-shell,
.menu-body.library-mode .media-results,
.menu-body.library-mode .media-items-list{
  width:100%!important;
  max-width:100%!important;
  min-width:0!important;
  overflow-x:hidden!important;
  transform:none!important;
  touch-action:pan-y!important;
  overscroll-behavior-x:none!important;
}
.menu-body.library-mode .library-shell{height:100%!important;contain:layout paint;}
.menu-body.library-mode .library-body,
#mobileMediaSearchResults{
  overflow-y:auto!important;
  overflow-x:hidden!important;
  -webkit-overflow-scrolling:touch;
  overscroll-behavior:contain!important;
  overflow-anchor:none!important;
  scroll-behavior:auto!important;
}
.menu-body.sheet-queue,
.menu-body.sheet-queue .queue-list{
  overflow-x:hidden!important;
  transform:none!important;
  touch-action:pan-y!important;
  overscroll-behavior-x:none!important;
}
.menu-sheet.sheet-queue,
.menu-body.sheet-queue .queue-row,
.menu-body.sheet-queue .queue-list,
.menu-body.sheet-queue .queue-page-head{
  animation:none!important;
  transition:none!important;
  transform:none!important;
  will-change:auto!important;
}
.menu-body.sheet-queue .queue-row,
.menu-body.sheet-queue .menu-thumb,
.menu-body.sheet-queue .chip-btn{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.menu-body.sheet-queue .queue-eq span{
  animation:none!important;
}
.menu-body.sheet-queue .queue-row.active,
.mini-queue-item.active{
  border-color:color-mix(in srgb, var(--ma-accent) 48%, rgba(255,255,255,.18))!important;
  background:
    linear-gradient(90deg, color-mix(in srgb, var(--ma-accent) 24%, transparent), transparent 72%),
    color-mix(in srgb, var(--ma-soft) 92%, transparent)!important;
  box-shadow:
    inset 0 0 0 1px color-mix(in srgb, var(--ma-accent) 28%, transparent),
    0 12px 28px rgba(0,0,0,.18)!important;
}
.menu-body.sheet-queue .queue-row.active .queue-title,
.menu-body.sheet-queue .queue-row.active .queue-index,
.mini-queue-item.active .mini-queue-name,
.mini-queue-item.active .mini-queue-index{
  color:color-mix(in srgb, var(--ma-accent) 76%, white 24%)!important;
}
.queue-action-backdrop,
.queue-action-sheet,
.queue-action-item{
  animation:none!important;
  transition:none!important;
  will-change:auto!important;
}
.queue-action-sheet,
.queue-action-item{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.card.layout-tablet .menu-body.sheet-queue,
.card.layout-tablet .menu-body.sheet-queue .queue-list{
  contain:layout paint!important;
  isolation:isolate!important;
}
.card.layout-tablet .menu-body.sheet-queue .queue-row,
.card.layout-tablet .menu-body.sheet-queue .queue-row:hover,
.card.layout-tablet .menu-body.sheet-queue .queue-row:active,
.card.layout-tablet .menu-body.sheet-queue .queue-row.tap-feedback{
  transform:none!important;
  filter:none!important;
  transition:none!important;
  will-change:auto!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.card.layout-tablet .menu-body.sheet-queue .queue-row::before,
.card.layout-tablet .menu-body.sheet-queue .queue-row::after{
  animation:none!important;
  transition:none!important;
}
.menu-body.sheet-queue .queue-row{
  transition:border-color .08s ease, background-color .08s ease, box-shadow .08s ease!important;
}
.menu-body.sheet-queue .queue-row.expanded{
  min-height:126px!important;
  overflow:visible!important;
}
.card.layout-tablet .menu-body.sheet-queue .queue-row.expanded{
  min-height:136px!important;
}
.menu-body.sheet-queue .queue-inline-actions{
  transition:none!important;
}
.theme-light .queue-row.expanded .queue-inline-actions{
  border-color:rgba(147,161,183,.18);
  background:rgba(255,255,255,.72);
}
.theme-light .queue-inline-actions .chip-btn{
  border-color:rgba(147,161,183,.18);
  background:rgba(255,255,255,.68);
  color:#253041;
}
.theme-light .queue-inline-actions .chip-btn.primary{
  border-color:color-mix(in srgb, var(--ma-accent) 44%, rgba(147,161,183,.18));
  background:linear-gradient(135deg, color-mix(in srgb, var(--ma-accent) 86%, white 14%), color-mix(in srgb, var(--ma-accent) 68%, black 32%));
  color:#111827;
}
.theme-light .queue-inline-actions .chip-btn.warn{
  border-color:rgba(220,72,72,.24);
  background:rgba(220,72,72,.11);
  color:#cf4d4d;
}
.card.layout-tablet .menu-sheet.sheet-schedules{width:min(calc(100% - 96px), 1080px)!important;max-width:min(calc(100% - 96px), 1080px)!important;height:calc(100% - 26px)!important;max-height:calc(100% - 26px)!important;}
.card.layout-tablet .menu-body.sheet-schedules{justify-items:stretch!important;align-content:start!important;overflow:auto!important;padding:20px 28px 28px!important;scrollbar-gutter:stable!important;}
.card.layout-tablet .menu-body.sheet-schedules .settings-shell{width:min(100%, 920px)!important;margin:0 auto!important;display:grid!important;grid-template-rows:auto auto!important;gap:18px!important;height:auto!important;min-height:auto!important;overflow:visible!important;}
.card.layout-tablet .schedule-tabs{width:100%!important;max-width:none!important;}
.card.layout-tablet .schedule-content{width:100%!important;display:grid!important;gap:16px!important;align-content:start!important;}
.card.layout-tablet .schedule-panel-card,
.card.layout-tablet .wake-schedule-layout,
.card.layout-tablet .wake-schedule-list-card,
.card.layout-tablet .wake-schedule-editor-card{width:100%!important;max-width:none!important;}
.card.layout-tablet .wake-schedule-layout{height:auto!important;display:grid!important;grid-template-columns:minmax(0,1fr)!important;gap:16px!important;overflow:visible!important;align-items:stretch!important;}
.card.layout-tablet .wake-schedule-list-card{max-height:none!important;overflow:visible!important;}
.card.layout-tablet .scheduled-start-grid,
.card.layout-tablet .scheduled-start-grid.two-col,
.card.layout-tablet .night-window-grid{grid-template-columns:minmax(0,1fr)!important;}
.card.layout-tablet .sleep-timer-action-row,
.card.layout-tablet .sleep-timer-action-row.with-cancel{grid-template-columns:repeat(3,minmax(0,1fr))!important;}
.card.layout-tablet .sleep-timer-action-row.with-cancel .danger{grid-column:1 / -1!important;}
.card.layout-tablet .menu-body.sheet-schedules .settings-group,
.card.layout-tablet .menu-body.sheet-schedules .scheduled-start-card,
.card.layout-tablet .menu-body.sheet-schedules .schedule-panel-card,
.card.layout-tablet .menu-body.sheet-schedules .schedule-row,
.card.layout-tablet .menu-body.sheet-schedules .settings-pill,
.card.layout-tablet .menu-body.sheet-schedules .night-time-card,
.card.layout-tablet .menu-body.sheet-schedules .scheduled-start-field,
.card.layout-tablet .menu-body.sheet-schedules select,
.card.layout-tablet .menu-body.sheet-schedules input{
  animation:none!important;
  transition:none!important;
  transform:none!important;
  will-change:auto!important;
}
.card.layout-tablet .menu-body.sheet-schedules select,
.card.layout-tablet .menu-body.sheet-schedules input,
.card.layout-tablet .menu-body.sheet-schedules .settings-pill,
.card.layout-tablet .menu-body.sheet-schedules .night-time-card,
.card.layout-tablet .menu-body.sheet-schedules .scheduled-start-field{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.menu-body.sheet-schedules{
  contain:layout paint;
  overscroll-behavior:contain;
}
.menu-body.sheet-schedules select,
.menu-body.sheet-schedules input,
.menu-body.sheet-schedules .night-time-card,
.menu-body.sheet-schedules .scheduled-start-field{
  transform:none!important;
  transition:none!important;
  will-change:auto!important;
}
.menu-body.sheet-schedules select:focus,
.menu-body.sheet-schedules input:focus{
  scroll-margin-block:120px 160px;
}
.menu-body.sheet-schedules .settings-select{
  min-height:58px;
  line-height:1.2;
}
.this-device-strip{width:max-content;max-width:100%;display:flex;align-items:center;justify-content:center;gap:10px;padding:7px 8px 7px 14px;margin:0 auto 12px;border-radius:999px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.07);box-shadow:inset 0 1px 0 rgba(255,255,255,.08);white-space:nowrap;direction:inherit;}
.this-device-copy{min-width:0;display:inline-flex;align-items:center;gap:7px;text-align:start;}
.this-device-title{font-size:calc(12px * var(--v2-font-scale));font-weight:900;color:rgba(255,255,255,.9);white-space:nowrap;line-height:1;}
.this-device-status{display:inline-flex;align-items:center;min-height:24px;padding:0 9px;border-radius:999px;background:rgba(255,255,255,.09);color:rgba(255,255,255,.68);font-size:calc(10px * var(--v2-font-scale));font-weight:900;white-space:nowrap;}
.this-device-toggle{flex:0 0 auto;display:inline-flex;align-items:center;gap:6px;min-height:34px;padding:3px 5px 3px 10px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:rgba(12,16,24,.34);color:#fff;font-size:calc(11px * var(--v2-font-scale));font-weight:900;cursor:pointer;touch-action:manipulation;box-shadow:0 10px 22px rgba(0,0,0,.14),inset 0 1px 0 rgba(255,255,255,.08);}
.this-device-toggle.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .18);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);}
.this-device-toggle:disabled{cursor:default;opacity:.88;}
.this-device-strip.disconnecting .this-device-toggle{background:rgba(255,255,255,.08);border-color:rgba(255,255,255,.12);}
.this-device-strip.connected .this-device-toggle{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .2);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);}
.this-device-toggle-knob{width:26px;height:26px;border-radius:999px;display:grid;place-items:center;background:rgba(255,255,255,.12);}
.this-device-toggle-knob .ui-ic{width:14px;height:14px;}
.theme-light .this-device-strip{background:rgba(255,255,255,.72);border-color:rgba(135,150,172,.18);box-shadow:0 14px 30px rgba(111,126,150,.1);}
.theme-light .this-device-title{color:#1f2937;}
.theme-light .this-device-status{color:#66758a;background:rgba(31,41,55,.08);}
.theme-light .this-device-toggle{color:#1f2937;background:rgba(238,242,247,.92);border-color:rgba(128,145,168,.2);}
.theme-light .this-device-toggle.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .16);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .26);}
.card.layout-mobile .this-device-strip{padding:6px 7px 6px 12px;border-radius:999px;gap:8px;}
.card.layout-mobile .this-device-toggle{max-width:none;padding-inline-start:9px;}
.card.layout-mobile .this-device-toggle span:first-child{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
@media (max-width: 1280px){
  .control-room-layout{gap:10px;}
  .control-room-grid-wrap{padding:24px 20px 0;}
  .control-room-grid{max-width:var(--control-room-grid-max-width, 100%);}
  .control-room-tile.selected{transform:none;}
  .control-room-tray{inset-block-end:128px;width:min(1080px, calc(100% - 36px));max-height:calc(100dvh - 220px);}
  .control-room-tray.compact{width:min(680px, calc(100% - 36px));}
  .control-room-picker-list,.control-room-library-results{max-height:calc(100dvh - 300px);}
  .control-room-transfer-list{max-height:calc(50dvh - 72px);}
  .control-room-dock{padding:12px 14px calc(14px + env(safe-area-inset-bottom, 0px));gap:10px;max-width:calc(100% - 2px);}
  .control-room-now-pill{min-width:220px;max-width:260px;}
  .control-room-dock-btn,.control-room-selection-pill{width:auto;height:50px;min-width:50px;}
  .control-room-dock-btn.library-pill{min-width:52px;padding:0;}
  .control-room-dock-divider{display:none;}
  .control-room-dock-section{padding:6px 8px;}
}
@media (max-width:760px){
  .control-room-grid-wrap{padding:18px 10px 0;}
  .control-room-tray{inset-block-end:112px;width:calc(100% - 16px);max-height:calc(100dvh - 206px);padding:12px;border-radius:24px;z-index:32;}
  .control-room-tray.compact,.control-room-tray.wide{width:calc(100% - 16px);}
  .control-room-transfer-board{grid-template-columns:minmax(0,1fr);gap:10px;}
  .control-room-transfer-arrow{display:none;}
  .control-room-transfer-action{width:100%;height:50px;}
  .control-room-tray.transfer-panel .control-room-transfer-list{max-height:min(30dvh, 220px);}
  .control-room-picker-list,.control-room-library-results{max-height:calc(100dvh - 320px);}
}

.control-room-tile-actions{position:relative;z-index:2;display:flex;align-items:center;gap:6px;align-self:end;justify-self:end;margin-top:-44px;margin-inline-end:2px;}
.control-room-tile-actions button{width:34px;height:34px;border-radius:13px;border:1px solid rgba(255,255,255,.14);background:rgba(12,15,22,.42);color:#fff;display:grid;place-items:center;box-shadow:0 10px 24px rgba(0,0,0,.18);backdrop-filter:blur(14px);-webkit-backdrop-filter:blur(14px);cursor:pointer;}
.control-room-tile-actions button.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .26);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .36);}
.control-room-tile-actions .ui-ic{width:17px;height:17px;}
.control-room-float-pill .ui-ic{width:13px;height:13px;margin-inline-end:4px;}
.control-room-float-pill.protocol{text-transform:uppercase;letter-spacing:.02em;}
.theme-light .control-room-tile-actions button{background:rgba(255,255,255,.76);border-color:rgba(28,42,68,.1);color:#1b2740;}
.control-room-dock{justify-content:flex-start;overflow-x:auto;overflow-y:visible;scrollbar-width:none;}
.control-room-dock::-webkit-scrollbar{display:none;}
.card.layout-tablet .control-room-dock{justify-content:flex-start!important;overflow-x:auto!important;overflow-y:visible!important;flex-wrap:nowrap!important;}
.card.layout-tablet .control-room-dock-section{flex:0 0 auto;}
.control-room-dock-btn.danger,.control-room-panel-action.danger{background:rgba(222,72,72,.16)!important;border-color:rgba(255,105,105,.24)!important;color:#ffb6b6!important;}
.control-room-dock-section.selected{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .07);}
.control-room-panel-action{min-height:52px;border-radius:18px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.07);color:#fff;display:inline-flex;align-items:center;justify-content:center;gap:9px;padding:0 16px;font-size:calc(13px * var(--v2-font-scale));font-weight:900;cursor:pointer;touch-action:manipulation;}
.control-room-panel-action.primary{background:linear-gradient(135deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .38), rgba(128,88,210,.46));border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);box-shadow:0 16px 36px rgba(var(--dynamic-accent-rgb,245 166 35) / .16);}
.control-room-panel-action.wide{width:100%;}
.control-room-panel-action:disabled{opacity:.45;cursor:not-allowed;}
.theme-light .control-room-panel-action{background:rgba(255,255,255,.78);border-color:rgba(28,42,68,.1);color:#17253a;}
.control-room-queue-actions{display:flex;align-items:center;justify-content:flex-end;gap:10px;flex-wrap:wrap;}
.control-room-queue-preview{display:grid;gap:8px;margin-top:10px;padding:10px;border-radius:20px;background:rgba(255,255,255,.055);border:1px solid rgba(255,255,255,.09);max-height:190px;overflow:auto;}
.control-room-queue-preview-head{display:flex;align-items:center;justify-content:space-between;gap:10px;}
.control-room-queue-player{font-size:calc(12px * var(--v2-font-scale));font-weight:900;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-queue-count{min-width:30px;height:24px;border-radius:999px;display:inline-grid;place-items:center;padding:0 8px;background:rgba(var(--dynamic-accent-rgb,245 166 35) / .18);font-weight:900;color:#fff;}
.control-room-queue-row{display:grid;grid-template-columns:36px minmax(0,1fr);align-items:center;gap:9px;min-height:42px;padding:5px;border-radius:14px;background:rgba(255,255,255,.045);}
.control-room-queue-row.current{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);}
.control-room-queue-art{width:36px;height:36px;border-radius:11px;overflow:hidden;display:grid;place-items:center;background:rgba(255,255,255,.08);color:#fff;}
.control-room-queue-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-queue-copy{min-width:0;display:grid;gap:2px;}
.control-room-queue-title,.control-room-queue-sub{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-queue-title{font-size:calc(12px * var(--v2-font-scale));font-weight:850;color:#fff;}
.control-room-queue-sub{font-size:calc(10px * var(--v2-font-scale));color:rgba(255,255,255,.62);}
.theme-light .control-room-queue-preview,.theme-light .control-room-queue-row{background:rgba(255,255,255,.72);border-color:rgba(28,42,68,.08);}
.theme-light .control-room-queue-player,.theme-light .control-room-queue-title{color:#17253a;}
.theme-light .control-room-queue-sub{color:#71829a;}
.control-room-media-grid.large{grid-template-columns:repeat(auto-fit,minmax(180px,1fr));}
.control-room-media-card{display:grid;grid-template-rows:minmax(0,1fr) auto;gap:10px;min-height:236px;}
.control-room-media-card.liked{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);}
.control-room-media-main{display:grid;gap:10px;padding:0;background:none;border:0;color:inherit;text-align:inherit;cursor:pointer;min-width:0;}
.control-room-media-kicker{font-size:calc(10px * var(--v2-font-scale));font-weight:900;color:rgba(var(--dynamic-accent-rgb,245 166 35) / .9);text-transform:uppercase;}
.control-room-media-actions{display:flex;align-items:center;gap:6px;flex-wrap:wrap;}
.control-room-media-action{height:32px;min-width:52px;padding:0 10px;border-radius:12px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.075);color:#fff;font-size:calc(10px * var(--v2-font-scale));font-weight:900;cursor:pointer;}
.control-room-media-action.primary{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .24);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32);}
.control-room-media-action.icon{min-width:32px;width:32px;padding:0;display:grid;place-items:center;}
.control-room-media-action.icon .ui-ic{width:15px;height:15px;}
.control-room-media-action.active{color:var(--ma-accent);}
.theme-light .control-room-media-action{background:rgba(245,248,252,.94);border-color:rgba(28,42,68,.08);color:#1b2740;}
.control-room-mix-panel,.control-room-announce-panel,.control-room-diagnostics{display:grid;gap:14px;min-width:0;}
.control-room-mix-grid,.control-room-scenes-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px;}
.control-room-mix-card,.control-room-scene-card{min-height:122px;border-radius:24px;border:1px solid rgba(255,255,255,.1);background:linear-gradient(145deg, rgba(255,255,255,.09), rgba(255,255,255,.035));color:#fff;display:grid;align-content:center;justify-items:center;gap:7px;padding:14px;text-align:center;box-shadow:0 16px 34px rgba(0,0,0,.16);cursor:pointer;}
.control-room-mix-card:hover,.control-room-scene-card:hover{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32);background:rgba(var(--dynamic-accent-rgb,245 166 35) / .12);}
.control-room-mix-icon{width:46px;height:46px;border-radius:17px;display:grid;place-items:center;background:rgba(var(--dynamic-accent-rgb,245 166 35) / .18);color:var(--ma-accent);}
.control-room-mix-title,.control-room-scene-card span{font-size:calc(16px * var(--v2-font-scale));font-weight:950;}
.control-room-mix-sub,.control-room-scene-card small{font-size:calc(11px * var(--v2-font-scale));font-weight:700;color:rgba(255,255,255,.62);}
.control-room-scene-card .ui-ic{width:30px;height:30px;color:var(--ma-accent);}
.theme-light .control-room-mix-card,.theme-light .control-room-scene-card{background:rgba(255,255,255,.76);border-color:rgba(28,42,68,.08);color:#17253a;}
.theme-light .control-room-mix-sub,.theme-light .control-room-scene-card small{color:#71829a;}
.control-room-diagnostic-row{min-height:44px;border-radius:16px;background:rgba(255,255,255,.06);display:flex;align-items:center;justify-content:space-between;gap:12px;padding:0 14px;color:#fff;}
.control-room-diagnostic-row span{font-size:calc(12px * var(--v2-font-scale));color:rgba(255,255,255,.64);font-weight:800;}
.control-room-diagnostic-row strong{font-size:calc(12px * var(--v2-font-scale));font-weight:950;color:#fff;}
.control-room-pro-actions{display:flex;gap:10px;flex-wrap:wrap;}
.theme-light .control-room-diagnostic-row{background:rgba(255,255,255,.72);color:#17253a;}
.theme-light .control-room-diagnostic-row span{color:#71829a;}
.theme-light .control-room-diagnostic-row strong{color:#17253a;}

.control-room-dock.focus-mode{gap:12px;max-width:min(1180px, calc(100% - 24px));justify-content:center;overflow:visible;padding:10px 14px calc(12px + env(safe-area-inset-bottom, 0px));}
.control-room-dock.focus-mode .control-room-now-pill{min-width:min(360px, 38vw);max-width:420px;background:linear-gradient(145deg, rgba(255,255,255,.09), rgba(255,255,255,.035));}
.control-room-dock.focus-mode .control-room-dock-section{background:rgba(255,255,255,.045);}
.control-room-dock.focus-mode .focus-nav{padding:7px;gap:8px;}
.control-room-dock.focus-mode .primary-actions{padding:7px;gap:8px;}
.control-room-dock.focus-mode .control-room-dock-btn,.control-room-dock.focus-mode .control-room-selection-pill{min-width:76px;height:56px;border-radius:18px;}
.control-room-dock.focus-mode .control-room-selection-pill{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .22);border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .28);}
.control-room-context-chip{display:inline-flex;align-items:center;gap:10px;justify-self:start;max-width:100%;padding:8px 12px;border-radius:999px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.065);color:#fff;box-shadow:0 12px 24px rgba(0,0,0,.12);}
.control-room-context-art{width:34px;height:34px;border-radius:12px;display:grid;place-items:center;overflow:hidden;background:rgba(255,255,255,.08);flex:none;}
.control-room-context-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-context-art .ui-ic{width:16px;height:16px;}
.control-room-context-copy{display:grid;gap:1px;min-width:0;}
.control-room-context-kicker{font-size:calc(10px * var(--v2-font-scale));font-weight:850;color:rgba(255,255,255,.58);white-space:nowrap;}
.control-room-context-name{font-size:calc(13px * var(--v2-font-scale));font-weight:950;color:#fff;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-hub-panel{grid-template-rows:auto auto minmax(0,1fr);}
.control-room-hub-grid,.control-room-action-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(178px,1fr));gap:12px;min-height:0;overflow:auto;padding-inline-end:2px;scrollbar-width:thin;}
.control-room-action-grid{grid-template-columns:repeat(auto-fit,minmax(160px,1fr));}
.control-room-hub-card{min-height:118px;padding:16px;border-radius:24px;border:1px solid rgba(255,255,255,.1);background:linear-gradient(145deg, rgba(255,255,255,.085), rgba(255,255,255,.035));color:#fff;display:grid;align-content:center;justify-items:start;gap:7px;text-align:start;box-shadow:0 16px 34px rgba(0,0,0,.14);cursor:pointer;touch-action:manipulation;}
.control-room-hub-card.primary{background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .28), rgba(128,88,210,.24));border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32);}
.control-room-hub-card.danger{background:rgba(222,72,72,.13);border-color:rgba(255,105,105,.22);color:#ffb6b6;}
.control-room-hub-card:disabled{opacity:.44;cursor:not-allowed;}
.control-room-hub-card .ui-ic{width:28px;height:28px;color:var(--ma-accent);}
.control-room-hub-card span{font-size:calc(16px * var(--v2-font-scale));font-weight:950;line-height:1.05;}
.control-room-hub-card small{font-size:calc(11px * var(--v2-font-scale));font-weight:750;color:rgba(255,255,255,.62);line-height:1.25;}
.control-room-announcement-tray{width:min(780px, calc(100% - 44px));grid-template-rows:auto auto auto;gap:14px;}
.control-room-announce-hero{display:flex;align-items:center;gap:14px;min-width:0;padding:6px 4px 0;}
.control-room-announce-icon{width:54px;height:54px;border-radius:20px;display:grid;place-items:center;flex:none;background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .24), rgba(128,88,210,.24));border:1px solid rgba(var(--dynamic-accent-rgb,245 166 35) / .22);color:#fff;}
.control-room-announce-icon .ui-ic{width:25px;height:25px;}
.control-room-announce-copy{display:grid;gap:4px;min-width:0;}
.control-room-announce-panel{display:grid;gap:14px;}
.control-room-announce-compose{display:grid;gap:8px;font-size:calc(12px * var(--v2-font-scale));font-weight:900;color:rgba(255,255,255,.7);}
.control-room-announce-compose .announcement-textarea{width:100%;min-height:122px;resize:vertical;border-radius:22px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.075);color:#fff;padding:16px 18px;font:inherit;font-weight:700;outline:none;box-sizing:border-box;}
.control-room-announce-compose .announcement-textarea:focus{border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .38);box-shadow:0 0 0 3px rgba(var(--dynamic-accent-rgb,245 166 35) / .12);}
.control-room-announce-controls{display:grid;grid-template-columns:minmax(0,1fr) minmax(180px, .42fr);gap:12px;align-items:stretch;}
.control-room-announce-volume-card{display:grid;align-content:center;gap:12px;min-height:82px;padding:14px 16px;border-radius:22px;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.055);}
.control-room-announce-volume-head{display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:calc(12px * var(--v2-font-scale));font-weight:900;color:rgba(255,255,255,.7);}
.control-room-announce-volume-head strong{font-size:calc(16px * var(--v2-font-scale));font-weight:950;color:#fff;}
.control-room-announce-send{min-height:82px;border-radius:22px;font-size:calc(14px * var(--v2-font-scale));}
.control-room-tile-actions{opacity:0;pointer-events:none;transform:translateY(4px);transition:opacity .16s ease,transform .16s ease;}
.control-room-tile:hover .control-room-tile-actions,.control-room-tile.selected .control-room-tile-actions,.control-room-tile.is-playing .control-room-tile-actions{opacity:1;pointer-events:auto;transform:translateY(0);}
.theme-light .control-room-context-chip,.theme-light .control-room-hub-card,.theme-light .control-room-announce-volume-card{background:rgba(255,255,255,.76);border-color:rgba(28,42,68,.08);color:#17253a;}
.theme-light .control-room-context-kicker,.theme-light .control-room-hub-card small,.theme-light .control-room-announce-compose,.theme-light .control-room-announce-volume-head{color:#71829a;}
.theme-light .control-room-context-name,.theme-light .control-room-announce-volume-head strong{color:#17253a;}
.theme-light .control-room-announce-compose .announcement-textarea{background:rgba(245,248,252,.94);border-color:rgba(28,42,68,.1);color:#17253a;}
@media (max-width: 980px){
  .control-room-dock.focus-mode{justify-content:flex-start;overflow-x:auto;overflow-y:visible;max-width:calc(100% - 12px);}
  .control-room-dock.focus-mode .control-room-now-pill{display:flex!important;min-width:230px;max-width:280px;}
  .control-room-announce-controls{grid-template-columns:minmax(0,1fr);}
  .control-room-hub-grid,.control-room-action-grid{grid-template-columns:repeat(auto-fit,minmax(142px,1fr));}
}
@media (max-height: 620px){
  .control-room-dock.focus-mode .control-room-now-pill{display:none!important;}
  .control-room-hub-card{min-height:96px;}
  .control-room-announcement-tray{max-height:calc(100dvh - 116px);}
  .control-room-announce-compose .announcement-textarea{min-height:86px;}
}

.control-room-scene{
  --cr-panel-bg:rgba(17,19,25,.965);
  --cr-panel-bg-soft:rgba(255,255,255,.065);
  --cr-panel-bg-raised:linear-gradient(145deg, rgba(255,255,255,.105), rgba(255,255,255,.045));
  --cr-border:rgba(255,255,255,.13);
  --cr-border-strong:rgba(255,255,255,.18);
  --cr-text:#fff;
  --cr-muted:rgba(255,255,255,.64);
  --cr-faint:rgba(255,255,255,.46);
  --cr-radius:24px;
  --cr-icon-box:48px;
  --cr-icon-size:22px;
  --cr-action-height:56px;
}
.theme-light .control-room-scene{
  --cr-panel-bg:rgba(250,252,255,.975);
  --cr-panel-bg-soft:rgba(255,255,255,.78);
  --cr-panel-bg-raised:linear-gradient(145deg, rgba(255,255,255,.96), rgba(241,246,252,.88));
  --cr-border:rgba(27,41,66,.1);
  --cr-border-strong:rgba(27,41,66,.15);
  --cr-text:#17253a;
  --cr-muted:#71829a;
  --cr-faint:#8a98aa;
}
.control-room-scene.panel-open .control-room-tray,
.control-room-tray{
  background:var(--cr-panel-bg)!important;
  border-color:var(--cr-border-strong)!important;
  box-shadow:0 28px 80px rgba(0,0,0,.38), inset 0 1px 0 rgba(255,255,255,.08)!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  padding:22px!important;
  gap:16px!important;
}
.control-room-tray.wide{width:min(1120px, calc(100% - 64px))!important;}
.control-room-tray.compact{width:min(780px, calc(100% - 64px))!important;}
.control-room-tray-title{font-size:calc(18px * var(--v2-font-scale))!important;font-weight:950!important;line-height:1.08!important;color:var(--cr-text)!important;}
.control-room-tray-sub{font-size:calc(12px * var(--v2-font-scale))!important;font-weight:750!important;line-height:1.35!important;color:var(--cr-muted)!important;max-width:720px;}
.control-room-context-chip{background:var(--cr-panel-bg-soft)!important;border-color:var(--cr-border)!important;color:var(--cr-text)!important;padding:9px 13px!important;}
.control-room-context-art{width:38px!important;height:38px!important;border-radius:14px!important;background:rgba(255,255,255,.09)!important;}
.control-room-context-art .ui-ic{width:18px!important;height:18px!important;}
.control-room-context-kicker{font-size:calc(10px * var(--v2-font-scale))!important;color:var(--cr-muted)!important;}
.control-room-context-name{font-size:calc(14px * var(--v2-font-scale))!important;color:var(--cr-text)!important;}
.control-room-hub-grid,.control-room-action-grid{grid-template-columns:repeat(auto-fit,minmax(220px,1fr))!important;gap:14px!important;align-content:start;}
.control-room-hub-card,
.control-room-mix-card,
.control-room-scene-card{
  min-height:112px!important;
  border-radius:var(--cr-radius)!important;
  border-color:var(--cr-border)!important;
  background:var(--cr-panel-bg-raised)!important;
  color:var(--cr-text)!important;
  box-shadow:0 18px 38px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.08)!important;
  display:grid!important;
  grid-template-columns:var(--cr-icon-box) minmax(0,1fr)!important;
  grid-template-rows:auto auto!important;
  align-content:center!important;
  align-items:center!important;
  justify-items:start!important;
  column-gap:14px!important;
  row-gap:4px!important;
  text-align:start!important;
  padding:16px!important;
}
.control-room-hub-card.primary,
.control-room-panel-action.primary{background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .3), rgba(128,88,210,.28))!important;border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34)!important;}
.control-room-hub-card.danger{background:rgba(222,72,72,.15)!important;border-color:rgba(255,105,105,.24)!important;color:#ffb6b6!important;}
.control-room-hub-card > .ui-ic,
.control-room-mix-icon,
.control-room-scene-card > .ui-ic{
  grid-row:1 / span 2!important;
  width:var(--cr-icon-box)!important;
  height:var(--cr-icon-box)!important;
  border-radius:17px!important;
  display:grid!important;
  place-items:center!important;
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .16)!important;
  color:var(--ma-accent)!important;
}
.control-room-hub-card > .ui-ic,
.control-room-scene-card > .ui-ic{padding:12px!important;box-sizing:border-box;}
.control-room-hub-card > .ui-ic *,
.control-room-scene-card > .ui-ic *{vector-effect:non-scaling-stroke;}
.control-room-mix-icon .ui-ic{width:var(--cr-icon-size)!important;height:var(--cr-icon-size)!important;}
.control-room-hub-card span:not(.ui-ic),
.control-room-mix-title,
.control-room-scene-card span:not(.ui-ic){
  font-size:calc(15px * var(--v2-font-scale))!important;
  font-weight:950!important;
  line-height:1.1!important;
  color:var(--cr-text)!important;
  white-space:normal!important;
}
.control-room-hub-card small,
.control-room-mix-sub,
.control-room-scene-card small{
  font-size:calc(11px * var(--v2-font-scale))!important;
  font-weight:760!important;
  line-height:1.28!important;
  color:var(--cr-muted)!important;
}
.control-room-picker-row,
.control-room-transfer-choice,
.control-room-media-card,
.control-room-queue-preview,
.control-room-diagnostic-row,
.control-room-announce-volume-card,
.control-room-announce-compose .announcement-textarea,
.control-room-search{
  background:var(--cr-panel-bg-soft)!important;
  border-color:var(--cr-border)!important;
  color:var(--cr-text)!important;
}
.control-room-picker-row,.control-room-transfer-choice{min-height:68px!important;border-radius:22px!important;padding:9px 12px!important;}
.control-room-picker-art,.control-room-transfer-art{width:48px!important;height:48px!important;border-radius:16px!important;}
.control-room-picker-art .ui-ic,.control-room-transfer-art .ui-ic{width:20px!important;height:20px!important;}
.control-room-picker-title,.control-room-transfer-title,.control-room-media-title,.control-room-queue-title{font-size:calc(13px * var(--v2-font-scale))!important;font-weight:920!important;color:var(--cr-text)!important;}
.control-room-picker-sub,.control-room-transfer-sub,.control-room-media-sub,.control-room-queue-sub{font-size:calc(11px * var(--v2-font-scale))!important;font-weight:720!important;color:var(--cr-muted)!important;}
.control-room-panel-action,
.control-room-media-action{
  min-height:var(--cr-action-height)!important;
  border-radius:18px!important;
  border-color:var(--cr-border)!important;
  background:var(--cr-panel-bg-soft)!important;
  color:var(--cr-text)!important;
  font-size:calc(12px * var(--v2-font-scale))!important;
  font-weight:900!important;
}
.control-room-panel-action .ui-ic{width:19px!important;height:19px!important;}
.control-room-media-action{height:36px!important;min-height:36px!important;min-width:58px!important;border-radius:13px!important;}
.control-room-dock.focus-mode{background:rgba(12,15,21,.92)!important;border-color:var(--cr-border)!important;box-shadow:0 24px 60px rgba(0,0,0,.34)!important;}
.control-room-dock.focus-mode .control-room-now-pill,
.control-room-dock.focus-mode .control-room-dock-section{background:rgba(255,255,255,.07)!important;border-color:var(--cr-border)!important;}
.control-room-dock.focus-mode .control-room-dock-btn,
.control-room-dock.focus-mode .control-room-selection-pill{height:58px!important;min-width:78px!important;border-radius:18px!important;background:rgba(255,255,255,.075)!important;border-color:var(--cr-border)!important;}
.control-room-dock.focus-mode .control-room-dock-btn.active,
.control-room-dock.focus-mode .control-room-selection-pill.active{background:rgba(var(--dynamic-accent-rgb,245 166 35) / .26)!important;border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34)!important;}
.control-room-dock-btn .ui-ic{width:20px!important;height:20px!important;}
.control-room-dock-label{font-size:calc(10px * var(--v2-font-scale))!important;font-weight:900!important;}
.control-room-announcement-tray{width:min(760px, calc(100% - 64px))!important;}
.control-room-announce-icon{width:50px!important;height:50px!important;border-radius:17px!important;}
.control-room-announce-icon .ui-ic{width:22px!important;height:22px!important;}
.control-room-announce-compose .announcement-textarea{min-height:112px!important;color:var(--cr-text)!important;font-size:calc(13px * var(--v2-font-scale))!important;}
.control-room-announce-send{min-height:78px!important;}
.theme-light .control-room-dock.focus-mode{background:rgba(250,252,255,.96)!important;}
@media (max-width:980px){
  .control-room-tray.wide,.control-room-tray.compact,.control-room-announcement-tray{width:calc(100% - 24px)!important;padding:16px!important;}
  .control-room-hub-grid,.control-room-action-grid{grid-template-columns:repeat(auto-fit,minmax(158px,1fr))!important;gap:10px!important;}
  .control-room-hub-card,.control-room-mix-card,.control-room-scene-card{min-height:100px!important;grid-template-columns:42px minmax(0,1fr)!important;padding:13px!important;column-gap:11px!important;}
  .control-room-hub-card > .ui-ic,.control-room-mix-icon,.control-room-scene-card > .ui-ic{width:42px!important;height:42px!important;border-radius:15px!important;}
}

.control-room-player-console{
  display:flex;
  align-items:center;
  gap:10px;
  min-width:min(620px, 52vw);
  max-width:min(720px, 58vw);
  padding:8px;
  border-radius:28px;
  border:1px solid var(--cr-border)!important;
  background:linear-gradient(145deg, rgba(255,255,255,.09), rgba(255,255,255,.045))!important;
  box-shadow:0 18px 44px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.08);
  min-height:78px;
}
.control-room-dock.focus-mode .control-room-player-console .control-room-now-pill,
.control-room-dock.focus-mode .control-room-player-console .control-room-dock-section{
  background:transparent!important;
  border-color:transparent!important;
  box-shadow:none!important;
}
.control-room-player-console .control-room-now-pill{
  flex:1 1 260px;
  min-width:220px!important;
  max-width:none!important;
  padding:4px 8px!important;
}
.control-room-player-console .control-room-now-art{width:54px!important;height:54px!important;border-radius:18px!important;}
.control-room-player-console .control-room-now-name{font-size:calc(15px * var(--v2-font-scale))!important;font-weight:950!important;}
.control-room-player-console .control-room-now-track{font-size:calc(12px * var(--v2-font-scale))!important;}
.control-room-player-console .primary-actions{
  flex:0 0 auto;
  padding:0!important;
  gap:8px!important;
}
.control-room-player-console .primary-actions .control-room-dock-btn{min-width:72px!important;}
.theme-light .control-room-player-console{background:linear-gradient(145deg, rgba(255,255,255,.96), rgba(241,246,252,.84))!important;}

.control-room-tray.transfer-panel{
  width:min(1240px, calc(100% - 64px))!important;
  max-height:min(76vh, 740px)!important;
  grid-template-rows:auto minmax(0,1fr)!important;
}
.control-room-queue-layout{
  display:grid;
  grid-template-rows:minmax(0, 1fr) minmax(132px, .56fr) auto;
  gap:14px;
  min-height:0;
  overflow:hidden;
}
.control-room-transfer-board.control-room-transfer-selectors{
  grid-template-columns:minmax(0,1fr) 52px minmax(0,1fr)!important;
  align-items:stretch!important;
  gap:14px!important;
  min-height:0;
}
.control-room-transfer-selectors .control-room-transfer-column{
  grid-template-rows:auto minmax(0,1fr)!important;
  min-height:0!important;
}
.control-room-tray.transfer-panel .control-room-transfer-list{
  max-height:270px!important;
  height:100%;
}
.control-room-queue-preview-board{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:14px;
  min-height:0;
}
.control-room-queue-preview-column{
  display:grid;
  grid-template-rows:auto minmax(0,1fr);
  gap:8px;
  min-width:0;
  min-height:0;
}
.control-room-tray.transfer-panel .control-room-queue-preview{
  margin-top:0!important;
  max-height:none!important;
  min-height:132px;
  height:100%;
  overflow:auto;
}
.control-room-queue-preview-column .control-room-empty{height:100%;display:grid;place-items:center;}
.control-room-tray.transfer-panel .control-room-queue-actions{justify-content:flex-end;padding-top:2px;}

.control-room-tile.grouped{
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .36)!important;
  box-shadow:0 20px 46px rgba(0,0,0,.24), 0 0 0 1px rgba(var(--dynamic-accent-rgb,245 166 35) / .11) inset!important;
}
.control-room-tile.grouped::after{
  content:"";
  position:absolute;
  inset:auto 18px 12px 18px;
  height:3px;
  border-radius:999px;
  background:linear-gradient(90deg, transparent, rgba(var(--dynamic-accent-rgb,245 166 35) / .7), transparent);
  pointer-events:none;
}
.control-room-float-pill.grouped{
  gap:4px;
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .22)!important;
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .32)!important;
  color:#fff!important;
}
.control-room-float-pill.grouped .ui-ic{width:12px!important;height:12px!important;}
.control-room-tile .control-room-select-fab{
  width:auto!important;
  min-width:86px!important;
  height:42px!important;
  padding:0 11px!important;
  grid-template-columns:auto auto;
  gap:6px;
  border-radius:999px!important;
  z-index:6!important;
  background:rgba(9,12,18,.48)!important;
  border-color:rgba(255,255,255,.16)!important;
}
.control-room-tile .control-room-select-fab.active{
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .28)!important;
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .42)!important;
}
.control-room-tile .control-room-select-fab.removable{
  background:rgba(222,72,72,.2)!important;
  border-color:rgba(255,105,105,.32)!important;
  color:#ffd1d1!important;
}
.control-room-select-label{
  font-size:calc(10px * var(--v2-font-scale));
  font-weight:950;
  line-height:1;
  white-space:nowrap;
}
.control-room-tile.grouped .control-room-tile-copy{
  box-shadow:0 0 0 1px rgba(var(--dynamic-accent-rgb,245 166 35) / .14),0 14px 30px rgba(0,0,0,.24)!important;
}
.control-room-tile.grouped .control-room-float-pill.grouped{
  font-size:calc(10px * var(--v2-font-scale))!important;
  padding-inline:9px!important;
}
.theme-light .control-room-tile .control-room-select-fab{background:rgba(255,255,255,.78)!important;border-color:rgba(27,41,66,.1)!important;color:#17253a!important;}
.theme-light .control-room-tile .control-room-select-fab.removable{background:rgba(255,235,235,.9)!important;border-color:rgba(210,62,62,.22)!important;color:#b94a4a!important;}

@media (max-width:980px){
  .control-room-player-console{
    min-width:min(540px, 72cqi);
    max-width:none;
    flex:0 0 auto;
  }
  .control-room-tray.transfer-panel{
    width:calc(100% - 24px)!important;
    max-height:calc(100dvh - 190px)!important;
  }
  .control-room-queue-layout{
    grid-template-rows:auto auto auto;
    overflow:auto;
  }
  .control-room-transfer-board.control-room-transfer-selectors,
  .control-room-queue-preview-board{
    grid-template-columns:minmax(0,1fr)!important;
  }
  .control-room-transfer-selectors .control-room-transfer-arrow{display:none!important;}
  .control-room-tray.transfer-panel .control-room-transfer-list{max-height:190px!important;}
  .control-room-tray.transfer-panel .control-room-queue-preview{max-height:180px!important;height:auto;}
}
@media (max-width:720px){
  .control-room-tile .control-room-select-fab{min-width:76px!important;height:38px!important;padding-inline:9px!important;}
  .control-room-select-label{font-size:9px!important;}
  .control-room-player-console{
    min-width:calc(100cqi - 34px);
    flex-wrap:wrap;
    justify-content:space-between;
  }
  .control-room-player-console .control-room-now-pill{flex:1 1 100%;min-width:0!important;}
  .control-room-player-console .primary-actions{width:100%;justify-content:space-between;}
  .control-room-player-console .primary-actions .control-room-dock-btn{flex:1 1 0;min-width:0!important;}
}

.control-room-action-console{
  display:grid;
  grid-template-columns:minmax(260px,.95fr) minmax(0,1.35fr);
  align-items:center;
  gap:16px;
  min-height:118px;
  padding:16px;
  border-radius:28px;
  border:1px solid var(--cr-border-strong)!important;
  background:linear-gradient(145deg, rgba(255,255,255,.09), rgba(255,255,255,.04))!important;
  box-shadow:0 22px 54px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.08);
}
.control-room-action-now{
  display:flex;
  align-items:center;
  gap:14px;
  min-width:0;
}
.control-room-action-art{
  width:72px;
  height:72px;
  border-radius:22px;
  overflow:hidden;
  display:grid;
  place-items:center;
  flex:none;
  background:rgba(255,255,255,.08);
  border:1px solid var(--cr-border);
}
.control-room-action-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-action-art .ui-ic{width:30px;height:30px;color:var(--ma-accent);}
.control-room-action-copy{display:grid;gap:4px;min-width:0;}
.control-room-action-kicker{font-size:calc(11px * var(--v2-font-scale));font-weight:900;color:var(--cr-muted);}
.control-room-action-name{font-size:calc(18px * var(--v2-font-scale));font-weight:950;line-height:1.05;color:var(--cr-text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-action-track{font-size:calc(12px * var(--v2-font-scale));font-weight:760;color:var(--cr-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-media-controls{
  display:grid;
  grid-template-columns:repeat(4,minmax(0,1fr));
  gap:10px;
  padding:8px;
  border-radius:24px;
  background:rgba(7,9,14,.28);
  border:1px solid rgba(255,255,255,.08);
}
.control-room-media-control{
  min-height:72px;
  border-radius:20px;
  border:1px solid var(--cr-border)!important;
  background:rgba(255,255,255,.07)!important;
  color:var(--cr-text)!important;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:7px;
  font-size:calc(12px * var(--v2-font-scale));
  font-weight:950;
  cursor:pointer;
  touch-action:manipulation;
}
.control-room-media-control .ui-ic{width:22px;height:22px;}
.control-room-media-control.primary,.control-room-media-control.active{
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .24)!important;
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .36)!important;
  box-shadow:0 16px 32px rgba(var(--dynamic-accent-rgb,245 166 35) / .12);
}
.control-room-media-control.danger{
  background:rgba(222,72,72,.16)!important;
  border-color:rgba(255,105,105,.26)!important;
  color:#ffc2c2!important;
}
.control-room-media-control:disabled{opacity:.42;cursor:not-allowed;}
.control-room-action-grid.management{
  grid-template-columns:repeat(auto-fit,minmax(210px,1fr))!important;
}
.control-room-action-grid.management .control-room-hub-card{min-height:100px!important;}

.control-room-tray.transfer-panel{
  width:min(1120px, calc(100% - 90px))!important;
  max-height:min(72vh, 690px)!important;
}
.control-room-tray.transfer-panel .control-room-tray-head{
  max-width:760px;
}
.control-room-tray.transfer-panel .control-room-queue-layout{
  grid-template-rows:minmax(0,1fr) auto!important;
  gap:16px!important;
  overflow:hidden!important;
}
.control-room-queue-lanes{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:16px;
  min-height:0;
}
.control-room-queue-lane{
  display:grid;
  grid-template-rows:auto minmax(120px,.45fr) auto minmax(132px,.55fr);
  gap:10px;
  min-width:0;
  min-height:0;
  padding:14px;
  border-radius:26px;
  border:1px solid var(--cr-border)!important;
  background:linear-gradient(145deg, rgba(255,255,255,.075), rgba(255,255,255,.035))!important;
  box-shadow:0 18px 42px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.06);
}
.control-room-queue-lane-head{
  display:flex;
  align-items:flex-end;
  justify-content:space-between;
  gap:14px;
  min-width:0;
  padding-inline:2px;
}
.control-room-queue-lane-head span{
  font-size:calc(11px * var(--v2-font-scale));
  font-weight:950;
  color:var(--cr-muted);
}
.control-room-queue-lane-head strong{
  font-size:calc(15px * var(--v2-font-scale));
  font-weight:950;
  color:var(--cr-text);
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.control-room-tray.transfer-panel .control-room-transfer-label{
  padding:0 2px!important;
  color:var(--cr-muted)!important;
}
.control-room-tray.transfer-panel .control-room-transfer-list{
  max-height:none!important;
  height:auto!important;
  min-height:0!important;
  overflow:auto!important;
  padding:2px 4px 2px 0!important;
}
.control-room-tray.transfer-panel .control-room-transfer-choice{
  min-height:56px!important;
  border-radius:18px!important;
  padding:7px 10px!important;
  grid-template-columns:42px minmax(0,1fr) 24px!important;
}
.control-room-tray.transfer-panel .control-room-transfer-art{
  width:42px!important;
  height:42px!important;
  border-radius:14px!important;
}
.control-room-tray.transfer-panel .control-room-queue-preview{
  margin-top:0!important;
  min-height:0!important;
  height:100%!important;
  max-height:none!important;
  padding:12px!important;
  border-radius:22px!important;
}
.control-room-tray.transfer-panel .control-room-queue-row{
  min-height:46px!important;
  border-radius:15px!important;
}
.control-room-tray.transfer-panel .control-room-queue-actions{
  justify-content:center!important;
  gap:12px!important;
  padding:2px 2px 0!important;
}
.control-room-tray.transfer-panel .control-room-panel-action{
  min-width:150px;
}

@media (max-width:1100px){
  .control-room-action-console{grid-template-columns:minmax(0,1fr);align-items:stretch;}
  .control-room-media-controls{grid-template-columns:repeat(4,minmax(88px,1fr));}
}
@media (max-width:980px){
  .control-room-tray.transfer-panel{
    width:calc(100% - 24px)!important;
    max-height:calc(100dvh - 190px)!important;
  }
  .control-room-queue-lanes{grid-template-columns:minmax(0,1fr);}
  .control-room-queue-lane{grid-template-rows:auto minmax(110px,180px) auto minmax(118px,170px);}
  .control-room-media-controls{grid-template-columns:repeat(2,minmax(0,1fr));}
}
@media (max-width:640px){
  .control-room-action-console{padding:12px;border-radius:24px;}
  .control-room-action-art{width:58px;height:58px;border-radius:18px;}
  .control-room-action-name{font-size:calc(15px * var(--v2-font-scale));}
  .control-room-media-control{min-height:62px;border-radius:17px;}
  .control-room-tray.transfer-panel .control-room-panel-action{min-width:0;flex:1 1 calc(50% - 8px);}
}

.control-room-grid-wrap{
  grid-template-rows:auto minmax(0,1fr)!important;
  gap:10px!important;
}
.control-room-group-summary{
  width:100%;
  max-width:min(var(--control-room-grid-max-width, 100%), 100%);
  display:flex;
  gap:10px;
  overflow-x:auto;
  padding:0 2px 2px;
  scrollbar-width:none;
  box-sizing:border-box;
}
.card:not(.layout-tablet) .control-room-group-summary::-webkit-scrollbar{display:none;}
.control-room-group-chip{
  flex:0 0 auto;
  max-width:min(420px, 82vw);
  min-height:48px;
  display:flex;
  align-items:center;
  gap:10px;
  padding:7px 12px 7px 8px;
  border-radius:18px;
  border:1px solid rgba(var(--dynamic-accent-rgb,245 166 35) / .24);
  background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), rgba(255,255,255,.06));
  color:var(--cr-text);
  box-shadow:0 14px 32px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.08);
}
.control-room-group-art{
  width:34px;
  height:34px;
  border-radius:12px;
  overflow:hidden;
  display:grid;
  place-items:center;
  flex:none;
  background:rgba(255,255,255,.08);
}
.control-room-group-art img{width:100%;height:100%;object-fit:cover;display:block;}
.control-room-group-art .ui-ic{width:17px;height:17px;color:var(--ma-accent);}
.control-room-group-copy{min-width:0;display:grid;gap:2px;}
.control-room-group-title{font-size:calc(11px * var(--v2-font-scale));font-weight:950;line-height:1.1;color:var(--cr-text);}
.control-room-group-members{font-size:calc(10px * var(--v2-font-scale));font-weight:760;color:var(--cr-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-library-shortcuts{
  display:flex;
  align-items:center;
  gap:8px;
  overflow-x:auto;
  padding-bottom:2px;
  scrollbar-width:none;
}
.card:not(.layout-tablet) .control-room-library-shortcuts::-webkit-scrollbar{display:none;}
.control-room-library-shortcuts button{
  min-height:42px;
  padding:0 13px;
  border-radius:999px;
  border:1px solid var(--cr-border)!important;
  background:var(--cr-panel-bg-soft)!important;
  color:var(--cr-text)!important;
  display:flex;
  align-items:center;
  gap:8px;
  font-size:calc(11px * var(--v2-font-scale));
  font-weight:920;
  white-space:nowrap;
  cursor:pointer;
}
.control-room-library-shortcuts .ui-ic{width:16px;height:16px;color:var(--ma-accent);}
.control-room-scene-save{
  display:grid;
  grid-template-columns:minmax(0,1fr) auto;
  gap:12px;
  align-items:center;
}
.control-room-scene-save .control-room-search{min-height:56px;}
.control-room-saved-scenes{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
  gap:10px;
  max-height:min(26dvh, 260px);
  overflow:auto;
  padding-inline-end:2px;
}
.control-room-saved-scene-card{
  min-width:0;
  display:grid;
  grid-template-columns:minmax(0,1fr) 48px;
  align-items:stretch;
  gap:8px;
}
.control-room-saved-scene-main,
.control-room-saved-scene-delete{
  border:1px solid var(--cr-border)!important;
  background:var(--cr-panel-bg-soft)!important;
  color:var(--cr-text)!important;
  box-shadow:0 14px 28px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.06);
  cursor:pointer;
}
.control-room-saved-scene-main{
  min-height:72px;
  border-radius:20px;
  display:grid;
  grid-template-columns:42px minmax(0,1fr);
  align-items:center;
  gap:12px;
  text-align:start;
  padding:10px 12px;
}
.control-room-saved-scene-main > .ui-ic{
  width:42px;
  height:42px;
  padding:11px;
  box-sizing:border-box;
  border-radius:15px;
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .16);
  color:var(--ma-accent);
}
.control-room-saved-scene-main span{display:grid;gap:4px;min-width:0;}
.control-room-saved-scene-main strong{font-size:calc(13px * var(--v2-font-scale));font-weight:950;color:var(--cr-text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-saved-scene-main small{font-size:calc(10px * var(--v2-font-scale));font-weight:760;color:var(--cr-muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.control-room-saved-scene-delete{
  width:48px;
  min-height:72px;
  border-radius:18px;
  display:grid;
  place-items:center;
  color:#ffc2c2!important;
  background:rgba(222,72,72,.14)!important;
  border-color:rgba(255,105,105,.24)!important;
}
.control-room-saved-scene-delete .ui-ic{width:18px;height:18px;}
.control-room-tray.transfer-panel{
  width:min(1180px, calc(100% - 72px))!important;
  max-height:min(calc(100% - var(--control-room-head-reserve, 74px) - 132px), 720px)!important;
  background:rgba(17,19,25,.985)!important;
}
.control-room-tray.transfer-panel .control-room-queue-layout{
  grid-template-rows:minmax(0,1fr) auto!important;
  min-height:0!important;
}
.control-room-queue-lane{
  grid-template-rows:auto minmax(110px,.48fr) auto minmax(132px,.52fr)!important;
}
.theme-light .control-room-group-chip,
.theme-light .control-room-saved-scene-main,
.theme-light .control-room-saved-scene-delete,
.theme-light .control-room-library-shortcuts button{
  background:rgba(255,255,255,.86)!important;
}
@media (max-width:980px){
  .control-room-scene-save{grid-template-columns:minmax(0,1fr);}
  .control-room-scene-save .control-room-panel-action{width:100%;}
  .control-room-saved-scenes{grid-template-columns:minmax(0,1fr);max-height:220px;}
  .control-room-group-summary{padding-inline:0;}
  .control-room-tray.transfer-panel{width:calc(100% - 24px)!important;}
}
.card.control-room-open .control-room-tray,
.card.control-room-open .control-room-tray.wide,
.card.control-room-open .control-room-tray.compact,
.card.control-room-open .control-room-announcement-tray,
.card.control-room-open .control-room-tray.transfer-panel{
  inset-inline-start:50%!important;
  inset-block-start:calc(var(--control-room-head-reserve, 74px) + 10px)!important;
  inset-block-end:calc(var(--control-room-dock-reserve, 116px) + 10px)!important;
  width:min(1320px, calc(100% - 28px))!important;
  height:auto!important;
  min-height:0!important;
  max-height:none!important;
  transform:translateX(-50%)!important;
}
.card.control-room-open .control-room-tray.compact,
.card.control-room-open .control-room-announcement-tray{
  width:min(980px, calc(100% - 28px))!important;
}
.card.control-room-open .control-room-tray.transfer-panel .control-room-transfer-list,
.card.control-room-open .control-room-tray.transfer-panel .control-room-queue-preview,
.card.control-room-open .control-room-picker-list,
.card.control-room-open .control-room-library-results{
  max-height:none!important;
}
@media (max-width:760px){
  .card.control-room-open .control-room-tray,
  .card.control-room-open .control-room-tray.wide,
  .card.control-room-open .control-room-tray.compact,
  .card.control-room-open .control-room-announcement-tray,
  .card.control-room-open .control-room-tray.transfer-panel{
    inset-block-start:calc(var(--control-room-head-reserve, 68px) + 8px)!important;
    inset-block-end:calc(var(--control-room-dock-reserve, 104px) + 8px)!important;
    width:calc(100% - 16px)!important;
    padding:14px!important;
    border-radius:24px!important;
  }
}

.card.performance-lite,
.card.performance-lite * ,
.card.performance-lite *::before,
.card.performance-lite *::after{
  animation:none!important;
  transition:none!important;
  scroll-behavior:auto!important;
  will-change:auto!important;
}
.card.performance-lite{
  box-shadow:none!important;
}
.card.performance-lite .bg{
  filter:none!important;
  transform:none!important;
  opacity:.72!important;
}
.card.performance-lite .shade{
  filter:none!important;
}
.card.performance-lite .glow,
.card.performance-lite .hero-aura,
.card.performance-lite .art-aura,
.card.performance-lite .compact-cover-echo,
.card.performance-lite .brand-light{
  display:none!important;
}
.card.performance-lite .menu-sheet,
.card.performance-lite .queue-action-sheet,
.card.performance-lite .history-drawer,
.card.performance-lite .control-room-backdrop,
.card.performance-lite .control-room-tray,
.card.performance-lite .control-room-dock,
.card.performance-lite .control-room-tile,
.card.performance-lite .player-menu-card,
.card.performance-lite .group-player-card,
.card.performance-lite .settings-group,
.card.performance-lite .menu-item,
.card.performance-lite .menu-list-item,
.card.performance-lite .queue-row,
.card.performance-lite .notice{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  box-shadow:none!important;
}
.card.performance-lite .queue-eq span,
.card.performance-lite .eq-icon span{
  animation:none!important;
  transform:none!important;
}
.card.performance-ultra-lite .np-sub.scroll-when-overflow.is-overflowing .scrolling-text-inner{
  animation:none!important;
  transform:none!important;
  will-change:auto!important;
}
.performance-profile-pills .settings-pill{
  flex:1 1 96px;
}
.card.performance-ultra-lite .menu-sheet,
.card.performance-ultra-lite .queue-action-sheet,
.card.performance-ultra-lite .history-drawer,
.card.performance-ultra-lite .control-room-backdrop,
.card.performance-ultra-lite .control-room-tray,
.card.performance-ultra-lite .control-room-dock{
  background:rgba(11,14,20,.98)!important;
  border-color:rgba(255,255,255,.11)!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  box-shadow:none!important;
}
.theme-light.card.performance-ultra-lite .menu-sheet,
.theme-light.card.performance-ultra-lite .queue-action-sheet,
.theme-light.card.performance-ultra-lite .history-drawer,
.theme-light.card.performance-ultra-lite .control-room-backdrop,
.theme-light.card.performance-ultra-lite .control-room-tray,
.theme-light.card.performance-ultra-lite .control-room-dock{
  background:rgba(248,250,253,.98)!important;
  border-color:rgba(123,139,164,.22)!important;
  box-shadow:none!important;
}
.card.performance-ultra-lite .menu-sheet::before,
.card.performance-ultra-lite .menu-sheet::after,
.card.performance-ultra-lite .queue-action-sheet::before,
.card.performance-ultra-lite .queue-action-sheet::after,
.card.performance-ultra-lite .history-drawer::before,
.card.performance-ultra-lite .history-drawer::after,
.card.performance-ultra-lite .control-room-backdrop::before,
.card.performance-ultra-lite .control-room-backdrop::after,
.card.performance-ultra-lite .control-room-tray::before,
.card.performance-ultra-lite .control-room-tray::after,
.card.performance-ultra-lite .control-room-dock::before,
.card.performance-ultra-lite .control-room-dock::after{
  display:none!important;
  content:none!important;
}
.card.performance-ultra-lite .menu-item,
.card.performance-ultra-lite .menu-list-item,
.card.performance-ultra-lite .queue-row,
.card.performance-ultra-lite .media-entry.grid,
.card.performance-ultra-lite .player-menu-card,
.card.performance-ultra-lite .group-player-card,
.card.performance-ultra-lite .settings-group,
.card.performance-ultra-lite .control-room-tile,
.card.performance-ultra-lite .notice{
  background:rgba(20,24,32,.96)!important;
  border-color:rgba(255,255,255,.11)!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  box-shadow:none!important;
}
.theme-light.card.performance-ultra-lite .menu-item,
.theme-light.card.performance-ultra-lite .menu-list-item,
.theme-light.card.performance-ultra-lite .queue-row,
.theme-light.card.performance-ultra-lite .media-entry.grid,
.theme-light.card.performance-ultra-lite .player-menu-card,
.theme-light.card.performance-ultra-lite .group-player-card,
.theme-light.card.performance-ultra-lite .settings-group,
.theme-light.card.performance-ultra-lite .control-room-tile,
.theme-light.card.performance-ultra-lite .notice{
  background:rgba(255,255,255,.96)!important;
  border-color:rgba(123,139,164,.2)!important;
}
.card.performance-ultra-lite.screensaver-active .bg,
.card.performance-ultra-lite.screensaver-active .shade,
.card.performance-ultra-lite.screensaver-active .glow,
.card.performance-ultra-lite .screensaver-backdrop.empty-mode::before,
.card.performance-ultra-lite .screensaver-backdrop.empty-mode::after{
  display:none!important;
}
.card.performance-ultra-lite .screensaver-backdrop{
  background:#02060d!important;
}
.theme-light.card.performance-ultra-lite .screensaver-backdrop{
  background:#f8fafc!important;
}
.card.performance-ultra-lite .screensaver-bg{
  filter:none!important;
  transform:none!important;
  opacity:.18!important;
  background:
    linear-gradient(180deg, rgba(3,7,13,.78), rgba(3,7,13,.94)),
    var(--screensaver-art-url, none) center/cover no-repeat!important;
}
.card.performance-ultra-lite .screensaver-backdrop.empty-mode .screensaver-bg{
  display:none!important;
}
.card.performance-ultra-lite .screensaver-brand{
  display:none!important;
}
.card.performance-ultra-lite .screensaver-art,
.card.performance-ultra-lite .screensaver-analog-clock,
.card.performance-ultra-lite .screensaver-message,
.card.performance-ultra-lite .screensaver-voice-btn{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
  box-shadow:none!important;
}
.card.performance-ultra-lite .screensaver-art{
  border-color:rgba(255,255,255,.12)!important;
}
.card.performance-ultra-lite .screensaver-info,
.card.performance-ultra-lite .screensaver-next{
  text-shadow:none!important;
}
.card.performance-ultra-lite .screensaver-empty-logo{
  filter:none!important;
  opacity:.68!important;
}
.card.performance-ultra-lite .screensaver-voice-btn{
  background:rgba(18,22,30,.96)!important;
  opacity:.62!important;
}
.card.performance-ultra-lite .screensaver-voice-btn.listening::after{
  display:none!important;
}
/* Low + Ultra Lite: eliminate backdrop-filter on always-visible UI
   controls. Each backdrop-filter forces the compositor to render and
   blur the backdrop behind the element every frame, even at idle.
   Substituting solid semi-transparent backgrounds preserves the layered
   look while removing the dominant GPU cost on weak hardware (kiosks,
   RPi). Applied to .performance-lite so both low and ultra_lite
   benefit; ultra_lite keeps its existing additional opacity tweaks. */
.card.performance-lite .player-focus,
.card.performance-lite .mobile-art-fab,
.card.performance-lite .side-btn,
.card.performance-lite .main-btn,
.card.performance-lite .source-badge,
.card.performance-lite .fallback-disc,
.card.performance-lite .control-room-close,
.card.performance-lite .empty-quick-card{
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.card.performance-lite .player-focus,
.card.performance-lite .mobile-art-fab,
.card.performance-lite .side-btn,
.card.performance-lite .main-btn,
.card.performance-lite .control-room-close,
.card.performance-lite .empty-quick-card{
  background:rgba(18,22,30,.78)!important;
  border-color:rgba(255,255,255,.16)!important;
}
.card.performance-lite .source-badge{
  background:rgba(18,22,30,.85)!important;
}
.card.performance-lite .fallback-disc{
  background:rgba(18,22,30,.78)!important;
}
.theme-light.card.performance-lite .player-focus,
.theme-light.card.performance-lite .mobile-art-fab,
.theme-light.card.performance-lite .side-btn,
.theme-light.card.performance-lite .main-btn,
.theme-light.card.performance-lite .control-room-close,
.theme-light.card.performance-lite .empty-quick-card{
  background:rgba(248,250,253,.86)!important;
  border-color:rgba(123,139,164,.22)!important;
}
.theme-light.card.performance-lite .source-badge{
  background:rgba(248,250,253,.92)!important;
}
.theme-light.card.performance-lite .fallback-disc{
  background:rgba(248,250,253,.86)!important;
}

.menu-sheet,
.queue-action-sheet,
.history-drawer{
  background:
    radial-gradient(circle at 16% 4%, rgba(var(--dynamic-accent-rgb,245 166 35) / .22), transparent 34%),
    radial-gradient(circle at 86% 18%, rgba(96,165,250,.12), transparent 34%),
    linear-gradient(180deg, rgba(25,28,36,.92), rgba(11,14,20,.94))!important;
  border-color:rgba(255,255,255,.13)!important;
}
.menu-list-item,
.queue-row,
.media-entry.grid,
.player-menu-card,
.settings-group{
  box-shadow:0 18px 42px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.055);
}
.media-entry.grid,
.queue-row,
.menu-list-item{
  background:
    linear-gradient(145deg, rgba(255,255,255,.092), rgba(255,255,255,.045)),
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .10), transparent 38%)!important;
  border:1px solid rgba(255,255,255,.12)!important;
}
.queue-more-btn,
.media-more-btn,
.media-play-btn{
  background:rgba(255,255,255,.075)!important;
  border:1px solid rgba(255,255,255,.14)!important;
  color:rgba(255,255,255,.82)!important;
  box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
}
.queue-more-btn .ui-ic,
.media-more-btn .ui-ic,
.media-play-btn .ui-ic{
  width:18px!important;
  height:18px!important;
}
.theme-light .menu-sheet,
.theme-light .queue-action-sheet,
.theme-light .history-drawer{
  background:
    radial-gradient(circle at 16% 4%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 86% 18%, rgba(59,130,246,.10), transparent 34%),
    linear-gradient(180deg, rgba(250,252,255,.97), rgba(238,243,249,.96))!important;
  color:#172033!important;
  border-color:rgba(123,139,164,.24)!important;
  box-shadow:0 24px 58px rgba(86,104,132,.18)!important;
}
.card.dynamic-theme .history-drawer{
  overflow:hidden;
  isolation:isolate;
}
.card.dynamic-theme .history-drawer::before{
  content:"";
  position:absolute;
  inset:-24px;
  background:var(--dynamic-art-url, none) center/cover no-repeat;
  filter:blur(30px) saturate(1.1);
  transform:scale(1.08);
  opacity:calc(.2 * var(--dynamic-theme-strength, .82));
  pointer-events:none;
  z-index:0;
}
.card.dynamic-theme .history-drawer::after{
  content:"";
  position:absolute;
  inset:0;
  background:
    radial-gradient(circle at 18% 8%, rgba(var(--dynamic-glow-rgb,255 178 56) / calc(.16 * var(--dynamic-theme-strength, .82))), transparent 34%),
    linear-gradient(180deg, rgba(14,17,24,.76), rgba(10,13,20,.9));
  pointer-events:none;
  z-index:0;
}
.theme-light.card.dynamic-theme .history-drawer::after{
  background:
    radial-gradient(circle at 18% 8%, rgba(var(--dynamic-glow-rgb,255 178 56) / calc(.1 * var(--dynamic-theme-strength, .82))), transparent 34%),
    linear-gradient(180deg, rgba(255,255,255,.82), rgba(244,248,253,.92));
}
.card.dynamic-theme .history-drawer>*{
  position:relative;
  z-index:1;
}
.card.performance-lite .history-drawer::before,
.card.performance-lite .history-drawer::after{
  display:none!important;
}
.theme-light .menu-body,
.theme-light .library-shell,
.theme-light .media-home-shell,
.theme-light .media-results,
.theme-light .settings-group,
.theme-light .queue-list{
  color:#172033!important;
}
.theme-light .menu-item,
.theme-light .menu-list-item,
.theme-light .queue-row,
.theme-light .media-entry.grid,
.theme-light .settings-group,
.theme-light .notice{
  background:
    linear-gradient(145deg, rgba(255,255,255,.92), rgba(242,246,251,.82)),
    radial-gradient(circle at 12% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .11), transparent 42%)!important;
  border:1px solid rgba(123,139,164,.20)!important;
  color:#172033!important;
  box-shadow:0 14px 34px rgba(86,104,132,.12), inset 0 1px 0 rgba(255,255,255,.65)!important;
}
.theme-light .menu-item-title,
.theme-light .queue-title,
.theme-light .media-section-title,
.theme-light .settings-label,
.theme-light .history-drawer-title,
.theme-light .player-premium-name{
  color:#172033!important;
}
.theme-light .menu-item-sub,
.theme-light .queue-sub,
.theme-light .player-premium-track,
.theme-light .settings-hint{
  color:#536176!important;
}
.theme-light .queue-more-btn,
.theme-light .media-more-btn,
.theme-light .media-play-btn{
  background:rgba(255,255,255,.88)!important;
  border-color:rgba(123,139,164,.24)!important;
  color:#4b586b!important;
}
.theme-light .media-sort-select,
.theme-light .settings-select,
.theme-light .settings-text-input,
.theme-light .announcement-textarea,
.theme-light .media-search-shell{
  background:rgba(255,255,255,.9)!important;
  color:#172033!important;
  border-color:rgba(123,139,164,.24)!important;
}
.theme-light .media-sort-select option,
.theme-light .settings-select option{
  background:#ffffff;
  color:#172033;
}
.library-shell{
  grid-template-rows:minmax(0,1fr) auto!important;
}
.library-toolbar{
  display:flex!important;
  align-items:center!important;
  justify-content:space-between!important;
  gap:12px!important;
  flex-wrap:wrap;
  margin:0 0 10px!important;
}
.library-toolbar-actions{
  display:flex;
  align-items:center;
  gap:9px;
  min-width:0;
  flex:1 1 320px;
  flex-wrap:wrap;
}
.library-toolbar .library-player-focus{
  flex:0 1 max-content;
  width:auto!important;
  max-width:min(100%, 360px);
  justify-self:end;
}
.card.layout-tablet .library-toolbar{
  flex-wrap:nowrap;
  width:min(100%, 940px);
  margin-inline:auto!important;
}
.card.layout-tablet .library-toolbar-actions{
  flex:0 1 auto;
}
.card.layout-tablet .library-toolbar .media-layout-toggle{
  background:transparent!important;
  border:none!important;
  box-shadow:none!important;
  padding:0!important;
  gap:8px!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
.card.layout-tablet .library-toolbar .media-layout-btn{
  width:44px;
  min-width:44px;
  height:44px;
  min-height:44px;
  border-radius:999px!important;
  background:rgba(255,255,255,.09);
  border:1px solid rgba(255,255,255,.12);
}
.card.layout-tablet .library-toolbar .library-player-focus{
  max-width:min(44%, 360px);
  min-height:52px;
  padding:7px 11px;
  border-radius:999px;
  grid-template-columns:38px minmax(104px, 1fr) auto auto;
}
.card.layout-tablet .library-toolbar .library-player-art{
  width:38px;
  height:38px;
  border-radius:999px;
}
.library-player-state{
  color:rgba(255,255,255,.62)!important;
  background:transparent!important;
  border:none!important;
  padding:0!important;
  font-size:11px;
  font-weight:780;
  overflow:hidden;
  white-space:nowrap;
  text-overflow:ellipsis;
}
.library-player-focus.is-playing .library-player-state{
  color:rgba(255,255,255,.76)!important;
}
.player-premium-title-row{
  display:flex;
  align-items:center;
  gap:9px;
  min-width:0;
}
.player-premium-title-row .player-premium-name{
  min-width:0;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.player-premium-bars{
  flex:0 0 auto;
  width:18px!important;
  height:15px!important;
  color:var(--ma-accent);
}
.player-premium-state{
  display:none!important;
}
.players-action-hub{
  width:min(100%, 760px);
  margin:0 auto 14px;
  display:grid;
  grid-template-columns:repeat(5, minmax(0,1fr));
  gap:8px;
}
.players-action-chip{
  min-width:0;
  min-height:58px;
  border-radius:18px;
  border:1px solid rgba(255,255,255,.12);
  background:linear-gradient(180deg, rgba(255,255,255,.095), rgba(255,255,255,.045));
  color:inherit;
  display:grid;
  place-items:center;
  gap:4px;
  padding:8px 6px;
  font:inherit;
  font-size:11px;
  font-weight:900;
  cursor:pointer;
  box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
}
.players-action-chip .ui-ic{
  width:19px;
  height:19px;
}
.players-action-chip strong{
  color:var(--ma-accent);
  font-size:12px;
  line-height:1;
}
.players-action-chip.danger{
  color:#ffb4b4;
  border-color:rgba(255,105,105,.22);
  background:rgba(255,72,72,.11);
}
.card.layout-mobile .players-action-hub{
  grid-template-columns:repeat(3, minmax(0,1fr));
}
.this-device-strip{
  width:min(100%, 520px)!important;
  margin:0 auto 12px!important;
  padding:0!important;
  background:transparent!important;
  border:none!important;
  box-shadow:none!important;
}
.this-device-toggle{
  width:100%!important;
  justify-content:center!important;
  min-height:46px!important;
  gap:9px!important;
  padding:0 16px!important;
  border-radius:999px!important;
  border:1px solid rgba(160,170,185,.38)!important;
  background:rgba(255,255,255,.045)!important;
  color:inherit!important;
  box-shadow:none!important;
}
.this-device-dot{
  width:9px;
  height:9px;
  border-radius:999px;
  background:#9ca3af;
  box-shadow:0 0 0 4px rgba(156,163,175,.14);
}
.this-device-toggle.active .this-device-dot{
  background:#36d67a;
  box-shadow:0 0 0 4px rgba(54,214,122,.16);
}
.this-device-toggle-label{
  font-weight:930;
  white-space:nowrap;
}
.this-device-toggle-status{
  color:rgba(255,255,255,.58);
  font-size:11px;
  font-weight:850;
  white-space:nowrap;
}
.queue-page-head-title{
  flex:1 1 auto;
}
.queue-head-transfer-btn{
  margin-inline-start:auto;
}
.queue-page-count{
  margin-inline-start:0!important;
  min-width:54px;
  justify-content:center;
}
.queue-page-count .ui-ic{
  width:17px;
  height:17px;
}
.queue-move-select{
  width:100%;
  min-width:72px;
  height:36px;
  border-radius:13px;
  border:1px solid rgba(255,255,255,.14);
  background:rgba(255,255,255,.08);
  color:inherit;
  font:inherit;
  font-size:13px;
  font-weight:900;
  text-align:center;
  outline:none;
  padding:0 10px;
}
.queue-move-select option{
  color:#111827;
  background:#fff;
}
.queue-inline-move select{
  height:34px;
  border-radius:12px;
}
.queue-move-control{
  grid-template-columns:minmax(0,1fr)!important;
}
.theme-light .menu-sheet,
.theme-light .queue-action-sheet,
.theme-light .history-drawer{
  background:
    radial-gradient(circle at 14% 0%, rgba(var(--dynamic-accent-rgb,245 166 35) / .10), transparent 28%),
    linear-gradient(180deg, rgba(255,255,255,.995), rgba(241,245,249,.985))!important;
  color:#0f172a!important;
  text-shadow:none!important;
}
.theme-light .menu-sheet *,
.theme-light .queue-action-sheet *,
.theme-light .history-drawer *{
  text-shadow:none!important;
}
.theme-light .menu-sheet button,
.theme-light .menu-sheet select,
.theme-light .menu-sheet input,
.theme-light .queue-action-sheet button,
.theme-light .queue-action-sheet select,
.theme-light .queue-action-sheet input{
  color:#0f172a;
}
.theme-light .menu-item-title,
.theme-light .queue-title,
.theme-light .media-section-title,
.theme-light .player-premium-name,
.theme-light .library-player-name,
.theme-light .settings-label{
  color:#0f172a!important;
}
.theme-light .menu-item-sub,
.theme-light .queue-sub,
.theme-light .player-premium-track,
.theme-light .library-player-state,
.theme-light .settings-hint,
.theme-light .this-device-toggle-status{
  color:#475569!important;
}
.theme-light .menu-thumb,
.theme-light .library-player-art,
.theme-light .player-premium-art{
  background:#f8fafc!important;
  border-color:rgba(100,116,139,.22)!important;
  color:#334155!important;
}
.theme-light .media-layout-toggle,
.theme-light .library-nav{
  background:rgba(255,255,255,.92)!important;
  border-color:rgba(100,116,139,.22)!important;
  box-shadow:0 12px 26px rgba(71,85,105,.10)!important;
}
.theme-light .card.layout-tablet .library-toolbar .media-layout-toggle{
  background:transparent!important;
  border:none!important;
  box-shadow:none!important;
}
.theme-light .media-layout-btn:not(.active),
.theme-light .library-nav-btn:not(.active),
.theme-light .players-action-chip{
  color:#334155!important;
  background:rgba(255,255,255,.94)!important;
  border:1px solid rgba(100,116,139,.20)!important;
}
.theme-light .library-player-focus,
.theme-light .player-menu-card,
.theme-light .group-player-card,
.theme-light .menu-list-item,
.theme-light .queue-row,
.theme-light .settings-group,
.theme-light .notice{
  background:linear-gradient(145deg, rgba(255,255,255,.98), rgba(248,250,252,.92))!important;
  border-color:rgba(100,116,139,.22)!important;
  color:#0f172a!important;
  box-shadow:0 14px 32px rgba(71,85,105,.11), inset 0 1px 0 rgba(255,255,255,.78)!important;
}
.theme-light .players-action-chip.danger{
  color:#b4232b!important;
  border-color:rgba(180,35,43,.20)!important;
  background:rgba(255,241,242,.96)!important;
}
.theme-light .this-device-toggle{
  border-color:rgba(100,116,139,.30)!important;
  background:rgba(255,255,255,.94)!important;
}
.theme-light .queue-move-select{
  background:#fff!important;
  color:#0f172a!important;
  border-color:rgba(100,116,139,.24)!important;
}
.player-premium-bars{
  display:inline-flex!important;
  align-items:flex-end!important;
  justify-content:center!important;
  gap:3px!important;
}
.player-premium-bars.is-active{
  color:var(--ma-accent)!important;
}
.player-premium-bars.is-static{
  color:rgba(190,198,210,.58)!important;
}
.player-premium-bars.is-static span{
  animation:none!important;
  transform:none!important;
  opacity:.82!important;
  background:currentColor!important;
}
.player-premium-bars.is-static span:nth-child(1){height:6px!important;}
.player-premium-bars.is-static span:nth-child(2){height:11px!important;}
.player-premium-bars.is-static span:nth-child(3){height:8px!important;}
.theme-light .player-premium-bars.is-static{
  color:rgba(100,116,139,.58)!important;
}
.players-action-hub{
  width:min(100%, 620px)!important;
  grid-template-columns:repeat(4, minmax(0,1fr))!important;
  gap:7px!important;
}
.players-action-chip{
  min-height:46px!important;
  border-radius:999px!important;
  padding:6px 8px!important;
  display:inline-flex!important;
  align-items:center!important;
  justify-content:center!important;
  gap:6px!important;
  font-size:10px!important;
}
.players-action-chip .ui-ic{
  width:17px!important;
  height:17px!important;
}
.card.layout-mobile .players-action-hub{
  grid-template-columns:repeat(2, minmax(0,1fr))!important;
}
.this-device-strip{
  width:max-content!important;
  max-width:100%!important;
  margin:0 auto 12px!important;
}
.this-device-toggle{
  width:auto!important;
  min-height:30px!important;
  padding:0 12px!important;
  gap:7px!important;
  font-size:11px!important;
}
.this-device-dot{
  width:8px!important;
  height:8px!important;
}
.queue-page-head{
  justify-content:space-between!important;
}
.queue-head-transfer-btn{
  margin-inline-start:0!important;
}
.card.layout-tablet .queue-row{
  grid-template-columns:34px 44px minmax(0,1fr) 56px!important;
  grid-template-areas:
    "idx actions meta thumb"
    "inline inline inline inline"!important;
}
.card.layout-tablet .queue-index{
  display:grid!important;
  place-items:center!important;
  width:28px!important;
  height:28px!important;
  border-radius:999px!important;
  background:rgba(255,255,255,.08)!important;
  color:color-mix(in srgb, var(--ma-accent) 72%, white 28%)!important;
  font-size:12px!important;
  font-weight:950!important;
}
.theme-light.card.layout-tablet .menu-sheet,
.theme-light.card.layout-tablet .queue-action-sheet,
.theme-light.card.layout-tablet .history-drawer{
  background:#f8fafc!important;
  color:#0f172a!important;
  -webkit-backdrop-filter:none!important;
  backdrop-filter:none!important;
}
.theme-light.card.layout-tablet .menu-body,
.theme-light.card.layout-tablet .library-shell,
.theme-light.card.layout-tablet .media-home-shell,
.theme-light.card.layout-tablet .media-results,
.theme-light.card.layout-tablet .queue-list{
  background:transparent!important;
  color:#0f172a!important;
}
.theme-light.card.layout-tablet .menu-sheet *,
.theme-light.card.layout-tablet .queue-action-sheet *,
.theme-light.card.layout-tablet .history-drawer *{
  text-shadow:none!important;
  letter-spacing:0!important;
}
.theme-light.card.layout-tablet .menu-list-item,
.theme-light.card.layout-tablet .media-entry,
.theme-light.card.layout-tablet .media-entry.grid,
.theme-light.card.layout-tablet .media-entry.list,
.theme-light.card.layout-tablet .queue-row,
.theme-light.card.layout-tablet .player-menu-card,
.theme-light.card.layout-tablet .group-player-card,
.theme-light.card.layout-tablet .settings-group,
.theme-light.card.layout-tablet .notice{
  background:#ffffff!important;
  border-color:rgba(15,23,42,.14)!important;
  color:#0f172a!important;
  box-shadow:0 10px 24px rgba(15,23,42,.08)!important;
}
.theme-light.card.layout-tablet .menu-item-title,
.theme-light.card.layout-tablet .media-entry-title,
.theme-light.card.layout-tablet .media-section-title,
.theme-light.card.layout-tablet .queue-title,
.theme-light.card.layout-tablet .player-premium-name,
.theme-light.card.layout-tablet .settings-label{
  color:#0f172a!important;
  font-family:'Rubik','Heebo','Segoe UI',system-ui,sans-serif!important;
}
.theme-light.card.layout-tablet .menu-item-sub,
.theme-light.card.layout-tablet .media-entry-sub,
.theme-light.card.layout-tablet .queue-sub,
.theme-light.card.layout-tablet .player-premium-track,
.theme-light.card.layout-tablet .settings-hint{
  color:#475569!important;
}
.theme-light.card.layout-tablet .media-search-shell,
.theme-light.card.layout-tablet .media-sort-select,
.theme-light.card.layout-tablet .queue-move-select,
.theme-light.card.layout-tablet .this-device-toggle{
  background:#ffffff!important;
  color:#0f172a!important;
  border-color:rgba(15,23,42,.18)!important;
}
.theme-light.card.layout-tablet .media-layout-btn:not(.active),
.theme-light.card.layout-tablet .library-nav-btn:not(.active),
.theme-light.card.layout-tablet .players-action-chip{
  background:#ffffff!important;
  color:#334155!important;
  border-color:rgba(15,23,42,.14)!important;
}
.theme-light.card.layout-tablet .queue-index{
  background:#f1f5f9!important;
  color:#0f172a!important;
}
.players-action-bar{
  width:min(100%, 760px)!important;
  margin:0 auto 18px!important;
  display:flex!important;
  align-items:center!important;
  justify-content:center!important;
  gap:10px!important;
}
.players-action-shell{
  width:max-content!important;
  max-width:100%!important;
  margin:0!important;
  padding:6px!important;
  border-radius:999px!important;
  border:1px solid rgba(255,255,255,.13)!important;
  background:linear-gradient(145deg, rgba(255,255,255,.075), rgba(255,255,255,.025))!important;
  box-shadow:0 14px 30px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08)!important;
  backdrop-filter:blur(18px)!important;
  -webkit-backdrop-filter:blur(18px)!important;
}
.players-action-shell .players-action-hub{
  width:auto!important;
  margin:0!important;
  display:flex!important;
  align-items:center!important;
  justify-content:center!important;
  gap:8px!important;
  flex-wrap:nowrap!important;
}
.players-action-shell .players-action-chip{
  position:relative!important;
  width:48px!important;
  height:48px!important;
  min-width:48px!important;
  min-height:48px!important;
  border-radius:999px!important;
  padding:0!important;
  display:grid!important;
  place-items:center!important;
  color:rgba(255,255,255,.86)!important;
  background:rgba(255,255,255,.07)!important;
  border:1px solid rgba(255,255,255,.10)!important;
  box-shadow:none!important;
  font-size:0!important;
}
.players-action-shell .players-action-chip:hover,
.players-action-shell .players-action-chip:focus-visible{
  background:rgba(255,255,255,.12)!important;
  border-color:rgba(255,255,255,.18)!important;
}
.players-action-shell .players-action-label{
  display:none!important;
}
.players-action-shell .players-action-badge{
  position:absolute!important;
  top:-5px!important;
  inset-inline-end:-5px!important;
  min-width:22px!important;
  height:22px!important;
  padding:0 6px!important;
  border-radius:999px!important;
  display:grid!important;
  place-items:center!important;
  color:#10141d!important;
  background:var(--ma-accent)!important;
  border:2px solid rgba(12,16,24,.84)!important;
  font-size:10px!important;
  font-weight:950!important;
  line-height:1!important;
}
.players-action-shell .players-action-dot{
  position:absolute!important;
  top:-5px!important;
  inset-inline-end:-5px!important;
  bottom:auto!important;
  width:17px!important;
  height:17px!important;
  border-radius:999px!important;
  background:#9ca3af!important;
  border:2px solid rgba(12,16,24,.84)!important;
  box-shadow:0 0 0 2px rgba(156,163,175,.12)!important;
}
.players-action-shell .players-action-chip.this-device.connected .players-action-dot{
  background:#36d67a!important;
  box-shadow:0 0 0 2px rgba(54,214,122,.18)!important;
}
.players-action-shell .players-action-chip.this-device.connecting .players-action-dot,
.players-action-shell .players-action-chip.this-device.disconnecting .players-action-dot{
  background:#f5c542!important;
  box-shadow:0 0 0 2px rgba(245,197,66,.18)!important;
}
.players-action-icon{
  flex:0 0 auto!important;
  width:auto!important;
  height:auto!important;
  display:grid!important;
  place-items:center!important;
  border-radius:0!important;
  background:transparent!important;
  color:color-mix(in srgb, var(--ma-accent) 62%, white 38%)!important;
}
.players-action-icon .ui-ic{
  width:19px!important;
  height:19px!important;
}
.players-action-shell .players-action-chip.danger{
  color:#ffd7d7!important;
  background:rgba(255,85,95,.10)!important;
  border-color:rgba(255,105,115,.20)!important;
}
.players-action-shell .players-action-chip.danger .players-action-icon{
  color:#ffc3c3!important;
  background:transparent!important;
}
.players-action-stop{
  height:52px!important;
  min-height:52px!important;
  border-radius:999px!important;
  border:1px solid rgba(255,105,115,.28)!important;
  background:linear-gradient(145deg, rgba(255,85,95,.18), rgba(255,85,95,.08))!important;
  color:#ffd7d7!important;
  display:inline-flex!important;
  align-items:center!important;
  justify-content:center!important;
  gap:8px!important;
  padding:0 16px!important;
  font:inherit!important;
  font-size:12px!important;
  font-weight:950!important;
  white-space:nowrap!important;
  cursor:pointer!important;
  box-shadow:0 14px 30px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.08)!important;
  backdrop-filter:blur(18px)!important;
  -webkit-backdrop-filter:blur(18px)!important;
}
.players-action-stop:hover,
.players-action-stop:focus-visible{
  background:linear-gradient(145deg, rgba(255,85,95,.25), rgba(255,85,95,.12))!important;
  border-color:rgba(255,130,138,.38)!important;
}
.players-action-stop-icon{
  width:30px!important;
  height:30px!important;
  border-radius:999px!important;
  display:grid!important;
  place-items:center!important;
  background:rgba(255,85,95,.16)!important;
  color:#ffc3c3!important;
}
.players-action-stop-icon .ui-ic{
  width:15px!important;
  height:15px!important;
}
.card.layout-mobile .players-action-bar{
  width:100%!important;
  max-width:430px!important;
  flex-wrap:wrap!important;
  gap:8px!important;
}
.card.layout-mobile .players-action-shell .players-action-hub{
  display:flex!important;
  flex-wrap:nowrap!important;
  gap:7px!important;
}
.card.layout-mobile .players-action-shell{
  width:max-content!important;
  max-width:100%!important;
  border-radius:999px!important;
}
.theme-light .players-action-shell,
.theme-light.card.layout-tablet .players-action-shell{
  background:rgba(255,255,255,.88)!important;
  border-color:rgba(15,23,42,.12)!important;
  box-shadow:0 14px 30px rgba(15,23,42,.09), inset 0 1px 0 rgba(255,255,255,.78)!important;
}
.theme-light .players-action-shell .players-action-chip,
.theme-light.card.layout-tablet .players-action-shell .players-action-chip{
  color:#334155!important;
  background:rgba(15,23,42,.05)!important;
  border-color:rgba(15,23,42,.10)!important;
}
.theme-light .players-action-shell .players-action-chip:hover,
.theme-light.card.layout-tablet .players-action-shell .players-action-chip:hover{
  background:#f8fafc!important;
  border-color:rgba(15,23,42,.10)!important;
}
.theme-light .players-action-icon,
.theme-light.card.layout-tablet .players-action-icon{
  color:#0f172a!important;
}
.theme-light .players-action-shell .players-action-badge,
.theme-light.card.layout-tablet .players-action-shell .players-action-badge{
  border-color:rgba(255,255,255,.9)!important;
}
.theme-light .players-action-shell .players-action-dot,
.theme-light.card.layout-tablet .players-action-shell .players-action-dot{
  border-color:rgba(255,255,255,.9)!important;
}
.theme-light .players-action-shell .players-action-chip.danger,
.theme-light.card.layout-tablet .players-action-shell .players-action-chip.danger{
  color:#b4232b!important;
  background:#fff1f2!important;
  border-color:rgba(180,35,43,.12)!important;
}
.theme-light .players-action-stop,
.theme-light.card.layout-tablet .players-action-stop{
  color:#b4232b!important;
  background:#fff1f2!important;
  border-color:rgba(180,35,43,.16)!important;
  box-shadow:0 14px 30px rgba(15,23,42,.08), inset 0 1px 0 rgba(255,255,255,.78)!important;
}
.theme-light .players-action-stop-icon,
.theme-light.card.layout-tablet .players-action-stop-icon{
  color:#b4232b!important;
  background:rgba(180,35,43,.10)!important;
}

.card.layout-mobile .menu-backdrop.action-fullscreen-open{
  padding:0!important;
  background:rgba(5,7,12,.72)!important;
  z-index:80!important;
}
.card.layout-mobile:has(.menu-backdrop.action-fullscreen-open.open) > .home-shortcut-fab{
  display:none!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open::before{
  opacity:.68!important;
  filter:blur(46px) saturate(1.2) brightness(.78)!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-sheet{
  width:100%!important;
  max-width:100%!important;
  height:100%!important;
  max-height:100%!important;
  margin:0!important;
  border-radius:0!important;
  box-shadow:none!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-head{
  min-height:62px;
  background:rgba(8,10,16,.54);
  border-bottom-color:rgba(255,255,255,.10);
  backdrop-filter:blur(18px);
  -webkit-backdrop-filter:blur(18px);
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-title{
  max-width:min(620px, calc(100% - 132px));
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-title-text{
  display:inline-block;
  min-width:0;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-sheet::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .16), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(91,141,255,.14), transparent 28%),
    linear-gradient(180deg, rgba(7,9,15,.68), rgba(8,10,16,.96))!important;
}
.theme-light.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-sheet::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .16), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(80,127,220,.13), transparent 28%),
    linear-gradient(180deg, rgba(248,250,253,.80), rgba(234,240,248,.98))!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-body{
  padding:clamp(12px, 3vw, 20px) clamp(14px, 4vw, 24px) max(24px, env(safe-area-inset-bottom))!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-body.library-mode{
  padding:0!important;
}
.card.layout-mobile .menu-backdrop.action-fullscreen-open .menu-body.sheet-artist-detail{
  padding:clamp(10px, 3vw, 18px) clamp(12px, 4vw, 22px) max(22px, env(safe-area-inset-bottom))!important;
}

.card.layout-tablet .menu-backdrop.library-fullscreen-open{
  padding:0!important;
  background:rgba(5,7,12,.72)!important;
  z-index:80!important;
}
.card.layout-tablet:has(.menu-backdrop.library-fullscreen-open.open) > .home-shortcut-fab{
  display:none!important;
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open::before{
  opacity:.66!important;
  filter:blur(46px) saturate(1.2) brightness(.78)!important;
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-library,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-search,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-artist-detail{
  width:100%!important;
  max-width:100%!important;
  height:100%!important;
  max-height:100%!important;
  margin:0!important;
  border-radius:0!important;
  box-shadow:none!important;
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-head{
  min-height:62px;
  background:rgba(8,10,16,.54);
  border-bottom-color:rgba(255,255,255,.10);
  backdrop-filter:blur(18px);
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-title{
  max-width:min(620px, calc(100% - 132px));
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-title-text{
  display:inline-block;
  min-width:0;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-library::after,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-search::after,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-artist-detail::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(91,141,255,.16), transparent 28%),
    linear-gradient(180deg, rgba(7,9,15,.68), rgba(8,10,16,.96))!important;
}
.theme-light.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-library::after,
.theme-light.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-search::after,
.theme-light.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-sheet.sheet-artist-detail::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(80,127,220,.14), transparent 28%),
    linear-gradient(180deg, rgba(248,250,253,.80), rgba(234,240,248,.98))!important;
}
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-body.sheet-library,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-body.sheet-search,
.card.layout-tablet .menu-backdrop.library-fullscreen-open .menu-body.sheet-artist-detail{
  overflow:auto!important;
  justify-items:stretch!important;
  align-content:start!important;
  padding:clamp(10px, 2vw, 24px) clamp(18px, 3vw, 46px) 36px!important;
}

.menu-backdrop.discovery-open{
  padding:0!important;
  background:rgba(5,7,12,.72)!important;
  z-index:80!important;
}
.card:has(.menu-backdrop.discovery-open.open) > .home-shortcut-fab{
  display:none!important;
}
.menu-backdrop.discovery-open::before{
  opacity:.72!important;
  filter:blur(46px) saturate(1.24) brightness(.78)!important;
}
.menu-backdrop.discovery-open .menu-sheet.sheet-discovery{
  width:100%!important;
  max-width:100%!important;
  height:100%!important;
  max-height:100%!important;
  margin:0!important;
  border-radius:0!important;
  box-shadow:none!important;
}
.menu-backdrop.discovery-open .menu-head{
  min-height:62px;
  background:rgba(8,10,16,.54);
  border-bottom-color:rgba(255,255,255,.10);
  backdrop-filter:blur(18px);
}
.menu-backdrop.discovery-open .menu-title{
  max-width:min(620px, calc(100% - 132px));
}
.menu-backdrop.discovery-open .menu-title-text{
  display:inline-block;
  min-width:0;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.menu-backdrop.discovery-open .menu-sheet.sheet-discovery::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(91,141,255,.16), transparent 28%),
    linear-gradient(180deg, rgba(7,9,15,.68), rgba(8,10,16,.96))!important;
}
.theme-light .menu-backdrop.discovery-open .menu-sheet.sheet-discovery::after{
  background:
    radial-gradient(circle at 14% 12%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 84% 4%, rgba(80,127,220,.14), transparent 28%),
    linear-gradient(180deg, rgba(248,250,253,.80), rgba(234,240,248,.98))!important;
}
.menu-body.sheet-discovery{
  overflow:auto!important;
  justify-items:stretch!important;
  align-content:start!important;
  padding:clamp(2px, .8vw, 10px) clamp(14px, 3vw, 42px) 36px!important;
}
.discovery-shell{
  width:100%;
  min-height:100%;
  display:grid;
  gap:clamp(18px, 3vw, 34px);
  align-content:start;
}
.discovery-hero{
  min-height:clamp(96px, 14vh, 148px);
  display:grid;
  grid-template-columns:minmax(230px,.32fr) minmax(280px,1fr);
  align-items:center;
  gap:clamp(16px, 3vw, 38px);
  padding:clamp(8px, 1.4vw, 18px) 2px 0;
}
.discovery-hero-copy{
  max-width:680px;
  min-width:0;
  display:grid;
  gap:6px;
}
.discovery-hero-title{
  max-width:min(100%, 540px);
  font-size:clamp(34px, 5vw, 72px);
  line-height:.98;
  font-weight:950;
  overflow-wrap:anywhere;
}
.discovery-hero-sub{
  max-width:min(100%, 460px);
  color:var(--ma-text-2);
  font-size:13px;
  line-height:1.4;
  font-weight:850;
  overflow-wrap:anywhere;
}
.discovery-filter-panel{
  min-width:0;
  align-self:center;
  max-width:min(820px, 100%);
  justify-self:stretch;
  display:grid;
  gap:10px;
}
.card.layout-tablet .discovery-filter-panel{
  grid-template-columns:minmax(260px, 360px) minmax(320px, 1fr);
  align-items:end;
  gap:16px;
}
.card.layout-tablet .discovery-player-focus{
  width:100%;
  max-width:none;
  align-self:end;
}
.card.layout-tablet .discovery-category-select{
  align-self:end;
}
.discovery-player-focus{
  min-width:0;
  max-width:min(100%, 430px);
  justify-self:start;
  min-height:58px;
  display:grid;
  grid-template-columns:auto 42px minmax(0,1fr) auto;
  align-items:center;
  gap:10px;
  border:1px solid rgba(255,255,255,.14);
  border-radius:18px;
  padding:8px 12px;
  color:var(--ma-text-1);
  background:linear-gradient(145deg, rgba(255,255,255,.11), rgba(255,255,255,.045));
  box-shadow:0 16px 34px rgba(0,0,0,.15), inset 0 1px 0 rgba(255,255,255,.1);
  cursor:pointer;
  text-align:start;
}
.discovery-player-focus:hover,
.discovery-player-focus:focus-visible{
  transform:translateY(-1px);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);
}
.discovery-player-kicker{
  font-size:11px;
  font-weight:950;
  color:var(--ma-accent);
  white-space:nowrap;
}
.discovery-player-art{
  width:42px;
  height:42px;
  border-radius:13px;
  overflow:hidden;
  display:grid;
  place-items:center;
  background:rgba(255,255,255,.08);
}
.discovery-player-art img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
}
.discovery-player-art .ui-ic{
  width:21px;
  height:21px;
}
.discovery-player-copy{
  min-width:0;
  display:grid;
  gap:2px;
}
.discovery-player-name,
.discovery-player-state{
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}
.discovery-player-name{
  font-size:14px;
  font-weight:950;
}
.discovery-player-state{
  font-size:12px;
  font-weight:760;
  color:var(--ma-text-2);
}
.theme-light .discovery-player-focus{
  background:rgba(255,255,255,.78);
  border-color:rgba(137,154,178,.22);
  box-shadow:0 16px 30px rgba(105,122,148,.14);
}
.discovery-filter-rail{
  display:flex;
  align-items:center;
  gap:10px;
  min-width:0;
  overflow:auto;
  padding:2px 2px 10px;
  scrollbar-width:none;
  mask-image:linear-gradient(90deg, transparent, #000 18px, #000 calc(100% - 18px), transparent);
}
.discovery-filter-rail::-webkit-scrollbar{display:none;}
.discovery-filter-divider{
  flex:0 0 1px;
  align-self:stretch;
  min-height:38px;
  background:rgba(255,255,255,.14);
  margin-inline:2px;
}
.discovery-filter-chip{
  flex:0 0 auto;
  min-height:48px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:9px;
  border:1px solid rgba(255,255,255,.13);
  border-radius:999px;
  background:linear-gradient(145deg, rgba(255,255,255,.105), rgba(255,255,255,.045));
  color:var(--ma-text-2);
  padding:9px 16px;
  font:inherit;
  font-size:13px;
  font-weight:930;
  white-space:nowrap;
  cursor:pointer;
  box-shadow:0 14px 30px rgba(0,0,0,.14), inset 0 1px 0 rgba(255,255,255,.10);
  transition:transform .18s ease, border-color .18s ease, color .18s ease, background .18s ease, box-shadow .18s ease;
}
.discovery-filter-chip:hover,
.discovery-filter-chip:focus-visible,
.discovery-filter-chip.active{
  transform:translateY(-2px);
  color:var(--ma-text-1);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);
  background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .20), rgba(255,255,255,.07));
  box-shadow:0 20px 42px rgba(0,0,0,.20), inset 0 1px 0 rgba(255,255,255,.14);
}
.discovery-filter-chip-genre.active{
  color:var(--ma-accent);
}
.discovery-filter-dot{
  width:7px;
  height:7px;
  border-radius:999px;
  background:currentColor;
  box-shadow:0 0 14px currentColor;
  flex:0 0 auto;
}
.discovery-filter-icon{
  width:24px;
  height:24px;
  border-radius:999px;
  display:grid;
  place-items:center;
  color:var(--ma-accent);
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);
}
.discovery-filter-icon .ui-ic{width:15px;height:15px;}
.theme-light .discovery-filter-chip{
  background:rgba(255,255,255,.72);
  border-color:rgba(15,23,42,.10);
  box-shadow:0 12px 26px rgba(15,23,42,.08), inset 0 1px 0 rgba(255,255,255,.74);
}
.theme-light .discovery-filter-divider{
  background:rgba(15,23,42,.12);
}
.discovery-subgenre-rail{
  display:flex;
  align-items:center;
  gap:8px;
  min-width:0;
  overflow:auto;
  padding:0 2px 8px;
  scrollbar-width:none;
  mask-image:linear-gradient(90deg, transparent, #000 18px, #000 calc(100% - 18px), transparent);
}
.discovery-subgenre-rail::-webkit-scrollbar{display:none;}
.discovery-subgenre-chip{
  flex:0 0 auto;
  min-height:34px;
  border:1px solid rgba(255,255,255,.11);
  border-radius:999px;
  background:rgba(255,255,255,.055);
  color:var(--ma-text-3);
  padding:7px 12px;
  font:inherit;
  font-size:12px;
  font-weight:850;
  white-space:nowrap;
  cursor:pointer;
  transition:transform .16s ease, border-color .16s ease, color .16s ease, background .16s ease;
}
.discovery-subgenre-chip:hover,
.discovery-subgenre-chip:focus-visible,
.discovery-subgenre-chip.active{
  transform:translateY(-1px);
  color:var(--ma-text-1);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .30);
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);
}
.theme-light .discovery-subgenre-chip{
  background:rgba(255,255,255,.62);
  border-color:rgba(15,23,42,.10);
}
.discovery-filter-panel{
  min-width:0;
  align-self:center;
  display:grid;
  gap:8px;
  max-width:min(820px, 100%);
  justify-self:stretch;
}
.discovery-category-select{
  display:grid;
  gap:7px;
  min-width:0;
}
.discovery-category-select span{
  color:var(--ma-text-2);
  font-size:10px;
  font-weight:950;
  text-transform:uppercase;
}
.discovery-category-select select{
  min-height:46px;
  width:100%;
}
.discovery-selector{
  display:grid;
  gap:8px;
  min-width:0;
}
.discovery-selector-label{
  width:fit-content;
  max-width:100%;
  padding:5px 10px;
  border-radius:999px;
  color:var(--ma-text-2);
  background:rgba(255,255,255,.07);
  border:1px solid rgba(255,255,255,.10);
  font-size:10px;
  font-weight:950;
  text-transform:uppercase;
}
.discovery-category-strip{
  display:flex;
  gap:10px;
  overflow:auto;
  padding:3px 2px 8px;
  scrollbar-width:none;
  mask-image:linear-gradient(90deg, transparent, #000 18px, #000 calc(100% - 18px), transparent);
}
.discovery-category-strip::-webkit-scrollbar{display:none;}
.discovery-category-chip{
  flex:0 0 auto;
  min-width:136px;
  min-height:56px;
  display:grid;
  grid-template-columns:34px minmax(0,1fr);
  align-items:center;
  gap:9px;
  border-radius:999px;
  border:1px solid rgba(255,255,255,.14);
  background:linear-gradient(145deg, rgba(255,255,255,.11), rgba(255,255,255,.045));
  color:var(--ma-text-1);
  padding:8px 13px 8px 9px;
  font:inherit;
  text-align:start;
  cursor:pointer;
  box-shadow:0 16px 34px rgba(0,0,0,.16), inset 0 1px 0 rgba(255,255,255,.12);
  transition:transform .18s ease, border-color .18s ease, background .18s ease, box-shadow .18s ease;
}
.discovery-category-chip.active,
.discovery-category-chip:hover,
.discovery-category-chip:focus-visible{
  transform:translateY(-2px);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .38);
  background:linear-gradient(145deg, rgba(var(--dynamic-accent-rgb,245 166 35) / .22), rgba(255,255,255,.075));
  box-shadow:0 20px 42px rgba(0,0,0,.20), inset 0 1px 0 rgba(255,255,255,.14);
}
.discovery-category-icon{
  width:34px;
  height:34px;
  border-radius:999px;
  display:grid;
  place-items:center;
  color:var(--ma-accent);
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);
}
.discovery-category-icon .ui-ic{width:18px;height:18px;}
.discovery-category-copy{
  min-width:0;
  display:grid;
  gap:2px;
}
.discovery-category-title,
.discovery-category-sub{
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.discovery-category-title{
  font-size:13px;
  font-weight:950;
}
.discovery-category-sub{
  color:var(--ma-text-3);
  font-size:10px;
  font-weight:760;
}
.discovery-genre-strip{
  display:flex;
  gap:9px;
  overflow:auto;
  padding:2px 2px 8px;
  scrollbar-width:none;
  mask-image:linear-gradient(90deg, transparent, #000 18px, #000 calc(100% - 18px), transparent);
}
.discovery-genre-strip::-webkit-scrollbar{display:none;}
.discovery-genre-chip{
  flex:0 0 auto;
  min-height:40px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:8px;
  border:1px solid rgba(255,255,255,.12);
  border-radius:999px;
  background:rgba(255,255,255,.06);
  color:var(--ma-text-2);
  padding:8px 14px;
  font:inherit;
  font-size:12px;
  font-weight:900;
  cursor:pointer;
  box-shadow:0 12px 28px rgba(0,0,0,.12), inset 0 1px 0 rgba(255,255,255,.08);
  transition:transform .18s ease, border-color .18s ease, color .18s ease, background .18s ease;
}
.discovery-genre-chip::before{
  content:"";
  width:7px;
  height:7px;
  border-radius:999px;
  background:currentColor;
  box-shadow:0 0 14px currentColor;
}
.discovery-genre-chip.active,
.discovery-genre-chip:hover,
.discovery-genre-chip:focus-visible{
  transform:translateY(-2px);
  color:var(--ma-accent);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .34);
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .13);
}
.theme-light .discovery-category-chip{
  background:rgba(255,255,255,.74);
  border-color:rgba(15,23,42,.10);
  box-shadow:0 14px 34px rgba(15,23,42,.10), inset 0 1px 0 rgba(255,255,255,.82);
}
.theme-light .discovery-category-chip.active,
.theme-light .discovery-category-chip:hover,
.theme-light .discovery-category-chip:focus-visible{
  background:rgba(var(--dynamic-accent-rgb,245 166 35) / .14);
  border-color:rgba(var(--dynamic-accent-rgb,245 166 35) / .26);
}
.theme-light .discovery-selector-label,
.theme-light .discovery-genre-chip{
  background:rgba(255,255,255,.70);
  border-color:rgba(15,23,42,.10);
  box-shadow:0 12px 26px rgba(15,23,42,.08), inset 0 1px 0 rgba(255,255,255,.74);
}
.discovery-endless{
  display:grid;
  gap:0;
}
.discovery-endless-head{
  display:none;
  align-items:end;
  justify-content:space-between;
  gap:12px;
  padding-inline:2px;
}
.discovery-endless-title{
  font-size:16px;
  font-weight:950;
  color:var(--ma-text-1);
}
.discovery-endless-sub{
  color:var(--ma-text-3);
  font-size:12px;
  font-weight:760;
}
.discovery-orb-field{
  position:relative;
  display:grid;
  grid-template-columns:repeat(auto-fit, minmax(154px, 1fr));
  gap:clamp(48px, 6vw, 72px) clamp(22px, 4vw, 56px);
  align-items:start;
  padding:clamp(6px, 1.4vw, 18px) clamp(4px, 1vw, 14px) 110px;
  isolation:isolate;
}
.discovery-orb-field::before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  z-index:-1;
  opacity:.46;
  background:
    radial-gradient(circle at 14% 22%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 18%),
    radial-gradient(circle at 72% 46%, rgba(118,168,255,.12), transparent 22%),
    radial-gradient(circle at 46% 88%, rgba(255,255,255,.08), transparent 16%);
  filter:blur(2px);
}
.discovery-orb{
  --orb-size:156px;
  --orb-shift:0px;
  --orb-lift:0px;
  --orb-delay:0s;
  width:var(--orb-size);
  justify-self:center;
  margin-block-start:var(--orb-lift);
  display:grid;
  gap:12px;
  border:0;
  background:transparent;
  color:var(--ma-text-3);
  padding:0;
  text-align:center;
  font:inherit;
  cursor:pointer;
  transform:translateX(var(--orb-shift));
  transition:transform .22s ease, color .22s ease;
  position:relative;
  z-index:1;
  isolation:isolate;
}
.discovery-orb::before{
  content:none;
}
.rtl .discovery-orb{
  transform:translateX(calc(var(--orb-shift) * -1));
}
.discovery-orb.is-active{
  color:var(--ma-text-1);
  z-index:8;
}
.discovery-orb-field.has-active .discovery-orb:not(.is-active){
  z-index:0;
}
.discovery-orb-field.has-active .discovery-orb:not(.is-active) .discovery-orb-art{
  opacity:.7;
  filter:blur(.45px) saturate(.76) brightness(.82);
}
.discovery-orb-field.has-active .discovery-orb:not(.is-active) .discovery-orb-copy{
  opacity:.66;
}
.discovery-orb-art{
  position:relative;
  width:var(--orb-size);
  height:var(--orb-size);
  scale:1;
  border-radius:999px;
  display:grid;
  place-items:center;
  overflow:hidden;
  background:
    radial-gradient(circle at 28% 22%, rgba(255,255,255,.34), transparent 15%),
    radial-gradient(circle at 64% 74%, rgba(255,255,255,.10), transparent 35%),
    rgba(255,255,255,.09);
  border:1px solid rgba(255,255,255,.14);
  box-shadow:0 24px 48px rgba(0,0,0,.26), inset 0 1px 0 rgba(255,255,255,.18);
  animation:discoveryOrbBreath 7.5s ease-in-out infinite;
  animation-delay:var(--orb-delay);
  transition:transform .24s ease, box-shadow .24s ease, border-color .24s ease, filter .24s ease;
  will-change:scale, opacity;
}
.discovery-orb-art::after{
  content:"";
  position:absolute;
  inset:0;
  border-radius:inherit;
  pointer-events:none;
  background:
    radial-gradient(circle at 28% 18%, rgba(255,255,255,.50), transparent 11%),
    linear-gradient(145deg, rgba(255,255,255,.22), transparent 38%, rgba(0,0,0,.16));
  mix-blend-mode:screen;
  opacity:.46;
}
.discovery-orb:hover .discovery-orb-art,
.discovery-orb:focus-visible .discovery-orb-art{
  transform:translateY(-3px) scale(1.04);
  box-shadow:0 24px 48px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.18);
}
.discovery-orb.is-active .discovery-orb-art{
  animation:none;
  translate:0 0;
  scale:1;
  transform:translateY(-8px) scale(1.24);
  border-color:rgba(255,255,255,.10);
  box-shadow:0 34px 76px rgba(0,0,0,.32), inset 0 1px 0 rgba(255,255,255,.18);
  filter:saturate(1.08) contrast(1.03);
}
.discovery-orb-art img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
}
.discovery-orb-play{
  position:absolute;
  inset:0;
  display:grid;
  place-items:center;
  opacity:0;
  color:rgba(255,255,255,.88);
  background:radial-gradient(circle, rgba(0,0,0,.22), rgba(0,0,0,.02) 58%, transparent 72%);
  transition:opacity .22s ease;
}
.discovery-orb-play::before{
  content:"";
  position:absolute;
  width:74px;
  height:74px;
  border-radius:999px;
  border:1px solid rgba(255,255,255,.34);
  opacity:0;
  transform:scale(.62);
}
.discovery-orb-play .ui-ic{
  width:42px;
  height:42px;
  filter:drop-shadow(0 8px 20px rgba(0,0,0,.36));
  transition:transform .18s ease, filter .18s ease;
}
.discovery-orb.is-active .discovery-orb-play{
  opacity:.82;
}
.discovery-orb.is-launching .discovery-orb-art{
  filter:saturate(1.14) contrast(1.06) brightness(1.08);
}
.discovery-orb.is-launching .discovery-orb-play{
  opacity:1;
}
.discovery-orb.is-launching .discovery-orb-play::before{
  animation:discoveryPlayConfirm .42s ease-out;
}
.discovery-orb.is-launching .discovery-orb-play .ui-ic{
  transform:scale(1.18);
  filter:drop-shadow(0 10px 24px rgba(0,0,0,.44));
}
.discovery-orb.library-action-feedback .discovery-orb-art{
  transform:translateY(-3px) scale(1.07);
  border-color:color-mix(in srgb, var(--ma-accent) 28%, rgba(255,255,255,.12));
  box-shadow:0 26px 56px rgba(0,0,0,.3), 0 0 30px color-mix(in srgb, var(--ma-accent) 18%, transparent), inset 0 1px 0 rgba(255,255,255,.18);
}
.discovery-orb.is-active.library-action-feedback .discovery-orb-art{
  transform:translateY(-8px) scale(1.27);
}
.discovery-orb.library-action-loading .discovery-orb-play{
  opacity:1;
}
.discovery-orb.library-action-loading .discovery-orb-play::before{
  opacity:.96;
  transform:scale(.78);
  border-color:rgba(255,255,255,.34);
  border-inline-end-color:var(--ma-accent);
  animation:homeiiLoadingSpin .74s linear infinite;
}
.discovery-orb.library-action-loading .discovery-orb-play .ui-ic{
  opacity:.42;
  transform:scale(.94);
}
.discovery-orb-copy{
  display:grid;
  gap:3px;
  min-width:0;
  padding:0 4px;
  transition:transform .24s ease, opacity .24s ease, background .24s ease, border-color .24s ease, padding .24s ease, box-shadow .24s ease;
}
.discovery-orb-title,
.discovery-orb-sub{
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.discovery-orb-title{
  font-size:13px;
  font-weight:950;
  color:rgba(255,255,255,.54);
}
.discovery-orb-sub{
  color:rgba(255,255,255,.38);
  font-size:11px;
  font-weight:700;
  opacity:0;
  max-height:0;
  transition:opacity .2s ease, max-height .2s ease, color .2s ease;
}
.discovery-orb.is-active .discovery-orb-copy{
  transform:translateY(10px);
  padding:8px 2px 0;
  border-radius:0;
  background:transparent;
  border:0;
  box-shadow:none;
  backdrop-filter:none;
}
.discovery-orb.is-active .discovery-orb-title{
  color:var(--ma-text-1);
  font-size:14px;
}
.discovery-orb.is-active .discovery-orb-sub{
  color:var(--ma-text-2);
  opacity:1;
  max-height:34px;
}
.theme-light .discovery-orb-title{
  color:rgba(15,23,42,.58);
}
.theme-light .discovery-orb-sub{
  color:rgba(15,23,42,.42);
}
.theme-light .discovery-orb.is-active .discovery-orb-copy{
  background:transparent;
  box-shadow:none;
}
.theme-light .discovery-orb-field.has-active .discovery-orb:not(.is-active) .discovery-orb-art{
  filter:blur(.45px) saturate(.72) brightness(.92);
}
@keyframes discoveryOrbBreath{
  0%,100%{scale:.97;opacity:.88;}
  50%{scale:1.045;opacity:1;}
}
@keyframes discoveryPlayConfirm{
  0%{opacity:.72;transform:scale(.58);}
  100%{opacity:0;transform:scale(1.28);}
}
@media (prefers-reduced-motion: reduce){
  .discovery-orb-art{
    animation:none;
  }
  .discovery-orb.library-action-loading .discovery-orb-play::before{
    animation:none;
  }
}
@media (max-width: 760px){
  .menu-backdrop.discovery-open .menu-head{min-height:58px;}
  .discovery-hero{
    min-height:auto;
    grid-template-columns:1fr;
    align-items:start;
    padding:2px 2px 0;
  }
  .discovery-hero-title{font-size:clamp(32px, 10vw, 44px);}
  .discovery-player-focus{
    width:100%;
    max-width:none;
    grid-template-columns:36px minmax(0,1fr) auto;
  }
  .discovery-player-kicker{
    grid-column:1 / -1;
    font-size:10px;
  }
  .discovery-player-art{
    width:36px;
    height:36px;
    border-radius:11px;
  }
  .discovery-filter-rail{padding-block-start:0;}
  .discovery-category-chip{min-width:128px;}
  .discovery-orb-field{
    grid-template-columns:repeat(2, minmax(0, 1fr));
    gap:32px 10px;
    padding-bottom:70px;
  }
  .discovery-orb,
  .rtl .discovery-orb{
    --orb-size:clamp(98px, 29vw, 126px);
    margin-block-start:0;
    transform:none;
  }
  .discovery-orb.is-active .discovery-orb-art{
    transform:translateY(-4px) scale(1.08);
  }
}
@media (max-width: 420px), (max-height: 560px) and (max-width: 900px){
  .menu-body.sheet-discovery{
    padding-inline:10px!important;
  }
  .discovery-shell{
    gap:16px;
  }
  .discovery-orb-field{
    grid-template-columns:repeat(2, minmax(0, 1fr))!important;
    gap:26px 8px!important;
    padding-inline:0!important;
  }
  .discovery-orb,
  .rtl .discovery-orb{
    --orb-size:clamp(86px, 26vw, 112px)!important;
    width:min(var(--orb-size), 100%)!important;
    transform:none!important;
  }
  .discovery-orb-copy{
    padding-inline:1px!important;
  }
  .discovery-orb.is-active .discovery-orb-art{
    transform:translateY(-3px) scale(1.06)!important;
  }
}
.power-fab{
  color:#ffd9d9!important;
  background:linear-gradient(145deg, rgba(255,85,95,.22), rgba(255,85,95,.1))!important;
  border-color:rgba(255,105,115,.24)!important;
}
.danger-fab{
  color:#ffd9d9!important;
  background:linear-gradient(145deg, rgba(255,85,95,.22), rgba(255,85,95,.1))!important;
  border-color:rgba(255,105,115,.24)!important;
}
.theme-light .power-fab{
  color:#b4232b!important;
  background:#fff1f2!important;
  border-color:rgba(180,35,43,.16)!important;
}
.theme-light .danger-fab{
  color:#b4232b!important;
  background:#fff1f2!important;
  border-color:rgba(180,35,43,.16)!important;
}
.card.screensaver-active .stage{
  pointer-events:none;
}
.card.screensaver-active .bg,
.card.screensaver-active .shade,
.card.screensaver-active .glow{
  animation-play-state:paused;
}

.screensaver-backdrop{
  position:absolute;
  inset:0;
  z-index:85;
  display:grid;
  place-items:center;
  padding:28px;
  background:#02060d;
  opacity:0;
  pointer-events:none;
  transition:opacity .46s cubic-bezier(.22,.78,.24,1);
  overflow:hidden;
  isolation:isolate;
  contain:layout paint style;
  will-change:opacity;
  transform:translateZ(0);
}
.screensaver-backdrop.open{
  opacity:1;
  pointer-events:auto;
}
.screensaver-backdrop.closing{
  opacity:0;
  pointer-events:none;
  transition-duration:.42s;
}
.screensaver-backdrop .screensaver-shell,
.screensaver-backdrop .screensaver-action-cluster,
.screensaver-backdrop .screensaver-brand{
  opacity:0;
  transform:translate3d(0, 10px, 0);
  transition:opacity .5s cubic-bezier(.22,.78,.24,1), transform .5s cubic-bezier(.22,.78,.24,1), filter .5s ease;
  will-change:opacity, transform;
}
.screensaver-backdrop.open .screensaver-shell,
.screensaver-backdrop.open .screensaver-action-cluster{
  opacity:1;
  transform:translate3d(0, 0, 0);
}
.screensaver-backdrop.open .screensaver-brand{
  opacity:.74;
  transform:translate3d(0, 0, 0);
}
.screensaver-backdrop.closing .screensaver-shell,
.screensaver-backdrop.closing .screensaver-action-cluster,
.screensaver-backdrop.closing .screensaver-brand{
  opacity:0;
  transform:translate3d(0, -6px, 0);
  transition-duration:.34s;
}
.screensaver-action-cluster{
  position:absolute;
  inset-inline-end:clamp(22px, 3vw, 48px);
  inset-block-end:clamp(20px, 2.8vh, 42px);
  z-index:5;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  flex-wrap:wrap;
  gap:10px;
  max-width:min(72vw, 560px);
}
.screensaver-voice-btn{
  position:relative;
  width:clamp(56px, 5.8vw, 72px);
  height:clamp(56px, 5.8vw, 72px);
  border-radius:999px;
  border:1px solid rgba(255,255,255,.115);
  background:rgba(255,255,255,.045);
  color:rgba(255,255,255,.7);
  display:grid;
  place-items:center;
  cursor:pointer;
  box-shadow:0 16px 34px rgba(0,0,0,.18), inset 0 1px 0 rgba(255,255,255,.075);
  backdrop-filter:blur(24px) saturate(108%);
  -webkit-backdrop-filter:blur(24px) saturate(108%);
  opacity:.48;
  filter:saturate(.9);
  transition:opacity .18s ease, transform .18s ease, background .18s ease, border-color .18s ease, color .18s ease, box-shadow .18s ease, filter .18s ease;
}
.screensaver-control-btn{
  width:clamp(48px, 4.8vw, 62px);
  height:clamp(48px, 4.8vw, 62px);
  opacity:.34;
}
.screensaver-control-btn.primary{
  opacity:.48;
}
.screensaver-voice-btn .ui-ic{
  width:32%;
  height:32%;
}
.screensaver-voice-btn:hover,
.screensaver-voice-btn:focus-visible,
.screensaver-voice-btn.active,
.screensaver-voice-btn.listening{
  opacity:.88;
  filter:saturate(1);
  color:#fff;
  border-color:color-mix(in srgb, var(--ma-accent) 42%, rgba(255,255,255,.18));
  background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 18%, rgba(255,255,255,.12)), rgba(255,255,255,.075));
  box-shadow:0 20px 46px color-mix(in srgb, var(--ma-accent) 14%, rgba(0,0,0,.26)), 0 0 0 8px color-mix(in srgb, var(--ma-accent) 8%, transparent), inset 0 1px 0 rgba(255,255,255,.16);
}
.screensaver-voice-btn:active,
.screensaver-voice-btn.pressed{
  transform:scale(.96);
}
.screensaver-voice-btn:disabled{
  cursor:default;
  opacity:.22;
  transform:none;
}
.screensaver-voice-btn.listening::after{
  content:"";
  position:absolute;
  inset:-9px;
  border-radius:inherit;
  border:1px solid color-mix(in srgb, var(--ma-accent) 42%, transparent);
  animation:voiceAssistantListenPulse 1.1s ease-out infinite;
}
.screensaver-backdrop.open .screensaver-art{
  animation:screensaverArtGuard 127s ease-in-out infinite;
}
.screensaver-backdrop.open .screensaver-track,
.screensaver-backdrop.open .screensaver-message,
.screensaver-backdrop.open .screensaver-next{
  animation:screensaverTextGuard 149s ease-in-out infinite;
}
.screensaver-backdrop.open .screensaver-brand{
  animation:screensaverBrandGuard 181s ease-in-out infinite;
}
.screensaver-bg{
  position:absolute;
  inset:-34px;
  background:
    linear-gradient(180deg, rgba(3,7,13,.46), rgba(3,7,13,.88)),
    var(--screensaver-art-url, none) center/cover no-repeat;
  filter:blur(22px) saturate(1.18);
  transform:scale(1.08);
  animation:screensaverBreath 16s ease-in-out infinite;
  z-index:-1;
}
.screensaver-backdrop.empty-mode{
  background:
    radial-gradient(circle at 18% 14%, rgba(var(--dynamic-accent-rgb,245 166 35) / .18), transparent 34%),
    radial-gradient(circle at 82% 22%, rgba(83,141,255,.16), transparent 30%),
    linear-gradient(135deg, #02060d, #070a12 52%, #02050a);
}
.screensaver-backdrop.empty-mode::before,
.screensaver-backdrop.empty-mode::after{
  content:"";
  position:absolute;
  inset:-18%;
  z-index:0;
  pointer-events:none;
  mix-blend-mode:screen;
  filter:blur(2px) saturate(1.2);
  will-change:transform, opacity, filter;
}
.screensaver-backdrop.empty-mode::before{
  background:
    radial-gradient(circle at 18% 74%, rgba(var(--dynamic-accent-rgb,245 166 35) / .28), transparent 14%),
    radial-gradient(circle at 76% 22%, rgba(96,165,250,.22), transparent 12%),
    radial-gradient(circle at 58% 86%, rgba(244,114,182,.16), transparent 12%),
    radial-gradient(circle at 34% 28%, rgba(255,255,255,.12), transparent 10%);
  opacity:.5;
  animation:screensaverLightFieldOne 26s ease-in-out infinite;
}
.screensaver-backdrop.empty-mode::after{
  inset:-24%;
  background:
    radial-gradient(circle at 84% 70%, rgba(45,212,191,.18), transparent 15%),
    radial-gradient(circle at 10% 18%, rgba(250,204,21,.16), transparent 11%),
    radial-gradient(circle at 52% 48%, rgba(147,197,253,.14), transparent 18%);
  opacity:.38;
  animation:screensaverLightFieldTwo 34s ease-in-out infinite;
}
.screensaver-backdrop.empty-mode .screensaver-bg{
  background:
    radial-gradient(circle at 18% 24%, rgba(var(--dynamic-accent-rgb,245 166 35) / .34), transparent 30%),
    radial-gradient(circle at 72% 38%, rgba(91,141,255,.22), transparent 34%),
    radial-gradient(circle at 48% 78%, rgba(255,255,255,.08), transparent 28%),
    linear-gradient(135deg, #050914, #080b10);
  filter:blur(30px) saturate(1.18);
  opacity:.92;
  animation:screensaverEmptyDrift 18s ease-in-out infinite;
}
.screensaver-backdrop.empty-mode .screensaver-shell,
.screensaver-backdrop.empty-mode .screensaver-brand{
  z-index:2;
}
.screensaver-backdrop.empty-mode.open .screensaver-clock{
  animation:screensaverClockGlow 5.8s ease-in-out infinite;
}
.screensaver-backdrop.empty-mode.open .screensaver-analog-clock{
  animation:screensaverClockRingPulse 5.8s ease-in-out infinite;
}
.screensaver-brand{
  display:none;
  position:absolute;
  left:clamp(34px, 4.8vw, 76px);
  bottom:clamp(26px, 4.4vh, 54px);
  width:clamp(94px, 9vw, 148px);
  color:rgba(255,255,255,.34);
  opacity:.74;
  filter:drop-shadow(0 12px 28px rgba(0,0,0,.34)) saturate(.9);
  pointer-events:none;
}
.screensaver-shell{
  position:relative;
  z-index:1;
  width:min(100%, 760px);
  display:grid;
  grid-template-columns:minmax(150px, 260px) minmax(0, 1fr);
  gap:28px;
  align-items:end;
}
.screensaver-art-wrap{
  position:relative;
  width:100%;
  min-width:0;
}
.screensaver-art{
  aspect-ratio:1;
  width:100%;
  border-radius:30px;
  overflow:hidden;
  background:rgba(255,255,255,.1);
  border:1px solid rgba(255,255,255,.16);
  box-shadow:0 28px 70px rgba(0,0,0,.36);
}
.screensaver-art img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
  opacity:.9;
  animation:screensaverImageIn .32s ease-out;
}
.screensaver-like-btn{
  position:absolute;
  inset-inline-end:clamp(10px, 4%, 18px);
  inset-block-end:clamp(10px, 4%, 18px);
  z-index:4;
  opacity:.72;
  width:clamp(48px, 5vw, 62px);
  height:clamp(48px, 5vw, 62px);
  color:rgba(255,255,255,.82);
  background:rgba(9,12,18,.38);
}
.screensaver-like-btn.active{
  color:#f5a623;
  opacity:.96;
  border-color:rgba(245,166,35,.34);
  box-shadow:0 18px 38px rgba(0,0,0,.28), 0 0 0 8px rgba(245,166,35,.08), inset 0 1px 0 rgba(255,255,255,.12);
}
.screensaver-like-btn[hidden]{
  display:none!important;
}
.screensaver-backdrop.empty-mode .screensaver-art{
  display:grid;
  place-items:center;
  background:
    radial-gradient(circle at 50% 42%, rgba(255,255,255,.08), transparent 58%),
    linear-gradient(145deg, rgba(255,255,255,.10), rgba(255,255,255,.035));
  border-color:rgba(255,255,255,.14);
}
.screensaver-empty-logo{
  width:68%;
  max-width:260px;
  height:auto;
  color:rgba(255,255,255,.58);
  opacity:.88;
  filter:drop-shadow(0 18px 34px rgba(0,0,0,.34));
}
.screensaver-backdrop.empty-mode .screensaver-artist:empty{
  display:none;
}
.screensaver-info{
  min-width:0;
  color:#fff;
  text-shadow:0 10px 36px rgba(0,0,0,.48);
  align-self:end;
}
.screensaver-lyrics{
  display:none;
  min-width:0;
  max-width:100%;
  color:#fff;
  text-align:start;
  text-shadow:0 16px 42px rgba(0,0,0,.54);
}
.screensaver-lyric-line{
  max-width:100%;
  font-size:clamp(24px, 4.2vw, 58px);
  line-height:1.14;
  font-weight:950;
  letter-spacing:0;
  opacity:.42;
  transform:translate3d(0, 0, 0);
  transition:opacity .24s ease, transform .24s ease, color .24s ease;
}
.screensaver-lyric-line.current{
  opacity:.96;
  color:#fff;
  transform:scale(1.015);
}
.screensaver-backdrop.lyrics-mode .screensaver-clock,
.screensaver-backdrop.lyrics-mode .screensaver-analog-clock,
.screensaver-backdrop.lyrics-mode .screensaver-next,
.screensaver-backdrop.lyrics-mode .screensaver-message{
  display:none!important;
}
.screensaver-backdrop.lyrics-mode .screensaver-shell{
  align-items:center;
}
.screensaver-backdrop.lyrics-mode .screensaver-info{
  align-self:center;
  display:grid;
  gap:clamp(18px, 2.8vh, 30px);
  max-width:min(58vw, 760px);
  padding-block-end:0;
}
.screensaver-backdrop.lyrics-mode .screensaver-track{
  margin-top:0;
  gap:clamp(5px, .8vh, 9px);
}
.screensaver-backdrop.lyrics-mode .screensaver-title{
  font-size:clamp(24px, 2.8vw, 42px);
  white-space:normal;
  overflow:visible;
  text-overflow:clip;
}
.screensaver-backdrop.lyrics-mode .screensaver-artist{
  font-size:clamp(14px, 1.5vw, 20px);
  white-space:normal;
  overflow:visible;
  text-overflow:clip;
}
.screensaver-backdrop.lyrics-mode .screensaver-lyrics{
  display:grid;
  gap:clamp(10px, 1.8vh, 18px);
  max-width:100%;
}
.screensaver-clock{
  font-size:96px;
  line-height:.9;
  font-weight:950;
  font-variant-numeric:tabular-nums;
}
.screensaver-track{
  margin-top:28px;
  display:grid;
  gap:5px;
}
.screensaver-title{
  font-size:24px;
  line-height:1.1;
  font-weight:950;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.screensaver-artist{
  color:rgba(255,255,255,.72);
  font-size:15px;
  font-weight:750;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.screensaver-message{
  margin-top:22px;
  max-width:30rem;
  padding:13px 16px;
  border-radius:18px;
  background:rgba(255,255,255,.12);
  border:1px solid rgba(255,255,255,.16);
  color:rgba(255,255,255,.9);
  font-size:15px;
  font-weight:800;
  line-height:1.35;
}
.screensaver-next{
  margin-top:18px;
  width:min(100%, 24rem);
  padding:0;
  border-radius:0;
  background:transparent;
  border:0;
  box-shadow:none;
  backdrop-filter:none;
  text-shadow:0 10px 28px rgba(0,0,0,.42);
}
.screensaver-next[hidden]{display:none!important;}
.screensaver-next-label{
  display:block;
  margin:0 0 7px;
  color:rgba(255,255,255,.54);
  font-size:10px;
  font-weight:950;
  letter-spacing:0;
  text-transform:uppercase;
}
.screensaver-next-main{
  display:grid;
  grid-template-columns:40px minmax(0, 1fr);
  gap:10px;
  align-items:center;
}
.screensaver-next-art{
  width:40px;
  aspect-ratio:1;
  border-radius:12px;
  overflow:hidden;
  display:grid;
  place-items:center;
  color:rgba(255,255,255,.82);
  opacity:.86;
  background:transparent;
  border:0;
}
.screensaver-next-art img{
  width:100%;
  height:100%;
  object-fit:cover;
  display:block;
  animation:screensaverImageIn .28s ease-out;
}
.screensaver-next-art svg{
  width:22px;
  height:22px;
}
.screensaver-next-copy{
  min-width:0;
  display:grid;
  gap:3px;
}
.screensaver-next-title,
.screensaver-next-artist{
  display:block;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}
.screensaver-next-title{
  color:#fff;
  font-size:14px;
  font-weight:950;
}
.screensaver-next-artist{
  color:rgba(255,255,255,.66);
  font-size:12px;
  font-weight:800;
}
.screensaver-analog-clock{
  width:190px;
  aspect-ratio:1;
  border-radius:999px;
  display:none;
  position:relative;
  border:2px solid rgba(255,255,255,.32);
  background:radial-gradient(circle, rgba(255,255,255,.16), rgba(255,255,255,.05));
  box-shadow:inset 0 0 0 10px rgba(255,255,255,.05), 0 24px 60px rgba(0,0,0,.28);
}
.screensaver-hand{
  position:absolute;
  left:50%;
  bottom:50%;
  width:4px;
  border-radius:999px;
  transform:translateX(-50%) rotate(var(--hand-rotation, 0deg));
  transform-origin:50% 100%;
  background:#fff;
}
.screensaver-hand.hour{height:30%;}
.screensaver-hand.minute{height:40%;opacity:.9;}
.screensaver-hand.second{height:43%;width:2px;background:var(--ma-accent);}
.screensaver-pin{
  position:absolute;
  width:12px;
  height:12px;
  border-radius:999px;
  left:50%;
  top:50%;
  transform:translate(-50%, -50%);
  background:#fff;
  box-shadow:0 0 0 4px rgba(255,255,255,.18);
}
.screensaver-backdrop.analog-mode .screensaver-clock{display:none;}
.screensaver-backdrop.analog-mode .screensaver-analog-clock{display:block;}
.card.layout-tablet .screensaver-backdrop{
  padding:clamp(44px, 5.4vw, 76px);
}
.card.layout-tablet .screensaver-brand{
  display:block;
}
.card.layout-tablet .screensaver-shell{
  width:min(92vw, 1180px);
  grid-template-columns:minmax(320px, 430px) minmax(0, 1fr);
  gap:clamp(48px, 6.2vw, 86px);
}
.card.layout-tablet .screensaver-art{
  border-radius:36px;
  box-shadow:0 34px 90px rgba(0,0,0,.4);
}
.card.layout-tablet .screensaver-info{
  max-width:610px;
}
.card.layout-tablet .screensaver-clock{
  font-size:clamp(94px, 11.6vw, 190px);
  line-height:.84;
}
.card.layout-tablet .screensaver-analog-clock{
  width:clamp(236px, 27vw, 380px);
}
.card.layout-tablet .screensaver-track{
  margin-top:22px;
  gap:7px;
}
.card.layout-tablet .screensaver-title{
  font-size:clamp(30px, 3vw, 42px);
}
.card.layout-tablet .screensaver-artist{
  font-size:19px;
}
.card.layout-tablet .screensaver-message,
.card.layout-tablet .screensaver-next{
  max-width:clamp(310px, 31vw, 430px);
}
.card.layout-tablet .screensaver-next{
  position:static;
  width:min(100%, 390px);
  margin-top:clamp(18px, 2.2vh, 26px);
  padding:0;
  border-radius:0;
  background:transparent;
  border:0;
  box-shadow:none;
  backdrop-filter:none;
  text-shadow:0 10px 28px rgba(0,0,0,.46);
}
.card.layout-tablet .screensaver-next-main{
  grid-template-columns:40px minmax(0, 1fr);
  gap:10px;
}
.card.layout-tablet .screensaver-next-art{
  width:40px;
  border-radius:12px;
  opacity:.84;
  background:transparent;
  border:0;
}
.card.layout-tablet .screensaver-next-title{
  font-size:15px;
}
.card.layout-tablet .screensaver-next-artist{
  font-size:12px;
}
.card.layout-tablet .screensaver-backdrop.lyrics-mode .screensaver-shell{
  grid-template-columns:minmax(320px, 430px) minmax(0, 720px);
  gap:clamp(54px, 6.6vw, 94px);
}
@media (max-width: 720px), (max-height: 540px){
  .screensaver-backdrop.lyrics-mode .screensaver-shell{
    width:min(100%, 520px);
    grid-template-columns:1fr;
    gap:clamp(18px, 4vh, 28px);
    justify-items:center;
    text-align:center;
  }
  .screensaver-backdrop.lyrics-mode .screensaver-art-wrap{
    width:min(48vw, 220px);
    max-width:220px;
  }
  .screensaver-backdrop.lyrics-mode .screensaver-info{
    max-width:min(100%, 520px);
    justify-items:center;
    text-align:center;
  }
  .screensaver-backdrop.lyrics-mode .screensaver-track,
  .screensaver-backdrop.lyrics-mode .screensaver-lyrics{
    width:100%;
    max-width:min(100%, 520px);
    text-align:center;
  }
  .screensaver-backdrop.lyrics-mode .screensaver-title{
    font-size:clamp(22px, 6.2vw, 34px);
  }
  .screensaver-backdrop.lyrics-mode .screensaver-artist{
    font-size:clamp(13px, 3.6vw, 17px);
  }
  .screensaver-lyric-line{
    font-size:clamp(24px, 7.2vw, 38px);
    line-height:1.18;
  }
}
@media (min-width: 760px) and (min-height: 620px){
  .screensaver-backdrop{
    padding:clamp(44px, 5.4vw, 76px);
  }
  .screensaver-brand{
    display:block;
  }
  .screensaver-shell{
    width:min(92vw, 1180px);
    grid-template-columns:minmax(320px, 430px) minmax(0, 1fr);
    gap:clamp(48px, 6.2vw, 86px);
    align-items:end;
  }
  .screensaver-art{
    border-radius:36px;
    box-shadow:0 34px 90px rgba(0,0,0,.4);
  }
  .screensaver-info{
    max-width:560px;
    align-self:end;
    padding-block-end:clamp(6px, 1.2vw, 18px);
  }
  .screensaver-clock,
  .screensaver-analog-clock{
    position:absolute;
    top:var(--screensaver-clock-y, 24%);
    left:var(--screensaver-clock-x, 82%);
    right:auto;
    z-index:2;
    transform:translate(-50%, -50%) scale(var(--screensaver-clock-scale, 1));
    transform-origin:center;
  }
  .screensaver-backdrop.open .screensaver-clock,
  .screensaver-backdrop.open .screensaver-analog-clock{
    animation:screensaverClockGuard 137s ease-in-out infinite;
  }
  .screensaver-backdrop.empty-mode.open .screensaver-clock{
    animation:screensaverClockGuard 137s ease-in-out infinite, screensaverClockGlow 5.8s ease-in-out infinite;
  }
  .screensaver-backdrop.empty-mode.open .screensaver-analog-clock{
    animation:screensaverClockGuard 137s ease-in-out infinite, screensaverClockRingPulse 5.8s ease-in-out infinite;
  }
  .screensaver-clock{
    font-size:clamp(78px, 7.4vw, 128px);
    line-height:.9;
    text-align:center;
    white-space:nowrap;
  }
  .screensaver-analog-clock{
    width:clamp(204px, 20.4vw, 292px);
  }
  .screensaver-track{
    margin-top:18px;
    gap:7px;
  }
  .screensaver-title{
    font-size:clamp(30px, 3vw, 44px);
  }
  .screensaver-artist{
    font-size:19px;
  }
  .screensaver-message,
  .screensaver-next{
    max-width:clamp(310px, 31vw, 430px);
  }
  .screensaver-next{
    position:static;
    width:min(100%, 390px);
    margin-top:clamp(18px, 2.2vh, 26px);
    padding:0;
    border-radius:0;
    background:transparent;
    border:0;
    box-shadow:none;
    backdrop-filter:none;
    text-shadow:0 10px 28px rgba(0,0,0,.46);
  }
  .screensaver-next-main{
    grid-template-columns:40px minmax(0, 1fr);
    gap:10px;
  }
  .screensaver-next-art{
    width:40px;
    border-radius:12px;
    opacity:.84;
    background:transparent;
    border:0;
  }
  .screensaver-next-title{
    font-size:15px;
  }
  .screensaver-next-artist{
    font-size:12px;
  }
}
.theme-light .screensaver-brand{
  color:rgba(20,26,38,.34);
  filter:drop-shadow(0 12px 28px rgba(255,255,255,.22)) saturate(.86);
}
.theme-light .screensaver-voice-btn{
  color:rgba(31,38,51,.56);
  background:rgba(255,255,255,.28);
  border-color:rgba(141,155,177,.18);
  box-shadow:0 16px 34px rgba(111,126,150,.1), inset 0 1px 0 rgba(255,255,255,.45);
}
.theme-light .screensaver-voice-btn:hover,
.theme-light .screensaver-voice-btn:focus-visible,
.theme-light .screensaver-voice-btn.listening{
  color:#5f420f;
  background:linear-gradient(180deg, color-mix(in srgb, var(--ma-accent) 14%, white 86%), rgba(255,255,255,.62));
  border-color:color-mix(in srgb, var(--ma-accent) 36%, rgba(141,155,177,.2));
  box-shadow:0 20px 44px color-mix(in srgb, var(--ma-accent) 12%, rgba(111,126,150,.14)), 0 0 0 8px color-mix(in srgb, var(--ma-accent) 7%, transparent), inset 0 1px 0 rgba(255,255,255,.72);
}
.theme-light .screensaver-backdrop.empty-mode{
  background:
    radial-gradient(circle at 18% 14%, rgba(var(--dynamic-accent-rgb,245 166 35) / .16), transparent 34%),
    radial-gradient(circle at 82% 22%, rgba(80,127,220,.14), transparent 30%),
    linear-gradient(135deg, #f4f7fb, #e7edf6 52%, #f8fafc);
}
.theme-light .screensaver-backdrop.empty-mode .screensaver-bg{
  background:
    radial-gradient(circle at 18% 24%, rgba(var(--dynamic-accent-rgb,245 166 35) / .24), transparent 30%),
    radial-gradient(circle at 72% 38%, rgba(80,127,220,.18), transparent 34%),
    radial-gradient(circle at 48% 78%, rgba(25,34,48,.08), transparent 28%),
    linear-gradient(135deg, #f8fafc, #e7edf6);
}
.theme-light .screensaver-empty-logo{
  color:rgba(20,26,38,.52);
  filter:drop-shadow(0 18px 34px rgba(255,255,255,.24));
}
#mobileMenu{
  --detail-accent-rgb:var(--dynamic-accent-rgb,245 166 35);
  --detail-surface-rgb:var(--dynamic-surface-rgb,24 30 44);
  --detail-glow-rgb:var(--dynamic-glow-rgb,96 165 250);
}
#mobileMenu .menu-head{
  grid-template-columns:52px minmax(0,1fr) 52px;
}
.card.layout-tablet #mobileMenu .menu-head{
  grid-template-columns:48px minmax(0,1fr) 48px;
}
#mobileMenuBackBtn{
  grid-column:1;
  grid-row:1;
  justify-self:start;
}
#mobileMenuTitle{
  grid-column:2;
  grid-row:1;
}
#mobileMenuCloseBtn{
  grid-column:3;
  grid-row:1;
  justify-self:end;
}
#mobileMenuAuxBtn{
  position:absolute;
  inset-block-start:18px;
  inset-inline-end:68px;
}
#mobileMenuAuxBtn[hidden]{
  display:none!important;
}
#mobileMenu.has-menu-detail-theme{
  --detail-accent-rgb:var(--menu-detail-accent-rgb, var(--dynamic-accent-rgb,245 166 35));
  --detail-surface-rgb:var(--menu-detail-surface-rgb, var(--dynamic-surface-rgb,24 30 44));
  --detail-glow-rgb:var(--menu-detail-glow-rgb, var(--dynamic-glow-rgb,96 165 250));
}
#mobileMenu.has-menu-detail-theme::before{
  opacity:.68;
  filter:blur(46px) saturate(1.18) brightness(.82);
}
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-media-detail,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-artist-detail,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-library{
  background:
    radial-gradient(circle at 18% 0%, rgba(var(--detail-accent-rgb) / .26), transparent 36%),
    radial-gradient(circle at 86% 18%, rgba(var(--detail-glow-rgb) / .18), transparent 34%),
    linear-gradient(180deg, rgba(var(--detail-surface-rgb) / .24), rgba(10,12,18,.96))!important;
  border-color:rgba(var(--detail-accent-rgb) / .24)!important;
}
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-media-detail::before,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-artist-detail::before,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-library::before{
  opacity:.3;
  filter:blur(34px) saturate(1.16) brightness(.78);
}
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-media-detail::after,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-artist-detail::after,
#mobileMenu.has-menu-detail-theme .menu-sheet.sheet-library::after{
  background:
    linear-gradient(180deg, rgba(8,10,16,.66), rgba(10,12,19,.9) 46%, rgba(9,11,17,.96)),
    radial-gradient(circle at 22% 12%, rgba(var(--detail-accent-rgb) / .12), transparent 38%);
}
#mobileMenu.has-menu-detail-theme .menu-head{
  overflow:hidden;
  isolation:isolate;
  min-height:74px;
  background:
    linear-gradient(90deg, rgba(var(--detail-surface-rgb) / .34), rgba(11,13,20,.5)),
    radial-gradient(circle at 50% 0%, rgba(var(--detail-accent-rgb) / .18), transparent 46%);
  backdrop-filter:blur(18px);
  -webkit-backdrop-filter:blur(18px);
  border-bottom-color:rgba(var(--detail-accent-rgb) / .2);
}
#mobileMenu.has-menu-detail-theme .menu-head::before{
  content:"";
  position:absolute;
  inset:-30px;
  background:var(--menu-dynamic-art, none) center 34%/cover no-repeat;
  filter:blur(26px) saturate(1.16) brightness(.72);
  transform:scale(1.12);
  opacity:.36;
  z-index:0;
  pointer-events:none;
}
#mobileMenu.has-menu-detail-theme .menu-head::after{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(90deg, rgba(8,10,16,.78), rgba(10,12,18,.48) 48%, rgba(8,10,16,.78)),
    radial-gradient(circle at 50% 0%, rgba(var(--detail-accent-rgb) / .18), transparent 58%);
  z-index:0;
  pointer-events:none;
}
#mobileMenu.has-menu-detail-theme .menu-head>*{
  position:relative;
  z-index:1;
}
#mobileMenu.has-menu-detail-theme .menu-title-icon,
#mobileMenu.has-menu-detail-theme .media-detail-kicker,
#mobileMenu.has-menu-detail-theme .artist-year-title{
  color:rgb(var(--detail-accent-rgb));
  filter:drop-shadow(0 0 14px rgba(var(--detail-accent-rgb) / .18));
}
#mobileMenu .media-detail-hero,
#mobileMenu .artist-detail-hero{
  position:relative;
  overflow:hidden;
  isolation:isolate;
  border:1px solid rgba(var(--detail-accent-rgb) / .22)!important;
  background:
    linear-gradient(135deg, rgba(var(--detail-surface-rgb) / .34), rgba(255,255,255,.055)),
    radial-gradient(circle at 16% 18%, rgba(var(--detail-accent-rgb) / .16), transparent 42%)!important;
  box-shadow:0 22px 54px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.09)!important;
}
#mobileMenu .media-detail-hero::before,
#mobileMenu .artist-detail-hero::before{
  content:"";
  position:absolute;
  inset:-34px;
  background:var(--detail-hero-art, var(--menu-dynamic-art, none)) center/cover no-repeat;
  filter:blur(30px) saturate(1.18) brightness(.78);
  transform:scale(1.12);
  opacity:.42;
  pointer-events:none;
  z-index:0;
}
#mobileMenu .media-detail-hero::after,
#mobileMenu .artist-detail-hero::after{
  content:"";
  position:absolute;
  inset:0;
  background:
    linear-gradient(90deg, rgba(10,12,18,.78), rgba(16,19,29,.5) 58%, rgba(10,12,18,.76)),
    radial-gradient(circle at 18% 12%, rgba(var(--detail-accent-rgb) / .18), transparent 40%);
  pointer-events:none;
  z-index:0;
}
#mobileMenu .media-detail-hero>*,
#mobileMenu .artist-detail-hero>*{
  position:relative;
  z-index:1;
}
#mobileMenu .album-detail-player{
  grid-template-columns:minmax(136px, clamp(150px, 20cqi, 238px)) minmax(0,1fr);
  grid-template-areas:"art copy";
  align-items:center;
  gap:clamp(16px, 3cqi, 28px);
  min-height:clamp(178px, 25cqi, 270px);
  padding:clamp(16px, 2.6cqi, 26px);
}
#mobileMenu .media-detail-art-stage{
  grid-area:art;
  position:relative;
  width:100%;
  max-width:238px;
  aspect-ratio:1/1;
  display:grid;
  place-items:center;
  justify-self:center;
  border-radius:26px;
}
#mobileMenu .media-detail-art{
  width:100%;
  height:100%;
  border-radius:26px;
  box-shadow:0 24px 58px rgba(0,0,0,.34), 0 0 0 1px rgba(255,255,255,.12) inset;
}
#mobileMenu .media-detail-art img{
  object-fit:contain;
}
#mobileMenu .media-detail-cover-actions{
  position:absolute;
  inset:auto 12px 12px auto;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:0;
  padding:0;
  border:0;
  background:transparent;
  box-shadow:none;
  pointer-events:none;
}
#mobileMenu .media-detail-cover-actions .media-detail-play-btn{
  width:40px;
  min-width:40px;
  height:40px;
  border-radius:999px;
  background:rgba(10,12,18,.48)!important;
  border:1px solid rgba(255,255,255,.22)!important;
  color:#fff!important;
  box-shadow:0 10px 24px rgba(0,0,0,.28), inset 0 1px 0 rgba(255,255,255,.12);
  backdrop-filter:blur(14px);
  -webkit-backdrop-filter:blur(14px);
  pointer-events:auto;
}
#mobileMenu .media-detail-cover-actions .media-detail-play-btn[data-media-detail-action="play"]{
  background:linear-gradient(135deg, rgba(var(--detail-accent-rgb) / .62), rgba(255,255,255,.16))!important;
}
#mobileMenu .media-detail-cover-actions .media-detail-play-btn.like.active{
  color:rgb(var(--detail-accent-rgb))!important;
  border-color:rgba(var(--detail-accent-rgb) / .42)!important;
  background:rgba(10,12,18,.58)!important;
}
#mobileMenu .media-detail-cover-actions .media-detail-play-btn .ui-ic{
  width:17px;
  height:17px;
}
#mobileMenu .media-detail-hero-actions{
  justify-content:flex-start;
  gap:12px;
  margin-top:2px;
}
#mobileMenu.rtl .media-detail-hero-actions,
.rtl #mobileMenu .media-detail-hero-actions{
  justify-content:flex-end;
}
#mobileMenu .media-detail-hero-actions .media-detail-play-btn{
  width:42px;
  min-width:42px;
  height:42px;
  border-radius:999px;
}
#mobileMenu .media-detail-hero-actions .media-detail-play-btn[data-media-detail-action="play"]{
  background:linear-gradient(135deg, rgba(var(--detail-accent-rgb) / .64), rgba(255,255,255,.14))!important;
}
#mobileMenu .media-detail-copy{
  grid-area:copy;
  align-self:center;
  justify-self:stretch;
  gap:9px;
  max-width:720px;
}
#mobileMenu .media-detail-title{
  font-size:clamp(24px, 3.2cqi, 42px);
  line-height:1.05;
}
#mobileMenu .media-detail-sub{
  display:flex;
  align-items:center;
  flex-wrap:wrap;
  gap:8px;
  white-space:normal;
  font-size:clamp(13px, 1.45cqi, 18px);
  font-weight:800;
}
#mobileMenu .media-detail-sub .media-detail-kind-badge{
  margin-inline-start:0;
}
#mobileMenu .media-detail-album-picker{
  width:min(100%, 360px);
  max-width:360px;
  grid-template-columns:1fr;
  justify-self:start;
  margin-top:12px;
}
#mobileMenu.rtl .media-detail-album-picker,
.rtl #mobileMenu .media-detail-album-picker{
  justify-self:end;
}
#mobileMenu .media-detail-picker-label{
  display:none;
}
#mobileMenu .media-detail-album-picker select{
  height:40px;
  border-radius:16px;
  padding-inline:14px 36px;
  font-size:13px;
}
#mobileMenu .artist-detail-hero{
  min-height:128px;
  grid-template-columns:112px minmax(0,1fr) auto;
  grid-template-areas:"art copy actions";
  align-items:center;
}
#mobileMenu.rtl .artist-detail-hero,
.rtl #mobileMenu .artist-detail-hero{
  grid-template-columns:auto minmax(0,1fr) 112px;
  grid-template-areas:"actions copy art";
}
#mobileMenu .artist-detail-art{
  grid-area:art;
  width:112px;
  border-radius:999px;
  box-shadow:0 18px 36px rgba(0,0,0,.24), inset 0 1px 0 rgba(255,255,255,.12);
}
#mobileMenu .artist-detail-art img{
  object-fit:cover;
}
#mobileMenu .artist-detail-copy{
  grid-area:copy;
}
#mobileMenu.rtl .artist-detail-copy,
.rtl #mobileMenu .artist-detail-copy{
  justify-items:end;
  text-align:end;
}
#mobileMenu .artist-detail-actions{
  grid-area:actions;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:8px;
  align-self:start;
  padding-top:4px;
}
#mobileMenu .artist-hero-icon-btn,
#mobileMenu .artist-info-btn{
  width:34px;
  min-width:34px;
  height:34px;
  min-height:34px;
  padding:0;
  border:0!important;
  border-radius:999px;
  background:transparent!important;
  box-shadow:none!important;
  color:rgba(255,255,255,.82)!important;
  display:grid;
  place-items:center;
  cursor:pointer;
}
#mobileMenu .artist-hero-icon-btn[aria-expanded="true"],
#mobileMenu .artist-hero-icon-btn:hover,
#mobileMenu .artist-info-btn:hover{
  background:rgba(var(--detail-accent-rgb) / .16)!important;
  color:rgb(var(--detail-accent-rgb))!important;
}
#mobileMenu .artist-hero-icon-btn .ui-ic,
#mobileMenu .artist-info-btn .ui-ic{
  width:17px;
  height:17px;
}
#mobileMenu .artist-detail-search{
  margin-top:-4px;
}
#mobileMenu .artist-search-shell{
  width:min(100%, 520px);
  min-height:44px;
  justify-self:stretch;
  grid-template-columns:auto minmax(0,1fr) auto;
  padding:6px 8px;
  border-radius:18px;
}
#mobileMenu .artist-search-shell input{
  min-height:30px;
  font-size:13px;
}
#mobileMenu .artist-search-btn{
  width:auto;
  grid-column:auto;
  min-height:32px;
  padding:0 13px;
  border-radius:999px;
}
#mobileMenu .library-toolbar-minimal{
  display:grid!important;
  grid-template-columns:auto minmax(0,1fr) auto auto;
  align-items:center!important;
  gap:10px!important;
  margin:0 0 12px!important;
  padding:0 2px;
}
#mobileMenu .library-toolbar-minimal .library-toolbar-actions{
  flex:0 0 auto;
  display:flex;
  align-items:center;
  gap:8px;
  min-width:0;
  flex-wrap:nowrap;
}
#mobileMenu .library-toolbar-minimal .library-toolbar-sort{
  justify-content:flex-start;
}
#mobileMenu .library-toolbar-minimal .library-toolbar-icons{
  justify-content:flex-end;
}
#mobileMenu .library-toolbar-minimal .library-toolbar-player{
  min-width:0;
  justify-self:center;
}
#mobileMenu .library-toolbar-minimal .library-player-focus{
  width:auto!important;
  max-width:min(34vw, 220px);
  min-height:34px!important;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:7px;
  padding:0 11px!important;
  border-radius:999px!important;
  background:rgba(255,255,255,.055)!important;
  border:1px solid rgba(255,255,255,.1)!important;
  box-shadow:none!important;
}
#mobileMenu .library-toolbar-minimal .library-player-art{
  width:18px!important;
  height:18px!important;
  min-width:18px;
  border:0!important;
  border-radius:999px!important;
  background:transparent!important;
}
#mobileMenu .library-toolbar-minimal .library-player-art img,
#mobileMenu .library-toolbar-minimal .library-player-state,
#mobileMenu .library-toolbar-minimal .library-player-focus .eq-icon,
#mobileMenu .library-toolbar-minimal .library-focus-badge{
  display:none!important;
}
#mobileMenu .library-toolbar-minimal .library-player-copy{
  display:block;
  min-width:0;
}
#mobileMenu .library-toolbar-minimal .library-player-name{
  display:block;
  max-width:100%;
  overflow:hidden;
  white-space:nowrap;
  text-overflow:ellipsis;
  font-size:12px;
  font-weight:850;
}
#mobileMenu .library-toolbar-minimal .media-sort-select{
  min-width:128px;
  width:auto;
  height:38px;
  min-height:38px;
  padding:0 13px;
  border-radius:999px;
  font-size:12px;
  font-weight:850;
}
#mobileMenu .library-toolbar-minimal .media-layout-toggle{
  background:transparent!important;
  border:0!important;
  box-shadow:none!important;
  padding:0!important;
  gap:7px!important;
  backdrop-filter:none!important;
  -webkit-backdrop-filter:none!important;
}
#mobileMenu .library-toolbar-minimal .media-layout-btn{
  width:36px;
  min-width:36px;
  height:36px;
  min-height:36px;
  padding:0;
  border-radius:999px!important;
  background:rgba(255,255,255,.075)!important;
  border:1px solid rgba(255,255,255,.12)!important;
  box-shadow:none!important;
}
#mobileMenu .library-toolbar-minimal .media-layout-btn[data-media-layout]{
  display:none!important;
}
#mobileMenu .library-toolbar-minimal .media-layout-btn.active{
  color:#fff!important;
  background:rgba(var(--detail-accent-rgb) / .3)!important;
  border-color:rgba(var(--detail-accent-rgb) / .36)!important;
}
#mobileMenu .library-toolbar-minimal{
  direction:rtl;
  grid-template-columns:minmax(126px, max-content) minmax(0, 1fr) max-content max-content;
  overflow:visible;
}
#mobileMenu .library-toolbar-player{
  grid-column:1;
  justify-self:end;
  min-width:0;
}
#mobileMenu .library-toolbar-search-inline{
  grid-column:2;
  justify-self:stretch;
}
#mobileMenu .library-toolbar-icons{
  grid-column:3;
  justify-self:start;
}
#mobileMenu .library-toolbar-sort{
  grid-column:4;
  justify-self:start;
}
#mobileMenu .library-toolbar-minimal .library-player-focus,
#mobileMenu .library-toolbar-minimal .library-toolbar-icons,
#mobileMenu .library-toolbar-minimal .library-toolbar-sort,
#mobileMenu .library-toolbar-minimal .media-layout-toggle{
  direction:rtl;
}
#mobileMenu .library-toolbar-minimal .media-sort-select,
#mobileMenu .library-toolbar-minimal .library-tab-search-row,
#mobileMenu .library-toolbar-minimal .library-toolbar-search{
  direction:auto;
}
#mobileMenu .library-toolbar-minimal .library-toolbar-icons,
#mobileMenu .library-toolbar-minimal .library-toolbar-sort{
  min-height:42px;
  padding:4px;
  gap:6px;
  border-radius:999px;
  background:rgba(255,255,255,.055);
  border:1px solid rgba(255,255,255,.1);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
}
#mobileMenu .library-toolbar-search,
#mobileMenu .library-tab-search-row{
  min-width:0;
  min-height:42px;
  display:grid;
  grid-template-columns:30px minmax(0,1fr) 28px;
  align-items:center;
  gap:8px;
  padding:0 12px;
  border-radius:999px;
  background:rgba(255,255,255,.07);
  border:1px solid rgba(255,255,255,.11);
  color:rgba(255,255,255,.86);
  box-shadow:inset 0 1px 0 rgba(255,255,255,.06);
}
#mobileMenu .library-tab-search-row{
  margin:-4px 2px 10px;
  display:none;
}
#mobileMenu .library-toolbar-search input,
#mobileMenu .library-tab-search-row input{
  min-width:0;
  width:100%;
  border:0;
  outline:0;
  background:transparent;
  color:inherit;
  font:inherit;
  font-size:13px;
  font-weight:780;
  direction:auto;
}
#mobileMenu .library-toolbar-search input::placeholder,
#mobileMenu .library-tab-search-row input::placeholder{
  color:rgba(255,255,255,.46);
}
#mobileMenu .library-tab-search-icon{
  display:grid;
  place-items:center;
  color:rgba(255,255,255,.68);
}
#mobileMenu .library-tab-search-submit{
  width:30px;
  height:30px;
  border:0;
  border-radius:999px;
  background:rgba(255,255,255,.07);
  cursor:pointer;
  padding:0;
}
#mobileMenu .library-tab-search-icon .ui-ic{
  width:18px;
  height:18px;
}
#mobileMenu .library-tab-search-clear{
  width:28px;
  height:28px;
  border:0;
  border-radius:999px;
  display:grid;
  place-items:center;
  background:transparent;
  color:rgba(255,255,255,.62);
  font-size:20px;
  font-weight:900;
  line-height:1;
  opacity:0;
  pointer-events:none;
  cursor:pointer;
}
#mobileMenu .library-tab-search-clear.visible{
  opacity:1;
  pointer-events:auto;
  background:rgba(255,255,255,.08);
}
#mobileMenu .library-toolbar-search-toggle{
  display:none;
}
#mobileMenu .artist-detail-shell .album-card-badge{
  position:absolute;
  inset-inline-start:18px;
  inset-block-start:calc(10px + var(--media-grid-thumb-size, 188px) - 32px);
  z-index:3;
  max-width:calc(100% - 92px);
  min-height:24px;
  display:inline-flex;
  align-items:center;
  gap:5px;
  padding:0 9px;
  border-radius:999px;
  background:rgba(12,15,22,.64);
  border:1px solid rgba(255,255,255,.18);
  color:rgba(255,255,255,.88);
  font-size:10px;
  font-weight:900;
  line-height:1;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
  box-shadow:0 12px 24px rgba(0,0,0,.22), inset 0 1px 0 rgba(255,255,255,.12);
  backdrop-filter:blur(12px);
  -webkit-backdrop-filter:blur(12px);
}
#mobileMenu .artist-detail-shell .album-card-badge-kind{
  color:color-mix(in srgb, var(--ma-accent) 86%, white 14%);
}
#mobileMenu .artist-detail-shell .album-card-badge-count{
  color:rgba(255,255,255,.74);
}
.theme-light #mobileMenu .library-toolbar-minimal .library-toolbar-icons,
.theme-light #mobileMenu .library-toolbar-minimal .library-toolbar-sort,
.theme-light #mobileMenu .library-toolbar-search,
.theme-light #mobileMenu .library-tab-search-row{
  background:rgba(255,255,255,.78);
  border-color:rgba(123,139,164,.22);
  color:#172033;
}
.theme-light #mobileMenu .library-toolbar-search input::placeholder,
.theme-light #mobileMenu .library-tab-search-row input::placeholder{
  color:rgba(50,62,78,.48);
}
.theme-light #mobileMenu .library-tab-search-submit{
  background:rgba(31,41,55,.06);
  color:rgba(31,41,55,.7);
}
.theme-light #mobileMenu .library-tab-search-icon,
.theme-light #mobileMenu .library-tab-search-clear{
  color:rgba(50,62,78,.62);
}
.theme-light #mobileMenu .artist-detail-shell .album-card-badge{
  background:rgba(255,255,255,.82);
  border-color:rgba(123,139,164,.22);
  color:#172033;
  box-shadow:0 10px 22px rgba(86,104,132,.12), inset 0 1px 0 rgba(255,255,255,.68);
}
.theme-light #mobileMenu .artist-detail-shell .album-card-badge-count{
  color:#607086;
}
#mobileMenu .media-detail-play-btn,
#mobileMenu .media-detail-action-btn.primary,
#mobileMenu .artist-search-btn{
  border-color:rgba(var(--detail-accent-rgb) / .34);
  background:linear-gradient(135deg, rgba(var(--detail-accent-rgb) / .42), rgba(255,255,255,.08));
  box-shadow:0 16px 34px rgba(var(--detail-accent-rgb) / .14), inset 0 1px 0 rgba(255,255,255,.14);
}
#mobileMenu .media-detail-album-picker select,
#mobileMenu .media-detail-kind-badge{
  border-color:rgba(var(--detail-accent-rgb) / .28);
  background:rgba(var(--detail-accent-rgb) / .13);
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid,
#mobileMenu .media-entry.grid.media-type-artist,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album{
  justify-items:stretch;
  align-content:start;
  padding:10px 10px 14px;
  border-radius:6px;
  text-align:start;
  background:
    linear-gradient(180deg, rgba(255,255,255,.075), rgba(255,255,255,.035)),
    radial-gradient(circle at 16% 12%, rgba(var(--detail-accent-rgb) / .1), transparent 40%)!important;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .media-entry-main,
#mobileMenu .media-entry.grid.media-type-artist .media-entry-main,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .media-entry-main{
  justify-items:stretch;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .menu-thumb,
#mobileMenu .media-entry.grid.media-type-artist .menu-thumb,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .menu-thumb{
  width:100%;
  max-width:none;
  height:var(--media-grid-thumb-size, 188px);
  border-radius:4px;
  justify-self:stretch;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .menu-thumb img,
#mobileMenu .media-entry.grid.media-type-artist .menu-thumb img{
  object-fit:cover;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid.media-type-album .menu-thumb img,
#mobileMenu .menu-sheet.sheet-library .media-entry.grid.media-type-playlist .menu-thumb img,
#mobileMenu .menu-sheet.sheet-library .media-entry.grid.media-type-podcast .menu-thumb img{
  object-fit:contain;
}
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .menu-thumb img{
  object-fit:contain;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .media-play-btn,
#mobileMenu .media-entry.grid.media-type-artist .media-play-btn,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .media-play-btn{
  inset-block-start:calc(10px + var(--media-grid-thumb-size, 188px) - 26px);
  inset-inline-end:18px;
  width:52px;
  min-width:52px;
  height:52px;
  min-height:52px;
  border-radius:999px;
  background:linear-gradient(135deg, rgba(96,165,250,.92), rgba(var(--detail-accent-rgb) / .74))!important;
  color:#fff!important;
  box-shadow:0 16px 30px rgba(0,0,0,.24), 0 0 0 1px rgba(255,255,255,.18) inset;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .media-more-btn,
#mobileMenu .media-entry.grid.media-type-artist .media-more-btn,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .media-more-btn{
  opacity:.78;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .menu-item-sub,
#mobileMenu .media-entry.grid.media-type-artist .menu-item-sub{
  display:none;
}
#mobileMenu .menu-sheet.sheet-library .media-entry.grid .menu-item-title,
#mobileMenu .media-entry.grid.media-type-artist .menu-item-title,
#mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .menu-item-title{
  text-align:start;
  font-size:15px;
  -webkit-line-clamp:1;
}
#mobileMenu .menu-sheet.sheet-library .media-layout-btn[data-media-layout]{
  display:none;
}
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-media-detail,
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-artist-detail,
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-library{
  background:
    radial-gradient(circle at 18% 0%, rgba(var(--detail-accent-rgb) / .16), transparent 36%),
    radial-gradient(circle at 86% 18%, rgba(var(--detail-glow-rgb) / .12), transparent 34%),
    linear-gradient(180deg, rgba(255,255,255,.94), rgba(239,244,251,.96))!important;
}
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-media-detail::after,
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-artist-detail::after,
.theme-light #mobileMenu.has-menu-detail-theme .menu-sheet.sheet-library::after{
  background:linear-gradient(180deg, rgba(255,255,255,.76), rgba(246,249,253,.93));
}
.theme-light #mobileMenu .media-detail-hero::after,
.theme-light #mobileMenu .artist-detail-hero::after{
  background:
    linear-gradient(90deg, rgba(255,255,255,.82), rgba(246,249,253,.58) 58%, rgba(255,255,255,.84)),
    radial-gradient(circle at 18% 12%, rgba(var(--detail-accent-rgb) / .12), transparent 40%);
}
@media (max-width: 620px){
  #mobileMenu .artist-detail-hero{
    min-height:104px;
    grid-template-columns:82px minmax(0,1fr) 112px;
    grid-template-areas:"art copy actions";
    gap:10px;
    padding:13px;
  }
  #mobileMenu.rtl .artist-detail-hero,
  .rtl #mobileMenu .artist-detail-hero{
    grid-template-columns:112px minmax(0,1fr) 82px;
    grid-template-areas:"actions copy art";
  }
  #mobileMenu .artist-detail-art{
    width:82px;
  }
  #mobileMenu .artist-detail-actions{
    flex-direction:row;
    align-self:center;
    justify-content:flex-end;
    padding-top:0;
  }
  #mobileMenu .artist-hero-icon-btn,
  #mobileMenu .artist-info-btn{
    width:32px;
    min-width:32px;
    height:32px;
    min-height:32px;
  }
  #mobileMenu .album-detail-player{
    grid-template-columns:minmax(112px, 34vw) minmax(0,1fr);
    grid-template-areas:"art copy";
    min-height:auto;
    gap:12px;
    padding:11px;
    text-align:start;
  }
  #mobileMenu.rtl .album-detail-player,
  .rtl #mobileMenu .album-detail-player{
    grid-template-columns:minmax(0,1fr) minmax(112px, 34vw);
    grid-template-areas:"copy art";
    text-align:end;
  }
  #mobileMenu .media-detail-art-stage{
    width:min(34vw, 154px);
  }
  #mobileMenu .media-detail-copy{
    justify-items:start;
    max-width:none;
    gap:5px;
  }
  #mobileMenu.rtl .media-detail-copy,
  .rtl #mobileMenu .media-detail-copy{
    justify-items:end;
  }
  #mobileMenu .media-detail-title{
    font-size:clamp(19px, 6vw, 24px);
    -webkit-line-clamp:2;
  }
  #mobileMenu .media-detail-sub{
    justify-content:flex-start;
    font-size:12px;
  }
  #mobileMenu.rtl .media-detail-sub,
  .rtl #mobileMenu .media-detail-sub{
    justify-content:flex-end;
  }
  #mobileMenu .media-detail-album-picker,
  #mobileMenu.rtl .media-detail-album-picker,
  .rtl #mobileMenu .media-detail-album-picker{
    width:min(100%, 236px);
    justify-self:start;
    margin-top:4px;
  }
  #mobileMenu.rtl .media-detail-album-picker,
  .rtl #mobileMenu .media-detail-album-picker{
    justify-self:end;
  }
  #mobileMenu .media-detail-album-picker select{
    height:34px;
    font-size:12px;
    border-radius:999px;
  }
  #mobileMenu .media-detail-cover-actions{
    inset:auto 10px 10px auto;
    flex-direction:row!important;
  }
  #mobileMenu .media-detail-cover-actions .media-detail-play-btn{
    width:34px;
    min-width:34px;
    height:34px;
  }
  #mobileMenu .media-detail-hero-actions{
    flex-direction:row!important;
    justify-content:flex-start;
    gap:8px;
    margin-top:1px;
  }
  #mobileMenu.rtl .media-detail-hero-actions,
  .rtl #mobileMenu .media-detail-hero-actions{
    justify-content:flex-end;
  }
  #mobileMenu .media-detail-hero-actions .media-detail-play-btn{
    width:34px;
    min-width:34px;
    height:34px;
  }
  #mobileMenu .library-toolbar-minimal{
    grid-template-columns:minmax(124px, 148px) minmax(0,1fr) minmax(86px, 104px);
    margin:0 0 8px!important;
    padding:0;
    gap:7px!important;
  }
  #mobileMenu .library-toolbar-search-inline{
    display:none;
  }
  #mobileMenu .library-tab-search-row{
    display:grid;
  }
  #mobileMenu .library-toolbar-search-toggle{
    display:grid;
  }
  #mobileMenu .library-toolbar-icons{
    grid-column:2;
    justify-self:start;
  }
  #mobileMenu .library-toolbar-sort{
    grid-column:3;
    justify-self:start;
  }
  #mobileMenu .library-toolbar-minimal .library-toolbar-icons,
  #mobileMenu .library-toolbar-minimal .library-toolbar-sort{
    min-height:34px;
    padding:0;
    background:transparent;
    border:0;
    box-shadow:none;
    gap:5px;
  }
  #mobileMenu .library-toolbar-minimal .library-player-focus{
    max-width:148px;
    min-height:32px!important;
    padding:0 9px!important;
  }
  #mobileMenu .library-toolbar-minimal .library-player-art{
    width:16px!important;
    height:16px!important;
    min-width:16px;
  }
  #mobileMenu .library-toolbar-minimal .library-player-name{
    font-size:11px;
  }
  #mobileMenu .library-toolbar-minimal .media-sort-select{
    min-width:86px;
    max-width:104px;
    height:34px;
    min-height:34px;
    padding:0 9px;
    font-size:11px;
  }
  #mobileMenu .library-toolbar-minimal .media-layout-toggle{
    gap:6px!important;
  }
  #mobileMenu .library-toolbar-minimal .media-layout-btn{
    width:34px;
    min-width:34px;
    height:34px;
    min-height:34px;
  }
  #mobileMenu .menu-sheet.sheet-library .media-entry.grid .media-play-btn,
  #mobileMenu .media-entry.grid.media-type-artist .media-play-btn,
  #mobileMenu .artist-detail-shell .media-entry.grid.media-type-album .media-play-btn{
    width:44px;
    min-width:44px;
    height:44px;
    min-height:44px;
    inset-block-start:calc(10px + var(--media-grid-thumb-size, 164px) - 22px);
  }
  #mobileMenu .artist-detail-shell .album-card-badge{
    inset-inline-start:16px;
    inset-block-start:calc(10px + var(--media-grid-thumb-size, 164px) - 30px);
    max-width:calc(100% - 76px);
    min-height:22px;
    padding:0 8px;
    font-size:9px;
  }
}
@keyframes screensaverBreath{
  0%,100%{transform:scale(1.08);filter:blur(22px) saturate(1.12);}
  50%{transform:scale(1.14);filter:blur(28px) saturate(1.26);}
}
@keyframes screensaverEmptyDrift{
  0%,100%{transform:scale(1.08) translate3d(0,0,0);}
  50%{transform:scale(1.14) translate3d(2%, -1%, 0);}
}
@keyframes screensaverLightFieldOne{
  0%,100%{transform:translate3d(-2%,1%,0) scale(.98);opacity:.4;filter:blur(2px) saturate(1.12);}
  30%{transform:translate3d(5%,-4%,0) scale(1.08);opacity:.56;filter:blur(4px) saturate(1.24);}
  62%{transform:translate3d(-5%,-1%,0) scale(1.16);opacity:.44;filter:blur(3px) saturate(1.18);}
  82%{transform:translate3d(2%,4%,0) scale(1.03);opacity:.52;filter:blur(1px) saturate(1.2);}
}
@keyframes screensaverLightFieldTwo{
  0%,100%{transform:translate3d(3%,-2%,0) scale(1.02);opacity:.3;filter:blur(2px) saturate(1.05);}
  42%{transform:translate3d(-4%,4%,0) scale(1.18);opacity:.44;filter:blur(5px) saturate(1.2);}
  72%{transform:translate3d(2%,1%,0) scale(.96);opacity:.36;filter:blur(1px) saturate(1.12);}
}
@keyframes screensaverImageIn{
  from{opacity:.38;transform:scale(1.015);}
  to{opacity:1;transform:scale(1);}
}
@keyframes screensaverArtGuard{
  0%,100%{transform:translate3d(-3px,2px,0);}
  25%{transform:translate3d(4px,-2px,0);}
  50%{transform:translate3d(2px,4px,0);}
  75%{transform:translate3d(-4px,-3px,0);}
}
@keyframes screensaverTextGuard{
  0%,100%{transform:translate3d(3px,-2px,0);opacity:.96;}
  33%{transform:translate3d(-4px,3px,0);opacity:.9;}
  66%{transform:translate3d(2px,4px,0);opacity:.94;}
}
@keyframes screensaverBrandGuard{
  0%,100%{transform:translate3d(0,0,0);opacity:.72;}
  50%{transform:translate3d(5px,-4px,0);opacity:.58;}
}
@keyframes screensaverClockGuard{
  0%,100%{transform:translate(calc(-50% - 4px), calc(-50% + 3px)) scale(var(--screensaver-clock-scale, 1));opacity:.94;}
  25%{transform:translate(calc(-50% + 5px), calc(-50% - 2px)) scale(var(--screensaver-clock-scale, 1));opacity:.88;}
  50%{transform:translate(calc(-50% + 2px), calc(-50% + 5px)) scale(var(--screensaver-clock-scale, 1));opacity:.92;}
  75%{transform:translate(calc(-50% - 5px), calc(-50% - 3px)) scale(var(--screensaver-clock-scale, 1));opacity:.9;}
}
@keyframes screensaverClockGlow{
  0%,100%{text-shadow:0 10px 36px rgba(0,0,0,.48), 0 0 0 rgba(var(--dynamic-accent-rgb,245 166 35) / 0);}
  50%{text-shadow:0 14px 42px rgba(0,0,0,.52), 0 0 28px rgba(var(--dynamic-accent-rgb,245 166 35) / .26), 0 0 56px rgba(96,165,250,.14);}
}
@keyframes screensaverClockRingPulse{
  0%,100%{box-shadow:inset 0 0 0 10px rgba(255,255,255,.05), 0 24px 60px rgba(0,0,0,.28), 0 0 0 rgba(var(--dynamic-accent-rgb,245 166 35) / 0);}
  50%{box-shadow:inset 0 0 0 10px rgba(255,255,255,.07), 0 28px 70px rgba(0,0,0,.3), 0 0 34px rgba(var(--dynamic-accent-rgb,245 166 35) / .22), 0 0 68px rgba(96,165,250,.12);}
}
@media (prefers-reduced-motion: reduce){
  .screensaver-backdrop,
  .screensaver-backdrop .screensaver-shell,
  .screensaver-backdrop .screensaver-action-cluster,
  .screensaver-backdrop .screensaver-brand{
    transition:none!important;
  }
  .screensaver-backdrop.empty-mode::before,
  .screensaver-backdrop.empty-mode::after,
  .screensaver-backdrop.empty-mode .screensaver-bg,
  .screensaver-backdrop.open .screensaver-art,
  .screensaver-backdrop.open .screensaver-track,
  .screensaver-backdrop.open .screensaver-message,
  .screensaver-backdrop.open .screensaver-next,
  .screensaver-backdrop.open .screensaver-brand,
  .screensaver-backdrop.open .screensaver-clock,
  .screensaver-backdrop.open .screensaver-analog-clock{
    animation:none!important;
  }
}
@media (max-width: 620px){
  .screensaver-shell{
    grid-template-columns:1fr;
    justify-items:center;
    text-align:center;
    gap:20px;
  }
  .screensaver-art{
    width:min(62vw, 230px);
  }
  .screensaver-art-wrap{
    width:min(62vw, 230px);
  }
  .screensaver-art-wrap .screensaver-art{
    width:100%;
  }
  .screensaver-clock{
    font-size:64px;
  }
  .screensaver-analog-clock{
    width:150px;
  }
  .screensaver-title,
  .screensaver-artist{
    white-space:normal;
  }
  .screensaver-next{
    width:min(90vw, 340px);
  }
  .screensaver-next-main{
    grid-template-columns:46px minmax(0, 1fr);
    gap:10px;
  }
  .screensaver-next-art{
    width:46px;
    border-radius:14px;
  }
  .screensaver-action-cluster{
    inset-inline-end:18px;
    inset-block-end:18px;
    gap:8px;
  }
  .screensaver-voice-btn{
    width:58px;
    height:58px;
  }
  .screensaver-control-btn{
    width:50px;
    height:50px;
  }
}

</style>
      <div class="card ${rtl ? "rtl" : ""} theme-${visualTheme} layout-${layoutMode}${layoutProfileClass ? ` ${layoutProfileClass}` : ""} mobile-layout-${mobileLayoutMode}${mobileLayoutMode === "full" ? " mobile-layout-forced-full" : ""}${mobileLayoutMode === "compact" ? " mobile-layout-forced-compact" : ""} performance-profile-${performanceProfile2}${performanceMode ? " performance-lite" : ""}${performanceUltraLite ? " performance-ultra-lite" : ""}${hotelMode ? " hotel-mode" : ""}${compactTileMode ? " compact-mode compact-collapsed" : compactMode ? " compact-expanded" : ""}${compactMiniWidget ? " compact-mini-widget" : ""}${this._compactMenuOverlayOpen() ? " compact-menu-open" : ""}${compactTransitionClass}${nightActive ? " night-mode" : ""}${showNightRow ? " night-mode-enabled" : ""}${tabletAutoFit ? " tablet-auto-fit" : ""}${tabletDenseUi ? " tablet-fit-dense" : ""}${showNightRow ? " tablet-fit-night" : ""}${showUpNextInline ? " tablet-fit-up-next" : ""}${mobileDenseContent ? " mobile-content-dense" : ""}${this._tabletStabilityModeEnabled() ? " tablet-stable" : ""}${!hotelMode && this._state.controlRoomOpen ? " control-room-open" : ""}${this._state.screensaverOpen ? " screensaver-active" : ""}" style="${layoutProfileStyle}--screensaver-clock-scale:${this._esc(screensaverClockSize.toFixed(2))};--screensaver-clock-x:${this._esc(screensaverClockX.toFixed(1))}%;--screensaver-clock-y:${this._esc(screensaverClockY.toFixed(1))}%;">
        <div class="bg" id="mobileBg"></div><div class="shade"></div><div class="glow"></div>
        ${compactCollapseFabHtml}
        ${homeShortcutFabHtml}
        ${mobileBrandSignatureHtml}
        ${floatingHistoryToggleFabHtml}
        ${floatingSleepTimerCornerHtml}
        ${tabletBrandWatermarkHtml}
        <div class="stage">
          ${compactTileMode ? compactTileHtml : layoutMode === "tablet" ? tabletStageHtml : `${centerHtml}${bottomHtml}${footerHtml}`}
        </div>
        <aside class="history-drawer ${historyEdgeClass}" id="historyDrawer" hidden>
          <div class="history-drawer-head">
            <div class="history-drawer-title-row">
              <div class="history-drawer-title-main">
                <div class="history-drawer-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("queue-action-logo")}</div>
                <div class="history-drawer-title">${this._esc(this._i18n("ui.recently_played_2"))}</div>
              </div>
              <button class="history-drawer-close" id="historyDrawerCloseBtn" title="${this._esc(this._i18n("ui.close"))}">${this._iconSvg("close")}</button>
            </div>
            <div class="history-drawer-tabs" role="tablist" aria-label="${this._esc(this._i18n("ui.history_tabs"))}">
              <button class="history-tab active" data-history-tab="recent" role="tab" aria-selected="true">${this._esc(this._i18n("ui.recent"))}</button>
              <button class="history-tab" data-history-tab="recommendations" role="tab" aria-selected="false">${this._esc(this._i18n("ui.recommended"))}</button>
            </div>
          </div>
          <div class="history-drawer-body" id="historyDrawerBody"></div>
        </aside>
        ${controlRoomBackdropHtml}
        <div class="menu-backdrop${this._state.menuOpen ? " open" : ""}" id="mobileMenu">
          <div class="menu-sheet">
            <div class="menu-head">
              <button id="mobileMenuBackBtn" hidden title="${this._i18n("ui.back_2")}">‹</button>
              <div class="menu-title" id="mobileMenuTitle"><span class="menu-title-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("menu-title-logo")}</span><span class="menu-title-main"><span class="menu-title-text">${this._i18n("ui.menu")}</span></span></div>
              <button id="mobileMenuAuxBtn" class="menu-aux-btn" hidden title="${this._i18n("ui.liked")}">${this._iconSvg("heart_outline")}</button>
              <button id="mobileMenuCloseBtn" title="${this._i18n("ui.close")}">×</button>
            </div>
            <div class="menu-body" id="mobileMenuBody"></div>
          </div>
        </div>
        <div class="menu-backdrop" id="maConfirmModal">
          <div class="menu-sheet confirm-sheet">
            <div class="menu-head">
              <div></div>
              <div class="menu-title"><span class="menu-title-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("menu-title-logo")}</span><span class="menu-title-main"><span class="menu-title-text">${this._i18n("ui.open_music_assistant_2")}</span></span></div>
              <button id="maConfirmCloseBtn" title="${this._i18n("ui.close")}">×</button>
            </div>
            <div class="menu-body">
              <div class="confirm-copy">${this._i18n("ui.open_the_full_music_assistant_interface")}</div>
              <div class="confirm-actions">
                <button class="menu-item" id="maConfirmContinueBtn">${this._i18n("ui.continue")}</button>
                <button class="menu-item" id="maConfirmCancelBtn">${this._i18n("ui.cancel")}</button>
              </div>
            </div>
          </div>
        </div>
        <div class="queue-action-backdrop" id="mobileQueueActionModal">
          <div class="queue-action-sheet" id="mobileQueueActionSheet"></div>
        </div>
        <div class="queue-action-backdrop" id="mobileVolumePresetModal">
          <div class="queue-action-sheet" id="mobileVolumePresetSheet"></div>
        </div>
        <div class="queue-action-backdrop clean-all-confirm-backdrop" id="cleanAllConfirmModal">
          <div class="queue-action-sheet clean-all-confirm-sheet" role="dialog" aria-modal="true" aria-labelledby="cleanAllConfirmTitle">
            <button class="clean-all-confirm-close" id="cleanAllConfirmCloseBtn" title="${this._esc(this._i18n("ui.close"))}" aria-label="${this._esc(this._i18n("ui.close"))}">×</button>
            <div class="clean-all-confirm-head">
              <span class="clean-all-confirm-icon danger-confirm-icon" aria-hidden="true">${this._iconSvg("close")}</span>
              <div class="clean-all-confirm-copy">
                <div class="clean-all-confirm-title" id="cleanAllConfirmTitle">${this._esc(this._cleanAllConfirmTitle())}</div>
                <div class="confirm-copy">${this._esc(this._cleanAllConfirmCopy())}</div>
              </div>
            </div>
            <div class="confirm-actions clean-all-confirm-actions">
              <button class="menu-item danger-confirm-action" id="cleanAllConfirmContinueBtn">${this._esc(this._cleanAllLabel())}</button>
              <button class="menu-item" id="cleanAllConfirmCancelBtn">${this._esc(this._i18n("ui.cancel"))}</button>
            </div>
          </div>
        </div>
        <div class="menu-backdrop" id="mobileSmartVoiceModal">
          <div class="menu-sheet confirm-sheet smart-voice-sheet" id="mobileSmartVoiceSheet"></div>
        </div>
        <div class="voice-assistant-dialog" id="voiceAssistantDialog" aria-live="polite"></div>
        <div class="hidden-tools"><select id="playerSel"></select><button id="themeToggleBtn"></button><button id="langBtn"></button><button id="maOpenBtn"></button><div id="content"></div></div>
        <audio id="homeiiLocalAudio" class="homeii-local-audio" playsinline aria-hidden="true"></audio>
        <div class="lyrics-backdrop" id="lyricsBackdrop"></div>
        <div class="screensaver-backdrop digital-mode" id="screensaverBackdrop" aria-hidden="true">
          <div class="screensaver-bg"></div>
          <div class="screensaver-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("screensaver-brand-logo")}</div>
          ${screensaverActionButtonsHtml ? `<div class="screensaver-action-cluster" aria-hidden="false">${screensaverActionButtonsHtml}</div>` : ""}
          <div class="screensaver-shell">
            <div class="screensaver-art-wrap">
              <div class="screensaver-art" id="screensaverArt"></div>
              ${screensaverLikeButtonHtml}
            </div>
            <div class="screensaver-info">
              <div class="screensaver-clock" id="screensaverClock">00:00</div>
              <div class="screensaver-analog-clock" aria-hidden="true">
                <span class="screensaver-hand hour" id="screensaverHour"></span>
                <span class="screensaver-hand minute" id="screensaverMinute"></span>
                <span class="screensaver-hand second" id="screensaverSecond"></span>
                <span class="screensaver-pin"></span>
              </div>
              <div class="screensaver-track">
                <div class="screensaver-title" id="screensaverTitle">${this._esc(this._i18n("ui.nothing_playing"))}</div>
                <div class="screensaver-artist" id="screensaverArtist"></div>
              </div>
              <div class="screensaver-lyrics" id="screensaverLyrics" aria-live="polite"></div>
              <div class="screensaver-next" id="screensaverNext" hidden>
                <span class="screensaver-next-label" id="screensaverNextLabel">${this._esc(this._i18n("ui.up_next_2"))}</span>
                <span class="screensaver-next-main">
                  <span class="screensaver-next-art" id="screensaverNextArt"></span>
                  <span class="screensaver-next-copy">
                    <span class="screensaver-next-title" id="screensaverNextTitle"></span>
                    <span class="screensaver-next-artist" id="screensaverNextArtist"></span>
                  </span>
                </span>
              </div>
              <div class="screensaver-message" id="screensaverMessage" hidden></div>
            </div>
          </div>
        </div>
        <div class="toast-wrap" id="toastWrap"></div>
        <div class="surprise-popup" id="surprisePopup"></div>
      </div>
    `;
    this._applyDynamicThemeStyles();
    this._applyBackgroundMotionStyles();
    this._setHistoryDrawerOpen(this._state.mobileHistoryDrawerOpen);
    this._syncRecentHistoryUi(true);
    this._syncSleepTimerChip();
    this._syncControlRoomUi();
    this._syncVoiceAssistantDialog();
    this._restoreMobileMenuAfterBuild("build");
    const cardEl = this.shadowRoot.querySelector(".card");
    cardEl?.addEventListener("pointerdown", this._boundScreensaverActivity, { passive: false });
    cardEl?.addEventListener("keydown", this._boundScreensaverActivity);
    this.$("screensaverBackdrop")?.addEventListener("click", this._boundScreensaverActivity);
    this.$("screensaverVoiceBtn")?.addEventListener("pointerdown", (e) => {
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false });
    this.$("screensaverVoiceBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget, [8, 18, 8])) return;
      this._startVoiceAssistantCommand({ keepScreensaver: true, ignoreWhenListening: true });
    });
    ["screensaverPrevBtn", "screensaverPlayPauseBtn", "screensaverNextBtn", "screensaverMuteBtn", "screensaverPowerBtn", "screensaverLikeBtn", "screensaverLyricsBtn"].forEach((id) => {
      this.$(id)?.addEventListener("pointerdown", (e) => {
        e.preventDefault();
        e.stopPropagation();
      }, { passive: false });
    });
    this.$("screensaverPrevBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._playerCmd("previous");
    });
    this.$("screensaverPlayPauseBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._togglePlay();
    });
    this.$("screensaverNextBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._playerCmd("next");
    });
    this.$("screensaverMuteBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._toggleMute();
    });
    this.$("screensaverPowerBtn")?.addEventListener("click", async (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget, [12, 18])) return;
      await this._runAuxiliaryButtonAction(0, { force: true });
    });
    this.$("screensaverLyricsBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      const overlay = this.$("screensaverBackdrop");
      if (overlay?.classList?.contains("lyrics-mode")) {
        this._state.screensaverLyricsOpen = false;
        if (!this._state.lyricsOpen) this._clearLyricsState?.();
        this._syncScreensaverUi();
        return;
      }
      if (!this._getSelectedPlayer()) return;
      this._state.screensaverLyricsOpen = true;
      this._syncLyricsForCurrentTrack?.();
      this._syncScreensaverUi();
    });
    this.$("screensaverLikeBtn")?.addEventListener("click", async (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      await this._toggleLikeCurrentMedia(e.currentTarget);
    });
    if (this._screensaverPageEntryPending) {
      this._screensaverPageEntryPending = false;
      this._resetScreensaverTimer({ hide: true, activity: true });
    } else {
      this._resetScreensaverTimer();
      this._restoreScreensaverIfOpen();
    }
    this.$("btnPlay")?.addEventListener("click", () => this._togglePlay());
    this.$("btnPrev")?.addEventListener("click", () => this._playerCmd("previous"));
    this.$("btnNext")?.addEventListener("click", () => this._playerCmd("next"));
    this.$("mobileShuffleBtn")?.addEventListener("click", () => this._toggleShuffle());
    this.$("mobileRepeatBtn")?.addEventListener("click", () => this._toggleRepeat());
    this.$("btnMute")?.addEventListener("click", () => this._toggleMute());
    this.shadowRoot.querySelectorAll("[data-volume-step]").forEach((btn) => btn.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._stepSelectedVolume(Number(e.currentTarget.dataset.volumeStep || 0));
    }));
    this.$("controlVolumeBtn")?.addEventListener("click", () => this._openTabletVolumePopup());
    this.$("compactExpandBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._setCompactExpanded(true);
    });
    this.$("compactCollapseBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._setCompactExpanded(false);
    });
    this._bindMobileQuickActionButtons();
    this.shadowRoot.querySelectorAll("[data-up-next-inline]").forEach((btn) => btn.addEventListener("click", async (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      await this._playMobileUpNext();
    }));
    this.$("nightModeQuickBtn")?.addEventListener("click", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._cycleNightMode();
    });
    this.$("nightSleepBtn")?.addEventListener("click", (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      this._cycleSleepTimer("night");
    });
    this.$("nightChillBtn")?.addEventListener("click", async (e) => {
      if (!this._pressUiButton(e.currentTarget)) return;
      await this._playNightMix();
    });
    this.$("homeShortcutFab")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      if (this._state.controlRoomOpen || this._homeShortcutNavigationSuppressed()) return;
      if (!this._pressUiButton(e.currentTarget)) return;
      this._goHomeAssistantDashboard();
    });
    this.$("historyDrawer")?.addEventListener("click", async (e) => {
      const closeBtn = e.target.closest("#historyDrawerCloseBtn");
      if (closeBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._setHistoryDrawerOpen(false);
        return;
      }
      const playAllBtn = e.target.closest("[data-history-play-all]");
      if (playAllBtn) {
        e.preventDefault();
        e.stopPropagation();
        if (!this._pressUiButton(playAllBtn)) return;
        const tab = this._state.mobileHistoryDrawerTab === "recommendations" ? "recommendations" : "recent";
        const items = tab === "recommendations" ? this._historyRecommendationItems() : this._visibleRecentHistoryItems();
        const playable = this._historyPlayableItems(items);
        if (!playable.length) {
          this._toastError(this._i18n("ui.no_results_found"));
          return;
        }
        await this._playAll(playable, false);
        this._setHistoryDrawerOpen(false);
        return;
      }
      const tabBtn = e.target.closest("[data-history-tab]");
      if (!tabBtn) return;
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileHistoryDrawerTab = tabBtn.dataset.historyTab === "recommendations" ? "recommendations" : "recent";
      this._state.mobileHistoryRenderedHtml = "";
      this._syncRecentHistoryUi();
    });
    this.$("sleepTimerCorner")?.addEventListener("click", (e) => {
      const chipBtn = e.target.closest("#sleepTimerChip");
      if (chipBtn) {
        if (!this._pressUiButton(chipBtn)) return;
        this._toggleSleepTimerMenu();
        return;
      }
      const addBtn = e.target.closest("[data-sleep-timer-add]");
      if (addBtn) {
        this._addSleepTimerMinutes(Number(addBtn.dataset.sleepTimerAdd || 15));
        this._toggleSleepTimerMenu(false);
        return;
      }
      const clearBtn = e.target.closest("[data-sleep-timer-clear]");
      if (clearBtn) {
        this._clearSleepTimer(true);
        return;
      }
      const closeBtn = e.target.closest("[data-sleep-timer-close]");
      if (closeBtn) {
        this._toggleSleepTimerMenu(false);
      }
    });
    this.$("controlRoomCloseBtn")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopImmediatePropagation?.();
      e.stopPropagation();
      this._suppressHomeShortcutNavigation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._closeControlRoom();
    });
    const keepControlRoomScroll = (e) => {
      if (e.target?.closest?.("[data-control-room-scroll]")) e.stopPropagation();
    };
    this.$("controlRoomBackdrop")?.addEventListener("wheel", keepControlRoomScroll, { passive: true });
    this.$("controlRoomBackdrop")?.addEventListener("touchmove", keepControlRoomScroll, { passive: true });
    this.$("controlRoomBackdrop")?.addEventListener("click", async (e) => {
      if (e.target?.id === "controlRoomBackdrop") {
        e.preventDefault();
        e.stopPropagation();
        if (!this._state.controlRoomOpen) {
          this._syncControlRoomChrome();
          return;
        }
        this._closeControlRoom();
        return;
      }
      const selectBtn = e.target.closest("[data-room-select]");
      if (selectBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(selectBtn);
        const entityId = selectBtn.dataset.roomSelect;
        const result = this._toggleControlRoomPlayerSelection(entityId);
        const name = this._controlRoomPlayerName(entityId);
        if (result === "kept") {
          this._toast(this._i18n("ui.at_least_one_player_must_stay_selected"));
        } else {
          this._toastSuccess(result === "removed" ? this._m(`${name} removed from studio selection`, `${name} הוסר מבחירת הסטודיו`) : this._m(`${name} added to studio selection`, `${name} נוסף לבחירת הסטודיו`));
        }
        return;
      }
      const primaryBtn = e.target.closest("[data-room-primary]");
      if (primaryBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(primaryBtn);
        const entityId = primaryBtn.dataset.roomPrimary;
        this._setControlRoomPrimary(entityId);
        this._toastSuccess(this._m(
          `Studio is now controlling ${this._controlRoomPlayerName(entityId)}`,
          `הסטודיו שולט עכשיו ב־${this._controlRoomPlayerName(entityId)}`
        ));
        return;
      }
      const playBtn = e.target.closest("[data-room-toggle-play]");
      if (playBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(playBtn);
        const entityId = playBtn.dataset.roomTogglePlay;
        const player = this._playerByEntityId(entityId);
        try {
          await this._togglePlayFor(entityId);
          this._toastSuccess(player?.state === "playing" ? this._m(`${this._controlRoomPlayerName(entityId)} paused`, `${this._controlRoomPlayerName(entityId)} הושהה`) : this._m(`${this._controlRoomPlayerName(entityId)} started playing`, `${this._controlRoomPlayerName(entityId)} התחיל לנגן`));
          setTimeout(() => this._updateNowPlayingState(), 250);
        } catch (error) {
          this._toastError(error?.message || this._i18n("ui.playback_command_failed_2"));
        }
        return;
      }
      const nextBtn = e.target.closest("[data-room-next]");
      if (nextBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(nextBtn);
        const entityId = nextBtn.dataset.roomNext;
        try {
          await this._playerCmdFor(entityId, "next");
          this._toastSuccess(this._m(`${this._controlRoomPlayerName(entityId)} skipped to next`, `${this._controlRoomPlayerName(entityId)} עבר לשיר הבא`));
          setTimeout(() => this._updateNowPlayingState(), 250);
        } catch (error) {
          this._toastError(error?.message || this._i18n("ui.next_track_failed"));
        }
        return;
      }
      const muteBtn = e.target.closest("[data-room-mute]");
      if (muteBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(muteBtn);
        const entityId = muteBtn.dataset.roomMute;
        const wasMuted = this._isMuted(this._playerByEntityId(entityId));
        try {
          await this._toggleMuteFor(entityId);
          this._toastSuccess(wasMuted ? this._m(`${this._controlRoomPlayerName(entityId)} unmuted`, `${this._controlRoomPlayerName(entityId)} הוחזר לווליום`) : this._m(`${this._controlRoomPlayerName(entityId)} muted`, `${this._controlRoomPlayerName(entityId)} הושתק`));
          setTimeout(() => this._updateNowPlayingState(), 160);
        } catch (error) {
          this._toastError(error?.message || this._i18n("ui.mute_command_failed"));
        }
        return;
      }
      const transferSourceBtn = e.target.closest("[data-room-transfer-source]");
      if (transferSourceBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(transferSourceBtn);
        this._state.controlRoomTransferSource = transferSourceBtn.dataset.roomTransferSource || "";
        this._syncControlRoomTransferDefaults();
        this._syncControlRoomUi();
        this._toast(this._m(
          `Transfer source: ${this._controlRoomPlayerName(this._state.controlRoomTransferSource)}`,
          `מקור להעברה: ${this._controlRoomPlayerName(this._state.controlRoomTransferSource)}`
        ));
        return;
      }
      const transferTargetBtn = e.target.closest("[data-room-transfer-target]");
      if (transferTargetBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(transferTargetBtn);
        const targetId = transferTargetBtn.dataset.roomTransferTarget || "";
        if (targetId && targetId !== this._state.controlRoomTransferSource) {
          this._state.controlRoomTransferTarget = targetId;
          this._syncControlRoomUi();
          this._toast(this._m(
            `Transfer target: ${this._controlRoomPlayerName(targetId)}`,
            `יעד להעברה: ${this._controlRoomPlayerName(targetId)}`
          ));
        }
        return;
      }
      const transferBtn = e.target.closest("[data-room-transfer]");
      if (transferBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(transferBtn);
        const ok = await this._transferQueueBetween(this._state.controlRoomTransferSource, this._state.controlRoomTransferTarget, { silent: true });
        if (ok) this._state.controlRoomPanel = "";
        if (ok) this._toastSuccess(this._i18n("ui.queue_transferred"));
        else this._toastError(this._i18n("ui.could_not_transfer_the_queue"));
        setTimeout(() => this._updateNowPlayingState(), 300);
        return;
      }
      const cloneBtn = e.target.closest("[data-room-clone]");
      if (cloneBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(cloneBtn);
        const ok = await this._cloneQueueBetween(this._state.controlRoomTransferSource, this._state.controlRoomTransferTarget, { silent: true });
        if (ok) this._toastSuccess(this._i18n("ui.queue_cloned"));
        else this._toastError(this._i18n("ui.could_not_clone_the_queue"));
        setTimeout(() => this._updateNowPlayingState(), 300);
        return;
      }
      const refreshQueuesBtn = e.target.closest("[data-room-refresh-queues]");
      if (refreshQueuesBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(refreshQueuesBtn);
        await this._loadControlRoomQueues([
          this._state.controlRoomTransferSource,
          this._state.controlRoomTransferTarget,
          ...this._controlRoomSelectedPlayerIds()
        ].filter(Boolean));
        this._toastSuccess(this._i18n("ui.queues_refreshed"));
        return;
      }
      const clearQueueBtn = e.target.closest("[data-room-clear-queue]");
      if (clearQueueBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(clearQueueBtn);
        const entityId = clearQueueBtn.dataset.roomClearQueue || "";
        if (!entityId) return;
        await this._clearQueueForPlayer(entityId);
        await this._loadControlRoomQueues([entityId]);
        this._toastSuccess(this._i18n("ui.queue_cleared"));
        return;
      }
      const libraryActionBtn = e.target.closest("[data-room-library-action]");
      if (libraryActionBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(libraryActionBtn);
        const action = libraryActionBtn.dataset.roomLibraryAction || "play";
        const entry = {
          uri: libraryActionBtn.dataset.roomLibraryUri || "",
          media_type: libraryActionBtn.dataset.roomLibraryType || "album",
          name: libraryActionBtn.dataset.roomLibraryName || "",
          subtitle: libraryActionBtn.dataset.roomLibrarySubtitle || "",
          image: libraryActionBtn.dataset.roomLibraryImage || ""
        };
        if (!entry?.uri) return;
        const played = await this._playControlRoomLibraryEntry(entry, action);
        if (played) {
          if (action !== "like") this._state.controlRoomPanel = "";
          if (action === "like") {
            if (this._state.controlRoomPanel === "favorites") this._loadControlRoomFavorites().catch(() => {
            });
            else this._syncControlRoomUi({ force: true });
          }
          const messages2 = {
            play: this._m(`Started ${entry.name || "media"} in Studio`, `${entry.name || "מדיה"} הופעל בסטודיו`),
            next: this._i18n("ui.will_play_next_in_studio"),
            add: this._i18n("ui.added_to_studio_queue"),
            radio_mode: this._i18n("ui.radio_mode_started"),
            like: this._i18n("ui.favorite_updated")
          };
          this._toastSuccess(messages2[action] || messages2.play);
          setTimeout(() => this._updateNowPlayingState(), 350);
        } else {
          this._toastError(this._i18n("ui.studio_media_action_failed"));
        }
        return;
      }
      const libraryPlayBtn = e.target.closest("[data-room-library-play]");
      if (libraryPlayBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(libraryPlayBtn);
        const entry = {
          uri: libraryPlayBtn.dataset.roomLibraryUri || "",
          media_type: libraryPlayBtn.dataset.roomLibraryType || "album",
          name: libraryPlayBtn.dataset.roomLibraryName || "",
          subtitle: libraryPlayBtn.dataset.roomLibrarySubtitle || "",
          image: libraryPlayBtn.dataset.roomLibraryImage || ""
        };
        if (!entry?.uri) return;
        const played = await this._playControlRoomLibraryEntry(entry);
        if (played) {
          this._state.controlRoomPanel = "";
          this._toastSuccess(this._m(`Started ${entry.name || "media"} in Studio`, `${entry.name || "מדיה"} הופעל בסטודיו`));
          setTimeout(() => this._updateNowPlayingState(), 350);
        } else {
          this._toastError(this._i18n("ui.could_not_start_playback_in_studio"));
        }
        return;
      }
      const smartMixBtn = e.target.closest("[data-room-smart-mix]");
      if (smartMixBtn) {
        e.preventDefault();
        e.stopPropagation();
        await this._startControlRoomMix(smartMixBtn.dataset.roomSmartMix || "", smartMixBtn);
        return;
      }
      const smartCustomBtn = e.target.closest("[data-room-smart-custom]");
      if (smartCustomBtn) {
        e.preventDefault();
        e.stopPropagation();
        await this._startControlRoomMix("custom", smartCustomBtn);
        return;
      }
      const saveSceneBtn = e.target.closest("[data-room-save-scene]");
      if (saveSceneBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._saveControlRoomSceneFromStudio(saveSceneBtn);
        return;
      }
      const deleteSceneBtn = e.target.closest("[data-room-delete-scene]");
      if (deleteSceneBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._deleteControlRoomScene(deleteSceneBtn.dataset.roomDeleteScene || "", deleteSceneBtn);
        return;
      }
      const sceneBtn = e.target.closest("[data-room-scene]");
      if (sceneBtn) {
        e.preventDefault();
        e.stopPropagation();
        await this._applyControlRoomScene(sceneBtn.dataset.roomScene || "home", sceneBtn);
        return;
      }
      const announceBtn = e.target.closest("[data-room-announce-send]");
      if (announceBtn) {
        e.preventDefault();
        e.stopPropagation();
        await this._sendControlRoomAnnouncement(announceBtn);
        return;
      }
      const thisDeviceBtn = e.target.closest("[data-room-this-device]");
      if (thisDeviceBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(thisDeviceBtn);
        const action = thisDeviceBtn.dataset.roomThisDevice;
        if (action === "disconnect") this._disconnectThisDevicePlayer();
        else this._connectThisDevicePlayer();
        this._syncControlRoomUi({ force: true });
        return;
      }
      const libraryMicBtn = e.target.closest("[data-room-library-mic]");
      if (libraryMicBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(libraryMicBtn);
        this._startControlRoomLibraryVoice();
        return;
      }
      const selectionToggleBtn = e.target.closest("[data-room-selection-toggle]");
      if (selectionToggleBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(selectionToggleBtn);
        const entityId = selectionToggleBtn.dataset.roomSelectionToggle;
        const result = this._toggleControlRoomPlayerSelection(entityId);
        if (result === "kept") {
          this._toast(this._i18n("ui.at_least_one_player_must_stay_selected"));
        } else {
          this._toastSuccess(result === "removed" ? this._m(`${this._controlRoomPlayerName(entityId)} removed from selection`, `${this._controlRoomPlayerName(entityId)} הוסר מהבחירה`) : this._m(`${this._controlRoomPlayerName(entityId)} selected`, `${this._controlRoomPlayerName(entityId)} נבחר`));
        }
        return;
      }
      const visibleToggleBtn = e.target.closest("[data-room-visible-toggle]");
      if (visibleToggleBtn) {
        e.preventDefault();
        e.stopPropagation();
        this._pressUiButton(visibleToggleBtn);
        const entityId = visibleToggleBtn.dataset.roomVisibleToggle;
        const wasVisible = this._controlRoomVisiblePlayerIds().includes(entityId);
        this._toggleControlRoomVisiblePlayer(entityId);
        this._toastSuccess(wasVisible ? this._m(`${this._controlRoomPlayerName(entityId)} hidden from Studio`, `${this._controlRoomPlayerName(entityId)} הוסתר מהסטודיו`) : this._m(`${this._controlRoomPlayerName(entityId)} shown in Studio`, `${this._controlRoomPlayerName(entityId)} מוצג בסטודיו`));
        return;
      }
      const dockBtn = e.target.closest("[data-room-selection-action]");
      if (dockBtn) {
        e.preventDefault();
        e.stopPropagation();
        const action = dockBtn.dataset.roomSelectionAction;
        const selectedIds = this._controlRoomActionTargetIds();
        if (action === "browse_library") {
          this._pressUiButton(dockBtn);
          this._toast(this._i18n("ui.opening_studio_library"));
          this._openControlRoomLibrary("library_playlists");
          return;
        }
        if (action === "browse_artists" || action === "browse_albums" || action === "browse_tracks" || action === "browse_radio") {
          this._pressUiButton(dockBtn);
          const pageMap = {
            browse_artists: "library_artists",
            browse_albums: "library_albums",
            browse_tracks: "library_tracks",
            browse_radio: "library_radio"
          };
          this._toast(this._i18n("ui.opening_studio_library"));
          this._openControlRoomLibrary(pageMap[action] || "library_playlists");
          return;
        }
        if (action === "timers") {
          this._pressUiButton(dockBtn);
          this._toast(this._i18n("ui.opening_timers"));
          this._openControlRoomLibrary("sleep_timer");
          return;
        }
        if (action === "open_ma") {
          this._pressUiButton(dockBtn);
          this._launchMusicAssistant();
          return;
        }
        if (["music", "actions", "library", "transfer", "selection", "visible", "mix", "recent", "favorites", "scenes", "announce", "pro"].includes(action)) {
          this._pressUiButton(dockBtn);
          const wasOpen = this._state.controlRoomPanel === action;
          this._toggleControlRoomPanel(action);
          this._toast(wasOpen ? this._m(`${this._controlRoomPanelLabel(action)} closed`, `${this._controlRoomPanelLabel(action)} נסגר`) : this._m(`${this._controlRoomPanelLabel(action)} opened`, `${this._controlRoomPanelLabel(action)} נפתח`));
          return;
        }
        const primaryId = this._controlRoomPrimaryPlayerId();
        if (action === "player_playpause") {
          if (!primaryId) return;
          this._pressUiButton(dockBtn);
          const player = this._playerByEntityId(primaryId);
          try {
            await this._togglePlayFor(primaryId);
            this._toastSuccess(player?.state === "playing" ? this._m(`${this._controlRoomPlayerName(primaryId)} paused`, `${this._controlRoomPlayerName(primaryId)} הושהה`) : this._m(`${this._controlRoomPlayerName(primaryId)} started playing`, `${this._controlRoomPlayerName(primaryId)} התחיל לנגן`));
            setTimeout(() => this._updateNowPlayingState(), 250);
          } catch (error) {
            this._toastError(error?.message || this._i18n("ui.playback_command_failed_2"));
          }
          return;
        }
        if (action === "player_next") {
          if (!primaryId) return;
          this._pressUiButton(dockBtn);
          try {
            await this._playerCmdFor(primaryId, "next");
            this._toastSuccess(this._m(`${this._controlRoomPlayerName(primaryId)} skipped to next`, `${this._controlRoomPlayerName(primaryId)} עבר לשיר הבא`));
            setTimeout(() => this._updateNowPlayingState(), 250);
          } catch (error) {
            this._toastError(error?.message || this._i18n("ui.next_track_failed"));
          }
          return;
        }
        if (action === "player_mute") {
          if (!primaryId) return;
          this._pressUiButton(dockBtn);
          const wasMuted = this._isMuted(this._playerByEntityId(primaryId));
          try {
            await this._toggleMuteFor(primaryId);
            this._toastSuccess(wasMuted ? this._m(`${this._controlRoomPlayerName(primaryId)} unmuted`, `${this._controlRoomPlayerName(primaryId)} הוחזר לווליום`) : this._m(`${this._controlRoomPlayerName(primaryId)} muted`, `${this._controlRoomPlayerName(primaryId)} הושתק`));
            setTimeout(() => this._updateNowPlayingState(), 160);
          } catch (error) {
            this._toastError(error?.message || this._i18n("ui.mute_command_failed"));
          }
          return;
        }
        if (action === "player_stop") {
          if (!primaryId) return;
          this._pressUiButton(dockBtn);
          try {
            await this._stopPlayer(primaryId);
            this._toastSuccess(this._m(`${this._controlRoomPlayerName(primaryId)} stopped`, `${this._controlRoomPlayerName(primaryId)} נעצר`));
            setTimeout(() => this._updateNowPlayingState(), 250);
          } catch (error) {
            this._toastError(error?.message || this._i18n("ui.stop_command_failed"));
          }
          return;
        }
        if (!selectedIds.length) {
          this._toastError(this._i18n("ui.select_at_least_one_studio_player"));
          return;
        }
        if (action === "playpause") {
          this._pressUiButton(dockBtn);
          await Promise.allSettled(selectedIds.map((entityId) => this._togglePlayFor(entityId)));
          this._toastSuccess(this._m(
            `Play / pause sent to ${this._controlRoomPlayerCountLabel(selectedIds.length)}`,
            `פקודת נגן / השהה נשלחה אל ${this._controlRoomPlayerCountLabel(selectedIds.length)}`
          ));
          setTimeout(() => this._updateNowPlayingState(), 250);
          return;
        }
        if (action === "next") {
          this._pressUiButton(dockBtn);
          await Promise.allSettled(selectedIds.map((entityId) => this._playerCmdFor(entityId, "next")));
          this._toastSuccess(this._m(
            `Next sent to ${this._controlRoomPlayerCountLabel(selectedIds.length)}`,
            `פקודת הבא נשלחה אל ${this._controlRoomPlayerCountLabel(selectedIds.length)}`
          ));
          setTimeout(() => this._updateNowPlayingState(), 250);
          return;
        }
        if (action === "mute") {
          this._pressUiButton(dockBtn);
          await Promise.allSettled(selectedIds.map((entityId) => this._toggleMuteFor(entityId)));
          this._toastSuccess(this._m(
            `Mute sent to ${this._controlRoomPlayerCountLabel(selectedIds.length)}`,
            `פקודת השתק נשלחה אל ${this._controlRoomPlayerCountLabel(selectedIds.length)}`
          ));
          setTimeout(() => this._updateNowPlayingState(), 250);
          return;
        }
        if (action === "clear") {
          this._pressUiButton(dockBtn);
          await Promise.allSettled(selectedIds.map((entityId) => this._clearQueueForPlayer(entityId)));
          this._toastSuccess(this._m(
            `Queues cleared for ${this._controlRoomPlayerCountLabel(selectedIds.length)}`,
            `התורים נוקו עבור ${this._controlRoomPlayerCountLabel(selectedIds.length)}`
          ));
          this._loadControlRoomQueues(selectedIds).catch(() => {
          });
          setTimeout(() => this._updateNowPlayingState(), 250);
          return;
        }
        if (action === "stop_all") {
          this._pressUiButton(dockBtn);
          await this._stopAllPlayers();
          setTimeout(() => this._updateNowPlayingState(), 350);
          return;
        }
        if (action === "group") {
          this._pressUiButton(dockBtn);
          const groupPrimaryId = selectedIds[0];
          const members = selectedIds.slice(1);
          if (members.length < 1) {
            this._toastError(this._i18n("ui.select_at_least_two_players_to_create_a_group"));
            return;
          }
          try {
            const ok = await this._applySpeakerGroupFor(groupPrimaryId, members);
            if (!ok) throw new Error(this._i18n("ui.select_at_least_two_players_to_create_a_group"));
            this._toastSuccess(this._i18n("ui.group_updated"));
          } catch (error) {
            this._toastError(error?.message || this._i18n("ui.player_groups_could_not_be_disconnected"));
          }
          setTimeout(() => this._updateNowPlayingState(), 350);
          return;
        }
        if (action === "ungroup") {
          this._pressUiButton(dockBtn);
          await Promise.allSettled(selectedIds.map((entityId) => this._clearSpeakerGroupFor(entityId)));
          this._toastSuccess(this._i18n("ui.group_cleared_2"));
          setTimeout(() => this._updateNowPlayingState(), 350);
        }
      }
    });
    this.$("controlRoomBackdrop")?.addEventListener("input", (e) => {
      const volumeInput = e.target.closest?.("[data-room-volume]");
      if (volumeInput) {
        const pct = Math.max(0, Math.min(100, Number(volumeInput.value || 0)));
        volumeInput.style.setProperty("--vol-pct", `${pct}%`);
        const label = volumeInput.closest(".control-room-volume-row")?.querySelector("[data-room-volume-value]");
        if (label) label.textContent = `${pct}%`;
        clearTimeout(this._controlRoomVolumeTimer);
        this._controlRoomVolumeTimer = setTimeout(() => this._setPlayerVolumeFor(volumeInput.dataset.roomVolume, pct / 100), 90);
        return;
      }
      const smartInput = e.target.closest?.("#controlRoomSmartQueryInput");
      if (smartInput) {
        this._state.controlRoomSmartQuery = smartInput.value || "";
        return;
      }
      const announceText = e.target.closest?.("#controlRoomAnnouncementText");
      if (announceText) {
        this._state.controlRoomAnnouncementText = announceText.value || "";
        return;
      }
      const sceneNameInput = e.target.closest?.("#controlRoomSceneNameInput");
      if (sceneNameInput) {
        this._state.controlRoomSceneName = sceneNameInput.value || "";
        return;
      }
      const announceVolume = e.target.closest?.("#controlRoomAnnouncementVolumeInput");
      if (announceVolume) {
        const pct = Math.max(20, Math.min(50, Number(announceVolume.value || 20)));
        this._state.controlRoomAnnouncementVolume = pct;
        const label = announceVolume.closest(".announcement-volume-field")?.querySelector(".settings-value");
        if (label) label.textContent = `+${pct}%`;
        return;
      }
      const sourceSelect = e.target.closest?.("#controlRoomTransferSource");
      if (sourceSelect) {
        this._state.controlRoomTransferSource = sourceSelect.value || "";
        this._syncControlRoomTransferDefaults();
        this._syncControlRoomUi();
        return;
      }
      const targetSelect = e.target.closest?.("#controlRoomTransferTarget");
      if (targetSelect) {
        this._state.controlRoomTransferTarget = targetSelect.value || "";
      }
    });
    this.$("controlRoomBackdrop")?.addEventListener("change", (e) => {
      const volumeInput = e.target.closest?.("[data-room-volume]");
      if (!volumeInput) return;
      const pct = Math.max(0, Math.min(100, Number(volumeInput.value || 0)));
      this._toastSuccess(this._m(
        `${this._controlRoomPlayerName(volumeInput.dataset.roomVolume)} volume set to ${pct}%`,
        `הווליום של ${this._controlRoomPlayerName(volumeInput.dataset.roomVolume)} עודכן ל־${pct}%`
      ));
    });
    this.$("controlRoomBackdrop")?.addEventListener("input", (e) => {
      const libraryInput = e.target.closest?.("#controlRoomLibraryInput");
      if (!libraryInput) return;
      this._state.controlRoomLibraryQuery = libraryInput.value || "";
      clearTimeout(this._searchTimer);
      this._searchTimer = setTimeout(() => this._searchControlRoomLibrary(libraryInput.value || ""), 180);
    });
    this.$("controlRoomBackdrop")?.addEventListener("keydown", (e) => {
      const libraryInput = e.target.closest?.("#controlRoomLibraryInput");
      if (!libraryInput) return;
      e.stopPropagation();
    });
    this.shadowRoot.querySelector(".card")?.addEventListener("click", (e) => {
      const chip = e.target.closest?.("#sleepTimerChip");
      const menu = e.target.closest?.("#sleepTimerMenu");
      if (chip || menu) return;
      if (this._state.mobileSleepTimerMenuOpen) this._toggleSleepTimerMenu(false);
    });
    this.shadowRoot.querySelectorAll("[data-mainbar-action]").forEach((btn) => btn.addEventListener("click", () => {
      const action = btn.dataset.mainbarAction;
      this._pressUiButton(btn);
      if (action === "library") this._openMobileMenu("library_playlists");
      else if (action === "search") this._openMobileMenu("library_search");
      else if (action === "settings") this._openMobileMenu("settings");
      else if (action === "actions") this._openMobileMenu("main");
      else if (action === "players") this._openMobileMenu("players");
      else if (action === "control_room") this._openControlRoom();
      else if (action === "home") this._goHomeAssistantDashboard();
      else if (action === "theme") {
        const reopenPage = this._state.menuOpen ? this._state.menuPage : "";
        this._toggleCardTheme();
        this._rebuildMobileUi({ reopenPage, reopenStudio: this._state.controlRoomOpen });
      }
    }));
    this.$("activePlayerChip")?.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopImmediatePropagation?.();
      e.stopPropagation();
      if (Number(this._state.activePlayerSwipeLockUntil || 0) > Date.now()) return;
      if (!this._isHotelMode() && this._hasPinnedPlayer() && this._pinnedPlayerCount() <= 1) {
        this._toast(this._i18n("ui.player_is_pinned_from_settings"));
        return;
      }
      this._openMobileMenu("players");
    });
    this.$("activePlayerPrevBtn")?.addEventListener("click", (e) => {
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._cycleActivePlayer(-1);
    });
    this.$("activePlayerNextBtn")?.addEventListener("click", (e) => {
      e.stopPropagation();
      if (!this._pressUiButton(e.currentTarget)) return;
      this._cycleActivePlayer(1);
    });
    this.$("activePlayersBubble")?.addEventListener("click", () => this._openMobileMenu("players_active"));
    this.$("npArt")?.addEventListener("click", async (e) => {
      if (this._isCompactTileMode()) {
        e.preventDefault();
        e.stopPropagation();
        await this._handleCompactCoverTap(e);
        return;
      }
      this._handleMobileArtTap(e);
    });
    queueMicrotask(() => {
      this._initMobileArtCarousel();
      this._bindActivePlayerChipSwipe();
    });
    this.$("mobileMenuTitle")?.addEventListener("click", (e) => {
      if (e.currentTarget?.dataset?.menuTitleAction === "players") this._pushMobileMenu("players");
    });
    this.$("mobileMenuAuxBtn")?.addEventListener("click", () => {
      if (this._state.menuPage?.startsWith("library_")) this._pushMobileMenu("library_liked");
    });
    this.$("mobileMenuCloseBtn")?.addEventListener("click", () => this._closeMobileMenu());
    this.$("mobileMenuBackBtn")?.addEventListener("click", () => this._backMobileMenu());
    this.$("mobileMenu")?.addEventListener("click", (e) => {
      if (e.target === this.$("mobileMenu")) this._closeMobileMenu();
    });
    this.$("mobileMenu")?.addEventListener("input", this._boundMobileMenuChange);
    this.$("mobileQueueActionModal")?.addEventListener("click", (e) => {
      if (e.target === this.$("mobileQueueActionModal")) this._closeMobileQueueActionMenu();
    });
    this.$("mobileVolumePresetModal")?.addEventListener("click", (e) => {
      if (e.target === this.$("mobileVolumePresetModal")) this._closeMobileVolumePresets();
    });
    this.$("mobileSmartVoiceModal")?.addEventListener("click", (e) => {
      if (e.target === this.$("mobileSmartVoiceModal")) this._closeSmartVoiceConfirm();
    });
    this.$("mobileVolumePresetSheet")?.addEventListener("click", this._boundMobileMenuClick);
    this.$("mobileQueueActionSheet")?.addEventListener("click", async (e) => {
      const btn = e.target.closest("[data-queue-popup],[data-media-popup]");
      if (!btn) return;
      const entry = this._state.mobileQueueActionEntry || null;
      const action = btn.dataset.queuePopup || btn.dataset.mediaPopup;
      const actionContext = this._state.mobileActionContext;
      if (action === "close") return this._closeMobileQueueActionMenu();
      if (!entry) return;
      if (actionContext === "media") {
        await this._handleMobileMediaAction(action, entry);
      } else if (action === "like") {
        await this._toggleLikeEntry(entry, btn);
      } else {
        const targetPosition = action === "move_to" ? this._queueMoveTargetFromElement(btn) : null;
        if (action === "move_to" && !targetPosition) return;
        await this._handleQueueAction(action, entry.queue_item_id, entry.uri || "", entry.sort_index || "", targetPosition);
      }
      this._closeMobileQueueActionMenu();
      if (this._state.menuOpen && (actionContext === "media" || action === "like" || String(this._state.menuPage || "").startsWith("library_"))) {
        await this._renderMobileMenu();
      }
    });
    this.$("mobileQueueActionSheet")?.addEventListener("change", async (e) => {
      if (!await this._handleQueueMoveAutoChange(e)) return;
      this._closeMobileQueueActionMenu();
      if (this._state.menuOpen && this._state.menuPage === "queue") await this._renderMobileMenu();
    });
    this.$("maConfirmCloseBtn")?.addEventListener("click", () => this._closeMaConfirm());
    this.$("maConfirmCancelBtn")?.addEventListener("click", () => this._closeMaConfirm());
    this.$("maConfirmContinueBtn")?.addEventListener("click", () => this._confirmMusicAssistantOpen());
    this.$("maConfirmModal")?.addEventListener("click", (e) => {
      if (e.target === this.$("maConfirmModal")) this._closeMaConfirm();
    });
    this.$("cleanAllConfirmCloseBtn")?.addEventListener("click", () => this._closeCleanAllConfirm());
    this.$("cleanAllConfirmCancelBtn")?.addEventListener("click", () => this._closeCleanAllConfirm());
    this.$("cleanAllConfirmContinueBtn")?.addEventListener("click", async (e) => {
      await this._runLockedUiAction(e.currentTarget, () => this._confirmCleanAllPlayers(), { pattern: [18, 24, 18], lockMs: 1600 });
    });
    this.$("cleanAllConfirmModal")?.addEventListener("click", (e) => {
      if (e.target === this.$("cleanAllConfirmModal")) this._closeCleanAllConfirm();
    });
    this.$("mobileMenuBody")?.addEventListener("click", this._boundMobileMenuClick);
    this.$("mobileMenuBody")?.addEventListener("change", this._boundMobileMenuChange);
    this.$("mobileMenuBody")?.addEventListener("keydown", this._boundMobileMenuKeydown);
    this.$("mobileMenuBody")?.addEventListener("scroll", this._boundMobileMenuScroll, { passive: true });
    this._bindProgressSeekBar(this.$("progressBar"));
    this.$("mobileVolPctLabel")?.addEventListener("click", () => this._openMobileVolumePresets());
    this.$("volSlider")?.addEventListener("input", (e) => {
      const pct = Number(e.target.value || 0);
      e.target.style.setProperty("--vol-pct", `${pct}%`);
      const player = this._getSelectedPlayer();
      if (player?.entity_id) this._setPlayerVolumeOptimistic(player.entity_id, pct / 100, pct === 0);
      this._setButtonIcon(this.$("btnMute"), pct === 0 ? "volume_mute" : pct < 40 ? "volume_low" : "volume_high");
      this.$("btnMute")?.classList.toggle("muted", pct === 0);
      const volLabel = this.$("mobileVolPctLabel");
      if (volLabel) volLabel.textContent = `${pct}%`;
      clearTimeout(this._volumeTimer);
      this._volumeTimer = setTimeout(() => this._setVolume(pct / 100), 120);
    });
  }
  async _init() {
    try {
      this._state.view = "now_playing";
      this._loadPlayers();
      this._connectMA();
      this._refreshGroupingState();
      await this._ensureQueueSnapshot();
      this._renderCurrentView();
      this._startLoops();
      if (!this._screensaverPageEntryPending) this._restoreScreensaverIfOpen();
      this._restoreMobileMenuAfterBuild("init");
    } catch (e) {
      this._renderError(e);
    }
  }
  _renderCurrentView() {
    this._state.view = "now_playing";
    this._syncNowPlayingUI();
  }
  async _renderNowPlayingPage() {
    this._state.view = "now_playing";
    await this._ensureQueueSnapshot();
    this._syncNowPlayingUI();
  }
  _updateThemeButton() {
  }
  _renderPlayerSummary() {
    const player = this._getSelectedPlayer();
    const chip = this.$("activePlayerChip");
    const title = this.$("selectedPlayerTitle");
    const sub = this.$("selectedPlayerSub");
    const thumb = this.$("selectedPlayerThumb");
    const tags = this.$("selectedPlayerTags");
    if (chip) chip.classList.toggle("is-playing", player?.state === "playing");
    if (!player) {
      if (title) title.textContent = this._musicAssistantRequiredTitle();
      if (sub) sub.textContent = this._state.musicAssistantIssueMessage || this._musicAssistantRequiredMessage();
      if (tags) tags.innerHTML = "";
      if (thumb) {
        thumb.classList.add("placeholder");
        thumb.classList.remove("brand-logo");
        thumb.style.backgroundImage = "";
        thumb.innerHTML = this._iconSvg("speaker");
      }
      this._syncMobilePlayerNavButtons();
      this._setMobileRandomFabVisible(false);
      return;
    }
    if (title) title.textContent = player?.attributes?.friendly_name || this._i18n("ui.selected_player");
    if (sub) sub.textContent = "";
    if (tags) {
      const groupCount = this._playerGroupCount(player);
      const pinned = this._hasPinnedPlayer() || this._frontPinnedPlayerEntity() === player?.entity_id;
      const nightMode = this._mobileNightMode();
      const nightActive = this._isNightModeActive();
      tags.innerHTML = [
        pinned ? `<span class="player-focus-pill pinned"><span>${this._esc(this._i18n("ui.pinned"))}</span></span>` : ``,
        nightMode !== "off" ? `<span class="player-focus-pill ${nightActive ? "night active" : "night"}"><span>${this._esc(this._i18n("ui.night"))}</span></span>` : ``,
        player?.state === "playing" ? `<span class="player-focus-pill playing"><span class="eq-icon" aria-hidden="true"><span></span><span></span><span></span></span><span>${this._esc(this._i18n("ui.playing"))}</span></span>` : ``,
        groupCount ? `<span class="player-group-badge player-focus-badge">${this._esc(groupCount)}</span>` : ``
      ].filter(Boolean).join("");
    }
    if (thumb) {
      const queueItem = this._state.maQueueState?.current_item || null;
      const art = this._queueItemImageUrl(queueItem, 180) || this._imageUrl(player?.attributes?.entity_picture_local, 180) || this._imageUrl(player?.attributes?.entity_picture, 180) || this._imageUrl(player?.attributes?.media_image_url, 180) || "";
      thumb.classList.toggle("placeholder", !art);
      thumb.classList.remove("brand-logo");
      if (art) {
        thumb.style.backgroundImage = `url("${art}")`;
        thumb.innerHTML = "";
      } else {
        thumb.style.backgroundImage = "";
        thumb.innerHTML = this._iconSvg("speaker");
      }
    }
    this._syncMobilePlayerNavButtons();
    this._setMobileRandomFabVisible(true);
  }
  _queueMoveSelectHtml(queueCount = 1, currentPosition = 1, entry = {}) {
    const total = Math.max(1, Math.round(Number(queueCount)) || 1);
    const current = Math.max(1, Math.min(total, Math.round(Number(currentPosition ?? entry?.position)) || 1));
    const queueItemId = String(entry?.queue_item_id || entry?.queueItemId || entry?.key || "").trim();
    const uri = String(entry?.uri || entry?.media_item?.uri || "").trim();
    const sortIndex = String(entry?.sort_index ?? "").trim();
    const options = Array.from({ length: total }, (_, index) => index + 1).map((pos) => `<option value="${this._esc(String(pos))}" ${pos === current ? "selected" : ""}>${this._esc(String(pos))}</option>`).join("");
    return `
      <select class="queue-move-select" data-queue-move-target data-queue-move-auto
        data-current-position="${this._esc(String(current))}"
        data-queue-item-id="${this._esc(queueItemId)}"
        data-queue-uri="${this._esc(uri)}"
        data-queue-sort-index="${this._esc(sortIndex)}"
        aria-label="${this._esc(this._i18n("ui.move_to_position"))}">
        ${options}
      </select>
    `;
  }
  _queueMoveTargetFromElement(sourceEl = null) {
    const container = sourceEl?.closest?.(".queue-inline-actions, .queue-action-sheet, .queue-ctx-menu");
    const input = container?.querySelector?.("[data-queue-move-target]");
    const value = Math.round(Number(input?.value));
    const total = Math.max(1, this._getNowPlayingQueueItems().length || (this._state.queueItems || []).length || Number(this._state.maQueueState?.items || 1));
    if (!Number.isFinite(value) || value < 1) {
      this._toastError(this._i18n("ui.choose_a_queue_position"));
      input?.focus?.();
      return null;
    }
    return Math.max(1, Math.min(total, value));
  }
  async _handleQueueMoveAutoChange(e) {
    const select = e.target?.closest?.("[data-queue-move-auto]");
    if (!select) return false;
    e.preventDefault?.();
    e.stopPropagation?.();
    const targetPosition = this._queueMoveTargetFromElement(select);
    if (!targetPosition) return true;
    const currentPosition = Math.round(Number(select.dataset.currentPosition || 0));
    if (targetPosition === currentPosition) return true;
    const queueRow = select.closest?.("[data-queue-item-id]");
    const queueItemId = select.dataset.queueItemId || queueRow?.dataset.queueItemId || "";
    const fallbackUri = select.dataset.queueUri || queueRow?.dataset.uri || "";
    const sortIndex = select.dataset.queueSortIndex || queueRow?.dataset.sortIndex || "";
    this._state.expandedQueueItemId = "";
    this._setQueueInlineActionsExpanded("");
    await this._handleQueueAction("move_to", queueItemId, fallbackUri, sortIndex, targetPosition);
    return true;
  }
  _openMobileQueueActionMenu(entry = {}) {
    this._state.mobileActionContext = "queue";
    this._state.mobileQueueActionEntry = entry;
    const host = this.$("mobileQueueActionSheet");
    const liked = this._isEntryLiked(entry);
    const currentInfo = this._currentTrackInfo();
    const queueCount = Math.max(1, this._getNowPlayingQueueItems().length || (this._state.queueItems || []).length || Number(this._state.maQueueState?.items || 1));
    const currentPosition = Math.max(1, Math.min(queueCount, this._queueDisplayPositionForEntry(entry, Math.round(Number(entry.sort_index || 0)) + 1 || 1)));
    if (host) {
      host.innerHTML = `
        <div class="queue-action-header">
          <div class="queue-action-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("queue-action-logo")}</div>
          <div class="queue-action-player">${this._esc(this._selectedPlayerName())}</div>
          <div class="queue-action-title">${this._esc(entry.name || currentInfo.title || this._i18n("ui.queue_actions"))}</div>
        </div>
        <div class="queue-move-control">
          <label>
            <span>${this._esc(this._i18n("ui.move_to_position"))}</span>
            ${this._queueMoveSelectHtml(queueCount, currentPosition, entry)}
          </label>
        </div>
        <button class="queue-action-item" data-queue-popup="next">${this._iconSvg("next")}<span>${this._esc(this._i18n("ui.play_next"))}</span></button>
        <button class="queue-action-item" data-queue-popup="remove">${this._iconSvg("trash")}<span>${this._esc(this._i18n("ui.remove"))}</span></button>
        <button class="queue-action-item" data-queue-popup="like">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}<span>${this._esc(this._i18n("ui.like_3"))}</span></button>
        <button class="queue-action-item warn" data-queue-popup="close">${this._iconSvg("close")}<span>${this._esc(this._i18n("ui.close"))}</span></button>
      `;
    }
    this.$("mobileQueueActionModal")?.classList.add("open");
  }
  _openMobileMediaActionMenu(entry = {}) {
    this._state.mobileActionContext = "media";
    this._state.mobileQueueActionEntry = entry;
    const host = this.$("mobileQueueActionSheet");
    const liked = this._isEntryLiked(entry);
    const currentInfo = this._currentTrackInfo();
    const mediaType = entry.media_type || entry.type || "album";
    const radioMode = this._supportsMusicAssistantRadioMode(mediaType);
    if (host) {
      host.innerHTML = `
        <div class="queue-action-header">
          <div class="queue-action-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("queue-action-logo")}</div>
          <div class="queue-action-player">${this._esc(this._selectedPlayerName())}</div>
          <div class="queue-action-title">${this._esc(entry.name || currentInfo.title || this._i18n("ui.media_actions"))}</div>
        </div>
        <button class="queue-action-item" data-media-popup="play">${this._iconSvg("play")}<span>${this._esc(this._i18n("ui.play"))}</span></button>
        <button class="queue-action-item" data-media-popup="play_clear">${this._iconSvg("play")}<span>${this._esc(this._i18n("ui.play_now_and_clear_queue"))}</span></button>
        <button class="queue-action-item" data-media-popup="next">${this._iconSvg("next")}<span>${this._esc(this._i18n("ui.play_next"))}</span></button>
        <button class="queue-action-item" data-media-popup="next_clear">${this._iconSvg("next")}<span>${this._esc(this._i18n("ui.play_next_and_clear_queue"))}</span></button>
        <button class="queue-action-item" data-media-popup="add">${this._iconSvg("queue")}<span>${this._esc(this._i18n("ui.add_to_queue"))}</span></button>
        ${radioMode ? `<button class="queue-action-item" data-media-popup="radio_mode">${this._iconSvg("radio")}<span>${this._esc(this._i18n("ui.start_radio_mode"))}</span></button>` : ``}
        <button class="queue-action-item" data-media-popup="like">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}<span>${this._esc(this._m(liked ? "Remove like" : "Like", liked ? "בטל אהבתי" : "סמן אהבתי"))}</span></button>
        <button class="queue-action-item warn" data-media-popup="close">${this._iconSvg("close")}<span>${this._esc(this._i18n("ui.close"))}</span></button>
      `;
    }
    this.$("mobileQueueActionModal")?.classList.add("open");
  }
  _closeMobileQueueActionMenu() {
    this._state.mobileActionContext = "";
    this._state.mobileQueueActionEntry = null;
    this.$("mobileQueueActionModal")?.classList.remove("open");
  }
  _mediaEntryFromDataset(dataset = {}) {
    return {
      uri: String(dataset.mediaUri || dataset.mediaPlay || dataset.mediaMore || dataset.mediaLike || "").trim(),
      media_type: String(dataset.mediaType || "track").trim() || "track",
      name: String(dataset.mediaName || "").trim(),
      artist: String(dataset.mediaArtist || "").trim(),
      album: String(dataset.mediaAlbum || "").trim(),
      image: String(dataset.mediaImage || "").trim()
    };
  }
  async _handleMediaDetailActionButton(button = null) {
    if (!button?.dataset) return false;
    const action = String(button.dataset.mediaDetailAction || "").trim();
    const entry = this._mediaEntryFromDataset(button.dataset);
    if (!action || !entry.uri) return false;
    const scrollSnapshot = this._captureMobileMenuScroll(this._state.menuPage || "media_detail");
    const loadingAction = action === "add" || action === "play";
    const feedbackEl = this._showLibraryInteractionFeedback(button, {
      loading: loadingAction,
      hold: loadingAction,
      press: !loadingAction
    });
    if (action === "like") {
      const wasLiked = this._isEntryLiked(entry);
      await this._toggleLikeEntry(entry, button);
      if (!this._useMaLikedMode()) this._toastSuccess(wasLiked ? this._i18n("ui.removed_from_liked") : this._i18n("ui.added_to_liked"));
      await this._renderMobileMenu();
      if (scrollSnapshot) this._restoreMobileMenuScrollSnapshot(scrollSnapshot, scrollSnapshot.page);
      return true;
    }
    if (action === "add") {
      try {
        const ok = await this._playMedia(entry.uri, entry.media_type || "track", "add", { label: entry.name, sourceEl: button, silent: true });
        if (ok) this._toastSuccess(this._i18n("ui.added_to_queue"));
        return ok;
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
    }
    if (action === "play") {
      try {
        const ok = await this._playMedia(entry.uri, entry.media_type || "track", "play", { label: entry.name, sourceEl: button, silent: true });
        if (ok) this._toastSuccess(this._i18n("ui.started_on_selected_player"));
        return ok;
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
    }
    return false;
  }
  async _handleMobileMediaAction(action, entry = {}) {
    if (!entry?.uri) return;
    const label = String(entry.name || "").trim();
    if (action === "like") {
      const wasLiked = this._isEntryLiked(entry);
      await this._toggleLikeEntry(entry);
      if (!this._useMaLikedMode()) {
        this._toastSuccess(wasLiked ? this._i18n("ui.removed_from_liked") : this._i18n("ui.added_to_liked"));
      }
      return;
    }
    if (action === "play" || action === "play_clear") {
      await this._playMedia(entry.uri, entry.media_type || "album", "play", { label });
      return;
    }
    if (action === "next") {
      const ok = await this._playMedia(entry.uri, entry.media_type || "album", "next", { label, silent: true });
      if (ok) this._toastSuccess(this._i18n("ui.will_play_next"));
      return;
    }
    if (action === "next_clear") {
      await this._clearQueueForPlayer(this._state.selectedPlayer);
      const ok = await this._playMedia(entry.uri, entry.media_type || "album", "next", { label, silent: true });
      if (ok) this._toastSuccess(this._i18n("ui.queue_cleared_and_item_moved_next"));
      return;
    }
    if (action === "add") {
      const ok = await this._playMedia(entry.uri, entry.media_type || "album", "add", { label, silent: true });
      if (ok) this._toastSuccess(this._i18n("ui.added_to_queue"));
    }
    if (action === "radio_mode") {
      const mediaType = entry.media_type || "album";
      if (!this._supportsMusicAssistantRadioMode(mediaType)) {
        this._toastError(this._i18n("ui.radio_mode_is_not_available_for_this_media_type"));
        return;
      }
      const ok = await this._playMedia(entry.uri, mediaType, "play", { label, radioMode: true, silent: true });
      if (ok) this._toastSuccess(this._i18n("ui.radio_mode_started"));
    }
  }
  _openMobileVolumePresets() {
    const host = this.$("mobileVolumePresetSheet");
    if (!host) return;
    host.classList.remove("tablet-volume-sheet-host");
    const selectedPlayer = this._getSelectedPlayer();
    const current = Math.round(this._effectivePlayerVolumeLevel(selectedPlayer) * 100);
    const values = Array.from({ length: 11 }, (_, index) => 100 - index * 10);
    host.innerHTML = `
      <div class="queue-action-header">
        <div class="queue-action-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("queue-action-logo")}</div>
        <div class="queue-action-title">${this._esc(this._i18n("ui.volume_presets"))}</div>
      </div>
      ${values.map((value) => `
      <button class="queue-action-item ${value === current ? "active" : ""}" data-volume-preset="${value}">
        <span>${value}%</span>
      </button>
    `).join("")}`;
    this.classList.add("volume-preset-open");
    this.$("mobileVolumePresetModal")?.classList.add("open");
  }
  _openTabletVolumePopup() {
    const host = this.$("mobileVolumePresetSheet");
    const player = this._getSelectedPlayer();
    if (!host || !player) return;
    host.classList.add("tablet-volume-sheet-host");
    const pct = Math.max(0, Math.min(100, Math.round(this._effectivePlayerVolumeLevel(player) * 100)));
    host.innerHTML = `
        <div class="tablet-volume-popup">
        <button class="volume-btn" id="tabletPopupMuteBtn">${this._iconSvg(this._volumeIconName(player))}</button>
        <input class="volume-slider tablet-volume-popup-slider" id="tabletPopupVolSlider" type="range" min="0" max="100" value="${pct}" style="--vol-pct:${pct}%">
        <div class="tablet-volume-popup-value" id="tabletPopupVolPct">${pct}%</div>
      </div>
    `;
    const slider = this.$("tabletPopupVolSlider");
    const muteBtn = this.$("tabletPopupMuteBtn");
    const pctLabel = this.$("tabletPopupVolPct");
    slider?.addEventListener("input", (e) => {
      const nextPct = Math.max(0, Math.min(100, Number(e.target.value || 0)));
      e.target.style.setProperty("--vol-pct", `${nextPct}%`);
      if (player?.entity_id) this._setPlayerVolumeOptimistic(player.entity_id, nextPct / 100, nextPct === 0);
      if (pctLabel) pctLabel.textContent = `${nextPct}%`;
      if (muteBtn) this._setButtonIcon(muteBtn, nextPct === 0 ? "volume_mute" : nextPct < 40 ? "volume_low" : "volume_high");
      clearTimeout(this._volumeTimer);
      this._volumeTimer = setTimeout(() => this._setVolume(nextPct / 100), 90);
    });
    muteBtn?.addEventListener("click", () => this._toggleMute());
    this.classList.add("volume-preset-open");
    this.$("mobileVolumePresetModal")?.classList.add("open");
  }
  _closeMobileVolumePresets() {
    this.$("mobileVolumePresetSheet")?.classList.remove("tablet-volume-sheet-host");
    this.$("mobileVolumePresetModal")?.classList.remove("open");
    this.classList.remove("volume-preset-open");
  }
  _emptyQuickSuggestionLabel(mediaType = "album") {
    const type = String(mediaType || "album").toLowerCase();
    if (type === "playlist") return this._i18n("ui.playlist");
    if (type === "radio") return this._i18n("ui.radio");
    if (type === "track") return this._i18n("ui.track");
    return this._i18n("ui.album");
  }
  _pickRandomItems(items = [], limit = 5) {
    const pool = Array.isArray(items) ? [...items] : [];
    const out = [];
    while (pool.length && out.length < limit) {
      const idx = Math.floor(Math.random() * pool.length);
      out.push(pool.splice(idx, 1)[0]);
    }
    return out;
  }
  _emptyQuickShelfPlayableItems(mediaTypes = []) {
    const allowed = new Set((Array.isArray(mediaTypes) ? mediaTypes : []).map((type) => String(type || "").toLowerCase()).filter(Boolean));
    return (Array.isArray(this._state.emptyQuickShelfItems) ? this._state.emptyQuickShelfItems : []).map((item) => this._normalizeMediaItem(item)).filter((item) => String(item?.uri || "").trim()).filter((item) => !allowed.size || allowed.has(String(item?.media_type || "album").toLowerCase()));
  }
  async _playEmptyQuickShelfPick(mediaTypes = [], toastKey = "", fallbackLabelKey = "ui.quick_play") {
    const pick = this._pickRandomItems(this._emptyQuickShelfPlayableItems(mediaTypes), 1)[0];
    if (!pick?.uri) return false;
    const mediaType = String(pick.media_type || "album").toLowerCase();
    const ok = await this._playMedia(pick.uri, mediaType, "play", {
      label: pick.name || pick.title || this._i18n(fallbackLabelKey),
      silent: true
    });
    if (!ok) return false;
    if (mediaType !== "radio") this._showSurprisePopup(pick);
    const resolvedToastKey = toastKey === "ui.playing_a_random_playlist" && mediaType !== "playlist" ? "ui.playback_started" : toastKey;
    if (resolvedToastKey) this._toastSuccess(this._i18n(resolvedToastKey));
    return true;
  }
  _bindEmptyQuickShelfButtons(host = null) {
    host?.querySelectorAll("[data-empty-media-uri]").forEach((btn) => {
      btn.addEventListener("click", async (e) => {
        const mediaBtn = e.currentTarget;
        await this._runLockedUiAction(mediaBtn, async () => {
          const uri = mediaBtn.dataset.emptyMediaUri || "";
          const mediaType = mediaBtn.dataset.emptyMediaType || "album";
          if (!uri) return;
          this._showEmptyPlaybackLoading(mediaBtn);
          const ok = await this._playMedia(uri, mediaType, "play", {
            label: mediaBtn.getAttribute("title") || "",
            sourceEl: mediaBtn
          });
          if (!ok) this._hideEmptyPlaybackLoading();
        }, { lockMs: 1200 });
      });
    });
  }
  async _renderEmptyQuickShelf() {
    const host = this.$("emptyQuickShelf");
    if (!host) return;
    if (this._state.musicAssistantIssueMessage && !(this._state.players || []).length) {
      this._state.emptyQuickShelfItems = [];
      host.hidden = true;
      host.innerHTML = "";
      return;
    }
    const shelfMode = String(this._state.emptyQuickShelfMode || "default");
    const fixedItems = Array.isArray(this._state.emptyQuickShelfItems) ? this._state.emptyQuickShelfItems : [];
    if (fixedItems.length) {
      host.innerHTML = fixedItems.map((item) => {
        const art = this._artUrl(item) || item?.image || item?.media_item?.image || item?.media_item?.album?.image || "";
        const title = item?.name || item?.title || this._i18n("ui.quick_play");
        const mediaType = item?.media_type || "album";
        return `
          <button class="empty-quick-card" data-empty-media-uri="${this._esc(item.uri)}" data-empty-media-type="${this._esc(mediaType)}" title="${this._esc(title)}">
            <span class="empty-quick-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg(mediaType === "radio" ? "radio" : mediaType === "playlist" ? "queue" : "music")}</span>
            <span class="empty-quick-copy">
              <span class="empty-quick-kicker">${this._esc(this._emptyQuickSuggestionLabel(mediaType))}</span>
              <span class="empty-quick-title">${this._esc(title)}</span>
            </span>
          </button>
        `;
      }).join("");
      host.hidden = false;
      this._bindEmptyQuickShelfButtons(host);
      return;
    }
    const token = `${Date.now()}_${Math.random().toString(36).slice(2, 7)}`;
    this._emptyQuickShelfToken = token;
    if (!host.children.length) host.hidden = true;
    try {
      const results = await Promise.allSettled(
        shelfMode === "radio" ? [
          this._fetchLibrary("radio", "sort_name", 14, true),
          this._fetchLibrary("radio", "sort_name", 40, false),
          this._fetchRadioBrowserStations("", 30, { countryCode: this._mobileRadioBrowserCountry() || "all" }),
          this._fetchLibrary("radio", "random", 18, false)
        ] : [
          this._fetchLibrary("playlist", "sort_name", 14, true),
          this._fetchLibrary("playlist", "sort_name", 40, false),
          this._fetchLibrary("album", "random", 18, false),
          this._fetchLibrary("radio", "sort_name", 8, true)
        ]
      );
      if (this._emptyQuickShelfToken !== token) return;
      if (!this.shadowRoot.querySelector(".card")?.classList.contains("empty-media")) return;
      const [likedPlaylistsRaw, playlistsRaw, albumsRaw, radiosRaw] = results.map(
        (result) => result.status === "fulfilled" && Array.isArray(result.value) ? result.value : []
      );
      const likedPlaylists = likedPlaylistsRaw.map((item) => this._normalizeMediaItem(item));
      const playlists = playlistsRaw.map((item) => this._normalizeMediaItem(item));
      const albums = albumsRaw.map((item) => this._normalizeMediaItem(item));
      const radios = radiosRaw.map((item) => this._normalizeMediaItem(item));
      const unique = [];
      const seenUris = /* @__PURE__ */ new Set();
      const pushUnique = (items = []) => {
        (Array.isArray(items) ? items : []).forEach((item) => {
          const uri = String(item?.uri || "").trim();
          if (!uri || seenUris.has(uri)) return;
          seenUris.add(uri);
          unique.push(item);
        });
      };
      const targetCount = this._layoutModeConfig() === "tablet" ? 10 : 7;
      if (shelfMode === "radio") {
        pushUnique(this._pickRandomItems(likedPlaylists.filter((item) => (item?.media_type || "") === "radio"), Math.min(4, targetCount)));
        pushUnique(this._pickRandomItems(playlists.filter((item) => (item?.media_type || "") === "radio"), Math.min(4, Math.max(1, targetCount - unique.length))));
        pushUnique(this._pickRandomItems(albums.filter((item) => (item?.media_type || "") === "radio"), Math.min(4, Math.max(1, targetCount - unique.length))));
        pushUnique(this._pickRandomItems(radios.filter((item) => (item?.media_type || "") === "radio"), Math.min(4, Math.max(1, targetCount - unique.length))));
        if (unique.length < targetCount) {
          pushUnique(this._pickRandomItems([...likedPlaylists, ...playlists, ...albums, ...radios].filter((item) => (item?.media_type || "") === "radio"), targetCount - unique.length));
        }
      } else {
        pushUnique(this._pickRandomItems(likedPlaylists, Math.min(3, targetCount)));
        pushUnique(this._pickRandomItems(playlists.filter((item) => (item?.media_type || "") === "playlist"), Math.min(3, Math.max(1, targetCount - unique.length))));
        pushUnique(this._pickRandomItems(albums, Math.min(2, Math.max(1, targetCount - unique.length))));
        pushUnique(this._pickRandomItems(radios, Math.min(2, Math.max(0, targetCount - unique.length))));
        if (unique.length < targetCount) {
          pushUnique(this._pickRandomItems([...playlists, ...albums, ...radios], targetCount - unique.length));
        }
      }
      const picks = unique.slice(0, targetCount);
      if (!picks.length) {
        this._state.emptyQuickShelfItems = [];
        host.hidden = true;
        host.innerHTML = "";
        return;
      }
      this._state.emptyQuickShelfItems = picks;
      host.innerHTML = picks.map((item) => {
        const art = this._artUrl(item) || item?.image || item?.media_item?.image || item?.media_item?.album?.image || "";
        const title = item?.name || item?.title || this._i18n("ui.quick_play");
        const mediaType = item?.media_type || "album";
        return `
          <button class="empty-quick-card" data-empty-media-uri="${this._esc(item.uri)}" data-empty-media-type="${this._esc(mediaType)}" title="${this._esc(title)}">
            <span class="empty-quick-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg(mediaType === "radio" ? "radio" : mediaType === "playlist" ? "queue" : "music")}</span>
            <span class="empty-quick-copy">
              <span class="empty-quick-kicker">${this._esc(this._emptyQuickSuggestionLabel(mediaType))}</span>
              <span class="empty-quick-title">${this._esc(title)}</span>
            </span>
          </button>
        `;
      }).join("");
      host.hidden = false;
      this._bindEmptyQuickShelfButtons(host);
    } catch (_) {
      if (this._emptyQuickShelfToken !== token) return;
      this._state.emptyQuickShelfItems = [];
      host.hidden = true;
      host.innerHTML = "";
    }
  }
  _renderEmpty(text = this._i18n("ui.no_active_media"), options = {}) {
    const notice = this.$("mobileNotice");
    if (notice) {
      notice.classList.remove("open");
      notice.textContent = "";
    }
    const card = this.shadowRoot.querySelector(".card");
    const wasEmpty = options.wasEmptyMedia === true || card?.classList.contains("empty-media");
    const disableEmptyAction = options.disableEmptyAction === true;
    const disableShelf = options.disableShelf === true;
    card?.classList.add("empty-media");
    const nextShelfMode = String(options.shelfMode || "default");
    card?.classList.toggle("radio-media", nextShelfMode === "radio");
    const displayArt = this._normalizeArtworkUrl(options.art || "", {
      size: 420,
      cacheKey: options.artCacheKey || `empty:${nextShelfMode}:${text}`
    });
    if (!wasEmpty || this._state.emptyQuickShelfMode !== nextShelfMode) {
      this._state.emptyQuickShelfMode = nextShelfMode;
      this._state.emptyQuickShelfItems = [];
    }
    const emptyActions = this.$("mobileArtActions");
    const configuredEmptyActions = this._mobileEmptyVisibleQuickActions(this._mobileQuickActions());
    const emptyQuickActionsBase = configuredEmptyActions;
    const emptyQuickActionsWithVoice = emptyQuickActionsBase;
    const emptyQuickActions = this._mobileActionsWithAuxiliary(emptyQuickActionsWithVoice).filter((action) => this._layoutModeConfig() !== "tablet" || action !== "history");
    if (emptyActions && emptyQuickActions.length) {
      const emptyClassName = `mobile-art-actions empty-quick-actions count-${Math.max(1, emptyQuickActions.length)}${emptyQuickActions.length === 1 && emptyQuickActions[0] === "home" ? " empty-home-actions" : ""}`;
      const emptySignature = [
        this._layoutModeConfig(),
        emptyQuickActions.join(","),
        this._enabledAuxiliaryButtons().map((button) => `${button.index}:${button.icon}:${button.name}`).join("|"),
        this._voiceAssistantEnabled() ? "voice-on" : "voice-off"
      ].join(";");
      if (emptyActions.className !== emptyClassName) emptyActions.className = emptyClassName;
      if (emptyActions.dataset.homeiiEmptyActionSignature !== emptySignature) {
        emptyActions.dataset.homeiiEmptyActionSignature = emptySignature;
        emptyActions.innerHTML = this._mobileQuickActionButtonsHtml("", emptyQuickActions);
        this._bindMobileQuickActionButtons();
      }
      emptyActions.removeAttribute("hidden");
    } else if (emptyActions) {
      emptyActions.classList.remove("empty-quick-actions");
      emptyActions.classList.remove("empty-home-actions");
      delete emptyActions.dataset.homeiiEmptyActionSignature;
      emptyActions.setAttribute("hidden", "");
    }
    if (this.$("npTitle")) this.$("npTitle").textContent = text;
    this._setNowPlayingSubtitle(options.subtitle || this._i18n("ui.choose_something_from_the_quick_shelf_or_tap_the_wand_for_a_random_playl"), { scrollWhenOverflow: true });
    if (this.$("bigCurTime")) this.$("bigCurTime").textContent = "0:00";
    if (this.$("bigTotalTime")) this.$("bigTotalTime").textContent = "0:00";
    if (this.$("progressFill")) this.$("progressFill").style.width = "0%";
    if (!wasEmpty || !this.$("surpriseMeBtn") || displayArt || disableEmptyAction) {
      const voiceButtonHtml = !disableEmptyAction && this._voiceAssistantEnabled() ? `<button class="empty-voice-btn ${this._state.voiceAssistantListening ? "listening" : ""}" id="emptyVoiceAssistantBtn" title="${this._esc(this._flowAssistantLabel())}" aria-label="${this._esc(this._flowAssistantLabel())}">${this._iconSvg("mic")}</button>` : "";
      const magicVisualHtml = disableEmptyAction ? `<div class="surprise-me-card compact magic-empty disabled ${displayArt ? "has-art" : ""}" id="surpriseMeBtn" aria-hidden="true">
            ${displayArt ? `<img src="${this._esc(displayArt)}" alt="${this._esc(text)}"><span class="surprise-me-wand art-overlay">${this._iconSvg(options.artIcon || "radio")}</span>` : `<span class="surprise-me-glow"></span><span class="surprise-me-wand">${this._iconSvg(options.artIcon || "speaker")}</span>`}
          </div>` : `<button class="surprise-me-card compact magic-empty ${displayArt ? "has-art" : ""}" id="surpriseMeBtn" aria-label="${this._esc(options.artLabel || this._i18n("ui.surprise_me"))}">
            ${displayArt ? `<img src="${this._esc(displayArt)}" alt="${this._esc(text)}"><span class="surprise-me-wand art-overlay">${this._iconSvg(options.artIcon || "radio")}</span>` : `<span class="surprise-me-glow"></span><span class="surprise-me-wand">${this._iconSvg(options.artIcon || "wand")}</span>`}
          </button>`;
      if (this.$("npArt")) this.$("npArt").innerHTML = `
        <div class="empty-magic-stack ${voiceButtonHtml ? "has-voice" : ""}">
          ${magicVisualHtml}
          ${voiceButtonHtml}
        </div>
      `;
      if (!disableEmptyAction) this.$("surpriseMeBtn")?.addEventListener("click", async (e) => {
        e.preventDefault();
        e.stopPropagation();
        this._showEmptyMagicRipple(e.currentTarget);
        await this._runLockedUiAction(e.currentTarget, async () => {
          this._showEmptyPlaybackLoading(e.currentTarget);
          const ok = options.artAction === "radio" ? await this._playRandomRadioStation() : await this._playRandomFromPlaylists();
          if (!ok) this._hideEmptyPlaybackLoading();
        }, { lockMs: 1600 });
      });
      if (!disableEmptyAction) this.$("emptyVoiceAssistantBtn")?.addEventListener("click", (e) => {
        e.preventDefault();
        e.stopPropagation();
        if (!this._lockUiButton(e.currentTarget, [8, 18, 8], { lockMs: 1200, disabled: false })) return;
        this._startVoiceAssistantCommand({ ignoreWhenListening: true });
      });
    }
    if (!emptyActions?.classList.contains("empty-quick-actions")) this._setMobileRandomFabVisible(false);
    this._setMobileRandomFabDisabled(false);
    this._destroyMobileEmbla();
    this._state.mobileArtRenderKey = "";
    if (displayArt) {
      this._syncDynamicThemeArtwork(displayArt).catch(() => {
      });
    } else {
      this._mobileDynamicThemeToken += 1;
      this._state.mobileDynamicThemeArtwork = "";
      this._state.mobileDynamicThemeArtworkUrl = "";
      this._state.mobileDynamicThemePalette = null;
      this._applyDynamicThemeStyles();
    }
    this._syncSourceBadgesUi(null, null);
    this._syncRecentHistoryUi();
    this._setDecodedBackgroundImage(this.$("mobileArtAura"), !this._isHotelMode() ? displayArt : "");
    this._setDecodedBackgroundImage(this.$("mobileHeroAura"), !this._isHotelMode() ? displayArt : "");
    this._setDecodedBackgroundImage(this.$("compactBackdropArt"), !this._isHotelMode() ? displayArt : "");
    this._setDecodedBackgroundImage(this.$("compactCoverAura"), !this._isHotelMode() ? displayArt : "");
    if (disableShelf) {
      this._state.emptyQuickShelfItems = [];
      const shelf = this.$("emptyQuickShelf");
      if (shelf) {
        shelf.hidden = true;
        shelf.innerHTML = "";
      }
    } else if (!wasEmpty || !this.$("emptyQuickShelf")?.children?.length) {
      this._renderEmptyQuickShelf().catch(() => {
      });
    }
    this._updateActivePlayersBubble();
  }
  async _playRandomFromPlaylists() {
    try {
      const inEmptyScreen = !!this.shadowRoot?.querySelector(".card")?.classList.contains("empty-media");
      if (inEmptyScreen && await this._playEmptyQuickShelfPick(["playlist", "album"], "ui.playing_a_random_playlist", "ui.random_playlist")) {
        return true;
      }
      const [allPlaylists, likedPlaylists] = await Promise.allSettled([
        this._fetchLibrary("playlist", "sort_name", 500, false),
        this._fetchLibrary("playlist", "sort_name", 180, true)
      ]);
      const playlists = [
        ...Array.isArray(allPlaylists.value) ? allPlaylists.value : [],
        ...Array.isArray(likedPlaylists.value) ? likedPlaylists.value : []
      ].filter((item) => item?.uri).filter((item, index, list) => list.findIndex((candidate) => candidate?.uri === item?.uri) === index);
      if (!playlists.length) {
        this._toastError(this._i18n("ui.no_playlists_found"));
        return false;
      }
      const pick = playlists[Math.floor(Math.random() * playlists.length)];
      if (!pick?.uri) {
        this._toastError(this._i18n("ui.could_not_choose_media"));
        return false;
      }
      const ok = await this._playMedia(pick.uri, pick.media_type || "playlist", "play", {
        label: pick.name || this._i18n("ui.random_playlist"),
        silent: true
      });
      if (ok) {
        this._showSurprisePopup(pick);
        this._toastSuccess(this._i18n("ui.playing_a_random_playlist"));
      }
      return !!ok;
    } catch (error) {
      this._toastError(error?.message || this._i18n("ui.could_not_start_playback_2"));
      return false;
    }
  }
  async _playRandomRadioStation() {
    try {
      const inEmptyScreen = !!this.shadowRoot?.querySelector(".card")?.classList.contains("empty-media");
      if (inEmptyScreen && await this._playEmptyQuickShelfPick(["radio"], "ui.playing_a_random_radio_station", "ui.random_radio")) {
        return true;
      }
      const [likedRadios, radios, browserRadios] = await Promise.allSettled([
        this._fetchLibrary("radio", "sort_name", 200, true),
        this._fetchLibrary("radio", "sort_name", 400, false),
        this._fetchRadioBrowserStations("", 60, { countryCode: this._mobileRadioBrowserCountry() || "all" })
      ]);
      const stations = [
        ...Array.isArray(likedRadios.value) ? likedRadios.value : [],
        ...Array.isArray(radios.value) ? radios.value : [],
        ...Array.isArray(browserRadios.value) ? browserRadios.value : []
      ].map((item) => this._normalizeMediaItem(item)).filter((item) => String(item?.uri || "").trim() && String(item?.media_type || "radio").toLowerCase() === "radio").filter((item, index, list) => list.findIndex((candidate) => String(candidate?.uri || "").trim() === String(item?.uri || "").trim()) === index);
      if (!stations.length) {
        this._toastError(this._i18n("ui.no_radio_stations_found"));
        return false;
      }
      const pick = stations[Math.floor(Math.random() * stations.length)];
      const ok = await this._playMedia(pick.uri, "radio", "play", {
        label: pick.name || this._i18n("ui.random_radio"),
        silent: true
      });
      if (ok) this._toastSuccess(this._i18n("ui.playing_a_random_radio_station"));
      return !!ok;
    } catch (error) {
      this._toastError(error?.message || this._i18n("ui.could_not_start_radio_playback"));
      return false;
    }
  }
  _renderRadioHero() {
    const artHost = this.$("npArt");
    if (!artHost) return;
    artHost.innerHTML = `
      <div class="radio-stage">
        <span class="radio-stage-card radio-stage-card-side"></span>
        <span class="radio-stage-card radio-stage-card-main"></span>
        <span class="radio-stage-card radio-stage-card-side radio-stage-card-side-end"></span>
        <button class="radio-stage-fab" id="radioHeroBtn" aria-label="${this._esc(this._i18n("ui.random_radio"))}">
          ${this._iconSvg("radio")}
        </button>
      </div>
    `;
    this.$("radioHeroBtn")?.addEventListener("click", async (e) => {
      const feedbackEl = this._showLibraryInteractionFeedback(e.currentTarget, { loading: true, hold: true });
      try {
        await this._playRandomRadioStation();
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
    });
  }
  _renderError(error) {
    const notice = this.$("mobileNotice");
    if (notice) {
      notice.classList.add("open");
      notice.textContent = error?.message || String(error || "Unknown error");
    }
  }
  _clearNotice() {
    const notice = this.$("mobileNotice");
    if (!notice) return;
    notice.textContent = "";
    notice.classList.remove("open");
  }
  _syncScreensaverDynamicArtwork() {
    const player = this._getSelectedPlayer();
    const art = this._currentArtworkUrl(player, this._state.maQueueState?.current_item || null, 720, { preferPlayerArtwork: true });
    this._syncDynamicThemeArtwork(art || "").catch(() => {
    });
  }
  _syncNowPlayingUI() {
    this._syncSleepTimerState();
    this._syncNightModeUi();
    const player = this._getSelectedPlayer();
    const currentQueueItem = this._state.maQueueState?.current_item || null;
    if (this._lyricsSessionActive?.()) this._syncLyricsForCurrentTrack();
    if (this._state.screensaverOpen) {
      this._syncScreensaverDynamicArtwork();
      this._syncScreensaverUi();
      this._syncAmbientLightForCurrentMedia("screensaver");
      this._syncLocalSendspinMediaSession(player, currentQueueItem);
      return;
    }
    if (!player) {
      this._syncControlRoomUi();
      this._resetLocalSendspinMediaSession();
    }
    const compactTileMode = this._isCompactTileMode();
    const syncMobileVolumeControls = () => {
      const volumePct = Math.max(0, Math.min(100, Math.round(this._effectivePlayerVolumeLevel(player) * 100)));
      const slider = this.$("volSlider");
      if (slider) {
        slider.value = volumePct;
        slider.style.setProperty("--vol-pct", `${volumePct}%`);
      }
      const label = this.$("mobileVolPctLabel");
      if (label) label.textContent = `${volumePct}%`;
      this._setButtonIcon(this.$("btnMute"), this._volumeIconName(player));
      this.$("btnMute")?.classList.toggle("active", this._isMuted(player));
      this.$("btnMute")?.classList.toggle("muted", this._isMuted(player));
      return volumePct;
    };
    const renderCompactTile = ({ title = "", subtitle = "", art: art2 = "", icon = "music_note", duration: duration2 = 0, position: position2 = 0, emptyAction = "", upNextItem: upNextItem2 = null, sourceQueueItem = null }) => {
      if (!compactTileMode) return false;
      const artHost = this.$("npArt");
      const artImage = this.$("compactCoverImage");
      const compactBackdrop = this.$("compactBackdropArt");
      const compactCoverAura = this.$("compactCoverAura");
      const bg2 = this.$("mobileBg");
      const browseStack = this._mobileArtStackItems();
      const browseOffset = Number(browseStack.offset || 0);
      const compactBrowsePreview = this._mobileSwipeMode() === "browse" && browseOffset !== 0;
      const browseArtists = Array.isArray(browseStack.current?.media_item?.artists) ? browseStack.current.media_item.artists.map((artist) => artist?.name).filter(Boolean).join(", ") : "";
      const browseTitle = browseStack.current?.media_item?.name || browseStack.current?.media_title || browseStack.current?.name || title;
      const browseArtist = browseStack.current?.media_artist || browseStack.current?.artist_str || browseArtists || browseStack.current?.artist || "";
      const browseAlbum = browseStack.current?.media_item?.album?.name || browseStack.current?.media_album_name || browseStack.current?.album || "";
      const displayTitle = compactBrowsePreview ? browseTitle || title : title;
      const displaySubtitle = compactBrowsePreview ? [browseArtist, browseAlbum].filter(Boolean).join(" · ") || subtitle : subtitle;
      const displayQueueItem2 = compactBrowsePreview ? browseStack.current || sourceQueueItem : sourceQueueItem;
      const browsePreviewArt = this._queueItemArtworkUrl(browseStack.current, 420, player);
      const effectiveArt = compactBrowsePreview ? browsePreviewArt || art2 : art2;
      const displayArt = this._normalizeArtworkUrl(effectiveArt || "", {
        size: 420,
        cacheKey: this._queueItemArtworkCacheKey(displayQueueItem2) || this._currentArtworkCacheKey(player, displayQueueItem2)
      });
      const overlay = this._mobileBackdropOverlay(this._effectiveTheme());
      if (artHost) {
        const displayQueueItemId = displayQueueItem2 ? this._getQueueItemStableId(displayQueueItem2) || this._getQueueItemKey(displayQueueItem2) : "";
        const displayQueueUri = displayQueueItem2 ? this._getQueueItemUri(displayQueueItem2) : "";
        const displaySortIndex = displayQueueItem2 && Number.isFinite(Number(displayQueueItem2?.sort_index)) ? String(displayQueueItem2.sort_index) : "";
        artHost.classList.toggle("placeholder", !displayArt);
        artHost.classList.remove("brand-fallback");
        artHost.style.backgroundImage = "";
        artHost.dataset.emptyAction = compactBrowsePreview ? "" : emptyAction || "";
        artHost.dataset.compactBrowseOffset = compactBrowsePreview ? String(browseOffset) : "0";
        artHost.dataset.queueItemId = compactBrowsePreview ? displayQueueItemId : "";
        artHost.dataset.uri = compactBrowsePreview ? displayQueueUri : "";
        artHost.dataset.type = compactBrowsePreview ? displayQueueItem2?.media_item?.media_type || displayQueueItem2?.media_type || "track" : "";
        artHost.dataset.sortIndex = compactBrowsePreview ? displaySortIndex : "";
      }
      if (artImage) {
        this._setDecodedArtworkImage(artImage, displayArt, displayTitle || this._i18n("ui.artwork"));
      }
      this._syncDynamicThemeArtwork(displayArt || effectiveArt || "").catch(() => {
      });
      this._setDecodedBackgroundImage(compactBackdrop, !this._isHotelMode() ? displayArt : "");
      this._setDecodedBackgroundImage(compactCoverAura, !this._isHotelMode() ? displayArt : "");
      this._setDecodedBackgroundCrossfade(bg2, !this._isHotelMode() ? displayArt : "", overlay);
      if (this.$("npTitle")) this.$("npTitle").textContent = displayTitle || this._i18n("ui.nothing_playing");
      this._setNowPlayingSubtitle(displaySubtitle || "—");
      this._syncMobileUpNextUi(upNextItem2);
      this._syncSourceBadgesUi(player, displayQueueItem2);
      if (this.$("progressFill")) this.$("progressFill").style.width = duration2 ? `${Math.min(100, position2 / duration2 * 100)}%` : "0%";
      if (this.$("bigCurTime")) this.$("bigCurTime").textContent = this._fmtDur(position2);
      if (this.$("bigTotalTime")) this.$("bigTotalTime").textContent = this._fmtDur(duration2);
      this._renderPlayerSummary();
      this._syncStatus();
      this._syncLikeButtons();
      this._updateActivePlayersBubble();
      return true;
    };
    if (!player) {
      this._syncMobileUpNextUi(null);
      this._syncSourceBadgesUi(null, null);
      this._syncRecentHistoryUi();
      const issue = this._state.musicAssistantIssueMessage || this._musicAssistantRequiredMessage();
      const shelf = this.$("emptyQuickShelf");
      if (shelf) {
        shelf.hidden = true;
        shelf.innerHTML = "";
      }
      if (renderCompactTile({
        title: this._musicAssistantRequiredTitle(),
        subtitle: issue,
        art: "",
        icon: "speaker",
        emptyAction: ""
      })) return;
      this._renderEmpty(this._musicAssistantRequiredTitle(), {
        subtitle: issue,
        artIcon: "speaker",
        disableEmptyAction: true,
        disableShelf: true
      });
      this._syncStatus();
      this._updateActivePlayersBubble();
      this._resetLocalSendspinMediaSession();
      return;
    }
    const cardRoot = this.shadowRoot.querySelector(".card");
    const wasEmptyMedia = !!cardRoot?.classList.contains("empty-media");
    cardRoot?.classList.remove("empty-media");
    cardRoot?.classList.remove("radio-media");
    this._hideEmptyPlaybackLoading();
    this.$("activePlayerChip")?.classList.toggle("playing", player.state === "playing");
    this._clearNotice();
    this._setButtonIcon(this.$("btnPlay"), this._playPauseIconName(player));
    this.$("btnPlay")?.classList.toggle("is-playing", player.state === "playing");
    this._setButtonIcon(this.$("btnMute"), this._volumeIconName(player));
    this._setButtonIcon(this.$("controlVolumeBtn"), this._volumeIconName(player));
    this.$("controlVolumeBtn")?.classList.toggle("muted", this._isMuted(player));
    this._setButtonIcon(this.$("mobileShuffleBtn"), "shuffle");
    const repeatMode = player.attributes.repeat || "off";
    const repeatLabel = this._repeatModeLabel(repeatMode);
    this._setButtonIcon(this.$("mobileRepeatBtn"), repeatMode === "one" ? "repeat_one" : "repeat");
    this.$("mobileShuffleBtn")?.classList.toggle("active", !!player.attributes.shuffle);
    const mobileRepeat = this.$("mobileRepeatBtn");
    if (mobileRepeat) {
      mobileRepeat.classList.toggle("active", repeatMode !== "off");
      mobileRepeat.dataset.repeatMode = repeatMode;
      mobileRepeat.title = repeatLabel;
      mobileRepeat.setAttribute("aria-label", repeatLabel);
    }
    const tabletPopupSlider = this.$("tabletPopupVolSlider");
    const tabletPopupMuteBtn = this.$("tabletPopupMuteBtn");
    const tabletPopupVolPct = this.$("tabletPopupVolPct");
    const tabletVol = Math.max(0, Math.min(100, Math.round(this._effectivePlayerVolumeLevel(player) * 100)));
    if (tabletPopupSlider) {
      tabletPopupSlider.value = tabletVol;
      tabletPopupSlider.style.setProperty("--vol-pct", `${tabletVol}%`);
    }
    if (tabletPopupVolPct) tabletPopupVolPct.textContent = `${tabletVol}%`;
    if (tabletPopupMuteBtn) this._setButtonIcon(tabletPopupMuteBtn, this._volumeIconName(player));
    const currentAnchorItem = this._state.maQueueState?.current_item || null;
    const currentAnchorKey = this._getQueueItemStableId(currentAnchorItem) || this._getQueueItemUri(currentAnchorItem) || `${player.entity_id}:${player.attributes.media_content_id || player.attributes.media_title || ""}`;
    if (this._state.mobileArtAnchorKey !== currentAnchorKey) {
      this._state.mobileArtAnchorKey = currentAnchorKey;
      this._state.mobileArtBrowseOffset = 0;
    }
    const stack = this._mobileArtStackItems();
    const displaySource = this._mobileNowPlayingDisplaySource(player, currentQueueItem, stack);
    const displayQueueItem = displaySource.queueItem || currentQueueItem;
    const currentMedia = displaySource.media || {};
    const hasPendingPlay = displaySource.hasPendingPlay;
    const playerUri = String(player.attributes.media_content_id || "").trim();
    const queueUri = String(displaySource.uri || this._getQueueItemUri(displayQueueItem) || "").trim();
    const currentTitle = displaySource.title;
    const currentArtist = displaySource.artist;
    const currentAlbum = displaySource.album;
    const currentMediaType = displaySource.mediaType;
    const upNextItem = this._mobileUpNextItem();
    const forceRadioHero = !!this._state.forceRadioHero;
    const likelyRadioPlayback = this._isLikelyRadioPlayback(player, displayQueueItem, currentMedia);
    const hasPlayableMedia = !!(player.attributes.media_title || player.attributes.media_content_id || displayQueueItem?.name || currentMedia?.name);
    const quickMixPending = Number(this._state.quickMixPendingUntil || 0) > Date.now() ? this._state.quickMixPendingEntry : null;
    if (hasPlayableMedia && this._state.quickMixPendingUntil) {
      this._state.quickMixPendingUntil = 0;
      this._state.quickMixPendingEntry = null;
    }
    if (!hasPlayableMedia && quickMixPending) {
      const pendingTitle = quickMixPending.name || this._i18n("ui.quick_mix");
      const pendingArt = this._normalizeArtworkUrl(quickMixPending.image || this._currentArtworkUrl(player, currentQueueItem, 420, { preferPlayerArtwork: true }), {
        size: 420,
        cacheKey: `quick-mix:${quickMixPending.uri || pendingTitle}`
      });
      this.$("mobileArtActions")?.removeAttribute("hidden");
      this._setMobileRandomFabVisible(true);
      this._setMobileRandomFabDisabled(true);
      if (renderCompactTile({
        title: pendingTitle,
        subtitle: this._i18n("ui.starting_quick_mix"),
        art: pendingArt,
        icon: "radio",
        duration: 0,
        position: 0,
        upNextItem: null,
        sourceQueueItem: displayQueueItem
      })) {
        syncMobileVolumeControls();
        return;
      }
      if (this.$("npTitle")) this.$("npTitle").textContent = pendingTitle;
      this._setNowPlayingSubtitle(this._i18n("ui.starting_quick_mix"));
      if (this.$("npArt")) {
        this.$("npArt").innerHTML = this._decodedArtworkImgHtml(pendingArt, pendingTitle, { current: true, fallbackIcon: "radio" });
        this._hydrateDecodedArtworkImages(this.$("npArt"));
      }
      if (this.$("progressFill")) this.$("progressFill").style.width = "0%";
      this._syncDynamicThemeArtwork(pendingArt || "").catch(() => {
      });
      syncMobileVolumeControls();
      this._syncControlRoomUi();
      return;
    }
    if (!hasPlayableMedia) {
      this._state.forceRadioHero = false;
      this._syncSourceBadgesUi(null, null);
      this._syncRecentHistoryUi();
      if (renderCompactTile({
        title: this._i18n("ui.player_is_ready"),
        subtitle: player.attributes?.friendly_name || this._i18n("ui.nothing_is_playing_right_now"),
        art: displaySource.art || this._displayArtworkForQueueItem(player, displayQueueItem, { pending: hasPendingPlay, size: 420 }),
        duration: 0,
        position: 0,
        emptyAction: "random",
        upNextItem: null,
        sourceQueueItem: displayQueueItem
      })) {
        this.$("btnPlay")?.classList.remove("is-playing");
        syncMobileVolumeControls();
        return;
      }
      this._renderEmpty(this._i18n("ui.player_is_ready_nothing_is_playing_right_now"), { wasEmptyMedia });
      this._syncMobileUpNextUi(null);
      this.$("btnPlay")?.classList.remove("is-playing");
      this._renderPlayerSummary();
      this._syncStatus();
      this._syncLikeButtons();
      this._updateActivePlayersBubble();
      this._syncControlRoomUi();
      this._syncLocalSendspinMediaSession(player, displayQueueItem);
      return;
    }
    this._syncActiveQuickActionRow();
    const isRadioNowPlaying = currentMediaType === "radio" || forceRadioHero || likelyRadioPlayback;
    if (isRadioNowPlaying) {
      cardRoot?.classList.add("radio-media");
    }
    this._state.forceRadioHero = false;
    const emptyShelf = this.$("emptyQuickShelf");
    if (emptyShelf) {
      emptyShelf.hidden = true;
      emptyShelf.innerHTML = "";
    }
    this.$("mobileArtActions")?.removeAttribute("hidden");
    this._setMobileRandomFabVisible(true);
    this._setMobileRandomFabDisabled(false);
    if (this.$("npTitle")) this.$("npTitle").textContent = currentTitle;
    this._setNowPlayingSubtitle([currentArtist || player.attributes.media_artist || "", currentAlbum].filter(Boolean).join(" · ") || "—");
    this._rememberRecentPlayback(player, displayQueueItem);
    this._syncRecentHistoryUi();
    this._syncSourceBadgesUi(player, displayQueueItem);
    this._syncMobileUpNextUi(upNextItem);
    const playingArt = displaySource.art || this._displayArtworkForQueueItem(player, displayQueueItem, { pending: hasPendingPlay, size: 420 });
    const previewArt = this._queueItemArtworkUrl(stack.current, 420, player) || playingArt;
    const art = this._mobileSwipeMode() === "browse" && stack.offset !== 0 ? playingArt : hasPendingPlay ? playingArt : Number(stack.offset || 0) !== 0 && previewArt ? previewArt : playingArt;
    const duration = this._getCurrentDuration();
    const position = this._getCurrentPosition();
    if (renderCompactTile({
      title: currentTitle,
      subtitle: [currentArtist || player.attributes.media_artist || "", currentAlbum].filter(Boolean).join(" · ") || "—",
      art,
      duration,
      position,
      upNextItem,
      sourceQueueItem: displayQueueItem
    })) {
      syncMobileVolumeControls();
      this._syncControlRoomUi();
      return;
    }
    this._refreshMobileArtStack();
    const bg = this.$("mobileBg");
    if (bg) {
      const overlay = this._mobileBackdropOverlay(this._effectiveTheme());
      this._setDecodedBackgroundCrossfade(bg, !this._isHotelMode() ? art : "", overlay);
    }
    this._syncDynamicThemeArtwork(art || "").catch(() => {
    });
    const vol = Math.round(this._effectivePlayerVolumeLevel(player) * 100);
    if (this.$("volSlider")) {
      this.$("volSlider").value = vol;
      this.$("volSlider").style.setProperty("--vol-pct", `${vol}%`);
    }
    this.$("btnMute")?.classList.toggle("active", this._isMuted(player));
    this.$("btnMute")?.classList.toggle("muted", this._isMuted(player));
    const volLabel = this.$("mobileVolPctLabel");
    if (volLabel) volLabel.textContent = `${vol}%`;
    if (this.$("progressFill")) this.$("progressFill").style.width = duration ? `${Math.min(100, position / duration * 100)}%` : "0%";
    if (this.$("bigCurTime")) this.$("bigCurTime").textContent = this._fmtDur(position);
    if (this.$("bigTotalTime")) this.$("bigTotalTime").textContent = this._fmtDur(duration);
    const nowPlayingUri = hasPendingPlay ? queueUri || playerUri || "" : playerUri || queueUri || "";
    if (nowPlayingUri !== this._state.nowPlayingUri) {
      this._state.nowPlayingUri = nowPlayingUri;
      this._highlightNowPlaying();
    }
    this._renderPlayerSummary();
    this._syncStatus();
    this._syncLikeButtons();
    this._updateActivePlayersBubble();
    this._syncControlRoomUi();
    if (this._state.screensaverOpen) this._syncScreensaverUi();
    this._syncLocalSendspinMediaSession(player, displayQueueItem);
  }
  _syncNowPlayingPageLive() {
    this._syncNowPlayingUI();
  }
  _mobileMenuScrollTargets(body = this.$("mobileMenuBody")) {
    if (!body) return [];
    const candidates = [
      ["body", body],
      ["libraryBody", body.querySelector?.(".library-body")],
      ["mediaResults", body.querySelector?.("#mobileMediaSearchResults")],
      ["queueList", body.querySelector?.(".queue-list")]
    ];
    const seen = /* @__PURE__ */ new Set();
    return candidates.filter(([, el]) => {
      if (!el || seen.has(el)) return false;
      seen.add(el);
      return true;
    });
  }
  _captureMobileMenuScroll(page = this._state.menuPage || "main") {
    const body = this.$("mobileMenuBody");
    if (!body) return null;
    const positions = {};
    this._mobileMenuScrollTargets(body).forEach(([key, el]) => {
      positions[key] = { top: el.scrollTop || 0, left: el.scrollLeft || 0 };
    });
    return { page: String(page || "main"), positions };
  }
  _rememberMobileMenuScroll(page = this._state.menuPage || "main") {
    const snapshot = this._captureMobileMenuScroll(page);
    if (!snapshot) return;
    if (!this._state.mobileMenuScrollPositions || typeof this._state.mobileMenuScrollPositions !== "object") {
      this._state.mobileMenuScrollPositions = {};
    }
    this._state.mobileMenuScrollPositions[snapshot.page] = snapshot.positions || {};
    if (snapshot.page === "settings") this._state.mobileSettingsScrollTop = snapshot.positions?.body?.top || 0;
  }
  _restoreMobileMenuScrollSnapshot(snapshot = null, page = this._state.menuPage || "main") {
    const key = String(page || snapshot?.page || "main");
    const positions = snapshot?.positions || this._state.mobileMenuScrollPositions?.[key] || {};
    const apply = () => {
      const body = this.$("mobileMenuBody");
      if (!body) return;
      this._mobileMenuScrollTargets(body).forEach(([targetKey, el]) => {
        const saved = positions[targetKey];
        if (!saved) return;
        const maxTop = Math.max(0, el.scrollHeight - el.clientHeight);
        const maxLeft = Math.max(0, el.scrollWidth - el.clientWidth);
        const nextTop = Math.min(Math.max(0, Number(saved.top || 0) || 0), maxTop);
        const nextLeft = Math.min(Math.max(0, Number(saved.left || 0) || 0), maxLeft);
        try {
          el.scrollTo({ top: nextTop, left: nextLeft, behavior: "auto" });
        } catch (_) {
          el.scrollTop = nextTop;
          el.scrollLeft = nextLeft;
        }
        el.scrollTop = nextTop;
        el.scrollLeft = nextLeft;
      });
    };
    apply();
    requestAnimationFrame(() => {
      apply();
      requestAnimationFrame(apply);
    });
    setTimeout(apply, 80);
    setTimeout(apply, 180);
  }
  _restoreMobileMenuScroll(scrollTop = this._state.mobileSettingsScrollTop || 0, page = this._state.menuPage || "main") {
    const key = String(page || "main");
    const positions = { ...this._state.mobileMenuScrollPositions?.[key] || {} };
    if (Number.isFinite(Number(scrollTop))) {
      positions.body = { ...positions.body || {}, top: Math.max(0, Number(scrollTop || 0) || 0) };
    }
    if (key === "settings") this._state.mobileSettingsScrollTop = positions.body?.top || 0;
    this._restoreMobileMenuScrollSnapshot({ page: key, positions }, key);
  }
  _normalizeMobileMenuPage(page = "main") {
    const raw = String(page || "main").trim().toLowerCase();
    const aliases = {
      actions: "main",
      menu: "main",
      player: "players",
      player_select: "players",
      select_player: "players",
      speaker: "players",
      speakers: "players",
      speaker_select: "players",
      active_players: "players_active"
    };
    const normalized = aliases[raw] || raw || "main";
    if (this._isHotelMode()) {
      const allowed = /* @__PURE__ */ new Set(["main", "players", "players_active", "quick_search", "library_search", "media_detail"]);
      return allowed.has(normalized) || String(normalized).startsWith("library_search") ? normalized : "main";
    }
    return normalized;
  }
  _restoreMobileMenuAfterBuild(reason = "build") {
    if (!this._state.menuOpen) return;
    this._state.menuPage = this._normalizeMobileMenuPage(this._state.menuPage || "main");
    this.$("mobileMenu")?.classList.add("open");
    this._renderMobileMenu().catch((error) => {
      this._debugLog?.("warn", `[Homeii Menu] failed to restore mobile menu after ${reason}`, error);
    });
  }
  _reopenSettingsMenuPreservingScroll({ rebuild = false, init = false } = {}) {
    const body = this.$("mobileMenuBody");
    const scrollTop = Math.max(0, Number(body?.scrollTop ?? this._state.mobileSettingsScrollTop ?? 0) || 0);
    this._state.mobileSettingsScrollTop = scrollTop;
    if (rebuild) this._build();
    if (init) this._init();
    this._openMobileMenu("settings", { scrollTop });
    this._restoreMobileMenuScroll(scrollTop, "settings");
  }
  _openMobileMenu(page = "main", options = {}) {
    const nextPage = this._normalizeMobileMenuPage(page);
    if (nextPage === "settings" && this._usesVisualSettings()) {
      this._toastSuccess(this._i18n("ui.card_settings_are_managed_from_the_visual_editor"));
      return;
    }
    const wasOpen = !!this._state.menuOpen;
    const previousPage = this._state.menuPage || "main";
    if (nextPage === "simple_wizard" && previousPage !== "simple_wizard") this._resetSimpleWizardState();
    if (nextPage === "discovery" && previousPage !== "discovery") this._startDiscoverySession();
    if (wasOpen) this._rememberMobileMenuScroll(previousPage);
    const hasExplicitScrollTop = options?.scrollTop !== void 0 && options?.scrollTop !== null;
    const explicitScrollTop = Number(options?.scrollTop);
    const rememberedSettingsScrollTop = nextPage === "settings" ? Number(this._state.mobileSettingsScrollTop || 0) : null;
    const samePageScrollTop = wasOpen && previousPage === nextPage ? this.$("mobileMenuBody")?.scrollTop || 0 : null;
    const restoreScrollTop = hasExplicitScrollTop && Number.isFinite(explicitScrollTop) ? explicitScrollTop : Number.isFinite(samePageScrollTop) && samePageScrollTop > 0 ? samePageScrollTop : Number.isFinite(rememberedSettingsScrollTop) && rememberedSettingsScrollTop > 0 ? rememberedSettingsScrollTop : samePageScrollTop;
    this._state.menuOpen = true;
    this._state.menuPage = nextPage;
    if (!String(nextPage || "").startsWith("library_") && nextPage !== "media_detail") {
      this._state.mobileMediaLayoutManual = false;
    }
    this._applyLibraryDefaultLayoutForPage(this._state.menuPage);
    if (nextPage === "main" || nextPage === "settings" || String(nextPage).startsWith("library_")) this._state.menuStack = [];
    if (this._isManualFrontContentSelectionPage(nextPage)) {
      this._refreshManualFrontPlayerHold(this._manualFrontContentHoldMs());
    }
    this.$("mobileMenu")?.classList.add("open");
    this._syncCompactMenuOverlayState();
    const renderTask = Promise.resolve(this._renderMobileMenu()).catch((error) => {
      this._debugLog?.("warn", "[Homeii Menu] failed to render mobile menu", error);
      this._state.menuOpen = false;
      this._state.menuPage = "main";
      this.$("mobileMenu")?.classList.remove("open", "search-open", "discovery-open", "action-fullscreen-open", "library-fullscreen-open");
      this._syncCompactMenuOverlayState();
      this._toastError(error?.message || this._i18n("ui.try_again"));
    });
    if (restoreScrollTop !== null) {
      renderTask.finally(() => {
        this._restoreMobileMenuScroll(restoreScrollTop, nextPage);
      });
    }
  }
  _closeMobileMenu() {
    this._state.menuOpen = false;
    this._state.menuPage = "main";
    this._state.menuStack = [];
    this._state.mobileMediaLayoutManual = false;
    this._shortenManualFrontPlayerHold(this._manualFrontDefaultHoldMs());
    this._closeMobileQueueActionMenu();
    clearTimeout(this._mobileQueueArtworkPrefetchTimer);
    this._mobileQueueArtworkPrefetchTimer = null;
    this._closeSmartVoiceConfirm();
    this._syncCompactMenuOverlayState();
    this.$("mobileMenu")?.classList.remove("open", "search-open", "discovery-open", "action-fullscreen-open", "library-fullscreen-open");
    this.$("homeShortcutFab")?.removeAttribute("hidden");
    this.$("mobileMenuBody")?.classList.remove("search-mode", "library-mode");
    if (this._state.controlRoomRestoreAfterMenu && this._controlRoomEnabled()) {
      this._state.controlRoomRestoreAfterMenu = false;
      this._state.controlRoomPanel = "";
      this._state.controlRoomOpen = true;
      this._syncControlRoomUi();
    }
  }
  _backMobileMenu() {
    if (this._state.menuPage === "media_detail" && Array.isArray(this._state.mobileLibraryDetailStack) && this._state.mobileLibraryDetailStack.length) {
      const previousDetail = this._state.mobileLibraryDetailStack.pop();
      const scrollSnapshot = previousDetail?._homeiiScrollSnapshot || null;
      const parentPage = previousDetail?._homeiiParentPage || this._libraryDetailParentPageForType(previousDetail?.media_type);
      const { _homeiiScrollSnapshot, _homeiiParentPage, ...detail } = previousDetail || {};
      this._state.mobileLibraryDetail = detail;
      this._state.mobileLibraryDetailParentPage = parentPage;
      this._syncCompactMenuOverlayState();
      this._renderMobileMenu();
      if (scrollSnapshot) {
        requestAnimationFrame(() => this._restoreMobileMenuScrollSnapshot(scrollSnapshot, "media_detail"));
      }
      return;
    }
    const prevRaw = this._state.menuStack.pop();
    if (!prevRaw) return this._closeMobileMenu();
    const prev = this._normalizeMobileMenuPage(prevRaw);
    this._state.menuPage = prev;
    this._applyLibraryDefaultLayoutForPage(this._state.menuPage);
    this._syncCompactMenuOverlayState();
    this._renderMobileMenu();
  }
  _pushMobileMenu(page) {
    const nextPage = this._normalizeMobileMenuPage(page);
    if (!nextPage || nextPage === this._state.menuPage) return;
    if (nextPage === "simple_wizard") this._resetSimpleWizardState();
    if (nextPage === "discovery") this._startDiscoverySession();
    this._state.menuStack.push(this._state.menuPage);
    this._state.menuPage = nextPage;
    if (!String(nextPage || "").startsWith("library_") && nextPage !== "media_detail") {
      this._state.mobileMediaLayoutManual = false;
    }
    this._applyLibraryDefaultLayoutForPage(this._state.menuPage);
    if (this._isManualFrontContentSelectionPage(nextPage)) {
      this._refreshManualFrontPlayerHold(this._manualFrontContentHoldMs());
    }
    this._syncCompactMenuOverlayState();
    this._renderMobileMenu();
  }
  _applyLibraryDefaultLayoutForPage(page = this._state.menuPage) {
    if (!String(page || "").startsWith("library_")) return;
    if (this._state.mobileMediaLayoutManual === true) return;
    this._state.mobileMediaLayout = this._defaultMobileMediaLayout();
  }
  _isPhoneActionFullscreenMenuPage(page = "") {
    if (String(page || "") === "media_detail") return true;
    if (String(page || "").startsWith("library_")) return true;
    return (/* @__PURE__ */ new Set([
      "main",
      "simple_wizard",
      "quick_search",
      "players",
      "players_active",
      "sleep_timer",
      "announcements",
      "queue",
      "transfer",
      "group",
      "ungroup_all",
      "stop_all"
    ])).has(String(page || ""));
  }
  _compactMenuOverlayOpen() {
    if (!this._isCompactTileMode() || !this._state?.menuOpen || !this._isPhoneActionFullscreenMenuPage(this._state.menuPage)) return false;
    const menu = this.$("mobileMenu");
    return !!menu?.classList?.contains("open");
  }
  _syncCompactMenuOverlayState() {
    const open = this._compactMenuOverlayOpen();
    this.classList.toggle("compact-menu-open", open);
    this.shadowRoot?.querySelector(".card")?.classList.toggle("compact-menu-open", open);
  }
  _isMobileSearchPage(page = this._state.menuPage) {
    return ["library_search", "quick_search"].includes(String(page || ""));
  }
  _navMenuItem(page, icon, title, subtitle = "", tone = "default") {
    return `
      <button class="menu-item action-tile tone-${this._esc(tone)}" data-menu-nav="${this._esc(page)}">
        <span class="menu-item-main">
          <span class="menu-item-ico">${icon}</span>
          <span style="min-width:0;flex:1;">
            <span class="menu-item-title">${this._esc(title)}</span>
            <span class="menu-item-sub">${this._esc(subtitle)}</span>
          </span>
        </span>
      </button>
    `;
  }
  _createSimpleWizardState(overrides = {}) {
    return {
      step: "players",
      selectedPlayers: [],
      source: "genre",
      genre: "pop",
      customGenre: "",
      contentType: "playlist",
      query: "",
      candidates: [],
      selectedIndex: 0,
      loading: false,
      error: "",
      ...overrides
    };
  }
  _resetSimpleWizardState() {
    this._simpleWizardToken = (this._simpleWizardToken || 0) + 1;
    this._state.simpleWizard = this._createSimpleWizardState({ selectedPlayers: this._simpleWizardDefaultPlayerIds() });
  }
  _simpleWizardPlayerPool() {
    this._loadPlayers();
    const players = Array.isArray(this._state.players) ? this._state.players : [];
    const pinnedEntities = new Set(this._resolvedPinnedPlayerEntities(players));
    return players.filter((player) => player?.entity_id).filter((player) => !(typeof this._isLikelyBrowserPlayer === "function" && this._isLikelyBrowserPlayer(player))).filter((player) => !pinnedEntities.size || pinnedEntities.has(player.entity_id) || this._isLocalSendspinPlayer(player));
  }
  _simpleWizardDefaultPlayerIds(players = this._simpleWizardPlayerPool()) {
    const selected = String(this._state.selectedPlayer || "").trim();
    if (selected && players.some((player) => player.entity_id === selected)) return [selected];
    const active = players.find((player) => this._isPlayerActive(player));
    return (active || players[0])?.entity_id ? [(active || players[0]).entity_id] : [];
  }
  _simpleWizardState() {
    const base = this._state.simpleWizard && typeof this._state.simpleWizard === "object" ? this._state.simpleWizard : this._createSimpleWizardState();
    const defaults = this._createSimpleWizardState();
    Object.keys(defaults).forEach((key) => {
      if (base[key] === void 0) base[key] = defaults[key];
    });
    const players = this._simpleWizardPlayerPool();
    const validIds = new Set(players.map((player) => player.entity_id));
    const selectedPlayers = (Array.isArray(base.selectedPlayers) ? base.selectedPlayers : []).map((entityId) => String(entityId || "").trim()).filter((entityId, index, list) => entityId && validIds.has(entityId) && list.indexOf(entityId) === index);
    if (!selectedPlayers.length) selectedPlayers.push(...this._simpleWizardDefaultPlayerIds(players));
    base.selectedPlayers = selectedPlayers;
    base.source = base.source === "content" ? "content" : "genre";
    base.genre = this._simpleWizardGenres().some((genre) => genre.id === base.genre) ? base.genre : "pop";
    base.contentType = this._simpleWizardContentTypes().some((type) => type.id === base.contentType) ? base.contentType : "playlist";
    this._state.simpleWizard = base;
    return base;
  }
  _musicStyleCatalog(options = {}) {
    const t = (en, he, de = en) => this._discoveryGenreLabel(en, he, de);
    const style = (id, icon, label, subtitle, queries, extra = {}) => ({
      id,
      key: id,
      icon,
      label,
      subtitle,
      query: queries?.[0] || label,
      queries,
      ...extra
    });
    const styles = [
      style("pop", "music_note", "Pop", t("Popular hits", "להיטי פופ"), ["pop hits playlist", "top pop playlist", "pop music"]),
      style("indie-pop", "music_note", "Indie Pop", t("Fresh alternative pop", "פופ אינדי"), ["indie pop playlist", "fresh indie pop", "alternative pop"]),
      style("k-pop", "music_note", "K-Pop", t("Korean pop", "קיי-פופ"), ["k-pop playlist", "kpop hits", "korean pop"]),
      style("j-pop", "music_note", "J-Pop", t("Japanese pop", "ג'יי-פופ"), ["j-pop playlist", "jpop hits", "japanese pop"]),
      style("hip-hop", "tracks", "Hip-Hop", t("Beats and flow", "היפ-הופ"), ["hip hop playlist", "hip hop hits", "rap hip hop"]),
      style("rap", "tracks", "Rap", t("Rap essentials", "ראפ"), ["rap playlist", "rap hits", "new rap"]),
      style("trap", "tracks", "Trap", t("Modern trap", "טראפ"), ["trap playlist", "trap hits", "modern trap"]),
      style("rnb", "heart_filled", "R&B", t("Smooth rhythm", "R&B"), ["r&b playlist", "rnb hits", "smooth r&b"]),
      style("soul", "heart_filled", "Soul", t("Warm vocals", "סול"), ["soul playlist", "neo soul", "soul classics"]),
      style("funk", "radio", "Funk", t("Groove and bass", "פאנק"), ["funk playlist", "funk classics", "groove funk"]),
      style("rock", "album", "Rock", t("Guitars and bands", "רוק"), ["rock playlist", "rock hits", "rock music"]),
      style("classic-rock", "album", "Classic Rock", t("Rock classics", "רוק קלאסי"), ["classic rock playlist", "rock classics", "70s rock"]),
      style("alternative-rock", "album", "Alternative Rock", t("Alternative bands", "רוק אלטרנטיבי"), ["alternative rock playlist", "alt rock", "alternative music"]),
      style("indie-rock", "album", "Indie Rock", t("Independent rock", "אינדי רוק"), ["indie rock playlist", "indie bands", "garage rock"]),
      style("punk", "album", "Punk", t("Fast guitars", "פאנק רוק"), ["punk rock playlist", "punk hits", "pop punk"]),
      style("metal", "album", "Metal", t("Heavy guitars", "מטאל"), ["metal playlist", "heavy metal", "metal hits"]),
      style("electronic", "grid", "Electronic", t("Electronic music", "אלקטרוני"), ["electronic playlist", "electronica", "electronic music"]),
      style("edm", "grid", "EDM", t("Festival energy", "EDM"), ["edm playlist", "edm hits", "festival edm"]),
      style("house", "grid", "House", t("House music", "האוס"), ["house music playlist", "house hits", "club house"]),
      style("deep-house", "grid", "Deep House", t("Deep club sound", "דיפ האוס"), ["deep house playlist", "deep house music", "melodic house"]),
      style("tech-house", "grid", "Tech House", t("Club groove", "טק האוס"), ["tech house playlist", "tech house music", "club tech house"]),
      style("techno", "grid", "Techno", t("Driving electronic", "טכנו"), ["techno playlist", "techno music", "melodic techno"]),
      style("trance", "grid", "Trance", t("Uplifting trance", "טראנס"), ["trance playlist", "uplifting trance", "psytrance"]),
      style("drum-bass", "grid", "Drum & Bass", t("Fast breaks", "דראם אנד בייס"), ["drum and bass playlist", "dnb playlist", "liquid drum and bass"]),
      style("dubstep", "grid", "Dubstep", t("Bass drops", "דאבסטפ"), ["dubstep playlist", "bass music", "dubstep hits"]),
      style("dance", "radio", "Dance", t("Dance hits", "דאנס"), ["dance playlist", "dance hits", "dance pop"]),
      style("disco", "radio", "Disco", t("Disco classics", "דיסקו"), ["disco playlist", "disco classics", "nu disco"]),
      style("party", "radio", this._i18n("ui.party"), t("Party hits", "להיטי מסיבה"), ["party hits playlist", "party music", "dance party"]),
      style("workout", "stats", "Workout", t("Training energy", "אימון"), ["workout playlist", "gym music", "running playlist"]),
      style("jazz", "music_note", "Jazz", t("Jazz essentials", "ג'אז"), ["jazz playlist", "smooth jazz", "vocal jazz"]),
      style("blues", "music_note", "Blues", t("Blues classics", "בלוז"), ["blues playlist", "blues classics", "modern blues"]),
      style("classical", "album", "Classical", t("Classical music", "קלאסי"), ["classical music playlist", "classical essentials", "orchestra classical"]),
      style("piano", "album", "Piano", t("Piano focus", "פסנתר"), ["piano playlist", "classical piano", "peaceful piano"]),
      style("ambient", "moon", "Ambient", t("Soundscapes", "אמביינט"), ["ambient playlist", "ambient music", "soundscape"]),
      style("chill", "moon", "Chill", t("Relaxed listening", "צ'יל"), ["chill playlist", "chillout music", "relaxing chill"]),
      style("lofi", "moon", "Lo-Fi", t("Lo-fi beats", "לו-פיי"), ["lo-fi playlist", "lofi beats", "lo-fi hip hop"]),
      style("acoustic", "album", "Acoustic", t("Unplugged sound", "אקוסטי"), ["acoustic playlist", "unplugged music", "acoustic chill"]),
      style("folk", "album", "Folk", t("Folk songs", "פולק"), ["folk playlist", "singer songwriter", "indie folk"]),
      style("country", "album", "Country", t("Country music", "קאנטרי"), ["country playlist", "country hits", "americana"]),
      style("reggae", "radio", "Reggae", t("Island groove", "רגאיי"), ["reggae playlist", "reggae classics", "dancehall reggae"]),
      style("latin", "radio", "Latin", t("Latin music", "לטיני"), ["latin playlist", "latin hits", "latin pop"]),
      style("reggaeton", "radio", "Reggaeton", t("Latin urban", "רגאטון"), ["reggaeton playlist", "reggaeton hits", "latin urban"]),
      style("salsa", "radio", "Salsa", t("Salsa dance", "סלסה"), ["salsa playlist", "salsa classics", "latin salsa"]),
      style("bachata", "radio", "Bachata", t("Bachata rhythm", "בצ'אטה"), ["bachata playlist", "bachata hits", "latin bachata"]),
      style("afrobeats", "radio", "Afrobeats", t("Afro rhythm", "אפרוביטס"), ["afrobeats playlist", "afrobeats hits", "afropop"]),
      style("amapiano", "radio", "Amapiano", t("South African groove", "אמאפיאנו"), ["amapiano playlist", "amapiano hits", "afro house amapiano"]),
      style("world", "radio", "World", t("Global sounds", "מוזיקת עולם"), ["world music playlist", "global music", "international playlist"]),
      style("soundtrack", "album", "Soundtracks", t("Movies and series", "פסקולים"), ["soundtrack playlist", "movie soundtracks", "film music"]),
      style("meditation", "moon", "Meditation", t("Calm focus", "מדיטציה"), ["meditation music", "calm meditation", "relaxing instrumental"]),
      style("sleep", "moon", "Sleep", t("Quiet night", "שינה"), ["sleep music", "sleep playlist", "deep sleep music"]),
      style("kids", "heart_filled", this._i18n("ui.kids"), t("Family music", "ילדים ומשפחה"), ["kids music playlist", "children songs", "family music kids"]),
      style("israeli", "music_note", this._i18n("ui.israeli"), t("Israeli music", "מוזיקה ישראלית"), ["ישראלי עברית פלייליסט", "israeli music", "israeli hits"]),
      style("hebrew-hits", "music_note", t("Hebrew Hits", "להיטים בעברית"), t("Hebrew songs", "שירים בעברית"), ["להיטים בעברית", "מוזיקה עברית", "hebrew hits playlist"]),
      style("mizrahi", "radio", t("Mizrahi", "מזרחית"), t("Mediterranean pop", "ים תיכוני"), ["מזרחית פלייליסט", "מוזיקה מזרחית", "mizrahit music"]),
      style("mediterranean", "radio", t("Mediterranean", "ים תיכוני"), t("Warm regional sound", "צליל ים תיכוני"), ["mediterranean music playlist", "ים תיכוני", "greek turkish mediterranean music"]),
      style("arabic", "radio", t("Arabic", "ערבית"), t("Arabic music", "מוזיקה ערבית"), ["arabic music playlist", "arabic hits", "middle eastern music"]),
      style("turkish", "radio", t("Turkish", "טורקית"), t("Turkish music", "מוזיקה טורקית"), ["turkish music playlist", "turkish pop", "turkish hits"]),
      style("greek", "radio", t("Greek", "יוונית"), t("Greek music", "מוזיקה יוונית"), ["greek music playlist", "greek hits", "greek pop"]),
      style("french", "radio", t("French", "צרפתית"), t("French music", "מוזיקה צרפתית"), ["french music playlist", "french pop", "chanson francaise"]),
      style("spanish", "radio", t("Spanish", "ספרדית"), t("Spanish music", "מוזיקה ספרדית"), ["spanish music playlist", "spanish pop", "musica espanola"]),
      style("singer-songwriter", "album", "Singer-Songwriter", t("Personal songs", "זמרים-יוצרים"), ["singer songwriter playlist", "singer-songwriter", "acoustic singer songwriter"]),
      style("soft-pop", "music_note", "Soft Pop", t("Soft pop songs", "פופ רך"), ["soft pop playlist", "soft pop hits", "easy pop"]),
      style("pop-rock", "album", "Pop Rock", t("Pop guitars", "פופ רוק"), ["pop rock playlist", "pop rock hits", "guitar pop"]),
      style("electropop", "grid", "Electropop", t("Electronic pop", "אלקטרו-פופ"), ["electropop playlist", "electro pop", "synth pop"]),
      style("synth-pop", "grid", "Synth Pop", t("Synth classics", "סינת' פופ"), ["synth pop playlist", "synthpop classics", "80s synth pop"]),
      style("dream-pop", "moon", "Dream Pop", t("Dreamy pop", "דרים פופ"), ["dream pop playlist", "dreamy indie pop", "shoegaze dream pop"]),
      style("bedroom-pop", "music_note", "Bedroom Pop", t("Lo-fi pop", "בד-רום פופ"), ["bedroom pop playlist", "lofi bedroom pop", "indie bedroom pop"]),
      style("hyperpop", "grid", "Hyperpop", t("Maximal pop", "הייפרפופ"), ["hyperpop playlist", "hyper pop", "glitch pop"]),
      style("emo", "heart_filled", "Emo", t("Emotional rock", "אימו"), ["emo playlist", "emo rock", "emo pop punk"]),
      style("post-punk", "album", "Post-Punk", t("Angular guitars", "פוסט-פאנק"), ["post-punk playlist", "post punk classics", "dark post punk"]),
      style("new-wave", "grid", "New Wave", t("80s alternative", "ניו ווייב"), ["new wave playlist", "80s new wave", "new wave classics"]),
      style("goth", "moon", "Goth", t("Dark wave sound", "גותי"), ["goth playlist", "gothic rock", "darkwave"]),
      style("darkwave", "moon", "Darkwave", t("Dark synth sound", "דארקווייב"), ["darkwave playlist", "dark wave", "coldwave"]),
      style("industrial", "grid", "Industrial", t("Heavy electronic", "אינדסטריאל"), ["industrial playlist", "industrial rock", "industrial electronic"]),
      style("grunge", "album", "Grunge", t("90s guitars", "גראנג'"), ["grunge playlist", "90s grunge", "grunge classics"]),
      style("hard-rock", "album", "Hard Rock", t("Hard guitars", "הארד רוק"), ["hard rock playlist", "hard rock hits", "arena rock"]),
      style("soft-rock", "album", "Soft Rock", t("Soft rock classics", "רוק רך"), ["soft rock playlist", "soft rock classics", "easy rock"]),
      style("prog-rock", "album", "Progressive Rock", t("Progressive rock", "רוק מתקדם"), ["progressive rock playlist", "prog rock", "classic prog rock"]),
      style("psychedelic-rock", "album", "Psychedelic Rock", t("Psych rock", "רוק פסיכדלי"), ["psychedelic rock playlist", "psych rock", "psychedelic music"]),
      style("shoegaze", "moon", "Shoegaze", t("Wall of sound", "שוגייז"), ["shoegaze playlist", "dream pop shoegaze", "shoegaze classics"]),
      style("garage-rock", "album", "Garage Rock", t("Raw rock", "גראז' רוק"), ["garage rock playlist", "garage rock revival", "raw rock"]),
      style("surf-rock", "radio", "Surf Rock", t("Surf guitars", "סרף רוק"), ["surf rock playlist", "surf guitar", "surf rock classics"]),
      style("metalcore", "album", "Metalcore", t("Modern heavy", "מטאלקור"), ["metalcore playlist", "modern metalcore", "metalcore hits"]),
      style("death-metal", "album", "Death Metal", t("Extreme metal", "דת' מטאל"), ["death metal playlist", "death metal classics", "melodic death metal"]),
      style("black-metal", "album", "Black Metal", t("Extreme dark metal", "בלאק מטאל"), ["black metal playlist", "atmospheric black metal", "black metal classics"]),
      style("progressive-metal", "album", "Progressive Metal", t("Technical metal", "מטאל מתקדם"), ["progressive metal playlist", "prog metal", "technical metal"]),
      style("nu-metal", "album", "Nu Metal", t("90s heavy crossover", "נו מטאל"), ["nu metal playlist", "nu metal hits", "rap metal"]),
      style("folk-metal", "album", "Folk Metal", t("Folk heavy sound", "פולק מטאל"), ["folk metal playlist", "celtic metal", "viking metal"]),
      style("old-school-hip-hop", "tracks", "Old School Hip-Hop", t("Classic hip-hop", "היפ-הופ קלאסי"), ["old school hip hop playlist", "classic hip hop", "90s hip hop"]),
      style("boom-bap", "tracks", "Boom Bap", t("Classic beats", "בום באפ"), ["boom bap playlist", "boom bap hip hop", "90s rap boom bap"]),
      style("conscious-rap", "tracks", "Conscious Rap", t("Lyrical rap", "ראפ מודע"), ["conscious rap playlist", "lyrical hip hop", "conscious hip hop"]),
      style("drill", "tracks", "Drill", t("Drill rap", "דריל"), ["drill playlist", "drill rap", "uk drill"]),
      style("grime", "tracks", "Grime", t("UK rap energy", "גריים"), ["grime playlist", "uk grime", "grime hits"]),
      style("phonk", "grid", "Phonk", t("Dark drift beats", "פונק"), ["phonk playlist", "drift phonk", "dark phonk"]),
      style("afro-trap", "tracks", "Afro Trap", t("Afro rap", "אפרו טראפ"), ["afro trap playlist", "afrotrap", "afro rap"]),
      style("neo-soul", "heart_filled", "Neo Soul", t("Modern soul", "נאו סול"), ["neo soul playlist", "modern soul", "neo soul r&b"]),
      style("motown", "heart_filled", "Motown", t("Motown classics", "מוטאון"), ["motown playlist", "motown classics", "classic soul motown"]),
      style("gospel", "heart_filled", "Gospel", t("Gospel vocals", "גוספל"), ["gospel playlist", "gospel music", "soul gospel"]),
      style("vocal-jazz", "music_note", "Vocal Jazz", t("Jazz vocals", "ג'אז ווקאלי"), ["vocal jazz playlist", "jazz singers", "vocal jazz classics"]),
      style("smooth-jazz", "music_note", "Smooth Jazz", t("Smooth jazz", "ג'אז חלק"), ["smooth jazz playlist", "smooth jazz hits", "jazz lounge"]),
      style("bebop", "music_note", "Bebop", t("Bebop jazz", "ביבופ"), ["bebop playlist", "bebop jazz", "charlie parker style jazz"]),
      style("swing", "music_note", "Swing", t("Swing jazz", "סווינג"), ["swing playlist", "swing jazz", "big band swing"]),
      style("big-band", "music_note", "Big Band", t("Big band jazz", "ביג בנד"), ["big band playlist", "big band jazz", "swing orchestra"]),
      style("latin-jazz", "radio", "Latin Jazz", t("Latin jazz", "ג'אז לטיני"), ["latin jazz playlist", "latin jazz classics", "bossa jazz"]),
      style("jazz-fusion", "grid", "Jazz Fusion", t("Fusion jazz", "פיוז'ן ג'אז"), ["jazz fusion playlist", "fusion jazz", "jazz rock fusion"]),
      style("opera", "album", "Opera", t("Opera voices", "אופרה"), ["opera playlist", "opera classics", "classical opera"]),
      style("orchestral", "album", "Orchestral", t("Orchestra music", "תזמורת"), ["orchestral playlist", "orchestra music", "symphony playlist"]),
      style("chamber", "album", "Chamber Music", t("Small ensemble", "מוזיקה קאמרית"), ["chamber music playlist", "classical chamber", "string quartet"]),
      style("baroque", "album", "Baroque", t("Baroque classical", "בארוק"), ["baroque playlist", "baroque classical", "bach baroque"]),
      style("romantic-classical", "album", "Romantic Classical", t("Romantic era", "קלאסי רומנטי"), ["romantic classical playlist", "romantic era classical", "chopin liszt classical"]),
      style("film-score", "album", "Film Score", t("Cinematic score", "מוזיקה לקולנוע"), ["film score playlist", "cinematic score", "movie score"]),
      style("synthwave", "grid", "Synthwave", t("Retro synths", "סינת'ווייב"), ["synthwave playlist", "retrowave", "80s synthwave"]),
      style("retrowave", "grid", "Retrowave", t("Retro electronic", "רטרו-ווייב"), ["retrowave playlist", "retro wave", "outrun synthwave"]),
      style("downtempo", "moon", "Downtempo", t("Slow electronic", "דאונטמפו"), ["downtempo playlist", "downtempo electronic", "chill downtempo"]),
      style("trip-hop", "moon", "Trip-Hop", t("Moody beats", "טריפ-הופ"), ["trip hop playlist", "trip-hop classics", "downtempo trip hop"]),
      style("chillhop", "moon", "Chillhop", t("Chill beats", "צ'ילהופ"), ["chillhop playlist", "chill hop beats", "lofi chillhop"]),
      style("future-bass", "grid", "Future Bass", t("Bright bass", "פיוצ'ר בס"), ["future bass playlist", "future bass hits", "melodic bass"]),
      style("uk-garage", "grid", "UK Garage", t("UK club sound", "UK גאראז'"), ["uk garage playlist", "ukg playlist", "2-step garage"]),
      style("breakbeat", "grid", "Breakbeat", t("Broken beats", "ברייקביט"), ["breakbeat playlist", "breaks playlist", "electro breaks"]),
      style("hardstyle", "grid", "Hardstyle", t("Hard dance", "הארדסטייל"), ["hardstyle playlist", "hard dance", "hardstyle hits"]),
      style("minimal-techno", "grid", "Minimal Techno", t("Minimal club", "טכנו מינימלי"), ["minimal techno playlist", "minimal techno", "minimal electronic"]),
      style("progressive-house", "grid", "Progressive House", t("Melodic house", "פרוגרסיב האוס"), ["progressive house playlist", "melodic progressive house", "progressive house hits"]),
      style("electro", "grid", "Electro", t("Electro beats", "אלקטרו"), ["electro playlist", "electro music", "electro dance"]),
      style("idm", "grid", "IDM", t("Experimental electronic", "IDM"), ["idm playlist", "intelligent dance music", "experimental electronic"]),
      style("ska", "radio", "Ska", t("Ska rhythm", "סקא"), ["ska playlist", "ska classics", "ska punk"]),
      style("dancehall", "radio", "Dancehall", t("Dancehall reggae", "דאנסהול"), ["dancehall playlist", "dancehall reggae", "dancehall hits"]),
      style("dub", "radio", "Dub", t("Dub reggae", "דאב"), ["dub playlist", "dub reggae", "roots dub"]),
      style("roots-reggae", "radio", "Roots Reggae", t("Roots reggae", "רגאיי שורשי"), ["roots reggae playlist", "roots reggae classics", "reggae roots"]),
      style("afro-house", "grid", "Afro House", t("Afro club sound", "אפרו האוס"), ["afro house playlist", "afro house music", "afro tech house"]),
      style("afropop", "radio", "Afropop", t("African pop", "אפרו פופ"), ["afropop playlist", "african pop", "afropop hits"]),
      style("highlife", "radio", "Highlife", t("West African sound", "היילייף"), ["highlife playlist", "ghana highlife", "west african highlife"]),
      style("soukous", "radio", "Soukous", t("Congo guitar sound", "סוקוס"), ["soukous playlist", "congolese music", "african soukous"]),
      style("cumbia", "radio", "Cumbia", t("Cumbia rhythm", "קומביה"), ["cumbia playlist", "cumbia classics", "latin cumbia"]),
      style("tango", "radio", "Tango", t("Tango classics", "טנגו"), ["tango playlist", "argentine tango", "tango classics"]),
      style("bossa-nova", "music_note", "Bossa Nova", t("Brazilian jazz", "בוסה נובה"), ["bossa nova playlist", "bossa nova classics", "brazilian bossa"]),
      style("samba", "radio", "Samba", t("Brazilian samba", "סמבה"), ["samba playlist", "brazilian samba", "samba classics"]),
      style("mpb", "radio", "MPB", t("Brazilian popular music", "MPB"), ["mpb playlist", "brazilian mpb", "musica popular brasileira"]),
      style("flamenco", "radio", "Flamenco", t("Spanish guitar", "פלמנקו"), ["flamenco playlist", "flamenco guitar", "spanish flamenco"]),
      style("bollywood", "radio", "Bollywood", t("Indian cinema", "בוליווד"), ["bollywood playlist", "bollywood hits", "hindi film songs"]),
      style("indian-classical", "album", "Indian Classical", t("Indian classical", "קלאסי הודי"), ["indian classical music", "hindustani classical", "carnatic classical"]),
      style("punjabi", "radio", "Punjabi", t("Punjabi hits", "פונג'אבי"), ["punjabi playlist", "punjabi hits", "bhangra punjabi"]),
      style("bhangra", "radio", "Bhangra", t("Bhangra dance", "באנגרה"), ["bhangra playlist", "bhangra hits", "punjabi bhangra"]),
      style("city-pop", "grid", "City Pop", t("Japanese city pop", "סיטי פופ"), ["city pop playlist", "japanese city pop", "80s city pop"]),
      style("mandopop", "music_note", "Mandopop", t("Mandarin pop", "מנדו-פופ"), ["mandopop playlist", "mandarin pop", "chinese pop"]),
      style("cantopop", "music_note", "Cantopop", t("Cantonese pop", "קנטו-פופ"), ["cantopop playlist", "cantonese pop", "hong kong pop"]),
      style("korean-indie", "music_note", "Korean Indie", t("Korean indie", "אינדי קוריאני"), ["korean indie playlist", "k-indie", "korean indie pop"]),
      style("persian", "radio", "Persian", t("Persian music", "פרסית"), ["persian music playlist", "persian pop", "iranian music"]),
      style("focus", "moon", "Focus", t("Music for focus", "ריכוז"), ["focus playlist", "deep focus music", "instrumental focus"]),
      style("study", "moon", "Study", t("Study music", "לימודים"), ["study playlist", "study music", "concentration music"]),
      style("coffeehouse", "album", "Coffeehouse", t("Coffeehouse mood", "קפה"), ["coffeehouse playlist", "coffee shop music", "acoustic coffeehouse"]),
      style("dinner", "heart_filled", "Dinner", t("Dinner music", "ארוחת ערב"), ["dinner playlist", "dinner music", "restaurant lounge"]),
      style("romance", "heart_filled", "Romance", t("Love songs", "רומנטי"), ["romantic playlist", "love songs", "romance music"]),
      style("sad", "moon", "Sad", t("Sad songs", "עצוב"), ["sad songs playlist", "melancholy music", "heartbreak playlist"]),
      style("happy", "radio", "Happy", t("Feel-good songs", "שמח"), ["happy playlist", "feel good music", "happy songs"]),
      style("summer", "radio", "Summer", t("Summer songs", "קיץ"), ["summer playlist", "summer hits", "beach music"]),
      style("beach", "radio", "Beach", t("Beach music", "חוף"), ["beach playlist", "beach music", "tropical playlist"]),
      style("road-trip", "radio", "Road Trip", t("Driving music", "נסיעה"), ["road trip playlist", "driving music", "car playlist"]),
      style("gaming", "grid", "Gaming", t("Gaming music", "גיימינג"), ["gaming playlist", "gaming music", "electronic gaming"]),
      style("anime", "music_note", "Anime", t("Anime songs", "אנימה"), ["anime playlist", "anime songs", "anime openings"]),
      style("50s", "radio", "50s", t("1950s music", "שנות ה-50"), ["50s playlist", "1950s music", "50s rock and roll"]),
      style("60s", "radio", "60s", t("1960s music", "שנות ה-60"), ["60s playlist", "1960s music", "60s hits"]),
      style("70s", "radio", "70s", t("1970s music", "שנות ה-70"), ["70s playlist", "1970s hits", "70s rock disco"]),
      style("80s", "grid", "80s", t("1980s music", "שנות ה-80"), ["80s playlist", "1980s hits", "80s pop rock"]),
      style("90s", "album", "90s", t("1990s music", "שנות ה-90"), ["90s playlist", "1990s hits", "90s pop rock"]),
      style("2000s", "music_note", "2000s", t("2000s music", "שנות ה-2000"), ["2000s playlist", "2000s hits", "00s music"]),
      style("2010s", "music_note", "2010s", t("2010s music", "שנות ה-2010"), ["2010s playlist", "2010s hits", "10s music"]),
      style("2020s", "music_note", "2020s", t("2020s music", "שנות ה-2020"), ["2020s playlist", "2020s hits", "new music hits"]),
      style("israeli-rock", "album", t("Israeli Rock", "רוק ישראלי"), t("Israeli bands", "להקות ישראליות"), ["רוק ישראלי", "israeli rock", "להקות רוק ישראלי"]),
      style("israeli-pop", "music_note", t("Israeli Pop", "פופ ישראלי"), t("Israeli pop hits", "פופ ישראלי"), ["פופ ישראלי", "israeli pop", "להיטי פופ ישראלי"]),
      style("israeli-rap", "tracks", t("Israeli Rap", "ראפ ישראלי"), t("Hebrew rap", "ראפ בעברית"), ["ראפ ישראלי", "היפ הופ ישראלי", "israeli rap"]),
      style("israeli-indie", "album", t("Israeli Indie", "אינדי ישראלי"), t("Local indie", "אינדי מקומי"), ["אינדי ישראלי", "israeli indie", "אינדי עברי"]),
      style("sephardic", "radio", t("Sephardic", "ספרדי"), t("Sephardic music", "מוזיקה ספרדית-יהודית"), ["sephardic music", "מוזיקה ספרדית יהודית", "ladino music"]),
      style("klezmer", "radio", "Klezmer", t("Jewish folk", "כליזמר"), ["klezmer playlist", "klezmer music", "jewish folk music"]),
      style("jewish", "radio", t("Jewish", "יהודית"), t("Jewish music", "מוזיקה יהודית"), ["jewish music playlist", "מוזיקה יהודית", "jewish songs"]),
      style("hasidic", "radio", t("Hasidic", "חסידית"), t("Hasidic music", "מוזיקה חסידית"), ["hasidic music", "מוזיקה חסידית", "chassidic music"]),
      style("piyyut", "music_note", t("Piyyut", "פיוט"), t("Traditional liturgy", "פיוטים"), ["פיוטים", "piyyut", "traditional jewish liturgy"])
    ];
    return options.includeCustom ? [...styles, style("custom", "search", this._i18n("ui.free_style"), this._i18n("ui.type_anything"), [])] : styles;
  }
  _simpleWizardGenres() {
    return this._musicStyleCatalog({ includeCustom: true });
  }
  _simpleWizardContentTypes() {
    return [
      { id: "playlist", icon: "playlist", label: this._i18n("ui.playlist"), subtitle: this._i18n("ui.play_a_saved_list") },
      { id: "artist", icon: "artist", label: this._i18n("ui.artist"), subtitle: this._i18n("ui.play_an_artist") },
      { id: "artist_radio", icon: "radio", label: this._i18n("ui.artist_radio"), subtitle: this._i18n("ui.songs_around_an_artist") },
      { id: "library_radio", icon: "radio", label: this._i18n("ui.library_radio"), subtitle: this._i18n("ui.a_radio_station") }
    ];
  }
  _simpleWizardStepIndex(step = "") {
    const order = ["players", "source", "review"];
    const index = order.indexOf(step);
    return index >= 0 ? index : 0;
  }
  _simpleWizardProgressHtml(step = "players") {
    const current = this._simpleWizardStepIndex(step);
    const steps = [
      this._i18n("ui.players"),
      this._i18n("ui.music"),
      this._i18n("ui.play_2")
    ];
    return `
      <div class="simple-wizard-progress" aria-hidden="true">
        ${steps.map((label, index) => `
          <span class="simple-wizard-progress-step ${index === current ? "active" : ""} ${index < current ? "done" : ""}">
            <span>${this._esc(String(index + 1))}</span>
            <strong>${this._esc(label)}</strong>
          </span>
        `).join("")}
      </div>
    `;
  }
  _simpleWizardSelectedPlayerNames(entityIds = []) {
    const ids = Array.isArray(entityIds) ? entityIds : [];
    if (ids.length > 1) return this._m(`${ids.length} players`, `${ids.length} נגנים`);
    const player = this._playerByEntityId(ids[0]);
    return player?.attributes?.friendly_name || ids[0] || this._i18n("ui.selected_player_3");
  }
  _simpleWizardPlayersHtml(state) {
    const players = this._simpleWizardPlayerPool();
    const selected = new Set(state.selectedPlayers || []);
    const allSelected = players.length > 0 && players.every((player) => selected.has(player.entity_id));
    if (!players.length) {
      return `
        <div class="simple-wizard-panel">
          <div class="notice open">${this._esc(this._i18n("ui.no_players_found_yet"))}</div>
        </div>
      `;
    }
    return `
      <div class="simple-wizard-panel">
        <div class="simple-wizard-title">${this._esc(this._i18n("ui.choose_where_to_play"))}</div>
        <div class="simple-wizard-player-grid">
          <button class="simple-wizard-player ${allSelected ? "active" : ""}" data-simple-all-players="1">
            <span class="simple-wizard-player-icon">${this._iconSvg("speaker")}</span>
            <span class="simple-wizard-player-copy">
              <span class="simple-wizard-option-title">${this._esc(this._i18n("ui.all_players"))}</span>
              <span class="simple-wizard-option-sub">${this._esc(this._i18n("ui.play_everywhere"))}</span>
            </span>
            <span class="simple-wizard-check">${this._iconSvg(allSelected ? "check" : "plus")}</span>
          </button>
          ${players.map((player) => {
      const entityId = player.entity_id;
      const active = selected.has(entityId);
      const art = this._playerArtworkUrl(player, 120);
      const name = player.attributes?.friendly_name || entityId;
      const subtitle = player.attributes?.media_title || this._playerStateLabel(player);
      return `
              <button class="simple-wizard-player ${active ? "active" : ""} ${this._isPlayerActive(player) ? "is-playing" : ""}" data-simple-player="${this._esc(entityId)}">
                <span class="simple-wizard-player-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("speaker")}</span>
                <span class="simple-wizard-player-copy">
                  <span class="simple-wizard-option-title">${this._esc(name)}</span>
                  <span class="simple-wizard-option-sub">${this._esc(subtitle || "")}</span>
                </span>
                <span class="simple-wizard-check">${this._iconSvg(active ? "check" : "plus")}</span>
              </button>
            `;
    }).join("")}
        </div>
        <div class="simple-wizard-footer single">
          <button class="simple-wizard-primary" data-simple-next="source" ${selected.size ? "" : "disabled"}>${this._esc(this._i18n("ui.continue_2"))}</button>
        </div>
      </div>
    `;
  }
  _simpleWizardSourceHtml(state) {
    const source = state.source === "content" ? "content" : "genre";
    const selectedGenre = state.genre || "pop";
    const contentType = state.contentType || "playlist";
    const customGenre = String(state.customGenre || "").trim();
    const genreOptions = this._simpleWizardGenres();
    const selectedGenreMeta = genreOptions.find((genre) => genre.id === selectedGenre) || genreOptions[0];
    return `
      <div class="simple-wizard-panel">
        <div class="simple-wizard-title">${this._esc(this._i18n("ui.choose_the_music"))}</div>
        <div class="simple-wizard-source-grid">
          <button class="simple-wizard-source ${source === "genre" ? "active" : ""}" data-simple-source="genre">
            <span>${this._iconSvg("wand")}</span>
            <strong>${this._esc(this._m("Style", "סגנון"))}</strong>
          </button>
          <button class="simple-wizard-source ${source === "content" ? "active" : ""}" data-simple-source="content">
            <span>${this._iconSvg("library_music")}</span>
            <strong>${this._esc(this._i18n("ui.existing_content"))}</strong>
          </button>
        </div>
        ${source === "genre" ? `
          <label class="simple-wizard-search simple-wizard-category-picker">
            <span>${this._esc(this._m("Style", "סגנון"))}</span>
            <select id="simpleWizardGenreSelect" class="media-sort-select settings-select simple-wizard-select" aria-label="${this._esc(this._m("Style", "סגנון"))}">
              ${genreOptions.map((genre) => `<option value="${this._esc(genre.id)}" ${selectedGenre === genre.id ? "selected" : ""}>${this._esc(genre.label)}</option>`).join("")}
            </select>
          </label>
          <div class="simple-wizard-option active simple-wizard-selected-option">
            <span class="simple-wizard-option-icon">${this._iconSvg(selectedGenreMeta?.icon || "wand")}</span>
            <span>
              <span class="simple-wizard-option-title">${this._esc(selectedGenreMeta?.label || "")}</span>
              <span class="simple-wizard-option-sub">${this._esc(selectedGenreMeta?.subtitle || "")}</span>
            </span>
          </div>
          ${selectedGenre === "custom" ? `
            <label class="simple-wizard-search simple-wizard-free-style">
              <span>${this._esc(this._i18n("ui.free_style"))}</span>
              <input id="simpleWizardCustomGenreInput" type="text" value="${this._esc(customGenre)}" placeholder="${this._esc(this._i18n("ui.example_quiet_jazz_greek_music_workout"))}">
            </label>
          ` : ""}
        ` : `
          <div class="simple-wizard-option-grid">
            ${this._simpleWizardContentTypes().map((type) => `
              <button class="simple-wizard-option ${contentType === type.id ? "active" : ""}" data-simple-content="${this._esc(type.id)}">
                <span class="simple-wizard-option-icon">${this._iconSvg(type.icon)}</span>
                <span>
                  <span class="simple-wizard-option-title">${this._esc(type.label)}</span>
                  <span class="simple-wizard-option-sub">${this._esc(type.subtitle)}</span>
                </span>
              </button>
            `).join("")}
          </div>
          <label class="simple-wizard-search">
            <span>${this._esc(this._i18n("ui.name_or_keyword"))}</span>
            <input id="simpleWizardQueryInput" type="text" value="${this._esc(state.query || "")}" placeholder="${this._esc(this._i18n("ui.optional_2"))}">
          </label>
        `}
        <div class="simple-wizard-footer">
          <button class="simple-wizard-secondary" data-simple-back="players">${this._esc(this._i18n("ui.back_2"))}</button>
          <button class="simple-wizard-primary" data-simple-build="1">${this._esc(this._i18n("ui.find_music"))}</button>
        </div>
      </div>
    `;
  }
  _simpleWizardReviewHtml(state) {
    if (state.loading) {
      return `
        <div class="simple-wizard-panel simple-wizard-loading">
          <div class="simple-wizard-loader">${this._iconSvg("wand")}</div>
          <div class="simple-wizard-title">${this._esc(this._i18n("ui.finding_music"))}</div>
        </div>
      `;
    }
    const candidates = Array.isArray(state.candidates) ? state.candidates : [];
    if (!candidates.length) {
      return `
        <div class="simple-wizard-panel">
          <div class="notice open">${this._esc(state.error || this._i18n("ui.no_matching_content_was_found"))}</div>
          <div class="simple-wizard-footer">
            <button class="simple-wizard-secondary" data-simple-back="source">${this._esc(this._i18n("ui.back_2"))}</button>
            <button class="simple-wizard-primary" data-simple-build="1">${this._esc(this._i18n("ui.try_again"))}</button>
          </div>
        </div>
      `;
    }
    const index = Math.max(0, Math.min(candidates.length - 1, Number(state.selectedIndex || 0)));
    const candidate = candidates[index] || candidates[0];
    const art = candidate.image || "";
    return `
      <div class="simple-wizard-panel">
        <div class="simple-wizard-title">${this._esc(this._i18n("ui.ready_to_play"))}</div>
        <div class="simple-wizard-review-card">
          <span class="simple-wizard-review-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg(candidate.media_type === "radio" ? "radio" : "playlist")}</span>
          <span class="simple-wizard-review-copy">
            <span class="simple-wizard-review-kicker">${this._esc(this._simpleWizardSelectedPlayerNames(state.selectedPlayers))}</span>
            <span class="simple-wizard-review-title">${this._esc(candidate.name || this._i18n("ui.selected_music"))}</span>
            <span class="simple-wizard-review-sub">${this._esc(candidate.subtitle || this._simpleWizardMediaTypeLabel(candidate.media_type))}</span>
          </span>
        </div>
        <div class="simple-wizard-section-head">
          <span>${this._esc(this._i18n("ui.results"))}</span>
          <small>${this._esc(this._i18n("ui.choose_one_clear_option"))}</small>
        </div>
        <div class="simple-wizard-result-grid">
          ${candidates.slice(0, 8).map((item, itemIndex) => {
      const itemArt = item.image || "";
      const itemType = this._simpleWizardMediaTypeLabel(item.media_type);
      return `
            <button class="simple-wizard-result ${itemIndex === index ? "active" : ""}" data-simple-candidate="${this._esc(String(itemIndex))}">
              <span class="simple-wizard-result-art">${itemArt ? `<img src="${this._esc(itemArt)}" alt="">` : this._iconSvg(item.media_type === "radio" ? "radio" : item.media_type === "artist" ? "artist" : "playlist")}</span>
              <span class="simple-wizard-result-copy">
                <span class="simple-wizard-result-kicker">${this._esc(itemType)}</span>
                <span class="simple-wizard-result-title">${this._esc(item.name || this._i18n("ui.option"))}</span>
                <span class="simple-wizard-result-sub">${this._esc(item.subtitle || itemType)}</span>
              </span>
              <span class="simple-wizard-result-check">${this._iconSvg(itemIndex === index ? "check" : "plus")}</span>
            </button>
          `;
    }).join("")}
        </div>
        <div class="simple-wizard-footer triple">
          <button class="simple-wizard-secondary" data-simple-back="source">${this._esc(this._i18n("ui.back_2"))}</button>
          <button class="simple-wizard-secondary" data-simple-build="1">${this._esc(this._i18n("ui.refresh"))}</button>
          <button class="simple-wizard-primary" data-simple-play="1">${this._esc(this._i18n("ui.play_now"))}</button>
        </div>
      </div>
    `;
  }
  _simpleWizardHtml() {
    const state = this._simpleWizardState();
    const step = ["players", "source", "review"].includes(state.step) ? state.step : "players";
    const body = step === "source" ? this._simpleWizardSourceHtml(state) : step === "review" ? this._simpleWizardReviewHtml(state) : this._simpleWizardPlayersHtml(state);
    return `
      <div class="simple-wizard-shell" data-simple-step="${this._esc(step)}">
        ${this._simpleWizardProgressHtml(step)}
        <div class="simple-wizard-toolbar">
          <button class="simple-wizard-reset-btn" data-simple-reset="1">${this._esc(this._i18n("ui.reset"))}</button>
        </div>
        ${body}
      </div>
    `;
  }
  _simpleWizardMediaTypeLabel(mediaType = "") {
    const type = String(mediaType || "").toLowerCase();
    if (type === "artist") return this._i18n("ui.artist");
    if (type === "radio") return this._i18n("ui.radio");
    if (type === "track") return this._i18n("ui.track");
    if (type === "album") return this._i18n("ui.album");
    return this._i18n("ui.playlist");
  }
  _simpleWizardCandidateFromItem(item = {}, fallbackType = "playlist", options = {}) {
    let normalized = {};
    try {
      normalized = this._normalizeMediaItem(item) || {};
    } catch (_) {
    }
    const uri = String(options.uri || normalized.uri || item?.uri || item?.media_item?.uri || "").trim();
    if (!uri) return null;
    const mediaType = String(options.mediaType || normalized.media_type || item?.media_type || item?.type || fallbackType || "playlist").toLowerCase();
    const name = normalized.name || item?.name || item?.title || item?.media_item?.name || uri;
    const subtitle = options.subtitle || this._artistName(item) || normalized.artist || item?.artist || item?.artist_str || item?.album?.name || item?.metadata?.description || this._simpleWizardMediaTypeLabel(mediaType);
    const image = this._artUrl(item) || normalized.image || item?.image || item?.image_url || item?.media_item?.image || item?.media_item?.album?.image || "";
    return {
      uri,
      media_type: mediaType,
      name,
      subtitle,
      image,
      radioMode: !!options.radioMode
    };
  }
  _simpleWizardUniqueCandidates(candidates = []) {
    const seen = /* @__PURE__ */ new Set();
    return (Array.isArray(candidates) ? candidates : []).filter((candidate) => {
      const uri = String(candidate?.uri || "").trim();
      if (!uri || seen.has(uri)) return false;
      seen.add(uri);
      return true;
    });
  }
  async _simpleWizardFindCandidates(state = this._simpleWizardState()) {
    const out = [];
    const addGroup = (items = [], mediaType = "playlist", options = {}) => {
      (Array.isArray(items) ? items : []).forEach((item) => {
        const candidate = this._simpleWizardCandidateFromItem(item, mediaType, options);
        if (candidate) out.push(candidate);
      });
    };
    if (state.source !== "content") {
      const genre = this._simpleWizardGenres().find((item) => item.id === state.genre) || this._simpleWizardGenres()[0];
      const customQuery = String(state.customGenre || "").trim();
      const searchLabel = genre.id === "custom" && customQuery ? customQuery : genre.label;
      const queries = genre.id === "custom" && customQuery ? [customQuery] : genre.queries || [genre.label];
      for (const query2 of queries.slice(0, 3)) {
        try {
          const results = await this._search(query2);
          addGroup(results.playlists, "playlist", { subtitle: searchLabel });
          addGroup(results.tracks, "track", { subtitle: searchLabel });
          addGroup(results.albums, "album", { subtitle: searchLabel });
          addGroup(results.radio, "radio", { subtitle: searchLabel });
        } catch (_) {
        }
        if (this._simpleWizardUniqueCandidates(out).length >= 8) break;
      }
      if (!out.length) {
        const fallbacks = await Promise.allSettled([
          this._fetchLibrary("playlist", "random", 18, false),
          this._fetchLibrary("album", "random", 12, false),
          this._fetchLibrary("radio", "random", 12, false)
        ]);
        fallbacks.forEach((result, index) => {
          if (result.status !== "fulfilled") return;
          addGroup(result.value, index === 1 ? "album" : index === 2 ? "radio" : "playlist", { subtitle: searchLabel });
        });
      }
      return this._shuffleDiscoveryItems(this._simpleWizardUniqueCandidates(out), Date.now() + (this._simpleWizardToken || 0)).slice(0, 8);
    }
    const query = String(state.query || "").trim();
    const type = state.contentType || "playlist";
    if (type === "library_radio") {
      if (query) {
        try {
          const results = await this._search(query);
          addGroup(results.radio, "radio");
        } catch (_) {
        }
        try {
          const stations = await this._fetchRadioBrowserStations(query, 18, { countryCode: this._mobileRadioBrowserCountry() || "all" });
          addGroup(stations, "radio");
        } catch (_) {
        }
      } else {
        const radios = await Promise.allSettled([
          this._fetchLibrary("radio", "sort_name", 80, true),
          this._fetchLibrary("radio", "random", 80, false),
          this._fetchRadioBrowserStations("", 30, { countryCode: this._mobileRadioBrowserCountry() || "all" })
        ]);
        radios.forEach((result) => {
          if (result.status === "fulfilled") addGroup(result.value, "radio");
        });
      }
      return this._shuffleDiscoveryItems(this._simpleWizardUniqueCandidates(out), Date.now() + (this._simpleWizardToken || 0)).slice(0, 8);
    }
    if (type === "artist" || type === "artist_radio") {
      if (query) {
        try {
          const results = await this._search(query);
          addGroup(results.artists, "artist", {
            subtitle: type === "artist_radio" ? this._i18n("ui.artist_radio") : this._i18n("ui.artist"),
            radioMode: type === "artist_radio",
            mediaType: "artist"
          });
        } catch (_) {
        }
      } else {
        try {
          const artists = await this._fetchLibrary("artist", "sort_name", 80, false);
          addGroup(artists, "artist", {
            subtitle: type === "artist_radio" ? this._i18n("ui.artist_radio") : this._i18n("ui.artist"),
            radioMode: type === "artist_radio",
            mediaType: "artist"
          });
        } catch (_) {
        }
      }
      return this._shuffleDiscoveryItems(this._simpleWizardUniqueCandidates(out), Date.now() + (this._simpleWizardToken || 0)).slice(0, 8);
    }
    if (query) {
      try {
        const results = await this._search(query);
        addGroup(results.playlists, "playlist");
      } catch (_) {
      }
    } else {
      const playlists = await Promise.allSettled([
        this._loadScheduledStartPlaylists(),
        this._fetchLibrary("playlist", "sort_name", 120, true),
        this._fetchLibrary("playlist", "random", 80, false)
      ]);
      playlists.forEach((result) => {
        if (result.status === "fulfilled") addGroup(result.value, "playlist");
      });
    }
    return this._shuffleDiscoveryItems(this._simpleWizardUniqueCandidates(out), Date.now() + (this._simpleWizardToken || 0)).slice(0, 8);
  }
  async _simpleWizardBuildCandidates(sourceEl = null) {
    const state = this._simpleWizardState();
    const queryInput = this.$("simpleWizardQueryInput");
    const genreSelect = this.$("simpleWizardGenreSelect");
    const customGenreInput = this.$("simpleWizardCustomGenreInput");
    if (queryInput) state.query = queryInput.value || "";
    if (genreSelect) state.genre = this._simpleWizardGenres().some((genre) => genre.id === genreSelect.value) ? genreSelect.value : state.genre;
    if (customGenreInput) state.customGenre = customGenreInput.value || "";
    if (!state.selectedPlayers?.length) {
      state.step = "players";
      this._toastError(this._i18n("ui.choose_at_least_one_player"));
      await this._renderMobileMenu();
      return;
    }
    if (state.source !== "content" && state.genre === "custom" && !String(state.customGenre || "").trim()) {
      state.step = "source";
      this._toastError(this._i18n("ui.type_a_free_style_first"));
      await this._renderMobileMenu();
      return;
    }
    if (sourceEl) this._flashInteraction(sourceEl);
    const token = ++this._simpleWizardToken;
    state.step = "review";
    state.loading = true;
    state.error = "";
    state.candidates = [];
    state.selectedIndex = 0;
    await this._renderMobileMenu();
    try {
      const candidates = await this._simpleWizardFindCandidates(state);
      if (token !== this._simpleWizardToken) return;
      state.candidates = candidates;
      state.error = candidates.length ? "" : this._i18n("ui.no_matching_content_was_found");
    } catch (error) {
      if (token !== this._simpleWizardToken) return;
      state.candidates = [];
      state.error = error?.message || this._i18n("ui.could_not_find_music");
    } finally {
      if (token === this._simpleWizardToken) {
        state.loading = false;
        await this._renderMobileMenu();
      }
    }
  }
  _showSimpleWizardPopup(candidate = {}, entityIds = []) {
    const host = this.$("surprisePopup");
    if (!host) return;
    const targetName = this._simpleWizardSelectedPlayerNames(entityIds);
    const art = candidate.image || "";
    host.innerHTML = `
      <div class="surprise-popup-card simple-wizard-popup-card">
        <div class="surprise-popup-player">${this._esc(this._i18n("ui.playing_on_2"))}: ${this._esc(targetName)}</div>
        <div class="surprise-popup-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("wand")}</div>
        <div class="surprise-popup-title">${this._esc(candidate.name || this._i18n("ui.selected_music"))}</div>
      </div>
    `;
    host.classList.add("open", "simple-wizard-popup");
    clearTimeout(this._simpleWizardPopupTimer);
    this._simpleWizardPopupTimer = setTimeout(() => {
      host.classList.remove("open", "simple-wizard-popup");
    }, 1700);
  }
  async _simpleWizardPlay(sourceEl = null) {
    const state = this._simpleWizardState();
    const candidates = Array.isArray(state.candidates) ? state.candidates : [];
    const index = Math.max(0, Math.min(candidates.length - 1, Number(state.selectedIndex || 0)));
    const candidate = candidates[index];
    if (!candidate?.uri) {
      await this._simpleWizardBuildCandidates(sourceEl);
      return;
    }
    const targets = [...new Set((state.selectedPlayers || []).filter((entityId) => this._playerByEntityId(entityId)))];
    if (!targets.length) {
      state.step = "players";
      await this._renderMobileMenu();
      this._toastError(this._i18n("ui.choose_at_least_one_player"));
      return;
    }
    if (sourceEl) this._flashInteraction(sourceEl);
    const primaryId = targets[0];
    if (targets.length > 1) {
      try {
        const joined = await this._applySpeakerGroupFor(primaryId, targets.slice(1));
        if (!joined) {
          this._toastError(this._i18n("ui.select_at_least_two_players_to_create_a_group"));
          return;
        }
        await new Promise((resolve) => window.setTimeout(resolve, 250));
      } catch (error) {
        this._toastError(error?.message || this._i18n("ui.queue_action_failed"));
        return;
      }
    }
    const ok = await this._playMediaOnPlayers([primaryId], candidate.uri, candidate.media_type || "playlist", "play", {
      label: candidate.name || "",
      silent: true,
      radioMode: !!candidate.radioMode
    });
    if (!ok) {
      this._toastError(this._i18n("ui.could_not_start_playback"));
      return;
    }
    this._selectPlayer(primaryId, true);
    this._showSimpleWizardPopup(candidate, targets);
    setTimeout(() => {
      this._closeMobileMenu();
      this._syncNowPlayingUI();
    }, 1750);
  }
  async _handleSimpleWizardClick(e) {
    const root = e.target.closest?.(".simple-wizard-shell");
    if (!root) return false;
    const button = e.target.closest?.("[data-simple-reset], [data-simple-player], [data-simple-all-players], [data-simple-next], [data-simple-back], [data-simple-source], [data-simple-genre], [data-simple-content], [data-simple-build], [data-simple-candidate], [data-simple-play]");
    if (!button) return false;
    e.preventDefault();
    e.stopPropagation();
    const state = this._simpleWizardState();
    if (button.dataset.simpleReset !== void 0) {
      this._resetSimpleWizardState();
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleAllPlayers !== void 0) {
      const players = this._simpleWizardPlayerPool();
      const ids = players.map((player) => player.entity_id);
      const selected = new Set(state.selectedPlayers || []);
      state.selectedPlayers = ids.length && ids.every((id) => selected.has(id)) ? this._simpleWizardDefaultPlayerIds(players) : ids;
      state.candidates = [];
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simplePlayer) {
      const entityId = button.dataset.simplePlayer;
      const selected = new Set(state.selectedPlayers || []);
      if (selected.has(entityId)) {
        if (selected.size <= 1) {
          this._toastError(this._i18n("ui.choose_at_least_one_player"));
          return true;
        }
        selected.delete(entityId);
      } else selected.add(entityId);
      state.selectedPlayers = Array.from(selected);
      state.candidates = [];
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleNext) {
      if (button.dataset.simpleNext === "source" && !state.selectedPlayers?.length) {
        this._toastError(this._i18n("ui.choose_at_least_one_player"));
        return true;
      }
      state.step = button.dataset.simpleNext;
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleBack) {
      state.step = button.dataset.simpleBack;
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleSource) {
      state.source = button.dataset.simpleSource === "content" ? "content" : "genre";
      state.candidates = [];
      state.selectedIndex = 0;
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleGenre) {
      state.genre = button.dataset.simpleGenre;
      state.candidates = [];
      state.selectedIndex = 0;
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleContent) {
      state.contentType = button.dataset.simpleContent;
      state.candidates = [];
      state.selectedIndex = 0;
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simpleBuild !== void 0) {
      await this._simpleWizardBuildCandidates(button);
      return true;
    }
    if (button.dataset.simpleCandidate !== void 0) {
      state.selectedIndex = Math.max(0, Number(button.dataset.simpleCandidate) || 0);
      await this._renderMobileMenu();
      return true;
    }
    if (button.dataset.simplePlay !== void 0) {
      await this._simpleWizardPlay(button);
      return true;
    }
    return false;
  }
  _sleepTimerActionTile(label, minutes, tone = "queue") {
    return `
      <button class="menu-item action-tile tone-${this._esc(tone)}" data-sleep-timer-start="${this._esc(String(minutes))}">
        <span class="menu-item-main">
          <span class="menu-item-ico">${this._iconSvg("timer")}</span>
          <span style="min-width:0;flex:1;">
            <span class="menu-item-title">${this._esc(label)}</span>
            <span class="menu-item-sub">${this._esc(this._i18n("ui.set_sleep_timer"))}</span>
          </span>
        </span>
      </button>
    `;
  }
  _sleepTimerMenuHtml() {
    this._loadPlayers();
    const remaining = this._sleepTimerRemainingLabel();
    const active = this._sleepTimerRemainingMs() > 0;
    const status = active ? this._m(`Active for ${remaining}`, `פעיל לעוד ${remaining}`) : this._i18n("ui.no_sleep_timer_is_active");
    const schedules = this._scheduledStartSchedules();
    const editSchedule = schedules.find((schedule) => schedule.id === this._state.mobileStartScheduleEditId) || null;
    const showWakeEditor = !!editSchedule || this._state.mobileStartScheduleEditId === "__new__";
    const wakeDraftSchedule = showWakeEditor ? {
      id: editSchedule?.id || "__new__",
      time: this._state.mobileStartTimerTime || editSchedule?.time || "07:00",
      player: this._state.mobileStartTimerPlayer || editSchedule?.player || "",
      playlist: this._state.mobileStartTimerPlaylist ?? editSchedule?.playlist ?? "",
      playlistName: this._state.mobileStartTimerPlaylistName ?? editSchedule?.playlistName ?? "",
      volume: this._state.mobileStartTimerVolume ?? editSchedule?.volume ?? 35,
      days: this._state.mobileStartTimerDays || editSchedule?.days,
      afterRun: this._state.mobileStartTimerAfterRun || editSchedule?.afterRun || "keep"
    } : editSchedule;
    const scheduledTime = this._normalizeClockTime(wakeDraftSchedule?.time || "07:00", "07:00");
    const scheduledPlayer = this._scheduledStartPlayerId(wakeDraftSchedule);
    const scheduledVolume = Math.max(0, Math.min(100, Number(wakeDraftSchedule?.volume ?? 35) || 35));
    const scheduledDays = new Set(this._normalizeNightModeDays(wakeDraftSchedule?.days || this._state.mobileStartTimerDays));
    const scheduledAfterRun = String(wakeDraftSchedule?.afterRun || "keep") === "disable" ? "disable" : "keep";
    const nightMode = this._mobileNightMode();
    const nightWindow = this._nightModeWindow();
    const nightDays = new Set(this._nightModeDays());
    const activeTab = ["timers", "wake", "night"].includes(this._state.mobileSchedulesTab) ? this._state.mobileSchedulesTab : "timers";
    const playerOptions = (this._state.players || []).map((player) => {
      const name = player.attributes?.friendly_name || player.entity_id;
      return `<option value="${this._esc(player.entity_id)}" ${player.entity_id === scheduledPlayer ? "selected" : ""}>${this._esc(name)}</option>`;
    }).join("");
    const scheduleRows = schedules.length ? `
      <div class="schedule-list">
        ${schedules.map((schedule) => {
      const player = this._playerByEntityId(this._scheduledStartPlayerId(schedule));
      const playerName = player?.attributes?.friendly_name || this._i18n("ui.selected_player_3");
      const afterRunLabel = schedule.afterRun === "disable" ? this._i18n("ui.turns_off_after_run") : this._i18n("ui.stays_active");
      const days = this._nightModeDayOptions().filter(([value]) => this._normalizeNightModeDays(schedule.days).includes(value)).map(([, label]) => label).join(" ");
      return `
            <div class="schedule-row ${schedule.enabled === false ? "disabled" : ""} ${schedule.id === this._state.mobileStartScheduleEditId ? "editing" : ""}">
              <button class="schedule-row-main" data-start-schedule-edit="${this._esc(schedule.id)}">
                <span class="schedule-row-time">${this._esc(schedule.time)}</span>
                <span class="schedule-row-copy">
                  <span class="schedule-row-title">${this._esc(playerName)}</span>
                  <span class="schedule-row-sub">${this._esc(`${this._scheduledStartPlaylistLabel(schedule)} · ${schedule.volume}% · ${afterRunLabel} · ${days}`)}</span>
                </span>
              </button>
              <div class="schedule-row-actions">
                <button class="settings-pill ${schedule.enabled !== false ? "active" : ""}" data-start-schedule-toggle="${this._esc(schedule.id)}">${this._esc(schedule.enabled !== false ? this._i18n("ui.on") : this._i18n("ui.off"))}</button>
                <button class="settings-pill" data-start-schedule-delete="${this._esc(schedule.id)}">${this._iconSvg("trash")}</button>
              </div>
            </div>
          `;
    }).join("")}
      </div>
    ` : `<div class="notice open">${this._i18n("ui.no_wake_schedules_yet")}</div>`;
    const timersHtml = `
      <div class="settings-group scheduled-start-card schedule-panel-card schedule-timers-card">
        <div class="settings-label">${this._esc(this._i18n("ui.sleep_timer_2"))}</div>
        <div class="settings-hint">${this._esc(status)}</div>
        <div class="sleep-timer-action-row ${active ? "with-cancel" : ""}" aria-label="${this._esc(this._i18n("ui.sleep_timer_presets"))}">
          <button class="sleep-timer-action-btn" data-sleep-timer-start="15">+15</button>
          <button class="sleep-timer-action-btn" data-sleep-timer-start="30">+30</button>
          <button class="sleep-timer-action-btn" data-sleep-timer-start="60">+60</button>
          ${active ? `<button class="sleep-timer-action-btn danger" data-sleep-timer-cancel>${this._esc(this._i18n("ui.cancel_2"))}</button>` : ``}
        </div>
      </div>
    `;
    const wakeHtml = `
      <div class="wake-schedule-layout">
        <div class="settings-group scheduled-start-card wake-schedule-list-card">
          <div class="settings-label">${this._esc(this._i18n("ui.wake_schedules"))}</div>
          <div class="settings-hint">${this._esc(this._scheduledStartStatusLabel())}</div>
          ${scheduleRows}
          <div class="settings-actions">
            <button class="settings-pill" data-start-schedule-new>${this._esc(this._i18n("ui.new_schedule"))}</button>
          </div>
        </div>
        ${showWakeEditor ? `<div class="settings-group scheduled-start-card wake-schedule-editor-card">
          <div class="settings-label">${this._esc(editSchedule ? this._i18n("ui.edit_schedule") : this._i18n("ui.new_wake_schedule"))}</div>
          <div class="scheduled-start-grid">
            <label class="night-time-card" for="scheduledStartTimeInput">
              <span class="night-time-label">${this._esc(this._i18n("ui.start_time"))}</span>
              <input class="night-time-input" id="scheduledStartTimeInput" data-schedule-form-control type="time" value="${this._esc(scheduledTime)}" step="60" aria-label="${this._esc(this._i18n("ui.start_time"))}">
            </label>
            <label class="scheduled-start-field" for="scheduledStartPlayerSelect">
              <span class="settings-label">${this._esc(this._i18n("ui.player_2"))}</span>
              <select class="media-sort-select settings-select" id="scheduledStartPlayerSelect" data-schedule-form-control aria-label="${this._esc(this._i18n("ui.player_2"))}">
                ${playerOptions || `<option value="">${this._esc(this._i18n("ui.no_players_found"))}</option>`}
              </select>
            </label>
            <label class="scheduled-start-field" for="scheduledStartPlaylistSelect">
              <span class="settings-label">${this._esc(this._i18n("ui.playlist"))}</span>
              <select class="media-sort-select settings-select" id="scheduledStartPlaylistSelect" data-schedule-form-control aria-label="${this._esc(this._i18n("ui.playlist"))}">
                ${this._scheduledStartPlaylistOptionsHtml(wakeDraftSchedule)}
              </select>
            </label>
            <label class="scheduled-start-field" for="scheduledStartAfterRunSelect">
              <span class="settings-label">${this._esc(this._i18n("ui.after_run"))}</span>
              <select class="media-sort-select settings-select" id="scheduledStartAfterRunSelect" data-schedule-form-control aria-label="${this._esc(this._i18n("ui.after_run"))}">
                <option value="keep" ${scheduledAfterRun === "keep" ? "selected" : ""}>${this._esc(this._i18n("ui.stay_active"))}</option>
                <option value="disable" ${scheduledAfterRun === "disable" ? "selected" : ""}>${this._esc(this._i18n("ui.turn_off"))}</option>
              </select>
            </label>
          </div>
          <div class="settings-range scheduled-volume-field">
            <div class="settings-label">${this._esc(this._i18n("ui.volume"))}</div>
            <input id="scheduledStartVolumeInput" data-schedule-form-control type="range" min="0" max="100" step="1" value="${this._esc(String(scheduledVolume))}">
            <div class="settings-value">${this._esc(String(scheduledVolume))}%</div>
          </div>
          <div class="settings-label">${this._esc(this._i18n("ui.active_days"))}</div>
          <div class="settings-check-grid">
            ${this._nightModeDayOptions().map(([value, label]) => `
              <label class="settings-check-pill">
                <input type="checkbox" data-schedule-form-control data-start-timer-day="${this._esc(String(value))}" ${scheduledDays.has(value) ? "checked" : ""}>
                <span>${this._esc(label)}</span>
              </label>`).join("")}
          </div>
          <div class="settings-actions">
            <button class="settings-pill active" data-start-timer-save>${this._esc(editSchedule ? this._i18n("ui.save_schedule") : this._i18n("ui.create_schedule"))}</button>
            ${editSchedule ? `<button class="settings-pill" data-start-timer-clear>${this._esc(this._i18n("ui.delete_schedule"))}</button>` : ``}
          </div>
        </div>` : ``}
      </div>
    `;
    const nightScheduleControlsHtml = nightMode === "auto" ? `
        <div class="scheduled-start-grid two-col">
          <label class="night-time-card" for="mobileNightStartInput">
            <span class="night-time-label">${this._esc(this._i18n("ui.start_time_2"))}</span>
            <input class="night-time-input" id="mobileNightStartInput" data-schedule-form-control type="time" value="${this._esc(nightWindow.start)}" step="60" aria-label="${this._esc(this._i18n("ui.start_time_2"))}">
          </label>
          <label class="night-time-card" for="mobileNightEndInput">
            <span class="night-time-label">${this._esc(this._i18n("ui.end_time"))}</span>
            <input class="night-time-input" id="mobileNightEndInput" data-schedule-form-control type="time" value="${this._esc(nightWindow.end)}" step="60" aria-label="${this._esc(this._i18n("ui.end_time"))}">
          </label>
        </div>
        <div class="settings-label">${this._esc(this._i18n("ui.active_days"))}</div>
        <div class="settings-check-grid">
          ${this._nightModeDayOptions().map(([value, label]) => `
            <label class="settings-check-pill">
              <input type="checkbox" data-schedule-form-control data-setting-night-day="${this._esc(String(value))}" ${nightDays.has(value) ? "checked" : ""}>
              <span>${this._esc(label)}</span>
            </label>`).join("")}
        </div>
        <div class="settings-actions">
          <button class="settings-pill active" data-setting-night-window-save>${this._esc(this._i18n("ui.apply_schedule"))}</button>
        </div>
      ` : `<div class="notice open">${this._esc(nightMode === "on" ? this._i18n("ui.night_mode_stays_on_until_you_choose_another_mode") : this._i18n("ui.night_mode_is_off_until_you_choose_another_mode"))}</div>`;
    const nightHtml = `
      <div class="settings-group scheduled-start-card schedule-panel-card schedule-night-card">
        <div class="settings-label">${this._esc(this._i18n("ui.night_mode"))}</div>
        <div class="settings-pills">
          ${this._settingsPill(this._i18n("ui.off"), "off", nightMode, "data-setting-night-mode")}
          ${this._settingsPill("Auto", "auto", nightMode, "data-setting-night-mode")}
          ${this._settingsPill(this._i18n("ui.on"), "on", nightMode, "data-setting-night-mode")}
        </div>
        ${nightScheduleControlsHtml}
      </div>
    `;
    return `
      <div class="settings-shell">
        <div class="schedule-tabs" role="tablist" aria-label="${this._esc(this._i18n("ui.schedules"))}">
          <button class="settings-pill ${activeTab === "timers" ? "active" : ""}" data-schedule-tab="timers">${this._esc(this._i18n("ui.timers"))}</button>
          <button class="settings-pill ${activeTab === "wake" ? "active" : ""}" data-schedule-tab="wake">${this._esc(this._i18n("ui.wake"))}</button>
          <button class="settings-pill ${activeTab === "night" ? "active" : ""}" data-schedule-tab="night">${this._esc(this._i18n("ui.night"))}</button>
        </div>
        <div class="schedule-content">
          ${activeTab === "wake" ? wakeHtml : activeTab === "night" ? nightHtml : timersHtml}
        </div>
      </div>
    `;
  }
  _mainMenuHtml() {
    if (this._isHotelMode()) {
      return `
        <div class="action-grid hotel-action-grid">
          ${this._navMenuItem("players", this._iconSvg("speaker"), this._i18n("ui.players"), this._m("Choose the room", "בחירת נגן"), "players")}
          ${this._navMenuItem("quick_search", this._iconSvg("search"), this._i18n("ui.search"), this._m("Find music", "חיפוש מוזיקה"), "simple")}
        </div>
      `;
    }
    return `
      <div class="action-grid">
        ${this._navMenuItem("simple_wizard", this._iconSvg("wand"), "FLOW", this._i18n("ui.a_guided_music_wizard"), "simple")}
        ${this._discoveryModeEnabled() ? this._navMenuItem("discovery", this._iconSvg("grid"), this._i18n("ui.discover_music", {}, this._m("Discover", "גלה מוזיקה")), this._i18n("ui.style_and_genres", {}, this._m("Style and genres", "סגנונות וז'אנרים")), "discovery") : ""}
        ${this._navMenuItem("players", this._iconSvg("speaker"), this._i18n("ui.players"), this._i18n("ui.players_queue_and_groups"), "players")}
        ${this._navMenuItem("library_liked", this._iconSvg("heart_filled"), this._i18n("ui.liked"), this._i18n("ui.open_saved_songs"), "liked")}
        ${this._navMenuItem("sleep_timer", this._iconSvg("timer"), this._i18n("ui.schedules"), this._i18n("ui.sleep_timer_and_morning_playback"), "announcement")}
        ${this._navMenuItem("announcements", this._iconSvg("announcement"), this._i18n("ui.announcements"), this._i18n("ui.send_a_voice_message"), "announcement")}
      </div>
    `;
  }
  _playerArtworkUrl(player = null, size = 180) {
    if (!player) return "";
    const attrs = player.attributes || {};
    const localQueueArt = this._isLocalSendspinPlayer(player) ? this._queueItemImageUrl(this._state.maQueueState?.current_item, size) : "";
    return localQueueArt || this._imageUrl(attrs.entity_picture_local, size) || this._imageUrl(attrs.entity_picture, size) || this._imageUrl(attrs.media_image_url, size) || this._imageUrl(attrs.media_image, size) || this._imageUrl(attrs.thumbnail, size) || "";
  }
  _playersActionHubHtml(options = {}) {
    const queueCount = this._getNowPlayingQueueItems().length || Number(this._state.maQueueState?.items || 0) || 0;
    const thisDeviceTitle = options.thisDeviceTitle || this._i18n("ui.player_on_this_device");
    const thisDeviceActionName = options.thisDeviceActionName || "connect_this_device";
    const thisDeviceActive = !!options.connectedThisDevice || !!options.startingThisDevice;
    const thisDeviceClass = [
      "players-action-chip",
      "this-device",
      options.connectedThisDevice ? "connected" : "",
      options.startingThisDevice ? "connecting" : "",
      options.disconnectingThisDevice ? "disconnecting" : ""
    ].filter(Boolean).join(" ");
    return `
      <div class="players-action-bar">
        <div class="players-action-shell">
          <div class="players-action-hub">
            <button class="players-action-chip" data-menu-nav="queue" title="${this._esc(this._i18n("ui.queue_2"))}">
              <span class="players-action-icon">${this._iconSvg("queue")}</span>
              <span class="players-action-label">${this._esc(this._i18n("ui.queue_2"))}</span>
              ${queueCount ? `<strong class="players-action-badge">${this._esc(String(queueCount))}</strong>` : ``}
            </button>
            <button class="players-action-chip" data-menu-nav="transfer" title="${this._esc(this._i18n("ui.transfer_queue_2"))}">
              <span class="players-action-icon">${this._iconSvg("repeat")}</span>
              <span class="players-action-label">${this._esc(this._i18n("ui.transfer_queue_2"))}</span>
            </button>
            <button class="players-action-chip" data-menu-nav="group" title="${this._esc(this._i18n("ui.group_speakers_2"))}">
              <span class="players-action-icon">${this._iconSvg("speaker_group")}</span>
              <span class="players-action-label">${this._esc(this._i18n("ui.group"))}</span>
            </button>
            <button class="${this._esc(thisDeviceClass)}" data-menu-action="${this._esc(thisDeviceActionName)}" aria-pressed="${thisDeviceActive ? "true" : "false"}" title="${this._esc(thisDeviceTitle)}" aria-label="${this._esc(thisDeviceTitle)}">
              <span class="players-action-icon">${this._iconSvg("speaker")}</span>
              <span class="players-action-label">${this._esc(this._i18n("ui.this_device_2"))}</span>
              <span class="players-action-dot" aria-hidden="true"></span>
            </button>
          </div>
        </div>
        <button class="players-action-stop" data-menu-nav="stop_all" title="${this._esc(this._cleanAllLabel())}">
          <span class="players-action-stop-icon">${this._iconSvg("stop")}</span>
          <span>${this._esc(this._cleanAllLabel())}</span>
        </button>
      </div>
    `;
  }
  _discoveryCategoryOptions() {
    return this._musicStyleCatalog().map((style) => ({
      ...style,
      key: style.id,
      query: style.query || style.queries?.[0] || style.label,
      queries: style.queries?.length ? style.queries : [`${style.label} playlist`]
    }));
  }
  _discoveryGenreFallbackOptions() {
    return [
      { key: "all", label: this._i18n("ui.all", {}, this._discoveryGenreLabel("All", "הכל", "Alle")), query: "" }
    ];
  }
  _discoveryCategory() {
    const categories = this._discoveryCategoryOptions();
    const found = categories.find((entry) => entry.key === this._state.discoveryCategoryKey) || categories[0];
    if (found?.key && this._state.discoveryCategoryKey !== found.key) this._state.discoveryCategoryKey = found.key;
    return found;
  }
  _startDiscoverySession() {
    this._state.discoverySessionSeed = Date.now() + Math.floor(Math.random() * 1e5);
    this._state.discoveryExpandedUri = "";
  }
  _discoveryShuffleScore(item = {}, index = 0, seed = this._state.discoverySessionSeed || Date.now()) {
    const text = `${seed}|${item?.uri || ""}|${item?.name || item?.title || ""}|${index}`;
    let hash = 2166136261;
    for (let i = 0; i < text.length; i += 1) {
      hash ^= text.charCodeAt(i);
      hash = Math.imul(hash, 16777619);
    }
    return hash >>> 0;
  }
  _shuffleDiscoveryItems(items = [], seed = this._state.discoverySessionSeed || Date.now()) {
    return (Array.isArray(items) ? items : []).map((item, index) => ({ item, index, score: this._discoveryShuffleScore(item, index, seed) })).sort((a, b) => a.score - b.score || a.index - b.index).map((entry) => entry.item);
  }
  _discoveryGenreKey(value = "") {
    const normalized = String(value || "").trim().toLowerCase().normalize("NFKD").replace(/[^\p{L}\p{N}]+/gu, "-").replace(/^-+|-+$/g, "");
    return normalized || "genre";
  }
  _discoveryGenreOptionFromItem(item = {}, index = 0) {
    const mediaItem = item?.media_item || item?.item || item;
    const label = String(mediaItem?.name || mediaItem?.title || item?.name || item?.title || mediaItem?.item_id || mediaItem?.uri || "").trim();
    if (!label) return null;
    const itemId = String(mediaItem?.item_id || item?.item_id || mediaItem?.id || item?.id || "").trim();
    const uri = String(mediaItem?.uri || item?.uri || "").trim();
    const keySeed = itemId ? `genre-${itemId}` : uri || label;
    return {
      key: this._discoveryGenreKey(keySeed) || `genre-${index}`,
      label,
      query: label,
      item_id: itemId,
      uri
    };
  }
  _discoveryGenreLabel(en, he, de = en) {
    const lang = this._language();
    if (lang === "he") return he;
    if (lang === "de") return de;
    return en;
  }
  _discoveryPopularGenreProfiles() {
    return [
      { key: "pop", label: this._discoveryGenreLabel("Pop", "פופ", "Pop"), query: "pop", aliases: ["pop", "pop music", "afropop", "synthpop", "synth pop", "k-pop", "kpop", "j-pop", "jpop", "israeli pop"], children: [
        { key: "dance-pop", label: this._discoveryGenreLabel("Dance Pop", "דאנס פופ", "Dance-Pop"), query: "dance pop" },
        { key: "k-pop", label: this._discoveryGenreLabel("K-Pop", "קיי־פופ", "K-Pop"), query: "k-pop" },
        { key: "indie-pop", label: this._discoveryGenreLabel("Indie Pop", "אינדי פופ", "Indie-Pop"), query: "indie pop" },
        { key: "israeli-pop", label: this._discoveryGenreLabel("Israeli Pop", "פופ ישראלי", "Israelischer Pop"), query: "israeli pop" }
      ] },
      { key: "hip-hop", label: this._discoveryGenreLabel("Hip-Hop", "היפ־הופ", "Hip-Hop"), query: "hip hop", aliases: ["hip hop", "hip-hop", "hiphop", "rap", "trap"], children: [
        { key: "rap", label: this._discoveryGenreLabel("Rap", "ראפ", "Rap"), query: "rap" },
        { key: "trap", label: this._discoveryGenreLabel("Trap", "טראפ", "Trap"), query: "trap" },
        { key: "old-school", label: this._discoveryGenreLabel("Old School", "אולד סקול", "Old School"), query: "old school hip hop" },
        { key: "israeli-rap", label: this._discoveryGenreLabel("Israeli Rap", "ראפ ישראלי", "Israelischer Rap"), query: "israeli rap" }
      ] },
      { key: "rock", label: this._discoveryGenreLabel("Rock", "רוק", "Rock"), query: "rock", aliases: ["rock", "classic rock", "alt rock", "alternative rock", "hard rock", "soft rock", "punk"], children: [
        { key: "classic-rock", label: this._discoveryGenreLabel("Classic Rock", "רוק קלאסי", "Classic Rock"), query: "classic rock" },
        { key: "alternative-rock", label: this._discoveryGenreLabel("Alternative Rock", "רוק אלטרנטיבי", "Alternative Rock"), query: "alternative rock" },
        { key: "indie-rock", label: this._discoveryGenreLabel("Indie Rock", "אינדי רוק", "Indie-Rock"), query: "indie rock" },
        { key: "punk", label: this._discoveryGenreLabel("Punk", "פאנק רוק", "Punk"), query: "punk rock" }
      ] },
      { key: "electronic", label: this._discoveryGenreLabel("Electronic", "אלקטרוני", "Elektronisch"), query: "electronic", aliases: ["electronic", "electronica", "edm", "house", "deep house", "tech house", "afro house", "afro tech", "techno", "trance", "dubstep", "drum and bass", "dnb"], children: [
        { key: "house", label: this._discoveryGenreLabel("House", "האוס", "House"), query: "house music" },
        { key: "techno", label: this._discoveryGenreLabel("Techno", "טכנו", "Techno"), query: "techno" },
        { key: "trance", label: this._discoveryGenreLabel("Trance", "טראנס", "Trance"), query: "trance" },
        { key: "edm", label: this._discoveryGenreLabel("EDM", "EDM", "EDM"), query: "edm" }
      ] },
      { key: "dance", label: this._discoveryGenreLabel("Dance", "דאנס", "Dance"), query: "dance", aliases: ["dance", "club", "disco", "dance pop", "party"], children: [
        { key: "party", label: this._discoveryGenreLabel("Party", "מסיבה", "Party"), query: "party hits" },
        { key: "club", label: this._discoveryGenreLabel("Club", "קלאב", "Club"), query: "club dance" },
        { key: "disco", label: this._discoveryGenreLabel("Disco", "דיסקו", "Disco"), query: "disco" },
        { key: "workout", label: this._discoveryGenreLabel("Workout", "אימון", "Training"), query: "workout dance" }
      ] },
      { key: "rnb", label: this._discoveryGenreLabel("R&B", "R&B", "R&B"), query: "r&b", aliases: ["r&b", "rnb", "rhythm and blues", "afro r&b"], children: [
        { key: "contemporary", label: this._discoveryGenreLabel("Contemporary", "עכשווי", "Zeitgenoessisch"), query: "contemporary r&b" },
        { key: "slow-jams", label: this._discoveryGenreLabel("Slow Jams", "סלואו ג׳אם", "Slow Jams"), query: "slow jams" },
        { key: "rnb-hits", label: this._discoveryGenreLabel("R&B Hits", "להיטי R&B", "R&B-Hits"), query: "r&b hits" }
      ] },
      { key: "soul", label: this._discoveryGenreLabel("Soul", "סול", "Soul"), query: "soul", aliases: ["soul", "neo soul", "afro soul"], children: [
        { key: "neo-soul", label: this._discoveryGenreLabel("Neo Soul", "נאו סול", "Neo Soul"), query: "neo soul" },
        { key: "funk", label: this._discoveryGenreLabel("Funk", "פאנק", "Funk"), query: "funk soul" },
        { key: "motown", label: this._discoveryGenreLabel("Motown", "מוטאון", "Motown"), query: "motown soul" }
      ] },
      { key: "jazz", label: this._discoveryGenreLabel("Jazz", "ג׳אז", "Jazz"), query: "jazz", aliases: ["jazz", "smooth jazz", "vocal jazz", "bebop"], children: [
        { key: "smooth", label: this._discoveryGenreLabel("Smooth Jazz", "ג׳אז חלק", "Smooth Jazz"), query: "smooth jazz" },
        { key: "vocal", label: this._discoveryGenreLabel("Vocal Jazz", "ג׳אז ווקאלי", "Vocal Jazz"), query: "vocal jazz" },
        { key: "fusion", label: this._discoveryGenreLabel("Fusion", "פיוז׳ן", "Fusion"), query: "jazz fusion" }
      ] },
      { key: "israeli", label: this._i18n("ui.israeli"), query: "israeli hebrew", aliases: ["israeli", "israel", "hebrew", "עברית", "ישראלי", "מזרחית"], children: [
        { key: "mizrahi", label: this._discoveryGenreLabel("Mizrahi", "מזרחית", "Mizrahi"), query: "מוזיקה מזרחית" },
        { key: "hebrew-hits", label: this._discoveryGenreLabel("Hebrew Hits", "להיטים בעברית", "Hebraeische Hits"), query: "להיטים בעברית" },
        { key: "israeli-rock", label: this._discoveryGenreLabel("Israeli Rock", "רוק ישראלי", "Israelischer Rock"), query: "רוק ישראלי" }
      ] },
      { key: "chill", label: this._discoveryGenreLabel("Chill", "צ׳יל", "Chill"), query: "chill", aliases: ["chill", "chillout", "chill out", "lofi", "lo-fi", "lounge", "relax", "relaxing"], children: [
        { key: "lofi", label: this._discoveryGenreLabel("Lo-Fi", "לו־פיי", "Lo-Fi"), query: "lo-fi chill" },
        { key: "lounge", label: this._discoveryGenreLabel("Lounge", "לאונג׳", "Lounge"), query: "lounge chill" },
        { key: "acoustic", label: this._discoveryGenreLabel("Acoustic", "אקוסטי", "Akustisch"), query: "acoustic chill" },
        { key: "sleep", label: this._discoveryGenreLabel("Sleep", "שינה", "Schlaf"), query: "sleep music" }
      ] },
      { key: "classical", label: this._discoveryGenreLabel("Classical", "קלאסי", "Klassik"), query: "classical", aliases: ["classical", "classical music", "orchestra", "orchestral", "piano", "opera"], children: [
        { key: "piano", label: this._discoveryGenreLabel("Piano", "פסנתר", "Klavier"), query: "classical piano" },
        { key: "orchestra", label: this._discoveryGenreLabel("Orchestra", "תזמורת", "Orchester"), query: "orchestra classical" },
        { key: "opera", label: this._discoveryGenreLabel("Opera", "אופרה", "Oper"), query: "opera" }
      ] },
      { key: "latin", label: this._discoveryGenreLabel("Latin", "לטיני", "Latin"), query: "latin", aliases: ["latin", "reggaeton", "salsa", "bachata", "bossa nova", "latino"], children: [
        { key: "reggaeton", label: this._discoveryGenreLabel("Reggaeton", "רגאטון", "Reggaeton"), query: "reggaeton" },
        { key: "salsa", label: this._discoveryGenreLabel("Salsa", "סלסה", "Salsa"), query: "salsa" },
        { key: "bachata", label: this._discoveryGenreLabel("Bachata", "בצ׳אטה", "Bachata"), query: "bachata" },
        { key: "latin-pop", label: this._discoveryGenreLabel("Latin Pop", "פופ לטיני", "Latin Pop"), query: "latin pop" }
      ] },
      { key: "afro", label: this._discoveryGenreLabel("Afro", "אפרו", "Afro"), query: "afro", aliases: ["afro", "afrobeats", "afrobeat", "afropop", "afropiano", "amapiano", "afro house", "afro tech", "afro soul", "afro r&b", "afroswing"], children: [
        { key: "afrobeats", label: this._discoveryGenreLabel("Afrobeats", "אפרוביטס", "Afrobeats"), query: "afrobeats" },
        { key: "amapiano", label: this._discoveryGenreLabel("Amapiano", "אמאפיאנו", "Amapiano"), query: "amapiano" },
        { key: "afro-house", label: this._discoveryGenreLabel("Afro House", "אפרו האוס", "Afro House"), query: "afro house" },
        { key: "afropop", label: this._discoveryGenreLabel("Afropop", "אפרו פופ", "Afropop"), query: "afropop" }
      ] },
      { key: "indie", label: this._discoveryGenreLabel("Indie", "אינדי", "Indie"), query: "indie", aliases: ["indie", "indie pop", "indie rock"], children: [
        { key: "indie-pop", label: this._discoveryGenreLabel("Indie Pop", "אינדי פופ", "Indie-Pop"), query: "indie pop" },
        { key: "indie-rock", label: this._discoveryGenreLabel("Indie Rock", "אינדי רוק", "Indie-Rock"), query: "indie rock" },
        { key: "alternative", label: this._discoveryGenreLabel("Alternative", "אלטרנטיבי", "Alternative"), query: "alternative music" }
      ] },
      { key: "metal", label: this._discoveryGenreLabel("Metal", "מטאל", "Metal"), query: "metal", aliases: ["metal", "heavy metal", "death metal", "black metal"], children: [
        { key: "heavy", label: this._discoveryGenreLabel("Heavy Metal", "הבי מטאל", "Heavy Metal"), query: "heavy metal" },
        { key: "alternative", label: this._discoveryGenreLabel("Alternative Metal", "מטאל אלטרנטיבי", "Alternative Metal"), query: "alternative metal" }
      ] },
      { key: "country", label: this._discoveryGenreLabel("Country", "קאנטרי", "Country"), query: "country", aliases: ["country", "americana", "bluegrass"] },
      { key: "reggae", label: this._discoveryGenreLabel("Reggae", "רגאיי", "Reggae"), query: "reggae", aliases: ["reggae", "dancehall", "dub"] },
      { key: "folk", label: this._discoveryGenreLabel("Folk", "פולק", "Folk"), query: "folk", aliases: ["folk", "singer songwriter", "singer-songwriter", "acoustic"] },
      { key: "ambient", label: this._discoveryGenreLabel("Ambient", "אמביינט", "Ambient"), query: "ambient", aliases: ["ambient", "new age", "soundscape"] },
      { key: "kids", label: this._i18n("ui.kids"), query: "kids", aliases: ["kids", "children", "childrens", "family", "ילדים"] }
    ];
  }
  _discoveryGenreMatchText(value = "") {
    return ` ${this._discoveryGenreKey(value).replace(/-/g, " ")} `;
  }
  _discoveryGenreProfileScore(genre = {}, profile = {}) {
    const haystack = this._discoveryGenreMatchText(genre.label || genre.query || genre.key || "");
    const compactHaystack = haystack.replace(/\s+/g, "");
    return [profile.key, profile.query, profile.label, ...profile.aliases || []].reduce((score, alias) => {
      const needle = this._discoveryGenreMatchText(alias).trim();
      if (!needle) return score;
      if (haystack.trim() === needle) return Math.max(score, 8);
      if (haystack.includes(` ${needle} `)) return Math.max(score, 5);
      if (compactHaystack.includes(needle.replace(/\s+/g, ""))) return Math.max(score, 2);
      return score;
    }, 0);
  }
  _discoveryGenreOptionsFlat(genres = []) {
    return (Array.isArray(genres) ? genres : []).flatMap((genre) => [
      genre,
      ...Array.isArray(genre?.children) ? genre.children : []
    ]);
  }
  _discoveryResponseItems(raw) {
    const candidates = [];
    const addCandidate = (value) => {
      if (!value) return;
      candidates.push(value);
      if (value && typeof value === "object" && !Array.isArray(value)) {
        candidates.push(value.response, value.result, value.items, value.genres);
      }
    };
    addCandidate(raw);
    addCandidate(raw?.response);
    addCandidate(raw?.result);
    for (const candidate of candidates) {
      if (Array.isArray(candidate)) return candidate;
      if (Array.isArray(candidate?.items)) return candidate.items;
      if (Array.isArray(candidate?.genres)) return candidate.genres;
    }
    return [];
  }
  _normalizeDiscoveryGenreOptions(genreItems = [], limit = 20) {
    const rawGenres = (Array.isArray(genreItems) ? genreItems : []).map((item, index) => this._discoveryGenreOptionFromItem(item, index)).filter(Boolean);
    const profiles = this._discoveryPopularGenreProfiles();
    return profiles.map((profile, index) => {
      const score = rawGenres.reduce((best, genre) => Math.max(best, this._discoveryGenreProfileScore(genre, profile)), 0);
      const children = (Array.isArray(profile.children) ? profile.children : []).map((child) => ({
        ...child,
        key: `${profile.key}:${child.key}`,
        parent_key: profile.key,
        parent_label: profile.label
      }));
      return {
        key: profile.key,
        label: profile.label,
        query: profile.query,
        children,
        score,
        index
      };
    }).filter((genre) => genre.score > 0 || genre.index < limit).sort((a, b) => a.index - b.index).slice(0, limit);
  }
  async _loadDiscoveryGenresFromMusicAssistant() {
    if (!this._hasDirectMAConnection()) return this._normalizeDiscoveryGenreOptions([]);
    const requests = [
      { limit: 120, order_by: "sort_name", hide_empty: true },
      { limit: 120, order_by: "sort_name", media_type: "playlist" },
      { limit: 120, order_by: "sort_name", media_type: "album" },
      { limit: 120, order_by: "sort_name", media_type: "track" }
    ];
    const results = await Promise.allSettled(
      requests.map((args) => this._callDirectMaCommand("music/genres/library_items", args))
    );
    const genreItems = results.flatMap((result) => result.status === "fulfilled" ? this._discoveryResponseItems(result.value) : []);
    return this._normalizeDiscoveryGenreOptions(genreItems);
  }
  _discoveryGenre(genres = []) {
    const options = Array.isArray(genres) && genres.length ? genres : this._discoveryGenreFallbackOptions();
    const flatOptions = this._discoveryGenreOptionsFlat(options);
    return flatOptions.find((entry) => entry.key === this._state.discoveryGenreKey) || options[0];
  }
  async _loadDiscoveryGenres() {
    const fallback = this._discoveryGenreFallbackOptions();
    const cacheKey = "discovery:genres:music-assistant";
    const ttl = Number(this._config.cache_ttl || 3e5);
    const cached = this._cache.library.get(cacheKey);
    if (cached && Date.now() - cached.ts < ttl) return cached.items;
    let genres = [];
    try {
      genres = await this._loadDiscoveryGenresFromMusicAssistant();
    } catch {
      genres = this._normalizeDiscoveryGenreOptions([]);
    }
    const items = genres.length ? [fallback[0], ...genres] : fallback;
    this._cache.library.set(cacheKey, { ts: Date.now(), items });
    return items;
  }
  _discoveryQueryWithGenre(query = "", genre = null) {
    const baseQuery = String(query || "").trim();
    const genreQuery = String(genre?.key === "all" ? "" : genre?.query || genre?.label || "").trim();
    if (!genreQuery) return baseQuery;
    if (!baseQuery) return `${genreQuery} playlist`;
    if (baseQuery.toLowerCase().includes(genreQuery.toLowerCase())) return baseQuery;
    return `${genreQuery} ${baseQuery}`;
  }
  _discoveryUniqueItems(items = [], fallbackType = "playlist") {
    const seen = /* @__PURE__ */ new Set();
    const unique = [];
    (Array.isArray(items) ? items : []).forEach((item) => {
      const normalized = this._normalizeMediaItem({ ...item, media_type: item?.media_type || fallbackType });
      const uri = String(normalized?.uri || "").trim();
      const name = String(normalized?.name || normalized?.title || "").trim().toLowerCase();
      const provider = String(normalized?.provider || normalized?.provider_domain || normalized?.provider_instance || "").trim().toLowerCase();
      const key = uri || `${provider}:${name}`;
      if (!key || seen.has(key)) return;
      seen.add(key);
      unique.push(normalized);
    });
    return unique;
  }
  async _searchDiscoveryProviderResults(query = "") {
    const q = String(query || "").trim();
    if (!q) return this._emptySearchResults();
    let results = this._emptySearchResults();
    try {
      results = this._normalizeSearchResponse(await this._callService("search", { query: q, limit: 30 }));
    } catch {
      try {
        results = this._normalizeSearchResponse(await this._callService("search", { name: q, limit: 30, media_type: ["playlist", "album", "track", "radio", "genre"] }));
      } catch {
      }
    }
    return results;
  }
  async _loadDiscoveryCategoryPlaylists(category = this._discoveryCategory(), genre = this._discoveryGenre()) {
    const queries = (Array.isArray(category?.queries) ? category.queries : [category?.query]).map((query) => String(query || "").trim()).filter(Boolean);
    const scopedQueries = (queries.length ? queries : ["recommended playlist"]).map((query) => this._discoveryQueryWithGenre(query, genre)).filter(Boolean);
    const genreScoped = String(genre?.key || "all") !== "all";
    const searchResults = genreScoped ? await Promise.allSettled(queries.map(
      (query) => category?.fresh ? this._searchDiscoveryProviderResults(query) : this._search(query)
    )) : [];
    const scopedSearchResults = await Promise.allSettled(scopedQueries.map(
      (query) => category?.fresh ? this._searchDiscoveryProviderResults(query) : this._search(query)
    ));
    const providerPlaylists = searchResults.flatMap(
      (result) => result.status === "fulfilled" && Array.isArray(result.value?.playlists) ? result.value.playlists : []
    );
    const scopedProviderPlaylists = scopedSearchResults.flatMap(
      (result) => result.status === "fulfilled" && Array.isArray(result.value?.playlists) ? result.value.playlists : []
    );
    const libraryResults = category?.fresh ? [] : await Promise.allSettled(scopedQueries.slice(0, 2).map((query) => this._fetchLibrary("playlist", "sort_name", 14, false, query)));
    const libraryPlaylists = libraryResults.flatMap(
      (result) => result.status === "fulfilled" && Array.isArray(result.value) ? result.value : []
    );
    return this._discoveryUniqueItems([...scopedProviderPlaylists, ...providerPlaylists, ...libraryPlaylists], "playlist").slice(0, 30);
  }
  async _loadDiscoverySections() {
    const categories = this._discoveryCategoryOptions();
    const activeCategory = this._discoveryCategory();
    const genres = this._discoveryGenreFallbackOptions();
    const activeGenre = genres[0];
    const genreSearch = "";
    if (this._state.discoveryGenreKey !== "all") this._state.discoveryGenreKey = "all";
    const results = await Promise.allSettled([
      this._loadDiscoveryCategoryPlaylists(activeCategory, activeGenre),
      this._fetchRecentlyPlayed(14),
      this._fetchLibrary("album", "random", 18, false, genreSearch),
      this._fetchLibrary("radio", "sort_name", 14, false, genreSearch)
    ]);
    const [categoryPlaylists, recent, albums, radios] = results.map(
      (result) => result.status === "fulfilled" && Array.isArray(result.value) ? result.value.map((item) => this._normalizeMediaItem(item)) : []
    );
    const seed = this._state.discoverySessionSeed || Date.now();
    return {
      categories,
      genres,
      activeCategory,
      activeGenre,
      categoryPlaylists: this._shuffleDiscoveryItems(categoryPlaylists, seed + 11),
      recent: this._shuffleDiscoveryItems(recent, seed + 23),
      albums: this._shuffleDiscoveryItems(albums, seed + 37),
      radios: this._shuffleDiscoveryItems(radios, seed + 41)
    };
  }
  _discoveryEndlessItems({ categoryPlaylists = [], recent = [], albums = [], radios = [] } = {}) {
    const sources = [
      { items: categoryPlaylists, type: "playlist", label: this._i18n("ui.provider_playlists") },
      { items: albums, type: "album", label: this._i18n("ui.discover_albums") },
      { items: recent, type: "track", label: this._i18n("ui.recently_played_2") },
      { items: radios, type: "radio", label: this._i18n("ui.favorite_radio") }
    ];
    const seen = /* @__PURE__ */ new Set();
    const endless = [];
    sources.forEach((source) => {
      (Array.isArray(source.items) ? source.items : []).forEach((item) => {
        const normalized = this._normalizeMediaItem({ ...item, media_type: item?.media_type || source.type });
        const uri = String(normalized?.uri || "").trim();
        const name = String(normalized?.name || normalized?.title || "").trim().toLowerCase();
        const key = uri || `${source.type}:${name}`;
        if (!key || seen.has(key)) return;
        seen.add(key);
        endless.push({ ...normalized, media_type: normalized?.media_type || source.type, _homeiiDiscoveryLabel: source.label });
      });
    });
    return this._shuffleDiscoveryItems(endless, (this._state.discoverySessionSeed || Date.now()) + 53).slice(0, 58);
  }
  _discoveryOrbStyle(item = {}, index = 0) {
    const text = `${item?.uri || ""}|${item?.name || item?.title || ""}|${index}`;
    let hash = 0;
    for (let i = 0; i < text.length; i += 1) {
      hash = (hash << 5) - hash + text.charCodeAt(i) | 0;
    }
    const seed = Math.abs(hash || index + 1);
    const delays = [0, -1.2, -2.4, -3.6, -4.8, -6];
    return `--orb-size:156px;--orb-shift:0px;--orb-lift:0px;--orb-delay:${delays[seed % delays.length]}s;`;
  }
  _syncDiscoveryOrbSelection(activeUri = "") {
    const uri = String(activeUri || "");
    Array.from(this.shadowRoot?.querySelectorAll?.(".discovery-orb-field") || []).forEach((field) => {
      field.classList.toggle("has-active", !!uri);
    });
    Array.from(this.shadowRoot?.querySelectorAll?.(".discovery-orb[data-media-uri]") || []).forEach((button) => {
      const active = !!uri && button.dataset.mediaUri === uri;
      button.classList.toggle("is-active", active);
      button.setAttribute("aria-pressed", active ? "true" : "false");
    });
  }
  _discoveryOrbCloudHtml(items = [], activeCategory = null, activeGenre = null) {
    const playable = (Array.isArray(items) ? items : []).filter((item) => String(item?.uri || "").trim()).slice(0, 58);
    if (!playable.length) return "";
    const genreLabel = activeGenre?.key && activeGenre.key !== "all" ? activeGenre.label : this._i18n("ui.all_genres");
    const activeUri = String(this._state.discoveryExpandedUri || "");
    const hasActive = !!activeUri && playable.some((item) => String(item?.uri || "") === activeUri);
    return `
      <section class="discovery-endless">
        <div class="discovery-endless-head">
          <div>
            <div class="discovery-endless-title">${this._esc(activeCategory?.label || this._i18n("ui.recommended"))}</div>
            <div class="discovery-endless-sub">${this._esc(`${this._i18n("ui.genre")}: ${genreLabel}`)}</div>
          </div>
        </div>
        <div class="discovery-orb-field ${hasActive ? "has-active" : ""}">
          ${playable.map((item, index) => {
      const mediaType = item?.media_type || "album";
      const titleText = item?.name || item?.title || this._i18n("ui.discover");
      const subText = this._artistName(item) || item?.artist || item?.album?.name || item?._homeiiDiscoveryLabel || this._emptyQuickSuggestionLabel(mediaType);
      const art = this._artUrl(item) || item?.image || item?.image_url || item?.album?.image || "";
      const active = String(this._state.discoveryExpandedUri || "") === String(item.uri || "");
      return `
              <button class="discovery-orb orb-${index % 7} ${active ? "is-active" : ""}" style="${this._esc(this._discoveryOrbStyle(item, index))}" data-media-uri="${this._esc(item.uri)}" data-media-type="${this._esc(mediaType)}" aria-pressed="${active ? "true" : "false"}" title="${this._esc(titleText)}">
                <span class="discovery-orb-art">
                  ${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg(mediaType === "radio" ? "radio" : "album")}
                  <span class="discovery-orb-play" aria-hidden="true">${this._iconSvg("play")}</span>
                </span>
                <span class="discovery-orb-copy">
                  <span class="discovery-orb-title">${this._esc(titleText)}</span>
                  <span class="discovery-orb-sub">${this._esc(subText)}</span>
                </span>
              </button>
            `;
    }).join("")}
        </div>
      </section>
    `;
  }
  _discoveryPlayerFocusHtml() {
    const player = this._getSelectedPlayer();
    const playerName = player?.attributes?.friendly_name || this._i18n("ui.choose_player");
    const isPlaying = player?.state === "playing";
    const groupCount = this._playerGroupCount(player);
    const art = this._playerArtworkUrl(player, 120);
    const stateLabel = isPlaying ? player?.attributes?.media_title || this._i18n("ui.playing") : this._i18n("ui.ready");
    return `
      <button class="discovery-player-focus ${isPlaying ? "is-playing" : ""}" data-menu-nav="players" title="${this._esc(this._i18n("ui.choose_player"))}">
        <span class="discovery-player-kicker">${this._esc(this._i18n("ui.selected_player"))}</span>
        <span class="discovery-player-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("speaker")}</span>
        <span class="discovery-player-copy">
          <span class="discovery-player-name">${this._esc(playerName)}</span>
          <span class="discovery-player-state">${this._esc(stateLabel)}</span>
        </span>
        ${groupCount ? `<span class="player-group-badge discovery-player-badge">${this._esc(groupCount)}</span>` : ``}
      </button>
    `;
  }
  _discoveryMenuHtml({ categories = [], genres = [], activeCategory = null, activeGenre = null, categoryPlaylists = [], recent = [], albums = [], radios = [] } = {}) {
    const endlessItems = this._discoveryEndlessItems({ categoryPlaylists, recent, albums, radios });
    const cloud = this._discoveryOrbCloudHtml(endlessItems, activeCategory, activeGenre);
    const selectedTitle = activeCategory?.label || this._i18n("ui.discover");
    const selectedSub = activeCategory?.subtitle || this._i18n("ui.discover_subtitle", {}, this._discoveryGenreLabel("Recommended playlists, albums and radio.", "המלצות, אלבומים ורדיו לפי הסגנון שבחרת.", "Empfehlungen, Alben und Radio nach deinem Stil."));
    return `
      <div class="discovery-shell">
        <div class="discovery-hero">
          <div class="discovery-hero-copy">
            <div class="discovery-hero-title">${this._esc(selectedTitle)}</div>
            <div class="discovery-hero-sub">${this._esc(selectedSub)}</div>
          </div>
          <div class="discovery-filter-panel">
            ${this._discoveryPlayerFocusHtml()}
            <label class="discovery-category-select">
              <span>${this._esc(this._m("Style", "סגנון"))}</span>
              <select class="media-sort-select settings-select" id="discoveryCategorySelect" aria-label="${this._esc(this._m("Style", "סגנון"))}">
                ${(Array.isArray(categories) ? categories : []).map((category) => `<option value="${this._esc(category.key)}" ${category.key === activeCategory?.key ? "selected" : ""}>${this._esc(category.label)}</option>`).join("")}
              </select>
            </label>
          </div>
        </div>
        ${cloud || `<div class="notice open">${this._esc(this._i18n("ui.no_content_found"))}</div>`}
      </div>
    `;
  }
  async _selectDiscoveryCategory(categoryKey = "") {
    const categories = this._discoveryCategoryOptions();
    const next = categories.find((entry) => entry.key === categoryKey) || categories[0];
    if (!next) return;
    this._state.discoveryCategoryKey = next.key;
    this._state.discoverySessionSeed = Date.now() + Math.floor(Math.random() * 1e5);
    this._state.discoveryExpandedUri = "";
    try {
      localStorage.setItem("homeii_music_flow_discovery_category_key", this._state.discoveryCategoryKey || "pop");
    } catch {
    }
    await this._renderMobileMenu();
  }
  async _selectDiscoveryGenre(genreKey = "") {
    this._state.discoveryGenreKey = String(genreKey || "all").trim() || "all";
    this._state.discoverySessionSeed = Date.now() + Math.floor(Math.random() * 1e5);
    this._state.discoveryExpandedUri = "";
    try {
      localStorage.setItem("homeii_music_flow_discovery_genre_key", this._state.discoveryGenreKey || "all");
    } catch {
    }
    await this._renderMobileMenu();
  }
  _playerRowHtml(p, attrs = "", active = false, options = {}) {
    const art = this._playerArtworkUrl(p, 180);
    const activePlayback = p?.state === "playing";
    const showControls = !!options.controls;
    const showFrontPin = options.pin !== false;
    const frontPinned = this._frontPinnedPlayerEntity() === p?.entity_id;
    const vol = Math.round((p.attributes?.volume_level || 0) * 100);
    const friendlyName = p.attributes?.friendly_name || p.entity_id;
    const track = p.attributes?.media_title || p.attributes?.media_artist || "";
    const activityIcon = `<span class="player-premium-bars eq-icon ${activePlayback ? "is-active" : "is-static"}" aria-label="${this._esc(activePlayback ? this._i18n("ui.playing") : this._i18n("ui.idle"))}"><span></span><span></span><span></span></span>`;
    const pinHtml = showFrontPin ? `<button type="button" class="player-premium-side player-front-pin ${frontPinned ? "active" : ""}" data-front-pin-player="${this._esc(p.entity_id)}" title="${this._esc(frontPinned ? this._m("Clear front pin", "בטל נעץ חזית") : this._m("Pin player to front", "נעץ נגן לחזית"))}" aria-pressed="${frontPinned ? "true" : "false"}">${this._iconSvg("pin")}</button>` : ``;
    const body = `
      <button class="player-premium-head ${active ? "active" : ""} ${activePlayback ? "is-playing" : ""}" ${attrs}>
        <span class="player-premium-art">
          ${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("speaker")}
        </span>
        <span class="player-premium-copy">
          <span class="player-premium-title-row">
            <span class="player-premium-name">${this._esc(friendlyName)}</span>
            ${activityIcon}
          </span>
          ${track ? `<span class="player-premium-track">${this._esc(track)}</span>` : ``}
        </span>
      </button>
    `;
    if (!showControls) {
      return `
        <div class="player-menu-card ${active ? "active" : ""}">
          ${body}
          ${pinHtml}
        </div>
      `;
    }
    return `
      <div class="player-menu-card ${active ? "active" : ""}">
        ${body}
        ${pinHtml}
        <div class="player-volume-row">
          <button class="player-mini-mute ${this._isMuted(p) ? "active" : ""}" data-player-mute="${this._esc(p.entity_id)}" title="${this._esc(this._i18n("ui.mute"))}">${this._iconSvg(this._volumeIconName(p))}</button>
          <input class="player-mini-volume" data-player-volume="${this._esc(p.entity_id)}" type="range" min="0" max="100" value="${vol}" style="--vol-pct:${vol}%">
          <span class="player-mini-value">${vol}%</span>
        </div>
      </div>
    `;
  }
  _playersMenuHtml(options = {}) {
    this._loadPlayers();
    const hotelMode = this._isHotelMode();
    const selected = this._state.selectedPlayer;
    const players = this._state.players || [];
    const pinnedEntities = new Set(this._resolvedPinnedPlayerEntities(players));
    const browserPlayers = this._getBrowserPlayers(players);
    const rememberedThisDevice = this._getThisDevicePlayer(players);
    const connectingThisDevice = !!this._localSendspinConnecting;
    const waitingThisDevice = !!this._state.awaitingThisDevicePlayer;
    const desiredThisDevice = this._isLocalSendspinDesired();
    const localStatus = String(this._state.localSendspinStatus || "").toLowerCase();
    const disconnectingThisDevice = !!this._state.localSendspinDisconnecting || localStatus === "disconnecting";
    const connectedThisDevice = !!rememberedThisDevice || this._localSendspinConnected || localStatus === "connected";
    const startingThisDevice = !connectedThisDevice && !disconnectingThisDevice && (connectingThisDevice || waitingThisDevice || desiredThisDevice || ["connecting", "reconnecting", "suspended"].includes(localStatus));
    const disconnectedThisDevice = !connectedThisDevice && !startingThisDevice && localStatus === "disconnected";
    const thisDeviceAction = disconnectingThisDevice ? this._i18n("ui.disconnecting") : connectedThisDevice ? this._i18n("ui.connected") : startingThisDevice ? this._i18n("ui.connecting") : disconnectedThisDevice ? this._i18n("ui.disconnected") : this._i18n("ui.enable");
    const thisDeviceActionName = connectedThisDevice || startingThisDevice || desiredThisDevice || connectingThisDevice || waitingThisDevice ? "disconnect_this_device" : "connect_this_device";
    const thisDeviceLabel = this._i18n("ui.player_on_this_device");
    const thisDeviceTitle = `${thisDeviceLabel} · ${thisDeviceAction}`;
    const thisDeviceHtml = rememberedThisDevice ? `
        <div class="players-premium-grid">
          ${this._playerRowHtml(rememberedThisDevice, `data-menu-player="${this._esc(rememberedThisDevice.entity_id)}"`, rememberedThisDevice.entity_id === selected, { controls: !hotelMode, pin: true })}
        </div>
      ` : ``;
    const frontPinnedEntityId = this._frontPinnedPlayerEntity(players);
    const visiblePlayers = players.filter((p) => !this._isLikelyBrowserPlayer(p) && (!pinnedEntities.size || pinnedEntities.has(p.entity_id) || p.entity_id === frontPinnedEntityId || p.entity_id === selected || p.state === "playing" || this._isPlayerActive(p)));
    const filteredPlayers = options.activeOnly ? visiblePlayers.filter((p) => p.state === "playing") : visiblePlayers;
    if (options.activeOnly && !filteredPlayers.length) return `<div class="notice open">${this._i18n("ui.no_active_players")}</div>`;
    const manualFrontEntityId = this._manualFrontPlayerEntity(players);
    const rankPlayer = (player) => {
      if (player?.entity_id === frontPinnedEntityId) return 0;
      if (player?.entity_id === manualFrontEntityId || player?.entity_id === selected) return 1;
      if (player?.state === "playing") return 2;
      if (this._isPlayerActive(player)) return 3;
      return 4;
    };
    const orderedPlayers = filteredPlayers.map((player, index) => ({ player, index })).sort((left, right) => {
      const rankDiff = rankPlayer(left.player) - rankPlayer(right.player);
      return rankDiff || left.index - right.index;
    }).map((entry) => entry.player);
    const otherPlayersHtml = orderedPlayers.map((p) => this._playerRowHtml(p, `data-menu-player="${this._esc(p.entity_id)}"`, p.entity_id === selected, { controls: !hotelMode, pin: true })).join("");
    const browserPlayersHtml = browserPlayers.filter((player) => !pinnedEntities.size || pinnedEntities.has(player.entity_id)).filter((player) => player.entity_id !== rememberedThisDevice?.entity_id).map((p) => this._playerRowHtml(p, `data-menu-player="${this._esc(p.entity_id)}"`, p.entity_id === selected, { controls: !hotelMode, pin: true })).join("");
    return `
      ${options.activeOnly || hotelMode ? "" : this._playersActionHubHtml({
      thisDeviceActionName,
      thisDeviceTitle,
      connectedThisDevice,
      startingThisDevice,
      disconnectingThisDevice
    })}
      ${options.activeOnly || hotelMode ? "" : thisDeviceHtml}
      ${!players.length && !options.activeOnly ? `<div class="notice open">${this._i18n("ui.no_players_found_yet")}</div>` : ""}
      ${otherPlayersHtml ? `<div class="players-premium-grid">${otherPlayersHtml}</div>` : ""}
      ${browserPlayersHtml && !hotelMode ? `<div class="media-section-title">${this._esc(this._i18n("ui.browser_players"))}</div><div class="players-premium-grid">${browserPlayersHtml}</div>` : ""}
    `;
  }
  _transferMenuHtml() {
    if (this._hasPinnedPlayer()) {
      return `<div class="notice open">${this._i18n("ui.transfer_is_unavailable_while_a_pinned_player_is_configured")}</div>`;
    }
    const current = this._getSelectedPlayer();
    const others = (this._state.players || []).filter((p) => p.entity_id !== current?.entity_id);
    if (!others.length) return `<div class="notice open">${this._i18n("ui.no_target_players_available_2")}</div>`;
    return `<div class="players-premium-grid">${others.map((p) => this._playerRowHtml(p, `data-menu-transfer="${this._esc(p.entity_id)}"`, false, { pin: false })).join("")}</div>`;
  }
  _settingsPill(label, value, current, attr = "data-setting-value") {
    return `<button class="settings-pill ${value === current ? "active" : ""}" ${attr}="${this._esc(value)}">${this._esc(label)}</button>`;
  }
  _settingsMenuHtml() {
    this._loadPlayers();
    const theme = this._state.cardTheme === "light" || this._state.cardTheme === "custom" ? this._state.cardTheme : "dark";
    const performanceProfile2 = this._performanceProfile();
    const dynamicThemeMode = this._mobileDynamicThemeMode();
    const backgroundMotionMode = this._mobileBackgroundMotionMode();
    const fontScale = Number(this._state.mobileFontScale || 1).toFixed(2);
    const iconScale = this._mobileIconScale().toFixed(2);
    const mobileLayoutMode = this._mobileLayoutMode();
    const compactMode = !!this._state.mobileCompactMode;
    const compactWidgetMode = this._mobileCompactWidgetMode();
    const showUpNext = this._mobileShowUpNextEnabled();
    const footerMode = this._mobileFooterMode();
    const volumeMode = this._mobileVolumeMode();
    const volumeStepButtonsEnabled = this._mobileVolumeStepButtonsEnabled();
    const volumeStepPercent = this._mobileVolumeStepPercent();
    const micMode = this._mobileMicMode();
    const voiceAssistantEnabled = this._voiceAssistantEnabled();
    const voiceAssistantMode = this._voiceAssistantMode();
    const voiceAssistantSpeakFeedback = this._voiceAssistantSpeakFeedbackEnabled();
    const voiceAssistantAgentOptions = this._voiceAssistantAgentOptions();
    const homeShortcut = this._mobileHomeShortcutEnabled();
    const studioShortcut = this._mobileStudioShortcutEnabled();
    const likedMode = this._useMaLikedMode() ? "ma" : "local";
    const mainBarOptions = [
      ["search", this._i18n("ui.search")],
      ["library", this._i18n("ui.library")],
      ["players", this._i18n("ui.players")],
      ["actions", this._i18n("ui.actions_2")],
      ["settings", this._i18n("ui.settings")],
      ["theme", this._i18n("ui.theme_toggle")]
    ];
    const tabOptions = [
      ["library_playlists", this._i18n("ui.playlists")],
      ["library_artists", this._i18n("ui.artists")],
      ["library_albums", this._i18n("ui.albums")],
      ["library_tracks", this._i18n("ui.tracks")],
      ["library_radio", this._i18n("ui.radio")],
      ["library_podcasts", this._i18n("ui.podcasts")],
      ["library_liked", this._i18n("ui.liked")],
      ["library_search", this._i18n("ui.search")]
    ];
    const selectedTabs = new Set(this._mobileLibraryTabs());
    const libraryDefaultLayout = this._defaultMobileMediaLayout();
    const selectedMainBar = new Set(this._mobileMainBarItems());
    const quickActionOptions = this._mobileQuickActionOptions();
    const quickActions = this._mobileQuickActions();
    const selectedQuickActions = new Set(quickActions);
    const radioCountry = this._mobileRadioBrowserCountry();
    const radioCountryOptions = this._radioBrowserCountryOptions();
    const pinnedPlayers = new Set(this._pinnedPlayerPreferences());
    const excludedPlayers = new Set(this._excludedPlayerPreferences());
    const playerSortMode = this._playerSortMode();
    const playerOrder = this._playerOrderPreferences();
    const playerOptions = this._pinnedPlayerOptionPlayers([], { includeExcluded: true }).map((player) => [player.entity_id, player.attributes?.friendly_name || player.entity_id]);
    const visibleMainBarOptions = mainBarOptions;
    const showStudioMainBarOption = this._controlRoomEnabled();
    const announcementLanguage = this._announcementLanguageSetting();
    const announcementLanguageOptions = this._announcementLanguageOptions();
    const settingsMainBarLocked = !this._usesVisualSettings();
    const ambientEntitiesText = this._ambientLightEntities().join(", ");
    const ambientPlayerMapText = this._ambientLightPlayerMap().join("\n");
    const screensaverClockMode = this._screensaverClockMode();
    const screensaverControlButtons = this._screensaverControlButtons({ includeDisabled: true });
    const selectedScreensaverControlButtons = new Set(screensaverControlButtons);
    const screensaverControlOptions = this._screensaverControlButtonOptions();
    const powerButtonAction = this._powerButtonAction();
    const auxiliaryButtonConfigs = this._auxiliaryButtonConfigs();
    const auxiliaryIconOptions = [
      ["power", this._i18n("ui.power")],
      ["home", this._i18n("ui.home")],
      ["speaker", this._i18n("ui.players")],
      ["music_note", this._i18n("ui.music")],
      ["wand", this._i18n("ui.surprise_me")],
      ["grid", this._i18n("ui.actions_2")],
      ["settings", this._i18n("ui.settings")],
      ["heart_outline", this._i18n("ui.like_2")],
      ["play", this._i18n("ui.play")],
      ["stop", this._i18n("ui.stop_all")],
      ["radio", this._i18n("ui.radio")],
      ["timer", this._i18n("ui.timer")],
      ["info", this._i18n("ui.info")]
    ];
    auxiliaryButtonConfigs.forEach((button) => {
      const icon = String(button?.icon || "").trim();
      if (icon && !auxiliaryIconOptions.some(([value]) => value === icon)) auxiliaryIconOptions.unshift([icon, icon]);
    });
    return `
      <div class="settings-shell">
        <div class="settings-group">
          <div class="settings-label">${this._i18n("ui.language")}</div>
          ${this._settingsLanguageSelectHtml()}
        </div>
        <div class="settings-group">
          <div class="settings-label">${this._i18n("ui.theme_2")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.dark"), "dark", theme, "data-setting-theme")}
            ${this._settingsPill(this._i18n("ui.light"), "light", theme, "data-setting-theme")}
            ${this._settingsPill(this._i18n("ui.custom"), "custom", theme, "data-setting-theme")}
          </div>
          <div class="settings-label">${this._i18n("ui.performance_profile")}</div>
          <div class="settings-pills performance-profile-pills">
            ${this._settingsPill("Full", "full", performanceProfile2, "data-setting-performance-profile")}
            ${this._settingsPill("High", "high", performanceProfile2, "data-setting-performance-profile")}
            ${this._settingsPill("Low", "low", performanceProfile2, "data-setting-performance-profile")}
            ${this._settingsPill("Ultra Lite", "ultra_lite", performanceProfile2, "data-setting-performance-profile")}
          </div>
          <div class="settings-hint">${this._i18n("ui.performance_profile_helper")}</div>
          <div class="settings-label">${this._i18n("ui.dynamic_theme")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.off"), "off", dynamicThemeMode, "data-setting-dynamic-theme")}
            ${this._settingsPill("Auto", "auto", dynamicThemeMode, "data-setting-dynamic-theme")}
            ${this._settingsPill(this._i18n("ui.strong"), "strong", dynamicThemeMode, "data-setting-dynamic-theme")}
          </div>
          <div class="settings-hint">${this._i18n("ui.auto_extracts_colors_from_the_current_artwork_and_keeps_the_effect_subtl")}</div>
          <div class="settings-label">${this._i18n("ui.background_motion")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.off"), "off", backgroundMotionMode, "data-setting-background-motion")}
            ${this._settingsPill(this._i18n("ui.subtle"), "subtle", backgroundMotionMode, "data-setting-background-motion")}
            ${this._settingsPill(this._i18n("ui.strong"), "strong", backgroundMotionMode, "data-setting-background-motion")}
            ${this._settingsPill(this._i18n("ui.extreme"), "extreme", backgroundMotionMode, "data-setting-background-motion")}
          </div>
          <div class="settings-hint">${this._i18n("ui.adds_motion_to_the_background_layers_subtle_keeps_it_calm_strong_is_noti")}</div>
          <div class="settings-color-wrap">
            <div class="settings-label">${this._i18n("ui.accent_color")}</div>
            <div class="settings-color-row">
              <input class="settings-color-picker" id="mobileCustomColorPicker" type="color" value="${this._esc(this._state.mobileCustomColor || "#f5a623")}">
              <div class="settings-value">${this._esc(String(this._state.mobileCustomColor || "#f5a623").toUpperCase())}</div>
            </div>
          </div>
          <div class="settings-range">
            <div class="settings-label">${this._i18n("ui.font_size")}</div>
            <input id="mobileFontScaleRange" type="range" min="0.5" max="1.5" step="0.05" value="${this._esc(fontScale)}">
            <div class="settings-value">${this._esc(fontScale)}x</div>
          </div>
          <div class="settings-range">
            <div class="settings-label">${this._i18n("ui.icon_size")}</div>
            <input id="mobileIconScaleRange" type="range" min="0.8" max="1.25" step="0.05" value="${this._esc(iconScale)}">
            <div class="settings-value">${this._esc(iconScale)}x</div>
          </div>
          <div class="settings-label">${this._esc(this._i18n("ui.mobile_layout_mode", {}, "Phone layout mode"))}</div>
          <div class="settings-pills">
            ${this._settingsPill("Auto", "auto", mobileLayoutMode, "data-setting-mobile-layout-mode")}
            ${this._settingsPill(this._i18n("ui.full", {}, "Full"), "full", mobileLayoutMode, "data-setting-mobile-layout-mode")}
            ${this._settingsPill(this._i18n("ui.compact", {}, "Compact"), "compact", mobileLayoutMode, "data-setting-mobile-layout-mode")}
          </div>
          <div class="settings-hint">${this._esc(this._i18n("ui.mobile_layout_mode_helper", {}, "Auto uses the available card space. Full keeps the phone player inline and requests a tall Sections slot. Compact always uses the compact tile."))}</div>
          <div class="settings-label">${this._i18n("ui.compact_mode")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", compactMode ? "on" : "off", "data-setting-compact-mode")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", compactMode ? "on" : "off", "data-setting-compact-mode")}
          </div>
          <div class="settings-label">${this._i18n("ui.compact_widget_mode")}</div>
          <div class="settings-pills">
            ${this._settingsPill("Auto", "auto", compactWidgetMode, "data-setting-compact-widget-mode")}
            ${this._settingsPill(this._i18n("ui.full"), "full", compactWidgetMode, "data-setting-compact-widget-mode")}
            ${this._settingsPill(this._i18n("ui.mini_widget"), "mini", compactWidgetMode, "data-setting-compact-widget-mode")}
          </div>
          <div class="settings-hint">${this._i18n("ui.choose_when_compact_mode_uses_the_two_row_mobile_widget")}</div>
          <div class="settings-label">${this._i18n("ui.show_up_next")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", showUpNext ? "on" : "off", "data-setting-show-up-next")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", showUpNext ? "on" : "off", "data-setting-show-up-next")}
          </div>
          <div class="settings-label">${this._i18n("ui.artwork_swipe")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.change_song"), "play", this._mobileSwipeMode(), "data-setting-swipe-mode")}
            ${this._settingsPill(this._i18n("ui.browse_covers"), "browse", this._mobileSwipeMode(), "data-setting-swipe-mode")}
          </div>
          <div class="settings-label">${this._i18n("ui.microphone")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.on"), "on", micMode, "data-setting-mic-mode")}
            ${this._settingsPill(this._i18n("ui.off_2"), "off", micMode, "data-setting-mic-mode")}
            ${this._settingsPill(this._i18n("ui.smart"), "smart", micMode, "data-setting-mic-mode")}
          </div>
          <div class="settings-label">${this._i18n("ui.footer_style")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.icon_only"), "icon", footerMode, "data-setting-footer-mode")}
            ${this._settingsPill(this._i18n("ui.text_only"), "text", footerMode, "data-setting-footer-mode")}
            ${this._settingsPill(this._i18n("ui.icon_and_text"), "both", footerMode, "data-setting-footer-mode")}
          </div>
          <div class="settings-label">${this._i18n("ui.home_shortcut")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", homeShortcut ? "on" : "off", "data-setting-home-shortcut")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", homeShortcut ? "on" : "off", "data-setting-home-shortcut")}
          </div>
          <div class="settings-label">${this._i18n("ui.home_path")}</div>
          <input class="media-sort-select settings-select" id="mobileHomeShortcutPathInput" type="text" value="${this._esc(this._mobileHomeShortcutPath())}" placeholder="/lovelace/home" aria-label="${this._esc(this._i18n("ui.home_path"))}">
          <div class="settings-hint">${this._i18n("ui.use_any_home_assistant_path_example_lovelace_home")}</div>
          <div class="settings-label">${this._i18n("ui.pinned_players")}</div>
          <div class="settings-check-grid">
            ${playerOptions.length ? playerOptions.map(([value, label]) => `
              <label class="settings-check-pill">
                <input type="checkbox" data-setting-pinned-player="${this._esc(value)}" ${pinnedPlayers.has(value) ? "checked" : ""}>
                <span>${this._esc(label)}</span>
              </label>`).join("") : `<div class="settings-hint">${this._i18n("ui.no_music_assistant_players_were_found_for_pinning")}</div>`}
          </div>
          <div class="settings-hint">${this._i18n("ui.only_music_assistant_players_are_shown_here_browser_and_local_sendspin_p")}</div>
          <div class="settings-label">${this._i18n("ui.excluded_players")}</div>
          <div class="settings-check-grid">
            ${playerOptions.length ? playerOptions.map(([value, label]) => `
              <label class="settings-check-pill">
                <input type="checkbox" data-setting-excluded-player="${this._esc(value)}" ${excludedPlayers.has(value) ? "checked" : ""}>
                <span>${this._esc(label)}</span>
              </label>`).join("") : `<div class="settings-hint">${this._i18n("ui.no_music_assistant_players_were_found_for_pinning")}</div>`}
          </div>
          <div class="settings-hint">${this._i18n("ui.choose_music_assistant_players_to_hide_from_the_card")}</div>
          <div class="settings-label">${this._i18n("ui.player_sort")}</div>
          <select class="media-sort-select settings-select" id="playerSortModeSelect" aria-label="${this._esc(this._i18n("ui.player_sort"))}">
            <option value="default" ${playerSortMode === "default" ? "selected" : ""}>${this._esc(this._i18n("ui.default_order"))}</option>
            <option value="alphabetical" ${playerSortMode === "alphabetical" ? "selected" : ""}>${this._esc(this._i18n("ui.alphabetical"))}</option>
            <option value="custom" ${playerSortMode === "custom" ? "selected" : ""}>${this._esc(this._i18n("ui.custom_order"))}</option>
          </select>
          ${playerSortMode === "custom" ? `
            <div class="settings-check-grid quick-actions-grid">
              ${playerOptions.map(([value, label]) => {
      const selectedIndex = playerOrder.includes(value) ? playerOrder.indexOf(value) : playerOrder.length + playerOptions.findIndex(([candidate]) => candidate === value);
      return `
                  <div class="settings-check-pill quick-action-pill">
                    <span>${this._esc(label)}</span>
                    <span class="quick-action-order-controls">
                      <button type="button" class="quick-action-order-btn" data-setting-player-order-move="${this._esc(value)}" data-direction="-1" title="${this._esc(this._i18n("ui.move_up"))}" ${selectedIndex <= 0 ? "disabled" : ""}>${this._iconSvg("up")}</button>
                      <button type="button" class="quick-action-order-btn" data-setting-player-order-move="${this._esc(value)}" data-direction="1" title="${this._esc(this._i18n("ui.move_down"))}" ${selectedIndex >= playerOptions.length - 1 ? "disabled" : ""}>${this._iconSvg("down")}</button>
                    </span>
                  </div>`;
    }).join("")}
            </div>
          ` : ``}
        </div>
        <div class="settings-group voice-assistant-settings-card">
          <div class="settings-label">${this._flowAssistantLabel()}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", voiceAssistantEnabled ? "on" : "off", "data-setting-voice-assistant")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", voiceAssistantEnabled ? "on" : "off", "data-setting-voice-assistant")}
          </div>
          <div class="settings-label">${this._i18n("ui.voice_assistant_mode")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.hybrid_music_plus_assist"), "hybrid", voiceAssistantMode, "data-setting-voice-assistant-mode")}
            ${this._settingsPill(this._i18n("ui.music_only"), "music", voiceAssistantMode, "data-setting-voice-assistant-mode")}
            ${this._settingsPill(this._i18n("ui.assist_only"), "assist", voiceAssistantMode, "data-setting-voice-assistant-mode")}
          </div>
          <div class="settings-hint">${this._i18n("ui.hybrid_handles_music_locally_and_sends_unknown_commands_to_assist")}</div>
          <div class="settings-label">${this._i18n("ui.assist_agent")}</div>
          <select class="media-sort-select settings-select" id="voiceAssistantAgentSelect" aria-label="${this._esc(this._i18n("ui.assist_agent"))}">
            ${voiceAssistantAgentOptions.map((option) => `
              <option value="${this._esc(option.value)}" ${option.value === this._voiceAssistantAgentId() ? "selected" : ""}>${this._esc(option.label)}</option>
            `).join("")}
          </select>
          <div class="settings-hint">${this._i18n("ui.optional_assist_agent_leave_empty_for_home_assistant_default")}</div>
          <div class="settings-label">${this._i18n("ui.voice_feedback")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", voiceAssistantSpeakFeedback ? "on" : "off", "data-setting-voice-feedback")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", voiceAssistantSpeakFeedback ? "on" : "off", "data-setting-voice-feedback")}
          </div>
          <div class="settings-hint">${this._i18n("ui.speak_voice_assistant_responses_out_loud")}</div>
        </div>
        <div class="settings-group quick-actions-settings-card">
          <div class="settings-label">${this._i18n("ui.quick_actions")}</div>
          <div class="settings-check-grid quick-actions-grid">
            ${quickActionOptions.map(({ value, label, icon, tone }) => {
      const selectedIndex = quickActions.indexOf(value);
      const selected = selectedIndex >= 0;
      return `
              <div class="settings-check-pill quick-action-pill ${tone === "danger" ? "danger" : ""}">
                <input type="checkbox" data-setting-quick-action="${this._esc(value)}" ${selectedQuickActions.has(value) ? "checked" : ""}>
                <span class="quick-action-setting-icon">${this._iconSvg(icon)}</span>
                <span class="quick-action-pill-label">${this._esc(label)}</span>
                ${selected ? `
                  <span class="quick-action-order-controls">
                    <button type="button" class="quick-action-order-btn" data-setting-quick-action-move="${this._esc(value)}" data-direction="-1" title="${this._esc(this._i18n("ui.move_up"))}" ${selectedIndex <= 0 ? "disabled" : ""}>${this._iconSvg("up")}</button>
                    <button type="button" class="quick-action-order-btn" data-setting-quick-action-move="${this._esc(value)}" data-direction="1" title="${this._esc(this._i18n("ui.move_down"))}" ${selectedIndex >= quickActions.length - 1 ? "disabled" : ""}>${this._iconSvg("down")}</button>
                  </span>
                ` : ``}
              </div>`;
    }).join("")}
          </div>
          <div class="settings-hint">${this._i18n("ui.choose_the_small_buttons_shown_under_the_song_title_timer_appears_first")}</div>
        </div>
        <div class="settings-group">
          <div class="settings-label">${this._i18n("ui.main_bar_items")}</div>
          <div class="settings-check-grid">
            ${showStudioMainBarOption ? `
              <label class="settings-check-pill">
                <input type="checkbox" data-setting-studio-shortcut ${studioShortcut ? "checked" : ""}>
                <span>${this._esc(this._controlRoomLabel())}</span>
              </label>
            ` : ``}
            ${visibleMainBarOptions.map(([value, label]) => {
      const locked = settingsMainBarLocked && value === "settings";
      return `
              <label class="settings-check-pill ${locked ? "is-locked" : ""}">
                <input type="checkbox" data-setting-main-bar-item="${this._esc(value)}" ${selectedMainBar.has(value) || locked ? "checked" : ""} ${locked ? "disabled" : ""}>
                <span>${this._esc(label)}</span>
                ${locked ? `<span class="settings-fixed-badge">${this._esc(this._i18n("ui.fixed"))}</span>` : ""}
              </label>`;
    }).join("")}
          </div>
          <div class="settings-label">${this._i18n("ui.volume_control_large_screen_only")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.always_visible"), "always", volumeMode, "data-setting-volume-mode")}
            ${this._settingsPill(this._i18n("ui.button"), "button", volumeMode, "data-setting-volume-mode")}
          </div>
          <div class="settings-label">${this._i18n("ui.volume_step_buttons")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", volumeStepButtonsEnabled ? "on" : "off", "data-setting-volume-step-buttons")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", volumeStepButtonsEnabled ? "on" : "off", "data-setting-volume-step-buttons")}
          </div>
          <div class="settings-range">
            <div class="settings-label">${this._i18n("ui.volume_step_percent")}</div>
            <input id="mobileVolumeStepRange" type="range" min="1" max="10" step="1" value="${this._esc(String(volumeStepPercent))}">
            <div class="settings-value">${this._esc(String(volumeStepPercent))}%</div>
          </div>
          <div class="settings-label">${this._i18n("ui.liked_sync")}</div>
          <div class="settings-pills">
            ${this._settingsPill("Music Assistant", "ma", likedMode, "data-setting-liked-mode")}
            ${this._config?.allow_local_likes === true ? this._settingsPill(this._i18n("ui.local"), "local", likedMode, "data-setting-liked-mode") : ""}
          </div>
          <div class="settings-label">Radio Browser</div>
          <select class="media-sort-select settings-select" id="mobileRadioCountrySelect" aria-label="${this._esc(this._i18n("ui.radio_browser_country"))}">
            ${radioCountryOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === radioCountry ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
          </select>
          <div class="settings-hint">${this._i18n("ui.choose_a_country_or_all_countries_to_browse_every_country_inside_the_rad")}</div>
        </div>
        <div class="settings-group">
          <div class="settings-label">${this._i18n("ui.library_pages")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.grid"), "grid", libraryDefaultLayout, "data-setting-library-default-layout")}
            ${this._settingsPill(this._i18n("ui.list"), "list", libraryDefaultLayout, "data-setting-library-default-layout")}
          </div>
          <div class="settings-hint">${this._i18n("ui.choose_how_library_pages_open_grid_or_list_can_still_be_changed_manually", {}, this._m("Choose how library pages open. You can still switch Grid/List inside the library.", "בחר איך דפי הספריה ייפתחו. עדיין אפשר להחליף ידנית בתוך הספריה."))}</div>
          <div class="settings-check-grid">
            ${tabOptions.map(([value, label]) => `
              <label class="settings-check-pill">
                <input type="checkbox" data-setting-library-tab="${this._esc(value)}" ${selectedTabs.has(value) ? "checked" : ""}>
                <span>${this._esc(label)}</span>
              </label>`).join("")}
          </div>
        </div>
        <div class="settings-group">
          <div class="settings-label">Music Assistant</div>
          <div class="settings-actions">
            <button class="settings-pill active" data-menu-action="open_app">${this._i18n("ui.open_full_interface")}</button>
          </div>
        </div>
        <div class="settings-group smart-home-settings-card">
          <div class="settings-label">${this._i18n("ui.smart_home")}</div>
          <div class="settings-label">${this._i18n("ui.ambient_light")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", this._ambientLightEnabled() ? "on" : "off", "data-setting-ambient-light")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", this._ambientLightEnabled() ? "on" : "off", "data-setting-ambient-light")}
          </div>
          <input class="settings-text-input" id="ambientLightEntitiesInput" type="text" value="${this._esc(ambientEntitiesText)}" placeholder="light.living_room, light.tv">
          <div class="settings-hint">${this._i18n("ui.choose_only_the_lights_that_should_follow_the_music")}</div>
          <div class="settings-label">${this._i18n("ui.ambient_light_player_map")}</div>
          <textarea class="settings-text-input ambient-map-input" id="ambientLightPlayerMapInput" rows="3" placeholder="media_player.kitchen = light.kitchen">${this._esc(ambientPlayerMapText)}</textarea>
          <div class="settings-hint">${this._i18n("ui.ambient_light_player_map_helper")}</div>
          <div class="settings-range">
            <div class="settings-label">${this._i18n("ui.ambient_light_brightness")}</div>
            <input id="ambientLightBrightnessInput" type="range" min="1" max="100" step="1" value="${this._esc(String(this._ambientLightBrightness()))}">
            <div class="settings-value">${this._esc(String(Math.round(this._ambientLightBrightness())))}%</div>
          </div>
          <div class="scheduled-start-grid two-col">
            <label class="scheduled-start-field" for="ambientLightTransitionInput">
              <span class="settings-label">${this._i18n("ui.ambient_light_transition")}</span>
              <input class="media-sort-select settings-select" id="ambientLightTransitionInput" type="number" min="0" max="120" step="1" value="${this._esc(String(this._ambientLightTransition()))}">
            </label>
            <label class="scheduled-start-field" for="ambientLightCooldownInput">
              <span class="settings-label">${this._i18n("ui.ambient_light_cooldown")}</span>
              <input class="media-sort-select settings-select" id="ambientLightCooldownInput" type="number" min="0" max="120" step="1" value="${this._esc(String(this._ambientLightCooldown()))}">
            </label>
          </div>
          <div class="settings-label">${this._i18n("ui.screensaver")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", this._screensaverEnabled() ? "on" : "off", "data-setting-screensaver")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", this._screensaverEnabled() ? "on" : "off", "data-setting-screensaver")}
          </div>
          <div class="settings-label">${this._i18n("ui.screensaver_controls")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", this._screensaverControlsEnabled() ? "on" : "off", "data-setting-screensaver-controls")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", this._screensaverControlsEnabled() ? "on" : "off", "data-setting-screensaver-controls")}
          </div>
          <div class="settings-check-grid screensaver-controls-grid">
            ${screensaverControlOptions.map(({ value, label, icon }) => `
              <label class="settings-check-pill">
                <input type="checkbox" data-setting-screensaver-control="${this._esc(value)}" ${selectedScreensaverControlButtons.has(value) ? "checked" : ""}>
                <span class="quick-action-setting-icon">${this._iconSvg(icon)}</span>
                <span>${this._esc(label)}</span>
              </label>
            `).join("")}
          </div>
          <div class="settings-hint">${this._i18n("ui.choose_which_buttons_appear_on_the_screensaver")}</div>
          <select class="media-sort-select settings-select" id="screensaverClockModeSelect" aria-label="${this._esc(this._i18n("ui.screensaver_clock_mode"))}">
            <option value="digital" ${screensaverClockMode === "digital" ? "selected" : ""}>${this._esc(this._i18n("ui.digital"))}</option>
            <option value="analog" ${screensaverClockMode === "analog" ? "selected" : ""}>${this._esc(this._i18n("ui.analog"))}</option>
          </select>
          <input class="settings-text-input" id="screensaverMessageInput" type="text" value="${this._esc(this._screensaverMessage())}" placeholder="${this._esc(this._i18n("ui.screensaver_message"))}">
          <div class="settings-range">
            <div class="settings-label">${this._i18n("ui.screensaver_timeout")}</div>
            <input id="screensaverTimeoutInput" type="range" min="15" max="600" step="5" value="${this._esc(String(this._screensaverTimeoutSeconds()))}">
            <div class="settings-value">${this._esc(String(Math.round(this._screensaverTimeoutSeconds())))}s</div>
          </div>
          <div class="settings-label">${this._i18n("ui.auxiliary_buttons")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", this._powerButtonEnabled() ? "on" : "off", "data-setting-power-button")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", this._powerButtonEnabled() ? "on" : "off", "data-setting-power-button")}
          </div>
          <input class="settings-text-input" id="powerButtonNameInput" type="text" value="${this._esc(this._state.powerButtonName || "")}" placeholder="${this._esc(this._i18n("ui.auxiliary_button"))}">
          <select class="media-sort-select settings-select" id="powerButtonIconSelect" aria-label="${this._esc(this._i18n("ui.auxiliary_button_icon"))}">
            ${auxiliaryIconOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === this._powerButtonIcon() ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
          </select>
          <select class="media-sort-select settings-select" id="powerButtonActionSelect" aria-label="${this._esc(this._i18n("ui.power_button_action"))}">
            <option value="stop_player" ${powerButtonAction === "stop_player" ? "selected" : ""}>${this._esc(this._i18n("ui.stop_player"))}</option>
            <option value="toggle" ${powerButtonAction === "toggle" ? "selected" : ""}>${this._esc(this._i18n("ui.toggle"))}</option>
            <option value="turn_on" ${powerButtonAction === "turn_on" ? "selected" : ""}>${this._esc(this._i18n("ui.turn_on"))}</option>
            <option value="turn_off" ${powerButtonAction === "turn_off" ? "selected" : ""}>${this._esc(this._i18n("ui.turn_off"))}</option>
            <option value="scene" ${powerButtonAction === "scene" ? "selected" : ""}>${this._esc(this._i18n("ui.scene"))}</option>
            <option value="script" ${powerButtonAction === "script" ? "selected" : ""}>${this._esc(this._i18n("ui.script"))}</option>
          </select>
          <input class="settings-text-input" id="powerButtonEntityInput" type="text" value="${this._esc(this._powerButtonEntity())}" placeholder="script.movie_time">
          ${auxiliaryButtonConfigs.slice(1).map((button, offset) => {
      const index = offset + 2;
      const action = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(button.action);
      const icon = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(button.icon || "power");
      return `
              <div class="settings-label">${this._esc(`${this._i18n("ui.auxiliary_button")} ${index}`)}</div>
              <div class="settings-pills">
                <button class="settings-pill ${button.enabled ? "active" : ""}" data-setting-aux-button="${this._esc(index)}" data-setting-aux-button-enabled="on">${this._esc(this._i18n("ui.enabled"))}</button>
                <button class="settings-pill ${!button.enabled ? "active" : ""}" data-setting-aux-button="${this._esc(index)}" data-setting-aux-button-enabled="off">${this._esc(this._i18n("ui.disabled"))}</button>
              </div>
              <input class="settings-text-input" id="auxButtonNameInput${index}" data-aux-button-name="${index}" type="text" value="${this._esc(button.name || "")}" placeholder="${this._esc(this._i18n("ui.auxiliary_button"))}">
              <select class="media-sort-select settings-select" id="auxButtonIconSelect${index}" data-aux-button-icon="${index}" aria-label="${this._esc(this._i18n("ui.auxiliary_button_icon"))}">
                ${auxiliaryIconOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === icon ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
              </select>
              <select class="media-sort-select settings-select" id="auxButtonActionSelect${index}" data-aux-button-action="${index}" aria-label="${this._esc(this._i18n("ui.auxiliary_button_action"))}">
                <option value="stop_player" ${action === "stop_player" ? "selected" : ""}>${this._esc(this._i18n("ui.stop_player"))}</option>
                <option value="toggle" ${action === "toggle" ? "selected" : ""}>${this._esc(this._i18n("ui.toggle"))}</option>
                <option value="turn_on" ${action === "turn_on" ? "selected" : ""}>${this._esc(this._i18n("ui.turn_on"))}</option>
                <option value="turn_off" ${action === "turn_off" ? "selected" : ""}>${this._esc(this._i18n("ui.turn_off"))}</option>
                <option value="scene" ${action === "scene" ? "selected" : ""}>${this._esc(this._i18n("ui.scene"))}</option>
                <option value="script" ${action === "script" ? "selected" : ""}>${this._esc(this._i18n("ui.script"))}</option>
              </select>
              <input class="settings-text-input" id="auxButtonEntityInput${index}" data-aux-button-entity="${index}" type="text" value="${this._esc(button.entity || "")}" placeholder="script.movie_time">
            `;
    }).join("")}
          <div class="settings-label">${this._i18n("ui.discovery_mode")}</div>
          <div class="settings-pills">
            ${this._settingsPill(this._i18n("ui.enabled"), "on", this._discoveryModeEnabled() ? "on" : "off", "data-setting-discovery-mode")}
            ${this._settingsPill(this._i18n("ui.disabled"), "off", this._discoveryModeEnabled() ? "on" : "off", "data-setting-discovery-mode")}
          </div>
        </div>
        <div class="settings-group">
          <div class="settings-label">${this._i18n("ui.announcement_presets")}</div>
          ${(this._state.mobileAnnouncementPresets || []).slice(0, 3).map((preset, index) => `
            <input class="settings-text-input" data-announcement-preset-index="${this._esc(index)}" type="text" value="${this._esc(preset || "")}" placeholder="${this._esc(this._i18n("ui.preset_message"))}">
          `).join("")}
          <div class="settings-label">${this._i18n("ui.tts_entity")}</div>
          <input class="settings-text-input" id="mobileAnnouncementTtsEntity" type="text" value="${this._esc(this._state.mobileAnnouncementTtsEntity || "")}" placeholder="tts.home_assistant_cloud">
          <div class="settings-label">${this._i18n("ui.tts_language")}</div>
          <select class="media-sort-select settings-select" id="mobileAnnouncementTtsLanguageSelect" aria-label="${this._esc(this._i18n("ui.tts_language"))}">
            ${announcementLanguageOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === announcementLanguage ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
          </select>
          <div class="settings-hint">${this._i18n("ui.text_announcements_use_home_assistant_tts_into_the_selected_music_assist")}</div>
        </div>
                <div class="settings-version">Version ${HOMEII_CARD_VERSION}</div>
      </div>
    `;
  }
  _libraryTabMeta(tab) {
    const map = {
      library_liked: { icon: "heart_filled", type: "liked", title: this._i18n("ui.liked") },
      library_playlists: { icon: "playlist", type: "playlist", title: this._i18n("ui.playlists") },
      library_artists: { icon: "artist", type: "artist", title: this._i18n("ui.artists") },
      library_albums: { icon: "album", type: "album", title: this._i18n("ui.albums") },
      library_tracks: { icon: "tracks", type: "track", title: this._i18n("ui.tracks") },
      library_radio: { icon: "radio", type: "radio", title: this._i18n("ui.radio") },
      library_podcasts: { icon: "podcast", type: "podcast", title: this._i18n("ui.podcasts") },
      library_search: { icon: "search", type: "search", title: this._i18n("ui.search") }
    };
    return map[tab] || map.library_playlists;
  }
  _libraryNavHtml(currentTab) {
    const tabs = this._mobileLibraryTabs();
    return `
      <div class="library-nav">
        ${tabs.map((tab) => {
      const meta = this._libraryTabMeta(tab);
      return `<button class="library-nav-btn ${tab === currentTab ? "active" : ""}" data-menu-nav="${this._esc(tab)}">${this._iconSvg(meta.icon)}</button>`;
    }).join("")}
      </div>
    `;
  }
  _libraryPlayerFocusHtml() {
    const player = this._getSelectedPlayer();
    const playerName = player?.attributes?.friendly_name || this._i18n("ui.choose_player");
    const isPlaying = player?.state === "playing";
    const groupCount = this._playerGroupCount(player);
    return `
      <button class="library-player-focus ${isPlaying ? "is-playing" : ""}" data-menu-nav="players">
        <span class="library-player-art">${this._iconSvg("speaker")}</span>
        <span class="library-player-copy">
          <span class="library-player-name">${this._esc(playerName)}</span>
          <span class="library-player-state">${this._esc(isPlaying ? player?.attributes?.media_title || this._i18n("ui.playing") : this._i18n("ui.ready"))}</span>
        </span>
        ${groupCount ? `<span class="player-group-badge library-focus-badge">${this._esc(groupCount)}</span>` : ``}
        <span class="eq-icon" aria-hidden="true"><span></span><span></span><span></span></span>
      </button>
    `;
  }
  _libraryShellHtml(content, currentTab) {
    return `
      <div class="library-shell">
        <div class="library-body">${content}</div>
        ${this._libraryNavHtml(currentTab)}
      </div>
    `;
  }
  _libraryDetailPopupShellHtml(content, className = "") {
    return `
      <div class="library-shell detail-popup-shell ${this._esc(className)}">
        <div class="library-body">${content}</div>
      </div>
    `;
  }
  _mediaDetailEntryData(item = {}, fallbackType = "track", parentDetail = {}) {
    const mediaItem = item?.media_item || {};
    const mediaType = String(item?.media_type || item?.type || mediaItem?.media_type || fallbackType || "track").toLowerCase();
    const albumValue = item?.album || mediaItem?.album || parentDetail?.album || {};
    const albumName = typeof albumValue === "string" ? albumValue : albumValue?.name || parentDetail?.name || "";
    const artist = this._artistName(item) || item?.artist || item?.artist_str || parentDetail?.artist || "";
    const image = this._artUrl(item) || item?.image || item?.image_url || mediaItem?.image || mediaItem?.image_url || parentDetail?.image || parentDetail?.image_url || "";
    return {
      uri: String(item?.uri || mediaItem?.uri || "").trim(),
      media_type: mediaType,
      name: String(item?.name || item?.title || mediaItem?.name || mediaItem?.title || "").trim(),
      artist,
      album: albumName,
      image
    };
  }
  _mediaDetailDataAttrs(entry = {}) {
    return `data-media-uri="${this._esc(entry.uri || "")}" data-media-type="${this._esc(entry.media_type || "track")}" data-media-name="${this._esc(entry.name || "")}" data-media-artist="${this._esc(entry.artist || "")}" data-media-album="${this._esc(entry.album || "")}" data-media-image="${this._esc(entry.image || "")}"`;
  }
  _mediaDetailTrackRowsHtml(tracks = [], parentDetail = {}) {
    if (!tracks.length) return "";
    return `
      <div class="media-detail-track-list">
        ${tracks.map((track, index) => {
      const entry = this._mediaDetailEntryData(track, "track", parentDetail);
      const dataAttrs = this._mediaDetailDataAttrs(entry);
      const liked = this._isEntryLiked(entry);
      const sub = [entry.artist, entry.album].filter(Boolean).join(" · ");
      const duration = this._fmtDur(track?.duration || track?.media_item?.duration || 0);
      return `
            <div class="media-detail-track-row">
              <div class="media-detail-track-index">${this._esc(String(index + 1))}</div>
              <div class="media-detail-track-copy">
                <div class="media-detail-track-title">${this._esc(entry.name || this._i18n("ui.track"))}</div>
                <div class="media-detail-track-sub">${this._esc(sub || duration || "—")}</div>
              </div>
              ${duration ? `<div class="media-detail-track-duration">${this._esc(duration)}</div>` : ``}
              <div class="media-detail-track-actions">
                <button class="media-detail-action-btn primary" data-media-detail-action="play" ${dataAttrs} title="${this._esc(this._i18n("ui.play_now"))}" aria-label="${this._esc(this._i18n("ui.play_now"))}">${this._iconSvg("play")}</button>
                <button class="media-detail-action-btn" data-media-detail-action="add" ${dataAttrs} title="${this._esc(this._i18n("ui.add_to_queue"))}" aria-label="${this._esc(this._i18n("ui.add_to_queue"))}">${this._iconSvg("queue")}</button>
                <button class="media-detail-action-btn ${liked ? "active" : ""}" data-media-detail-action="like" ${dataAttrs} title="${this._esc(this._i18n("ui.like_2"))}" aria-label="${this._esc(this._i18n("ui.like_2"))}" aria-pressed="${liked ? "true" : "false"}">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}</button>
              </div>
            </div>
          `;
    }).join("")}
      </div>
    `;
  }
  _albumBrowseSelectHtml(browse = null) {
    if (!browse?.albums?.length) return "";
    return `
      <label class="media-detail-album-picker">
        <span class="media-detail-picker-label">${this._esc(this._m("Album", "אלבום"))}</span>
        <select id="mediaDetailAlbumSelect" aria-label="${this._esc(this._m("Select album", "בחר אלבום"))}">
          ${browse.albums.map((album, index) => `
            <option value="${this._esc(String(index))}" ${index === browse.index ? "selected" : ""}>${this._esc(this._albumSelectLabel(album, index))}</option>
          `).join("")}
        </select>
      </label>
    `;
  }
  _libraryMediaDetailHtml(detail = {}) {
    const mediaType = String(detail.media_type || detail.type || "album").toLowerCase();
    if (mediaType === "artist") return this._libraryArtistDetailHtml(detail);
    const title = detail.name || (mediaType === "playlist" ? this._i18n("ui.playlist") : this._i18n("ui.album"));
    const subtitle = [detail.artist, typeof detail.album === "string" ? detail.album : detail.album?.name].filter(Boolean).join(" · ") || (mediaType === "playlist" ? this._i18n("ui.playlist") : this._i18n("ui.album"));
    const art = detail.image || detail.image_url || this._artUrl(detail);
    const fallbackIcon = mediaType === "playlist" ? "playlist" : "album";
    const tracks = Array.isArray(detail.tracks) ? detail.tracks : [];
    const loading = !!detail.loading;
    const trackCount = tracks.length ? `${tracks.length} ${this._i18n("ui.tracks").toLowerCase()}` : "";
    const emptyCopy = detail.error || this._m("No tracks were returned. You can still play it.", "לא נטענו שירים. עדיין אפשר לנגן.");
    const detailEntry = this._mediaDetailEntryData({ ...detail, image: art, image_url: art }, mediaType, detail);
    const detailDataAttrs = this._mediaDetailDataAttrs(detailEntry);
    const detailLiked = this._isEntryLiked(detailEntry);
    const browse = mediaType === "album" ? this._albumBrowseState(detail) : null;
    const kindLabel = mediaType === "album" ? this._albumKindLabel(detail) : "";
    const artHtml = art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg(fallbackIcon);
    return `
      <div class="media-detail-shell album-detail-shell">
        <div class="menu-list-item media-detail-hero album-detail-player">
          <span class="media-detail-art-stage">
            <span class="media-detail-art">${artHtml}</span>
            <span class="media-detail-player-actions media-detail-cover-actions">
              <button class="chip-btn media-detail-play-btn like ${detailLiked ? "active" : ""}" data-media-detail-action="like" ${detailDataAttrs} title="${this._esc(this._i18n("ui.like_2"))}" aria-label="${this._esc(this._i18n("ui.like_2"))}" aria-pressed="${detailLiked ? "true" : "false"}">${this._iconSvg(detailLiked ? "heart_filled" : "heart_outline")}</button>
            </span>
          </span>
          <span class="media-detail-copy">
            <span class="media-detail-kicker">${this._esc(mediaType === "playlist" ? this._i18n("ui.playlist") : this._i18n("ui.album"))}</span>
            <span class="media-detail-title">${this._esc(title)}</span>
            <span class="media-detail-sub">${this._esc(trackCount || subtitle || "—")}${kindLabel ? `<span class="media-detail-kind-badge">${this._esc(kindLabel)}</span>` : ``}</span>
            <span class="media-detail-player-actions media-detail-hero-actions">
              <button class="chip-btn media-detail-play-btn subtle" data-media-detail-action="add" ${detailDataAttrs} title="${this._esc(this._i18n("ui.add_to_queue"))}" aria-label="${this._esc(this._i18n("ui.add_to_queue"))}">${this._iconSvg("queue")}</button>
              <button class="chip-btn media-detail-play-btn" data-media-detail-action="play" ${detailDataAttrs} title="${this._esc(this._i18n("ui.play_now"))}" aria-label="${this._esc(this._i18n("ui.play_now"))}">${this._iconSvg("play")}</button>
            </span>
            ${browse ? this._albumBrowseSelectHtml(browse) : ``}
          </span>
        </div>
        <div class="media-detail-toolbar">
          <div class="media-section-title">${this._esc(this._i18n("ui.tracks"))}</div>
          ${trackCount ? `<div class="media-detail-count-badge">${this._esc(trackCount)}</div>` : ``}
        </div>
        ${loading ? this._loadingStateHtml(this._i18n("ui.loading_library"), { notice: true }) : tracks.length ? this._mediaDetailTrackRowsHtml(tracks, detail) : `<div class="notice open media-detail-empty">${this._esc(emptyCopy)}</div>`}
      </div>
    `;
  }
  _artistAlbumYearGroups(albums = []) {
    const groups = /* @__PURE__ */ new Map();
    (Array.isArray(albums) ? albums : []).forEach((album) => {
      const year = this._mediaYearValue(album);
      const key = year ? String(year) : this._m("Unknown year", "שנה לא ידועה");
      if (!groups.has(key)) groups.set(key, []);
      groups.get(key).push(album);
    });
    return Array.from(groups.entries()).sort(([left], [right]) => {
      const leftYear = Number(left);
      const rightYear = Number(right);
      if (Number.isFinite(leftYear) && Number.isFinite(rightYear)) return rightYear - leftYear;
      if (Number.isFinite(leftYear)) return -1;
      if (Number.isFinite(rightYear)) return 1;
      return String(left).localeCompare(String(right), this._isHebrew() ? "he" : "en", { sensitivity: "base", numeric: true });
    });
  }
  _artistInfoPopupHtml(title = "", description = "") {
    const body = String(description || "").trim() || this._m("Music Assistant did not return detailed artist information yet.", "Music Assistant עדיין לא החזיר מידע מפורט על האמן.");
    return `
      <div class="artist-info-backdrop" data-artist-info-close="1">
        <div class="artist-info-dialog" role="dialog" aria-modal="true" aria-label="${this._esc(title || this._i18n("ui.artist"))}">
          <div class="artist-info-head">
            <div class="artist-info-title">${this._esc(title || this._i18n("ui.artist"))}</div>
            <button class="artist-info-close" data-artist-info-close="1" title="${this._esc(this._i18n("ui.close"))}" aria-label="${this._esc(this._i18n("ui.close"))}">${this._iconSvg("close")}</button>
          </div>
          <div class="artist-info-body">${this._esc(body)}</div>
        </div>
      </div>
    `;
  }
  _libraryArtistDetailHtml(detail = {}) {
    const artistInfo = detail.artistInfo || detail;
    const title = artistInfo.name || detail.name || this._i18n("ui.artist");
    const art = this._artUrl(artistInfo, { size: 512 }) || detail.image || detail.image_url || this._artUrl(detail, { size: 512 });
    const description = this._artistDescriptionText(artistInfo);
    const albums = Array.isArray(detail.albums) ? detail.albums : [];
    const playlists = Array.isArray(detail.playlists) ? detail.playlists : [];
    const loading = !!detail.loading;
    const emptyCopy = detail.error || this._m("No albums were returned for this artist.", "לא נטענו אלבומים לאמן הזה.");
    const searchQuery = detail.artistSearchQuery || "";
    const searchOpen = !!detail.artistSearchOpen;
    const albumGroups = this._artistAlbumYearGroups(albums);
    const artistRadioTitle = this._m("Start artist radio", "הפעל רדיו אמן");
    return `
      <div class="media-detail-shell artist-detail-shell">
        <div class="artist-detail-hero">
          <span class="artist-detail-art">${art ? `<img src="${this._esc(art)}" alt="">` : this._iconSvg("artist")}</span>
          <span class="artist-detail-copy">
            <span class="media-detail-kicker">${this._esc(this._i18n("ui.artist"))}</span>
            <span class="artist-detail-title">${this._esc(title)}</span>
          </span>
          <span class="artist-detail-actions">
            <button class="artist-hero-icon-btn" data-artist-radio="1" data-media-uri="${this._esc(detail.uri || artistInfo.uri || "")}" data-media-type="artist" data-media-name="${this._esc(title)}" title="${this._esc(artistRadioTitle)}" aria-label="${this._esc(artistRadioTitle)}">${this._iconSvg("radio")}</button>
            <button class="artist-hero-icon-btn" data-artist-search-toggle="1" title="${this._esc(this._i18n("ui.search"))}" aria-label="${this._esc(this._i18n("ui.search"))}" aria-expanded="${searchOpen ? "true" : "false"}">${this._iconSvg("search")}</button>
            <button class="artist-info-btn" data-artist-info-open="1" title="${this._esc(this._m("Artist info", "מידע על האמן"))}" aria-label="${this._esc(this._m("Artist info", "מידע על האמן"))}">${this._iconSvg("info")}</button>
          </span>
        </div>
        ${detail.artistInfoOpen ? this._artistInfoPopupHtml(title, description) : ``}
        ${searchOpen ? `
          <div class="artist-detail-search">
            <div class="media-search-shell artist-search-shell">
              <span>${this._iconSvg("search")}</span>
              <input id="artistDetailSearchInput" type="text" value="${this._esc(searchQuery)}" placeholder="${this._esc(this._m("Search another artist", "חפש אמן אחר"))}">
              <button class="chip-btn artist-search-btn" data-artist-detail-search title="${this._esc(this._i18n("ui.search"))}"><span>${this._esc(this._i18n("ui.search"))}</span></button>
            </div>
          </div>
        ` : ``}
        ${loading ? this._loadingStateHtml(this._i18n("ui.loading_library"), { notice: true }) : `
            <div class="artist-detail-section">
              <div class="artist-section-head">
                <div class="media-section-title">${this._esc(this._i18n("ui.albums"))}</div>
                ${albums.length ? `<div class="media-detail-count-badge">${this._esc(`${albums.length} ${this._i18n("ui.albums").toLowerCase()}`)}</div>` : ``}
              </div>
              ${albumGroups.length ? albumGroups.map(([year, yearAlbums]) => `
                  <div class="artist-year-group">
                    <div class="artist-year-title">${this._esc(year)}</div>
                    ${this._mediaItemsListHtml(yearAlbums, "album", { layout: "grid", albumBadges: true })}
                  </div>
                `).join("") : `<div class="notice open media-detail-empty">${this._esc(emptyCopy)}</div>`}
            </div>
            <div class="artist-detail-section">
              <div class="media-section-title">${this._esc(this._m("Artist playlist recommendations", "המלצות לפלייליסטים של האמן"))}</div>
              ${playlists.length ? this._mediaItemsListHtml(playlists, "playlist", { layout: "list" }) : `<div class="notice open media-detail-empty">${this._esc(this._m("No playlist recommendations were returned yet.", "עדיין לא נמצאו המלצות לפלייליסטים."))}</div>`}
            </div>
          `}
      </div>
    `;
  }
  async _searchArtistDetailFromInput(sourceEl = null) {
    const input = this.$("artistDetailSearchInput");
    const query = String(input?.value || "").trim();
    if (!query) return false;
    if (this._state.mobileLibraryDetail) this._state.mobileLibraryDetail.artistSearchQuery = query;
    sourceEl?.setAttribute?.("aria-busy", "true");
    try {
      const results = await this._search(query);
      const normalizedQuery = HomeiiMediaQueueFoundation.normalizeComparableText(query);
      const artists = Array.isArray(results.artists) ? results.artists : [];
      const artist = artists.find((item) => HomeiiMediaQueueFoundation.normalizeComparableText(item?.name || "") === normalizedQuery) || artists[0];
      if (!artist) {
        this._toastError(this._m("No artist was found.", "לא נמצא אמן."));
        return false;
      }
      const opened = this._openLibraryMediaDetail({ ...artist, media_type: "artist" }, sourceEl, { replaceCurrentDetail: true });
      if (!opened) this._toastError(this._m("This artist cannot be opened from the returned result.", "לא ניתן לפתוח את האמן מהתוצאה שחזרה."));
      return opened;
    } catch (error) {
      this._toastError(error?.message || this._m("Artist search failed.", "חיפוש האמן נכשל."));
      return false;
    } finally {
      sourceEl?.removeAttribute?.("aria-busy");
    }
  }
  _librarySearchHomeHtml() {
    const q = this._state.mediaQuery || "";
    const voiceSupported = this._isVoiceSearchSupported();
    return `
      <div class="media-home-shell">
        <div class="media-search-zone">
          <div class="media-search-shell">
            <span>${this._iconSvg("search")}</span>
            <input id="mobileMediaSearchInput" type="text" value="${this._esc(q)}" placeholder="${this._i18n("ui.what_would_you_like_to_listen_to")}">
            <button class="media-voice-btn ${voiceSupported ? "" : "unsupported"}" id="mobileVoiceSearchBtn" title="${this._esc(this._i18n("ui.voice_search"))}">${this._iconSvg("mic")}</button>
            <button class="media-search-clear ${q ? "visible" : ""}" id="mobileMediaSearchClear" style="display:${q ? "" : "none"};" title="${this._esc(this._i18n("ui.clear_search"))}">×</button>
          </div>
        </div>
        <div id="mobileMediaSearchResults"></div>
      </div>
    `;
  }
  _quickSearchShellHtml() {
    return `
      <div class="quick-search-shell">
        ${this._librarySearchHomeHtml()}
      </div>
    `;
  }
  _quickSearchRecommendationItems(limit = 8) {
    const seen = /* @__PURE__ */ new Set();
    const out = [];
    const pushItem = (item = {}, fallbackType = "track") => {
      const normalized = this._normalizeMediaItem({
        ...item,
        name: item.name || item.title || item.media_item?.name || "",
        media_type: item.media_type || item.type || fallbackType,
        image: item.image || item.image_url || item.media_item?.image || item.media_item?.album?.image || ""
      });
      const uri = String(normalized?.uri || item?.uri || "").trim();
      if (!uri || seen.has(uri) || out.length >= limit) return;
      seen.add(uri);
      out.push({
        ...normalized,
        uri,
        name: normalized.name || item.title || item.name || this._i18n("ui.recommended_track"),
        media_type: normalized.media_type || fallbackType,
        image: normalized.image || normalized.image_url || item.image || item.image_url || ""
      });
    };
    this._historyRecommendationItems(limit).forEach((item) => pushItem(item, item.media_type || "track"));
    (this._state.emptyQuickShelfItems || []).forEach((item) => pushItem(item, item.media_type || "album"));
    this._visibleRecentHistoryItems().forEach((item) => pushItem(item, item.media_type || "track"));
    return out.slice(0, limit);
  }
  _quickSearchRecommendationsHtml() {
    const items = this._quickSearchRecommendationItems(8);
    if (!items.length) {
      return `<div class="notice open">${this._esc(this._i18n("ui.recommendations_will_appear_once_homeii_flow_sees_your_queue_or_recent_l"))}</div>`;
    }
    return `
      <div class="media-results quick-search-recommendations">
        <div>
          <div class="media-section-title">${this._esc(this._i18n("ui.recommended"))}</div>
          ${this._mediaItemsListHtml(items, "track", { layout: "list", openDetails: false })}
        </div>
      </div>
    `;
  }
  _speechRecognitionCtor() {
    if (typeof window === "undefined") return null;
    return window.SpeechRecognition || window.webkitSpeechRecognition || null;
  }
  _isVoiceSearchSupported() {
    return !!this._speechRecognitionCtor();
  }
  _normalizeSmartVoiceCandidates(results = {}) {
    const order = [
      ["playlists", "playlist"],
      ["tracks", "track"],
      ["albums", "album"],
      ["artists", "artist"],
      ["radio", "radio"],
      ["podcasts", "podcast"]
    ];
    const items = [];
    order.forEach(([groupKey, mediaType]) => {
      const group = Array.isArray(results?.[groupKey]) ? results[groupKey] : [];
      group.forEach((item) => {
        const uri = String(item?.uri || item?.media_item?.uri || "").trim();
        if (!uri) return;
        const mediaItem = item?.media_item || {};
        const artist = this._artistName(item) || this._artistName(mediaItem) || item?.artist || item?.artist_str || item?.media_artist || mediaItem?.artist || mediaItem?.artist_str || mediaItem?.media_artist || "";
        items.push({
          uri,
          media_type: this._voiceAssistantCanonicalMediaType(item?.media_type || item?.type || mediaType, mediaType),
          name: item?.name || item?.title || mediaItem?.name || mediaItem?.title || uri,
          artist,
          album: item?.album?.name || item?.album || mediaItem?.album?.name || mediaItem?.album || "",
          image: this._artUrl(item) || item?.image || item?.image_url || mediaItem?.image || mediaItem?.image_url || "",
          _homeiiVoiceFocused: item?._homeiiVoiceFocused === true
        });
      });
    });
    return items;
  }
  _currentSmartVoiceCandidate() {
    const state = this._state.mobileSmartVoice || null;
    if (!state?.candidates?.length) return null;
    const index = Math.max(0, Math.min(state.candidates.length - 1, Number(state.index || 0)));
    return state.candidates[index] || null;
  }
  _stopSmartVoiceCountdown() {
    clearInterval(this._mobileSmartVoiceTimer);
    this._mobileSmartVoiceTimer = null;
  }
  _closeSmartVoiceConfirm() {
    this._stopSmartVoiceCountdown();
    this._state.mobileSmartVoice = null;
    this.$("mobileSmartVoiceModal")?.classList.remove("open");
    const host = this.$("mobileSmartVoiceSheet");
    if (host) host.innerHTML = "";
  }
  _renderSmartVoiceConfirm() {
    const host = this.$("mobileSmartVoiceSheet");
    const state = this._state.mobileSmartVoice || null;
    const candidate = this._currentSmartVoiceCandidate();
    if (!host || !state || !candidate) return;
    const targetName = this._selectedPlayerName();
    const subtitle = [candidate.artist, candidate.album].filter(Boolean).join(" · ");
    host.innerHTML = `
      <div class="smart-voice-head">
        <div class="smart-voice-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("smart-voice-logo")}</div>
        <div class="smart-voice-title">${this._esc(this._i18n("ui.smart_voice_selection"))}</div>
        <div class="smart-voice-target">${this._esc(this._i18n("ui.player_2"))}: ${this._esc(targetName)}</div>
      </div>
      <div class="smart-voice-card">
        <div class="smart-voice-chip">${this._iconSvg("mic")}<span>${this._esc(state.query || "")}</span></div>
        <div class="smart-voice-name">${this._esc(candidate.name || "")}</div>
        <div class="smart-voice-sub">${this._esc(subtitle || this._i18n("ui.ready_to_play"))}</div>
        <div class="smart-voice-countdown"><span>${this._esc(String(state.countdown || 0))}</span></div>
      </div>
      <div class="confirm-actions smart-voice-actions">
        <button class="menu-item" id="smartVoicePlayNowBtn">${this._esc(this._i18n("ui.play"))}</button>
        <button class="menu-item" id="smartVoiceOtherBtn">${this._esc(this._i18n("ui.other"))}</button>
        <button class="menu-item" id="smartVoiceCancelBtn">${this._esc(this._i18n("ui.cancel_2"))}</button>
      </div>
    `;
    host.querySelector("#smartVoiceCancelBtn")?.addEventListener("click", () => this._closeSmartVoiceConfirm());
    host.querySelector("#smartVoiceOtherBtn")?.addEventListener("click", () => this._chooseAnotherSmartVoiceCandidate());
    host.querySelector("#smartVoicePlayNowBtn")?.addEventListener("click", () => this._playSmartVoiceCandidateNow());
  }
  _openSmartVoiceConfirm(query = "", candidates = []) {
    if (!Array.isArray(candidates) || !candidates.length) {
      this._toastError(this._i18n("ui.no_matching_content_was_found"));
      return;
    }
    this._state.mobileSmartVoice = {
      query,
      candidates,
      index: 0,
      countdown: 5
    };
    this.$("mobileSmartVoiceModal")?.classList.add("open");
    this._renderSmartVoiceConfirm();
    this._stopSmartVoiceCountdown();
    this._mobileSmartVoiceTimer = window.setInterval(() => {
      const state = this._state.mobileSmartVoice;
      if (!state) return this._closeSmartVoiceConfirm();
      state.countdown = Number(state.countdown || 0) - 1;
      if (state.countdown <= 0) {
        this._playSmartVoiceCandidateNow();
        return;
      }
      this._renderSmartVoiceConfirm();
    }, 1e3);
  }
  _chooseAnotherSmartVoiceCandidate() {
    const state = this._state.mobileSmartVoice;
    if (!state?.candidates?.length) return;
    if (state.candidates.length === 1) {
      state.countdown = 5;
      this._renderSmartVoiceConfirm();
      return;
    }
    const currentUri = this._currentSmartVoiceCandidate()?.uri || "";
    const pool = state.candidates.filter((item) => item?.uri && item.uri !== currentUri);
    const next = pool[Math.floor(Math.random() * pool.length)] || state.candidates[(Number(state.index || 0) + 1) % state.candidates.length];
    const nextIndex = Math.max(0, state.candidates.findIndex((item) => item?.uri === next?.uri));
    state.index = nextIndex;
    state.countdown = 5;
    this._hapticTap([8]);
    this._renderSmartVoiceConfirm();
  }
  async _playSmartVoiceCandidateNow() {
    const candidate = this._currentSmartVoiceCandidate();
    if (!candidate?.uri) {
      this._closeSmartVoiceConfirm();
      return;
    }
    this._stopSmartVoiceCountdown();
    await this._playMedia(candidate.uri, candidate.media_type || "playlist", "play", { label: candidate.name || "" });
    this._closeSmartVoiceConfirm();
    this._closeMobileMenu();
  }
  _voiceAssistantRecognitionLanguage() {
    if (this._isHebrew()) return "he-IL";
    try {
      const languages = Array.isArray(window.navigator?.languages) ? window.navigator.languages : [window.navigator?.language || ""];
      if (languages.some((language) => String(language || "").toLowerCase().startsWith("he"))) return "he-IL";
    } catch (_) {
    }
    return "en-US";
  }
  _voiceAssistantAssistLanguage() {
    return this._voiceAssistantRecognitionLanguage().toLowerCase().startsWith("he") ? "he" : "en";
  }
  _normalizeVoiceCommandText(value = "") {
    return normalizeVoiceCommandText(value);
  }
  _voiceCommandHasAny(normalizedText = "", terms = []) {
    return voiceCommandHasAny(normalizedText, terms);
  }
  _voiceAssistantAliasIndex(normalizedText = "", alias = "") {
    return voiceAssistantAliasIndex(normalizedText, alias);
  }
  _voiceAssistantPlayerPool() {
    this._loadPlayers();
    return (this._state.players || []).filter((player) => this._isMusicAssistantPlayer(player)).filter((player) => !this._isLikelyBrowserPlayer(player) || this._isLocalSendspinPlayer(player)).filter((player) => this._isAvailableThisDevicePlayer(player));
  }
  _voiceAssistantPlayerAliases(player = null) {
    if (!player) return [];
    const attrs = player.attributes || {};
    const raw = player.__homeiiRawPlayer || {};
    const candidates = [
      player.entity_id,
      String(player.entity_id || "").replace(/^media_player\./, "").replace(/_/g, " "),
      attrs.friendly_name,
      attrs.name,
      attrs.display_name,
      attrs.mass_player_id,
      attrs.player_id,
      raw.name,
      raw.display_name,
      raw.friendly_name
    ];
    const aliases = [];
    candidates.forEach((value) => {
      const alias = this._normalizeVoiceCommandText(value);
      if (!alias || alias.length < 2) return;
      if (["media player", "music assistant", "homeii direct", "homeii flow"].includes(alias)) return;
      if (!aliases.includes(alias)) aliases.push(alias);
    });
    return aliases.sort((left, right) => right.length - left.length);
  }
  _voiceAssistantMentionedPlayers(transcript = "") {
    const normalized = this._normalizeVoiceCommandText(transcript);
    if (!normalized) return [];
    const matches = [];
    this._voiceAssistantPlayerPool().forEach((player) => {
      this._voiceAssistantPlayerAliases(player).forEach((alias) => {
        const index = this._voiceAssistantAliasIndex(normalized, alias);
        if (index < 0) return;
        matches.push({ player, alias, index, end: index + alias.length });
      });
    });
    const ranges = [];
    const seen = /* @__PURE__ */ new Set();
    return matches.sort((left, right) => left.index - right.index || right.alias.length - left.alias.length).filter((match) => {
      if (!match.player?.entity_id || seen.has(match.player.entity_id)) return false;
      const overlaps = ranges.some(([start, end]) => match.index < end && match.end > start);
      if (overlaps) return false;
      seen.add(match.player.entity_id);
      ranges.push([match.index, match.end]);
      return true;
    }).map((match) => match.player);
  }
  _voiceAssistantDefaultPlayer(excludeEntityIds = [], { allowIdle = false } = {}) {
    const excluded = new Set((Array.isArray(excludeEntityIds) ? excludeEntityIds : []).filter(Boolean));
    const players = this._voiceAssistantPlayerPool();
    const selected = this._getSelectedPlayer();
    if (selected?.entity_id && !excluded.has(selected.entity_id) && players.some((player) => player.entity_id === selected.entity_id)) return selected;
    return players.find((player) => player.state === "playing" && !excluded.has(player.entity_id)) || (allowIdle ? players.find((player) => !excluded.has(player.entity_id)) : null) || null;
  }
  _resolveVoiceAssistantTarget(transcript = "") {
    const normalized = this._normalizeVoiceCommandText(transcript);
    const players = this._voiceAssistantPlayerPool();
    const explicit = players.find((player) => this._voiceAssistantPlayerAliases(player).some((alias) => normalized.includes(alias)));
    if (explicit) return { player: explicit, explicit: true };
    const selected = this._getSelectedPlayer();
    if (selected && players.some((player) => player.entity_id === selected.entity_id)) {
      return { player: selected, explicit: false };
    }
    return {
      player: players.find((player) => player.state === "playing") || players[0] || null,
      explicit: false
    };
  }
  _stripVoiceAssistantPlayerAliases(text = "", player = null) {
    return stripVoiceAssistantPlayerAliases(text, this._voiceAssistantPlayerAliases(player));
  }
  _extractVoiceAssistantMusicQuery(transcript = "", player = null) {
    return extractVoiceAssistantMusicQuery(transcript, this._voiceAssistantPlayerAliases(player));
  }
  _voiceAssistantQueueIntent(transcript = "") {
    const normalized = this._normalizeVoiceCommandText(transcript);
    if (!normalized) return null;
    const hasQueueWord = this._voiceCommandHasAny(normalized, ["queue", "current queue", "play queue", "music queue", "תור", "טור", "התור", "הטור", "תור ניגון", "רשימת השמעה"]);
    const hasTransferWord = this._voiceCommandHasAny(normalized, ["transfer", "move", "send", "move queue", "transfer queue", "העבר", "להעביר", "תעביר", "תעבירי", "הזז", "העברת"]);
    const mentioned = this._voiceAssistantMentionedPlayers(transcript);
    if (!hasTransferWord || !hasQueueWord && mentioned.length < 2) return null;
    let sourcePlayer = null;
    let targetPlayer = null;
    if (mentioned.length >= 2) {
      sourcePlayer = mentioned[0];
      targetPlayer = mentioned[1];
    } else if (mentioned.length === 1) {
      targetPlayer = mentioned[0];
      sourcePlayer = this._voiceAssistantDefaultPlayer([targetPlayer.entity_id]);
    }
    return {
      type: "queue_transfer",
      sourcePlayerId: sourcePlayer?.entity_id || "",
      targetPlayerId: targetPlayer?.entity_id || ""
    };
  }
  _voiceAssistantSpeakerGroupIntent(transcript = "") {
    const normalized = this._normalizeVoiceCommandText(transcript);
    if (!normalized) return null;
    const mentioned = this._voiceAssistantMentionedPlayers(transcript);
    const hasSpeakerWord = this._voiceCommandHasAny(normalized, ["speaker", "speakers", "player", "players", "room", "rooms", "רמקול", "רמקולים", "נגן", "נגנים", "חדר", "חדרים"]);
    const hasGroupWord = this._voiceCommandHasAny(normalized, ["group", "group speakers", "join", "connect speakers", "link speakers", "ungroup", "disconnect group", "speaker group", "קבוצה", "קבוצת נגנים", "חיבור רמקולים", "חבר רמקולים", "ניתוק רמקולים", "צימוד רמקולים"]);
    const hasDisconnectWord = this._voiceCommandHasAny(normalized, ["ungroup", "disconnect group", "disconnect speakers", "unjoin", "clear group", "נתק", "תנתק", "תנתקי", "פרק", "תפרק", "הפרד", "נתק קבוצה", "נתק רמקולים"]);
    const hasConnectWord = this._voiceCommandHasAny(normalized, ["group", "join", "connect", "link", "pair", "activate speakers", "start speakers", "חבר", "תחבר", "תחברי", "צרף", "תצרף", "תצרפי", "צמד", "אחד", "סנכרן", "הפעל", "תפעיל", "תפעילי"]);
    const allGroups = this._voiceCommandHasAny(normalized, ["all groups", "all speakers", "all players", "כל הקבוצות", "כל הרמקולים", "כל הנגנים", "כולם"]);
    const speakerCountHint = this._voiceCommandHasAny(normalized, ["two speakers", "2 speakers", "שני רמקולים", "2 רמקולים", "שתי רמקולים", "שני נגנים", "2 נגנים"]);
    if (hasDisconnectWord && (hasGroupWord || hasSpeakerWord || allGroups || mentioned.length)) {
      if (allGroups) return { type: "group_disconnect_all" };
      const player = mentioned[0] || this._voiceAssistantDefaultPlayer();
      return { type: "group_disconnect", playerId: player?.entity_id || "" };
    }
    if (!hasConnectWord || !hasGroupWord && !hasSpeakerWord && !speakerCountHint && mentioned.length < 2) return null;
    let primaryPlayer = null;
    let memberPlayers = [];
    if (mentioned.length >= 2) {
      primaryPlayer = mentioned[0];
      memberPlayers = mentioned.slice(1);
    } else if (mentioned.length === 1) {
      primaryPlayer = this._voiceAssistantDefaultPlayer([mentioned[0].entity_id]);
      memberPlayers = primaryPlayer ? [mentioned[0]] : [];
    }
    return {
      type: "group_connect",
      primaryPlayerId: primaryPlayer?.entity_id || "",
      memberPlayerIds: memberPlayers.map((player) => player?.entity_id).filter(Boolean)
    };
  }
  _voiceAssistantVolumeIntent(normalizedText = "") {
    return voiceAssistantVolumeIntent(normalizedText);
  }
  _voiceAssistantCommandIntent(transcript = "", player = null, { forceMusic = false } = {}) {
    const normalized = this._normalizeVoiceCommandText(transcript);
    if (!normalized) return { type: "unknown" };
    const queueIntent = this._voiceAssistantQueueIntent(transcript);
    if (queueIntent) return queueIntent;
    const speakerGroupIntent = this._voiceAssistantSpeakerGroupIntent(transcript);
    if (speakerGroupIntent) return speakerGroupIntent;
    const volumeIntent = this._voiceAssistantVolumeIntent(normalized);
    if (volumeIntent) return volumeIntent;
    if (this._voiceCommandHasAny(normalized, ["next", "skip", "הבא", "הבאה", "דלג", "תעביר", "תעבירי"])) return { type: "next" };
    if (this._voiceCommandHasAny(normalized, ["previous", "back", "last song", "הקודם", "הקודמת", "חזור", "תחזור"])) return { type: "previous" };
    if (this._voiceCommandHasAny(normalized, ["pause", "hold", "השהה", "להשהות"])) return { type: "pause" };
    if (this._voiceCommandHasAny(normalized, ["stop", "turn off music", "עצור", "תעצור", "תפסיק", "כבה מוזיקה"])) return { type: "stop" };
    if (this._voiceCommandHasAny(normalized, ["resume", "continue", "play music", "המשך", "תמשיך", "להמשיך"])) return { type: "resume" };
    const hasMusicVerb = this._voiceCommandHasAny(normalized, [
      "play",
      "put on",
      "listen to",
      "start music",
      "נגן",
      "נגני",
      "תנגן",
      "תנגני",
      "שים",
      "שימי",
      "השמע",
      "תשמיע",
      "תשמיעי",
      "הפעל",
      "תפעיל",
      "תפעילי"
    ]);
    if (hasMusicVerb || forceMusic) {
      const query = this._extractVoiceAssistantMusicQuery(transcript, player);
      return query ? { type: "music", query } : { type: "resume" };
    }
    return { type: "unknown" };
  }
  _voiceAssistantRequestedMediaType(query = "") {
    return voiceAssistantRequestedMediaType(query);
  }
  _voiceAssistantCanonicalMediaType(value = "", fallback = "track") {
    return voiceAssistantCanonicalMediaType(value, fallback);
  }
  _voiceAssistantImportantMusicTokens(value = "") {
    return voiceAssistantImportantMusicTokens(value);
  }
  _voiceAssistantCleanMusicPhrase(value = "", { allowStopWordFallback = false } = {}) {
    return voiceAssistantCleanMusicPhrase(value, { allowStopWordFallback });
  }
  _voiceAssistantTransliterateHebrewToken(value = "") {
    return voiceAssistantTransliterateHebrewToken(value);
  }
  _voiceAssistantLatinPhoneticKeys(value = "") {
    return voiceAssistantLatinPhoneticKeys(value);
  }
  _voiceAssistantTextHasToken(normalizedText = "", token = "") {
    return voiceAssistantTextHasToken(normalizedText, token);
  }
  _voiceAssistantMatchedTokenCount(tokens = [], normalizedText = "") {
    return voiceAssistantMatchedTokenCount(tokens, normalizedText);
  }
  _voiceAssistantMusicQueryParts(query = "") {
    return voiceAssistantMusicQueryParts(query);
  }
  _voiceAssistantFocusedMusicQuery(query = "") {
    return voiceAssistantFocusedMusicQuery(query);
  }
  async _voiceAssistantFocusedMusicSearch(query = "", mediaType = "track") {
    const safeQuery = String(query || "").trim();
    const type = this._voiceAssistantCanonicalMediaType(mediaType, "track");
    if (!safeQuery) return this._emptySearchResults();
    try {
      const raw = await this._callService("search", { name: safeQuery, query: safeQuery, limit: 30, media_type: [type] });
      return this._normalizeSearchResponse(raw);
    } catch (_) {
      try {
        const raw2 = await this._callService("search", { name: safeQuery, limit: 30, media_type: type });
        return this._normalizeSearchResponse(raw2);
      } catch (_2) {
      }
    }
    return this._emptySearchResults();
  }
  _markVoiceAssistantFocusedResults(results = {}) {
    const out = this._emptySearchResults();
    Object.keys(out).forEach((group) => {
      out[group] = (Array.isArray(results?.[group]) ? results[group] : []).map((item) => ({ ...item, _homeiiVoiceFocused: true }));
    });
    return out;
  }
  _voiceAssistantCandidateScore(candidate = {}, query = "", request = this._voiceAssistantRequestedMediaType(query)) {
    return voiceAssistantCandidateScore(candidate, query, request);
  }
  _voiceAssistantCandidateMatch(candidate = {}, query = "", request = this._voiceAssistantRequestedMediaType(query), index = 0) {
    return voiceAssistantCandidateMatch(candidate, query, request, index);
  }
  _voiceAssistantRankedCandidates(results = {}, query = "") {
    return voiceAssistantRankedCandidates(
      this._normalizeSmartVoiceCandidates(results),
      query
    );
  }
  _voiceAssistantBestCandidate(results = {}, query = "") {
    return voiceAssistantBestCandidate(
      this._normalizeSmartVoiceCandidates(results),
      query
    );
  }
  async _playVoiceAssistantMusic(query = "", player = null) {
    const target = player || this._getSelectedPlayer();
    if (!target?.entity_id) {
      const message = this._i18n("ui.voice_command_no_player");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
    const safeQuery = String(query || "").trim();
    if (!safeQuery) {
      const message = this._i18n("ui.voice_command_not_understood");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
    this._toast(this._i18n("ui.voice_music_searching", { query: safeQuery }));
    try {
      let results = this._emptySearchResults();
      try {
        results = await this._search(safeQuery);
      } catch (error) {
        this._debugLog?.("warn", "[HOMEii Voice] Music search failed", { query: safeQuery, error });
      }
      const requested = this._voiceAssistantRequestedMediaType(safeQuery);
      const focusedQuery = this._voiceAssistantFocusedMusicQuery(safeQuery);
      const focusedType = this._voiceAssistantCanonicalMediaType(requested?.type || "track", "track");
      if (!this._hasSearchResults(results) && focusedQuery && focusedType) {
        const focusedResults = await this._voiceAssistantFocusedMusicSearch(focusedQuery, focusedType);
        results = this._mergeSearchResults(this._markVoiceAssistantFocusedResults(focusedResults), results);
      }
      const rankedCandidates = this._voiceAssistantRankedCandidates(results, safeQuery);
      const candidate = rankedCandidates.find((match) => match.accepted && match.candidate?.uri)?.candidate || rankedCandidates.find((match) => match.candidate?.uri)?.candidate || this._normalizeSmartVoiceCandidates(results).find((item) => item?.uri) || null;
      if (!candidate?.uri) {
        const message = this._i18n("ui.no_matching_content_was_found");
        this._toastError(message);
        return { handled: true, ok: false, message };
      }
      if (target.entity_id !== this._state.selectedPlayer) this._selectPlayer(target.entity_id, true);
      const title = candidate.name || safeQuery;
      this._updateVoiceAssistantDialog({ status: "processing", response: this._i18n("ui.voice_starting_playback", { title }) });
      const mediaType = this._voiceAssistantCanonicalMediaType(candidate.media_type || focusedType, focusedType);
      const played = await this._playMediaOnPlayer(target.entity_id, candidate.uri, mediaType, "play", {
        label: title,
        silent: true
      });
      if (!played) {
        const message = this._i18n("ui.could_not_play_label", { label: title });
        this._toastError(message);
        return { handled: true, ok: false, message };
      }
      return {
        handled: true,
        ok: true,
        message: this._i18n("ui.voice_playing_result", { title }),
        autoCloseMs: 1400
      };
    } catch (error) {
      const message = error?.message || this._i18n("ui.voice_command_failed");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
  }
  async _runVoiceAssistantPlayerManagementCommand(intent = {}) {
    const type = String(intent?.type || "");
    if (!["queue_transfer", "group_connect", "group_disconnect", "group_disconnect_all"].includes(type)) return null;
    try {
      if (type === "queue_transfer") {
        const sourcePlayerId = String(intent.sourcePlayerId || "").trim();
        const targetPlayerId = String(intent.targetPlayerId || "").trim();
        if (!sourcePlayerId || !targetPlayerId || sourcePlayerId === targetPlayerId) {
          const message2 = this._i18n("ui.voice_queue_transfer_needs_players");
          this._toastError(message2);
          return { handled: true, ok: false, message: message2 };
        }
        const ok = await this._transferQueueBetween(sourcePlayerId, targetPlayerId, { silent: true });
        const source = this._controlRoomPlayerName(sourcePlayerId);
        const target = this._controlRoomPlayerName(targetPlayerId);
        const message = ok ? this._i18n("ui.voice_queue_transferred_between", { source, target }) : this._i18n("ui.queue_action_failed");
        (ok ? this._toastSuccess : this._toastError).call(this, message);
        return { handled: true, ok, message, autoCloseMs: ok ? 1400 : 0 };
      }
      if (type === "group_connect") {
        const primaryPlayerId = String(intent.primaryPlayerId || "").trim();
        const memberPlayerIds = [...new Set((Array.isArray(intent.memberPlayerIds) ? intent.memberPlayerIds : []).filter((id) => id && id !== primaryPlayerId))];
        if (!primaryPlayerId || !memberPlayerIds.length) {
          const message2 = this._i18n("ui.voice_group_connect_needs_players");
          this._toastError(message2);
          return { handled: true, ok: false, message: message2 };
        }
        const grouped = await this._applySpeakerGroupFor(primaryPlayerId, memberPlayerIds);
        if (!grouped) {
          const message2 = this._i18n("ui.select_at_least_two_players_to_create_a_group");
          this._toastError(message2);
          return { handled: true, ok: false, message: message2 };
        }
        const primary = this._controlRoomPlayerName(primaryPlayerId);
        const members = memberPlayerIds.map((entityId) => this._controlRoomPlayerName(entityId)).join(", ");
        const message = this._i18n("ui.voice_group_connected_players", { primary, members });
        this._toastSuccess(message);
        setTimeout(() => {
          this._loadPlayers();
          this._refreshGroupingState();
          if (this._state.menuOpen) this._renderMobileMenu();
        }, 550);
        return { handled: true, ok: true, message, autoCloseMs: 1400 };
      }
      if (type === "group_disconnect") {
        const playerId = String(intent.playerId || "").trim();
        if (!playerId) {
          const message2 = this._i18n("ui.voice_group_disconnect_needs_player");
          this._toastError(message2);
          return { handled: true, ok: false, message: message2 };
        }
        const ok = await this._clearSpeakerGroupFor(playerId);
        const player = this._controlRoomPlayerName(playerId);
        const message = ok ? this._i18n("ui.voice_group_disconnected_player", { player }) : this._i18n("ui.player_groups_could_not_be_disconnected");
        (ok ? this._toastSuccess : this._toastError).call(this, message);
        return { handled: true, ok, message, autoCloseMs: ok ? 1200 : 0 };
      }
      if (type === "group_disconnect_all") {
        const result = await this._disconnectPlayerGroups({ silent: true });
        const ok = result?.ok !== false;
        const message = ok ? Number(result?.count || 0) > 0 ? this._i18n("ui.all_player_groups_disconnected") : this._i18n("ui.no_player_groups_to_disconnect") : this._i18n("ui.player_groups_could_not_be_disconnected");
        (ok ? this._toastSuccess : this._toastError).call(this, message);
        return { handled: true, ok, message, autoCloseMs: ok ? 1200 : 0 };
      }
    } catch (error) {
      const message = error?.message || this._i18n("ui.voice_command_failed");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
    return null;
  }
  async _runVoiceAssistantMediaCommand(intent = {}, player = null) {
    const playerManagementResult = await this._runVoiceAssistantPlayerManagementCommand(intent);
    if (playerManagementResult) return playerManagementResult;
    if (!player?.entity_id) {
      const message = this._i18n("ui.voice_command_no_player");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
    const entityId = player.entity_id;
    try {
      if (entityId !== this._state.selectedPlayer) this._selectPlayer(entityId, true);
      if (intent.type === "next" || intent.type === "previous") {
        await this._playerCmdFor(entityId, intent.type === "previous" ? "previous" : "next");
      } else if (intent.type === "pause") {
        if (this._isDirectMaPlayer(player)) await this._callDirectMaPlayerCommand(player, "players/cmd/pause");
        else await this._callHaServiceRaw("media_player", "media_pause", { entity_id: entityId });
      } else if (intent.type === "resume") {
        if (this._isDirectMaPlayer(player)) await this._callDirectMaPlayerCommand(player, "players/cmd/play");
        else await this._callHaServiceRaw("media_player", "media_play", { entity_id: entityId });
      } else if (intent.type === "stop") {
        if (this._isDirectMaPlayer(player)) await this._callDirectMaPlayerCommand(player, "players/cmd/stop");
        else await this._callHaServiceRaw("media_player", "media_stop", { entity_id: entityId });
      } else if (intent.type === "mute" || intent.type === "unmute") {
        const shouldMute = intent.type === "mute";
        if (this._isMuted(player) !== shouldMute) await this._toggleMuteFor(entityId);
      } else if (intent.type === "volume_set") {
        await this._setPlayerVolumeFor(entityId, intent.level);
      } else if (intent.type === "volume_delta") {
        const current = Number(player.attributes?.volume_level);
        const base = Number.isFinite(current) ? current : 0.35;
        await this._setPlayerVolumeFor(entityId, Math.max(0, Math.min(1, base + Number(intent.delta || 0))));
      } else {
        return { handled: false, ok: false, message: "" };
      }
      const actionLabel = {
        next: this._m("next track", "השיר הבא"),
        previous: this._m("previous track", "השיר הקודם"),
        pause: this._m("pause", "השהיה"),
        resume: this._m("play", "ניגון"),
        stop: this._m("stop", "עצירה"),
        mute: this._m("mute", "השתקה"),
        unmute: this._m("unmute", "ביטול השתקה"),
        volume_set: this._m("volume", "עוצמת קול"),
        volume_delta: this._m("volume", "עוצמת קול")
      }[intent.type] || this._i18n("ui.voice_command_executed");
      const message = this._i18n("ui.voice_command_completed_action", { action: actionLabel });
      this._toastSuccess(message);
      return { handled: true, ok: true, message, autoCloseMs: 1200 };
    } catch (error) {
      const message = error?.message || this._i18n("ui.voice_command_failed");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
  }
  _assistResponseSpeech(response = null) {
    const candidates = [
      response?.response?.speech?.plain?.speech,
      response?.response?.speech?.plain,
      response?.speech?.plain?.speech,
      response?.speech?.plain,
      response?.response?.speech,
      response?.speech
    ];
    const found = candidates.find((value) => typeof value === "string" && value.trim());
    return String(found || "").trim();
  }
  async _sendVoiceCommandToAssist(transcript = "") {
    const text = String(transcript || "").trim();
    if (!text) return false;
    const payload = {
      type: "conversation/process",
      text,
      language: this._voiceAssistantAssistLanguage()
    };
    const agentId = this._voiceAssistantAgentId();
    if (agentId) payload.agent_id = agentId;
    try {
      const response = await this._callHomeAssistantWs(payload);
      const speech = this._assistResponseSpeech(response);
      if (speech) this._toast(speech, "info", { duration: 6500 });
      else this._toastSuccess(this._i18n("ui.voice_command_sent"));
      return {
        handled: true,
        ok: true,
        message: speech || this._i18n("ui.voice_command_sent")
      };
    } catch (error) {
      const message = error?.message || this._i18n("ui.voice_command_failed");
      this._toastError(message);
      return { handled: true, ok: false, message };
    }
  }
  async _handleVoiceAssistantTranscript(transcript = "") {
    const text = String(transcript || "").trim();
    if (!text) {
      const message2 = this._i18n("ui.no_speech_was_captured");
      this._toastError(message2);
      return { handled: true, ok: false, message: message2 };
    }
    const mode = this._voiceAssistantMode();
    const target = this._resolveVoiceAssistantTarget(text);
    if (mode !== "assist") {
      const intent = this._voiceAssistantCommandIntent(text, target.player, { forceMusic: mode === "music" });
      if (intent.type === "music") {
        return this._playVoiceAssistantMusic(intent.query, target.player);
      }
      if (intent.type !== "unknown") {
        return this._runVoiceAssistantMediaCommand(intent, target.player);
      }
    }
    if (mode !== "music") {
      return this._sendVoiceCommandToAssist(text);
    }
    const message = this._i18n("ui.voice_command_not_understood");
    this._toastError(message);
    return { handled: true, ok: false, message };
  }
  _stopVoiceAssistantRecognition() {
    clearTimeout(this._voiceAssistantRecognitionTimer);
    this._voiceAssistantRecognitionTimer = null;
    const recognition = this._voiceAssistantRecognition;
    this._voiceAssistantRecognition = null;
    this._state.voiceAssistantListening = false;
    this.$("mobileVoiceAssistantBtn")?.classList.remove("listening");
    this.$("emptyVoiceAssistantBtn")?.classList.remove("listening");
    this.$("screensaverVoiceBtn")?.classList.remove("listening");
    try {
      if (recognition) recognition.__homeiiCancelled = true;
      recognition?.abort?.();
    } catch {
    }
  }
  _voiceAssistantStatusLabel(status = "") {
    const safeStatus = String(status || "").toLowerCase();
    if (safeStatus === "listening") return this._i18n("ui.voice_listening_status");
    if (safeStatus === "processing") return this._i18n("ui.voice_processing_status");
    if (safeStatus === "success") return this._i18n("ui.voice_done_status");
    if (safeStatus === "error") return this._i18n("ui.voice_error_status");
    return this._i18n("ui.voice_ready_status");
  }
  _voiceAssistantDialogIcon(status = "") {
    return String(status || "").toLowerCase() === "error" ? "close" : "mic";
  }
  _openVoiceAssistantDialog(status = "listening", updates = {}) {
    clearTimeout(this._voiceAssistantDialogCloseTimer);
    this._state.voiceAssistantDialogOpen = true;
    this._state.voiceAssistantKeepScreensaver = updates.keepScreensaver === true;
    this._state.voiceAssistantDialogStatus = status;
    this._state.voiceAssistantTranscript = updates.transcript ?? "";
    this._state.voiceAssistantResponse = updates.response ?? "";
    this._syncVoiceAssistantDialog();
  }
  _updateVoiceAssistantDialog(updates = {}) {
    if (updates.status && !["success", "error"].includes(String(updates.status || "").toLowerCase())) {
      clearTimeout(this._voiceAssistantDialogCloseTimer);
    }
    this._state.voiceAssistantDialogOpen = updates.open ?? this._state.voiceAssistantDialogOpen ?? true;
    if (updates.keepScreensaver !== void 0) this._state.voiceAssistantKeepScreensaver = updates.keepScreensaver === true;
    if (updates.status !== void 0) this._state.voiceAssistantDialogStatus = updates.status;
    if (updates.transcript !== void 0) this._state.voiceAssistantTranscript = updates.transcript;
    if (updates.response !== void 0) this._state.voiceAssistantResponse = updates.response;
    this._syncVoiceAssistantDialog();
  }
  _closeVoiceAssistantDialog({ stopRecognition = true } = {}) {
    clearTimeout(this._voiceAssistantDialogCloseTimer);
    clearTimeout(this._voiceAssistantRecognitionTimer);
    this._voiceAssistantRecognitionTimer = null;
    if (stopRecognition) this._stopVoiceAssistantRecognition();
    this._state.voiceAssistantDialogOpen = false;
    this._state.voiceAssistantKeepScreensaver = false;
    this._syncVoiceAssistantDialog();
  }
  _scheduleVoiceAssistantDialogClose(delayMs = 1500) {
    clearTimeout(this._voiceAssistantDialogCloseTimer);
    this._voiceAssistantDialogCloseTimer = setTimeout(() => {
      this._voiceAssistantDialogCloseTimer = null;
      this._closeVoiceAssistantDialog({ stopRecognition: false });
    }, Math.max(500, Number(delayMs) || 1500));
  }
  _syncVoiceAssistantDialog() {
    const host = this.$("voiceAssistantDialog");
    if (!host) return;
    const open = !!this._state.voiceAssistantDialogOpen;
    const status = String(this._state.voiceAssistantDialogStatus || "ready").toLowerCase();
    const transcript = String(this._state.voiceAssistantTranscript || "").trim();
    const response = String(this._state.voiceAssistantResponse || "").trim();
    const keepScreensaver = this._state.voiceAssistantKeepScreensaver === true;
    host.className = `voice-assistant-dialog ${open ? "open" : ""} ${keepScreensaver ? "keep-screensaver" : ""} status-${this._esc(status)}`;
    if (!open) {
      host.innerHTML = "";
      return;
    }
    if (!host.querySelector(".voice-assistant-panel")) {
      host.innerHTML = `
      <div class="voice-assistant-panel" data-screensaver-dialog role="dialog" aria-label="FLOW ASSISTANT">
        <div class="voice-assistant-head">
          <div class="voice-assistant-title-row">
            <span class="voice-assistant-icon" id="voiceAssistantDialogIconSlot"></span>
            <span class="voice-assistant-copy">
              <span class="voice-assistant-brand" aria-hidden="true">${this._tabletBrandSignatureHtml("voice-assistant-logo")}</span>
              <span class="voice-assistant-title">FLOW ASSISTANT</span>
              <span class="voice-assistant-status" id="voiceAssistantDialogStatus"></span>
            </span>
          </div>
          <button class="voice-assistant-close" id="voiceAssistantDialogClose" title="${this._esc(this._i18n("ui.close"))}">${this._iconSvg("close")}</button>
        </div>
        <div class="voice-assistant-meter" aria-hidden="true"><span></span></div>
        <div class="voice-assistant-wave" aria-hidden="true">
          <span></span><span></span><span></span><span></span><span></span>
        </div>
        <div class="voice-assistant-lines">
          <div class="voice-assistant-line">
            <span class="voice-assistant-line-label">${this._esc(this._i18n("ui.voice_transcript"))}</span>
            <span class="voice-assistant-line-text" id="voiceAssistantDialogTranscript"></span>
          </div>
          <div class="voice-assistant-line">
            <span class="voice-assistant-line-label">${this._esc(this._i18n("ui.voice_response"))}</span>
            <span class="voice-assistant-line-text" id="voiceAssistantDialogResponse"></span>
          </div>
        </div>
        <div class="voice-assistant-actions">
          <button type="button" id="voiceAssistantDialogRetry" class="primary">${this._esc(this._i18n("ui.try_again"))}</button>
          <button type="button" id="voiceAssistantDialogCloseSecondary">${this._esc(this._i18n("ui.close"))}</button>
        </div>
      </div>
    `;
      const panel = host.querySelector(".voice-assistant-panel");
      const keepPanelEvent = (event) => {
        if (this._state.screensaverOpen && this._state.voiceAssistantKeepScreensaver === true) {
          event.stopPropagation();
        }
      };
      panel?.addEventListener("pointerdown", keepPanelEvent);
      panel?.addEventListener("click", keepPanelEvent);
      panel?.addEventListener("keydown", keepPanelEvent);
      host.querySelector("#voiceAssistantDialogClose")?.addEventListener("click", () => this._closeVoiceAssistantDialog());
      host.querySelector("#voiceAssistantDialogCloseSecondary")?.addEventListener("click", () => this._closeVoiceAssistantDialog());
      host.querySelector("#voiceAssistantDialogRetry")?.addEventListener("click", () => {
        this._startVoiceAssistantCommand({ keepScreensaver: this._state.voiceAssistantKeepScreensaver === true });
      });
    }
    const retryBtn = host.querySelector("#voiceAssistantDialogRetry");
    if (retryBtn) retryBtn.hidden = status === "listening" || status === "processing";
    const iconSlot = host.querySelector("#voiceAssistantDialogIconSlot");
    const iconName = this._voiceAssistantDialogIcon(status);
    if (iconSlot && iconSlot.dataset.iconName !== iconName) {
      iconSlot.dataset.iconName = iconName;
      iconSlot.innerHTML = this._iconSvg(iconName);
    }
    const statusEl = host.querySelector("#voiceAssistantDialogStatus");
    if (statusEl) statusEl.textContent = this._voiceAssistantStatusLabel(status);
    const transcriptEl = host.querySelector("#voiceAssistantDialogTranscript");
    if (transcriptEl) {
      transcriptEl.textContent = transcript || this._i18n("ui.waiting_for_speech");
      transcriptEl.classList.toggle("voice-assistant-placeholder", !transcript);
    }
    const responseEl = host.querySelector("#voiceAssistantDialogResponse");
    if (responseEl) {
      responseEl.textContent = response || this._i18n("ui.voice_response_will_appear_here");
      responseEl.classList.toggle("voice-assistant-placeholder", !response);
    }
  }
  _voiceAssistantRecognitionErrorMessage(errorCode = "") {
    const code = String(errorCode || "").trim();
    if (code === "no-speech") return this._i18n("ui.no_speech_was_captured");
    if (code === "not-allowed" || code === "service-not-allowed") return this._i18n("ui.microphone_permission_or_browser_blocked");
    if (code === "language-not-supported") return this._i18n("ui.voice_language_is_not_supported");
    if (code === "network") return this._i18n("ui.voice_recognition_network_failed");
    return this._i18n("ui.voice_input_failed");
  }
  _speakVoiceAssistantFeedback(text = "") {
    const message = String(text || "").trim();
    if (!message || !this._voiceAssistantSpeakFeedbackEnabled()) return;
    if (typeof window === "undefined") return;
    const synth = window.speechSynthesis;
    if (!synth || typeof window.SpeechSynthesisUtterance !== "function") return;
    try {
      synth.cancel();
      const utterance = new window.SpeechSynthesisUtterance(message);
      utterance.lang = this._voiceAssistantRecognitionLanguage();
      utterance.rate = 1;
      utterance.pitch = 1;
      synth.speak(utterance);
    } catch (_) {
    }
  }
  _startVoiceAssistantCommand(options = {}) {
    const keepScreensaver = options?.keepScreensaver === true;
    const ignoreWhenListening = options?.ignoreWhenListening === true;
    const scheduleAutoClose = (status = "error", requestedDelay = null) => {
      const requested = Number(requestedDelay);
      const closeMs = Number.isFinite(requested) && requested > 0 ? requested : this._flowAssistantAutoCloseMs(status);
      if (closeMs > 0) this._scheduleVoiceAssistantDialogClose(closeMs);
    };
    if (keepScreensaver) this._resetScreensaverTimer({ hide: false, activity: true });
    if (!this._voiceAssistantEnabled()) {
      const message = this._i18n("ui.voice_assistant_disabled");
      this._toastError(message);
      this._openVoiceAssistantDialog("error", { response: message, keepScreensaver });
      scheduleAutoClose("error");
      return;
    }
    if (this._mobileMicMode() === "off") {
      const message = this._i18n("ui.microphone_is_disabled");
      this._toastError(message);
      this._openVoiceAssistantDialog("error", { response: message, keepScreensaver });
      scheduleAutoClose("error");
      return;
    }
    if (this._state.voiceAssistantListening) {
      if (ignoreWhenListening) {
        this._openVoiceAssistantDialog("listening", {
          transcript: this._state.voiceAssistantTranscript || "",
          response: this._state.voiceAssistantResponse || "",
          keepScreensaver
        });
        return;
      }
      this._stopVoiceAssistantRecognition();
      this._closeVoiceAssistantDialog();
      return;
    }
    if (!keepScreensaver) this._resetScreensaverTimer({ hide: true, activity: true });
    this._openVoiceAssistantDialog("listening", { transcript: "", response: "", keepScreensaver });
    const SpeechRecognition = this._speechRecognitionCtor();
    const micButtons = Array.from(this.shadowRoot?.querySelectorAll("#mobileVoiceAssistantBtn, #emptyVoiceAssistantBtn, #screensaverVoiceBtn") || []);
    if (!SpeechRecognition) {
      const message = this._i18n("ui.voice_input_is_not_supported_on_this_device");
      this._toastError(message);
      this._updateVoiceAssistantDialog({ status: "error", response: message, keepScreensaver });
      scheduleAutoClose("error");
      return;
    }
    clearTimeout(this._voiceAssistantRecognitionTimer);
    this._voiceAssistantRecognitionTimer = null;
    try {
      this._voiceAssistantRecognition?.abort?.();
    } catch {
    }
    const recognition = new SpeechRecognition();
    this._voiceAssistantRecognition = recognition;
    recognition.lang = this._voiceAssistantRecognitionLanguage();
    recognition.interimResults = true;
    recognition.continuous = false;
    recognition.maxAlternatives = 1;
    let capturedTranscript = "";
    let handled = false;
    let recognitionFailed = false;
    const stopListeningUi = () => {
      clearTimeout(this._voiceAssistantRecognitionTimer);
      this._voiceAssistantRecognitionTimer = null;
      this._state.voiceAssistantListening = false;
      micButtons.forEach((btn) => btn.classList.remove("listening"));
    };
    const finishTranscript = (transcript) => {
      if (handled) return;
      handled = true;
      stopListeningUi();
      if (this._voiceAssistantRecognition === recognition) this._voiceAssistantRecognition = null;
      try {
        recognition.abort?.();
      } catch {
      }
      this._updateVoiceAssistantDialog({ status: "processing", transcript, response: this._i18n("ui.voice_processing_status") });
      this._withTimeout(
        this._handleVoiceAssistantTranscript(transcript),
        this._flowAssistantResponseTimeoutMs(),
        this._timeoutMessage(this._flowAssistantLabel())
      ).then((result) => {
        const message = result?.message || (result?.ok === false ? this._i18n("ui.voice_command_failed") : this._i18n("ui.voice_command_executed"));
        const status = result?.ok === false ? "error" : "success";
        this._updateVoiceAssistantDialog({ status, transcript, response: message });
        if (result?.ok !== false) {
          this._speakVoiceAssistantFeedback(message);
        }
        scheduleAutoClose(status, result?.autoCloseMs);
      }).catch((error) => {
        const message = error?.message || this._i18n("ui.voice_command_failed");
        this._toastError(message);
        this._updateVoiceAssistantDialog({ status: "error", transcript, response: message });
        scheduleAutoClose("error");
      });
    };
    recognition.onresult = (event) => {
      const transcript = Array.from(event.results || []).map((result) => result?.[0]?.transcript || "").join(" ").trim();
      if (transcript) capturedTranscript = transcript;
      if (capturedTranscript) this._updateVoiceAssistantDialog({ status: "listening", transcript: capturedTranscript });
      const finalized = Array.from(event.results || []).some((result) => result?.isFinal);
      if (finalized && capturedTranscript) finishTranscript(capturedTranscript);
    };
    recognition.onerror = (event) => {
      if (handled) return;
      if (recognition.__homeiiCancelled) return;
      recognitionFailed = true;
      stopListeningUi();
      if (this._voiceAssistantRecognition === recognition) this._voiceAssistantRecognition = null;
      const message = this._voiceAssistantRecognitionErrorMessage(event?.error);
      this._toastError(message);
      this._updateVoiceAssistantDialog({ status: "error", response: message });
      scheduleAutoClose("error");
    };
    recognition.onend = () => {
      stopListeningUi();
      if (this._voiceAssistantRecognition === recognition) this._voiceAssistantRecognition = null;
      if (recognition.__homeiiCancelled) return;
      if (!handled && capturedTranscript) {
        finishTranscript(capturedTranscript);
      } else if (!handled && !recognitionFailed) {
        const message = this._i18n("ui.no_speech_was_captured");
        this._toastError(message);
        this._updateVoiceAssistantDialog({ status: "error", response: message });
        scheduleAutoClose("error");
      }
    };
    try {
      this._state.voiceAssistantListening = true;
      micButtons.forEach((btn) => btn.classList.add("listening"));
      this._hapticTap([8, 18, 8]);
      this._toast(this._i18n("ui.voice_assistant_listening"));
      recognition.start();
      this._voiceAssistantRecognitionTimer = setTimeout(() => {
        if (handled) return;
        handled = true;
        recognitionFailed = true;
        stopListeningUi();
        if (this._voiceAssistantRecognition === recognition) this._voiceAssistantRecognition = null;
        try {
          recognition.__homeiiCancelled = true;
          recognition.abort?.();
        } catch {
        }
        const message = this._timeoutMessage(this._flowAssistantLabel());
        this._toastError(message);
        this._updateVoiceAssistantDialog({ status: "error", transcript: capturedTranscript, response: message });
        scheduleAutoClose("error");
      }, this._flowAssistantListenTimeoutMs());
    } catch {
      stopListeningUi();
      if (this._voiceAssistantRecognition === recognition) this._voiceAssistantRecognition = null;
      const message = this._i18n("ui.voice_command_failed");
      this._toastError(message);
      this._updateVoiceAssistantDialog({ status: "error", response: message });
      scheduleAutoClose("error");
    }
  }
  async _handleSmartVoiceTranscript(transcript = "") {
    const query = String(transcript || "").trim();
    if (!query) return;
    this._state.mediaQuery = query;
    const input = this.$("mobileMediaSearchInput");
    if (input) input.value = query;
    this._toast(this._i18n("ui.searching_smart_selection"));
    const results = await this._search(query);
    const candidates = this._normalizeSmartVoiceCandidates(results);
    this._openSmartVoiceConfirm(query, candidates);
  }
  _startMobileVoiceSearch() {
    const SpeechRecognition = this._speechRecognitionCtor();
    const input = this.$("mobileMediaSearchInput");
    const micBtn = this.$("mobileVoiceSearchBtn");
    const micMode = this._mobileMicMode();
    if (!SpeechRecognition) {
      this._toastError(this._i18n("ui.voice_search_is_not_supported_on_this_device"));
      return;
    }
    if (micMode === "off") {
      this._toastError(this._i18n("ui.microphone_is_disabled"));
      return;
    }
    try {
      this._voiceRecognition?.abort?.();
    } catch (_) {
    }
    const recognition = new SpeechRecognition();
    this._voiceRecognition = recognition;
    recognition.lang = this._isHebrew() ? "he-IL" : "en-US";
    recognition.interimResults = true;
    recognition.continuous = false;
    recognition.maxAlternatives = 1;
    micBtn?.classList.add("listening");
    this._hapticTap([8, 18, 8]);
    this._toast(this._i18n("ui.listening"));
    recognition.onresult = (event) => {
      const transcript = Array.from(event.results || []).map((result) => result?.[0]?.transcript || "").join(" ").trim();
      if (!transcript) return;
      const finalized = Array.from(event.results || []).some((result) => result?.isFinal);
      this._state.mediaQuery = transcript;
      if (input) {
        input.value = transcript;
        input.focus({ preventScroll: true });
      }
      if (micMode === "smart" && finalized) {
        this._handleSmartVoiceTranscript(transcript).catch((error) => {
          this._toastError(error?.message || this._i18n("ui.voice_search_failed"));
        });
        return;
      }
      clearTimeout(this._searchTimer);
      this._searchTimer = setTimeout(() => this._renderMobileMediaResults(), 120);
    };
    recognition.onerror = () => {
      this._toastError(this._i18n("ui.voice_search_failed"));
    };
    recognition.onend = () => {
      micBtn?.classList.remove("listening");
      if (this._voiceRecognition === recognition) this._voiceRecognition = null;
    };
    try {
      recognition.start();
    } catch (_) {
      micBtn?.classList.remove("listening");
      this._toastError(this._i18n("ui.voice_search_failed"));
    }
  }
  _mobileSortOptions() {
    return [
      { value: "name_asc", label: `${this._i18n("ui.ascending")} ↑` },
      { value: "name_desc", label: `${this._i18n("ui.descending")} ↓` },
      { value: "date_desc", label: this._i18n("ui.newest") },
      { value: "date_asc", label: this._i18n("ui.oldest") }
    ];
  }
  _itemDateValue(item = {}) {
    const parseDate = (value) => {
      if (value === void 0 || value === null || value === "") return 0;
      if (typeof value === "number") {
        if (!Number.isFinite(value) || value <= 0) return 0;
        if (value >= 1900 && value <= 2100) return Date.UTC(value, 0, 1);
        if (value > 1e12) return value;
        if (value > 1e9) return value * 1e3;
        return 0;
      }
      if (typeof value === "object") return this._itemDateValue(value);
      const raw = String(value || "").trim();
      if (!raw) return 0;
      const yearOnly = raw.match(/^(19|20)\d{2}$/);
      if (yearOnly) return Date.UTC(Number(yearOnly[0]), 0, 1);
      const numeric = Number(raw);
      if (Number.isFinite(numeric) && numeric > 0) return parseDate(numeric);
      const normalized = raw.replace(/^(\d{4}-\d{2}-\d{2})\s+/, "$1T").replace(/(\.\d{3})\d+/, "$1");
      const parsed = Date.parse(normalized);
      if (Number.isFinite(parsed)) return parsed;
      const yearMatch = raw.match(/\b(19|20)\d{2}\b/);
      return yearMatch ? Date.UTC(Number(yearMatch[0]), 0, 1) : 0;
    };
    const releaseCandidates = [
      item.release_date,
      item.releaseDate,
      item.release_year,
      item.releaseYear,
      item.year,
      item.metadata?.release_date,
      item.metadata?.releaseDate,
      item.metadata?.release_year,
      item.metadata?.releaseYear,
      item.metadata?.year,
      item.album?.release_date,
      item.album?.release_year,
      item.album?.year,
      item.media_item?.release_date,
      item.media_item?.release_year,
      item.media_item?.year,
      item.media_item?.metadata?.release_date,
      item.media_item?.metadata?.release_year,
      item.media_item?.metadata?.year
    ];
    const libraryCandidates = [
      item.timestamp_added,
      item.date_added,
      item.added_at,
      item.created_at,
      item.modified_at,
      item.last_modified,
      item.last_updated,
      item.timestamp,
      item.updated_at,
      item.metadata?.timestamp_added,
      item.metadata?.date_added,
      item.metadata?.added_at,
      item.metadata?.created_at,
      item.metadata?.modified_at,
      item.metadata?.last_modified,
      item.metadata?.last_updated,
      item.metadata?.last_refresh,
      item.media_item?.timestamp_added,
      item.media_item?.date_added,
      item.media_item?.added_at,
      item.media_item?.last_modified,
      item.media_item?.last_updated,
      ...Array.isArray(item.provider_mappings) ? item.provider_mappings.map((mapping) => mapping?.details) : []
    ];
    for (const value of [...releaseCandidates, ...libraryCandidates]) {
      const parsed = parseDate(value);
      if (parsed > 0) return parsed;
    }
    return 0;
  }
  _sortLibraryItemsLocally(items = []) {
    const mode = this._state.mobileLibrarySort || "name_asc";
    const copy = [...items];
    if (mode === "name_desc") {
      return copy.sort((a, b) => String(b?.name || "").localeCompare(String(a?.name || ""), this._isHebrew() ? "he" : "en", { sensitivity: "base", numeric: true }));
    }
    if (mode === "date_desc") {
      const ranked = copy.map((item, index) => ({ item, index, date: this._itemDateValue(item) }));
      const withDates = ranked.some((entry) => entry.date > 0);
      if (!withDates) return [...copy];
      ranked.sort((a, b) => {
        if (a.date !== b.date) return b.date - a.date;
        return a.index - b.index;
      });
      return ranked.map((entry) => entry.item);
    }
    if (mode === "date_asc") {
      const ranked = copy.map((item, index) => ({ item, index, date: this._itemDateValue(item) }));
      const withDates = ranked.some((entry) => entry.date > 0);
      if (!withDates) return [...copy].reverse();
      ranked.sort((a, b) => {
        if (a.date !== b.date) return a.date - b.date;
        return b.index - a.index;
      });
      return ranked.map((entry) => entry.item);
    }
    return copy.sort((a, b) => String(a?.name || "").localeCompare(String(b?.name || ""), this._isHebrew() ? "he" : "en", { sensitivity: "base", numeric: true }));
  }
  _libraryTabSearchPageKey(page = this._state.menuPage) {
    const normalized = this._normalizeMobileMenuPage(page || this._state.menuPage || "");
    if (!String(normalized || "").startsWith("library_")) return "";
    if (normalized === "library_search") return "";
    return normalized;
  }
  _libraryTabSearchQuery(page = this._state.menuPage) {
    const key = this._libraryTabSearchPageKey(page);
    if (!key) return "";
    const queries = this._state.libraryTabSearchQueries || {};
    return String(queries[key] || "").trim();
  }
  _libraryTabSearchDraft(page = this._state.menuPage) {
    const key = this._libraryTabSearchPageKey(page);
    if (!key) return "";
    const drafts = this._state.libraryTabSearchDrafts || {};
    if (Object.prototype.hasOwnProperty.call(drafts, key)) return String(drafts[key] || "");
    return this._libraryTabSearchQuery(key);
  }
  _setLibraryTabSearchDraft(query = "", page = this._state.menuPage) {
    const key = this._libraryTabSearchPageKey(page);
    if (!key) return;
    const next = { ...this._state.libraryTabSearchDrafts || {} };
    const value = String(query || "");
    if (value) next[key] = value;
    else delete next[key];
    this._state.libraryTabSearchDrafts = next;
  }
  _setLibraryTabSearchQuery(query = "", page = this._state.menuPage) {
    const key = this._libraryTabSearchPageKey(page);
    if (!key) return;
    const next = { ...this._state.libraryTabSearchQueries || {} };
    const value = String(query || "").trim();
    if (value) next[key] = value;
    else delete next[key];
    this._state.libraryTabSearchQueries = next;
    this._setLibraryTabSearchDraft(value, key);
  }
  _commitLibraryTabSearchQuery(page = this._state.menuPage, query = null) {
    const key = this._libraryTabSearchPageKey(page);
    if (!key) return "";
    const value = query === null ? this._libraryTabSearchDraft(key) : String(query || "");
    this._setLibraryTabSearchQuery(value, key);
    return this._libraryTabSearchQuery(key);
  }
  _restoreLibraryTabSearchFocus() {
    const focusId = String(this._state.libraryTabSearchFocusId || "").trim();
    if (!focusId) return;
    this._state.libraryTabSearchFocusId = "";
    requestAnimationFrame(() => {
      const input = this.$(focusId);
      if (!input) return;
      input.focus?.({ preventScroll: true });
      try {
        const end = String(input.value || "").length;
        input.setSelectionRange?.(end, end);
      } catch (_) {
      }
    });
  }
  _libraryTabSearchPlaceholder(page = this._state.menuPage) {
    const meta = this._libraryTabMeta(page);
    const title = String(meta?.title || this._i18n("ui.media_2") || "").trim();
    return `${this._i18n("ui.search")} ${title}`.trim();
  }
  _libraryTabSearchBoxHtml(mode = "inline") {
    const draft = this._libraryTabSearchDraft();
    const query = this._libraryTabSearchQuery();
    const page = this._libraryTabSearchPageKey();
    const placeholder = this._libraryTabSearchPlaceholder();
    const className = mode === "row" ? "library-tab-search-row" : "library-toolbar-search library-toolbar-search-inline";
    const hasText = !!String(draft || query || "").trim();
    return `
      <div class="${className}">
        <button class="library-tab-search-submit library-tab-search-icon" data-library-tab-search-submit="${this._esc(page)}" title="${this._esc(this._i18n("ui.search"))}" aria-label="${this._esc(this._i18n("ui.search"))}">${this._iconSvg("search")}</button>
        <input id="${mode === "row" ? "mobileLibraryTabSearchRowInput" : "mobileLibraryTabSearchInput"}" data-library-tab-search-input="${this._esc(page)}" type="search" enterkeyhint="search" value="${this._esc(draft)}" placeholder="${this._esc(placeholder)}" aria-label="${this._esc(placeholder)}">
        <button class="library-tab-search-clear ${hasText ? "visible" : ""}" data-library-tab-search-clear="${this._esc(page)}" title="${this._esc(this._i18n("ui.clear_search"))}" aria-label="${this._esc(this._i18n("ui.clear_search"))}">×</button>
      </div>
    `;
  }
  _libraryItemSearchText(item = {}) {
    const media = item?.media_item || {};
    const artistNames = [
      this._artistName(item),
      item.artist,
      item.artist_str,
      media.artist,
      ...Array.isArray(media.artists) ? media.artists.map((artist) => artist?.name) : [],
      ...Array.isArray(item.artists) ? item.artists.map((artist) => artist?.name) : []
    ];
    const albumValue = item.album || media.album || {};
    return [
      item.name,
      item.title,
      media.name,
      media.title,
      ...artistNames,
      typeof albumValue === "string" ? albumValue : albumValue?.name,
      item.publisher,
      item.metadata?.description,
      media.metadata?.description
    ].filter(Boolean).join(" ");
  }
  _filterLibraryItemsByQuery(items = [], query = "") {
    const normalizedQuery = HomeiiMediaQueueFoundation.normalizeComparableText(query);
    if (!normalizedQuery) return Array.isArray(items) ? items : [];
    return (Array.isArray(items) ? items : []).filter((item) => HomeiiMediaQueueFoundation.normalizeComparableText(this._libraryItemSearchText(item)).includes(normalizedQuery));
  }
  _dedupeLibraryItems(items = []) {
    const seen = /* @__PURE__ */ new Set();
    return (Array.isArray(items) ? items : []).filter((item) => {
      const normalized = this._normalizeMediaItem(item);
      const key = String(normalized?.uri || item?.uri || item?.media_item?.uri || "").trim() || `${String(normalized?.media_type || item?.media_type || item?.type || "")}:${String(normalized?.name || item?.name || item?.title || "").trim()}`;
      if (!key || seen.has(key)) return false;
      seen.add(key);
      return true;
    });
  }
  async _getLibraryTabItems(meta = {}, orderBy = "sort_name", limit = 250, query = "") {
    const mediaType = String(meta?.type || "").trim();
    if (!mediaType || mediaType === "liked" || mediaType === "search") return [];
    const search = String(query || "").trim();
    if (!search) return this._getLibrary(mediaType, orderBy, limit);
    const cacheKey = `tab-search:${mediaType}:${orderBy}:${limit}:${search.toLowerCase()}`;
    const ttl = Number(this._config.cache_ttl || 3e5);
    const cached = this._cache.library.get(cacheKey);
    if (cached && Date.now() - cached.ts < ttl) return cached.items;
    let items = [];
    try {
      items = await this._fetchLibrary(mediaType, orderBy, limit, false, search);
    } catch (error) {
      if (this._isMusicAssistantAvailabilityError(error)) {
        this._handleMusicAssistantIssue(error);
      } else if (orderBy !== "sort_name") {
        items = await this._fetchLibrary(mediaType, "sort_name", limit, false, search);
      } else {
        throw error;
      }
    }
    if (!items.length) {
      try {
        const fallbackItems = await this._getLibrary(mediaType, orderBy, limit);
        items = this._filterLibraryItemsByQuery(fallbackItems, search);
      } catch (_) {
      }
    }
    items = this._dedupeLibraryItems(items);
    this._cache.library.set(cacheKey, { ts: Date.now(), items });
    return items;
  }
  _libraryTabSearchResultGroup(mediaType = "") {
    const type = String(mediaType || "").toLowerCase();
    if (type === "podcast") return "podcasts";
    if (type === "album") return "albums";
    if (type === "artist") return "artists";
    if (type === "track") return "tracks";
    if (type === "playlist") return "playlists";
    if (type === "radio") return "radio";
    return "";
  }
  async _searchLibraryTabProviderItems(meta = {}, query = "", limit = 50) {
    const mediaType = String(meta?.type || "").trim();
    const search = String(query || "").trim();
    const group = this._libraryTabSearchResultGroup(mediaType);
    if (!mediaType || !group || !search) return [];
    let results = this._emptySearchResults();
    try {
      results = this._normalizeSearchResponse(await this._callService("search", {
        query: search,
        name: search,
        limit,
        media_type: [mediaType]
      }));
    } catch (_) {
      try {
        results = this._normalizeSearchResponse(await this._callService("search", {
          name: search,
          limit,
          media_type: mediaType
        }));
      } catch (_2) {
      }
    }
    return this._dedupeLibraryItems((Array.isArray(results?.[group]) ? results[group] : []).map((item) => ({
      ...item,
      media_type: item?.media_type || mediaType
    })));
  }
  _libraryItemStableKey(item = {}, fallbackType = "") {
    const normalized = this._normalizeMediaItem({ ...item, media_type: item?.media_type || fallbackType });
    const uri = String(normalized?.uri || item?.uri || item?.media_item?.uri || "").trim();
    if (uri) return `uri:${uri}`;
    const mediaType = String(normalized?.media_type || item?.media_type || fallbackType || "").toLowerCase();
    const name = HomeiiMediaQueueFoundation.normalizeComparableText(normalized?.name || item?.name || item?.title || item?.media_item?.name || "");
    const artist = HomeiiMediaQueueFoundation.normalizeComparableText(this._artistName(item) || item?.artist || item?.artist_str || "");
    return `${mediaType}:${name}:${artist}`;
  }
  _excludeLibraryDuplicateItems(providerItems = [], libraryItems = [], fallbackType = "") {
    const seen = new Set((Array.isArray(libraryItems) ? libraryItems : []).map((item) => this._libraryItemStableKey(item, fallbackType)).filter(Boolean));
    return (Array.isArray(providerItems) ? providerItems : []).filter((item) => {
      const key = this._libraryItemStableKey(item, fallbackType);
      if (!key || seen.has(key)) return false;
      seen.add(key);
      return true;
    });
  }
  _libraryTabSearchResultsHtml(libraryItems = [], providerItems = [], meta = {}, options = {}) {
    const mediaType = String(meta?.type || "track").trim() || "track";
    const providerLoading = options.providerLoading === true;
    const providerError = String(options.providerError || "").trim();
    const hasLibrary = Array.isArray(libraryItems) && libraryItems.length;
    const hasProvider = Array.isArray(providerItems) && providerItems.length;
    if (!hasLibrary && !hasProvider && !providerLoading && !providerError) {
      return `<div class="notice open">${this._i18n("ui.no_results_found")}</div>`;
    }
    return `
      <div class="media-results library-tab-search-results">
        <div>
          <div class="media-section-title">${this._esc(this._m("In library", "בספריה"))}</div>
          ${hasLibrary ? this._mediaItemsListHtml(libraryItems, mediaType, { layout: "grid", librarySkin: true }) : `<div class="notice open">${this._esc(this._m("No local library matches yet.", "אין עדיין תוצאות מהספריה המקומית."))}</div>`}
        </div>
        ${providerLoading || providerError || hasProvider ? `
          <div>
            <div class="media-section-title">${this._esc(this._m("Content providers", "ספקי תוכן"))}</div>
            ${providerLoading ? this._loadingStateHtml(this._i18n("ui.searching"), { notice: true }) : providerError ? `<div class="notice open">${this._esc(providerError)}</div>` : this._mediaItemsListHtml(providerItems, mediaType, { layout: "grid", librarySkin: true })}
          </div>
        ` : ""}
      </div>
    `;
  }
  _mediaLayoutToolbarHtml() {
    const layout = this._state.mobileMediaLayout || this._defaultMobileMediaLayout();
    const sort = this._state.mobileLibrarySort || "name_asc";
    const page = this._libraryTabSearchPageKey();
    const isSearch = this._state.menuPage === "library_search";
    const tabSearchOpen = this._state.libraryTabSearchOpen === true || !!this._libraryTabSearchQuery(page);
    return `
      <div class="media-toolbar library-toolbar library-toolbar-minimal">
        <div class="library-toolbar-player">
          ${this._libraryPlayerFocusHtml()}
        </div>
        ${this._libraryTabSearchBoxHtml("inline")}
        <div class="library-toolbar-actions library-toolbar-icons">
          <button class="media-layout-btn library-toolbar-search-toggle ${tabSearchOpen ? "active" : ""}" data-library-tab-search-toggle="${this._esc(page)}" title="${this._esc(this._i18n("ui.search"))}" aria-label="${this._esc(this._i18n("ui.search"))}" aria-expanded="${tabSearchOpen ? "true" : "false"}">${this._iconSvg("search")}</button>
          <div class="media-layout-toggle" role="tablist" aria-label="${this._esc(this._i18n("ui.media_layout"))}">
            ${!isSearch ? `<button class="media-layout-btn subtle-heart ${this._state.menuPage === "library_liked" ? "active" : ""}" data-menu-nav="library_liked" title="${this._esc(this._i18n("ui.liked"))}">${this._iconSvg("heart_filled")}</button>` : ``}
            ${!isSearch ? `<button class="media-layout-btn" data-media-surprise="1" title="${this._esc(this._i18n("ui.surprise_me"))}">${this._iconSvg("wand")}</button>` : ``}
            <button class="media-layout-btn ${layout === "grid" ? "active" : ""}" data-media-layout="grid" title="${this._esc(this._i18n("ui.grid"))}">${this._iconSvg("grid")}</button>
            <button class="media-layout-btn ${layout === "list" ? "active" : ""}" data-media-layout="list" title="${this._esc(this._i18n("ui.list"))}">${this._iconSvg("list")}</button>
          </div>
        </div>
        <div class="library-toolbar-actions library-toolbar-sort">
          <select class="media-sort-select library-toolbar-sort-select" id="mobileLibrarySortSelect" aria-label="${this._esc(this._i18n("ui.sort"))}">
            ${this._mobileSortOptions().map((opt) => `<option value="${this._esc(opt.value)}" ${opt.value === sort ? "selected" : ""}>${this._esc(opt.label)}</option>`).join("")}
          </select>
        </div>
      </div>
      ${tabSearchOpen ? this._libraryTabSearchBoxHtml("row") : ""}
    `;
  }
  _albumTrackCount(item = {}) {
    const metadata = item?.metadata || item?.media_item?.metadata || {};
    const values = [
      item.track_count,
      item.trackCount,
      item.number_of_tracks,
      item.total_tracks,
      item.item_count,
      item.media_item?.track_count,
      item.media_item?.number_of_tracks,
      item.media_item?.total_tracks,
      metadata.track_count,
      metadata.trackCount,
      metadata.number_of_tracks,
      metadata.total_tracks,
      metadata.item_count,
      Array.isArray(item.tracks) ? item.tracks.length : 0,
      Array.isArray(item.media_item?.tracks) ? item.media_item.tracks.length : 0
    ];
    for (const value of values) {
      const count = Math.round(Number(value || 0));
      if (Number.isFinite(count) && count > 0) return count;
    }
    return 0;
  }
  _albumCardBadgeHtml(item = {}) {
    const kind = this._albumKindLabel(item);
    const trackCount = this._albumTrackCount(item);
    if (!kind && !trackCount) return "";
    return `
      <span class="album-card-badge">
        ${kind ? `<span class="album-card-badge-kind">${this._esc(kind)}</span>` : ``}
        ${trackCount ? `<span class="album-card-badge-count">${this._esc(`${trackCount} ${this._i18n("ui.tracks").toLowerCase()}`)}</span>` : ``}
      </span>
    `;
  }
  _mediaItemsListHtml(items = [], mediaType, options = {}) {
    if (!items.length) return `<div class="notice open">${this._i18n("ui.no_results_found")}</div>`;
    const layout = options.layout || this._state.mobileMediaLayout || this._defaultMobileMediaLayout();
    const iconMap = { track: "tracks", radio: "radio", album: "album", artist: "artist", podcast: "podcast", playlist: "playlist" };
    return `<div class="media-items-list layout-${this._esc(layout)}">${items.map((item) => {
      const entryMediaType = String(item?.media_type || item?.type || mediaType || "album").toLowerCase();
      const canOpenDetails = options.openDetails !== false && this._mediaTypeCanOpenDetails(entryMediaType);
      const art = this._artUrl(item);
      const fallbackIcon = iconMap[entryMediaType] || iconMap[mediaType] || "repeat";
      const thumbHtml = art ? `<span class="menu-thumb" data-img="${this._esc(art)}" data-placeholder="${this._esc(fallbackIcon)}">${this._iconSvg(fallbackIcon)}</span>` : `<span class="menu-thumb">${this._iconSvg(fallbackIcon)}</span>`;
      const artistName2 = this._artistName(item) || "";
      const sub = entryMediaType === "artist" ? this._i18n("ui.artist") : entryMediaType === "radio" ? item.metadata?.description || "" : artistName2 || item.album?.name || item.publisher || "";
      const dataAttrs = `data-media-type="${this._esc(entryMediaType)}" data-media-name="${this._esc(item.name || "")}" data-media-artist="${this._esc(artistName2)}" data-media-album="${this._esc(item.album?.name || "")}" data-media-image="${this._esc(art || "")}"`;
      return `
        <div class="menu-list-item media-entry ${this._esc(layout)} media-type-${this._esc(entryMediaType)}">
          <button class="media-entry-main" ${canOpenDetails ? `data-media-open="${this._esc(item.uri || "")}"` : `data-media-uri="${this._esc(item.uri || "")}"`} ${dataAttrs}>
            ${thumbHtml}
            <span style="min-width:0;flex:1;">
              <span class="menu-item-title">${this._esc(item.name || "")}</span>
              <span class="menu-item-sub">${this._esc(sub || "—")}</span>
            </span>
          </button>
          ${options.albumBadges && entryMediaType === "album" ? this._albumCardBadgeHtml(item) : ``}
          ${item.uri ? `<button class="chip-btn queue-more-btn media-play-btn" data-media-play="${this._esc(item.uri || "")}" ${dataAttrs} title="${this._esc(this._i18n("ui.play"))}" aria-label="${this._esc(this._i18n("ui.play"))}">${this._iconSvg("play")}</button>` : ``}
          <button class="chip-btn queue-more-btn media-more-btn" data-media-more="${this._esc(item.uri || "")}" ${dataAttrs} title="${this._esc(this._i18n("ui.actions_2"))}">${this._iconSvg("more")}</button>
        </div>
      `;
    }).join("")}</div>`;
  }
  _countryFlagEmoji(code = "") {
    return countryFlagEmoji(code);
  }
  _radioBrowserCountriesHtml(countries = []) {
    if (!countries.length) return `<div class="notice open">${this._i18n("ui.no_radio_browser_countries_found")}</div>`;
    return `<div class="media-items-list layout-list radio-country-list">${countries.map((country) => {
      const flag = this._countryFlagEmoji(country.code);
      return `
      <button class="menu-list-item media-entry list radio-country-entry" data-radio-country="${this._esc(country.code)}" data-radio-country-name="${this._esc(country.name)}">
        <span class="media-entry-main">
          <span class="menu-thumb flag-thumb">${flag ? `<span class="flag-emoji">${this._esc(flag)}</span>` : this._iconSvg("radio")}</span>
          <span style="min-width:0;flex:1;">
            <span class="menu-item-title">${this._esc(country.name)}</span>
            <span class="menu-item-sub">${this._esc(`${country.stationcount} ${this._i18n("ui.stations")}`)}</span>
          </span>
        </span>
      </button>
    `;
    }).join("")}</div>`;
  }
  _radioBrowserCountryBackHtml(label = "") {
    return `
      <div class="radio-browser-country-head">
        <button class="media-layout-btn" data-radio-countries-back title="${this._esc(this._i18n("ui.back_to_countries"))}">${this._iconSvg("previous")}</button>
        <div class="media-section-title">${this._esc(label || this._i18n("ui.radio_browser"))}</div>
      </div>
    `;
  }
  _likedMediaEntriesHtml(entries = []) {
    if (!entries.length) return `<div class="notice open">${this._i18n("ui.no_liked_media_yet")}</div>`;
    const layout = "grid";
    const selectedSet = new Set(Array.isArray(this._state.likedSelectedUris) ? this._state.likedSelectedUris : []);
    const playableCount = this._likedPlayableEntries(entries, true).length;
    return `
      <div class="liked-toolbar">
        <div class="liked-toolbar-actions">
          <button class="chip-btn" data-liked-play-all>${this._esc(this._i18n("ui.play_all"))}</button>
          <button class="chip-btn" data-liked-selection-toggle>${this._esc(this._state.likedSelectionMode ? this._i18n("ui.cancel_selection") : this._i18n("ui.select_2"))}</button>
          ${this._state.likedSelectionMode ? `<button class="chip-btn accent" data-liked-play-selected>${this._esc(this._i18n("ui.play_selected"))}${playableCount ? ` (${this._esc(String(playableCount))})` : ``}</button>` : ``}
        </div>
        <div class="media-layout-toggle liked-layout-toggle" role="tablist" aria-label="${this._esc(this._i18n("ui.media_layout"))}">
          <button class="media-layout-btn ${"active"}" data-media-layout="grid" title="${this._esc(this._i18n("ui.grid"))}" aria-pressed="${"true"}">${this._iconSvg("grid")}</button>
          <button class="media-layout-btn ${""}" data-media-layout="list" title="${this._esc(this._i18n("ui.list"))}" aria-pressed="${"false"}">${this._iconSvg("list")}</button>
        </div>
      </div>
      <div class="media-items-list layout-${this._esc(layout)} liked-items-list">${entries.map((entry) => {
      const uri = String(entry?.uri || "").trim();
      const checked = selectedSet.has(uri);
      const entryMediaType = String(entry.media_type || entry.type || "track").toLowerCase();
      const canOpenDetails = this._mediaTypeCanOpenDetails(entryMediaType);
      const entryArt = entry.image || entry.image_url || "";
      const dataAttrs = `data-media-type="${this._esc(entryMediaType)}" data-media-name="${this._esc(entry.name || "")}" data-media-artist="${this._esc(entry.artist || "")}" data-media-album="${this._esc(entry.album || "")}" data-media-image="${this._esc(entryArt)}"`;
      return `
          <div class="menu-list-item media-entry ${this._esc(layout)} liked-entry media-type-${this._esc(entryMediaType)}" ${canOpenDetails ? "" : `data-media-uri="${this._esc(uri)}"`} ${dataAttrs}>
            ${this._state.likedSelectionMode ? `<label class="liked-select-box ${checked ? "checked" : ""}"><input type="checkbox" data-liked-select-uri="${this._esc(uri)}" ${checked ? "checked" : ""}><span></span></label>` : ``}
            <button class="media-entry-main" ${canOpenDetails ? `data-media-open="${this._esc(uri)}"` : `data-media-uri="${this._esc(uri)}"`} ${dataAttrs}>
              <span class="menu-thumb">${entryArt ? `<img src="${this._esc(entryArt)}" alt="">` : this._iconSvg("heart_filled")}</span>
              <span style="min-width:0;flex:1;">
                <span class="menu-item-title">${this._esc(entry.name || "")}</span>
                <span class="menu-item-sub">${this._esc([entry.artist, entry.album].filter(Boolean).join(" · ") || "—")}</span>
              </span>
            </button>
            <div class="media-entry-actions">
              ${uri ? `<button class="chip-btn queue-more-btn media-play-btn" data-media-play="${this._esc(uri)}" ${dataAttrs} title="${this._esc(this._i18n("ui.play"))}" aria-label="${this._esc(this._i18n("ui.play"))}">${this._iconSvg("play")}</button>` : ``}
              <button class="chip-btn queue-more-btn media-more-btn" data-media-more="${this._esc(uri)}" ${dataAttrs} title="${this._esc(this._i18n("ui.actions_2"))}">${this._iconSvg("more")}</button>
            </div>
          </div>
        `;
    }).join("")}</div>
    `;
  }
  _mediaSearchSectionsHtml(results = {}, options = {}) {
    const sections = [
      ["radio", this._i18n("ui.radio"), results.radio || []],
      ["playlists", this._i18n("ui.playlists"), results.playlists || []],
      ["albums", this._i18n("ui.albums"), results.albums || []],
      ["artists", this._i18n("ui.artists"), results.artists || []],
      ["tracks", this._i18n("ui.tracks"), results.tracks || []],
      ["podcasts", this._i18n("ui.podcasts"), results.podcasts || []]
    ];
    const used = sections.filter(([, , items]) => Array.isArray(items) && items.length);
    if (!used.length) return `<div class="notice open">${this._i18n("ui.no_results_found")}</div>`;
    return `<div class="media-results">${used.map(([type, title, items]) => `
      <div>
        <div class="media-section-title">${this._esc(title)}</div>
        ${this._mediaItemsListHtml(items.slice(0, 8), type === "tracks" ? "track" : type === "playlists" ? "playlist" : type === "albums" ? "album" : type === "artists" ? "artist" : type === "podcasts" ? "podcast" : "radio", { layout: "list", openDetails: options.openDetails !== false })}
      </div>
    `).join("")}</div>`;
  }
  _bindMobileMediaSearch() {
    const input = this.$("mobileMediaSearchInput");
    const clearBtn = this.$("mobileMediaSearchClear");
    const voiceBtn = this.$("mobileVoiceSearchBtn");
    if (input && !input.dataset.boundSearch) {
      input.dataset.boundSearch = "1";
      input.addEventListener("input", this._boundMobileMediaInput);
    }
    if (voiceBtn && !voiceBtn.dataset.boundVoice) {
      voiceBtn.dataset.boundVoice = "1";
      voiceBtn.addEventListener("click", (e) => {
        e.preventDefault();
        e.stopPropagation();
        this._startMobileVoiceSearch();
      });
    }
    if (clearBtn && !clearBtn.dataset.boundClear) {
      clearBtn.dataset.boundClear = "1";
      clearBtn.addEventListener("click", (e) => {
        e.preventDefault();
        e.stopPropagation();
        this._state.mediaQuery = "";
        if (input) {
          input.value = "";
          input.focus({ preventScroll: true });
        }
        this._renderMobileMediaResults();
      });
    }
  }
  async _renderMobileMediaResults() {
    if (!this._state.menuOpen || !this._isMobileSearchPage()) return;
    const body = this.$("mobileMenuBody");
    const resultsHost = body?.querySelector("#mobileMediaSearchResults");
    const clearBtn = this.$("mobileMediaSearchClear");
    if (!resultsHost) return;
    const query = (this._state.mediaQuery || "").trim();
    const token = ++this._state.mediaSearchToken;
    const isCurrentSearch = () => this._state.menuOpen && this._isMobileSearchPage() && token === this._state.mediaSearchToken && (this._state.mediaQuery || "").trim() === query;
    if (clearBtn) clearBtn.style.display = query ? "" : "none";
    if (clearBtn) clearBtn.classList.toggle("visible", !!query);
    if (!query) {
      resultsHost.innerHTML = this._state.menuPage === "quick_search" ? this._quickSearchRecommendationsHtml() : "";
      if (this._state.menuPage === "quick_search") this._hydrateImages(body);
      return;
    }
    const cachedResults = this._cachedLibrarySearchResults(query, 10);
    const searchRenderOptions = { openDetails: this._state.menuPage !== "quick_search" };
    let hasInterimResults = this._hasSearchResults(cachedResults);
    resultsHost.innerHTML = hasInterimResults ? this._mediaSearchSectionsHtml(cachedResults, searchRenderOptions) : this._loadingStateHtml(this._i18n("ui.searching"), { notice: true });
    if (hasInterimResults) this._hydrateImages(body);
    if (!hasInterimResults) {
      this._withTimeout(this._searchPreviewResults(query, 8), 1800, "Preview search timed out").then((previewResults) => {
        if (!isCurrentSearch() || !this._hasSearchResults(previewResults)) return;
        hasInterimResults = true;
        resultsHost.innerHTML = this._mediaSearchSectionsHtml(previewResults, searchRenderOptions);
        this._hydrateImages(body);
      }).catch(() => {
      });
    }
    let results;
    try {
      results = await this._search(query);
    } catch (_) {
      if (!isCurrentSearch()) return;
      if (hasInterimResults) return;
      resultsHost.innerHTML = `<div class="notice open">${this._i18n("ui.search_failed")}</div>`;
      return;
    }
    if (!isCurrentSearch()) return;
    if (!this._hasSearchResults(results) && hasInterimResults) return;
    resultsHost.innerHTML = this._mediaSearchSectionsHtml(results, searchRenderOptions);
    this._hydrateImages(body);
  }
  _announcementsMenuHtml() {
    const text = this._state.mobileAnnouncementText || "";
    const presets = (this._state.mobileAnnouncementPresets || []).slice(0, 3);
    const targetValue = this._announcementTargetValue();
    const announcementVolume = this._announcementVolumePct();
    const announcementLanguage = this._announcementLanguageSetting();
    const targetOptions = [
      ["all", this._i18n("ui.announce_to_all_players")],
      ...this._announcementEligiblePlayers().map((player) => [player.entity_id, player.attributes?.friendly_name || player.entity_id])
    ];
    const languageOptions = this._announcementLanguageOptions();
    return `
      <div class="announcements-shell">
        <div class="announcement-target">
          <span class="announcement-target-icon">${this._iconSvg("speaker")}</span>
          <select class="media-sort-select announcement-target-select" id="mobileAnnouncementTargetSelect" aria-label="${this._esc(this._i18n("ui.announcement_target"))}">
            ${targetOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === targetValue ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
          </select>
        </div>
        <div class="announcement-target announcement-language-target">
          <span class="announcement-target-icon">${this._iconSvg("announcement")}</span>
          <select class="media-sort-select announcement-target-select" id="mobileAnnouncementTtsLanguageSelect" aria-label="${this._esc(this._i18n("ui.announcement_language"))}">
            ${languageOptions.map(([value, label]) => `<option value="${this._esc(value)}" ${value === announcementLanguage ? "selected" : ""}>${this._esc(label)}</option>`).join("")}
          </select>
        </div>
        <div class="announcement-input-wrap">
          <textarea id="mobileAnnouncementText" class="announcement-textarea" rows="4" placeholder="${this._esc(this._i18n("ui.type_an_announcement"))}">${this._esc(text)}</textarea>
          <button class="announcement-voice-btn" data-announcement-voice title="${this._esc(this._i18n("ui.dictate"))}">${this._iconSvg("mic")}</button>
        </div>
        <div class="announcement-presets">
          ${presets.map((preset, index) => preset ? `
            <button class="settings-pill" data-announcement-preset-fill="${this._esc(index)}">${this._esc(preset)}</button>
          ` : ``).join("")}
        </div>
        <div class="settings-range announcement-volume-field">
          <div class="settings-label">${this._esc(this._i18n("ui.announcement_volume_boost"))}</div>
          <input id="mobileAnnouncementVolumeInput" type="range" min="20" max="50" step="1" value="${this._esc(String(announcementVolume))}">
          <div class="settings-value">+${this._esc(String(announcementVolume))}%</div>
        </div>
        <button class="action-btn announcement-send-btn" data-announcement-send>
          ${this._iconSvg("announcement")}
          <span>${this._esc(this._i18n("ui.announce"))}</span>
        </button>
      </div>
    `;
  }
  _announcementTtsEntity() {
    const explicit = String(this._state.mobileAnnouncementTtsEntity || this._config?.announcement_tts_entity || "").trim();
    if (explicit) return explicit;
    const ttsEntity = Object.keys(this._hass?.states || {}).find((entityId) => entityId.startsWith("tts."));
    return String(ttsEntity || "").trim();
  }
  _announcementLanguageOptions() {
    return [
      ["auto", this._i18n("ui.auto_cloud_default")],
      ["en-US", "English (US)"],
      ["en-GB", "English (UK)"],
      ["he-IL", this._i18n("ui.hebrew")],
      ["de-DE", "Deutsch"],
      ["fr-FR", "Français"],
      ["es-ES", "Español"],
      ["it-IT", "Italiano"]
    ];
  }
  _normalizeAnnouncementLanguage(value = "") {
    const normalized = String(value || "auto").trim();
    const allowed = new Set(this._announcementLanguageOptions().map(([option]) => option));
    return allowed.has(normalized) ? normalized : "auto";
  }
  _announcementLanguageSetting() {
    return this._normalizeAnnouncementLanguage(this._state.mobileAnnouncementTtsLanguage || this._config?.announcement_tts_language || "auto");
  }
  _announcementLanguageCode(text = "") {
    const configured = this._announcementLanguageSetting();
    if (configured !== "auto") return configured;
    return /[\u0590-\u05FF]/.test(String(text || "")) ? "he-IL" : "";
  }
  _announcementRecognitionLanguageCode() {
    const configured = this._announcementLanguageSetting();
    if (configured !== "auto") return configured;
    try {
      const browserLanguage = typeof navigator !== "undefined" ? navigator.language : "";
      const lang = this._hass?.locale?.language || this._hass?.language || browserLanguage || "";
      if (lang) return String(lang);
    } catch (_) {
    }
    return this._isHebrew() ? "he-IL" : "en-US";
  }
  _announcementPayloadWithLanguage(payload = {}, language = "") {
    const next = { ...payload };
    if (language) next.language = language;
    else delete next.language;
    if (next.options && !Object.keys(next.options).length) delete next.options;
    return next;
  }
  _preferredAnnouncementSayService(message = "") {
    const services = Object.keys(this._hass?.services?.tts || {});
    const hasHebrew = /[\u0590-\u05FF]/.test(String(message || ""));
    if (hasHebrew && services.includes("google_translate_say")) return "google_translate_say";
    return services.find((service) => service === "google_translate_say" || service.endsWith("_say")) || "";
  }
  _prepareAnnouncementVolumes(targets = []) {
    const boost = this._announcementVolumePct() / 100;
    return (Array.isArray(targets) ? targets : []).map((player) => {
      const entityId = String(player?.entity_id || "").trim();
      const previousVolume = this._playerVolumeLevel(entityId);
      return {
        entityId,
        previousVolume,
        targetVolume: Number.isFinite(previousVolume) ? Math.max(0, Math.min(1, previousVolume + boost)) : boost,
        targetVolumePct: Math.round((Number.isFinite(previousVolume) ? Math.max(0, Math.min(1, previousVolume + boost)) : boost) * 100)
      };
    }).filter((snapshot) => snapshot.entityId);
  }
  _announcementVolumeSnapshot(playerOrEntityId, snapshots = []) {
    const entityId = String(playerOrEntityId?.entity_id || playerOrEntityId || "").trim();
    return snapshots.find((item) => item.entityId === entityId) || null;
  }
  _announcementTargetVolumePct(playerOrEntityId, snapshots = []) {
    const snapshot = this._announcementVolumeSnapshot(playerOrEntityId, snapshots);
    const value = Number(snapshot?.targetVolumePct);
    return Number.isFinite(value) ? Math.max(0, Math.min(100, Math.round(value))) : null;
  }
  _announcementTtsOptions(player, snapshots = [], language = "") {
    const options = {};
    if (language) options.language = language;
    const targetVolumePct = this._announcementTargetVolumePct(player, snapshots);
    if (Number.isFinite(targetVolumePct)) options.announce_volume = targetVolumePct;
    return options;
  }
  async _callMusicAssistantAnnouncement(player, url, snapshots = []) {
    const targetVolumePct = this._announcementTargetVolumePct(player, snapshots);
    const payload = {
      entity_id: player.entity_id,
      url
    };
    if (Number.isFinite(targetVolumePct)) payload.announce_volume = targetVolumePct;
    try {
      await this._callHaServiceTargeted("music_assistant", "play_announcement", payload, { entity_id: player.entity_id });
    } catch (error) {
      if (!Number.isFinite(targetVolumePct)) throw error;
      await this._callHaServiceTargeted("music_assistant", "play_announcement", {
        entity_id: player.entity_id,
        url
      }, { entity_id: player.entity_id });
    }
  }
  _announcementRestoreDelayMs(message = "") {
    const textLength = String(message || "").trim().length;
    return Math.max(5e3, Math.min(22e3, 3200 + textLength * 90));
  }
  _scheduleAnnouncementVolumeRestore(snapshots = [], delayMs = 0) {
    this._announcementVolumeRestoreTimers = this._announcementVolumeRestoreTimers || /* @__PURE__ */ new Map();
    snapshots.forEach((snapshot) => {
      if (!snapshot?.entityId || !Number.isFinite(snapshot.previousVolume)) return;
      const existing = this._announcementVolumeRestoreTimers.get(snapshot.entityId);
      if (Array.isArray(existing)) existing.forEach((timer) => clearTimeout(timer));
      else if (existing) clearTimeout(existing);
      const baseDelay = Math.max(0, Number(delayMs) || 0);
      const delays = [baseDelay, baseDelay + 4500, baseDelay + 9e3];
      const timers = delays.map((delay, index) => setTimeout(() => {
        this._setPlayerVolumeForAnnouncement(snapshot.entityId, snapshot.previousVolume).catch(() => {
        });
        if (index === delays.length - 1) this._announcementVolumeRestoreTimers.delete(snapshot.entityId);
      }, delay));
      this._announcementVolumeRestoreTimers.set(snapshot.entityId, timers);
    });
  }
  async _sendMobileAnnouncement() {
    const message = String(this._state.mobileAnnouncementText || "").trim();
    const targetValue = this._announcementTargetValue();
    const eligiblePlayers = this._announcementEligiblePlayers();
    const targets = targetValue === "all" ? eligiblePlayers : eligiblePlayers.filter((player) => player.entity_id === targetValue);
    const isHebrewMessage = /[\u0590-\u05FF]/.test(message);
    if (!message) {
      this._toastError(this._i18n("ui.enter_an_announcement_first"));
      return;
    }
    if (!targets.length) {
      this._toastError(this._i18n("ui.select_a_player_first"));
      return;
    }
    this._hapticTap([12, 24, 12]);
    const playerName = targetValue === "all" ? this._i18n("ui.all_players_2") : targets[0]?.attributes?.friendly_name || targets[0]?.entity_id || this._selectedPlayerName();
    const preview = message.length > 72 ? `${message.slice(0, 69)}...` : message;
    const language = this._announcementLanguageCode(message);
    this._toast(this._i18n("ui.announcement_to_player_preview", {
      player: playerName,
      preview
    }));
    const volumeSnapshots = this._prepareAnnouncementVolumes(targets);
    let sentAny = false;
    try {
      const sayService = this._preferredAnnouncementSayService(message);
      const ttsEntity = this._announcementTtsEntity();
      for (const player of targets) {
        if (/^https?:\/\//i.test(message)) {
          await this._callMusicAssistantAnnouncement(player, message, volumeSnapshots);
          sentAny = true;
          continue;
        }
        const trySpeak = async () => {
          const ttsOptions = this._announcementTtsOptions(player, volumeSnapshots, language);
          const primarySpeakPayload = this._announcementPayloadWithLanguage({
            media_player_entity_id: player.entity_id,
            message,
            cache: false,
            options: ttsOptions
          }, language);
          const speakAttempts = /[\u0590-\u05FF]/.test(message) ? [
            primarySpeakPayload,
            { media_player_entity_id: player.entity_id, message, cache: false }
          ] : [
            primarySpeakPayload,
            { media_player_entity_id: player.entity_id, message, cache: false }
          ];
          let speakError = null;
          for (const payload of speakAttempts) {
            try {
              await this._callHaServiceTargeted("tts", "speak", payload, { entity_id: ttsEntity });
              return true;
            } catch (error) {
              speakError = error;
            }
          }
          if (speakError) throw speakError;
          return false;
        };
        if (isHebrewMessage && ttsEntity && this._hasService("tts", "speak")) {
          await trySpeak();
          sentAny = true;
          continue;
        }
        if (sayService) {
          const primarySayPayload = this._announcementPayloadWithLanguage({
            entity_id: player.entity_id,
            message,
            cache: false
          }, language);
          const sayAttempts = isHebrewMessage ? [
            primarySayPayload,
            { entity_id: player.entity_id, message, cache: false }
          ] : [
            primarySayPayload,
            { entity_id: player.entity_id, message, cache: false }
          ];
          let sayError = null;
          for (const payload of sayAttempts) {
            try {
              await this._callHaServiceTargeted("tts", sayService, payload, { entity_id: player.entity_id });
              sayError = null;
              break;
            } catch (error) {
              sayError = error;
            }
          }
          if (!sayError) {
            sentAny = true;
            continue;
          }
          if (ttsEntity && this._hasService("tts", "speak")) {
            await trySpeak();
            sentAny = true;
            continue;
          }
          throw sayError;
        } else if (ttsEntity && this._hasService("tts", "speak")) {
          await trySpeak();
          sentAny = true;
        } else {
          throw new Error(this._i18n("ui.no_tts_service_or_entity_is_configured"));
        }
      }
      this._toastSuccess(this._i18n("ui.announcement_sent_to_player", { player: playerName }));
    } catch (error) {
      this._toastError(this._i18n("ui.announcement_failed_with_error", {
        error: error?.message ? `: ${error.message}` : ""
      }));
    } finally {
      this._scheduleAnnouncementVolumeRestore(volumeSnapshots, sentAny ? this._announcementRestoreDelayMs(message) : 0);
    }
  }
  _startMobileAnnouncementVoice() {
    const SpeechRecognition = this._speechRecognitionCtor();
    const input = this.$("mobileAnnouncementText");
    if (!SpeechRecognition) {
      this._toastError(this._i18n("ui.voice_input_is_not_supported_on_this_device"));
      return;
    }
    try {
      this._voiceRecognition?.abort?.();
    } catch (_) {
    }
    const recognition = new SpeechRecognition();
    this._voiceRecognition = recognition;
    recognition.lang = this._announcementRecognitionLanguageCode();
    recognition.interimResults = true;
    recognition.continuous = false;
    recognition.maxAlternatives = 1;
    let capturedTranscript = false;
    let recognitionFailed = false;
    this._toast(this._i18n("ui.listening"));
    recognition.onresult = (event) => {
      const transcript = Array.from(event.results || []).map((result) => result?.[0]?.transcript || "").join(" ").trim();
      if (!transcript) return;
      capturedTranscript = true;
      this._state.mobileAnnouncementText = transcript;
      if (input) {
        input.value = transcript;
        input.focus({ preventScroll: true });
      }
    };
    recognition.onnomatch = () => {
      recognitionFailed = true;
      this._toastError(this._i18n("ui.no_speech_was_captured"));
    };
    recognition.onerror = () => {
      recognitionFailed = true;
      this._toastError(this._i18n("ui.voice_input_failed"));
    };
    recognition.onend = () => {
      if (!capturedTranscript && !recognitionFailed) this._toastError(this._i18n("ui.no_speech_was_captured"));
      if (this._voiceRecognition === recognition) this._voiceRecognition = null;
    };
    try {
      recognition.start();
    } catch (_) {
      this._toastError(this._i18n("ui.voice_input_failed"));
    }
  }
  _groupMenuHtml() {
    const players = this._getAvailableGroupPlayers();
    if (!players.length) return `<div class="notice open">${this._i18n("ui.no_extra_ma_players_2")}</div>`;
    const selected = this._getSelectedPlayer();
    const connectedNames = this._playerGroupMemberNames(selected);
    const groupCount = this._playerGroupCount(selected);
    const groupVol = this._groupAverageVolume(selected);
    const connectedRow = connectedNames.length > 1 ? `<div class="group-connected-row"><span>${this._esc(this._i18n("ui.connected_2"))}</span><strong>${this._esc(connectedNames.join(" · "))}</strong></div>` : "";
    return `
      ${connectedRow}
      ${groupCount ? `
        <div class="group-volume-card">
          <div class="group-volume-title">${this._esc(this._i18n("ui.group_volume"))}<span>${this._esc(String(groupCount))}</span></div>
          <div class="player-volume-row">
            <button class="player-mini-mute ${this._isGroupMuted(selected) ? "active" : ""}" data-group-mute="${this._esc(selected?.entity_id || "")}" title="${this._esc(this._i18n("ui.mute"))}">${this._iconSvg(this._isGroupMuted(selected) ? "volume_mute" : this._volumeIconName(selected))}</button>
            <input class="player-mini-volume" data-group-volume="${this._esc(selected?.entity_id || "")}" type="range" min="0" max="100" value="${groupVol}" style="--vol-pct:${groupVol}%">
          </div>
        </div>
      ` : ``}
      <div class="players-premium-grid">
      ${players.map((p) => {
      const checked = (this._state.pendingGroupSelections || []).includes(p.entity_id);
      const playerGroupCount2 = this._playerGroupCount(p);
      const vol = Math.round((p.attributes?.volume_level || 0) * 100);
      const art = this._playerArtworkUrl(p, 180);
      const activePlayback = p?.state === "playing";
      const track = p.attributes?.media_title || p.attributes?.media_artist || "";
      return `
          <div class="group-player-card ${checked ? "checked" : ""}">
            <label class="group-player-row player-premium-head ${checked ? "checked" : ""}">
              <span class="group-player-toggle ${checked ? "checked" : ""}" aria-hidden="true">${this._iconSvg(checked ? "check" : "plus")}</span>
              <span class="player-premium-art">
                ${art ? this._imgHtml(art, "", { loading: "lazy", fetchpriority: "low" }) : this._iconSvg("speaker")}
                ${playerGroupCount2 ? `<span class="player-group-badge">${this._esc(playerGroupCount2)}</span>` : ``}
              </span>
              <span class="player-premium-copy">
                <span class="player-premium-title-row group-player-title-row">
                  <span class="player-premium-name">${this._esc(p.attributes?.friendly_name || p.entity_id)}</span>
                  <span class="player-premium-bars eq-icon ${activePlayback ? "is-active" : "is-static"}" aria-label="${this._esc(activePlayback ? this._i18n("ui.playing") : this._i18n("ui.idle"))}"><span></span><span></span><span></span></span>
                </span>
                ${track ? `<span class="player-premium-track">${this._esc(track)}</span>` : ``}
              </span>
              <input class="group-player-check" type="checkbox" data-menu-group-player="${this._esc(p.entity_id)}" ${checked ? "checked" : ""}>
            </label>
            <div class="player-volume-row group-inline-volume">
              <button class="player-mini-mute ${this._isMuted(p) ? "active" : ""}" data-player-mute="${this._esc(p.entity_id)}" title="${this._esc(this._i18n("ui.mute"))}">${this._iconSvg(this._volumeIconName(p))}</button>
              <input class="player-mini-volume" data-player-volume="${this._esc(p.entity_id)}" type="range" min="0" max="100" value="${vol}" style="--vol-pct:${vol}%">
            </div>
          </div>
        `;
    }).join("")}
      </div>
      <div class="group-actions">
        <button class="action-btn" data-menu-action="apply_group">${this._i18n("ui.apply_group")}</button>
        <button class="action-btn warn" data-menu-action="clear_group">${this._i18n("ui.ungroup")}</button>
      </div>
    `;
  }
  _queueMenuHtml() {
    const queueItems = this._getNowPlayingQueueItems();
    if (!queueItems.length) return `<div class="notice open">${this._i18n("ui.queue_is_empty")}</div>`;
    this._prefetchQueueArtworkWindow(queueItems, { immediate: true, before: 2, after: 18, visibleStartIndex: 0, visibleCount: 32 });
    return `<div class="queue-list">${queueItems.map((item, index) => {
      const key = this._getQueueItemKey(item);
      const img = this._queueItemImageUrl(item, 120);
      const artist = item.media_item?.artists?.map((a) => a.name).join(", ") || "";
      const current = this._isQueueItemCurrent(item);
      const media = item.media_item || {};
      const expanded = String(this._state.expandedQueueItemId || "") === String(key || "");
      const displayPosition = index + 1;
      const queueLead = this._esc(String(displayPosition));
      const eager = current || index < 24;
      return `
        <div class="queue-row ${current ? "active" : ""} ${expanded ? "expanded" : ""}" data-queue-item-id="${this._esc(key)}" data-uri="${this._esc(item.media_item?.uri || "")}" data-type="track" data-sort-index="${this._esc(item.sort_index ?? "")}" data-queue-position="${this._esc(String(displayPosition))}">
          <div class="queue-index">${queueLead}</div>
          <div class="menu-thumb">${img ? this._imgHtml(img, "", { loading: eager ? "eager" : "lazy", fetchpriority: eager ? "high" : "low" }) : this._iconSvg("music_note")}</div>
          <div class="queue-meta">
            <div class="queue-title">${this._esc(item.media_item?.name || item.name || "")}</div>
            <div class="queue-sub">${this._esc(artist)}</div>
          </div>
          <div class="queue-actions">
            <button class="chip-btn queue-more-btn" data-queue-menu="${this._esc(key)}" data-queue-uri="${this._esc(media.uri || "")}" data-queue-sort-index="${this._esc(item.sort_index ?? "")}" data-queue-position="${this._esc(String(displayPosition))}" data-queue-name="${this._esc(media.name || item.name || "")}" data-queue-artist="${this._esc(artist)}" data-queue-album="${this._esc(media.album?.name || "")}" data-queue-image="${this._esc(img || media.image || media.album?.image || "")}" title="${this._esc(this._i18n("ui.actions_2"))}" aria-expanded="${expanded ? "true" : "false"}">${this._iconSvg("more")}</button>
          </div>
          ${this._renderQueueInlineActions(item, displayPosition, queueItems.length)}
        </div>
      `;
    }).join("")}</div>`;
  }
  _queueLikeEntryForItem(item = {}) {
    const media = item?.media_item || {};
    const artist = Array.isArray(media?.artists) ? media.artists.map((entry) => entry?.name).filter(Boolean).join(", ") : "";
    const image = this._queueItemImageUrl(item, 120) || media.image || media.image_url || media.album?.image || media.album?.image_url || "";
    return {
      uri: media.uri || item?.uri || "",
      media_type: media.media_type || item?.media_type || "track",
      name: media.name || item?.name || "",
      artist,
      album: media.album?.name || item?.album || "",
      image
    };
  }
  _queueLikeActionButtonHtml(item = {}, key = "") {
    const itemKey = key || this._getQueueItemKey(item);
    const likeEntry = this._queueLikeEntryForItem(item);
    if (!String(likeEntry.uri || "").trim()) return "";
    const liked = this._isEntryLiked(likeEntry);
    return `<button class="chip-btn icon-only queue-inline-like like-action ${liked ? "active" : ""}" data-queue-action="like" data-queue-item-id="${this._esc(itemKey)}" data-queue-uri="${this._esc(likeEntry.uri)}" data-queue-type="${this._esc(likeEntry.media_type)}" data-queue-name="${this._esc(likeEntry.name)}" data-queue-artist="${this._esc(likeEntry.artist)}" data-queue-album="${this._esc(likeEntry.album)}" data-queue-image="${this._esc(likeEntry.image)}" title="${this._esc(this._i18n("ui.like_2"))}" aria-pressed="${liked ? "true" : "false"}" data-queue-liked="${liked ? "true" : "false"}">${this._iconSvg(liked ? "heart_filled" : "heart_outline")}</button>`;
  }
  _renderQueueInlineActions(item, displayPosition = null, queueCountOverride = null) {
    const key = this._getQueueItemKey(item);
    const queueCount = Math.max(1, Math.round(Number(queueCountOverride)) || this._getNowPlayingQueueItems().length || (this._state.queueItems || []).length || Number(this._state.maQueueState?.items || 1));
    const currentPosition = Math.max(1, Math.min(queueCount, Math.round(Number(displayPosition)) || this._queueDisplayPositionForEntry(item, Math.round(Number(item?.sort_index || 0)) + 1 || 1)));
    const likeActionHtml = this._queueLikeActionButtonHtml(item, key);
    return `
      <div class="queue-inline-actions">
        <button class="chip-btn primary text-action" data-queue-action="next" data-queue-item-id="${this._esc(key)}"><span>${this._esc(this._i18n("ui.play_next"))}</span></button>
        <button class="chip-btn secondary text-action" data-queue-action="play" data-queue-item-id="${this._esc(key)}" title="${this._esc(this._i18n("ui.play_now"))}"><span>${this._esc(this._i18n("ui.play_now"))}</span></button>
        ${likeActionHtml}
        <label class="queue-inline-move">
          ${this._queueMoveSelectHtml(queueCount, currentPosition, { ...item, key, position: currentPosition })}
        </label>
        <button class="chip-btn warn icon-only" data-queue-action="remove" data-queue-item-id="${this._esc(key)}" title="${this._esc(this._i18n("ui.remove"))}">${this._iconSvg("trash")}</button>
      </div>`;
  }
  _setQueueInlineActionsExpanded(key = "") {
    const nextKey = String(key || "");
    this._state.expandedQueueItemId = nextKey;
    const body = this.$("mobileMenuBody");
    if (!body) return;
    body.querySelectorAll(".queue-row[data-queue-item-id]").forEach((row) => {
      const expanded = nextKey && String(row.dataset.queueItemId || "") === nextKey;
      row.classList.toggle("expanded", !!expanded);
      row.querySelector("[data-queue-menu]")?.setAttribute("aria-expanded", expanded ? "true" : "false");
    });
  }
  async _renderMobileMenu() {
    const body = this.$("mobileMenuBody");
    const title = this.$("mobileMenuTitle");
    const back = this.$("mobileMenuBackBtn");
    const aux = this.$("mobileMenuAuxBtn");
    const close = this.$("mobileMenuCloseBtn");
    if (!body || !title || !back || !aux || !close) return;
    const page = this._normalizeMobileMenuPage(this._state.menuPage || "main");
    if (page !== this._state.menuPage) this._state.menuPage = page;
    if (page === "sleep_timer" && this._isScheduleFormEditing()) return;
    const renderToken = this._mobileMenuRenderToken = (this._mobileMenuRenderToken || 0) + 1;
    const isCurrentRender = () => renderToken === this._mobileMenuRenderToken && this._state.menuOpen && this._state.menuPage === page;
    const previousRenderedPage = body.dataset.menuPage || "";
    const scrollSnapshot = previousRenderedPage === page ? this._captureMobileMenuScroll(page) : null;
    const finishMenuRender = () => {
      if (!isCurrentRender()) return false;
      body.dataset.menuPage = page;
      if (scrollSnapshot) this._restoreMobileMenuScrollSnapshot(scrollSnapshot, page);
      this._hydrateImages(body);
      return true;
    };
    const hasBackTarget = Array.isArray(this._state.menuStack) && this._state.menuStack.length > 0;
    const currentDetailType = page === "media_detail" ? this._libraryDetailMediaType(this._state.mobileLibraryDetail || {}) : "";
    const isArtistDetailPage = page === "media_detail" && currentDetailType === "artist";
    const isLibraryDetailPage = page === "media_detail";
    const isSearchPage = this._isMobileSearchPage(page);
    const isLibraryPage = page.startsWith("library_") || isLibraryDetailPage;
    const isTabletLibraryPage = (page.startsWith("library_") || isArtistDetailPage) && this._layoutModeConfig() === "tablet";
    const isPhoneFullscreenPage = this._isPhoneActionFullscreenMenuPage(page) || isArtistDetailPage;
    back.hidden = !(hasBackTarget || page === "media_detail" && Array.isArray(this._state.mobileLibraryDetailStack) && this._state.mobileLibraryDetailStack.length);
    aux.hidden = true;
    close.hidden = false;
    body.classList.toggle("library-mode", isLibraryPage);
    body.classList.toggle("search-mode", isSearchPage);
    this.$("mobileMenu")?.classList.toggle("search-open", isSearchPage);
    this.$("mobileMenu")?.classList.toggle("discovery-open", page === "discovery");
    this.$("mobileMenu")?.classList.toggle("action-fullscreen-open", isPhoneFullscreenPage);
    this.$("mobileMenu")?.classList.toggle("library-fullscreen-open", isTabletLibraryPage);
    this.$("homeShortcutFab")?.toggleAttribute("hidden", page === "discovery" || isTabletLibraryPage);
    this._syncCompactMenuOverlayState();
    const menu = this.$("mobileMenu");
    const sheet = menu?.querySelector(".menu-sheet");
    const detail = page === "media_detail" ? this._state.mobileLibraryDetail || {} : null;
    const detailArt = detail ? this._libraryDetailArtworkUrl(detail, 960) : "";
    const menuArt = detailArt || this._currentArtworkUrl(this._getSelectedPlayer(), this._state.maQueueState?.current_item || null, 720);
    if (menu) {
      if (menuArt) {
        menu.style.setProperty("--menu-dynamic-art", `url(${JSON.stringify(menuArt)})`);
        menu.classList.add("has-menu-art");
      } else {
        menu.style.removeProperty("--menu-dynamic-art");
        menu.classList.remove("has-menu-art");
      }
      if (detail) this._applyMenuDetailTheme(menu, detailArt, detail);
      else this._clearMenuDetailTheme(menu);
    }
    const sheetClasses = [
      "sheet-actions",
      "sheet-simple",
      "sheet-players",
      "sheet-queue",
      "sheet-library",
      "sheet-media-detail",
      "sheet-artist-detail",
      "sheet-search",
      "sheet-group",
      "sheet-transfer",
      "sheet-announcements",
      "sheet-settings",
      "sheet-schedules",
      "sheet-discovery"
    ];
    body.classList.remove(...sheetClasses);
    sheet?.classList.remove(...sheetClasses);
    const sheetClass = page === "main" ? "sheet-actions" : page === "discovery" ? "sheet-discovery" : page === "simple_wizard" ? "sheet-simple" : page === "players" || page === "players_active" ? "sheet-players" : page === "queue" ? "sheet-queue" : page === "media_detail" ? isArtistDetailPage ? "sheet-artist-detail" : "sheet-media-detail" : isSearchPage ? "sheet-search" : isLibraryPage ? "sheet-library" : page === "group" ? "sheet-group" : page === "transfer" ? "sheet-transfer" : page === "announcements" ? "sheet-announcements" : page === "settings" ? "sheet-settings" : page === "sleep_timer" ? "sheet-schedules" : "";
    if (sheetClass) {
      body.classList.add(sheetClass);
      sheet?.classList.add(sheetClass);
    }
    if (page === "main") {
      this._setMobileMenuHeader(this._i18n("ui.actions_2"), this._menuPageIcon(page));
      body.innerHTML = this._mainMenuHtml();
      finishMenuRender();
      return;
    }
    if (page === "simple_wizard") {
      this._setMobileMenuHeader("FLOW", this._menuPageIcon(page));
      body.innerHTML = this._simpleWizardHtml();
      finishMenuRender();
      return;
    }
    if (page === "discovery") {
      this._setMobileMenuHeader(this._i18n("ui.discover_music", {}, this._m("Discover", "גלה מוזיקה")), this._menuPageIcon(page));
      body.innerHTML = this._loadingStateHtml(this._i18n("ui.loading"), { notice: true });
      const discovery = await this._loadDiscoverySections();
      if (!isCurrentRender()) return;
      body.innerHTML = this._discoveryMenuHtml(discovery);
      finishMenuRender();
      return;
    }
    if (page === "quick_search") {
      this._setMobileMenuHeader(this._i18n("ui.search"), this._menuPageIcon(page));
      body.innerHTML = this._quickSearchShellHtml();
      this._bindMobileMediaSearch();
      await this._renderMobileMediaResults();
      if (!isCurrentRender()) return;
      finishMenuRender();
      return;
    }
    if (page === "settings") {
      this._setMobileMenuHeader(this._i18n("ui.settings"), this._menuPageIcon(page));
      body.innerHTML = this._settingsMenuHtml();
      finishMenuRender();
      return;
    }
    if (page === "media_detail") {
      const detail2 = this._state.mobileLibraryDetail || {};
      const mediaType = String(detail2.media_type || "").toLowerCase();
      const iconName = mediaType === "playlist" ? "playlist" : mediaType === "artist" ? "artist" : "album";
      const fallbackTitle = mediaType === "playlist" ? this._i18n("ui.playlist") : mediaType === "artist" ? this._i18n("ui.artist") : this._i18n("ui.album");
      this._setMobileMenuHeader(detail2.name || fallbackTitle, iconName, "players");
      body.innerHTML = this._libraryDetailPopupShellHtml(this._libraryMediaDetailHtml(detail2), mediaType === "artist" ? "artist-detail-page-shell" : "");
      finishMenuRender();
      return;
    }
    if (page.startsWith("library_")) {
      const meta = this._libraryTabMeta(page);
      this._setMobileMenuHeader(meta.title, meta.icon, "players");
      if (page === "library_liked") {
        if (this._useMaLikedMode()) await this._loadMaLikedEntries(true);
        if (!isCurrentRender()) return;
        const likedEntries = this._likedEntries();
        const likedUriSet = new Set(likedEntries.map((entry) => String(entry?.uri || "").trim()).filter(Boolean));
        this._state.likedSelectedUris = (Array.isArray(this._state.likedSelectedUris) ? this._state.likedSelectedUris : []).filter((uri) => likedUriSet.has(String(uri || "").trim()));
        const likedQuery = this._libraryTabSearchQuery(page);
        const filteredLikedEntries = likedQuery ? this._filterLibraryItemsByQuery(likedEntries, likedQuery) : likedEntries;
        body.innerHTML = this._libraryShellHtml(`${this._mediaLayoutToolbarHtml()}${this._likedMediaEntriesHtml(filteredLikedEntries)}`, page);
        this._applyMenuLibraryThemeFromItems(menu, filteredLikedEntries.length ? filteredLikedEntries : likedEntries, "track");
        finishMenuRender();
        this._restoreLibraryTabSearchFocus();
        return;
      }
      if (page === "library_search") {
        body.innerHTML = this._libraryShellHtml(this._librarySearchHomeHtml(), page);
        this._bindMobileMediaSearch();
        await this._renderMobileMediaResults();
        if (!isCurrentRender()) return;
        finishMenuRender();
        return;
      }
      const limitMap = { playlist: 250, artist: 250, album: 250, track: 350, radio: 200, podcast: 250 };
      const orderBy = this._mobileLibraryOrderBy();
      const limit = limitMap[meta.type] || 250;
      const tabSearchQuery = this._libraryTabSearchQuery(page);
      const cacheKey = tabSearchQuery ? `tab-search:${meta.type}:${orderBy}:${limit}:${tabSearchQuery.toLowerCase()}` : `${meta.type}:${orderBy}:${limit}:false`;
      const ttl = Number(this._config.cache_ttl || 3e5);
      const cachedLibrary = this._cache.library.get(cacheKey);
      if (!cachedLibrary || Date.now() - cachedLibrary.ts >= ttl) {
        body.innerHTML = this._loadingStateHtml(this._i18n("ui.loading"), { notice: true });
      }
      let items = [];
      let libraryError = "";
      try {
        items = await this._getLibraryTabItems(meta, orderBy, limit, tabSearchQuery);
      } catch (error) {
        if (page !== "library_radio") throw error;
        libraryError = error?.message || this._m("Could not load Music Assistant radio.", "לא ניתן לטעון רדיו מ-Music Assistant.");
      }
      if (!isCurrentRender()) return;
      const sortedItems = this._sortLibraryItemsLocally(items);
      let libraryThemeItems = sortedItems;
      if (tabSearchQuery && page !== "library_radio") {
        const searchContent = (providerItems2 = [], options = {}) => `
          ${this._mediaLayoutToolbarHtml()}
          ${this._libraryTabSearchResultsHtml(sortedItems, providerItems2, meta, options)}
        `;
        body.innerHTML = this._libraryShellHtml(searchContent([], { providerLoading: true }), page);
        this._applyMenuLibraryThemeFromItems(menu, sortedItems, meta.type);
        finishMenuRender();
        this._restoreLibraryTabSearchFocus();
        let providerItems = [];
        let providerError = "";
        try {
          const rawProviderItems = await this._searchLibraryTabProviderItems(meta, tabSearchQuery, 60);
          providerItems = this._excludeLibraryDuplicateItems(rawProviderItems, sortedItems, meta.type);
          if (!providerItems.length && !sortedItems.length) providerError = this._i18n("ui.no_results_found");
        } catch (error) {
          if (!sortedItems.length) providerError = error?.message || this._i18n("ui.search_failed");
        }
        if (!isCurrentRender()) return;
        libraryThemeItems = sortedItems.length ? sortedItems : providerItems;
        body.innerHTML = this._libraryShellHtml(searchContent(providerItems, { providerError }), page);
        this._applyMenuLibraryThemeFromItems(menu, libraryThemeItems, meta.type);
        finishMenuRender();
        this._restoreLibraryTabSearchFocus();
        return;
      }
      let content = `${this._mediaLayoutToolbarHtml()}${this._mediaItemsListHtml(sortedItems, meta.type, { layout: "grid", librarySkin: true })}`;
      if (page === "library_radio") {
        const maRadioHtml = libraryError ? `<div class="notice open">${this._esc(libraryError)}</div>` : this._mediaItemsListHtml(sortedItems, meta.type, { layout: "grid", librarySkin: true });
        if (tabSearchQuery) {
          try {
            const configuredCountry = this._mobileRadioBrowserCountry();
            const browserStations = await this._fetchRadioBrowserStations(tabSearchQuery, 80, { countryCode: configuredCountry || "all" });
            if (!libraryThemeItems.length && browserStations.length) libraryThemeItems = browserStations;
            if (isCurrentRender()) {
              content = `
                ${this._mediaLayoutToolbarHtml()}
                <div class="media-results">
                  <div>
                    <div class="media-section-title">Radio Browser</div>
                    ${this._mediaItemsListHtml(browserStations, meta.type, { layout: "grid", librarySkin: true })}
                  </div>
                  <div>
                    <div class="media-section-title">Music Assistant</div>
                    ${maRadioHtml}
                  </div>
                </div>
              `;
            }
          } catch (error) {
            content = `
              ${this._mediaLayoutToolbarHtml()}
              <div class="media-results">
                <div>
                  <div class="media-section-title">Radio Browser</div>
                  <div class="notice open">${this._esc(error?.message || this._i18n("ui.no_radio_stations_found"))}</div>
                </div>
                <div>
                  <div class="media-section-title">Music Assistant</div>
                  ${maRadioHtml}
                </div>
              </div>
            `;
          }
        } else {
          try {
            const configuredCountry = this._mobileRadioBrowserCountry();
            const browseCountry = this._state.mobileRadioBrowseCountry || (configuredCountry === "all" ? "" : configuredCountry);
            const browseLabel = this._radioBrowserCountryLabel(browseCountry);
            if (browseCountry) {
              let browserStations = [];
              let browserError = "";
              try {
                browserStations = await this._fetchRadioBrowserStations("", 80, { countryCode: browseCountry });
              } catch (error) {
                browserError = error?.message || this._i18n("ui.no_radio_stations_found");
              }
              if (!libraryThemeItems.length && browserStations.length) libraryThemeItems = browserStations;
              if (isCurrentRender()) {
                content = `
                ${this._mediaLayoutToolbarHtml()}
                <div class="media-results">
                  <div>
                    ${configuredCountry === "all" ? this._radioBrowserCountryBackHtml(browseLabel) : `<div class="media-section-title">Radio Browser · ${this._esc(browseLabel)}</div>`}
                    ${browserError ? `<div class="notice open">${this._esc(browserError)}</div>` : this._mediaItemsListHtml(browserStations, meta.type, { layout: "grid", librarySkin: true })}
                  </div>
                  <div>
                    <div class="media-section-title">Music Assistant</div>
                    ${maRadioHtml}
                  </div>
                </div>
              `;
              }
            } else {
              const [countriesResult, browserStationsResult] = await Promise.allSettled([
                this._fetchRadioBrowserCountries(260),
                this._fetchRadioBrowserStations("", 40, { countryCode: "all" })
              ]);
              const countries = countriesResult.status === "fulfilled" ? countriesResult.value : [];
              const browserStations = browserStationsResult.status === "fulfilled" ? browserStationsResult.value : [];
              const browserError = browserStationsResult.status === "rejected" ? browserStationsResult.reason?.message || this._i18n("ui.no_radio_stations_found") : "";
              const countriesError = countriesResult.status === "rejected" ? countriesResult.reason?.message || this._i18n("ui.no_radio_browser_countries_found") : "";
              if (!libraryThemeItems.length && browserStations.length) libraryThemeItems = browserStations;
              if (isCurrentRender()) {
                content = `
                ${this._mediaLayoutToolbarHtml()}
                <div class="media-results">
                  <div>
                    <div class="media-section-title">${this._esc(this._i18n("ui.worldwide_popular"))}</div>
                    ${browserError ? `<div class="notice open">${this._esc(browserError)}</div>` : this._mediaItemsListHtml(browserStations, meta.type, { layout: "grid", librarySkin: true })}
                  </div>
                  <div>
                    <div class="media-section-title">${this._esc(this._i18n("ui.radio_browser_countries"))}</div>
                    ${countriesError ? `<div class="notice open">${this._esc(countriesError)}</div>` : this._radioBrowserCountriesHtml(countries)}
                  </div>
                  <div>
                    <div class="media-section-title">Music Assistant</div>
                    ${maRadioHtml}
                  </div>
                </div>
              `;
              }
            }
          } catch (error) {
            content = `
            ${this._mediaLayoutToolbarHtml()}
            <div class="media-results">
              <div>
                <div class="media-section-title">Radio Browser</div>
                <div class="notice open">${this._esc(error?.message || this._i18n("ui.no_radio_stations_found"))}</div>
              </div>
              <div>
                <div class="media-section-title">Music Assistant</div>
                ${maRadioHtml}
              </div>
            </div>
          `;
          }
        }
      }
      if (!isCurrentRender()) return;
      body.innerHTML = this._libraryShellHtml(content, page);
      this._applyMenuLibraryThemeFromItems(menu, libraryThemeItems, meta.type);
      finishMenuRender();
      this._restoreLibraryTabSearchFocus();
      return;
    }
    aux.hidden = true;
    const titles = {
      queue: this._i18n("ui.queue_2"),
      players: this._i18n("ui.players"),
      players_active: this._i18n("ui.active_players"),
      transfer: this._i18n("ui.transfer_queue"),
      group: this._i18n("ui.group_speakers_2"),
      sleep_timer: this._i18n("ui.schedules"),
      announcements: this._i18n("ui.announcements"),
      simple_wizard: "FLOW",
      ungroup_all: this._i18n("ui.disconnect_player_groups"),
      stop_all: this._i18n("ui.stop_all_players")
    };
    this._setMobileMenuHeader(titles[page] || this._i18n("ui.menu"), this._menuPageIcon(page));
    if (page === "queue") {
      body.innerHTML = this._loadingStateHtml(this._i18n("ui.loading"), { notice: true });
      await this._ensureQueueSnapshot(true);
      if (!isCurrentRender()) return;
      const queueCount = this._getNowPlayingQueueItems().length;
      body.innerHTML = `
        <div class="queue-page-head">
          <div class="queue-page-count">
            ${this._iconSvg("queue")}
            <strong>${this._esc(String(queueCount))}</strong>
          </div>
          <button class="queue-head-transfer-btn" data-menu-nav="transfer" title="${this._esc(this._i18n("ui.transfer_queue_2"))}">
            ${this._iconSvg("repeat")}
            <span class="queue-head-transfer-label">${this._esc(this._i18n("ui.transfer_queue_3"))}</span>
          </button>
        </div>
        ${this._queueMenuHtml()}
      `;
      finishMenuRender();
      return;
    }
    if (page === "players") body.innerHTML = this._playersMenuHtml();
    else if (page === "players_active") body.innerHTML = this._playersMenuHtml({ activeOnly: true });
    else if (page === "sleep_timer") {
      body.innerHTML = this._loadingStateHtml(this._i18n("ui.loading_schedules"), { notice: true });
      await this._loadScheduledStartPlaylists();
      if (!isCurrentRender()) return;
      body.innerHTML = this._sleepTimerMenuHtml();
    } else if (page === "transfer") body.innerHTML = this._transferMenuHtml();
    else if (page === "group") body.innerHTML = this._groupMenuHtml();
    else if (page === "announcements") body.innerHTML = this._announcementsMenuHtml();
    else if (page === "ungroup_all") {
      body.innerHTML = this._loadingStateHtml(this._i18n("ui.disconnecting_player_groups"), { notice: true });
      await this._ungroupAllPlayers();
      if (!isCurrentRender()) return;
      this._closeMobileMenu();
    } else if (page === "stop_all") {
      this._closeMobileMenu();
      this._openCleanAllConfirm();
      return;
    } else {
      this._debugLog?.("warn", "[Homeii Menu] unknown mobile menu page", page);
      this._state.menuPage = "main";
      this._setMobileMenuHeader(this._i18n("ui.actions_2"), this._menuPageIcon("main"));
      body.innerHTML = this._mainMenuHtml();
    }
    finishMenuRender();
  }
  async _handleMobileMenuClick(e) {
    this._rememberMobileMenuScroll();
    if (this._shouldHoldManualFrontForContentSelection()) {
      this._refreshManualFrontPlayerHold(this._manualFrontContentHoldMs());
    }
    const scheduleFormControl = e.target.closest?.(".sheet-schedules input, .sheet-schedules select, .sheet-schedules textarea");
    if (scheduleFormControl && this._isScheduleFormControl(scheduleFormControl)) {
      this._markScheduleFormControlActive(scheduleFormControl);
      e.stopPropagation();
      return;
    }
    if (e.target.closest?.("[data-queue-move-target]")) {
      e.stopPropagation();
      return;
    }
    if (await this._handleSimpleWizardClick(e)) return;
    const artistInfoCloseBtn = e.target.closest(".artist-info-close");
    const artistInfoBackdrop = e.target.classList?.contains("artist-info-backdrop") ? e.target : null;
    if (artistInfoCloseBtn || artistInfoBackdrop) {
      e.preventDefault();
      e.stopPropagation();
      if (this._state.mobileLibraryDetail) this._state.mobileLibraryDetail.artistInfoOpen = false;
      await this._renderMobileMenu();
      return;
    }
    const artistInfoOpenBtn = e.target.closest("[data-artist-info-open]");
    if (artistInfoOpenBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(artistInfoOpenBtn);
      if (this._state.mobileLibraryDetail) this._state.mobileLibraryDetail.artistInfoOpen = true;
      await this._renderMobileMenu();
      return;
    }
    const artistRadioBtn = e.target.closest("[data-artist-radio]");
    if (artistRadioBtn) {
      e.preventDefault();
      e.stopPropagation();
      const detail = this._state.mobileLibraryDetail || {};
      const uri = String(artistRadioBtn.dataset.mediaUri || detail.uri || "").trim();
      if (!uri) return;
      const feedbackEl = this._showLibraryInteractionFeedback(artistRadioBtn, { loading: true, hold: true });
      try {
        const ok = await this._playMedia(uri, "artist", "play", {
          label: artistRadioBtn.dataset.mediaName || detail.name || this._i18n("ui.artist"),
          radioMode: true,
          silent: true,
          sourceEl: artistRadioBtn
        });
        if (ok) this._toastSuccess(this._m("Artist radio started", "רדיו אמן הופעל"));
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
      return;
    }
    const artistSearchToggleBtn = e.target.closest("[data-artist-search-toggle]");
    if (artistSearchToggleBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(artistSearchToggleBtn);
      if (this._state.mobileLibraryDetail) {
        this._state.mobileLibraryDetail.artistSearchOpen = !this._state.mobileLibraryDetail.artistSearchOpen;
      }
      await this._renderMobileMenu();
      if (this._state.mobileLibraryDetail?.artistSearchOpen) {
        requestAnimationFrame(() => this.$("artistDetailSearchInput")?.focus?.());
      }
      return;
    }
    const artistDetailSearchBtn = e.target.closest("[data-artist-detail-search]");
    if (artistDetailSearchBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(artistDetailSearchBtn);
      await this._searchArtistDetailFromInput(artistDetailSearchBtn);
      return;
    }
    const frontPinBtn = e.target.closest("[data-front-pin-player]");
    if (frontPinBtn?.dataset.frontPinPlayer) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(frontPinBtn);
      this._toggleFrontPinnedPlayer(frontPinBtn.dataset.frontPinPlayer);
      await this._renderMobileMenu();
      return;
    }
    const quickActionMoveBtn = e.target.closest("[data-setting-quick-action-move]");
    if (quickActionMoveBtn) {
      e.preventDefault();
      e.stopPropagation();
      if (quickActionMoveBtn.disabled) return;
      const item = String(quickActionMoveBtn.dataset.settingQuickActionMove || "").trim();
      const direction = Number(quickActionMoveBtn.dataset.direction || 0);
      const actions = this._mobileQuickActions().slice();
      const index = actions.indexOf(item);
      const nextIndex = index + (direction < 0 ? -1 : 1);
      if (index < 0 || nextIndex < 0 || nextIndex >= actions.length) return;
      [actions[index], actions[nextIndex]] = [actions[nextIndex], actions[index]];
      this._flashInteraction(quickActionMoveBtn);
      this._state.mobileQuickActions = actions;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const playerOrderMoveBtn = e.target.closest("[data-setting-player-order-move]");
    if (playerOrderMoveBtn) {
      e.preventDefault();
      e.stopPropagation();
      if (playerOrderMoveBtn.disabled) return;
      const item = String(playerOrderMoveBtn.dataset.settingPlayerOrderMove || "").trim();
      const direction = Number(playerOrderMoveBtn.dataset.direction || 0);
      const optionIds = this._pinnedPlayerOptionPlayers([], { includeExcluded: true }).map((player) => player.entity_id);
      const current = this._playerOrderPreferences();
      const actions = [
        ...current.filter((entityId) => optionIds.includes(entityId)),
        ...optionIds.filter((entityId) => !current.includes(entityId))
      ];
      const index = actions.indexOf(item);
      const nextIndex = index + (direction < 0 ? -1 : 1);
      if (index < 0 || nextIndex < 0 || nextIndex >= actions.length) return;
      [actions[index], actions[nextIndex]] = [actions[nextIndex], actions[index]];
      this._flashInteraction(playerOrderMoveBtn);
      this._state.playerOrderEntities = actions;
      this._state.playerSortMode = "custom";
      this._persistMobileAppearance();
      this._loadPlayers();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const announcementPresetBtn = e.target.closest("[data-announcement-preset-fill]");
    if (announcementPresetBtn) {
      e.preventDefault();
      e.stopPropagation();
      const index = Number(announcementPresetBtn.dataset.announcementPresetFill);
      const preset = (this._state.mobileAnnouncementPresets || [])[index] || "";
      this._state.mobileAnnouncementText = preset;
      const input = this.$("mobileAnnouncementText");
      if (input) input.value = preset;
      this._flashInteraction(announcementPresetBtn);
      this._hapticTap([8]);
      return;
    }
    const announcementVoiceBtn = e.target.closest("[data-announcement-voice]");
    if (announcementVoiceBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(announcementVoiceBtn);
      this._startMobileAnnouncementVoice();
      return;
    }
    const announcementSendBtn = e.target.closest("[data-announcement-send]");
    if (announcementSendBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(announcementSendBtn);
      await this._sendMobileAnnouncement();
      return;
    }
    const discoveryGenreBtn = e.target.closest("[data-discovery-genre]");
    if (discoveryGenreBtn?.dataset.discoveryGenre) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(discoveryGenreBtn);
      await this._selectDiscoveryGenre(discoveryGenreBtn.dataset.discoveryGenre);
      return;
    }
    const volumePresetBtn = e.target.closest("[data-volume-preset]");
    if (volumePresetBtn) {
      const pct = Math.max(0, Math.min(100, Number(volumePresetBtn.dataset.volumePreset) || 0));
      this._setVolume(pct / 100);
      this._closeMobileVolumePresets();
      setTimeout(() => this._syncNowPlayingUI(), 120);
      return;
    }
    const likedSelectBox = e.target.closest(".liked-select-box");
    if (likedSelectBox) {
      e.preventDefault();
      e.stopPropagation();
      const input = likedSelectBox.querySelector("input[data-liked-select-uri]");
      if (!input) return;
      input.checked = !input.checked;
      const uri = String(input.dataset.likedSelectUri || "").trim();
      const next = new Set(Array.isArray(this._state.likedSelectedUris) ? this._state.likedSelectedUris : []);
      if (input.checked) next.add(uri);
      else next.delete(uri);
      this._state.likedSelectedUris = Array.from(next);
      await this._renderMobileMenu();
      return;
    }
    const likedPlayAllBtn = e.target.closest("[data-liked-play-all]");
    if (likedPlayAllBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(likedPlayAllBtn);
      const playable = this._likedPlayableEntries(this._likedEntries(), false);
      if (!playable.length) {
        this._toastError(this._i18n("ui.no_liked_tracks_to_play"));
        return;
      }
      await this._playAll(playable, false);
      this._closeMobileMenu();
      return;
    }
    const likedSelectionToggleBtn = e.target.closest("[data-liked-selection-toggle]");
    if (likedSelectionToggleBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(likedSelectionToggleBtn);
      this._state.likedSelectionMode = !this._state.likedSelectionMode;
      if (!this._state.likedSelectionMode) this._state.likedSelectedUris = [];
      await this._renderMobileMenu();
      return;
    }
    const likedPlaySelectedBtn = e.target.closest("[data-liked-play-selected]");
    if (likedPlaySelectedBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(likedPlaySelectedBtn);
      const playable = this._likedPlayableEntries(this._likedEntries(), true);
      if (!playable.length) {
        this._toastError(this._i18n("ui.no_selected_tracks_to_play"));
        return;
      }
      await this._playAll(playable, false);
      this._state.likedSelectionMode = false;
      this._state.likedSelectedUris = [];
      this._closeMobileMenu();
      return;
    }
    const likedRemoveBtn = e.target.closest("[data-liked-remove]");
    if (likedRemoveBtn?.dataset.likedRemove) {
      e.preventDefault();
      e.stopPropagation();
      if (this._useMaLikedMode()) {
        const uri = likedRemoveBtn.dataset.likedRemove;
        const likedEntry = this._likedEntries().find((entry) => String(entry?.uri || "").trim() === String(uri || "").trim());
        if (likedEntry) await this._toggleLikeEntry(likedEntry, likedRemoveBtn);
      } else {
        this._removeLikedUri(likedRemoveBtn.dataset.likedRemove);
      }
      await this._renderMobileMenu();
      return;
    }
    const queueAction = e.target.closest("[data-queue-action]");
    if (queueAction) {
      e.preventDefault();
      e.stopPropagation();
      const queueRow2 = queueAction.closest("[data-queue-item-id]");
      const action2 = queueAction.dataset.queueAction;
      const queueItemId = queueAction.dataset.queueItemId || queueRow2?.dataset.queueItemId || "";
      if (action2 === "like") {
        const likeEntry = {
          uri: queueAction.dataset.queueUri || queueRow2?.dataset.uri || "",
          media_type: queueAction.dataset.queueType || queueRow2?.dataset.type || "track",
          name: queueAction.dataset.queueName || "",
          artist: queueAction.dataset.queueArtist || "",
          album: queueAction.dataset.queueAlbum || "",
          image: queueAction.dataset.queueImage || ""
        };
        if (!String(likeEntry.uri || "").trim()) return;
        const wasLiked = this._isEntryLiked(likeEntry);
        this._syncQueueLikeActionButton(queueAction, !wasLiked);
        const result = await this._toggleLikeEntry(likeEntry, queueAction);
        if (result === false) {
          this._syncQueueLikeActionButton(queueAction, wasLiked);
        } else {
          this._syncQueueLikeActionButton(queueAction, this._isEntryLiked(likeEntry));
        }
        await this._renderMobileMenu();
        return;
      }
      this._state.expandedQueueItemId = "";
      if (action2 === "play") {
        await this._playQueueItem(
          queueItemId,
          queueRow2?.dataset.uri || "",
          queueRow2?.dataset.type || "track",
          queueRow2?.dataset.sortIndex || ""
        );
        await this._renderMobileMenu();
        return;
      }
      const targetPosition = action2 === "move_to" ? this._queueMoveTargetFromElement(queueAction) : null;
      if (action2 === "move_to" && !targetPosition) return;
      await this._handleQueueAction(action2, queueItemId, queueRow2?.dataset.uri || "", queueRow2?.dataset.sortIndex || "", targetPosition);
      return;
    }
    const scheduleTabBtn = e.target.closest("[data-schedule-tab]");
    if (scheduleTabBtn?.dataset.scheduleTab) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      const tab = String(scheduleTabBtn.dataset.scheduleTab || "");
      this._state.mobileSchedulesTab = ["timers", "wake", "night"].includes(tab) ? tab : "timers";
      this._persistMobileAppearance();
      await this._renderMobileMenu();
      return;
    }
    const startScheduleNewBtn = e.target.closest("[data-start-schedule-new]");
    if (startScheduleNewBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startScheduleNewBtn);
      this._state.mobileSchedulesTab = "wake";
      this._newScheduledStartDraft();
      this._persistMobileAppearance();
      await this._renderMobileMenu();
      return;
    }
    const startScheduleEditBtn = e.target.closest("[data-start-schedule-edit]");
    if (startScheduleEditBtn?.dataset.startScheduleEdit) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startScheduleEditBtn);
      this._state.mobileSchedulesTab = "wake";
      this._editScheduledStart(startScheduleEditBtn.dataset.startScheduleEdit);
      this._persistMobileAppearance();
      await this._renderMobileMenu();
      return;
    }
    const startScheduleToggleBtn = e.target.closest("[data-start-schedule-toggle]");
    if (startScheduleToggleBtn?.dataset.startScheduleToggle) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startScheduleToggleBtn);
      this._toggleScheduledStart(startScheduleToggleBtn.dataset.startScheduleToggle);
      await this._renderMobileMenu();
      return;
    }
    const startScheduleDeleteBtn = e.target.closest("[data-start-schedule-delete]");
    if (startScheduleDeleteBtn?.dataset.startScheduleDelete) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startScheduleDeleteBtn);
      this._deleteScheduledStart(startScheduleDeleteBtn.dataset.startScheduleDelete);
      await this._renderMobileMenu();
      return;
    }
    const sleepTimerStartBtn = e.target.closest("[data-sleep-timer-start]");
    if (sleepTimerStartBtn?.dataset.sleepTimerStart) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(sleepTimerStartBtn);
      this._setSleepTimerMinutes(Number(sleepTimerStartBtn.dataset.sleepTimerStart || 15), "general");
      await this._renderMobileMenu();
      return;
    }
    const sleepTimerCancelBtn = e.target.closest("[data-sleep-timer-cancel]");
    if (sleepTimerCancelBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(sleepTimerCancelBtn);
      this._clearSleepTimer(true);
      await this._renderMobileMenu();
      return;
    }
    const startTimerSaveBtn = e.target.closest("[data-start-timer-save]");
    if (startTimerSaveBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startTimerSaveBtn);
      this._setScheduledStartFromMenu();
      await this._renderMobileMenu();
      return;
    }
    const startTimerClearBtn = e.target.closest("[data-start-timer-clear]");
    if (startTimerClearBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(startTimerClearBtn);
      this._clearScheduledStart(true);
      await this._renderMobileMenu();
      return;
    }
    const action = e.target.closest("[data-menu-action]");
    if (action) {
      this._flashInteraction(action);
      if (this._isHotelMode() && [
        "connect_this_device",
        "disconnect_this_device",
        "toggle_theme",
        "apply_group",
        "clear_group"
      ].includes(action.dataset.menuAction)) return;
      if (action.dataset.menuAction === "open_app") return this._openMusicAssistant();
      if (action.dataset.menuAction === "connect_this_device") return this._connectThisDevicePlayer();
      if (action.dataset.menuAction === "disconnect_this_device") return this._disconnectThisDevicePlayer();
      if (action.dataset.menuAction === "toggle_lang") return this._toggleLanguage();
      if (action.dataset.menuAction === "toggle_theme") {
        this._toggleCardTheme();
        this._rebuildMobileUi({ reopenPage: this._state.menuPage || "settings", reopenStudio: this._state.controlRoomOpen });
        return;
      }
      if (action.dataset.menuAction === "apply_group") {
        const ok = await this._runMenuButtonLoading(action, this._i18n("ui.connecting_2"), () => this._applySpeakerGroup(), { kind: "connect" });
        if (ok) return this._closeMobileMenu();
        return;
      }
      if (action.dataset.menuAction === "clear_group") {
        const ok = await this._runMenuButtonLoading(action, this._i18n("ui.disconnecting"), () => this._clearSpeakerGroup(), { kind: "disconnect" });
        if (ok) return this._closeMobileMenu();
        return;
      }
    }
    const playerMuteBtn = e.target.closest("[data-player-mute]");
    if (playerMuteBtn?.dataset.playerMute) {
      e.preventDefault();
      e.stopPropagation();
      await this._toggleMuteFor(playerMuteBtn.dataset.playerMute);
      return;
    }
    const playerFavoriteBtn = e.target.closest("[data-player-favorite]");
    if (playerFavoriteBtn?.dataset.playerFavorite) {
      e.preventDefault();
      e.stopPropagation();
      this._flashInteraction(playerFavoriteBtn);
      try {
        await this._pressFavoriteButtonEntity(playerFavoriteBtn.dataset.playerFavorite);
        this._toastSuccess(this._i18n("ui.favorite_action_sent"));
        [300, 900, 1800].forEach((delay) => setTimeout(() => this._refreshFavoriteState(true).catch(() => {
        }), delay));
      } catch (error) {
        this._toastError(this._i18n("ui.favorite_action_failed") + (error?.message ? `: ${error.message}` : ""));
      }
      return;
    }
    const groupMuteBtn = e.target.closest("[data-group-mute]");
    if (groupMuteBtn?.dataset.groupMute) {
      e.preventDefault();
      e.stopPropagation();
      await this._toggleGroupMuteFor(groupMuteBtn.dataset.groupMute);
      return;
    }
    const langBtn = e.target.closest("[data-setting-lang]");
    if (langBtn?.dataset.settingLang) {
      this._state.lang = langBtn.dataset.settingLang;
      try {
        localStorage.setItem("homeii_music_flow_lang", this._state.lang);
      } catch (_) {
      }
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const themeBtn = e.target.closest("[data-setting-theme]");
    if (themeBtn?.dataset.settingTheme) {
      this._state.cardTheme = themeBtn.dataset.settingTheme;
      try {
        localStorage.setItem("homeii_music_flow_theme", this._state.cardTheme);
      } catch (_) {
      }
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const dynamicThemeBtn = e.target.closest("[data-setting-dynamic-theme]");
    if (dynamicThemeBtn?.dataset.settingDynamicTheme) {
      this._flashInteraction(dynamicThemeBtn);
      this._state.mobileDynamicThemeMode = ["off", "auto", "strong"].includes(dynamicThemeBtn.dataset.settingDynamicTheme) ? dynamicThemeBtn.dataset.settingDynamicTheme : "auto";
      if (this._state.mobileDynamicThemeMode === "off") {
        this._state.mobileDynamicThemePalette = null;
      }
      this._persistMobileAppearance();
      this._applyDynamicThemeStyles();
      this._syncNowPlayingUI();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const performanceProfileBtn = e.target.closest("[data-setting-performance-profile]");
    if (performanceProfileBtn?.dataset.settingPerformanceProfile) {
      this._flashInteraction(performanceProfileBtn);
      const performanceProfile2 = HomeiiMobileSettingsFoundation.normalizePerformanceProfile(
        performanceProfileBtn.dataset.settingPerformanceProfile,
        false
      );
      this._state.performanceProfile = performanceProfile2;
      this._state.performanceMode = ["low", "ultra_lite"].includes(performanceProfile2);
      this._state.performanceModeLocalOverride = true;
      if (this._state.performanceMode) {
        this._state.mobileDynamicThemePalette = null;
        this._state.mobileDynamicThemeArtwork = "";
        this._state.mobileDynamicThemeArtworkUrl = "";
      }
      this._persistMobileAppearance();
      this._applyDynamicThemeStyles();
      this._applyBackgroundMotionStyles();
      this._syncNowPlayingUI();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const performanceModeBtn = e.target.closest("[data-setting-performance-mode]");
    if (performanceModeBtn?.dataset.settingPerformanceMode) {
      this._flashInteraction(performanceModeBtn);
      const performanceProfile2 = performanceModeBtn.dataset.settingPerformanceMode === "on" ? "low" : "full";
      this._state.performanceProfile = performanceProfile2;
      this._state.performanceMode = performanceProfile2 === "low";
      this._state.performanceModeLocalOverride = true;
      if (this._state.performanceMode) {
        this._state.mobileDynamicThemePalette = null;
        this._state.mobileDynamicThemeArtwork = "";
        this._state.mobileDynamicThemeArtworkUrl = "";
      }
      this._persistMobileAppearance();
      this._applyDynamicThemeStyles();
      this._applyBackgroundMotionStyles();
      this._syncNowPlayingUI();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const backgroundMotionBtn = e.target.closest("[data-setting-background-motion]");
    if (backgroundMotionBtn?.dataset.settingBackgroundMotion) {
      this._flashInteraction(backgroundMotionBtn);
      this._state.mobileBackgroundMotionMode = ["off", "subtle", "strong", "extreme"].includes(backgroundMotionBtn.dataset.settingBackgroundMotion) ? backgroundMotionBtn.dataset.settingBackgroundMotion : "subtle";
      this._persistMobileAppearance();
      this._applyBackgroundMotionStyles();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const ambientLightBtn = e.target.closest("[data-setting-ambient-light]");
    if (ambientLightBtn?.dataset.settingAmbientLight) {
      this._flashInteraction(ambientLightBtn);
      this._state.ambientLightEnabled = ambientLightBtn.dataset.settingAmbientLight === "on";
      this._persistMobileAppearance();
      this._syncAmbientLightForCurrentMedia("settings");
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const screensaverBtn = e.target.closest("[data-setting-screensaver]");
    if (screensaverBtn?.dataset.settingScreensaver) {
      this._flashInteraction(screensaverBtn);
      this._state.screensaverEnabled = screensaverBtn.dataset.settingScreensaver === "on";
      this._persistMobileAppearance();
      this._resetScreensaverTimer({ hide: true });
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const screensaverControlsBtn = e.target.closest("[data-setting-screensaver-controls]");
    if (screensaverControlsBtn?.dataset.settingScreensaverControls) {
      this._flashInteraction(screensaverControlsBtn);
      this._state.screensaverControlsEnabled = screensaverControlsBtn.dataset.settingScreensaverControls === "on";
      this._persistMobileAppearance();
      this._syncScreensaverUi();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const powerButtonBtn = e.target.closest("[data-setting-power-button]");
    if (powerButtonBtn?.dataset.settingPowerButton) {
      this._flashInteraction(powerButtonBtn);
      this._state.powerButtonEnabled = powerButtonBtn.dataset.settingPowerButton === "on";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const discoveryModeBtn = e.target.closest("[data-setting-discovery-mode]");
    if (discoveryModeBtn?.dataset.settingDiscoveryMode) {
      this._flashInteraction(discoveryModeBtn);
      this._state.discoveryModeEnabled = discoveryModeBtn.dataset.settingDiscoveryMode === "on";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const nightModeBtn = e.target.closest("[data-setting-night-mode]");
    if (nightModeBtn?.dataset.settingNightMode) {
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(nightModeBtn);
      this._state.mobileNightMode = ["off", "auto", "on"].includes(nightModeBtn.dataset.settingNightMode) ? nightModeBtn.dataset.settingNightMode : "auto";
      this._persistMobileAppearance();
      this._rebuildMobileUi({ reopenPage: this._state.menuOpen ? this._state.menuPage || "sleep_timer" : "sleep_timer", reopenStudio: this._state.controlRoomOpen });
      return;
    }
    const nightWindowSaveBtn = e.target.closest("[data-setting-night-window-save]");
    if (nightWindowSaveBtn) {
      this._state.mobileScheduleControlActiveUntil = 0;
      this._flashInteraction(nightWindowSaveBtn);
      const startInput = this.$("mobileNightStartInput");
      const endInput = this.$("mobileNightEndInput");
      const checkedDays = Array.from(this.shadowRoot?.querySelectorAll("input[data-setting-night-day]:checked") || []).map((input) => Number(input.dataset.settingNightDay)).filter((value) => Number.isInteger(value) && value >= 0 && value <= 6);
      this._state.mobileNightModeStart = this._normalizeClockTime(startInput?.value || "22:00", "22:00");
      this._state.mobileNightModeEnd = this._normalizeClockTime(endInput?.value || "06:00", "06:00");
      this._state.mobileNightModeDays = this._normalizeNightModeDays(checkedDays);
      this._persistMobileAppearance();
      this._toastSuccess(this._i18n("ui.night_schedule_updated"));
      this._build();
      this._init();
      this._openMobileMenu(this._state.menuPage || "sleep_timer");
      return;
    }
    const sleepTimerBtn = e.target.closest("[data-setting-sleep-timer]");
    if (sleepTimerBtn) {
      this._flashInteraction(sleepTimerBtn);
      this._cycleSleepTimer();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const sleepClearBtn = e.target.closest("[data-setting-sleep-clear]");
    if (sleepClearBtn) {
      this._flashInteraction(sleepClearBtn);
      this._clearSleepTimer(true);
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const compactModeBtn = e.target.closest("[data-setting-compact-mode]");
    if (compactModeBtn?.dataset.settingCompactMode) {
      this._flashInteraction(compactModeBtn);
      this._state.mobileCompactMode = compactModeBtn.dataset.settingCompactMode === "on";
      this._state.mobileCompactExpanded = false;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const mobileLayoutModeBtn = e.target.closest("[data-setting-mobile-layout-mode]");
    if (mobileLayoutModeBtn?.dataset.settingMobileLayoutMode) {
      this._flashInteraction(mobileLayoutModeBtn);
      this._state.mobileLayoutMode = HomeiiMobileSettingsFoundation.normalizeMobileLayoutMode(mobileLayoutModeBtn.dataset.settingMobileLayoutMode);
      if (!this._mobileCompactModeEnabled()) this._state.mobileCompactExpanded = false;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const compactWidgetModeBtn = e.target.closest("[data-setting-compact-widget-mode]");
    if (compactWidgetModeBtn?.dataset.settingCompactWidgetMode) {
      this._flashInteraction(compactWidgetModeBtn);
      this._state.mobileCompactWidgetMode = HomeiiMobileSettingsFoundation.normalizeMobileCompactWidgetMode(compactWidgetModeBtn.dataset.settingCompactWidgetMode);
      this._state.mobileCompactExpanded = false;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const compactEdgeToEdgeBtn = e.target.closest("[data-setting-compact-edge-to-edge]");
    if (compactEdgeToEdgeBtn?.dataset.settingCompactEdgeToEdge) {
      this._flashInteraction(compactEdgeToEdgeBtn);
      this._state.mobileCompactEdgeToEdge = compactEdgeToEdgeBtn.dataset.settingCompactEdgeToEdge === "on";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const showUpNextBtn = e.target.closest("[data-setting-show-up-next]");
    if (showUpNextBtn?.dataset.settingShowUpNext) {
      this._flashInteraction(showUpNextBtn);
      this._state.mobileShowUpNext = showUpNextBtn.dataset.settingShowUpNext === "on";
      this._persistMobileAppearance();
      this._syncMobileUpNextUi(this._mobileUpNextItem());
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const swipeModeBtn = e.target.closest("[data-setting-swipe-mode]");
    if (swipeModeBtn?.dataset.settingSwipeMode) {
      this._flashInteraction(swipeModeBtn);
      this._state.mobileSwipeMode = swipeModeBtn.dataset.settingSwipeMode === "browse" ? "browse" : "play";
      this._state.mobileArtBrowseOffset = 0;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const homeShortcutBtn = e.target.closest("[data-setting-home-shortcut]");
    if (homeShortcutBtn?.dataset.settingHomeShortcut) {
      this._flashInteraction(homeShortcutBtn);
      const enabled = homeShortcutBtn.dataset.settingHomeShortcut === "on";
      this._state.mobileHomeShortcutEnabled = enabled;
      const actions = this._mobileQuickActions().filter((action2) => action2 !== "home");
      this._state.mobileQuickActions = enabled ? ["home", ...actions] : actions;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const micModeBtn = e.target.closest("[data-setting-mic-mode]");
    if (micModeBtn?.dataset.settingMicMode) {
      this._flashInteraction(micModeBtn);
      this._state.mobileMicMode = micModeBtn.dataset.settingMicMode;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const voiceAssistantBtn = e.target.closest("[data-setting-voice-assistant]");
    if (voiceAssistantBtn?.dataset.settingVoiceAssistant) {
      this._flashInteraction(voiceAssistantBtn);
      this._state.voiceAssistantEnabled = voiceAssistantBtn.dataset.settingVoiceAssistant === "on";
      if (!this._state.voiceAssistantEnabled) this._stopVoiceAssistantRecognition();
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const voiceAssistantModeBtn = e.target.closest("[data-setting-voice-assistant-mode]");
    if (voiceAssistantModeBtn?.dataset.settingVoiceAssistantMode) {
      this._flashInteraction(voiceAssistantModeBtn);
      this._state.voiceAssistantMode = HomeiiMobileSettingsFoundation.normalizeVoiceAssistantMode(voiceAssistantModeBtn.dataset.settingVoiceAssistantMode);
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const voiceFeedbackBtn = e.target.closest("[data-setting-voice-feedback]");
    if (voiceFeedbackBtn?.dataset.settingVoiceFeedback) {
      this._flashInteraction(voiceFeedbackBtn);
      this._state.voiceAssistantSpeakFeedback = voiceFeedbackBtn.dataset.settingVoiceFeedback === "on";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const footerModeBtn = e.target.closest("[data-setting-footer-mode]");
    if (footerModeBtn?.dataset.settingFooterMode) {
      this._flashInteraction(footerModeBtn);
      this._state.mobileFooterMode = footerModeBtn.dataset.settingFooterMode;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const volumeModeBtn = e.target.closest("[data-setting-volume-mode]");
    if (volumeModeBtn?.dataset.settingVolumeMode) {
      this._flashInteraction(volumeModeBtn);
      this._state.mobileVolumeMode = volumeModeBtn.dataset.settingVolumeMode === "always" ? "always" : "button";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const volumeStepButtonsBtn = e.target.closest("[data-setting-volume-step-buttons]");
    if (volumeStepButtonsBtn?.dataset.settingVolumeStepButtons) {
      this._flashInteraction(volumeStepButtonsBtn);
      this._state.mobileVolumeStepButtonsEnabled = volumeStepButtonsBtn.dataset.settingVolumeStepButtons === "on";
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const likedModeBtn = e.target.closest("[data-setting-liked-mode]");
    if (likedModeBtn?.dataset.settingLikedMode) {
      this._flashInteraction(likedModeBtn);
      this._state.mobileLikedMode = likedModeBtn.dataset.settingLikedMode;
      this._persistMobileAppearance();
      this._cache.library.delete("liked:ma");
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const libraryTabSearchToggleBtn = e.target.closest("[data-library-tab-search-toggle]");
    if (libraryTabSearchToggleBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(libraryTabSearchToggleBtn);
      this._state.libraryTabSearchOpen = this._state.libraryTabSearchOpen !== true;
      if (this._state.libraryTabSearchOpen) this._state.libraryTabSearchFocusId = "mobileLibraryTabSearchRowInput";
      await this._renderMobileMenu();
      return;
    }
    const libraryTabSearchSubmitBtn = e.target.closest("[data-library-tab-search-submit]");
    if (libraryTabSearchSubmitBtn) {
      e.preventDefault();
      e.stopPropagation();
      const page = libraryTabSearchSubmitBtn.dataset.libraryTabSearchSubmit || this._state.menuPage;
      const input = libraryTabSearchSubmitBtn.closest(".library-toolbar-search, .library-tab-search-row")?.querySelector("[data-library-tab-search-input]");
      this._showLibraryInteractionFeedback(libraryTabSearchSubmitBtn, { loading: true, loadingMs: 700 });
      this._commitLibraryTabSearchQuery(page, input?.value ?? null);
      this._state.libraryTabSearchOpen = true;
      this._state.libraryTabSearchFocusId = input?.id || (libraryTabSearchSubmitBtn.closest(".library-tab-search-row") ? "mobileLibraryTabSearchRowInput" : "mobileLibraryTabSearchInput");
      await this._renderMobileMenu();
      return;
    }
    const libraryTabSearchClearBtn = e.target.closest("[data-library-tab-search-clear]");
    if (libraryTabSearchClearBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(libraryTabSearchClearBtn);
      this._setLibraryTabSearchQuery("", libraryTabSearchClearBtn.dataset.libraryTabSearchClear || this._state.menuPage);
      this._state.libraryTabSearchOpen = true;
      this._state.libraryTabSearchFocusId = libraryTabSearchClearBtn.closest(".library-tab-search-row") ? "mobileLibraryTabSearchRowInput" : "mobileLibraryTabSearchInput";
      await this._renderMobileMenu();
      return;
    }
    const mediaDetailLayoutBtn = e.target.closest("[data-media-detail-layout]");
    if (mediaDetailLayoutBtn?.dataset.mediaDetailLayout) {
      this._flashInteraction(mediaDetailLayoutBtn);
      this._state.mobileMediaDetailLayout = mediaDetailLayoutBtn.dataset.mediaDetailLayout === "list" ? "list" : "grid";
      await this._renderMobileMenu();
      return;
    }
    const libraryDefaultLayoutBtn = e.target.closest("[data-setting-library-default-layout]");
    if (libraryDefaultLayoutBtn?.dataset.settingLibraryDefaultLayout) {
      this._flashInteraction(libraryDefaultLayoutBtn);
      this._state.mobileLibraryDefaultLayout = HomeiiMobileSettingsFoundation.normalizeMobileLibraryDefaultLayout(
        libraryDefaultLayoutBtn.dataset.settingLibraryDefaultLayout,
        this._defaultMobileMediaLayout()
      );
      this._state.mobileMediaLayoutManual = false;
      this._state.mobileMediaLayout = this._defaultMobileMediaLayout();
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const mediaLayoutBtn = e.target.closest("[data-media-layout]");
    if (mediaLayoutBtn?.dataset.mediaLayout) {
      this._showLibraryInteractionFeedback(mediaLayoutBtn);
      this._state.mobileMediaLayout = mediaLayoutBtn.dataset.mediaLayout === "grid" ? "grid" : "list";
      this._state.mobileMediaLayoutManual = true;
      await this._renderMobileMenu();
      return;
    }
    const mediaSurpriseBtn = e.target.closest("[data-media-surprise]");
    if (mediaSurpriseBtn) {
      const feedbackEl = this._showLibraryInteractionFeedback(mediaSurpriseBtn, { loading: true, hold: true });
      try {
        await this._playRandomFromPlaylists();
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
      return;
    }
    const discoveryOrbBtn = e.target.closest(".discovery-orb[data-media-uri]");
    if (this._state.menuPage === "discovery" && discoveryOrbBtn?.dataset.mediaUri) {
      const uri = discoveryOrbBtn.dataset.mediaUri;
      if (this._state.discoveryExpandedUri !== uri || !discoveryOrbBtn.classList.contains("is-active")) {
        e.preventDefault();
        e.stopPropagation();
        this._state.discoveryExpandedUri = uri;
        this._syncDiscoveryOrbSelection(uri);
        this._showLibraryInteractionFeedback(discoveryOrbBtn);
        this._hapticTap([6]);
        return;
      }
      this._showLibraryInteractionFeedback(discoveryOrbBtn, { loading: true, loadingMs: 2800 });
      discoveryOrbBtn.classList.add("is-launching");
      discoveryOrbBtn.setAttribute("aria-busy", "true");
      this._hapticTap([10, 24, 10]);
    }
    const discoveryBlank = e.target.closest(".discovery-shell, .discovery-endless, .discovery-orb-field");
    if (this._state.menuPage === "discovery" && this._state.discoveryExpandedUri && discoveryBlank && !e.target.closest("button, a, input, textarea, select")) {
      e.preventDefault();
      e.stopPropagation();
      this._state.discoveryExpandedUri = "";
      this._syncDiscoveryOrbSelection("");
      this._hapticTap([4]);
      return;
    }
    const radioBackBtn = e.target.closest("[data-radio-countries-back]");
    if (radioBackBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(radioBackBtn);
      this._state.mobileRadioBrowseCountry = "";
      this._state.mobileRadioBrowseCountryName = "";
      await this._renderMobileMenu();
      return;
    }
    const radioCountryBtn = e.target.closest("[data-radio-country]");
    if (radioCountryBtn?.dataset.radioCountry) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(radioCountryBtn, { loading: true, loadingMs: 1200 });
      this._hapticTap([8]);
      this._state.mobileRadioBrowseCountry = radioCountryBtn.dataset.radioCountry;
      this._state.mobileRadioBrowseCountryName = radioCountryBtn.dataset.radioCountryName || radioCountryBtn.dataset.radioCountry;
      await this._renderMobileMenu();
      return;
    }
    const nav = e.target.closest("[data-menu-nav]");
    if (nav) {
      this._showLibraryInteractionFeedback(nav);
      if (nav.dataset.menuNav === "stop_all") {
        e.preventDefault();
        e.stopPropagation();
        this._openCleanAllConfirm();
        return;
      }
      return this._pushMobileMenu(nav.dataset.menuNav);
    }
    if (this._state.menuPage === "library_liked" && this._state.likedSelectionMode && (e.target.closest("[data-media-uri]") || e.target.closest("[data-media-play]") || e.target.closest("[data-media-open]")) && !e.target.closest(".liked-select-box")) {
      e.preventDefault();
      e.stopPropagation();
      return;
    }
    const mediaDetailActionBtn = e.target.closest("[data-media-detail-action]");
    if (mediaDetailActionBtn) {
      e.preventDefault();
      e.stopPropagation();
      await this._handleMediaDetailActionButton(mediaDetailActionBtn);
      return;
    }
    const mediaDetailBrowseBtn = e.target.closest("[data-media-detail-browse]");
    if (mediaDetailBrowseBtn) {
      e.preventDefault();
      e.stopPropagation();
      if (mediaDetailBrowseBtn.disabled) return;
      const feedbackEl = this._showLibraryInteractionFeedback(mediaDetailBrowseBtn, { loading: true, hold: true });
      const direction = mediaDetailBrowseBtn.dataset.mediaDetailBrowse === "prev" ? -1 : 1;
      try {
        await this._openLibraryAdjacentAlbum(direction, mediaDetailBrowseBtn);
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
      return;
    }
    const mediaLikeBtn = e.target.closest("[data-media-like]");
    if (mediaLikeBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(mediaLikeBtn);
      const scrollSnapshot = this._captureMobileMenuScroll(this._state.menuPage || "main");
      await this._toggleLikeEntry({
        uri: mediaLikeBtn.dataset.mediaLike,
        media_type: mediaLikeBtn.dataset.mediaType || "album",
        name: mediaLikeBtn.dataset.mediaName || "",
        artist: mediaLikeBtn.dataset.mediaArtist || "",
        album: mediaLikeBtn.dataset.mediaAlbum || "",
        image: mediaLikeBtn.dataset.mediaImage || ""
      }, mediaLikeBtn);
      await this._renderMobileMenu();
      if (scrollSnapshot) this._restoreMobileMenuScrollSnapshot(scrollSnapshot, scrollSnapshot.page);
      return;
    }
    const mediaMoreBtn = e.target.closest("[data-media-more]");
    if (mediaMoreBtn) {
      e.preventDefault();
      e.stopPropagation();
      this._showLibraryInteractionFeedback(mediaMoreBtn);
      this._hapticTap([10]);
      this._openMobileMediaActionMenu({
        uri: mediaMoreBtn.dataset.mediaMore,
        media_type: mediaMoreBtn.dataset.mediaType || "album",
        name: mediaMoreBtn.dataset.mediaName || "",
        artist: mediaMoreBtn.dataset.mediaArtist || "",
        album: mediaMoreBtn.dataset.mediaAlbum || "",
        image: mediaMoreBtn.dataset.mediaImage || ""
      });
      return;
    }
    const mediaPlayBtn = e.target.closest("[data-media-play]");
    if (mediaPlayBtn?.dataset.mediaPlay) {
      e.preventDefault();
      e.stopPropagation();
      const sourceEl = mediaPlayBtn.closest(".media-entry, .media-detail-hero") || mediaPlayBtn;
      const feedbackEl = this._showLibraryInteractionFeedback(sourceEl, { loading: true, hold: true });
      const label = mediaPlayBtn.dataset.mediaName || sourceEl.querySelector?.(".menu-item-title, .media-detail-title")?.textContent?.trim() || "";
      try {
        const played = await this._playMedia(mediaPlayBtn.dataset.mediaPlay, mediaPlayBtn.dataset.mediaType || "album", "play", {
          label,
          sourceEl,
          forceRadioHero: this._state.menuPage === "library_radio"
        });
        if (played) return this._closeMobileMenu();
        await this._renderMobileMenu();
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
      return;
    }
    const mediaOpenBtn = e.target.closest("[data-media-open]");
    if (mediaOpenBtn?.dataset.mediaOpen) {
      e.preventDefault();
      e.stopPropagation();
      const sourceEl = mediaOpenBtn.closest(".media-entry") || mediaOpenBtn;
      const feedbackEl = this._showLibraryInteractionFeedback(sourceEl, { loading: true, loadingMs: 1600 });
      const opened = this._openLibraryMediaDetail({
        uri: mediaOpenBtn.dataset.mediaOpen,
        media_type: mediaOpenBtn.dataset.mediaType || "album",
        name: mediaOpenBtn.dataset.mediaName || sourceEl.querySelector?.(".menu-item-title")?.textContent?.trim() || "",
        artist: mediaOpenBtn.dataset.mediaArtist || "",
        album: mediaOpenBtn.dataset.mediaAlbum || "",
        image: mediaOpenBtn.dataset.mediaImage || ""
      }, sourceEl);
      if (!opened) this._clearLibraryInteractionFeedback(feedbackEl);
      if (opened) return;
    }
    const mediaBtn = e.target.closest("[data-media-uri]");
    if (mediaBtn?.dataset.mediaUri) {
      e.preventDefault();
      e.stopPropagation();
      const sourceEl = mediaBtn.closest(".media-entry") || mediaBtn;
      const feedbackEl = this._showLibraryInteractionFeedback(sourceEl, { loading: true, hold: true });
      const label = mediaBtn.dataset.mediaName || mediaBtn.querySelector(".menu-item-title, .discovery-orb-title")?.textContent?.trim() || "";
      try {
        const played = await this._playMedia(mediaBtn.dataset.mediaUri, mediaBtn.dataset.mediaType || "album", "play", {
          label,
          sourceEl,
          forceRadioHero: this._state.menuPage === "library_radio"
        });
        if (played) return this._closeMobileMenu();
        await this._renderMobileMenu();
      } finally {
        this._clearLibraryInteractionFeedback(feedbackEl);
      }
      return;
    }
    const playerBtn = e.target.closest("[data-menu-player]");
    if (playerBtn) {
      this._selectPlayer(playerBtn.dataset.menuPlayer, true);
      this._toast(this._i18n("ui.player_selected"));
      const previousPage = this._state.menuStack[this._state.menuStack.length - 1];
      if (String(previousPage || "").startsWith("library_") || previousPage === "media_detail" || previousPage === "discovery") {
        this._state.menuPage = this._state.menuStack.pop();
        await this._renderMobileMenu();
        return;
      }
      return this._closeMobileMenu();
    }
    const queueMenuBtn = e.target.closest("[data-queue-menu]");
    if (queueMenuBtn) {
      e.preventDefault();
      e.stopPropagation();
      const key = String(queueMenuBtn.dataset.queueMenu || "");
      this._setQueueInlineActionsExpanded(this._state.expandedQueueItemId === key ? "" : key);
      return;
    }
    if (e.target.closest(".queue-inline-actions")) {
      e.stopPropagation();
      return;
    }
    const transferBtn = e.target.closest("[data-menu-transfer]");
    if (transferBtn) {
      await this._transferQueueTo(transferBtn.dataset.menuTransfer);
      return this._closeMobileMenu();
    }
    const queueRow = e.target.closest(".queue-row");
    if (queueRow?.dataset.queueItemId || queueRow?.dataset.uri) {
      await this._playQueueItem(
        queueRow.dataset.queueItemId,
        queueRow.dataset.uri,
        queueRow.dataset.type || "track",
        queueRow.dataset.sortIndex || ""
      );
      this._closeMobileMenu();
      return;
    }
  }
  async _handleMobileMenuKeydown(e) {
    const libraryTabSearchInput = e.target?.closest?.("[data-library-tab-search-input]");
    if (!libraryTabSearchInput || e.key !== "Enter") return;
    e.preventDefault();
    e.stopPropagation();
    const page = libraryTabSearchInput.dataset.libraryTabSearchInput || this._state.menuPage;
    this._commitLibraryTabSearchQuery(page, libraryTabSearchInput.value || "");
    this._state.libraryTabSearchOpen = true;
    this._state.libraryTabSearchFocusId = libraryTabSearchInput.id || "";
    await this._renderMobileMenu();
  }
  async _handleMobileMenuChange(e) {
    const libraryTabSearchInput = e.target?.closest?.("[data-library-tab-search-input]");
    if (libraryTabSearchInput) {
      const page = libraryTabSearchInput.dataset.libraryTabSearchInput || this._state.menuPage;
      this._setLibraryTabSearchDraft(libraryTabSearchInput.value || "", page);
      this._state.libraryTabSearchOpen = true;
      this._state.libraryTabSearchFocusId = libraryTabSearchInput.id || "";
      const wrap = libraryTabSearchInput.closest(".library-toolbar-search, .library-tab-search-row");
      const clearBtn = wrap?.querySelector("[data-library-tab-search-clear]");
      clearBtn?.classList?.toggle("visible", !!String(libraryTabSearchInput.value || "").trim() || !!this._libraryTabSearchQuery(page));
      return;
    }
    if (this._isScheduleFormControl(e.target)) this._markScheduleFormControlActive(e.target);
    if (e.type === "input" && e.target?.matches?.('input[type="checkbox"]')) return;
    if (e.target?.matches?.("[data-queue-move-auto]")) {
      await this._handleQueueMoveAutoChange(e);
      return;
    }
    if (e.target?.matches?.("[data-queue-move-target]")) return;
    this._rememberMobileMenuScroll();
    if (e.target?.id === "simpleWizardQueryInput") {
      const state = this._state.simpleWizard && typeof this._state.simpleWizard === "object" ? this._state.simpleWizard : this._createSimpleWizardState();
      state.query = e.target.value || "";
      this._state.simpleWizard = state;
      return;
    }
    if (e.target?.id === "simpleWizardGenreSelect") {
      const state = this._state.simpleWizard && typeof this._state.simpleWizard === "object" ? this._state.simpleWizard : this._createSimpleWizardState();
      state.genre = this._simpleWizardGenres().some((genre) => genre.id === e.target.value) ? e.target.value : "pop";
      state.candidates = [];
      state.selectedIndex = 0;
      this._state.simpleWizard = state;
      await this._renderMobileMenu();
      return;
    }
    if (e.target?.id === "simpleWizardCustomGenreInput") {
      const state = this._state.simpleWizard && typeof this._state.simpleWizard === "object" ? this._state.simpleWizard : this._createSimpleWizardState();
      state.customGenre = e.target.value || "";
      this._state.simpleWizard = state;
      return;
    }
    if (e.target?.id === "scheduledStartTimeInput") {
      if (e.target.value) this._state.mobileStartTimerTime = this._normalizeClockTime(e.target.value, this._state.mobileStartTimerTime || "07:00");
      return;
    }
    if (e.target?.id === "scheduledStartPlayerSelect") {
      this._state.mobileStartTimerPlayer = String(e.target.value || "").trim();
      return;
    }
    if (e.target?.id === "scheduledStartPlaylistSelect") {
      const playlist = String(e.target.value || "").trim();
      this._state.mobileStartTimerPlaylist = playlist;
      this._state.mobileStartTimerPlaylistName = playlist ? String(e.target.selectedOptions?.[0]?.textContent || "").trim() : "";
      return;
    }
    if (e.target?.id === "scheduledStartAfterRunSelect") {
      this._state.mobileStartTimerAfterRun = String(e.target.value || "keep") === "disable" ? "disable" : "keep";
      return;
    }
    const startDayCheckbox = e.target?.closest?.("input[data-start-timer-day]");
    if (startDayCheckbox) {
      this._state.mobileStartTimerDays = this._normalizeNightModeDays(
        Array.from(this.shadowRoot?.querySelectorAll("input[data-start-timer-day]:checked") || []).map((input) => Number(input.dataset.startTimerDay)).filter((value) => Number.isInteger(value) && value >= 0 && value <= 6)
      );
      return;
    }
    const nightDayCheckbox = e.target?.closest?.("input[data-setting-night-day]");
    if (nightDayCheckbox) {
      this._state.mobileNightModeDays = this._normalizeNightModeDays(
        Array.from(this.shadowRoot?.querySelectorAll("input[data-setting-night-day]:checked") || []).map((input) => Number(input.dataset.settingNightDay)).filter((value) => Number.isInteger(value) && value >= 0 && value <= 6)
      );
      return;
    }
    if (e.target?.id === "mobileAnnouncementText") {
      this._state.mobileAnnouncementText = e.target.value || "";
      return;
    }
    if (e.target?.id === "mobileAnnouncementTargetSelect") {
      this._state.mobileAnnouncementTarget = e.target.value || "";
      return;
    }
    if (e.target?.id === "mobileAnnouncementVolumeInput") {
      const pct = Math.max(20, Math.min(50, Number(e.target.value || 20)));
      this._state.mobileAnnouncementVolume = pct;
      const valueEl = e.target.closest(".announcement-volume-field")?.querySelector(".settings-value");
      if (valueEl) valueEl.textContent = `+${pct}%`;
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.dataset?.announcementPresetIndex !== void 0) {
      const index = Number(e.target.dataset.announcementPresetIndex);
      if (Number.isFinite(index)) {
        const presets = Array.isArray(this._state.mobileAnnouncementPresets) ? [...this._state.mobileAnnouncementPresets] : ["", "", ""];
        presets[index] = e.target.value || "";
        this._state.mobileAnnouncementPresets = presets.slice(0, 3);
        this._persistMobileAppearance();
      }
      return;
    }
    if (e.target?.id === "mobileAnnouncementTtsEntity") {
      this._state.mobileAnnouncementTtsEntity = e.target.value || "";
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "mobileLanguageSelect") {
      this._state.lang = e.target.value || "en";
      try {
        localStorage.setItem("homeii_music_flow_lang", this._state.lang);
      } catch (_) {
      }
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    if (e.target?.id === "mobileAnnouncementTtsLanguageSelect") {
      this._state.mobileAnnouncementTtsLanguage = this._normalizeAnnouncementLanguage(e.target.value || "auto");
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "voiceAssistantAgentSelect") {
      this._state.voiceAssistantAgentId = String(e.target.value || "").trim();
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "ambientLightEntitiesInput") {
      this._state.ambientLightEntities = HomeiiMobileSettingsFoundation.normalizeEntityList(e.target.value || "");
      this._persistMobileAppearance();
      this._syncAmbientLightForCurrentMedia("settings");
      return;
    }
    if (e.target?.id === "ambientLightPlayerMapInput") {
      this._state.ambientLightPlayerMap = HomeiiMobileSettingsFoundation.normalizeStringArray(
        String(e.target.value || "").split(/[\n,]+/)
      );
      this._persistMobileAppearance();
      this._syncAmbientLightForCurrentMedia("settings");
      return;
    }
    if (e.target?.id === "ambientLightBrightnessInput") {
      const pct = HomeiiMobileSettingsFoundation.clampPercent(e.target.value || 35, 35, { min: 1, max: 100 });
      this._state.ambientLightBrightness = pct;
      const valueEl = e.target.closest(".settings-range")?.querySelector(".settings-value");
      if (valueEl) valueEl.textContent = `${Math.round(pct)}%`;
      this._persistMobileAppearance();
      this._syncAmbientLightForCurrentMedia("settings");
      return;
    }
    if (e.target?.id === "ambientLightTransitionInput") {
      this._state.ambientLightTransition = HomeiiMobileSettingsFoundation.clampSeconds(e.target.value || 3, 3, { min: 0, max: 120 });
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "ambientLightCooldownInput") {
      this._state.ambientLightCooldown = HomeiiMobileSettingsFoundation.clampSeconds(e.target.value || 8, 8, { min: 0, max: 120 });
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "screensaverClockModeSelect") {
      this._state.screensaverClockMode = HomeiiMobileSettingsFoundation.normalizeScreensaverClockMode(e.target.value || "digital");
      this._persistMobileAppearance();
      this._syncScreensaverUi();
      return;
    }
    if (e.target?.id === "screensaverMessageInput") {
      this._state.screensaverMessage = e.target.value || "";
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "screensaverTimeoutInput") {
      const seconds = HomeiiMobileSettingsFoundation.clampSeconds(e.target.value || 90, 90, { min: 15, max: 3600 });
      this._state.screensaverTimeoutSeconds = seconds;
      const valueEl = e.target.closest(".settings-range")?.querySelector(".settings-value");
      if (valueEl) valueEl.textContent = `${Math.round(seconds)}s`;
      this._persistMobileAppearance();
      this._resetScreensaverTimer({ hide: true });
      return;
    }
    if (e.target?.id === "powerButtonActionSelect") {
      this._state.powerButtonAction = HomeiiMobileSettingsFoundation.normalizePowerButtonAction(e.target.value || "stop_player");
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "powerButtonNameInput") {
      this._state.powerButtonName = String(e.target.value || "").trim();
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "powerButtonIconSelect") {
      this._state.powerButtonIcon = HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(e.target.value || "power");
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    if (e.target?.id === "powerButtonEntityInput") {
      this._state.powerButtonEntity = String(e.target.value || "").trim();
      this._persistMobileAppearance();
      return;
    }
    const auxButtonToggle = e.target?.closest?.("[data-setting-aux-button-enabled]");
    if (auxButtonToggle?.dataset?.settingAuxButton) {
      const index = Math.max(2, Math.min(4, Number(auxButtonToggle.dataset.settingAuxButton) || 2));
      const next = this._auxiliaryButtonConfigs().slice(1);
      const offset = index - 2;
      next[offset] = { ...next[offset] || {}, enabled: auxButtonToggle.dataset.settingAuxButtonEnabled === "on" };
      this._state.auxiliaryButtons = next;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const auxNameInput = e.target?.closest?.("[data-aux-button-name]");
    if (auxNameInput) {
      const index = Math.max(2, Math.min(4, Number(auxNameInput.dataset.auxButtonName) || 2));
      const next = this._auxiliaryButtonConfigs().slice(1);
      const offset = index - 2;
      next[offset] = { ...next[offset] || {}, name: String(auxNameInput.value || "").trim() };
      this._state.auxiliaryButtons = next;
      this._persistMobileAppearance();
      return;
    }
    const auxIconSelect = e.target?.closest?.("[data-aux-button-icon]");
    if (auxIconSelect) {
      const index = Math.max(2, Math.min(4, Number(auxIconSelect.dataset.auxButtonIcon) || 2));
      const next = this._auxiliaryButtonConfigs().slice(1);
      const offset = index - 2;
      next[offset] = { ...next[offset] || {}, icon: HomeiiMobileSettingsFoundation.normalizeAuxiliaryButtonIcon(auxIconSelect.value || "power") };
      this._state.auxiliaryButtons = next;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const auxActionSelect = e.target?.closest?.("[data-aux-button-action]");
    if (auxActionSelect) {
      const index = Math.max(2, Math.min(4, Number(auxActionSelect.dataset.auxButtonAction) || 2));
      const next = this._auxiliaryButtonConfigs().slice(1);
      const offset = index - 2;
      next[offset] = { ...next[offset] || {}, action: HomeiiMobileSettingsFoundation.normalizePowerButtonAction(auxActionSelect.value || "toggle") };
      this._state.auxiliaryButtons = next;
      this._persistMobileAppearance();
      return;
    }
    const auxEntityInput = e.target?.closest?.("[data-aux-button-entity]");
    if (auxEntityInput) {
      const index = Math.max(2, Math.min(4, Number(auxEntityInput.dataset.auxButtonEntity) || 2));
      const next = this._auxiliaryButtonConfigs().slice(1);
      const offset = index - 2;
      next[offset] = { ...next[offset] || {}, entity: String(auxEntityInput.value || "").trim() };
      this._state.auxiliaryButtons = next;
      this._persistMobileAppearance();
      return;
    }
    const checkbox = e.target.closest("input[data-menu-group-player]");
    if (checkbox) {
      const entityId = checkbox.dataset.menuGroupPlayer;
      const next = new Set(this._state.pendingGroupSelections || []);
      if (checkbox.checked) next.add(entityId);
      else next.delete(entityId);
      this._state.pendingGroupSelections = Array.from(next);
      const playerCard = checkbox.closest(".group-player-card");
      playerCard?.classList.toggle("checked", checkbox.checked);
      playerCard?.querySelector(".group-player-row")?.classList.toggle("checked", checkbox.checked);
      const toggle = playerCard?.querySelector(".group-player-toggle");
      if (toggle) {
        toggle.classList.toggle("checked", checkbox.checked);
        toggle.innerHTML = this._iconSvg(checkbox.checked ? "check" : "plus");
      }
      return;
    }
    if (e.target?.id === "mobileCustomColorPicker") {
      const color = e.target.value || "#f5a623";
      this._state.mobileCustomColor = color;
      this._persistMobileAppearance();
      this.style?.setProperty("--accent-color", color);
      this.shadowRoot?.querySelector(".card")?.style?.setProperty("--accent-color", color);
      this.shadowRoot?.querySelector(".card")?.style?.setProperty("--ma-accent", color);
      this._applyDynamicThemeStyles();
      const valueEl = e.target.closest(".settings-color-row")?.querySelector(".settings-value");
      if (valueEl) valueEl.textContent = String(color).toUpperCase();
      return;
    }
    if (e.target?.id === "mobileFontScaleRange") {
      this._state.mobileFontScale = Math.max(0.5, Math.min(1.5, Number(e.target.value || 1) || 1));
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    if (e.target?.id === "mobileIconScaleRange") {
      this._state.mobileIconScale = HomeiiMobileSettingsFoundation.clampMobileIconScale(e.target.value || 1);
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    if (e.target?.id === "mobileVolumeStepRange") {
      this._state.mobileVolumeStepPercent = HomeiiMobileSettingsFoundation.clampMobileVolumeStepPercent(e.target.value || 5);
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    if (e.target?.id === "scheduledStartVolumeInput") {
      const pct = Math.max(0, Math.min(100, Number(e.target.value || 0)));
      this._state.mobileStartTimerVolume = pct;
      const valueEl = e.target.closest(".scheduled-volume-field")?.querySelector(".settings-value");
      if (valueEl) valueEl.textContent = `${pct}%`;
      return;
    }
    if (e.target?.id === "mobileNightStartInput" || e.target?.id === "mobileNightEndInput") {
      if (e.target.value) {
        if (e.target.id === "mobileNightStartInput") {
          this._state.mobileNightModeStart = this._normalizeClockTime(e.target.value, this._state.mobileNightModeStart || "22:00");
        } else {
          this._state.mobileNightModeEnd = this._normalizeClockTime(e.target.value, this._state.mobileNightModeEnd || "06:00");
        }
      }
      return;
    }
    if (e.target?.dataset?.playerVolume) {
      const pct = Math.max(0, Math.min(100, Number(e.target.value || 0)));
      e.target.style.setProperty("--vol-pct", `${pct}%`);
      this._setPlayerVolumeFor(e.target.dataset.playerVolume, pct / 100);
      return;
    }
    if (e.target?.dataset?.groupVolume) {
      const pct = Math.max(0, Math.min(100, Number(e.target.value || 0)));
      e.target.style.setProperty("--vol-pct", `${pct}%`);
      this._setGroupVolumeFor(e.target.dataset.groupVolume, pct / 100);
      return;
    }
    if (e.target?.id === "mobileLibrarySortSelect") {
      this._state.mobileLibrarySort = e.target.value || "name_asc";
      try {
        localStorage.setItem("homeii_music_flow_mobile_library_sort", this._state.mobileLibrarySort);
      } catch (_) {
      }
      this._cache.library.clear();
      this._renderMobileMenu();
      return;
    }
    if (e.target?.id === "mediaDetailAlbumSelect") {
      const index = Number(e.target.value);
      if (Number.isInteger(index)) {
        await this._openLibraryBrowseAlbumAtIndex(index, e.target);
      }
      return;
    }
    if (e.target?.id === "discoveryCategorySelect") {
      this._startDiscoverySession();
      await this._selectDiscoveryCategory(e.target.value || "");
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "mobileRadioCountrySelect") {
      this._state.mobileRadioBrowserCountry = e.target.value || "all";
      this._state.mobileRadioBrowseCountry = "";
      this._state.mobileRadioBrowseCountryName = "";
      this._persistMobileAppearance();
      this._cache.library.delete("radio-browser:countries");
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    if (e.target?.id === "mobileHomeShortcutPathInput") {
      this._state.mobileHomeShortcutPath = e.target.value || "/";
      this._persistMobileAppearance();
      return;
    }
    if (e.target?.id === "playerSortModeSelect") {
      this._state.playerSortMode = HomeiiMobileSettingsFoundation.normalizePlayerSortMode(e.target.value || "default");
      this._persistMobileAppearance();
      this._loadPlayers();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const pinnedPlayerCheckbox = e.target?.closest?.("input[data-setting-pinned-player]");
    if (pinnedPlayerCheckbox) {
      const entityId = String(pinnedPlayerCheckbox.dataset.settingPinnedPlayer || "").trim();
      const next = new Set(this._pinnedPlayerPreferences());
      if (pinnedPlayerCheckbox.checked) next.add(entityId);
      else next.delete(entityId);
      this._state.pinnedPlayerEntities = Array.from(next);
      this._persistMobileAppearance();
      this._loadPlayers();
      if (this._state.selectedPlayer && !this._resolvedPinnedPlayerEntities().includes(this._state.selectedPlayer)) {
        this._state.selectedPlayer = this._resolvedPinnedPlayerEntity() || this._state.selectedPlayer;
      }
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const excludedPlayerCheckbox = e.target?.closest?.("input[data-setting-excluded-player]");
    if (excludedPlayerCheckbox) {
      const entityId = String(excludedPlayerCheckbox.dataset.settingExcludedPlayer || "").trim();
      const next = new Set(this._excludedPlayerPreferences());
      if (excludedPlayerCheckbox.checked) next.add(entityId);
      else next.delete(entityId);
      this._state.excludedPlayerEntities = Array.from(next);
      this._persistMobileAppearance();
      this._loadPlayers();
      if (this._state.selectedPlayer && this._isPlayerExcluded(this._state.selectedPlayer)) {
        this._state.selectedPlayer = this._resolvedPinnedPlayerEntity() || this._state.players?.[0]?.entity_id || null;
      }
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const tabCheckbox = e.target?.closest?.("input[data-setting-library-tab]");
    if (tabCheckbox) {
      const tab = tabCheckbox.dataset.settingLibraryTab;
      const current = new Set(this._mobileLibraryTabs());
      if (tabCheckbox.checked) current.add(tab);
      else current.delete(tab);
      const next = Array.from(current);
      this._state.mobileLibraryTabs = next.length ? next : this._defaultMobileLibraryTabs();
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll();
      return;
    }
    const mainBarCheckbox = e.target?.closest?.("input[data-setting-main-bar-item]");
    if (mainBarCheckbox) {
      const item = mainBarCheckbox.dataset.settingMainBarItem;
      if (!this._usesVisualSettings() && item === "settings") {
        mainBarCheckbox.checked = true;
        return;
      }
      const current = new Set(this._mobileMainBarItems());
      if (mainBarCheckbox.checked) current.add(item);
      else current.delete(item);
      const next = Array.from(current);
      this._state.mobileMainBarItems = next.length ? next : this._defaultMobileMainBarItems();
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true });
      return;
    }
    const quickActionCheckbox = e.target?.closest?.("input[data-setting-quick-action]");
    if (quickActionCheckbox) {
      const item = String(quickActionCheckbox.dataset.settingQuickAction || "").trim();
      const current = new Set(this._mobileQuickActions());
      if (quickActionCheckbox.checked) current.add(item);
      else current.delete(item);
      const next = Array.from(current);
      this._state.mobileQuickActions = next;
      if (item === "home") this._state.mobileHomeShortcutEnabled = !!quickActionCheckbox.checked;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const screensaverControlCheckbox = e.target?.closest?.("input[data-setting-screensaver-control]");
    if (screensaverControlCheckbox) {
      const item = String(screensaverControlCheckbox.dataset.settingScreensaverControl || "").trim();
      const current = new Set(this._screensaverControlButtons({ includeDisabled: true }));
      if (screensaverControlCheckbox.checked) current.add(item);
      else current.delete(item);
      this._state.screensaverControlButtons = HomeiiMobileSettingsFoundation.normalizeScreensaverControlButtons(Array.from(current), []);
      this._persistMobileAppearance();
      this._syncScreensaverUi();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true, init: true });
      return;
    }
    const studioShortcutCheckbox = e.target?.closest?.("input[data-setting-studio-shortcut]");
    if (studioShortcutCheckbox) {
      this._state.mobileStudioShortcutEnabled = !!studioShortcutCheckbox.checked;
      this._persistMobileAppearance();
      this._reopenSettingsMenuPreservingScroll({ rebuild: true });
      return;
    }
  }
  async _handleMobileMediaInput(e) {
    this._state.mediaQuery = e.target.value || "";
    clearTimeout(this._searchTimer);
    this._searchTimer = setTimeout(() => {
      if (this._state.menuOpen && this._isMobileSearchPage()) this._renderMobileMediaResults();
    }, 220);
  }
}
class HomeiiMusicFlowBaseEditor extends HomeiiBaseMusicEditor {
  constructor() {
    super();
    this._config = { ...HomeiiMusicFlowBaseCard.getStubConfig(), type: "custom:homeii-music-flow" };
  }
  _getCardCtor() {
    return HomeiiMusicFlowBaseCard;
  }
  setConfig(config) {
    const nextConfig = {
      ...HomeiiMusicFlowBaseCard.getStubConfig(),
      ...config,
      type: "custom:homeii-music-flow"
    };
    const validator = this._getConfigValidator?.();
    if (typeof validator === "function") {
      validator(nextConfig);
    }
    this._config = nextConfig;
    this._render();
  }
  _getConfigValidator() {
    return HomeiiConfigValidators.validateMobileCardEditorConfig;
  }
}
class HomeiiMusicFlowCard extends HomeiiMusicFlowBaseCard {
  static getStubConfig() {
    return HomeiiMusicFlowBaseCard.getStubConfig();
  }
  static getConfigForm() {
    return HomeiiMusicFlowBaseCard.getConfigForm();
  }
  static async getConfigElement() {
    return document.createElement(HOMEII_MOBILE_EDITOR_TAG);
  }
}
class HomeiiMusicMobileCard extends HomeiiMusicFlowBaseCard {
  static async getConfigElement() {
    return document.createElement(HOMEII_MOBILE_EDITOR_TAG);
  }
}
class HomeiiMusicFlowEditor extends HomeiiMusicFlowBaseEditor {
}
class HomeiiMusicMobileEditor extends HomeiiMusicFlowBaseEditor {
}
if (!customElements.get("homeii-music-flow")) {
  customElements.define("homeii-music-flow", HomeiiMusicFlowCard);
}
if (!customElements.get("homeii-music-mobile")) {
  customElements.define("homeii-music-mobile", HomeiiMusicMobileCard);
}
if (!customElements.get(HOMEII_MOBILE_EDITOR_TAG)) {
  customElements.define(HOMEII_MOBILE_EDITOR_TAG, HomeiiMusicFlowBaseEditor);
}
if (!customElements.get("homeii-music-flow-editor")) {
  customElements.define("homeii-music-flow-editor", HomeiiMusicFlowEditor);
}
if (!customElements.get("homeii-music-mobile-editor")) {
  customElements.define("homeii-music-mobile-editor", HomeiiMusicMobileEditor);
}
function registerHomeiiDashboardCard() {
  const customCardsRegistry = Array.isArray(window.customCards) ? window.customCards : window.customCards = [];
  for (let index = customCardsRegistry.length - 1; index >= 0; index -= 1) {
    const card = customCardsRegistry[index];
    if (card?.type === "custom:homeii-music-flow" || card?.type === "homeii-music-flow") {
      customCardsRegistry.splice(index, 1);
    }
  }
  customCardsRegistry.push({
    type: "homeii-music-flow",
    name: "HOMEii Flow",
    description: `Premium Music Assistant dashboard card v${HOMEII_CARD_VERSION}`,
    preview: false,
    documentationURL: "https://github.com/r11a/homeii-music-flow"
  });
}
registerHomeiiDashboardCard();
if (typeof queueMicrotask === "function") queueMicrotask(registerHomeiiDashboardCard);
setTimeout(registerHomeiiDashboardCard, 500);
